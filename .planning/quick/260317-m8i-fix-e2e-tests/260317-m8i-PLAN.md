---
phase: quick
plan: 260317-m8i
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/conftest.py
autonomous: true
must_haves:
  truths:
    - "All 18 e2e tests pass when run with `pytest -m e2e`"
    - "Tables are created in test DB before any e2e test that needs them"
    - "Table creation is idempotent (safe to re-run)"
  artifacts:
    - path: "tests/e2e/conftest.py"
      provides: "DB table creation fixture for e2e tests"
  key_links:
    - from: "tests/e2e/conftest.py"
      to: "app/models.py"
      via: "SQLModel metadata.create_all"
      pattern: "create_all"
---

<objective>
Fix all 14 failing e2e tests. The root cause is that the `chats` and `messages` tables
do not exist in the test database when e2e tests run. The app lifespan (which creates the
TestClient) does NOT run `CREATE TABLE` -- it assumes tables already exist. The e2e conftest's
`db_session` fixture creates a raw async engine but never ensures tables are present.

Purpose: Make e2e tests runnable against a fresh/empty test database.
Output: Updated `tests/e2e/conftest.py` with table-creation logic.
</objective>

<execution_context>
@/Users/vay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/e2e/conftest.py
@app/models.py (BaseTable SQLModel with Chat + Message tables)
@app/config.py (MainConfig with app_config field, AppConfig with prompt/examples as required fields)
@app/api/main.py (lifespan references MainConfig().app_config)

<interfaces>
From app/models.py:
```python
class BaseTable(SQLModel):
    pass

class Chat(BaseTable, table=True):
    __tablename__ = "chats"
    # ...

class Message(BaseTable, table=True):
    __tablename__ = "messages"
    # ...
```

BaseTable.metadata contains both tables. Use `SQLModel.metadata.create_all(engine)` with
the sync engine from the async engine to create tables if they don't exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add table-creation fixture to e2e conftest</name>
  <files>tests/e2e/conftest.py</files>
  <action>
Add a session-scoped fixture `ensure_tables` that runs once at the start of the e2e test
session and creates all SQLModel tables (CREATE TABLE IF NOT EXISTS) in the test database.

Implementation:
1. Import `SQLModel` from `sqlmodel`.
2. Import `create_engine` from `sqlalchemy` (sync engine for metadata.create_all).
3. Create a session-scoped fixture `ensure_tables` that:
   - Builds a SYNC database URL from the same DB_* env vars (use `postgresql://` not `postgresql+asyncpg://`).
   - Calls `SQLModel.metadata.create_all(sync_engine)` which issues CREATE TABLE IF NOT EXISTS.
   - Disposes the sync engine afterward.
4. Make `db_engine` fixture depend on `ensure_tables` so tables are guaranteed to exist
   before any async engine usage.
5. Also make `real_client` depend on `ensure_tables` so that even tests that don't use
   db_session directly (like test_chats.py, test_flows.py which go through the API) have
   tables present when the app lifespan starts the TestClient.

Key detail: The sync URL must use `postgresql://` driver (not asyncpg). Build it from the
same `_db_url()` helper by replacing the scheme, or create a parallel `_sync_db_url()`.

Keep the opening delimiter alignment style for multiline constructs per project convention.
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker && python -m pytest tests/e2e/ -m e2e -x --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>All 18 e2e tests pass (14 previously failing + 4 already passing). No test produces "relation does not exist" errors.</done>
</task>

</tasks>

<verification>
```bash
# All e2e tests pass
python -m pytest tests/e2e/ -m e2e --tb=short

# Unit tests still pass (no regression)
python -m pytest tests/unit/ --tb=short
```
</verification>

<success_criteria>
- `pytest -m e2e` reports 18 passed, 0 failed
- `pytest -m "not e2e"` still reports all unit tests passing
- No manual DB setup required before running e2e tests
</success_criteria>

<output>
After completion, create `.planning/quick/260317-m8i-fix-e2e-tests/260317-m8i-SUMMARY.md`
</output>
