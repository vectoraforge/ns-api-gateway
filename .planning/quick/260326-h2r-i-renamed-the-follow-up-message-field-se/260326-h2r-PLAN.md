---
phase: quick
plan: 260326-h2r
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_services.py
  - tests/unit/test_webhooks.py
autonomous: true
requirements: [quick-fix]
must_haves:
  truths:
    - "All 163 unit tests pass with zero failures"
    - "Only the pre-existing e2e failure (test_create_chat_autodetect_lang) remains"
  artifacts:
    - path: "tests/unit/test_services.py"
      provides: "Corrected assertion using Message.content attribute"
    - path: "tests/unit/test_webhooks.py"
      provides: "Corrected assertion using httpx Response.content attribute"
  key_links: []
---

<objective>
Fix two unit test assertions broken by overzealous find-and-replace during the question->message field rename (commit bdf5d45).

Purpose: The rename commit incorrectly changed `.content` to `.message` in two test files where `.content` refers to unrelated attributes (SQLModel field and httpx Response bytes), not the renamed follow-up field.
Output: All unit tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260326-h2r-i-renamed-the-follow-up-message-field-se/260326-h2r-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert two incorrect content->message renames in unit tests</name>
  <files>tests/unit/test_services.py, tests/unit/test_webhooks.py</files>
  <action>
Two assertions were incorrectly changed by the rename commit. Revert them:

1. In `tests/unit/test_services.py` line 57:
   Change `human_msg.message["context"]` back to `human_msg.content["context"]`.
   Reason: `Message.content` is the JSONB dict field on the SQLModel — it has nothing to do with the question->message rename.

2. In `tests/unit/test_webhooks.py` line 16:
   Change `response.message` back to `response.content`.
   Reason: `response` is an httpx `Response` object; `.content` returns raw bytes. httpx Response has no `.message` attribute.

No other changes needed. Do NOT touch any other files.
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker/ns-api-gateway && python -m pytest tests/unit/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All unit tests pass (163 passed, 0 failures). The two reverted assertions correctly use .content again.</done>
</task>

</tasks>

<verification>
Run full unit test suite: `python -m pytest tests/unit/ -q`
Expected: 163 passed, 0 failures.
</verification>

<success_criteria>
- `python -m pytest tests/unit/` reports all tests passing
- Only the pre-existing e2e failure (test_create_chat_autodetect_lang) remains in e2e tests
</success_criteria>

<output>
After completion, create `.planning/quick/260326-h2r-i-renamed-the-follow-up-message-field-se/260326-h2r-SUMMARY.md`
</output>
