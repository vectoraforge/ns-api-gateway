---
phase: quick
plan: 260317-lj4
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/conftest.py
  - .env.example
autonomous: true
requirements: []
must_haves:
  truths:
    - "E2e tests use the same DB_* env vars as the app (DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME)"
    - ".env.example documents all env vars needed for both app and tests"
  artifacts:
    - path: "tests/e2e/conftest.py"
      provides: "DB URL built from DB_* env vars instead of separate TEST_DATABASE_URL"
    - path: ".env.example"
      provides: "Complete env var reference including Firebase test credentials"
  key_links:
    - from: "tests/e2e/conftest.py"
      to: ".env"
      via: "pytest-dotenv loads .env, conftest reads DB_* env vars"
      pattern: "os\\.environ.*DB_"
---

<objective>
Align e2e test database configuration with the app's env vars so tests use the same DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME variables from .env (loaded by pytest-dotenv) instead of a separate TEST_DATABASE_URL with hardcoded fallback. Also update .env.example to document all required env vars.

Purpose: Single source of truth for database connection -- no drift between app config and test config.
Output: Updated tests/e2e/conftest.py and .env.example.
</objective>

<execution_context>
@/Users/vay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/config.py (DatabaseConfig with DB_* env vars and .url property)
@tests/e2e/conftest.py (current hardcoded TEST_DATABASE_URL fallback)
@pyproject.toml (pytest-dotenv: env_files = [".env"])
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace TEST_DATABASE_URL with DB_* env vars in e2e conftest</name>
  <files>tests/e2e/conftest.py</files>
  <action>
Replace the `DB_URL` construction (lines 50-52) that uses `os.environ.get("TEST_DATABASE_URL", "postgresql+asyncpg://...")` with a helper that builds the URL from the same env vars the app uses:

```python
def _db_url() -> str:
    user = os.environ.get("DB_USER", "postgres")
    password = os.environ.get("DB_PASSWORD", "postgres")
    host = os.environ.get("DB_HOST", "localhost")
    port = os.environ.get("DB_PORT", "5432")
    name = os.environ.get("DB_NAME", "nativespeaker")
    return f"postgresql+asyncpg://{user}:{password}@{host}:{port}/{name}"
```

Then replace `DB_URL` usage in `db_engine` fixture with `_db_url()`. The defaults match DatabaseConfig defaults so this is safe. The env vars are loaded from .env by pytest-dotenv before conftest runs.

Remove the `TEST_DATABASE_URL` variable entirely.
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker && python -m pytest tests/unit/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>DB_URL in e2e conftest is built from DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME env vars. No TEST_DATABASE_URL reference remains.</done>
</task>

<task type="auto">
  <name>Task 2: Update .env.example with all required env vars</name>
  <files>.env.example</files>
  <action>
Update .env.example to include all env vars needed by both the app and tests. Add a "# Tests (e2e)" section with the Firebase test credentials. Keep existing vars. Final content:

```
# App
CONFIG_DIR=config/
OPENAI_API_KEY=...
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=nativespeaker

# Tests (e2e)
FIREBASE_API_KEY=...
FIREBASE_TEST_EMAIL=...
FIREBASE_TEST_PASSWORD=...
```

This documents every env var so a new developer knows what to fill in.
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker && grep -c "FIREBASE_API_KEY\|FIREBASE_TEST_EMAIL\|FIREBASE_TEST_PASSWORD\|DB_HOST\|OPENAI_API_KEY" .env.example | xargs test 5 -eq</automated>
  </verify>
  <done>.env.example contains all app env vars (CONFIG_DIR, OPENAI_API_KEY, DB_*) and all test env vars (FIREBASE_*).</done>
</task>

</tasks>

<verification>
- `grep -r "TEST_DATABASE_URL" tests/` returns no matches
- `python -m pytest tests/unit/ -x -q` passes (no import breakage)
- `.env.example` contains DB_*, OPENAI_API_KEY, CONFIG_DIR, FIREBASE_API_KEY, FIREBASE_TEST_EMAIL, FIREBASE_TEST_PASSWORD
</verification>

<success_criteria>
- E2e conftest builds DB URL from DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME (same as app)
- No TEST_DATABASE_URL anywhere in codebase
- .env.example documents all env vars for app and tests
</success_criteria>

<output>
After completion, create `.planning/quick/260317-lj4-use-the-same-env-variables-in-tests-as-i/260317-lj4-SUMMARY.md`
</output>
