---
phase: 16-update-tests
plan: 04
type: execute
wave: 3
depends_on: ["16-02", "16-03"]
files_modified:
  - tests/integration/conftest.py
  - tests/integration/test_cross_user_isolation.py
  - tests/integration/test_health_endpoints.py
  - tests/integration/test_prompts_endpoints.py
  - tests/integration/test_root_endpoint.py
  - tests/llm/test_real_llm.py
  - tests/jwt_helpers.py
  - pyproject.toml
autonomous: true
requirements:
  - E2E-CLEANUP

must_haves:
  truths:
    - "tests/integration/ directory no longer exists"
    - "tests/llm/ directory no longer exists"
    - "tests/jwt_helpers.py no longer exists"
    - "Unit tests still pass (not broken by removal)"
    - "pyproject.toml addopts excludes both llm and db markers"
  artifacts: []
  key_links: []
---

<objective>
Remove old test directories and files that have been replaced by e2e tests.

Purpose: Clean up the test tree by removing tests/integration/, tests/llm/, and tests/jwt_helpers.py. These are fully replaced by tests/e2e/ (Plans 02-03). Also verify pyproject.toml marker config is correct.
Output: Clean test directory with only tests/unit/ and tests/e2e/ subdirectories.
</objective>

<execution_context>
@/Users/vay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-update-tests-to-reflect-the-current-codebase-replace-llm-test-real-llm-py-with-more-general-end-to-end-tests-checking-every-endpoint-use-pytest-mark-to-keep-these-tests-separate/16-01-SUMMARY.md
@.planning/phases/16-update-tests-to-reflect-the-current-codebase-replace-llm-test-real-llm-py-with-more-general-end-to-end-tests-checking-every-endpoint-use-pytest-mark-to-keep-these-tests-separate/16-02-SUMMARY.md
@.planning/phases/16-update-tests-to-reflect-the-current-codebase-replace-llm-test-real-llm-py-with-more-general-end-to-end-tests-checking-every-endpoint-use-pytest-mark-to-keep-these-tests-separate/16-03-SUMMARY.md

@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old test directories and verify</name>
  <read_first>
    - pyproject.toml
    - tests/conftest.py
  </read_first>
  <files>tests/integration/ (delete), tests/llm/ (delete), tests/jwt_helpers.py (delete)</files>
  <action>
1. Delete the entire `tests/integration/` directory:
   ```bash
   rm -rf tests/integration/
   ```
   This removes: conftest.py, test_cross_user_isolation.py, test_health_endpoints.py, test_prompts_endpoints.py, test_root_endpoint.py, and any __pycache__ dirs.

2. Delete the entire `tests/llm/` directory:
   ```bash
   rm -rf tests/llm/
   ```
   This removes: test_real_llm.py and any __pycache__ dirs.

3. Delete `tests/jwt_helpers.py`:
   ```bash
   rm tests/jwt_helpers.py
   ```
   This file's `_FixedKeyVerifier` and `make_token` are only used by `tests/unit/test_jwt_security.py` which has its own fixtures. Verify by checking: `grep -r 'jwt_helpers' tests/` should return nothing after deletion (the only import was in test_jwt_security.py's own fixture, but check first).

   **IMPORTANT:** Before deleting jwt_helpers.py, verify it is not imported by any remaining test file:
   ```bash
   grep -r 'jwt_helpers' tests/unit/ tests/e2e/ tests/conftest.py
   ```
   If any file imports from jwt_helpers, update that import first.

4. Verify `pyproject.toml` [tool.pytest.ini_options] already has correct config:
   - `addopts = "-v --tb=short -m 'not llm and not db'"` -- excludes both markers from default runs
   - `markers` includes both `llm` and `db`
   If addopts or markers need changes, update them. Expected current state is already correct.

5. Clean up any remaining __pycache__ dirs in deleted directories (rm -rf handles this).

6. Run unit tests to verify nothing broke:
   ```bash
   pytest tests/unit/ -x -q
   ```
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker && python -m pytest tests/unit/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/` directory does not exist: `ls tests/integration/` returns error
    - `tests/llm/` directory does not exist: `ls tests/llm/` returns error
    - `tests/jwt_helpers.py` does not exist: `ls tests/jwt_helpers.py` returns error
    - `pytest tests/unit/ -x -q` exits 0 (unit tests not broken)
    - `grep -r 'from tests.integration' tests/` returns no results
    - `grep -r 'from tests.llm' tests/` returns no results
    - pyproject.toml addopts contains `not llm and not db`
    - Test directory structure is: tests/ with conftest.py, unit/, e2e/ only (no integration/ or llm/)
  </acceptance_criteria>
  <done>Old test directories and files removed. Test tree contains only tests/conftest.py (minimal), tests/unit/ (mocked fixtures + unit tests), and tests/e2e/ (real-infra fixtures + e2e tests). Unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify jwt_helpers.py is not imported by remaining tests</name>
  <read_first>
    - tests/unit/test_jwt_security.py
  </read_first>
  <files>tests/unit/test_jwt_security.py (possible update)</files>
  <action>
Before or after jwt_helpers.py deletion, check if `tests/unit/test_jwt_security.py` imports from `tests.jwt_helpers`:

```bash
grep 'jwt_helpers' tests/unit/test_jwt_security.py
```

If it imports `make_token`, `make_test_verifier`, `PRIVATE_KEY_PEM`, `PUBLIC_KEY_PEM`, `TEST_PROJECT_ID`, or `TEST_ISSUER` from `tests.jwt_helpers`:
- Move those specific helpers inline into `tests/unit/test_jwt_security.py` or into `tests/unit/conftest.py`
- The helpers are: `make_token()` (signed JWT factory), `make_test_verifier()` (verifier with ephemeral RSA keypair), and the constants `TEST_PROJECT_ID`, `TEST_ISSUER`, `PRIVATE_KEY_PEM`, `PUBLIC_KEY_PEM`
- Copy the full implementations from `tests/jwt_helpers.py` into the test file or unit conftest
- Update the import to be local instead of `from tests.jwt_helpers import ...`

If no file imports from jwt_helpers, this task is a no-op (the deletion in Task 1 is safe).
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker && grep -r 'jwt_helpers' tests/ 2>&1; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r 'jwt_helpers' tests/` returns no results (no imports of deleted module)
    - If test_jwt_security.py previously imported from jwt_helpers, those imports are replaced with inline code or unit/conftest imports
    - `pytest tests/unit/test_jwt_security.py -x -q` exits 0
  </acceptance_criteria>
  <done>No remaining test file imports from tests.jwt_helpers. JWT test helpers are either inlined or moved to unit conftest. Unit JWT tests pass.</done>
</task>

</tasks>

<verification>
- `ls tests/` shows only: conftest.py, unit/, e2e/ (no integration/, llm/, jwt_helpers.py)
- `pytest tests/unit/ -x -q` passes
- `grep -r 'jwt_helpers\|from tests.integration\|from tests.llm' tests/` returns nothing
- `ruff check tests/` passes
</verification>

<success_criteria>
Old test infrastructure removed. Clean test directory with only unit/ and e2e/ subdirectories. All unit tests pass. No dangling imports to deleted modules.
</success_criteria>

<output>
After completion, create `.planning/phases/16-update-tests-to-reflect-the-current-codebase-replace-llm-test-real-llm-py-with-more-general-end-to-end-tests-checking-every-endpoint-use-pytest-mark-to-keep-these-tests-separate/16-04-SUMMARY.md`
</output>
