---
phase: 15-refactor-chats
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/service.py
  - app/api/main.py
  - app/config.py
autonomous: true
requirements: [REFACT-01, REFACT-04, REFACT-06]

must_haves:
  truths:
    - "ty check reports zero errors in app/service.py"
    - "ty check reports zero errors in app/api/main.py"
    - "ty check reports zero errors in app/config.py"
    - "All unit tests still pass (82 tests)"
    - "ruff check still passes"
  artifacts:
    - path: "app/service.py"
      provides: "Type-narrowed content access in ask_llm"
      contains: "isinstance"
    - path: "app/api/main.py"
      provides: "Non-None assertion for config after MainConfig().app"
      contains: "assert config is not None"
    - path: "app/config.py"
      provides: "type: ignore comments on pydantic-settings incompatibilities"
      contains: "type: ignore"
  key_links:
    - from: "app/service.py"
      to: "app/models.py"
      via: "isinstance(msg.content, HumanContent) narrowing"
      pattern: "isinstance.*HumanContent"
    - from: "app/api/main.py"
      to: "app/config.py"
      via: "MainConfig().app assertion"
      pattern: "assert config is not None"
---

<objective>
Fix all ty type-check errors in app/service.py (6 errors), app/api/main.py (19 errors), and app/config.py (4 errors) -- 29 of 52 total errors.

Purpose: These three files contain the bulk of type errors (56%) and can be fixed with proper type narrowing and targeted type: ignore annotations where ty misunderstands framework patterns.
Output: Three files with zero ty errors, all tests still passing.
</objective>

<execution_context>
@/Users/vay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-refactor-chats/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix type errors in app/service.py and app/api/main.py</name>
  <files>app/service.py, app/api/main.py</files>
  <read_first>app/service.py, app/api/main.py, app/models.py, app/config.py</read_first>
  <action>
**app/service.py** -- 6 errors, all `unresolved-attribute` on `HumanContent | AIContent` union:

In the `ask_llm` method, the loop iterates over `chat.messages` and accesses `msg.content.phrase` and `msg.content.comment` without narrowing the union type. The code already checks `msg.role == Role.human` but ty does not understand that implies the content is HumanContent.

Fix by using `isinstance` narrowing before accessing `.phrase`/`.comment`:

1. Lines 39-46 (history loop): Replace the `if msg.role == Role.human:` check with `if isinstance(msg.content, HumanContent):` which serves the same purpose AND narrows the type for ty. For the `else` branch, keep `AIMessage(content=msg.content.model_dump_json())` which is fine since `.model_dump_json()` exists on both union members.

2. Lines 48-50 (current message content): The current code accesses `message.content.phrase` and `message.content.comment` directly. Since `message` is always a human message at this point (constructed with `HumanContent` in `create_chat` and `send_message`), add an `assert isinstance(message.content, HumanContent)` before line 48 to narrow the type.

Exact changes in `ask_llm`:
```python
        for msg in chat.messages:
            if isinstance(msg.content, HumanContent):
                text = msg.content.phrase
                if msg.content.comment:
                    text = f"{text}\n{msg.content.comment}"
                history.append(HumanMessage(content=text))
            else:
                history.append(AIMessage(content=msg.content.model_dump_json()))

        assert isinstance(message.content, HumanContent)
        content_text = message.content.phrase
        if message.content.comment:
            content_text = f"{content_text}\n{message.content.comment}"
```

**app/api/main.py** -- 19 errors:

1. **16x `unresolved-attribute` on `AppConfig | None`**: `MainConfig().app` returns `AppConfig | None` because `app` has default `None`. After `config = MainConfig().app`, add `assert config is not None` on the next line. This narrows the type for all subsequent accesses. Place it right after line 32 (`config = MainConfig().app`).

2. **1x `invalid-argument-type` on `create_chain(llm, config.prompt)`**: After the assert narrows `config` to `AppConfig`, `config.prompt` is still `str | None`. Add an `assert config.prompt is not None` before line 50 (`app.state.chain = create_chain(llm, config.prompt)`), OR change the assertion to a combined form. Actually, the prompt is loaded in MainConfig.load_config validator so it's always set. Add `assert config.prompt is not None` before the create_chain call.

3. **1x `invalid-assignment` on `app.openapi = custom_openapi`**: This is a well-known FastAPI monkey-patch pattern. Add `# type: ignore[invalid-assignment]` to line 92 (`app.openapi = custom_openapi`).

Exact changes:
- After line 32 (`config = MainConfig().app`), add: `assert config is not None`
- Before line 50 (`app.state.chain = create_chain(llm, config.prompt)`), add: `assert config.prompt is not None`
- Line 92: change `app.openapi = custom_openapi` to `app.openapi = custom_openapi  # type: ignore[invalid-assignment]`
  </action>
  <verify>
    <automated>python -m ty check 2>&1 | grep -E "app/service\.py|app/api/main\.py" | grep "^error" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `python -m ty check 2>&1 | grep "app/service.py" | grep "^error"` returns zero lines
    - `python -m ty check 2>&1 | grep "app/api/main.py" | grep "^error"` returns zero lines
    - app/service.py contains `isinstance(msg.content, HumanContent)`
    - app/service.py contains `assert isinstance(message.content, HumanContent)`
    - app/api/main.py contains `assert config is not None`
    - app/api/main.py contains `assert config.prompt is not None`
    - app/api/main.py contains `type: ignore[invalid-assignment]`
    - `python -m pytest tests/unit/ -x --tb=short -q` exits 0 with 82 passed
    - `python -m ruff check app/service.py app/api/main.py` exits 0
  </acceptance_criteria>
  <done>Zero ty errors in app/service.py and app/api/main.py. All 82 unit tests pass. Ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix type errors in app/config.py</name>
  <files>app/config.py</files>
  <read_first>app/config.py</read_first>
  <action>
**app/config.py** -- 4 errors:

1. **3x `invalid-assignment`** on lines 84-86: `Field(default="config/config.yaml")` not assignable to `Path`. This is a pydantic-settings pattern where `Field(default=...)` with a string value gets coerced to `Path` by Pydantic at runtime. ty doesn't understand this coercion. Fix by wrapping defaults in `Path()`:
   - Line 84: `config_dir: Path = Field(default=Path("config/config.yaml"))`
   - Line 85: `prompt_path: Path = Field(default=Path("config/prompt.txt"))`
   - Line 86: `examples_path: Path = Field(default=Path("config/examples.yaml"))`

2. **1x `unknown-argument`** on line 93: `AppConfig(_env_prefix="__NONE__", **yaml_data)`. The `_env_prefix` is a pydantic-settings internal init arg that ty doesn't recognize. Fix by adding `# type: ignore[unknown-argument]` to that line.

Add `from pathlib import Path` is already imported on line 3.

Exact changes:
- Line 84: `config_dir: Path = Field(default=Path("config/config.yaml"))`
- Line 85: `prompt_path: Path = Field(default=Path("config/prompt.txt"))`
- Line 86: `examples_path: Path = Field(default=Path("config/examples.yaml"))`
- Line 93: `app_config = AppConfig(_env_prefix="__NONE__", **yaml_data)  # type: ignore[unknown-argument]`
  </action>
  <verify>
    <automated>python -m ty check 2>&1 | grep "app/config\.py" | grep "^error" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `python -m ty check 2>&1 | grep "app/config.py" | grep "^error"` returns zero lines
    - app/config.py line 84 contains `Path("config/config.yaml")`
    - app/config.py line 85 contains `Path("config/prompt.txt")`
    - app/config.py line 86 contains `Path("config/examples.yaml")`
    - app/config.py contains `type: ignore[unknown-argument]`
    - `python -m pytest tests/unit/test_config.py -x --tb=short -q` exits 0
    - `python -m ruff check app/config.py` exits 0
  </acceptance_criteria>
  <done>Zero ty errors in app/config.py. Config tests pass. Ruff clean.</done>
</task>

</tasks>

<verification>
After both tasks:
- `python -m ty check 2>&1 | grep -c "^error"` should report 23 or fewer errors (down from 52, minus 29 fixed here)
- `python -m pytest tests/unit/ -x --tb=short -q` exits 0 with 82 passed
- `python -m ruff check .` exits 0
</verification>

<success_criteria>
- Zero ty errors in app/service.py, app/api/main.py, and app/config.py
- All 82 unit tests pass
- Ruff check clean
</success_criteria>

<output>
After completion, create `.planning/phases/15-refactor-chats/15-06-SUMMARY.md`
</output>
