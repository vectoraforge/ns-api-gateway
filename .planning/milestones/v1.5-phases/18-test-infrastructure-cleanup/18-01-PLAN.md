---
phase: 18-test-infrastructure-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/conftest.py
  - tests/e2e/test_isolation.py
  - tests/e2e/test_chat_queries.py
  - tests/e2e/test_chats.py
  - tests/e2e/test_flows.py
  - tests/e2e/test_health.py
  - tests/e2e/test_root.py
  - tests/e2e/test_examples.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "Each e2e test runs inside a database transaction that rolls back automatically on completion"
    - "No cleanup_chat function or try/finally cleanup blocks exist in the test codebase"
    - "Running the full e2e test suite twice in succession produces identical results with no leftover data"
    - "All e2e tests use httpx.AsyncClient instead of starlette TestClient"
    - "create_chat helper uses the transaction-bound session factory (not a separate connection)"
  artifacts:
    - path: "tests/e2e/conftest.py"
      provides: "Transaction isolation fixtures, async client, create_chat helper"
      contains: "join_transaction_mode"
    - path: "tests/e2e/test_isolation.py"
      provides: "Cross-user isolation tests without cleanup"
    - path: "tests/e2e/test_chat_queries.py"
      provides: "Chat query tests without cleanup"
    - path: "tests/e2e/test_chats.py"
      provides: "Chat CRUD tests using async client"
    - path: "tests/e2e/test_flows.py"
      provides: "Full lifecycle test using async client"
    - path: "tests/e2e/test_health.py"
      provides: "Health endpoint test using async client"
    - path: "tests/e2e/test_root.py"
      provides: "Root endpoint test using async client"
    - path: "tests/e2e/test_examples.py"
      provides: "Examples endpoint test using async client"
  key_links:
    - from: "tests/e2e/conftest.py::_db_transaction"
      to: "app.state.session_factory"
      via: "swap session_factory with transaction-bound factory per test"
      pattern: "join_transaction_mode.*create_savepoint"
    - from: "tests/e2e/conftest.py::async_client"
      to: "app.api.main.app"
      via: "httpx.AsyncClient with ASGITransport"
      pattern: "ASGITransport"
    - from: "tests/e2e/conftest.py::create_chat"
      to: "_db_transaction fixture"
      via: "uses the same transaction-bound session factory"
      pattern: "create_chat.*factory"
---

<objective>
Replace manual cleanup_chat() try/finally test isolation with transaction-based rollback using SQLAlchemy 2.0's join_transaction_mode="create_savepoint" pattern. Switch e2e tests from sync TestClient to async httpx.AsyncClient so the test fixtures and app request handlers share a single event loop and database connection.

Purpose: Eliminate fragile manual cleanup, ensure zero database artifacts between tests, and prepare the test infrastructure for future phases that add more database tables (users, subscriptions).

Output: Rewritten tests/e2e/conftest.py with transaction isolation fixtures, and all 7 e2e test files converted to async with no cleanup blocks.
</objective>

<execution_context>
@/Users/vay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-test-infrastructure-cleanup/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/api/main.py:
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # Sets: app.state.config, app.state.session_factory, app.state.verifier, app.state.llm_service
    db_engine = create_async_engine(config.db.url, pool_size=config.db.pool_size, max_overflow=0,
                                        connect_args=config.db.connect_args)
    app.state.session_factory = async_sessionmaker(db_engine, class_=SQLModelAsyncSession,
                                                       expire_on_commit=False)
    # ...
    yield
    await db_engine.dispose()
```

From app/api/dependencies.py:
```python
async def get_db(request: Request) -> AsyncGenerator[AsyncSession, None]:
    async with request.app.state.session_factory() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```

From app/models.py (used by create_chat helper):
```python
class Chat(SQLModel, table=True): ...       # id: UUID, user_id: str, title: str
class Message(SQLModel, table=True): ...    # chat_id: UUID, role: Role, content: JSON
class Role(str, Enum): human, ai
class HumanContent(BaseModel): phrase: str
class AIContent(BaseModel): response: str, issues: list, suggestions: list
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite e2e conftest with transaction isolation fixtures and async client</name>
  <files>tests/e2e/conftest.py</files>
  <read_first>
    - tests/e2e/conftest.py (current fixtures to replace)
    - app/api/main.py (lifespan function, session_factory setup, db_engine creation)
    - app/api/dependencies.py (get_db pattern that must work within savepoints)
    - .planning/phases/18-test-infrastructure-cleanup/18-RESEARCH.md (patterns 1-4, pitfalls 1-6, code examples 1-5)
  </read_first>
  <action>
Rewrite `tests/e2e/conftest.py` completely. The new file must contain these fixtures and helpers in this order:

**Imports needed:**

```python
import os
from uuid import uuid4

import httpx
import pytest
import pytest_asyncio
from httpx import ASGITransport, AsyncClient
from sqlalchemy.ext.asyncio import async_sessionmaker
from sqlmodel.ext.asyncio.session import AsyncSession as SQLModelAsyncSession

from api.main import app
from config import MainConfig
from models import AIContent, Chat, HumanContent, Message, Role
```

**Keep unchanged (session-scoped, sync):**
- `_app_config` fixture (scope="session") -- loads `MainConfig().app_config` once
- `ensure_tables` fixture (scope="session") -- creates tables via `create_async_engine` + `SQLModel.metadata.create_all`
- `firebase_token` fixture (scope="session") -- obtains Firebase token via httpx.post to identitytoolkit

**Replace `real_client` with two module-scoped async fixtures:**

1. `_app_lifespan` (scope="module", loop_scope="module"):
   - Decorated with `@pytest_asyncio.fixture(scope="module", loop_scope="module")`
   - Depends on `ensure_tables` (tables exist before app starts)
   - Uses `async with app.router.lifespan_context(app)` to start the app lifespan
   - Yields `app` inside the context manager
   - This triggers config loading, engine creation, verifier setup, LLM service setup

2. `async_client` (scope="module", loop_scope="module"):
   - Decorated with `@pytest_asyncio.fixture(scope="module", loop_scope="module")`
   - Depends on `_app_lifespan` and `firebase_token`
   - Creates `ASGITransport(app=_app_lifespan)` (note: `_app_lifespan` yields the app object)
   - Creates `AsyncClient(transport=transport, base_url="http://test")`
   - Sets `client.headers["Authorization"] = f"Bearer {firebase_token}"`
   - Yields client inside `async with AsyncClient(...) as client`

**Keep `test_user_id` unchanged** (scope="module", reads from `os.environ["FIREBASE_TEST_USER_ID"]`)

**Add `_db_transaction` function-scoped autouse fixture:**
- Decorated with `@pytest_asyncio.fixture(loop_scope="module", autouse=True)`
- Depends on `_app_lifespan`
- Implementation:
  1. Save original factory: `original_factory = app.state.session_factory`
  2. Get the engine. The lifespan creates the engine as a local variable (`db_engine`) and passes it to `async_sessionmaker`. Access it via the factory's bind: inspect `original_factory.kw` dict for the `"bind"` key. If that does not work, the engine can be obtained by looking at `original_factory.kw["bind"]`. The executor should verify the exact attribute path by inspecting the object at runtime if needed -- the critical requirement is obtaining the `AsyncEngine` that was created during lifespan.
  3. Open connection: `async with engine.connect() as connection:`
  4. Begin transaction: `transaction = await connection.begin()`
  5. Create test factory: `async_sessionmaker(bind=connection, class_=SQLModelAsyncSession, expire_on_commit=False, join_transaction_mode="create_savepoint")`
  6. Swap: `app.state.session_factory = test_factory`
  7. `try: yield test_factory`
  8. `finally: app.state.session_factory = original_factory` then `await transaction.rollback()`

**Rewrite `create_chat` helper (no changes to signature logic, but remove cleanup):**
- Keep the same function signature: `async def create_chat(factory, user_id: str):`
- Same body: creates Chat, HumanContent Message, AIContent Message, adds to session via `async with factory() as session`, commits, returns `chat_id`
- This is identical to the current implementation

**DELETE entirely:**
- `cleanup_chat` function (the entire async def)
- `test_db_factory` fixture (the entire fixture)

**Do NOT add `pytestmark = pytest.mark.e2e` at module level in conftest.py** -- it's already in each test file.
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker && python -c "import ast; tree = ast.parse(open('tests/e2e/conftest.py').read()); names = [n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]; assert 'cleanup_chat' not in names, 'cleanup_chat still exists'; assert 'test_db_factory' not in names, 'test_db_factory still exists'; assert 'create_chat' in names, 'create_chat missing'; assert '_db_transaction' in names, '_db_transaction missing'; assert '_app_lifespan' in names, '_app_lifespan missing'; assert 'async_client' in names, 'async_client missing'; print('conftest structure OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/conftest.py contains `async def _app_lifespan` with `app.router.lifespan_context(app)`
    - tests/e2e/conftest.py contains `async def async_client` with `ASGITransport` and `AsyncClient`
    - tests/e2e/conftest.py contains `async def _db_transaction` with `join_transaction_mode="create_savepoint"`
    - tests/e2e/conftest.py contains `async def create_chat(factory, user_id`
    - tests/e2e/conftest.py does NOT contain `def cleanup_chat` or `async def cleanup_chat`
    - tests/e2e/conftest.py does NOT contain `def test_db_factory` or `async def test_db_factory`
    - tests/e2e/conftest.py does NOT contain `from fastapi.testclient import TestClient`
    - tests/e2e/conftest.py does NOT contain `def real_client`
    - `grep -c "join_transaction_mode" tests/e2e/conftest.py` returns 1
    - `grep -c "ASGITransport" tests/e2e/conftest.py` returns at least 1
    - `grep -c "autouse=True" tests/e2e/conftest.py` returns 1
  </acceptance_criteria>
  <done>conftest.py contains transaction isolation fixtures (_app_lifespan, async_client, _db_transaction with autouse), create_chat helper, and no cleanup_chat/test_db_factory/real_client/TestClient</done>
</task>

<task type="auto">
  <name>Task 2: Convert all e2e test files to async with no cleanup blocks</name>
  <files>
    tests/e2e/test_isolation.py
    tests/e2e/test_chat_queries.py
    tests/e2e/test_chats.py
    tests/e2e/test_flows.py
    tests/e2e/test_health.py
    tests/e2e/test_root.py
    tests/e2e/test_examples.py
  </files>
  <read_first>
    - tests/e2e/test_isolation.py (5 tests with cleanup_chat try/finally)
    - tests/e2e/test_chat_queries.py (3 tests with cleanup_chat try/finally)
    - tests/e2e/test_chats.py (sync tests using real_client)
    - tests/e2e/test_flows.py (sync lifecycle test using real_client)
    - tests/e2e/test_health.py (sync test using real_client)
    - tests/e2e/test_root.py (sync test using real_client)
    - tests/e2e/test_examples.py (sync tests using real_client)
    - tests/e2e/conftest.py (the rewritten conftest from Task 1 -- to verify fixture names)
  </read_first>
  <action>
Convert all 7 e2e test files. The changes follow a consistent pattern for each file:

**Global changes applied to EVERY file:**
1. Add `@pytest.mark.asyncio(loop_scope="module")` decorator to every test class
2. Convert every `def test_*` to `async def test_*`
3. Replace `real_client` fixture parameter with `async_client`
4. Add `await` before every `async_client.get(...)`, `async_client.post(...)`, `async_client.delete(...)` call
5. Keep `pytestmark = pytest.mark.e2e` at module level (already present in all files)

**File-specific changes:**

**test_isolation.py:**
- Remove `from tests.e2e.conftest import cleanup_chat, create_chat` -- change to `from tests.e2e.conftest import create_chat`
- Remove every `try: ... finally: await cleanup_chat(test_db_factory, chat_id)` block -- just keep the test body without the try/finally wrapper
- Replace `test_db_factory` fixture parameter with `_db_transaction` in tests that call `create_chat`
- Change `create_chat(test_db_factory, ...)` to `create_chat(_db_transaction, ...)`
- Add `@pytest.mark.asyncio(loop_scope="module")` to `TestCrossUserIsolation` class
- All 5 test methods become `async def` with `await` on client calls
- The `test_can_delete_own_chat` test (last one) already has no try/finally -- just update fixture names and add await

**test_chat_queries.py:**
- Remove `from tests.e2e.conftest import cleanup_chat, create_chat` -- change to `from tests.e2e.conftest import create_chat`
- Remove every `try: ... finally: await cleanup_chat(test_db_factory, chat_id)` block
- Replace `test_db_factory` with `_db_transaction` in fixture parameters and `create_chat` calls
- Replace `real_client` with `async_client`
- Add `@pytest.mark.asyncio(loop_scope="module")` to each class (`TestListChats`, `TestGetChatMessages`, `TestDeleteChat`)
- All 3 test methods: `async def`, `await` on client calls
- The `test_delete_chat` test (last one) already has no try/finally -- just update fixture names and add await

**test_chats.py:**
- No imports to change (no cleanup_chat import)
- Replace `real_client` with `async_client` in all fixture parameters
- Add `@pytest.mark.asyncio(loop_scope="module")` to `TestCreateChat` and `TestFollowup` classes
- Convert all 5 methods to `async def`, add `await` before every client call
- In `TestFollowup.test_followup_message`: both `async_client.post(...)` calls need `await`

**test_flows.py:**
- No imports to change
- Replace `real_client` with `async_client`
- Add `@pytest.mark.asyncio(loop_scope="module")` to `TestChatLifecycle` class
- Convert `test_full_chat_lifecycle` to `async def`, add `await` before all 6 client calls (post, post, get, get, delete, get)

**test_health.py:**
- No imports to change
- Replace `real_client` with `async_client`
- Add `@pytest.mark.asyncio(loop_scope="module")` to `TestHealthEndpoint` class
- Convert `test_health_ready_returns_up` to `async def`, add `await` before `async_client.get(...)`

**test_root.py:**
- No imports to change
- Replace `real_client` with `async_client`
- Add `@pytest.mark.asyncio(loop_scope="module")` to `TestRootEndpoint` class
- Convert `test_root_returns_app_info` to `async def`, add `await` before `async_client.get(...)`

**test_examples.py:**
- No imports to change
- Replace `real_client` with `async_client`
- Add `@pytest.mark.asyncio(loop_scope="module")` to `TestExamplesEndpoint` class
- Convert both test methods to `async def`, add `await` before each `async_client.get(...)`

**Important: Remove individual `@pytest.mark.asyncio` decorators** from test methods in test_isolation.py and test_chat_queries.py -- the class-level `@pytest.mark.asyncio(loop_scope="module")` covers all methods in the class. Do NOT add both class-level and method-level asyncio marks.

**After all files are converted, run the full e2e test suite:**
```bash
pytest -m e2e --override-ini="addopts=" -x -v
```

If tests fail, diagnose and fix. Common issues:
- Engine access path in `_db_transaction` may need adjustment -- inspect the actual `session_factory` object
- Event loop scope mismatches -- ensure all module-scoped fixtures have `loop_scope="module"`
- Connection/transaction lifecycle -- ensure rollback happens even on fixture errors
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker && ! grep -r "cleanup_chat" tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "cleanup_chat" tests/` returns no matches (exit code 1)
    - `grep -r "test_db_factory" tests/e2e/test_` returns no matches
    - `grep -r "real_client" tests/e2e/` returns no matches
    - `grep -r "try:" tests/e2e/test_isolation.py tests/e2e/test_chat_queries.py` returns no matches
    - `grep -r "finally:" tests/e2e/test_isolation.py tests/e2e/test_chat_queries.py` returns no matches
    - `grep -c "async_client" tests/e2e/test_chats.py` returns at least 5
    - `grep -c "await" tests/e2e/test_chats.py` returns at least 5
    - `grep -c "loop_scope" tests/e2e/test_isolation.py` returns at least 1
    - `grep -c "async def test_" tests/e2e/test_flows.py` returns 1
    - `pytest -m e2e --override-ini="addopts=" -x` passes all tests (exit code 0)
    - Running `pytest -m e2e --override-ini="addopts=" -x` a SECOND time also passes (proving no DB artifacts)
  </acceptance_criteria>
  <done>All 7 e2e test files use async_client instead of real_client, all test methods are async def with await, no cleanup_chat/try-finally/test_db_factory references remain, and the full e2e suite passes twice in succession</done>
</task>

</tasks>

<verification>
Phase-level verification after both tasks complete:

1. **TEST-01 (transaction rollback):** `grep "join_transaction_mode" tests/e2e/conftest.py` shows `create_savepoint` -- confirms per-test transaction isolation
2. **TEST-02 (no cleanup helpers):** `grep -r "cleanup_chat\|cleanup_" tests/` returns nothing
3. **TEST-03 (no DB artifacts):** Run `pytest -m e2e --override-ini="addopts=" -x` twice in succession -- both runs pass with identical results

Static checks:
- `grep -r "TestClient" tests/e2e/` returns nothing (fully migrated to AsyncClient)
- `grep -r "try:.*finally:.*cleanup" tests/e2e/` returns nothing (no manual cleanup patterns)
</verification>

<success_criteria>
- All e2e tests pass via `pytest -m e2e --override-ini="addopts=" -x`
- Running the e2e suite twice consecutively produces identical results
- Zero references to cleanup_chat, test_db_factory, or real_client in tests/e2e/
- Transaction rollback confirmed by join_transaction_mode="create_savepoint" in conftest
- Unit tests unaffected: `pytest tests/unit/ -x` still passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-test-infrastructure-cleanup/18-01-SUMMARY.md`
</output>
