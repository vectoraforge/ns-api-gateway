---
phase: 23-envoy-gateway-rate-limiting
plan: 04
type: execute
wave: 1
depends_on: ["23-01"]
files_modified: ["app/routers/root.py"]
autonomous: true
requirements: ["ENVOY-01", "ENVOY-02", "ENVOY-03", "ENVOY-04", "ENVOY-05"]
gap_closure: true

must_haves:
  truths:
    - "GET / returns name 'NativeSpeaker API Gateway' and version from ns-api-gateway package"
    - "importlib.metadata.version('ns-api-gateway') resolves without error"
    - "test_error_contract.py collects and passes (no PackageNotFoundError)"
  artifacts:
    - path: "app/routers/root.py"
      provides: "Corrected package name and branding"
      contains: "ns-api-gateway"
  key_links:
    - from: "app/routers/root.py"
      to: "pyproject.toml"
      via: "importlib.metadata.version('ns-api-gateway')"
      pattern: 'version\("ns-api-gateway"\)'
---

<objective>
Complete the project rename from sn-api-gateway to ns-api-gateway by fixing two missed artifacts: update root.py branding/version call, and re-install the package in editable mode so importlib.metadata resolves the new name.

Purpose: The rename in Plan 01 missed app/routers/root.py and did not re-install the package. This causes GET / to fail at runtime and test_error_contract.py to fail on collection.
Output: root.py updated, package metadata registered, all tests passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-envoy-gateway-rate-limiting/23-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix root.py branding and re-install package</name>
  <files>app/routers/root.py</files>
  <read_first>app/routers/root.py, pyproject.toml</read_first>
  <action>
Two changes:

1. In app/routers/root.py, change:
   - `"SpeakNative API Gateway"` to `"NativeSpeaker API Gateway"`
   - `version("sn-api-gateway")` to `version("ns-api-gateway")`

2. Run `uv pip install -e .` to register the ns-api-gateway package metadata in the virtual environment. This replaces the stale sn-api-gateway 1.4.0 entry with ns-api-gateway 1.5.0.

No other files need changes. The test code in test_error_contract.py is already correct -- it just cannot collect because importlib.metadata fails at import time.
  </action>
  <verify>
    <automated>cd /Users/vay/Work/git/native-speaker && python -c "from importlib.metadata import version; print(version('ns-api-gateway'))" && pytest tests/unit/test_error_contract.py -x -q && grep -q 'ns-api-gateway' app/routers/root.py && grep -q 'NativeSpeaker API Gateway' app/routers/root.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "ns-api-gateway" app/routers/root.py returns: version("ns-api-gateway")
    - grep "NativeSpeaker API Gateway" app/routers/root.py returns the updated name
    - grep "sn-api-gateway" app/routers/root.py returns NO matches
    - grep "SpeakNative" app/routers/root.py returns NO matches
    - python -c "from importlib.metadata import version; print(version('ns-api-gateway'))" prints "1.5.0"
    - pytest tests/unit/test_error_contract.py -x -q passes (no collection errors)
    - pytest tests/unit/ -x -q passes (full unit suite green)
  </acceptance_criteria>
  <done>
    root.py uses "NativeSpeaker API Gateway" and version("ns-api-gateway"). Package metadata resolves ns-api-gateway 1.5.0. All unit tests pass including test_error_contract.py.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "sn-api-gateway" app/routers/root.py` returns 0
2. `grep -c "SpeakNative" app/routers/root.py` returns 0
3. `python -c "from importlib.metadata import version; print(version('ns-api-gateway'))"` prints 1.5.0
4. `pytest tests/unit/ -x -q` all tests pass
</verification>

<success_criteria>
- No references to "sn-api-gateway" or "SpeakNative" remain in app/routers/root.py
- importlib.metadata.version("ns-api-gateway") returns "1.5.0"
- test_error_contract.py collects and passes
- Full unit test suite is green
</success_criteria>

<output>
After completion, create `.planning/phases/23-envoy-gateway-rate-limiting/23-04-SUMMARY.md`
</output>
