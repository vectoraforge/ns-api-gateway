---
phase: 33-propagate-quota-exceeded-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nativespeaker/api/app/errors.py
  - tests/unit/test_usage.py
  - tests/unit/test_error_contract.py
  - k8s/templates/backend-traffic-policy.yaml
autonomous: true
requirements: [DEP-04, DEP-06]
gap_closure: true

must_haves:
  truths:
    - "HTTP exception handler returns quota_exceeded (not rate_limited) for 429 responses"
    - "QuotaExceededError tests assert against quota_exceeded code"
    - "Error contract test set includes quota_exceeded (not rate_limited)"
    - "K8s traffic policy inline 429 response body uses quota_exceeded"
    - "POST /chats/{id} test sends message field (not stale content field)"
    - "Full unit test suite passes with zero failures"
  artifacts:
    - path: "src/nativespeaker/api/app/errors.py"
      provides: "_CODE_MAP with correct 429 mapping"
      contains: 'quota_exceeded'
    - path: "tests/unit/test_usage.py"
      provides: "Quota tests with correct assertions"
      contains: 'quota_exceeded'
    - path: "tests/unit/test_error_contract.py"
      provides: "Contract code set with quota_exceeded"
      contains: 'quota_exceeded'
    - path: "k8s/templates/backend-traffic-policy.yaml"
      provides: "Inline 429 response with quota_exceeded"
      contains: 'quota_exceeded'
  key_links:
    - from: "src/nativespeaker/api/app/errors.py"
      to: "src/nativespeaker/api/exceptions.py"
      via: "_CODE_MAP[429] must match QuotaExceededError.error_code"
      pattern: '429.*quota_exceeded'
    - from: "tests/unit/test_error_contract.py"
      to: "src/nativespeaker/api/exceptions.py"
      via: "CONTRACT_CODES must include every ErrorCode literal value"
      pattern: 'quota_exceeded'
---

<objective>
Propagate the Phase 31 `rate_limited` -> `quota_exceeded` rename to all remaining stale references: the HTTP exception handler code map, two test files, and the k8s traffic policy. Also fix a stale `content` field name in test_usage.py that should be `message`.

Purpose: Without this fix, the `http_exception_handler` path would return `rate_limited` for generic 429s (mismatching the `QuotaExceededError.error_code = "quota_exceeded"` already set in exceptions.py), tests assert against the wrong string, and the k8s inline response body disagrees with the application error contract.

Output: Four files updated, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Source of truth: QuotaExceededError already uses "quota_exceeded" since Phase 31 -->
From src/nativespeaker/api/exceptions.py:
```python
ErrorCode = Literal["invalid_request",
                    "validation_error",
                    "unauthorized",
                    "not_found",
                    "service_unavailable",
                    "internal_error",
                    "quota_exceeded",
                    "out_of_scope"]

class QuotaExceededError(ServiceError):
    status_code = 429
    error_code = "quota_exceeded"
```

From src/nativespeaker/api/models/api.py:
```python
class MessageRequest(BaseModel):
    """Followup message in existing chat."""
    message: str = Field(..., max_length=4096)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rate_limited -> quota_exceeded in production code and k8s config</name>
  <files>src/nativespeaker/api/app/errors.py, k8s/templates/backend-traffic-policy.yaml</files>
  <read_first>
    src/nativespeaker/api/app/errors.py
    src/nativespeaker/api/exceptions.py
    k8s/templates/backend-traffic-policy.yaml
  </read_first>
  <action>
In `src/nativespeaker/api/app/errors.py`, change line 39 in the `_CODE_MAP` dict:
- FROM: `429: "rate_limited",`
- TO:   `429: "quota_exceeded",`

In `k8s/templates/backend-traffic-policy.yaml`, change line 53 in the responseOverride inline body:
- FROM: `inline: '{"code":"rate_limited"}'`
- TO:   `inline: '{"code":"quota_exceeded"}'`

No other lines in either file change.
  </action>
  <verify>
    <automated>grep -n 'quota_exceeded' src/nativespeaker/api/app/errors.py && grep -n 'quota_exceeded' k8s/templates/backend-traffic-policy.yaml && ! grep 'rate_limited' src/nativespeaker/api/app/errors.py && ! grep 'rate_limited' k8s/templates/backend-traffic-policy.yaml</automated>
  </verify>
  <acceptance_criteria>
    - errors.py line 39 contains `429: "quota_exceeded",`
    - errors.py contains zero occurrences of the string `rate_limited`
    - backend-traffic-policy.yaml line 53 contains `inline: '{"code":"quota_exceeded"}'`
    - backend-traffic-policy.yaml contains zero occurrences of the string `rate_limited`
  </acceptance_criteria>
  <done>_CODE_MAP[429] maps to "quota_exceeded" and k8s inline 429 body uses "quota_exceeded"</done>
</task>

<task type="auto">
  <name>Task 2: Fix rate_limited -> quota_exceeded and content -> message in test files</name>
  <files>tests/unit/test_usage.py, tests/unit/test_error_contract.py</files>
  <read_first>
    tests/unit/test_usage.py
    tests/unit/test_error_contract.py
    src/nativespeaker/api/exceptions.py
    src/nativespeaker/api/models/api.py
  </read_first>
  <action>
In `tests/unit/test_usage.py`, make these 5 changes:

1. Line 13 docstring: change `rate_limited` to `quota_exceeded`
   - FROM: `"""Error contract: QuotaExceededError returns 429 with rate_limited code."""`
   - TO:   `"""Error contract: QuotaExceededError returns 429 with quota_exceeded code."""`

2. Line 19 assertion: change `"rate_limited"` to `"quota_exceeded"`
   - FROM: `assert QuotaExceededError.error_code == "rate_limited"`
   - TO:   `assert QuotaExceededError.error_code == "quota_exceeded"`

3. Line 91 assertion: change `"rate_limited"` to `"quota_exceeded"`
   - FROM: `assert response.json()["code"] == "rate_limited"`
   - TO:   `assert response.json()["code"] == "quota_exceeded"`

4. Line 97 JSON payload: change `"content"` to `"message"` (matches `MessageRequest.message` field)
   - FROM: `response = client.post(f"/chats/{uuid.uuid4()}", json={"content": "test"})`
   - TO:   `response = client.post(f"/chats/{uuid.uuid4()}", json={"message": "test"})`

5. Line 99 assertion: change `"rate_limited"` to `"quota_exceeded"`
   - FROM: `assert response.json()["code"] == "rate_limited"`
   - TO:   `assert response.json()["code"] == "quota_exceeded"`

In `tests/unit/test_error_contract.py`, make this 1 change:

6. Line 9 in CONTRACT_CODES set: change `"rate_limited"` to `"quota_exceeded"`
   - FROM: `CONTRACT_CODES = {"invalid_request", "validation_error", "unauthorized", "not_found", "service_unavailable", "internal_error", "rate_limited", "out_of_scope"}`
   - TO:   `CONTRACT_CODES = {"invalid_request", "validation_error", "unauthorized", "not_found", "service_unavailable", "internal_error", "quota_exceeded", "out_of_scope"}`

After edits, run the full unit test suite:
```bash
python -m pytest tests/unit/ -x -v
```
  </action>
  <verify>
    <automated>grep -n 'quota_exceeded' tests/unit/test_usage.py && grep -n 'quota_exceeded' tests/unit/test_error_contract.py && ! grep 'rate_limited' tests/unit/test_usage.py && ! grep 'rate_limited' tests/unit/test_error_contract.py && grep '"message"' tests/unit/test_usage.py && ! grep '"content"' tests/unit/test_usage.py && python -m pytest tests/unit/ -x -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test_usage.py contains `"quota_exceeded"` on lines with error_code and response assertions (3 occurrences)
    - test_usage.py contains zero occurrences of string `rate_limited`
    - test_usage.py line 97 uses `json={"message": "test"}` (not `"content"`)
    - test_usage.py contains zero occurrences of `"content"` as a JSON field key
    - test_error_contract.py CONTRACT_CODES set contains `"quota_exceeded"` (not `"rate_limited"`)
    - test_error_contract.py contains zero occurrences of string `rate_limited`
    - `python -m pytest tests/unit/ -x` exits with code 0
  </acceptance_criteria>
  <done>All test assertions use "quota_exceeded", stale "content" field renamed to "message", full unit suite passes</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Zero stale references: `grep -r "rate_limited" src/ tests/ k8s/ --include="*.py" --include="*.yaml"` returns no matches
2. Correct references: `grep -rn "quota_exceeded" src/nativespeaker/api/app/errors.py tests/unit/test_usage.py tests/unit/test_error_contract.py k8s/templates/backend-traffic-policy.yaml` shows all 4 files
3. Full unit test suite green: `python -m pytest tests/unit/ -x -v` exits 0 with no failures
</verification>

<success_criteria>
- _CODE_MAP[429] == "quota_exceeded" in errors.py
- All test_usage.py quota assertions check for "quota_exceeded"
- CONTRACT_CODES in test_error_contract.py includes "quota_exceeded"
- k8s backend-traffic-policy.yaml inline 429 response uses "quota_exceeded"
- test_usage.py POST /chats/{id} sends {"message": ...} not {"content": ...}
- `python -m pytest tests/unit/ -x` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/33-propagate-quota-exceeded-rename/33-01-SUMMARY.md`
</output>
