---
phase: 30-e2e-and-security-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_models.py
  - tests/unit/test_services.py
  - tests/unit/test_subscriptions.py
autonomous: true
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "test_models.py collects and passes without import errors"
    - "test_services.py collects and passes without import errors"
    - "All async test methods in test_subscriptions.py classes execute as proper async tests"
    - "Full unit suite has 0 collection errors"
  artifacts:
    - path: "tests/unit/test_models.py"
      provides: "Fixed import for Issue"
      contains: "from nativespeaker.api.models.content import Issue"
    - path: "tests/unit/test_services.py"
      provides: "Fixed import for Issue"
      contains: "from nativespeaker.api.models.content import Issue"
    - path: "tests/unit/test_subscriptions.py"
      provides: "Async test markers on class methods"
      contains: "@pytest.mark.asyncio"
  key_links:
    - from: "tests/unit/test_models.py"
      to: "nativespeaker.api.models.content"
      via: "import Issue"
      pattern: "from nativespeaker\\.api\\.models\\.content import Issue"
    - from: "tests/unit/test_services.py"
      to: "nativespeaker.api.models.content"
      via: "import Issue"
      pattern: "from nativespeaker\\.api\\.models\\.content import Issue"
---

<objective>
Fix broken test imports and async test markers so the full unit suite collects and runs cleanly.

Purpose: Two test modules (test_models.py, test_services.py) fail to collect due to importing `Issue` from the old `nativespeaker.api.schema` location. Additionally, 7 async test methods in test_subscriptions.py classes lack explicit `@pytest.mark.asyncio` decorators. This plan fixes all of these so subsequent plans can rely on a clean baseline.

Output: All unit tests collect and pass with 0 errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-make-sure-all-e2e-cases-are-covered-by-tests-for-example-are-subscriptions-being-tested-also-make-sure-you-have-security-unit-tests/30-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken imports in test_models.py and test_services.py</name>
  <files>tests/unit/test_models.py, tests/unit/test_services.py</files>
  <read_first>
    - tests/unit/test_models.py
    - tests/unit/test_services.py
    - src/nativespeaker/api/schema.py
    - src/nativespeaker/api/models/content.py
  </read_first>
  <action>
In tests/unit/test_models.py line 7, replace:
```python
from nativespeaker.api.schema import ChatRequest, ChatResponse, ExamplesResponse, Issue, MessageRequest, MessageResponse
```
with:
```python
from nativespeaker.api.schema import ChatRequest, ChatResponse, ExamplesResponse, MessageRequest, MessageResponse
from nativespeaker.api.models.content import Issue
```

In tests/unit/test_services.py line 5, replace:
```python
from nativespeaker.api.schema import ExamplesResponse, Issue
```
with:
```python
from nativespeaker.api.schema import ExamplesResponse
from nativespeaker.api.models.content import Issue
```

`Issue` was moved from `nativespeaker.api.schema` to `nativespeaker.api.models.content` during an earlier refactor but these test files were not updated.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_models.py tests/unit/test_services.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_models.py contains "from nativespeaker.api.models.content import Issue"
    - tests/unit/test_models.py does NOT contain "from nativespeaker.api.schema import.*Issue"
    - tests/unit/test_services.py contains "from nativespeaker.api.models.content import Issue"
    - tests/unit/test_services.py does NOT contain "from nativespeaker.api.schema import.*Issue"
    - `python -m pytest tests/unit/test_models.py tests/unit/test_services.py -q` exits 0 with all tests passing
  </acceptance_criteria>
  <done>test_models.py and test_services.py collect and pass without import errors</done>
</task>

<task type="auto">
  <name>Task 2: Add async test markers to subscription test class methods</name>
  <files>tests/unit/test_subscriptions.py</files>
  <read_first>
    - tests/unit/test_subscriptions.py
    - pyproject.toml
  </read_first>
  <action>
Add `@pytest.mark.asyncio` decorator to these 7 async methods that are inside test classes (top-level async functions get auto-detected by `asyncio_mode = "auto"` but class methods need explicit markers):

1. `TestSubscriptionLifecycle.test_ignored_notification_types` (line ~181)
2. `TestIdempotency.test_duplicate_notification_ignored` (line ~198)
3. `TestPlanTierUpdate.test_plan_updated_on_subscription_change` (line ~227)
4. `TestFirebaseSync.test_firebase_sync_on_tier_change` (line ~261)
5. `TestFirebaseSync.test_uses_to_thread` (line ~296)
6. `TestFirebaseSync.test_firebase_failure_does_not_raise` (line ~305)
7. `TestFirebaseSync.test_no_firebase_sync_when_tier_unchanged` (line ~314)

Each of these is an `async def` method inside a class but lacks `@pytest.mark.asyncio`. Add the decorator immediately above the `async def` line. For example:

```python
    @pytest.mark.asyncio
    async def test_ignored_notification_types(self, subscription_service,
                                                mock_verifier, mock_subscriptions_db):
```

Do NOT add the decorator to the synchronous `def test_*` methods in `TestSubscriptionLifecycle` (lines 79-179) -- those are already correct as sync tests.

After adding all markers, verify the full test_subscriptions.py passes and that pytest collection shows `<Function>` not `<Coroutine>` for these methods.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_subscriptions.py -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR|Coroutine"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_subscriptions.py contains exactly 7 occurrences of "@pytest.mark.asyncio" (one for each async method)
    - `python -m pytest tests/unit/test_subscriptions.py --co -q` output does NOT contain "Coroutine"
    - `python -m pytest tests/unit/test_subscriptions.py -q` exits 0 with all tests passing
    - `python -m pytest tests/unit/ -q --tb=short` exits 0 with 0 errors and 0 failures
  </acceptance_criteria>
  <done>All async test methods in subscription test classes have explicit markers and execute as proper async tests. Full unit suite passes with 0 collection errors.</done>
</task>

</tasks>

<verification>
Run the complete unit test suite to confirm zero collection errors and all tests pass:
```bash
python -m pytest tests/unit/ -q --tb=short
```
Expected: 100+ passed (the previously broken ~20 tests from test_models.py and test_services.py now run), 0 errors, 0 failures.
</verification>

<success_criteria>
- Zero `ImportError` during collection
- Zero `<Coroutine>` entries in test collection
- All unit tests pass (100+ tests)
- test_models.py imports Issue from nativespeaker.api.models.content
- test_services.py imports Issue from nativespeaker.api.models.content
- test_subscriptions.py async class methods have @pytest.mark.asyncio
</success_criteria>

<output>
After completion, create `.planning/phases/30-make-sure-all-e2e-cases-are-covered-by-tests-for-example-are-subscriptions-being-tested-also-make-sure-you-have-security-unit-tests/30-01-SUMMARY.md`
</output>
