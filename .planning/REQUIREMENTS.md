# Requirements: ns-api-gateway — v2.0 Authentication & Entitlements

**Defined:** 2026-08-19
**Core Value:** The analysis pipeline must work reliably — correct LLM invocation, proper resilience under load, and safe per-user data isolation.

**Specification:** `/home/init/native-speaker/specs/auth-refactor-phases/` — one file per phase. `SHARED-INVARIANTS.md` binds every phase and wins over any phase brief on conflict.

**Organizing principle:** one phase = one REQ-ID prefix, 1:1. Endpoint phases implement exactly one endpoint. No requirement spans phases.

**Phase map:** 34 SCHEMA · 35 FOUND · 36 REBIND · 37 CREATE · 38 SYNC · 39 PROF · 40 UPGRADE · 41 ANONGRANT · 42 REGGRANT · 43 APPLEHOOK · 44 PLAYHOOK · 45 RESTORE · 46 SIGNOUT

> **Amended by Phase 37.1 (2026-08-24) for two deletions.** The auth-event audit subsystem was deleted outright (D-01) and the auth barrier middleware plus the route registry were replaced by a router-level FastAPI dependency (D-06). Every requirement those deletions touched carries a note saying which treatment it got and why: **amended** (mechanism changed, substance intact), **withdrawn** (its subject no longer exists), or **flagged forward** (an unbuilt phase must decide for itself). One requirement was deleted outright — see the FOUND block.
>
> **Amended again by Phase 37.2 (2026-08-25) for the auth-module simplification.** Challenge issuance moved out of create-user's prepare mode onto its own `POST /auth/challenge` route (D-01/D-03/D-05), the Firebase service-account credential path was deleted in favour of Application Default Credentials (D-06…D-08), and the zero-consumer adapter interfaces were deleted along with the lazy re-export facade (D-09/D-11). CREATE-01/02 and FOUND-08 are **amended**; FOUND-08 is additionally **flagged forward** to phases 43–46. This phase deleted and withdrew nothing — every requirement it touched kept its substance.
>
> **Amended again by Phase 37.3 (2026-08-26) for the returned-rejection refactor.** Four sites that *returned* a rejection vocabulary — the consuming transaction, identity resolution, the provider lookup and the challenge binding — now raise DRF-style exceptions carrying their own `ErrorClass`, answered by one handler (D-01/D-02). The `AuthEventResult` outcome enum, the `ProviderData*` result types and the `Admit`/`Reject` union are deleted (D-03/D-09/D-12). **FOUND-08 is amended again** and its forward flag to phases 43–46 is carried unchanged. Zero client-visible change: every status, error class and body is byte-identical, and only the structured-log event vocabulary moved. No requirement was deleted, withdrawn or unchecked, and **no new conflict against `SHARED-INVARIANTS.md` was found** — the three below are still the three.
>
> **Amended again by Phase 37.4 (2026-08-30) for the third machine-generated-code refactor.** The identity union and its `RequestContext` carrier collapsed into one `Identity` class and one accessor pair (D-01/D-02/D-03); the two exception trees, the `ErrorClass` indirection and the module-level registry collapsed into one tree rooted at `AppError`, served by one response builder and gated at startup by a registry-free totality walk (D-08/D-09); the quota gate became a plain `charge_quota()` function (D-07); and three pieces of named machinery were deleted, each against an explicit spec requirement — the HMAC keyring (D-11), the hand-rolled bearer extractor (D-10) and `claim_attempt_id` (D-03). **FOUND-04 is amended** (the shared registry module became a shared exception tree in one module behind one handler; every property the requirement states still holds). **FOUND-02 was deleted outright at the developer's direction** — the second requirement this project has deleted rather than amended; the wire contract it stated is recorded under FOUND-01 as a property given up, not as a flagged conflict, because the developer removed the invariant asserting it from `SHARED-INVARIANTS.md` in the same decision. **This phase records three new conflicts and resolves none.**
>
> **Amended again by Phase 37.5 (2026-08-31) for the fourth machine-generated-code refactor.** The layered architecture was restored and written into `AGENTS.md`: `Identity` moved to `schemas/auth.py`, the identity queries to `crud/identities.py` (D-01/D-04), the completion to `services/auth.py::AuthService` (D-03) and the quota to `services/quota.py::QuotaService` as one merged charge (D-06); `auth/identity.py`, `auth/create_user.py` and the top-level `quota.py` are deleted, and `auth/` is down to its three external-SDK seam files. The docstring and comment rules were applied to every file in `src/` and `tests/` (D-12), gated by `tests/unit/test_docstring_bar.py`, whose baselines went from a **measured 29** violations pre-sweep to **0 on every root**. `assert_tree_total` and its two helpers left production code for `tests/unit/error_tree.py` (D-09), and four exception classes nothing raises were deleted (D-10). `ResiliencePolicy` grew an explicit `admission()` context manager handing out an `Admitted` token, with the quota charge inside admission and outside tenacity's retry (D-07/A-01), so **a request that never reached the provider is no longer billed**. **FOUND-04 is amended** — the startup totality walk is now a test, and the boot-time fail-closed it gave up is recorded there as an accepted consequence. **No requirement was deleted, withdrawn or unchecked, and no client-visible status, code or body changed.**
>
> **This phase records no new conflict and resolves none — the six below are still the six.** D-15's verdict was re-read against `SHARED-INVARIANTS.md` (§ Errors, § Rate limits, § Locks, § The barrier) rather than assumed, and two adjacent items stand, neither of them a seventh conflict. **(a) The layering rule, read literally, would move `ErrorResponse` out of `errors.py`** into `schemas/`, and `SHARED-INVARIANTS.md:44-45` requires the one shared error module to own the client-visible response shape. The specification binds, so `AGENTS.md`'s D-01 text carries a **one-line carve-out** naming `errors.py` — a carve-out written before the code moved, not a divergence discovered after it (A-07/P-08). **(b) The `limits` bookkeeping item (A-16/P-11).** `SHARED-INVARIANTS.md` § Rate limits mandates the `limits` library (*"`parse_many()`, moving-window, shared Redis/Valkey in production"*) and this project has none — verified twice this phase, `git log -S'limits' -- pyproject.toml` empty and no `limits` among the 110 distributions in `.venv`. **That divergence is pre-existing and already recorded, at `:50` and under FOUND-06, as a Phase 35 D-05 *override* rather than as one of the counted flagged conflicts.** It is deliberately **not** added as a new conflict. **The mismatch is therefore stated rather than reconciled away:** the count below reads six, the set of known divergences from the binding specification is seven, and the seventh is the `limits` override — recorded as an override since Phase 35 and left in that category, because re-filing it now would rewrite Phase 35's own decision. A reader auditing divergences must read the count **and** the override at `:50`. It is also why the developer's opening expectation in this phase's discussion was correct as a reading of the specification: they were reading the spec, not misremembering it (see § Phase 37.5 records, D-11).
>
> **Eleven premises of this phase's own context file were corrected against the installed source before any of it was planned** (P-01…P-11, in `37.5-RESEARCH.md`). **Three changed the shape of the plan:** **P-01** — `QueueFullError` has **always** answered **503**, not 429; it subclasses `ServiceUnavailable` and declares no status of its own, so the queue-full rejection never was a 429 and D-07's and D-11's claim that it was is withdrawn as factually wrong. **P-02** — the property protecting a quota rejection from reading as a provider outage is **structural**, not located at `resilience.py:73-79` (that range is `LLMExecutionGate.__init__`, a citation inherited wrongly from 37.4 A-03); it holds because the charge sits outside `attempt()`'s `except Exception`. **P-06** — D-09's replacement test **already existed**: `tests/unit/test_error_registry.py` held all four totality properties with seven negative controls, so the work was **rehosting a checker**, not writing a test, and the hard part was a `PYTHONPATH` the checker's five subprocess callers needed. The other eight: **P-03** tenacity retries `attempt()` three times, so a charge inside it charges three times; **P-04** `37.4-REVIEW.md` WR-01's own proposed fix is the `on_admitted` callback D-07 forbids — its diagnosis was adopted, its remedy never; **P-05** deleting `ChallengeRequired` would **not** drop a mandated code, so one of D-10's two stated grounds was wrong (the dispatch ground is the load-bearing one); **P-07** D-08's deletion list contradicted D-08's own rule for `_sleep_if_positive`; **P-08** D-01's layering rule would pull `ErrorResponse` out of `errors.py` — item (a) above; **P-09** `tests/schema/` is 2,297 lines and the test tree 59 files, not 916 and 39; **P-10** `tests/e2e/test_startup_assertion.py` never tested the startup assertion — its name was a leftover from the route-enumeration assertion 37.1 deleted, and plan `37.5-09` renamed it `test_unauthenticated_access.py`; **P-11** the `limits` mandate — item (b) above.
>
> **The two wire-contract divergences recorded at `:75` keep their only executable trace.** Phase 37.4 gave up the exactly-one-`Authorization` property and recorded it under FOUND-01 as a loss rather than a flagged conflict. Of the seven cases in `test_identity_accessors.py::TestTheWireContractTheFrameworkNowEnforces`, **two were deliberately kept** (A-09) as the sole executable record of those divergences, and a **third** was kept beyond the plan's letter — `test_trailing_content_after_the_token_degrades_to_a_verification_failure`, because no survivor covers a `Bearer <token> extra` value and the comma-joined case provably does not. The rest were deleted as assertions about Starlette's behaviour rather than this codebase's.
>
> **What this phase did NOT resolve, per the 37.1–37.4 convention.** **(1) `tables/__init__.py` still re-exports the `schemas/` names** — `ChatRequest`, `ChatResponse`, `ExamplesResponse`, `MessageRequest`, `MessageResponse`, `AnalyzeInput`, `AnalyzeResponse`, `FollowUpInput`, `FollowUpResponse`, `Issue`, `RejectResponse` — which `37.5-PATTERNS.md` lists as this phase's layering work. No plan claimed it and none did it; it is layering, not prose, and it is **open**. **(2) `tests/unit/test_app_wiring.py::TestEveryRouteIsAuthenticated` has no negative control against an injected undeclared route**, and three vacuous cases stand in `test_quota_resolver.py` — see the FOUND-03 correction and § Phase 37.5 records. **(3) One test case was deleted whose architectural guarantee no survivor carries** — `test_foundation_calls_no_adapter_method_anywhere_in_src` — a real coverage loss, recorded and not restored. **(4) A runtime string at `tests/schema/conftest.py:56`** still calls the database *"a crud"* (`"refusing to interpolate {name!r} as a crud identifier"`), left by plan `37.5-09` because changing it would have changed the AST under that plan's no-logic-change proof. **(5) The database pool finding (A-15)** — recorded, costed, and deliberately not fixed. **(6) `errors.py` at 397 measured lines** against 37.4 A-01's projection of roughly 250, and `37.4-REVIEW.md`'s warnings that fell outside moved or swept code. All six are detailed in § Phase 37.5 records.
>
> **The consequence D-09 accepted, restated here because it is the one behaviour this phase gave up:** a defective error tree **no longer refuses to boot**. It fails in the test run instead. Deploying without running the tests turns a refused start into served 500s. The mitigating fact, verified: `pyproject.toml:58`'s `addopts` deselects only `e2e` and `schema`, so the rehosted checker runs in the **default** `pytest` invocation and not behind a marker.
>
> **Amended again by Phase 38 (2026-09-01) for the milestone's audit removal.** The obligation Phase 37.1 flagged forward under SYNC-03 was decided rather than carried: **option (b)** — no durable `audit.auth_events` row is rebuilt for `POST /auth/sync`, and no success event is added, because `RequestLoggingMiddleware` already writes one `request` line per attempt and the shared error handler already writes one WARNING per rejection (D-01/D-02). The obligation was removed at its source rather than diverged from: plan 38-04 struck § "Audit" in full from `SHARED-INVARIANTS.md` along with the § "Errors" clause naming `core.auth_event_result`, relocating that section's two live non-audit rules into § "Fail-closed defaults" (D-03). **SYNC-03 is amended** — its subject, attempt telemetry, still exists as the middleware line, so it is not withdrawn. The same stroke settles the **audit half** of the three siblings Phase 37.1 flagged forward on the same mechanism: **SIGNOUT-02** (46), **APPLEHOOK-02** (43) and **PLAYHOOK-03** (44), each carrying a dated note naming exactly which half is settled and which half still binds (D-04). **APPLEHOOK-02 and PLAYHOOK-03 are NOT closed** — § "Global deletions" still forbids wildcard or path-prefix provider-callback membership, those routes are still enumerated individually by exact path, and Phase 38 removes no clause from that section. **SIGNOUT-01 and SIGNOUT-02's fail-closed half are untouched and stay fully binding:** an indeterminate or failed revocation must never report success. **No requirement was deleted, withdrawn, unchecked or checked by this phase**, and for the first time in this file a flagged conflict is **resolved** rather than added — the § "Audit" conflict under FOUND-05 is void by removal, so the count below drops from six to five.
>
> **Amended again by Phase 40 (2026-09-02) for `POST /auth/upgrade-anonymous`.** Four decisions this phase made differ from what was asked for — three from its brief and one reversing the phase's own recorded decision — and all four are stated under UPGRADE-01 and UPGRADE-02 rather than by editing anything. **(1) The client-supplied target provider is gone (D-01)**, continuing Phase 37 D-12 and answering the question Phase 37 D-13 flagged forward; the server derives the provider solely from the mandatory Firebase Admin read. This is **one new flagged conflict**, against unamended text in `05-upgrade-anonymous.md`. **(2) `core.auth_operation` is shrunk to its four challenge-bearing labels (D-11)** and the `core.auth_challenges` CHECK that restated that membership is dropped; the three removed labels existed only for the audit table Phase 37.1 D-01 deleted. This is **not** counted as a conflict — it is **flagged forward to phases 45 and 46**, whose briefs name labels that no longer exist, following the treatment Phase 37.1 gave the same class of loss. **(3) The unbounded Firebase exposure is accepted and flagged (D-22)**, on the Phase 37 D-01 precedent for unlimited account creation; it is a consequence of the pre-existing Phase 35 D-05 `limits` override and is **not** a new conflict. **(4) D-18's test-credential mechanism was reversed** — nothing is minted or signed; a stored Google refresh token is exchanged and linked per run. That reverses a decision in `40-CONTEXT.md`, not a rule in the specification, so it is a ledger entry rather than a conflict. **The brief's rate-limit and audit obligations were already dead before this phase began** — the backend rate-limit engine by Phase 35 D-05, the audit subsystem by Phase 37.1 D-01, and the invariant requiring it by Phase 38 D-03 — so a later reader must not read `05-upgrade-anonymous.md:19`, `:29`, `:30`, `:66`, `:88` and `:89` as obligations this phase failed to meet. **UPGRADE-01 and UPGRADE-02 are marked met**, on `uv run pytest -q` at 857 passed and `uv run pytest -m 'e2e or schema' -q` at 337 passed (216 e2e + 121 schema), with `uv run ruff check src tests` clean. **The count below rises from five to six, and the set of known divergences from six to nine** — the new conflict is counted at both ends, and this phase's two forward-flagged items (the enum shrink, and the unbounded provider call) are counted only at the second, joining the `limits` override that was already the gap between the two numbers. The re-derivation, and the sections re-read to make it, are recorded under UPGRADE-01.
>
> **Amended again by Phase 41 (2026-09-03) for `POST /auth/claim-anonymous-grant`.** Four decisions this phase made differ from what `06-claim-anonymous-grant.md` asks for, and all four are stated under ANONGRANT-01 … ANONGRANT-03 rather than by editing the brief, which stays verbatim (D-19). **(1) The device gate is iOS DeviceCheck only (D-01)** — the Android Play Integrity branch and the web Cloudflare Turnstile branch, both scoped into this phase at `:7` and `:42`–`:45`, are deferred to another milestone; recorded under ANONGRANT-01 with what dissolves with them and why the deferral costs no migration. **(2) The database is checked before Apple is asked (D-03)**, against the brief's steps 8 and 9 (`:73`–`:78`) and its rule that no DB grant may substitute for or suppress the platform-gate read; recorded under ANONGRANT-02 with the mitigating fact that the check is repeated inside the locked transaction regardless. **(3) Only anonymous identities may claim (D-08)**, against `:51` and `:83`, which admit registered native claimants; recorded under ANONGRANT-03 with the fallback it gives up. **(4) A repeat claim is an idempotent 200 (D-09)**, against `:26` and `:78`, which say never idempotent success; recorded under ANONGRANT-03 with the two states that still refuse. **A fifth item is a conflict inside the specification itself and is resolved rather than flagged:** the brief's step 11 (`:80`) locks the target user before the grant set and `SHARED-INVARIANTS.md` § "Locks and transactions" forbids a user-row lock tier ahead of the grant locks — **the invariants win** (D-13), and the code does what the binding text says, so it is counted only in the set of divergences, under ANONGRANT-02. **Two further items are consequences, not conflicts of their own:** `verification_required` is not registered because every producer the brief gives it (`:83`) lives in the deferred web branch, and an absent or empty device token collapses into the framework's 422 rather than `proof_rejected` — the second is recorded as an uncounted divergence, since it is a real difference in what the client sees. **The unbounded Apple exposure is accepted and flagged (D-20)**, on the Phase 40 D-22 and Phase 37 D-01 precedent, and is **not** a new conflict. **The brief's rate-limit, audit, mode-partition, `claim_attempt_id`, HMAC-keyring and route-registry obligations were already dead before this phase began** — by Phase 35 D-05, Phase 37.1 D-01 with Phase 38 D-03, Phase 37.2, Phase 37.4 D-03, Phase 37.4 D-11 and Phase 37.1 D-06 respectively — so a later reader must not read `:16`, `:18`, `:19`, `:28`, `:29`–`:35`, `:55`, `:56`, `:58`–`:62`, `:67`, `:69`, `:73`, `:76`, `:80`, `:88` and `:89` as obligations this phase failed to meet; the inventory is under ANONGRANT-01. **ANONGRANT-01, -02 and -03 are marked met**, on `uv run pytest -q` at 950 passed, `uv run pytest -m e2e -q` at 226 passed and `uv run pytest -m schema -q` at 134 passed, with `uv run ruff check src tests` clean. **The count below rises from six to ten, and the set of known divergences from nine to sixteen** — the four new conflicts are counted at both ends, and this phase's three uncounted items (the lock-order precedence resolution, the 422 collapse, and the unbounded Apple read) are counted only at the second. The re-derivation, and the six invariant sections re-read to make it, are recorded under ANONGRANT-01.
>
> **Amended once more by Phase 41's code review (2026-09-03), after 41-05 had already closed the phase.** A standard-depth review of the phase's 38 changed files returned two blockers, both fixed and committed before the phase was marked complete, and the second of them **changed the shipped wire shape after the amendment above was written**. **(1) The race-loser arm crashed (`ed95eae`).** The arm called `session.refresh()` on `identity.user` and `identity.identity` to undo what a `rollback()` was believed to have expired. The premise was false: `app/dependencies.py::get_identity` resolves on its own short session and returns from inside it, so those rows are **detached** and were never members of the handler's session — `refresh()` on them raises `sqlalchemy.exc.InvalidRequestError`, which is not an `AppError` and so escaped `_complete`'s handler, answering **500** where D-13 requires 200 and leaving the challenge claimed-but-unconsumed. Reproduced against the installed SQLAlchemy before the fix, and both refreshes deleted. The `MissingGreenlet` failure that originally motivated them was an artifact of `tests/schema/test_claim_race.py` resolving the identity on the *same* session it handed to the service; that test now resolves on a closed session as production does, and asserts the caller's rows are detached. **Note for a later reader: the loser arm can correctly do nothing only because `expire_on_commit=False` (`app/lifespan.py:53`) keeps a detached row's attributes loaded.** That setting is load-bearing here and is documented nowhere else. **(2) The device gate did not bind its read to its write (`e2e18de`) — this supersedes the two-token body recorded above and throughout Phase 41's planning artifacts.** `AnonymousGrantClaimRequest` took independent `query_token` and `update_token` values, and two opaque DeviceCheck tokens cannot be tied to one device: device A's token as the query (never written, bit0 stays clear) and device B's as the update (re-writing a set bit returns 200) passed every account, so two devices bought unlimited free grants — defeating the very rule ANONGRANT-03 enforces. The two fields are now one `device_token` used for both Apple calls. **`41-01-PLAN.md`, `41-01-SUMMARY.md` and `41-RESEARCH.md` §564-565 still describe the superseded two-token body**; per D-19's convention they are left as written and corrected here, where a later reader finds it. **The suite counts quoted below are the ones the requirements were marked on and are left as the record of that moment; after both fixes the suite reads 952 unit, 226 e2e and 135 schema passed (1313 total, 0 failed, 0 skipped), `ruff check src tests` clean.** Net +3 cases, none removed. **Neither fix adds a conflict against the binding specification** — both restore properties the brief and the invariants already required — so the counts of ten and sixteen above stand unchanged. **Seventeen non-blocking findings remain open** in `41-REVIEW.md`, of which four are worth a later reader's attention: WR-01 (`except IntegrityError` is read as a race loss too broadly), WR-02 (Apple's `400 Failed to find bit state` is classified as a definitive `proof_rejected` rather than a 503 outage), WR-04 (the Apple round trips run with the request's database transaction open, against the pool rule this phase itself wrote into `AGENTS.md`), and WR-09 (`device_token` and `challenge_id` carry no upper length bound).
>
> **Amended again by Phase 42 (2026-09-03) for `POST /auth/claim-registered-grant`.** Six decisions this phase made differ from what `07-claim-registered-grant.md` or `06-schema-reference.md` asks for. All six are stated under REGGRANT-01 … REGGRANT-03. Neither specification file is edited, and no passage of either is amended (D-15). **(1) The device gate is skipped on the conversion path (D-02)**, against brief steps 7, 8 and 10 (`:52`, `:53`, `:55`), which gate every iOS claim; recorded under REGGRANT-02. **(2) The body carries one device token (D-04)**, against the brief's separate query token and update token at `:52`; recorded under REGGRANT-01. This is the collapse Phase 41's code review already made for the anonymous route, applied to a second route. **(3) Claimants are registered identities read from the stored row, with no live provider confirmation and no `verification_required` code (D-05)**, against brief steps 2 and 6 (`:47`, `:51`), the error-class list at `:67` and the confirmation-point hardening at `:73`; recorded under REGGRANT-01. **(4) Account uniqueness rests on the indexes the schema already carries, and `account_already_claimed` is not added (D-06)**, against the transaction's step 5 (`:61`), the class registration at `:25` and the uniqueness hardening at `:72`; recorded under REGGRANT-03. **(5) Three tables and everything only they need leave the single migration (D-07), and no hash, key or key version is built (D-08)**, against `06-schema-reference.md:287`, `:349` and `:357` with their semantics at `:1127` and `:1257`, and against brief step 3 (`:48`) and the transaction's steps 3, 4 and 5 (`:59`, `:60`, `:61`); recorded under REGGRANT-02. **(6) The refusal for an account holding another active grant carries no end date (D-09(b))**, against `held_grant_ends_at` at `:25`, `:58` and `:67`; recorded under REGGRANT-03. **The unbounded Apple exposure on this route is accepted and flagged (D-16)**, on the Phase 41 D-20 and Phase 40 D-22 precedent, and is **not** a new conflict. **The brief's rate-limit, vendor-budget, audit, mode-signal, `claim_attempt_id`, HMAC-keyring, route-registry and Firebase-confirmation obligations were already dead before this phase began**, by Phase 35 D-05, Phase 37.1 D-01 with Phase 38 D-03, Phase 37.2, Phase 37.4 D-03, Phase 37.4 D-11, Phase 37.1 D-06 and Phase 41 D-08 respectively; the inventory, by line, is under REGGRANT-01. **REGGRANT-01, -02 and -03 are marked met**, on `pytest -q` at **1001 passed**, `pytest -m e2e -q` at **237 passed** and `pytest -m schema -q` at **147 passed**, with `ruff check src tests` clean. **The count below rises from ten to sixteen, and the set of known divergences from sixteen to twenty-three** — the six new conflicts are counted at both ends, and this phase's one uncounted item, the unbounded Apple read on this route, is counted only at the second. The re-derivation, and the six invariant sections re-read to make it, are recorded under REGGRANT-01.
>
> **Amended again by Phase 43 (2026-09-04) for `POST /webhooks/app-store`.** Three decisions this phase made differ from what `08-webhook-app-store.md` asks for. All three are stated under APPLEHOOK-01. Neither `08-webhook-app-store.md` nor `SHARED-INVARIANTS.md` is edited, and no passage of either is amended (D-27). **(1) The router is always registered, and an incomplete configuration answers 503 (D-02)**, against `:26` and `:49`, which require that the route not be registered at all while the store integration is unconfigured and that startup fail closed. **(2) The refusal body is the shared one-field shape (D-04)**, against `:7`, `:21` and `:32`, which say the route never emits the shared client-visible error classes; the classes are the route's own and no `ErrorCode` member was added, but every status this application answers is mapped to a class, so a body-less status would need a second response path. **(3) The gateway carries no per-IP and no per-URL limit (D-06)**, against `:24`, `:50` and `:51`; recorded as a **flagged deferral** to the v2.1 gateway contract on the Phase 35 D-05 and D-08 precedent, and counted, because it is a knowing divergence from unamended text. **APPLEHOOK-02 is ANSWERED AND CLOSED (D-01)** — the provider-callback partition is a dedicated `APIRouter`, membership is the set of routes on it, and `tests/unit/test_app_wiring.py`'s `PROVIDER_CALLBACK_PATHS` literal makes it countable, so § "Global deletions" is satisfied by exact-path registration rather than by machinery. **PLAYHOOK-03 inherits that mechanism** and adds one route and one literal member rather than a second partition. **D-09 is recorded as a divergence from Apple's production guidance and is in NEITHER count**, because the binding specification mandates no online revocation check; its measured cost is that with online checks off the certificate-validity window is evaluated at the payload's own claimed signing date, so leaf expiry is not enforced against wall-clock time. **The unbounded unauthenticated request cost is accepted and flagged (T-43-08)**, on the Phase 35 D-05 and D-08 precedent, and is recorded as **wider** than the three residuals under UPGRADE-01, ANONGRANT-01 and REGGRANT-01, each of which needs a valid token for an existing account first, whereas this one needs nothing; it is **not** a new conflict. **The brief's rate-limit, vendor-budget, audit, operation-metadata, route-registry and foundation-store-seam obligations were already dead before this phase began**, by Phase 35 D-05 and D-08, Phase 37.1 D-01 with Phase 38 D-03, Phase 40 D-11, Phase 37.1 D-06 and D-10, and Phase 37.2 D-09 respectively; the inventory, by line, is under APPLEHOOK-01. **APPLEHOOK-01 and APPLEHOOK-02 are marked met**, on `pytest -q` at **1089 passed**, `pytest -m e2e -q` at **272 passed** and `pytest -m schema -q` at **182 passed**, with `ruff check src tests` clean, all four commands run in plan 43-06 rather than copied. **The count below rises from sixteen to nineteen, and the set of known divergences from twenty-three to twenty-seven** — the three new conflicts are counted at both ends, and this phase's one uncounted item, the unbounded unauthenticated request cost, is counted only at the second. The re-derivation, and the four invariant sections re-read to make it, are recorded under APPLEHOOK-01.
>
> **Nineteen conflicts against the binding specification are recorded as FLAGGED, not resolved** — under FOUND-01 (Phase 37.1 D-06 against its pre-handler-barrier rule), under CREATE-01 (Phase 37.2 D-08, Application Default Credentials against its no-ambient-credential rule), three added by Phase 37.4: two under CREATE-02 (D-11's plaintext pre-auth subject and D-03's dropped attempt-id consumption condition, both against `02-create-user.md`) and one under CREATE-04 (D-06's provider-account collapse), **one added by Phase 40 on 2026-09-02: D-01's removal of the client-declared target provider, against `05-upgrade-anonymous.md:26`/`:46`/`:53`/`:56`/`:60`/`:61`/`:81` (under UPGRADE-01)**, and **four added by Phase 41 on 2026-09-03, all against `06-claim-anonymous-grant.md`: D-01's iOS-only device gate against `:7`/`:42`–`:45`/`:70`/`:75`/`:76` (under ANONGRANT-01), D-03's database-before-Apple ordering against `:73`–`:78` (under ANONGRANT-02), D-08's anonymous-only claimant against `:51`/`:83` and D-09's idempotent repeat against `:26`/`:78` (both under ANONGRANT-03)**, and **six added by Phase 42 on 2026-09-03: D-04's single device token against `07-claim-registered-grant.md:52` and D-05's stored-row claimant with no live provider confirmation against `:47`/`:51`/`:67`/`:73` (both under REGGRANT-01); D-02's device gate skipped on the conversion path against `:52`/`:53`/`:55` and D-07 with D-08's schema deletion against `06-schema-reference.md:287`/`:349`/`:357`/`:1127`/`:1257` and `07-claim-registered-grant.md:48`/`:59`/`:60`/`:61` (both under REGGRANT-02); D-06's account uniqueness resting on the existing indexes against `:25`/`:61`/`:72` and D-09(b)'s refusal carrying no end date against `:25`/`:58`/`:67` (both under REGGRANT-03)**, and **three added by Phase 43 on 2026-09-04, all against `08-webhook-app-store.md` and all under APPLEHOOK-01: D-02's always-registered router with a 503 on an incomplete configuration against `:26`/`:49`, D-04's shared one-field body against `:7`/`:21`/`:32`, and D-06's absent per-IP and per-URL gateway limits against `:24`/`:50`/`:51`**. **A further one stood under FOUND-05 until 2026-09-01 — Phase 37.1 D-01 against § "Audit" — and is now resolved by removal:** Phase 38 (D-03) deleted § "Audit" from `SHARED-INVARIANTS.md` rather than carry the divergence, so no invariant text is left to diverge from. Its entry under FOUND-05 is **kept as a resolved record, not deleted**, and still states what the removed text used to require. `SHARED-INVARIANTS.md` binds every phase and wins over any phase brief; it has now been amended twice — by the developer on 2026-08-30 to remove the exactly-one-`Authorization` wire contract, and by Phase 38 on 2026-09-01 to remove § "Audit" and the § "Errors" `core.auth_event_result` clause — and **none of the ten surviving flagged conflicts is against text either removal touched**. Per the standing rule from `37-CONTEXT.md`, such conflicts are flagged so a later reader finds the divergence rather than discovering it. **This count read six and the set of known divergences nine from 2026-09-02 until 2026-09-03; it read ten and sixteen after Phase 41, sixteen and twenty-three after Phase 42, and now reads nineteen and twenty-seven.** The arithmetic has always been unequal at the two ends, and the gap is now **eight** rather than seven. **The eight uncounted divergences are:** (1) the `limits` mandate from § Rate limits, recorded at `:50` and under FOUND-06 as a Phase 35 **override** and deliberately left in that category rather than re-filed as a conflict; (2) Phase 40's `core.auth_operation` shrink, forward-flagged because its consequence lands on phases 45 and 46; (3) Phase 40's unbounded Firebase read, a consequence of the `limits` override rather than a divergence of its own; (4) Phase 41's lock-order item, which is a conflict **inside the specification** — brief against invariants — **resolved by precedence** in the invariants' favour, so the code diverges from the brief while obeying the binding rule (under ANONGRANT-02); (5) Phase 41's collapse of an absent or empty device token into the framework's 422 rather than `proof_rejected`, recorded as a divergence because the client sees a different status, and uncounted because it applies an existing project convention rather than making a fresh decision against the brief (under ANONGRANT-01); (6) Phase 41's unbounded Apple read, recorded on the Phase 40 D-22 precedent as a consequence of the same `limits` override (under ANONGRANT-01); (7) Phase 42's unbounded Apple read on the registered route, recorded on the same precedent and for the same reason (under REGGRANT-01); and (8) **Phase 43's unbounded unauthenticated request cost on `POST /webhooks/app-store`**, recorded on the same precedent and for the same reason (under APPLEHOOK-01) — **wider than items (3), (6) and (7), because those three each need a valid token for an existing account and this one needs no credential at all**. **A ninth divergence is deliberately in neither number, and a reader must not mistake its absence for its non-existence: Phase 43 D-09's departure from Apple's production guidance** — online certificate revocation checks are off, so the certificate-validity window is evaluated at the payload's own claimed signing date. It joins neither number because the binding specification mandates no online check, so there is no specification text to diverge from; it is recorded in full under APPLEHOOK-01. **The gap is not an error to be fixed:** it is the difference between a counted conflict and a recorded override, forward flag or precedence resolution, and it has been kept deliberately since Phase 37.5. A reader auditing divergences must therefore read the count **and** the override at `:50` **and** the forward flags under UPGRADE-01, SCHEMA-01, ANONGRANT-01, ANONGRANT-02 and REGGRANT-01 **and** the Apple-guidance divergence under APPLEHOOK-01. Phase 37.5 (A-16) re-derived these numbers as six and seven; Phase 38 resolved one conflict and added none, taking both one lower; Phase 40 added one conflict and three uncounted divergences; Phase 41 added four conflicts and three uncounted divergences, and resolved none; Phase 42 added six conflicts and one uncounted divergence, and resolved none; Phase 43 added three conflicts and one uncounted divergence, resolved none, and **closed one forward flag** — APPLEHOOK-02, open since 2026-08-24.

## v2.0 Requirements

### SCHEMA — Phase 34 (`00-schema.md`)

- [x] **SCHEMA-01**: The single initial migration `migrations/20260818_01_initial-release.sql` replaces the deleted prior migration and creates the complete v2.0 schema in one apply against an empty database — no incremental migration files are added
  > **Upheld against a later conflicting decision (Phase 37, plan 37-01 Task 1, 2026-08-22).** `37-CONTEXT.md` D-13 called for "a new migration" to drop `core.auth_challenges.operation_variant`. That mechanism was rejected in favor of this requirement: the initial migration is **edited in place** and the disposable dev/test database is dropped and re-applied. SCHEMA-01 is unamended and `tests/schema/test_apply_rollback.py::test_exactly_one_sql_file` stays green unmodified. The consequence for any future v2.0 schema change is the same: edit in place, re-apply, never add a file.
  >
  > **Upheld a second time, and the file edited in place again (Phase 40, plan 40-01, 2026-09-02).** D-11 removed three labels from `CREATE TYPE core.auth_operation` — `sync`, `restore_subscription` and `sign_out_all` — and deleted the `core.auth_challenges` CHECK that restated the four-value membership, because the type itself now carries it. PostgreSQL has no `ALTER TYPE … DROP VALUE`, so there is no incremental migration that could express this even if SCHEMA-01 permitted one. The single migration was edited in place and both disposable databases were re-applied: `ns_schema_test` by the schema suite's own session fixture, and the dev database `nativespeaker` by hand after the destructive rebuild was authorised by the developer at plan 40-01's blocking decision gate — the second the same session fixture cannot reach, and the reason WINDOWS entry 10 was open for three minutes. **Why the file now shows a shape that never ran anywhere in its earlier form:** a reader diffing `migrations/20260818_01_initial-release.sql` sees a seven-label enum become four and a CHECK disappear, with no migration recording the transition, because under SCHEMA-01 there is nothing for such a record to live in. The same reviewer-confusion cost the Phase 37 note above discharged once is discharged here a second time; **the third such edit should expect this note to be extended rather than a new mechanism proposed.**
  >
  > **FLAGGED FORWARD to phases 45 and 46 (Phase 40 D-11, 2026-09-02) — recorded, NOT resolved, and deliberately not counted as a conflict.** `00-schema.md:190` still reads that `core.auth_operation` *"lists all seven state-changing operations, including `restore_subscription`, `sign_out_all`, and `sync`"*; `10-restore-subscription.md:25` still names `restore_subscription` as a `core.auth_operation` value and `11-sign-out-all.md:25` still names `sign_out_all`; `01-foundation.md:137` still maps phases 03, 10 and 11 onto the three removed labels. None of that text is amended. **The labels' only consumers were already gone before this phase:** `audit.auth_events` and `core.auth_event_result` were deleted by Phase 37.1 D-01, and the route registry that carried the route→operation metadata by Phase 37.1 D-06, leaving `core.auth_challenges.operation` as the type's sole consumer — and restore and sign-out are explicitly **not** challenge-bearing, by their own briefs. So nothing in the product lost a value it could use. What phases 45 and 46 inherit is a naming question, not a mechanism: their briefs name an enum value that does not exist, and each must decide for itself whether it needs one at all. **This is filed as a forward flag rather than a counted conflict on the Phase 37.1 precedent** — the same phase deleted the whole of `audit.auth_events` against `00-schema.md` and that was recorded by **withdrawing** SCHEMA-06, with the counted conflict filed against `SHARED-INVARIANTS.md` § "Audit" instead. `SHARED-INVARIANTS.md` says nothing about `core.auth_operation`, before or after Phase 38's edit, so there is no invariant text here to diverge from. **Accepted cost, in the developer's own terms (D-11):** for one phase each, a client can obtain a `claim_anonymous_grant` or `claim_registered_grant` handle for an endpoint that does not exist yet; it expires unspent in 300 seconds, nothing can consume it, and the account-less caller is still refused by plan 40-06's condition. That was chosen over maintaining a written list of issuable operations because the concern being optimised for is **fewer copies of one fact**, not tidiness — a dead handle costs nothing, a list has to be kept in step forever. **Second-order effect, recorded by Phase 40 UAT (WR-02, 2026-09-02):** because the two grant operations now answer 200 where every unknown string answers 400, `/auth/challenge` *can* be asked which operation names are real — the property `tests/unit/test_challenge_endpoint.py`'s outside-the-vocabulary class was written to protect. D-11's acceptance above covers the unspendable handle but never named this. Accepted on the same terms: the four labels are already public in the committed migration, so the oracle discloses nothing a reader of the repo does not have.

- [x] **SCHEMA-02**: `core.users`, `core.external_identities`, and the `core.identity_provider` enum support `(issuer, subject)` → user resolution, with `identity_state` and permanently-retained tombstone rows
- [x] **SCHEMA-03**: `core.access_grants`, `core.access_tiers`, and `core.user_monthly_usage` enforce at most one active grant per user, with monthly usage keyed by grant id
- [x] **SCHEMA-04**: `core.subscriptions`, `core.store_purchases`, and `core.store_purchase_tokens` support both stores, with `product_entitled_subscription_id` as a STORED generated column over `('active','grace_period')`
- [x] **SCHEMA-05**: `core.auth_challenges` supports the claim/consume protocol for challenge-bearing operations
- [x] **SCHEMA-06** — **WITHDRAWN.** Delivered by Phase 34 exactly as written, and true until 2026-08-24: `audit.auth_events` enforces the actor-field CHECK constraints and the `core.auth_operation` / `core.auth_event_result` enums in full
  > **Withdrawn by Phase 37.1 (D-01), 2026-08-24.** The `audit.auth_events` table, its nine CHECK constraints and its four `ix_auth_events_*` indexes were deleted from the initial migration, along with the `core.auth_event_result` type. Nothing this requirement asserts is checkable any more, so it is withdrawn rather than reworded into something true — the schema it describes really did ship, and editing that away would falsify Phase 34's own delivery. `tests/schema/test_constraints.py`'s `TestAuthEventAuditConstraints` class (6 cases) went with it.
  >
  > **Only half the enum clause fell. `core.auth_operation` survives** — `core.auth_challenges.operation` stores it and `auth/creation.py` uses the matching `AuthOperation` enum — as do the `audit` **schema** and `audit.subscription_events` (D-02), so phases 43 and 44 still have a recorded home for store notifications. Only `core.auth_event_result` was dropped.
  >
  > **Consequence for later phases:** no phase may assert anything about `audit.auth_events`. One-way: restoring the table means re-editing the single initial migration under SCHEMA-01.
  >
  > *Corrected by Phase 37.3 (D-12), 2026-08-26.* This note previously read that `AuthEventResult` "survives in Python as a plain 44-member `StrEnum` backed by no database type (D-04)". It does not survive: all 44 members were deleted from `models/auth.py`. The internal rejection vocabulary is the `AuthRejected` exception-class family in `auth/exceptions.py` now, and the class name snake_cased is the structured log event. Nothing persisted changed — the enum had been backed by no database type since this phase-37.1 deletion, so there is no column and no migration. `AuthOperation` is unaffected and still backs `core.auth_challenges.operation`.

- [x] **SCHEMA-07**: Legacy structures are gone — `core.users.jwt_sub`, `core.users.subscription_plan`, `core.usage_monthly`, `core.subscription_events`, the `core.subscription_plan` enum, and the `promo` grant source
- [x] **SCHEMA-08**: Every acceptance check in `00-schema.md §10` passes against a freshly migrated database

### FOUND — Phase 35 (`01-foundation.md §1–§4, §6, §7`)

Shared machinery only. Rebinding the pre-existing routes is Phase 36.

> Scope narrowed by the Phase 35 discussion (`35-CONTEXT.md`): `§5` (backend rate-limit engine) is **deleted from the product** — Envoy Gateway is the sole request-rate enforcement point (D-05), overriding `SHARED-INVARIANTS.md` § Rate limits and `01-foundation.md §5`. `§9` (the Envoy contract) is **deferred to v2.1** (D-08). The `§7.1` provider-call budget seam survives as FOUND-06 (D-06).

- [x] **FOUND-01**: A mandatory, default-on pre-handler dependency — declared on every non-public `APIRouter` and again on each endpoint — is the only place JWT acceptance and identity resolution happen; it admits only `identity_state='active'` AND `users.active` exactly TRUE
  > **Mechanism amended by Phase 37.1 (D-06/D-07/D-11), 2026-08-24. Substance unchanged — every property this requirement asserts still holds.** The pure-ASGI `AuthBarrierMiddleware` was deleted; `get_request_context` now does the work. It is **mandatory** and **default-on** (declared on the `APIRouter`, so an endpoint that forgets its own `Depends` is still authenticated), it runs **before the handler**, and it is the **only** place JWT acceptance and identity resolution happen. FastAPI's per-request dependency cache resolves the router-level and endpoint-level declarations to one execution. Only the word "barrier" stopped being true.
  >
  > The admission rule above is carried **verbatim** and is not amended. `auth/verification.py`, `auth/wire.py` and `auth/identity.py` were kept and re-hosted rather than rewritten, so the admission matrix moved intact: `tests/e2e/test_admission.py`'s 23 cases passed the swap with **zero** edits to any status code or body literal.
  >
  > *Module names corrected by Phase 37.5 (D-01/D-04/A-06), 2026-08-31 — pointer only; every property this requirement asserts still holds.* `auth/identity.py` no longer exists. `Identity` lives in `schemas/auth.py` and `resolve_identity` in `crud/identities.py`, and **its four rejections stayed with the query** — `PreAuthIdentityNotAllowed`, `IdentityUnresolvable`, `HistoricalIdentity` and `BlockedUser` — because splitting them off would create a caller that can read a broken link as an unlinked pair, the exact failure the deleted `auth/identity.py:44` comment warned about. The admission rule itself is still in **one** place, `app/dependencies.py::get_identity`, which is what § The barrier requires; it was deliberately not moved into a service.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved.** `SHARED-INVARIANTS.md` § "The barrier — the only place identity happens" still reads *"JWT acceptance and identity resolution happen ONLY in the shared, mandatory, default-on pre-handler **barrier**"*, and still requires that *"Every registered route sits in exactly one of three categories … The CI/startup route-enumeration assertion must pass"*. That document binds every phase and wins over any conflicting phase brief, and **it has not been amended**. This project has knowingly diverged: the barrier is a FastAPI dependency, and the route registry and its enumeration assertion are gone (withdrawn just below). Recorded here per the standing rule from `37-CONTEXT.md` — conflicts against `SHARED-INVARIANTS.md` are flagged, never silently resolved — so a later reader finds the divergence rather than discovering it.
  >
  > **Credential extraction amended by Phase 37.4 (D-10/A-04/A-05), 2026-08-30. Every property this requirement asserts still holds; what changed is who parses the header.** The 43-line hand-rolled `auth/extract_bearer.py` was deleted and replaced by `fastapi.security.HTTPBearer(auto_error=False)`, declared once at module level and taken as a `Depends()` parameter on `get_identity`. A `None` credential still raises `InvalidExternalJwt` from this project's own code, so the **401**, the `auth_required` code and the `invalid_external_jwt` log event are all unchanged — D-10's premise that `HTTPBearer` answers 403 was **factually wrong** and was withdrawn as A-05 after the installed FastAPI 0.135.1 was read and executed against six header shapes. The dependency is still mandatory, still default-on, still pre-handler, and still the only place JWT acceptance and identity resolution happen.
  >
  > **What the wire gave up, measured against fastapi 0.135.1 / starlette 0.52.1 and now asserted as named cases in `tests/unit/test_identity_accessors.py::TestTheWireContractTheFrameworkNowEnforces`.** Five shapes moved, with three distinct causes:
  >
  > | Shape | Was | Is now | Cause |
  > |---|---|---|---|
  > | duplicate `Authorization` field instances | `duplicate_authorization` rejection | **the first value is used and the rest hidden** | the framework reads the header through a mapping that returns the first value (D-10) |
  > | `Bearer A, Bearer B` (comma-joined) | `duplicate_authorization` rejection | the whole string is verified and fails as `bad_signature` | same (D-10) |
  > | line-folded value | `duplicate_authorization` rejection | not distinctly rejected | same (D-10) |
  > | `Bearer␣␣␣TOKEN␣␣` (padding) | `malformed` rejection | **accepted, and authenticates** | `get_authorization_scheme_param` calls `param.strip()` — a **loosening**, whose cause differs from the first/last resolution above (A-09) |
  > | `Bearer TOKEN extra` (trailing content) | `malformed` rejection | verified as the literal `"TOKEN extra"`, fails as `bad_signature` | concatenation — **found during plan `37.4-06`, enumerated by neither D-10 nor A-09** |
  >
  > The bounded reason vocabulary narrowed with them: `BoundedReason` moved to `auth/jwt_verifier.py`, and `missing_token`, `malformed` and `duplicate_authorization` were deleted, leaving the **five** the verifier still produces (A-04, correcting D-10's claim that all eight die). `InvalidExternalJwt` keeps its `bounded_reason` field for the verification arm.
  >
  > **The developer's framing, recorded so a later reader can re-examine it rather than rediscover it:** *"I would rather sacrifice a requirement than bloat my code."* The mitigating factor named at the time was that Envoy sits in front and normalises headers. **That mitigation is unverified, and plan `37.4-06` established that it is unverifiable from this repository's configuration.** `k8s/templates/security-policy.yaml` does ship a `SecurityPolicy` with a Firebase JWT provider bound to the app-routes and llm-routes `HTTPRoute`s — so RESEARCH assumption A5's premise that no Envoy configuration exists here was false — but **no header-normalisation filter ships anywhere in the chart**, `service.type` is `ClusterIP` with no `NetworkPolicy`, and `SHARED-INVARIANTS.md` § "Wire contract" says verbatim that *"Envoy's JWT filter is defense-in-depth only; no backend correctness depends on it"*, which is exactly the reliance this mitigation would need. The residual is an in-cluster caller reaching the pod directly. FOUND-09 (the Envoy contract) stays deferred to v2.1.
  >
  > **Why this is a record and not a flagged conflict, and what is left unresolved.** Phase 37.4 planned to flag D-10 and A-09 as two conflicts against `SHARED-INVARIANTS.md` § "Wire contract". Before this plan ran, the developer instead **deleted** the sentence asserting that rule from that document, and **deleted FOUND-02 outright** from this file — the requirement reading *"The auth dependency enforces the exactly-one-Authorization wire contract — zero values, duplicate field instances, comma-joined values, multiple credentials, empty tokens, and trailing content all reject as `invalid_external_jwt`"*. There is no longer text in the binding invariants for the code to diverge from, so nothing is flagged against it; the property is recorded above as knowingly given up. **Reported, not resolved:** the same rule survives verbatim and unamended in four phase briefs — `01-foundation.md:40` (*"exactly one `Authorization` field value carrying exactly one well-formed Bearer credential"*), its rejection list at `:42-46` and its bounded-reason vocabulary at `:260`; `02-create-user.md:81` (*"zero/multiple/comma-joined values, empty token, or trailing content reject — never take first/last"*) and `:69`, which names *"duplicated Authorization"* as a cause of `invalid_external_jwt`; `06-claim-anonymous-grant.md:86`; and `11-sign-out-all.md:42`. Whether those follow `SHARED-INVARIANTS.md` is a specification decision this phase was given no direction on and does not take — it is surfaced here so the next reader meets it rather than discovers it.

> **FOUND-03 was withdrawn outright by Phase 37.1 (D-06), 2026-08-24 — it is the one requirement this project has deleted rather than amended.** It required a route registry placing every registered route in exactly one of three categories plus a startup/CI enumeration assertion, and `auth/registry.py`, the assertion and `tests/unit/test_route_registry.py` were all deleted; nothing of it survives to amend. The fail-closed property its first two conditions protected is now obtained **structurally** instead of by assertion — the router a route is registered on *is* its declaration, so there is no second table left to disagree with, and `tests/unit/test_app_wiring.py::TestEveryRouteIsAuthenticated` asserts the property directly over `app.routes` (negative-controlled against an injected undeclared route). What was genuinely lost: the closed provider-callback category, flagged forward to phases 43 and 44 under APPLEHOOK-02 and PLAYHOOK-03. The conflict this creates with `SHARED-INVARIANTS.md` is flagged under FOUND-01.
>
> *Count corrected by Phase 37.4, 2026-08-30.* This note's opening claim — that FOUND-03 is "the one requirement this project has deleted rather than amended" — was true until this date and is now off by one. **FOUND-02 was deleted outright** at the developer's direction, making two. Everything else the note states is unchanged.
>
> ⚠️ *Correction by Phase 37.5, 2026-08-31 — the parenthesis above is false and has been since it was written.* This note says `TestEveryRouteIsAuthenticated` asserts the property *"(negative-controlled against an injected undeclared route)"*. **It does not, and no such control exists anywhere in the repository.** Plan `37.5-08` read the class: what it holds is `test_the_public_allowlist_is_exactly_the_readiness_probe`, which fails if the unauthenticated set ever *widens*. That is a real control, but a different one — it catches a route added **without** a declaration; it does not catch a gate that enumerates nothing and therefore reports nothing. A `_api_routes()` that silently returned `[]` would pass all four cases in the class. The property FOUND-03 gave up is still obtained structurally and the assertion still runs, so nothing regressed; what was overstated is the assurance. **This is recorded as open, not fixed.** Plan `37.5-08` was a pure-deletion plan and adding the control would have added assertion lines and broken its own no-weakening gate; plan `37.5-09` did not name the file. Restoring it means one case injecting an undeclared `APIRoute` into a copy of the router and asserting the gate reports it.

- [ ] **FOUND-04**: One shared error-registry module owns the single client-visible response shape, statuses, copy, and exception handlers; within an error class the body, status, and copy are identical across every triggering branch
  > **Mechanism amended by Phase 37.4 (D-08/D-09/A-01), 2026-08-30. Substance unchanged — every property this requirement asserts still holds, and no conflict arises.** Only the word *registry* stopped being literally true. `ServiceError`, `AuthRejected`, `ErrorClass`, `REGISTRY` and `STATUS_TO_CLASS` are deleted, along with the 16 module-level error-class constants and `auth/exceptions.py`. One exception tree rooted at `AppError` lives in `src/nativespeaker/api/errors.py` — **45 classes carrying exactly the 16 codes** the `ErrorCode` Literal declares, asserted in both directions — and each class carries its own status and code rather than pointing at a registry entry that held them. One module still owns the shape, the statuses and the copy; **one** response builder (`app_error_handler`) is reached by four handler registrations; and a class appended anywhere in that tree is appended to that one module. The response body is still exactly one field and an exception's message never reaches it, which matters because several classes carry server-side-only diagnostic detail.
  >
  > **The anti-oracle clause is now structural rather than asserted.** Where several classes deliberately share one client answer — the five challenge rejections' single 409, and D-05's two account-unavailable classes' single 403 — the answer is **declared once on a base and no leaf re-declares it**, so the branches cannot drift apart into an enumeration oracle. `tests/unit/test_rejection_vocabulary.py` asserts that none of those seven leaves carries `status` or `code` in its own `vars()`.
  >
  > **The startup totality check survived the loss of the thing it walked.** `assert_registry_total()` walked the registry; `assert_tree_total()` walks `AppError.__subclasses__()` recursively and makes five assertions: no code is declared at two different statuses; the codes the tree carries equal `get_args(ErrorCode)` in both directions; every leaf has a declaring strict ancestor below the root; every class declares `status` and `code` **together or neither** (the replacement for the frozen-pair guarantee `ErrorClass` gave); and the classes marked `answers_framework_status` yield exactly one class per framework status. The first assertion is deliberately **not** "no two classes claim the same code", which is unsatisfiable against a tree where `service_unavailable` is legitimately declared by two classes and `internal_error` by seven. The property the registry actually guaranteed, and the one whose violation is client-visible, is that a code is never bound to two different statuses.
  >
  > **Two fail-closed defaults are load-bearing and were preserved deliberately.** A class declaring neither status nor code still answers **500 `internal_error`**, and a class that is silent today stays silent: `log_level = None` is declared on the generic bases so that twelve previously-silent classes — including a routine 404 — did not start emitting a WARNING per occurrence when the merged base's default became `logging.WARNING`.
  >
  > **Amended by Phase 37.5 (D-09/D-10/A-11/A-12), 2026-08-31 — the totality check left production code. Every property this requirement asserts still holds; what changed is *when* a defect is caught.** The paragraph above says *"The startup totality check survived the loss of the thing it walked"*, and that was true until this date. `errors.py:65-131` — `_undeclared`, `_tree_problems` and `assert_tree_total`, 67 lines — plus the call at `app/lifespan.py:29-30` are deleted; the checker is `tests/unit/error_tree.py` (73 lines), a module and not a `conftest.py`, because five existing cases invoke it from a bare `python -c` subprocess that needs an explicit `PYTHONPATH`. **The five assertions are unchanged and byte-identical**, as are the three message strings seven negative controls match substrings of; `tests/unit/test_error_registry.py` already held all four totality properties before the move (A-11/P-06), so no assertion was written to replace one. `errors.py` is **495 → 399** lines.
  >
  > **What this gives up, knowingly (D-09).** A defective error tree no longer refuses to boot; it fails in the test run. Deploying without running the tests turns a refused start into served 500s. `assert_tree_total` was the last startup self-check in `src/` — `warn_missing_older` went with the HMAC keyring and the route-enumeration assertion went in 37.1 — so there is no second instance. Mitigating and verified: `pyproject.toml:58`'s `addopts` deselects only `e2e` and `schema`, so the checker runs in the **default** `pytest` invocation.
  >
  > **Four dead classes were deleted and `ChallengeRequired` was kept — with one of D-10's two stated grounds withdrawn.** `InvalidCursorError`, `PageSizeLimitError`, `WebhookVerificationError` and `DatabaseNotInitializedError` had zero raise sites; none declared its own `code`, so the `ErrorCode` vocabulary is unchanged and the totality test is unaffected. `ChallengeRequired` was offered for deletion during the discussion and the offer was **wrong**. Per A-12, D-10's claim that deleting it would drop a mandated code is **withdrawn** — `ChallengeRejected` carries `challenge_required` too. The load-bearing ground is the other one: it declares `answers_framework_status = True` at 409, so `class_answering_status(409)` resolves to it and a bare framework 409 would otherwise become `error_registry_unmapped_status` and a **500**. `_family` and `class_answering_status` are live dispatch and were kept; `_family` now has one caller and will read as a deletable step to a later sweep — **it is not one, it is recursive** and cannot be inlined, which `AGENTS.md` § Function shape now says in a line.
  >
  > **`ChallengeRequired` no longer reads as dead to a grep**, which is the discrepancy that produced the wrong deletion offer: it had **zero** references by name outside its own definition while being load-bearing. Plan `37.5-04` added `test_the_409_answering_class_is_challenge_required_by_name`, asserting identity (`class_answering_status(409) is ChallengeRequired`) rather than the `(status, code)` pair the sibling case already covers, because a pair assertion would still pass if another class took the slot.

- [ ] **FOUND-05** — **WITHDRAWN.** As written: an audit writer emits exactly one durable `audit.auth_events` row per on-path attempt before the response returns, with redacted `details` and an HMAC-SHA-256 `actor_subject_hash` carrying its key version
  > **Withdrawn by Phase 37.1 (D-01), 2026-08-24.** `auth/audit.py`, the `AuditWriter`, `build_details`, `redact`, the `AuthEvent` model and all five call sites were deleted together with the table. **No auth audit row is written anywhere, on any path** — the requirement is not merely unmet, its subject no longer exists, so it is withdrawn rather than reworded. The developer's judgement, consistent with `AGENTS.md`: auditing every auth attempt to a dedicated Postgres schema is over-engineering for a pre-launch sub-$5/month grammar app; the subsystem was built because the specification mandates it, not because a threat model asked for it. One-way — restoring it means re-editing the single initial migration and rebuilding roughly 1,600 lines of code and tests.
  >
  > **What compensates, and what does not.** The rejection log was deliberately kept (D-03) — it lives in `app/dependencies.py::_reject`, folded into the dependency that raises the rejection by Phase 37.2 (D-10), 2026-08-25 — and still emits the `auth_rejected` structured security-log event carrying the stable internal result, the bounded reason and the route template on **every** rejection. That log line is now the **only** record a rejection leaves. There is no durable queryable trail, no success record, no redacted `details`, and no HMAC `actor_subject_hash` — so nothing derives attack volume or reconstructs an attempt after the fact except the deployment's log pipeline.
  >
  > *Mechanism corrected by Phase 37.3 (D-02/D-06), 2026-08-26 — the compensating record still exists; only its shape changed.* `_reject` and the single `auth_rejected` event are both gone. Every rejection now raises an `AuthRejected` subclass, answered by one handler in `app/errors.py` that emits **one** WARNING per rejection, named for the exception class snake_cased, carrying the matched route's path template and whatever bounded scalars that class declares. There is no single greppable rejection event any more; that was accepted knowingly, no dashboard or log pipeline being deployed. The substance of the paragraph above is unchanged — a rejection still leaves exactly one structured log line and still leaves no durable row — so the conflict flagged below stands exactly as recorded.
  >
  > **FLAGGED CONFLICT — RESOLVED BY REMOVAL by Phase 38 (D-03), 2026-09-01. The record is kept; only the divergence is gone.** From 2026-08-24 until this date this was a standing flagged conflict, and it stood on this text: `SHARED-INVARIANTS.md` § "Audit" mandated that *"Every on-path attempt writes exactly one durable `audit.auth_events` row for its terminal outcome, before the response returns"*, that *"Barrier rejections are first-class rows, never collapsed into a generic 401 log line"*, and that route metadata be *"readable BEFORE the barrier runs, so barrier rejections are audited with the operation set"*. This project knowingly diverged from all three, and the conflict was recorded here per the standing rule from `37-CONTEXT.md` rather than resolved.
  >
  > **What resolves it.** Plan 38-04 deleted § "Audit" in full from `SHARED-INVARIANTS.md`, together with the § "Errors" clause naming the `core.auth_event_result` type Phase 37.3 had already removed, and relocated that section's two live non-audit rules into § "Fail-closed defaults" — a rejection still leaves exactly one structured security-log line, and admission-phase rejections still write no per-rejection database row. **There is no surviving invariant text left to diverge from, so the conflict is void rather than outstanding.** Audit removal is a milestone-level decision by the developer (SYNC-03, D-01/D-03), and removing the invariant follows the precedent set in Phase 37.4, where the invariant asserting the wire contract was removed rather than carried as a permanent conflict. This paragraph is kept rather than deleted, and keeps stating what the removed text used to require, so the history of the divergence survives for a later reader instead of leaving a silent gap.
  >
  > **What is deliberately NOT changed, and a reader will meet it.** `01-foundation.md §4` still states the same obligation in phase form. The phase briefs under `specs/auth-refactor-phases/` are marked verbatim and are not edited by this project, so a reader who reaches the brief before the binding specification sees an obligation `SHARED-INVARIANTS.md` no longer carries. What settles it is the standing rule that `SHARED-INVARIANTS.md` binds every phase and wins over any phase brief — and it now carries no audit obligation at all.

- [ ] **FOUND-06**: A `§7.1` provider-call budget seam meters outbound provider calls per request with plain in-process counters — the 3-attempt Firebase `getUser` retry budget, plus a helper that checks every applicable budget non-destructively from broadest to narrowest and charges them together only on success; exhaustion maps to internal `firebase_lookup_unavailable` → client `verification_temporarily_unavailable`. No `limits` dependency, no Redis/Valkey, no traffic rate limiting
- [ ] **FOUND-07**: A challenge store implements the claim/consume protocol that challenge-bearing operations depend on
  > **The protocol's shape changed under Phase 37.4 (D-03/D-11), 2026-08-30 — pointer only; the two divergences are flagged under CREATE-02, where their spec text lives.** `ChallengesDB` takes no keyring argument, `claim_attempt_id` is gone from the column, the CHECK, the model, the store signatures and the route, and `preauth_subject_hash BYTEA` is now `preauth_subject TEXT`. The claim/consume protocol this requirement names still exists and still runs one direction only — issued → claimed → consumed, single-use — so the requirement stands as written.

- [x] **FOUND-08**: Adapter interfaces are defined as interfaces only — no store, device-check, or Firebase Admin implementations ship in this phase
  > **Amended by Phase 37.2 (D-09), 2026-08-25 — delivered exactly as written by Phase 35 and true until this date; what survives is the one seam the live code consumes.** Eight zero-consumer adapter types were deleted — `StoreAdapter`, `VendorProofAdapter`, `VerifiedNotification`, `VerifiedTransaction`, `StoreState`, `ClaimKind`, `DeviceBitState` and `RevocationOutcome` — together with `FirebaseAdminAdapter`'s two unconsumed Protocol methods, `verify_id_token` and `revoke_refresh_tokens`. Nothing regressed on the way out: every one of them had zero implementations and zero callers, so no behavior left with them. What remains in `auth/adapters.py` is the **providerData** surface the create-user flow actually calls — `ProviderDataOutcome`, `ProviderDataEntry`, `ProviderDataResult`, and a one-method `FirebaseAdminAdapter` carrying `get_user_provider_data`. `tests/unit/test_adapter_interfaces.py::TestNoProviderDependency` still holds: that seam imports no `firebase_admin`.
  >
  > **Flagged forward to phases 43, 44, 45 and 46 — the replacement is deliberately NOT designed here.** Each of those phases defines its adapter interface **beside its first implementation**, the same treatment Phase 37.1 gave the provider-callback category and for the same reason: designing a seam from outside the phase that owns it is how twelve unconsumed Protocols came to exist in the first place. The requirement's own principle still binds anything those phases declare ahead of building it — an interface ships with no implementation behind it in the phase that only declares it. APPLEHOOK-01, PLAYHOOK-02, RESTORE-01 and SIGNOUT-01 each carry the flag.
  >
  > **Amended by Phase 37.3 (D-09/D-19), 2026-08-26 — the surviving providerData surface named above was true until this date, and has now itself been replaced.** `ProviderDataOutcome`, `ProviderDataEntry` and `ProviderDataResult` are deleted. The seam carries **one** immutable value type, `VerifiedProviderIdentity(provider, provider_uid, email)`, frozen and slotted, and the read **raises** rather than returning an outcome to be translated: a `ProviderLookupError` hierarchy — `UserNotFound` (→ `AUTH_REQUIRED`), `Unavailable` (→ `VERIFICATION_TEMPORARILY_UNAVAILABLE`) and `NotLinked` (→ `OPERATION_NOT_ALLOWED`) — each carrying its own `error_class` and answered by one FastAPI handler. Classification and the email-copy rule moved inline into `firebase.py::_read`, behind the seam, so they live in exactly one place. `FirebaseAdminAdapter` is still the one-method Protocol; only its return type changed.
  >
  > **The requirement's actual content is still met, and that is what was checked.** `tests/unit/test_adapter_interfaces.py::TestNoProviderDependency` still holds: `auth/adapters.py` imports no `firebase_admin` and no provider SDK of any kind. **No client-visible behaviour changed** — every status, error class and body is byte-identical, gated by an unedited `tests/e2e/test_admission.py` and by a `tests/unit/test_create_user_precedence.py` carrying zero edits to any status code or body literal. FOUND-08 stays satisfied and is **not** unchecked.
  >
  > **The forward flag to phases 43–46 is carried unchanged**, with one addition from D-12: `AuthEventResult` — the 44-member internal outcome enum those phases' briefs reference — was deleted entirely by this phase. Phases 43, 44, 45 and 46 name their outcomes as **exception classes** in the `AuthRejected` family rather than re-growing an outcome enum, and they still define each adapter interface beside its first implementation.

> **FOUND-09 is deferred to v2.1** per D-08 — see Future Requirements below. Nothing in `k8s/` is touched by Phase 35.

> Phase 35 ends with a **booting** application (D-14): the model layer is repaired against the v2.0 schema, so imports and lifespan run at real startup against the real router. Chat and quota routes still fail at runtime until Phase 36 rewires them onto the grant model (D-15).
>
> *Amended by Phase 37.1 (D-06), 2026-08-24: this note also named the `§2.3` enumeration assertion running at real startup. That assertion was deleted with the route registry. The boot-clean half of D-14 is unchanged and still holds.*

### REBIND — Phase 36 (`01-foundation.md §8`)

- [x] **REBIND-01**: Partition membership is declared for every pre-existing route — `GET /health/ready` public; `GET /`, `GET /examples`, and the `/chats` family authenticated — and no route can sit outside its declared partition
  > **Enumeration half amended by Phase 37.1 (D-06), 2026-08-24 — now satisfied structurally rather than by assertion.** "The enumeration assertion passes in both directions" described set equality between a parallel declaration table and the live router, and the whole failure class it guarded was that table drifting. Membership is now declared once, by which router a route is registered on, so the two cannot disagree — there is no second table. `tests/unit/test_app_wiring.py::TestEveryRouteIsAuthenticated` asserts the property directly over `app.routes`: the public allowlist is exactly `{/health/ready}` and every other `APIRoute` declares the auth dependency. **Membership itself is unchanged** — the same eight routes sit in the same partitions, and no route changed its authentication status.

- [x] **REBIND-02**: These routes are off the audited attempt path and write no `audit.auth_events` row ever; rejections keep their internal result in the structured security log (the hand-rolled `RejectionCounter` metric was removed — see D-15 below; rejection rate is derived from the log by the deployment's log pipeline, not by a counter subsystem the service maintains itself)
  > **Audit half became *trivially* true — Phase 37.1 (D-01), 2026-08-24 — so it is kept, not withdrawn.** "Write no `audit.auth_events` row ever" still holds, but now because *no route anywhere* writes one and the table itself is gone, not because these routes sit off a path that still exists. The statement is not falsified; it has simply stopped discriminating between routes.
  >
  > **The substantive half survives intact and is now load-bearing.** Rejections keep their stable internal result in the structured security log because D-03 deliberately kept the rejection log — `app/dependencies.py::_reject` since Phase 37.2 (D-10), 2026-08-25 — the requirement this one already depended on. After D-01 that log event is the only record of a rejection anywhere in the system, which is exactly the argument D-03 made for keeping it.

- [ ] **REBIND-03**: Auth rejections on these routes surface through the shared error taxonomy and response shape, while their existing non-auth business error contracts are unchanged
- **REBIND-04** — **Void.** The named `quota_checked_request` admission entry it required no longer exists: Phase 35 D-05 deletes backend rate limiting from the product. REBIND-05's grant resolution, lock order, and lazy rollover are unaffected.
- [x] **REBIND-05**: The quota flow resolves one effective grant under the shared predicate, locks grant-then-usage in ascending grant id, fails closed on a missing usage row, performs lazy monthly rollover in the same locked transaction, and never lets `remaining` go negative
- [x] **REBIND-06**: The application starts and every pre-existing route behaves as it did in v1.6, apart from auth rejections now using the shared error classes

### CREATE — Phase 37 (`02-create-user.md`) — `POST /auth/create-user`

- [x] **CREATE-01**: Pre-auth admission is a closed, enumerated set of exactly two routes — `POST /auth/challenge` and `POST /auth/create-user`; every other route rejects an unlinked caller with `preauth_identity_not_allowed`
  > **Amended by Phase 37.2 (D-01/D-03), 2026-08-25 — the count changed from one route to two; the substance did not.** As written this read *"The endpoint is the only pre-auth-callable route"*, and it was true until this date. Challenge issuance moved out of `POST /auth/create-user`'s prepare mode onto its own `POST /auth/challenge` route, and that route **must** be pre-auth-callable: an unlinked caller needs a challenge in order to create an account at all, so demanding a link first would make account creation unreachable. What this requirement protects is unchanged — pre-auth admission is still a **closed** set, still **enumerated** in one place, and every route outside it still rejects an unlinked caller with `preauth_identity_not_allowed`. Neither route is exempt from *authentication*: both still resolve the request context, which is why an already-linked caller is owed a 409 rather than silence.
  >
  > **The enforcement point is `tests/unit/test_app_wiring.py`'s `PREAUTH_CALLABLE_PATHS` literal** — today exactly `{"/auth/create-user", "/auth/challenge"}`. It is deliberately hand-maintained rather than derived from the routers, so widening the set is a visible edit to a two-element literal that a reviewer meets in the diff. A third pre-auth-callable route added without touching it fails `TestEveryRouteIsAuthenticated::test_every_route_but_the_two_exemptions_requires_a_linked_identity`, so a later phase widening this set walks into both a written statement and a failing test rather than neither.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved.** `SHARED-INVARIANTS.md` § "Wire contract" reads *"Exactly one configured Firebase integration; every Admin call goes through the client selected by the request-verified issuer. No ambient, default, global, or fallback client exists; selection fails closed, and an issuer mismatch rejects at the barrier before any Admin work."* That document binds every phase, wins over any conflicting phase brief, and **it has not been amended**. This project has knowingly diverged at the **credential** layer: Phase 37.2 (D-06/D-08) deleted the explicit service-account credential branch, so the Admin app is now built from **Application Default Credentials** — ambient by definition, discovered from the environment, and in a developer's shell capable of silently resolving to that developer's own `gcloud` user credentials. That last property is precisely why Phase 37 D-08 rejected ADC, and **Phase 37 D-08 is superseded** by the developer's directive.
  >
  > **The reasoning, written down so a later reader can re-examine it rather than rediscover it:** production runs on GKE, where Application Default Credentials means Workload Identity — no key material at rest, nothing to rotate, nothing to leak — which is strictly better than the mounted service-account key the deleted branch loaded. Maintaining a parallel credential path purely to satisfy the letter of the rule is not justified by this product's threat model, a pre-launch sub-$5/month grammar app (`AGENTS.md`). **What is unchanged is the named-app-per-issuer discipline:** `build_admin_apps` still builds one named Admin app per configured issuer and never a `[DEFAULT]` app, every Admin call still goes through the client the request-verified issuer selects, and selection still fails closed — so the no-ambient-***client*** rule is still satisfied at the app-selection layer. The divergence is confined to where that client's credential comes from. Recorded here per the standing rule from `37-CONTEXT.md` — conflicts against `SHARED-INVARIANTS.md` are flagged, never silently resolved.

- [x] **CREATE-02**: The operation has both a prepare phase and a completion phase, partitioned by route — `POST /auth/challenge` issues the challenge, `POST /auth/create-user` completes against it
  > **Amended by Phase 37.2 (D-01/D-03/D-05), 2026-08-25 — the partition mechanism changed; both phases still exist, so this is amended rather than retired.** As written it read *"The endpoint implements both prepare mode and completion mode, partitioned by the mode signal"*. Deleted: the `?challenge=true` query-parameter signal, `auth/modesignal.py` and its `classify_mode_signal`, the `get_raw_query_string` dependency, and the `_prepare` handler branch. Replacing them: **one route per phase**. `POST /auth/create-user` now parses no query string, dispatches on no mode, and reads only its body.
  >
  > **The substance survives intact.** Both phases still exist, both are still challenge-bearing, and their order is still enforced — completion still requires a handle the prepare phase issued, and `tests/unit/test_create_user_precedence.py` still pins the rejection precedence below the challenge lookup with **zero** edits to any status code or body literal. What a route cannot express that a query parameter could is an *ambiguous* mode — two signals, neither signal, a non-`true` value — so the e2e cases guarding that ambiguity were deleted rather than moved: the ambiguity is no longer expressible at all.
  >
  > **Two status codes moved, both sanctioned as part of the split.** A malformed or unusable `challenge_id` is now the framework's `422 validation_error` rather than a hand-rolled `400 invalid_request` (D-04) — the `Any`-typed field existed only to route unusable handles into the mode signal's 400, and died with it. An already-linked caller now hears `409 identity_already_linked` at **completion**, raised by the in-transaction constraint conflict, instead of from a best-effort racy pre-check at prepare (D-03); the accepted consequence is that such a caller spends a challenge row and one Firebase lookup before hearing it. No other status code in the operation moved.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 37.4 D-11/A-08, 2026-08-30). The pre-auth subject is stored in plaintext.** `02-create-user.md:49` requires the prepare phase to persist the binding as `(preauth_issuer plaintext, preauth_subject_hash)` where *"`preauth_subject_hash` = HMAC-SHA-256(k_actor_subject_vN, "actor-subject:v1:" || preauth_issuer || ":" || canonical_subject)"*, and states in terms that *"raw subject never stored"*; `:56` requires completion to verify *"recomputed HMAC verifier == `preauth_subject_hash`"*. That document binds this phase and wins over any conflicting phase brief, and **it has not been amended**. This project has knowingly diverged: `preauth_subject_hash BYTEA` is now `preauth_subject TEXT` holding the raw subject, the comparison is a plain `!=`, and `auth/hmac_keyring.py` (118 lines), `tests/unit/test_hmac_keys.py` (314 lines), `HmacConfig`, the `config/config.yaml` `hmac:` block, the lifespan wiring, both message prefixes, the key-version machinery and `warn_missing_older` are all deleted.
  >
  > **The finding that drove it, written down so a later reader can re-examine it rather than rediscover it:** `core.external_identities.subject` already stores that same raw subject **permanently, in plaintext**, with `provider_uid` — the stable Google/Apple account UID — in plaintext in the column beside it. 432 lines of key-versioned machinery protected a value for the 300 seconds a challenge row lives, that the schema keeps forever one table over. **A-08 accepts the timing consequence the decision did not originally name:** `hmac.compare_digest` is gone and the replacement is a non-constant-time comparison, accepted on the same grounds — the compared value is a Firebase uid that an attacker holding the token already knows. Two pieces were dead on their own terms and went regardless: `idp_account_hash` and `IDP_ACCOUNT_PREFIX` had zero production consumers, and the key versioning was already inert because `:49` itself stores **no** key version on the row.
  >
  > **What survives, and is asserted.** The clear-on-consume half of the rule holds: consumption still clears the pre-auth subject in the same state transition, and `verify_binding`'s already-used arm now keys on `preauth_subject IS NULL` instead of a cleared hash — pinned by `tests/e2e/test_challenge_store.py::TestTheLifecycleRunsOneDirectionOnly::test_consume_clears_the_preauth_subject_on_a_preauth_bound_row` and negative-controlled by swapping the cleared-subject check with the issuer check, which failed a named case. The pre-auth **issuer** was always stored in plaintext and is unchanged. Recorded here per the standing rule from `37-CONTEXT.md` — conflicts against the binding specification are flagged, never silently resolved — so a later reader finds the divergence rather than discovering it.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 37.4 D-03, 2026-08-30). Consumption is no longer conditioned on this attempt's claim id.** `02-create-user.md:65` requires the challenge be consumed *"exactly once — atomic conditional update to `consumed`, **conditioned on still-claimed under this attempt's `claim_attempt_id`**"*, and that a transient commit failure *"retries the same local transaction under the same `claim_attempt_id`"*. That document binds this phase and **has not been amended**. This project has knowingly diverged: the `claim_attempt_id` column is dropped from the initial migration, from the lifecycle CHECK, from `tables/auth.py`, from the store's `claim`/`consume` signatures and from the route, so no attempt identity exists to condition on.
  >
  > **Why:** the condition could never fail. It reads as a compare-and-swap guard, but **nothing anywhere un-claims a challenge** — the lifecycle is one-way, issued → claimed → consumed — so the predicate was always true for the attempt holding the claim and the guard discriminated nothing.
  >
  > **What survives, and is asserted.** The consuming `WHERE` still keys on the challenge id, a non-null claim time and a null consumption time, so **a double consumption still matches no row** (`tests/e2e/test_challenge_store.py::…::test_a_second_consume_of_a_consumed_row_returns_false`), and a consume without a claim still changes neither column. The lifecycle CHECK was **rewritten rather than trimmed**, to `CHECK (consumed_at IS NULL OR claimed_at IS NOT NULL)`, which rejects a consumed row that was never claimed; its non-vacuity was proven by replacing it with a tautology and watching exactly one named schema case fail. **A premise correction worth inheriting:** the phase's threat register rated this hazard *high* on the belief that a naive trim would leave a tautology. It would not — striking the two attempt-id conjuncts leaves `(A ∧ B) ∨ ¬A`, i.e. `¬A ∨ B`, which is false when `claimed_at IS NULL AND consumed_at IS NOT NULL`, and `IS NULL` is two-valued in SQL so no three-valued subtlety rescues the claim. The prescribed rewrite was right; its stated reason was wrong, and the severity was overstated. Recorded here per the standing rule so a later reader finds the divergence rather than discovering it.

- [x] **CREATE-03**: The creation transaction atomically produces one `core.users` row, exactly one ACTIVE `core.external_identities` row, and the per-store purchase-attribution tokens — never a partial account
- [x] **CREATE-04**: Concurrent create-user attempts for the same `(issuer, subject)` never produce duplicate accounts; the losing caller reconciles through `POST /auth/sync`
  > **The requirement itself is unchanged and still met.** The `UNIQUE (issuer, subject)` arbiter is untouched, the race loser still rolls back every business mutation, still answers `409 identity_already_linked`, and is still pointed at `/auth/sync`; the uniqueness violation still never escapes as a generic 500. What changed is what a *different* conflict on the same insert now answers.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 37.4 D-06/A-07, 2026-08-30). The provider-account conflict lost its own answer on the race path.** `02-create-user.md:63` requires that a *"Provider-account uniqueness conflict on the registered binding write → internal `provider_account_already_linked` → client `operation_not_allowed`"*, and `:74` lists `provider_account_already_linked` among the causes of `operation_not_allowed`. That document binds this phase and **has not been amended**. This project has knowingly diverged: `_insert_account` and `_flush_account` merged into one function, the `begin_nested()` savepoint, the cause-chain walk for `constraint_name`, both constraint-name literals, the three-way constraint map and the unmapped-constraint re-raise are all deleted, and **any `IntegrityError` from the insert becomes one `IdentityAlreadyLinked`**. `ProviderAccountAlreadyLinked` was deleted outright (A-07) because D-06 removed its only raise site, leaving it carrying no distinction; the `operation_not_allowed` code itself survives on `NotLinked`, which still raises it, so nothing dropped out of the vocabulary and `assert_tree_total()` stays satisfied.
  >
  > **Both accepted consequences, named because the developer accepted them when choosing this.** First, **the distinct answer is lost only on the race path.** The pre-check — `resolve_existing_identity` plus `_reject_existing_identity` — is byte-for-byte unchanged under D-04, so the common already-linked case still earns its distinct answer before the insert; only two completions racing on the same provider account collapse onto the 409. Exactly one status/code pair moved across the whole repository as a result, in `tests/e2e/test_create_user.py::TestTheProviderAccountReservation`. Second, **the unmapped-constraint 500 tripwire dies.** A constraint rename or a newly added constraint will now read as a benign 409 rather than surfacing loudly. That was Phase 37.3 D-03's explicitly preserved fail-loudly property and 37.3-REVIEW WR-12's subject, and it is knowingly given up; `tests/schema/test_inventory.py` still asserts the full constraint and index inventory, so the schema side keeps its coverage even though the runtime tripwire is gone.
  >
  > **One bug this collapse would have shipped, found by reading the diff rather than by the suite.** Removing the savepoint was not neutral: SQLAlchemy's full `Session.rollback()` expires *every* state in the identity map where a savepoint rollback expires only the dirty ones, so the route's post-rollback read of `challenge.challenge_id` became a lazy refresh — `MissingGreenlet`, a **500 on every conflicting create-user** where a 409 was owed. Fixed by capturing the plain values before the rollback. Recorded here because the savepoint's real job was identity-map scoping, not rollback scoping, and nothing in the plan or the specification said so. Recorded per the standing rule so a later reader finds the divergence rather than discovering it.

### SYNC — Phase 38 (`03-sync.md`) — `POST /auth/sync`

- [x] **SYNC-01**: The endpoint returns the effective grant, `current_period`, `monthly_used`, and stored `identity_provider`, all derived from one captured evaluation time
- [x] **SYNC-02**: The endpoint is strictly read-only — no rollover, no grant-row flip, no invariant repair, no profile write
- [x] **SYNC-03**: Every `/auth/sync` attempt earns exactly one `request` line from `RequestLoggingMiddleware` carrying the request id, the method, the path, the status code and the call's duration, and every rejection additionally earns one WARNING named for its exception class from the shared error handler. The endpoint writes no durable row and adds no per-attempt telemetry beyond those lines
  > **Amended by Phase 38 (D-01/D-02), 2026-09-01 — the decision Phase 37.1 flagged forward is made here.** As written this requirement read *"the endpoint writes exactly one `audit.auth_events` row with `operation='sync'` per attempt despite mutating nothing, admission rejections included"*, and it carried a lead declaring itself blocked on a deleted mechanism plus a choice between two options. Both are gone because the choice has been made. The treatment is **amended**, not **withdrawn**: this requirement's subject was attempt telemetry, and attempt telemetry still exists — it is the middleware line. What the endpoint owes is stated above, in terms of what is actually built.
  >
  > **Option (b) was chosen, dated 2026-09-01. The durable-row obligation is dropped, not rebuilt.** The ground is what rebuilding actually costs: the `audit.auth_events` table back in the single initial migration, HMAC-SHA-256 actor hashing with key versioning, the six-key `{schema_version, context, verification, resolved, mutation, failure}` `details` shape, and route→operation metadata readable before the barrier. That is not warranted for a read-only endpoint's attempt telemetry, and it reopens the cost argument that removed the subsystem in the first place. **SYNC-02 makes this endpoint strictly read-only, so the row was never a mutation record** — it was telemetry about attempts, the cheaper of the two cases Phase 37.1 named.
  >
  > **No success event was added either (D-02).** `RequestLoggingMiddleware` (`api/logs.py`) already writes one `request` line per attempt and `/auth/sync` is not among its `_EXCLUDED_PATHS`; every rejection already earns one WARNING from the shared handler (`app/error_handlers.py::app_error_handler`), whose event name is the raised class snake_cased — `invalid_external_jwt`, `pre_auth_identity_not_allowed`, `historical_identity`, `blocked_user`. A second line saying a 200 succeeded is duplication, so no `auth_sync_succeeded` event, no user-id label and no other per-attempt telemetry was added.
  >
  > **The obligation was removed at its source rather than diverged from (D-03).** Plan 38-04 struck § "Audit" in full from `SHARED-INVARIANTS.md`, together with the § "Errors" clause naming `core.auth_event_result`, and relocated that section's two live non-audit rules into § "Fail-closed defaults". No new flagged conflict was filed because after that edit there is no surviving invariant text to diverge from — the precedent the developer set in Phase 37.4, where the invariant asserting the wire contract was removed rather than carried as a permanent conflict. The conflict already recorded under FOUND-05 is resolved by that removal; the note there keeps its history.
  >
  > **One-way in practice.** Restoring the row means re-editing `migrations/20260818_01_initial-release.sql` under a new id — SCHEMA-01 forbids incremental migration files — so this is a replacement of the milestone's one migration, not an addition to it.

### PROF — Phase 39 (`04-users-me.md`) — `GET /users/me`

- [x] **PROF-01**: The endpoint returns profile fields, the stored `identity_provider`, and per-store purchase-attribution tokens unconditionally for every store provider, with no platform or client-signal branching
  > **Amended by Phase 39 (D-02/D-03), 2026-09-01 — two knowing divergences from `04-users-me.md`, recorded here because the phase briefs are marked verbatim and are not edited (Phase 38 D-05 precedent).** Neither touches what this requirement asks of the body: every store's token still ships on every request, with no platform or client-signal branch. What is recorded is where the phase departs from the brief's literal instructions, and on what ground.
  >
  > **D-03 diverges from the brief's literal handler step 1** (`04-users-me.md:41`), *"Load the resolved user's profile fields from `core.users`"*. The step is satisfied in substance rather than by a second read: the row *was* loaded from `core.users`, by the barrier, in this request, and `app/lifespan.py:36` sets `expire_on_commit=False`, so `identity.user.email` and `identity.user.display_name` stay readable after the barrier's short session closes — `/auth/sync` already reads `identity.user.id` exactly this way, so the detached read is established and not novel. Re-reading would issue a second query for a row already in memory, and the freshness it buys is empty: no path in this milestone edits either field, email sync and `display_name` population both being explicit deletions in the brief. The purchase-token read is therefore the request's only query.
  >
  > **The brief's `users_me` rate-limit entry is omitted (D-02's other half), because there is no engine to register it with.** `04-users-me.md:26` and `:52` mandate a named backend entry — `30/minute`, key `issuer+subject_hash`, evaluated only after backend JWT verification. Phase 35 D-05 deleted the backend rate-limit engine **from the product, not deferred**: `AGENTS.md` § Resilience states this and `limits` is absent from `pyproject.toml`, so the entry has nowhere to live and the `429` answer the brief lists for this endpoint is unreachable. The auth surface is knowingly unlimited this milestone (Phase 38 D-08), and the gateway contract is deferred to v2.1. This is an omission with a decision behind it, not a missed obligation.

- [x] **PROF-02**: The endpoint is off the audited attempt path and writes no `audit.auth_events` row ever; rejections keep their stable internal result in the structured security log instead
  > **Amended by Phase 37.1 (D-01/D-03), 2026-08-24 — both clauses, for different reasons.** The **audit clause became *trivially* true**: no route anywhere writes an auth audit row and the table is gone, so it is kept rather than withdrawn, exactly as REBIND-02's matching clause was. The **counter clause was already false before this phase** — Phase 36 D-15 removed the hand-rolled `RejectionCounter`, and REBIND-02 records that rejection rate is derived from the log by the deployment's log pipeline. It has been reworded to name what actually exists: the `auth_rejected` event, emitted by `app/dependencies.py::_reject` since Phase 37.2 (D-10), 2026-08-25.
  >
  > **Consequence: Phase 39 inherits no obligation to build either subsystem** — not an audit writer, not a counter. Correcting the stale counter clause now is the point of saying so; left as written it would have read as a standing instruction to build a metric this project deliberately removed.

### UPGRADE — Phase 40 (`05-upgrade-anonymous.md`) — `POST /auth/upgrade-anonymous`

- [x] **UPGRADE-01**: The endpoint records the client-side same-Firebase-UID `linkWithCredential` upgrade by flipping the existing `core.external_identities` row's provider in place
  > **The requirement itself is unchanged and met as written (Phase 40, 2026-09-02).** One `core.external_identities` row's provider flips in place, `provider_uid` makes its only assignment transition, `registered_at` and any email copy commit in the same transaction as the flip, and no identity row is created, retired or marked historical. `crud/identities.py::flip_provider` is the sole writer of both halves, and `tests/schema/test_registration_pairing.py` scans for rows in the third state that no CHECK can express. What follows is what this phase decided **differently from what was asked for**, recorded here rather than by editing the brief (D-25) or the context that recorded the reversed decision (D-18).
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 40 D-01, 2026-09-02). There is no client-supplied target provider.** `05-upgrade-anonymous.md` states the declaration six times and none of that text is amended: `:26` makes `operation_variant` *"the declared target provider (`google` | `apple`), normalized once at prepare by exact case-sensitive enum match and immutable thereafter"*; `:46` requires a *"REQUIRED client `provider` field, `google` or `apple` (no default)"* at prepare; `:53` requires that *"completion's REQUIRED `provider` must equal the stored variant byte-for-byte (no re-normalization/defaulting)"*, running *"BEFORE any Firebase Admin lookup"*; `:56` requires that the *"Classified provider must equal the declaration for any success"*; `:60` and `:61` phrase the (c) and (d) rejections in terms of the declaration; and `:81` lists *"missing or invalid declared target provider"* in the mandatory rejection scope. This project has knowingly diverged: `CompletionRequest` is exactly `{challenge_id}` and `ChallengeRequest` exactly `{operation}`, neither carries a provider field, `core.auth_challenges.operation_variant` stays deleted, and the provider comes solely from the mandatory `getUser` read the brief also requires at `:55`.
  >
  > **What dissolves with it, so nothing is looked for later that was never built:** the required field at both phases, the exact-case normalization, the persisted immutable variant, the byte-for-byte re-check at completion, and the **third bounded cause** of the not-linked rejection. `:60` bounds that cause as `empty | invalid-shape | supported-provider-mismatch`; `supported-provider-mismatch` cannot occur when there is no declaration to mismatch, so `NotLinked`'s causes are `empty | invalid-shape` here as they already are at create-user. The case matrix collapses to two inputs — what the row stores and what the live read says — with no third.
  >
  > **Why, and why it is a conflict rather than an override.** This is the direct continuation of **Phase 37 D-12**, which deleted create-user's `provider` declaration for the same reason, and it is the answer to the question **Phase 37 D-13** flagged forward when it dropped `operation_variant`: a value the client declares and the server must then re-derive anyway is a second copy of a fact the Firebase read already owns, and the only thing the re-check can catch is a client contradicting itself. `SHARED-INVARIANTS.md` § "Identity and ownership" independently forbids rederiving the provider from *"token claims, headers, client input"*, and this endpoint is one of the five enumerated read points, so the shipped design is the one the invariants ask for and the brief's declaration is the part in tension with them — but `SHARED-INVARIANTS.md` wins over a brief only where they conflict on a rule, and the brief's declaration is an additional obligation rather than a contradicted one. It is therefore recorded as a conflict, per the standing rule from `37-CONTEXT.md`, so a later reader finds the divergence rather than discovering it. **Reported, not resolved:** the identical removal at create-user (Phase 37 D-12, against `02-create-user.md:48` and `:58`) was never filed in this file as a conflict at all. That gap is left as Phase 37's to close rather than re-filed here, on the same grounds Phase 37.5 left the `limits` override in Phase 35's category — but a reader auditing divergences should know the shape exists twice and is counted once.
  >
  > **FLAGGED, NOT COUNTED (Phase 40 D-22, 2026-09-02). Every completion makes one unbounded Firebase Admin read.** `:55` and `:87` make the `getUser` read mandatory on **every** branch, *"the idempotent repeat included; no branch may skip it because the stored row already carries the declared provider"*, and that rule is right: the read is the only thing that detects a diverged binding, so skipping it on an already-registered row would let a corrupted row report success forever. With the backend rate-limit engine deleted from the product (Phase 35 D-05, not deferred) and the Envoy 3/hour gateway limit deferred to v2.1 (Phase 35 D-08), a looping client can call it without bound. **Accepted and flagged, mirroring how Phase 37 D-01 recorded unlimited account creation.** The mitigating facts, for the record: the caller must already hold a valid Firebase ID token for an **existing linked account**, so this is one subject looping on itself rather than a fan-out across accounts; the client contract at `:69` says to call only when the client's own SDK providerData and the backend disagree; and the exposure closes when the gateway contract lands. A narrow per-route limiter was offered and declined. **Not counted as a new conflict** because it is a consequence of the pre-existing Phase 35 D-05 `limits` override recorded at `:50` and under FOUND-06, not a fresh divergence — the same treatment PROF-01 gives the `users_me` entry.
  >
  > **LEDGER ENTRY, not a conflict (Phase 40, 2026-09-02). D-18's test-credential mechanism was reversed, and the reversal is recorded forward rather than by rewriting D-18.** `40-CONTEXT.md` D-18 decided that each run would *"mint a custom token for that UID through the Admin SDK"* and said in terms: *"**no per-run OAuth consent flow and no stored refresh token.**"* That is not what shipped. **The cause has two halves, both established by execution rather than inference** (`40-RESEARCH.md` § P-01): first, this machine's Application Default Credentials resolve to an `authorized_user` — a user credential with no `client_email` and no signer — so `firebase_admin.auth.create_custom_token` raises *"Failed to determine service account … Make sure to initialize the SDK with service account credentials or specify a service account ID with `iam.serviceAccounts.signBlob` permission"*; second, the project's org policy sets `iam.disableServiceAccountKeyCreation`, recorded in `.env.example`, so **no service-account key can be minted here at all** to supply one. The one remaining route to a signer — granting the ADC principal `roles/iam.serviceAccountTokenCreator` — was available and was **reversed by the developer**, leaving this project minting and signing nothing whatsoever. **The mechanism that shipped is exchange-and-link** (plan 40-03): one browser consent performed by hand yields a long-lived Google refresh token held in `.env`, and each run redeems it for a fresh Google ID token, creates a fresh anonymous Firebase user over Identity Toolkit REST with the existing `JWT_API_KEY`, links the Google credential onto that user, and deletes the user afterwards. **What it preserves** is exactly what D-18 was for: a real Admin `getUser` against a genuine `google.com` `providerData` entry, read through the production `FirebaseAdminLookup` seam. **What it costs:** three new `FIREBASE_TEST_GOOGLE_*` variables documented in `.env.example`, and a consent the developer performs by hand and can revoke at any time. It **supersedes all three routes `40-RESEARCH.md` § P-01 costed** — the IAM grant, the locally minted ID token, and the skip — as § Open Questions Q1 records. **Two further clauses of D-18's body are now false and are corrected here rather than there:** the Firebase side is **not** read-only for this test, and the account is **not** fixed test data — each run creates and deletes a Firebase user, and the Google account holds only one Firebase link at a time, which is why the fixture needs a recovery preamble that deletes a leftover holder before creating a new one. **`40-CONTEXT.md` D-18's text is deliberately left describing the reversed mechanism**, carrying only a dated one-line pointer: that file records what the developer decided at discuss time, and a reversal is recorded forward, here, not by rewriting the history of the decision.
  >
  > **A verification that is not reproducible from this repository alone, recorded so it is not mistaken for one that is.** Plan 40-03 had to prove that an absent credential *fails* rather than silently skips. `pytest-dotenv` reloads `.env` at initial-conftest time, so clearing the shell variable proves nothing; the variable was removed from `os.environ` inside a throwaway `-p` plugin's `pytest_configure`, which runs after that load, and the plugin lived in `/tmp` and was deleted after use. The result — **zero skipped**, with the cause a `KeyError` at the subscripted read — is quoted in full in `40-03-SUMMARY.md`, which is the only surviving record of it.
  >
  > **The flagged-conflict count, re-derived rather than inherited (2026-09-02).** Five sections of `SHARED-INVARIANTS.md` were re-read against what this phase shipped before the count was stated: **§ "Identity and ownership"**, **§ "The barrier — the only place identity happens"**, **§ "Fail-closed defaults"**, **§ "Locks and transactions"** and **§ "Global deletions — build NONE of these, in any phase"**. `05-upgrade-anonymous.md` was read in full, and `00-schema.md:190`, `01-foundation.md:137`, `10-restore-subscription.md:25` and `11-sign-out-all.md:25` were read for the enum shrink's reach. **The result: one new conflict — D-01, above — taking the count from five to six.** The four sections that produced none, and why, because a null result is only useful if it says what it examined: **§ "Identity and ownership"** requires the stored provider column to be *"the sole per-request classifier"* and forbids rederiving it from *"token claims, headers, client input, or live providerData outside the five enumerated Firebase Admin providerData read points"* — this endpoint is one of the five, D-01 removes the only client input there was, and `registered_at` stays reporting-only; identity rows are never deleted and the flip creates none. **§ "The barrier"** requires that only create-user be pre-auth-callable and that every other route reject an unlinked caller with `preauth_identity_not_allowed` — which is exactly what D-10 implements at the issuance route; its route-enumeration clause is the pre-existing FOUND-01 conflict, and this phase adds a route through the surviving `tests/unit/test_app_wiring.py` mechanism rather than a new divergence. **§ "Fail-closed defaults"** requires that divergent stored state be *"refused, never auto-rewritten or converged by retry"* (the drift refusal), that every branch of one error class return an identical status, body and timing (D-02), and that a rejection leave exactly one structured line carrying its stable internal result (D-05/D-07) — all three hold. **§ "Locks and transactions"** forbids any provider call *"while any DB lock is held or a consuming transaction is open"* and requires the mutating transaction to re-resolve and revalidate the locked rows before writing — the Admin read runs strictly before the write transaction opens and the claim is committed first, and D-15 locks and revalidates the identity and user rows at the top of it; the fixed grant lock order does not engage because this path touches no grant or usage row, as `:64` also forbids. **§ "Global deletions"** forbids, among others, `display_name` population from auth context — D-13 never populates it — and no scheduled cleanup, reconciliation or background healer was built for the stranded-upgrade repair path, which is the client-driven retry loop the brief itself specifies at `:69`.

- [x] **UPGRADE-02**: The endpoint is challenge-bearing with prepare and completion modes, callable only by an authenticated linked identity
  > **Amended by Phase 40 (D-09), 2026-09-02 — the partition mechanism was already gone before this phase existed; both phases still exist, so this is amended rather than retired.** As written, "modes" described the `?challenge=true` mode-signal partition the brief specifies at `05-upgrade-anonymous.md:41` and `:43`. That partition was deleted by **Phase 37.2** (D-01/D-03/D-05), which moved challenge issuance onto its own `POST /auth/challenge` route and amended CREATE-01/02 to say so; `classify_mode_signal` does not exist in this codebase. This phase followed it rather than rebuilding it: **prepare is served by `POST /auth/challenge`, completion by `POST /auth/upgrade-anonymous`.** No change to `crud/challenges.py` was needed — `ChallengesDB.issue` already binds a linked caller to `bound_external_identity_id`, which is precisely the row-id binding the brief's prepare step 4 (`:47`) requires. **The substance is delivered in full:** both phases exist, both are challenge-bearing, their order is enforced because completion requires a handle prepare issued, and the admission rule holds — `get_linked_identity` narrows the completion route to an authenticated, linked, active caller, and `verify_binding` proves the presenter is the identity row the handle was bound to. No new flagged conflict is filed for the mode-signal partition: it is the same divergence already recorded under CREATE-02 and it was made three phases ago, not here.
  >
  > **The account-less rule this phase added, and why it needs no list (D-10/D-11).** `/auth/challenge` is deliberately behind unnarrowed `Depends(get_identity)`, because an unlinked caller must be able to prepare `create_user`. The rule shipped as one derived condition in `routers/auth.py`: any operation other than `create_user` presented by a caller with no account raises `PreAuthIdentityNotAllowed`. Create-user is the only operation an account-less caller may ever prepare, because it is the only route in the application that admits unlinked callers — a fact already pinned by `tests/unit/test_app_wiring.py::PREAUTH_CALLABLE_PATHS` — so the condition stays correct for phases 41 and 42 with nobody touching it. The membership test is `body.operation not in AuthOperation`, which is complete **only because** D-11 shrank the type to the four challenge-bearing labels; that is the whole reason the shrink was worth its cost, and the forward flag it carries is recorded under SCHEMA-01. **Three registry-shaped alternatives were rejected and should not be re-litigated:** putting the rule in `ChallengesDB.issue` (the store would have to learn that sign-up is special), a per-operation admission table (a parallel list to keep in step), and deriving the issuable set from FastAPI route metadata — both `openapi_extra` and a marker decorator were **verified working against live FastAPI** and rejected anyway, because a forgotten marker fails silently as an unexplained 400 rather than at the point you are reading, and `openapi_extra` additionally publishes the flag into `/openapi.json`.
  >
  > **What the brief asks for on this route that this phase did not build, because it was already deleted from the product.** Read `05-upgrade-anonymous.md` without this note and all of it reads as unmet work. **Rate limiting — none of it exists.** `:29` names two backend entries (`upgrade_anonymous_prepare`, `upgrade_anonymous_to_registered_firebase_identity_lookup`) plus the global `adapter_firebase_lookup` budget, `:30` and `:88` name the load-bearing Envoy entry `gateway_rate_limits.upgrade_anonymous`, and `:54` specifies the broadest-first non-destructive-check-then-joint-increment budget order. **Phase 35 D-05 deleted the backend rate-limit engine from the product, not deferred it** — `AGENTS.md` § Resilience states this and `limits` is absent from `pyproject.toml` — so those entries have nowhere to live and the `429` at `:79` is unreachable on this route; Phase 37 D-01 rejected a narrow per-route reinstatement and Phase 37 D-02 dropped the cross-request Firebase lookup budgets; the Envoy contract is v2.1 (Phase 35 D-08). `BudgetGate` and `auth/budgets.py` were retired by Phase 37 D-04, so `:54`'s budget ordering has nothing to sit on; the three-attempt retry survives as `tenacity` in `auth/firebase.py::lookup_with_retry`. **Audit — the subsystem is gone.** `:66` (completion step 11), `:89` in full, and `:18`'s foundation audit writer describe `audit.auth_events` rows, movement classification, actor-subject HMAC hashing and details JSON. **Phase 37.1 D-01 deleted the table, its writer and every call site**, and **Phase 38 D-03 struck § "Audit" from `SHARED-INVARIANTS.md` outright** — the first time this milestone resolved a flagged conflict by removing the obligation rather than diverging from it. The distinct-result obligation `:89` states survives **only** in the structured log, which is satisfied: `ProviderTransitionNotAllowed` and `ProviderAccountAlreadyLinked` are separate exception classes, not one class with a field, so `app/error_handlers.py` writes `provider_transition_not_allowed` and `provider_account_already_linked` as distinct WARNING event names, and searching for accounts needing manual repair is a match on two event names rather than a filter on a field value. **The route registry and its startup enumeration assertion** (`:16`) went with Phase 37.1 D-06; route categorisation is carried by `tests/unit/test_app_wiring.py` alone, and the conflict that creates is the one already flagged under FOUND-01. **None of this is a Phase 40 omission** — every one of these was decided and recorded before this phase began, and it is written here so a later reader does not open the brief and read it as unmet.
  >
  > **Two client-visible facts worth finding here rather than in a test.** The three refusals — the client called before its own `linkWithCredential` finished, a stored-versus-live drift, and the target provider account already held by another identity row — answer an **identical** 403 `operation_not_allowed` with a one-field body (D-02). `SHARED-INVARIANTS.md` § "Errors" requires it: a differing body within one class would let any token holder probe which provider accounts are taken. The client distinguishes them from state it already holds, per the brief's own contract at `:69`; operators distinguish them in the log. And a repeat call that changed nothing answers **identically** to the one that performed the flip — 200 with the same `{identity_provider}` body, no `changed` flag (D-04) — which is the brief's idempotent repair path at `:59` behaving as `:37` demands: *"its idempotence must not be optimized away."*

### ANONGRANT — Phase 41 (`06-claim-anonymous-grant.md`) — `POST /auth/claim-anonymous-grant`

- [x] **ANONGRANT-01**: The endpoint is the only operation that may create a `core.access_grants` row with `source='anonymous_device_grant'`, across the prepare/completion mode partition with a server-determined branch
  > **The single-writer half is met as written, and proven against the whole tree rather than asserted (Phase 41, 2026-09-03).** The literal `anonymous_device_grant` is constructed from exactly one site in `src/` — `crud/grants.py::activate_anonymous_device_grant` — and `tests/unit/test_grant_sources.py` walks the tree for it with four controls; the walk is known to fire against the real tree, not only its synthetic control, because a second construction site was added to the crud writer, observed to fail two named cases, and reverted with the file left byte-identical. **Marked met on a named suite result, re-run whole after the phase's last code commit:** `uv run pytest -q` **950 passed**, `uv run pytest -m e2e -q` **226 passed**, `uv run pytest -m schema -q` **134 passed**, `uv run ruff check src tests` **exit 0** — quoted from `41-04-SUMMARY.md` § Verification. What follows is what this phase decided **differently from what was asked for**, recorded here rather than by editing `06-claim-anonymous-grant.md`, which stays verbatim (D-19).
  >
  > **Amended by Phase 41 (D-18), 2026-09-03 — the second half of this requirement's own wording describes machinery that was gone before this phase existed.** "Across the prepare/completion mode partition with a server-determined branch" restates `06-claim-anonymous-grant.md:7`, `:55`, `:56` and `:58`: a `?challenge=true` query flag partitioning prepare from completion on the same endpoint, and a server-determined selection over three evidence sets. **Neither exists.** The mode-signal partition was replaced by a dedicated issuance route in **Phase 37.2** (D-01/D-03/D-05) — `classify_mode_signal` is not in this codebase — so prepare is `POST /auth/challenge`, which already issues for `claim_anonymous_grant` (Phase 40 D-11), and completion is `POST /auth/claim-anonymous-grant`. This is **amended, not withdrawn, and files no new conflict**: both phases still exist, their order is enforced because completion requires a handle that route issued and bound to the caller's `core.external_identities.id`, and the mode-signal divergence is the one already recorded under CREATE-02 and UPGRADE-02, made three phases ago rather than here. The "server-determined branch" is discussed under the next entry: with one branch built, selection is not a decision the server makes at all.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 41 D-01, 2026-09-03). The device gate is iOS DeviceCheck only.** The brief scopes all three branches into this phase and none of that text is amended: `:7` puts *"server-determined branch selection over iOS DeviceCheck / Android Play Integrity Device Recall / web Cloudflare Turnstile evidence"* in scope; `:42`–`:45` require all three adapters be implemented; `:70` makes branch selection *"deterministic, server-verified, from which complete evidence set the body carries"*, with zero, more than one, or a partial set rejected as `proof_malformed`/`proof_rejected`; `:75` and `:76` specify the Android and web flows in full. **What shipped is the iOS arm alone.** `auth/devicecheck.py` is the only vendor seam this phase added, the completion body carries the challenge handle plus a separate DeviceCheck query token and update token (D-02), and there is no branch to select — with one evidence set the partition at `:70` collapses to one body shape, so a missing token is a body-shape failure rather than a branch-selection failure. The Android and web branches are **deferred to another milestone**, not abandoned.
  >
  > **What dissolves with the deferral, so nothing is looked for later that was never built:** the Play Integrity token/verdict verification and its Device Recall read and write; the Cloudflare Turnstile siteverify call, its per-environment secret, its hostname allow-list and its production-startup test-key rejection; the web branch's Firebase Admin `getUser` providerData read on this route and the closed classifier over it; the HMAC `idp_account_hash` and its key version; the resolve-or-create of a `core.provider_accounts` row and the `web_anonymous_gate` row in `core.provider_account_gate_consumptions`; the `anti_abuse_already_claimed` result and the web gate-consumption conflict that produces it; and the `verification_required` class, treated separately below.
  >
  > **Why the deferral costs no migration, which is the fact that makes it cheap to reverse.** `core.access_grants_anti_abuse` already carries every column all three branches need. The row this phase writes populates the native arm — `native_claim_provider = 'ios_devicecheck'`, both hash columns NULL — and that is one arm of the table's per-source CHECK (`:80`) whose other arm is exactly what the web branch will write: `idp_account_hash` plus `idp_account_hash_key_version`, `native_claim_provider` NULL. The generated `registered_account_grant_id` column stays unmapped on the SQLModel, as `AccessGrant`'s four generated columns already are. Adding a branch is new code beside the existing adapter, not a schema change.
  >
  > ⚠️ **Corrected by Phase 42 (D-07), 2026-09-03 — the paragraph above was true when it was written and is false from this date.** `core.access_grants_anti_abuse`, `core.provider_accounts` and `core.provider_account_gate_consumptions` were deleted from `migrations/20260818_01_initial-release.sql`, with the `core.gate_consumption_kind` enum, two generated columns on `core.access_grants`, both deferred foreign keys, `UNIQUE (id, source)` and two indexes. **So the web branch now does cost a schema change**, and the columns the paragraph above points at do not exist. What is unchanged is the price: under SCHEMA-01 the one migration is edited in place and the disposable databases are re-applied, which is cheap while there are no users. The deletion is recorded as a flagged conflict under REGGRANT-02, and the web branch's account record stays in `42-CONTEXT.md` § Deferred Ideas, to be rebuilt there on its own terms. **Phase 41's own delivery is not rewritten:** it really did write the receipt row, and the paragraph above records why the deferral looked free at the time.
  >
  > **CONSEQUENCE of the above, not a conflict of its own and not counted (Phase 41 D-11, 2026-09-03). `verification_required` is not registered.** `:25` lists it among the three classes this phase adds and `:83` gives it four producers — a durable device-check read denial, a server-validated Cloudflare denial, a completed web providerData lookup failing the classifier or the stored binding, and internal `policy_rejected`. Every one of those is in the deferred web branch or in a durable-denial mode the DeviceCheck read does not have: Apple answers a bit, a refusal, or nothing, and the ambiguous and exhausted cases are `verification_temporarily_unavailable` by `:79`'s own rule. Nothing in this phase would raise it, so registering it would have added an unreachable client-visible code. It arrives with the web branch. `ErrorCode` grew from 16 to 18, not 19: `proof_rejected` and `device_grant_exhausted`, both 403.
  >
  > **FLAGGED DIVERGENCE with its ground — recorded, not counted as a conflict (Phase 41, 2026-09-03). An absent or empty device token is the framework's 422, not `proof_rejected`.** `:70` and `:83` put *"missing/malformed DeviceCheck … material"* inside `proof_rejected`, auditing the unified `proof_malformed`. What shipped: `AnonymousGrantClaimRequest` declares both tokens as required non-empty strings, so an absent, null or empty token is rejected by FastAPI's own validation as **422** before the handler runs — the same treatment a missing `challenge_id` already gets, the Phase 40 precedent this follows — and `proof_rejected` (403) covers a present token the vendor rejects. **This is recorded as a divergence rather than a consequence because it is a real difference in what the client sees:** a client sending an empty string gets a different status and a different body shape than the brief specifies. It is not counted as a conflict because it is the application of an existing, already-recorded project convention to a new field rather than a fresh decision against the brief's rule.
  >
  > **FLAGGED, NOT COUNTED (Phase 41 D-20, 2026-09-03). Every eligible claim makes an unbounded pair of Apple round trips.** `:79` makes the bit write load-bearing and forbids any coalesced or cached substitute for either the read or the write — *"every claim performs its own read and own confirmed write"* — and that rule is right: a cached bit is a bit that can be stale in the direction that hands out a second grant. With the backend rate-limit engine deleted from the product (Phase 35 D-05, not deferred) and the Envoy gateway contract deferred to v2.1 (Phase 35 D-08), a caller holding a valid ID token for an **eligible** anonymous account can make the backend call Apple as often as it can prepare challenges. **Accepted and flagged, mirroring exactly how Phase 40 D-22 recorded the unbounded Firebase read and how Phase 37 D-01 recorded unlimited account creation.** The mitigating facts, for the record: the caller must already hold a valid Firebase ID token for an existing linked anonymous account, so this is one subject looping on itself rather than a fan-out; the eligibility preflight (D-03, under ANONGRANT-02) refuses an ineligible account **before** Apple is reached, so the loop is only available to an account that has not yet claimed; each claim additionally costs a fresh challenge from `POST /auth/challenge`; and the exposure closes with the v2.1 gateway contract. **Not counted as a new conflict** because it is a consequence of the pre-existing Phase 35 D-05 `limits` override recorded at `:50` and under FOUND-06, not a fresh divergence — the same treatment UPGRADE-01 and PROF-01 give theirs.
  >
  > **A fact about the state of the world, recorded so the unit tests are not read as evidence they are not.** No iOS app exists, so no real device token has ever been produced and **no real round trip to Apple has ever been made** (D-04). The wire shapes `auth/devicecheck.py` implements — the host, the two paths, the request bodies and the five ordered response-parse arms — come from secondary sources and are marked `[ASSUMED]`; no official Apple page was fetchable during research. `tests/unit/test_devicecheck_adapter.py` carries that provenance in its module docstring, so **the first real 400 or 401 from Apple is authoritative over anything in this repository** and reads as evidence about those literals rather than as a regression. This is not a gap this phase left open; it is a gap only an app can close.
  >
  > **What the brief asks for on this route that this phase did not build, because it was already deleted from the product.** Read `06-claim-anonymous-grant.md` without this paragraph and every item below reads as unmet work by this phase. None of it is. **Rate limiting and every vendor budget — none of it exists.** `:29`–`:35` name six entries for this route (`claim_anonymous_grant_prepare` and `claim_anonymous_grant_prepare_ip` at prepare, `claim_anonymous_grant` and `claim_anonymous_grant_ip` at completion, and the `adapter_devicecheck_read` and `adapter_devicecheck_write` vendor budgets); `:59` and `:67` make them admission gates that must pass before issuing and before the claim; `:73` puts the two device-bit budgets immediately before their vendor calls with four dedicated exhaustion results; `:89` mandates the `limits` library and config-driven values; and `:19` describes the whole engine as foundation-provided. **Phase 35 D-05 deleted the backend rate-limit engine from the product rather than deferring it** — `AGENTS.md` § Resilience says so and `limits` is absent from `pyproject.toml` — so those entries have nowhere to live, the `429` at `:67` is unreachable on this route, and the four budget-exhausted results have nothing to sit on. Phase 37 D-01 rejected a narrow per-route reinstatement and Phase 37 D-04 retired `BudgetGate` and `auth/budgets.py`. What survives of `:79`'s retry policy is `tenacity`: three attempts total, in `auth/devicecheck.py`, mirroring `auth/firebase.py::lookup_with_retry`. **The `audit.auth_events` row and the internal-result vocabulary — the subsystem is gone.** `:18` describes the audit writer, `:28` enumerates seventeen `core.auth_event_result` values this phase would add, `:80` requires the success audit inside the activation transaction, and `:88` states the one-row-per-attempt contract in full. **Phase 37.1 D-01 deleted the table, its writer and every call site, and Phase 38 D-03 struck § "Audit" from `SHARED-INVARIANTS.md` outright.** The distinct-result obligation survives **only** as structured-log event names, produced from exception class names by `app/error_handlers.py::camel_to_snake` — which is why this phase's three refusal leaves are three classes rather than one class with a field: `claimant_not_anonymous`, `free_grant_already_consumed` and `other_active_grant_held` are three distinct WARNING events behind one byte-identical 403 body. **The mode-signal partition and any prepare-mode query flag** (`:7`, `:55`, `:56`, `:58`–`:62`) went with **Phase 37.2**; prepare is `POST /auth/challenge` and `classify_mode_signal` does not exist. **`claim_attempt_id`** (`:69`'s claim stamp, `:80`'s consume condition and its same-attempt-identity commit retry) went with **Phase 37.4 D-03**; the consume condition here is `claimed_at IS NOT NULL AND consumed_at IS NULL`, and a transient commit failure is not retried under an attempt identity — it surfaces as the ordinary failure and leaves the challenge claimed and dead, exactly as it does for create-user and upgrade. **The HMAC keyring** behind `:76`'s `idp_account_hash` and `:88`'s actor-subject hashing went with **Phase 37.4 D-11**; only the deferred web branch needed one. **The route registry and its startup route-enumeration assertion** (`:16`) went with **Phase 37.1 D-06**; route categorisation is carried by `tests/unit/test_app_wiring.py` alone, and the conflict that creates is the one already flagged under FOUND-01.
  >
  > **The two counts, re-derived rather than inherited (2026-09-03).** Six sections of `SHARED-INVARIANTS.md` were re-read against what this phase shipped before any number was stated, and `06-claim-anonymous-grant.md` was read in full. **The result: all four new conflicts are against the brief, and not one of the six invariant sections produced a new divergence.** A null result is only useful if it says what it examined, so: **§ "Identity and ownership"** requires the stored provider column to be the sole per-request classifier and forbids rederiving it — this route performs **no** Firebase read at all, D-08 reads `external_identities.provider` and nothing else, usage rows are keyed by `core.access_grants.id`, and no identity row is deleted or retired. **§ "The barrier"** requires that only create-user be pre-auth-callable — this route sits behind `get_linked_identity`, the handler re-verifies nothing, and the new route is categorised through the surviving `tests/unit/test_app_wiring.py` mechanism, which is the FOUND-01 conflict rather than a new one. **§ "Fail-closed defaults"** requires that an exhausted retry never read as success (the three-attempt DeviceCheck budget exhausts to 503 with no grant), that divergent stored state be refused rather than repaired (an identity row that flipped to registered inside the window is refused, not converged), that every branch of one class return an identical status, body and timing (the four refusals return a byte-identical one-field 403, asserted by equality), and that a rejection leave exactly one structured line carrying its stable internal result (three event names for those four branches, one per class). **§ "Locks and transactions"** is where the brief's own step 11 conflicts with the invariants; that is treated under ANONGRANT-02 and is **not** a divergence of the code from either. **§ "Grants and evaluation time"** requires exactly one active grant per user (enforced by `ix_access_grants_one_active_per_user` and by the other-source refusal), the shared effective-grant predicate, an exhaustive source set with no generic writing path (the single-writer walk), no implicit allocation and no lazily minted usage row (the writer creates it in the same flush; the resolver still treats a missing one as a tripwire), and one captured evaluation time per request — which `get_evaluated_at` now makes **structural**, since the auth and sync services on this route take one cached instant rather than reading the clock twice. **§ "Global deletions"** forbids a `promo` source (none), any scheduled cleanup or background healer (none — the accepted burn at `:81` is remediated by an operator `manual` grant, not by a reconciler), any device-check component in a rate-limit key (vacuous here, there are no rate-limit keys), backend token minting (none), wildcard provider-callback membership (no callback route added) and any distributed lock, lease or multi-phase commit (none — the two-ledger model is precisely the refusal to build one). **The result: four new flagged conflicts, taking the header's count from six to ten, and three new uncounted divergences, taking the set of known divergences from nine to sixteen.** The gap between the two numbers is therefore **six**, and what sits in it is listed in the header: it is the difference between a counted conflict and a recorded override, forward flag or precedence resolution, and previous phases have kept it deliberately rather than reconciling it away.

- [x] **ANONGRANT-02**: The grant transaction honors the fixed global lock order — grant rows `FOR UPDATE` ascending by id, then their `core.user_monthly_usage` rows — with no provider or network call while a lock is held
  > **Met as written, and proven against a real database rather than by reading the writer (Phase 41, 2026-09-03).** The SQL `crud/grants.py::activate_anonymous_device_grant` actually emits is captured at `before_cursor_execute` and asserted to lock `core.access_grants` ascending by id and then `core.user_monthly_usage`, with **exactly two distinct tiers** and neither `core.external_identities` nor `core.users` among them; the identity row is revalidated by a plain non-locking re-read. Capturing the emitted SQL rather than comparing a mirrored literal is what lets a **third** tier added by a future writer fail a named case. Both Apple calls are asserted to precede the activation, and the crud writer is asserted unable to reach the vendor at all, by an import-root allowlist and a clean-subprocess import check. **Marked met on the same suite result quoted under ANONGRANT-01** — 950 unit, 226 e2e and 134 schema passed with `ruff check src tests` clean — and specifically on `tests/schema/test_grant_locks.py::TestTheActivationAddsNoThirdLockTier`, `tests/unit/test_claim_ordering.py` and `tests/unit/test_claim_precedence.py::TestNoVendorCallHappensUnderALockOrInsideTheTransaction`.
  >
  > **A CONFLICT INSIDE THE SPECIFICATION ITSELF, resolved by precedence — and deliberately NOT counted among this phase's four flagged conflicts (Phase 41 D-13, 2026-09-03).** `06-claim-anonymous-grant.md:80` orders the activation transaction *"lock target user first, then the user's whole live `core.access_grants` set FOR UPDATE in ascending grant-id order, then their `core.user_monthly_usage` rows"*. `SHARED-INVARIANTS.md` § "Locks and transactions" forbids exactly that: *"never an account/user-row lock tier ahead of the grant locks"*. These two texts cannot both be satisfied, and the conflict is between the brief and the invariants rather than between this code and either of them. **The invariants win**, by the rule stated at the top of `SHARED-INVARIANTS.md` and in this file's own header — grant rows `FOR UPDATE` ascending by id, then their usage rows, with the identity and user rows revalidated by a plain re-read taking no lock. **This is why it is recorded as resolved rather than flagged:** a flagged conflict is a place where this project knowingly diverges from binding text; here the binding text says what to do, and the code does it. It is counted only in the set of known divergences, because a reader comparing the brief against the code will still find a difference and is owed the reason.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 41 D-03, 2026-09-03). The database is checked before Apple is asked.** The brief orders it the other way and is explicit about why: `:73`–`:77` (step 8) put all gate work — the bit read, the bit write's budget, the vendor calls — **after the claim and before the preflight**; `:78` (step 9) places the DB eligibility preflight *"after gate state, before any vendor bit or gate slot is burned"* and closes with the rule this diverges from: *"no DB grant may substitute for or suppress the platform-gate read."* **What shipped runs the preflight first:** claim → eligibility preflight (`free_grant_consumed_at` and the account's grant history) → Apple bit read → Apple bit write → activation transaction. **The observable cost, stated plainly:** an account the database already refuses never costs an Apple round trip, and therefore never learns that its device's bit is set — it answers the eligibility refusal (403 `operation_not_allowed`) where the brief would answer `device_grant_exhausted`. **The mitigating fact, equally plainly:** the eligibility check is repeated inside the locked activation transaction regardless, against the same lifetime index that arbitrates the race, so the platform gate is **deferred past a cheap refusal, not skipped** — for every account that can actually receive a grant, the bit read and the confirmed bit write both happen, before the transaction opens, exactly as `:79` requires. The two-ledger model at `:90` is unchanged: the database stays authoritative for user-received-grant and the vendor bit for device-slot.
  >
  > **Implemented as written, listed so nobody re-derives it from the brief.** bit0 only and bit1 never written with a value this phase invented — the update carries the bit1 the query returned; no client-supplied bit value is ever read; the write is fail-closed and load-bearing, so only Apple's explicit confirmation permits activation and every exhausted failure, timeout or ambiguity answers `verification_temporarily_unavailable` with no grant; every claim performs its own read and its own write with no caching or coalescing; the query token is never reused for the update and each call carries its own transaction id, which is never the challenge handle; raw tokens reach no log, no row and no error message — `auth/devicecheck.py` holds no logger at all, so no code path exists that could write one. The crash-after-write-before-commit case at `:81` is accepted and uncompensated as the brief specifies: the device slot burns with no grant, bits are never auto-cleared, and remediation is an operator `manual` grant.

- [x] **ANONGRANT-03**: The one-free-grant-per-account rule is enforced here; no grant row, free credit, or usage row is created as a side effect of any other path
  > **Met as written, with the concurrency half proven live rather than argued (Phase 41, 2026-09-03).** The lifetime rule is enforced by a read carrying **no status predicate** — matching `ix_access_grants_one_free_grant_per_user_source`, which has none — over the named `FREE_GRANT_SOURCES` set, so a free grant that was revoked, expired or superseded still refuses a second claim. The named set is tied to the live index predicate's membership by a schema case that was proven to fire: narrowing the constant to one member was applied, observed to fail, and reverted. Two simultaneous first claims for one account, driven through the production completion on two independent connections against real PostgreSQL, leave **exactly one** grant row, one anti-abuse row, one usage row and one `free_grant_consumed_at` value, with the premise recorded at a barrier so the case cannot degrade into two sequential runs. The negative half — that no other path allocates — is bounded by the single-writer walk over `src/`. **Marked met on the same suite result quoted under ANONGRANT-01** — 950 unit, 226 e2e and 134 schema passed with `ruff check src tests` clean — and specifically on `tests/schema/test_claim_race.py`, `tests/schema/test_grant_locks.py::TestTheFreeGrantSourceSetMatchesTheIndex` and `tests/e2e/test_claim_anonymous_grant.py::TestTheFourRefusals`.
  >
  > ⚠️ **Corrected by Phase 42 (D-07), 2026-09-03 — one successful anonymous claim now writes three rows, not four.** The paragraph above says the race leaves *"exactly one grant row, one anti-abuse row, one usage row and one `free_grant_consumed_at` value"*. That was true until this date. `core.access_grants_anti_abuse` and its model `AccessGrantAntiAbuse` were deleted, and `crud/grants.py::activate_anonymous_device_grant` stopped writing the receipt row. **One successful anonymous claim writes the grant row, its usage row and `free_grant_consumed_at`.** **The device platform survives and is recorded on the identity row:** `external_identities.native_claim_platform` still holds `ios_devicecheck`, the `core.native_claim_provider` enum that types it was kept for exactly that reason, and a live case in `tests/schema/test_claim_race.py` asserts it. **Nothing this requirement asserts stopped holding:** the lifetime rule, the named `FREE_GRANT_SOURCES` set, the two partial unique indexes and the single-writer bound are untouched, and the race case still proves one allocation. The receipt row decided nothing — every fact on it existed elsewhere. The deletion is recorded as a flagged conflict under REGGRANT-02. **The original text above is left as written**, because it records what Phase 41 delivered.
  >
  > **The negative half is a grep-shaped guarantee, not a database one — stated as it is rather than as it reads.** `tests/unit/test_grant_sources.py` walks today's `src/` and records the set of modules naming the member. It bounds this tree; it does not bound a path a future phase adds. The planner recorded this as flagged assumption `EDGE-ANONGRANT-03-unclassified` and it remains unresolved.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 41 D-08, 2026-09-03). Only anonymous identities may claim.** The brief admits registered native claimants twice and neither passage is amended: `:51` defines native claimants as *"an active linked identity that is either anonymous OR registered with stored provider `google`/`apple` (a registered claimant burns the device bit the same way; same grant, same cap)"*, and `:83` says in terms that *"a registered native claimant is legitimate"*. **What shipped refuses them.** The route sits behind `get_linked_identity` and the service then requires `identity.identity.provider is IdentityProvider.anonymous`, reading the stored column and nothing else; a `google` or `apple` caller is refused with the shared 403 `operation_not_allowed` through `ClaimantNotAnonymous` and waits for Phase 42. The identity row is re-read inside the activation transaction, so a row that flipped to registered inside the window is refused the same way. **The fallback this gives up, named so it is not rediscovered as a bug:** a registered user whose Google or Apple account has already spent its own grant can no longer fall back to the device gate for a second free grant — under the brief they could, at the cost of one claimant class becoming two. That fallback is **recorded under Deferred Ideas in `41-CONTEXT.md`, not lost.** Declined on the merits: one claimant class, and the grant means what its name says.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 41 D-09, 2026-09-03). A repeat claim is an idempotent 200.** The brief forbids it twice and in the same words both times: `:26` describes the destination-already-holds-an-active-grant path as *"an existing active `anonymous_device_grant` included, which is never idempotent success"*, and `:78` repeats *"An existing active `anonymous_device_grant` fails under the active-grant-invariant path — NOT idempotent success"*. **What shipped:** when the account already holds an **active** `anonymous_device_grant`, the preflight returns before Apple is reached, **nothing is written**, Apple is not called at all, and the response is 200 carrying the current entitlement — byte-identical to what a fresh claim returns, because both are assembled by the same post-commit read. This follows Phase 40 D-04's idempotent repeat for the upgrade. **The two states that still refuse, so this is not read as "repeats always succeed":** a free grant that was consumed but is **no longer active** — revoked, expired or superseded — refuses 403 (the lifetime rule, `FreeGrantAlreadyConsumed`), and an **active grant of another source** — a subscription or a `manual` grant — refuses 403 (one active grant per user, `OtherActiveGrantHeld`). The race loser answers the same 200 by the same path (D-13), which is a third way to arrive at the repeat's body rather than a fourth outcome.
  >
  > **Where the outcomes are decided, corrected against what shipped rather than what was expected.** All seven post-claim outcomes — the idempotent repeat, all four refusals and all three Apple failure arms — are decided **after** the challenge is claimed, and every one of them **consumes the challenge exactly once**, including the registered-caller refusal, whose check is the first thing the post-claim work does. **Nothing among the seven rejects before the claim.** The only pre-claim rejections are the three handle-level ones (unknown handle, identity mismatch, operation mismatch) and the two losses of the claim's own conditional update (expired, already consumed); all five spend nothing. This is compliance with `:87`, not a divergence — *"a claimed challenge is dead — any post-claim failure consumes it"* — and it is written down because a reader who assumed the registered-caller refusal happens before the claim would look for a pre-claim rejection that does not exist.
  >
  > **A production defect this phase's own concurrency proof found and fixed, recorded because the behaviour it broke is the one D-09 and D-13 promise.** The loser arm's `session.rollback()` expires **every** instance in the session, including the `User` and `ExternalIdentity` rows the router then reads for `identity.user.id` and `identity.identity.provider`; each was a lazy load with no greenlet, so a genuine race answered **500** on exactly the path that must answer 200. Three lines in `services/auth.py` reload both instances where an `await` is still available. Nothing caught it earlier: the unit case uses a stub session that neither expires nor lazy-loads, and no e2e case can produce a real index conflict. **The residual concern is open, not closed:** the fix is proven at the service layer by a real two-connection race, but no case drives two concurrent HTTP requests through the router, and the same expire-after-rollback hazard exists anywhere a service rolls back and its caller then reads an instance loaded before the rollback. Recorded as coverage `D8` with `human_judgment: true` in `41-04-SUMMARY.md`.

### REGGRANT — Phase 42 (`07-claim-registered-grant.md`) — `POST /auth/claim-registered-grant`

- [x] **REGGRANT-01**: The endpoint is the only creator of `source='registered_account_grant'` rows, across prepare and completion modes
  > **The single-writer half is met as written, and proven against the whole tree rather than asserted (Phase 42, 2026-09-03).** The literal `registered_account_grant` is constructed from exactly one site in `src/` — `crud/grants.py::activate_registered_account_grant`. `tests/unit/test_grant_sources.py::TestTheRegisteredAccountGrantHasExactlyOneWriter` walks every module under `src/` for that construction. The walk is known to fire against the real tree, not only against its synthetic control: a second construction site was added to `services/auth.py`, observed to fail a named case, and reverted, and the site was then moved out of the writer inside the same module and failed a different named case. **Marked met on a suite run made in plan 42-06 itself, not copied from a plan or a summary:** `pytest -q` **1001 passed**, `pytest -m e2e -q` **237 passed**, `pytest -m schema -q` **147 passed**, `ruff check src tests` **exit 0**. What follows is what this phase decided **differently from what was asked for**, recorded here rather than by editing `07-claim-registered-grant.md`, which stays verbatim (D-15).
  >
  > **Amended by Phase 42, 2026-09-03 — the second half of this requirement's own wording describes machinery that was gone before this phase existed.** "Across prepare and completion modes" restates `07-claim-registered-grant.md:7` and `:41`: a `?challenge=true` query flag partitioning prepare from completion on one endpoint. **That partition does not exist.** It was replaced by a dedicated issuance route in **Phase 37.2** (D-01/D-03/D-05), so prepare is `POST /auth/challenge`, which already issues for `claim_registered_grant`, and completion is `POST /auth/claim-registered-grant`. This is **amended, not withdrawn, and files no new conflict**: both phases still exist, and their order is enforced, because completion requires a handle that route issued and bound to the caller's `core.external_identities.id`. The mode-signal divergence is the one already recorded under CREATE-02, UPGRADE-02 and ANONGRANT-01. This is the same treatment ANONGRANT-01 received on the same date.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 42 D-04, 2026-09-03). The body carries one device token.** `:52` requires *"separate per-transaction DeviceCheck query + update tokens"* on every iOS claim, and `:31` repeats that this route reuses phase 06's adapters unchanged. **What shipped is one field.** `GrantClaimRequest` carries `challenge_id` and one `device_token`, and that one token drives both the Apple read and the Apple write. **The ground is a defect Phase 41's own code review found and fixed, not a preference:** two opaque DeviceCheck tokens cannot be tied to one device, so device A's token as the query and device B's as the update passed every account and bought unlimited free grants. The registered route inherits the fixed shape rather than reintroducing the defect. `AnonymousGrantClaimRequest` was renamed `GrantClaimRequest` and is now shared by both claim routes, so there is one body model and not two copies of one fact. **An absent or empty token is the framework's 422**, which is the same uncounted divergence recorded under ANONGRANT-01, applied to the same field on a second route rather than a new one.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 42 D-05, 2026-09-03). The claimant is read from the stored identity row, and Firebase is never asked.** The brief requires two things this phase did not build. `:47` (step 2) requires that a caller whose stored provider is not `google` or `apple` be audited `idp_account_not_eligible` and answered **`verification_required`**. `:51` (step 6) requires *"exactly one `getUser(subject)` read through the issuer-selected Admin client, on EVERY branch, the idempotent repeat included; no branch may skip it"*, with a closed classifier over the live `providerData` result and a match against both the stored provider and the stored `provider_uid`; `:73` states the same rule as a hardening and names this endpoint as one of exactly five providerData read points. **What shipped:** the route sits behind `get_linked_identity`, and the service requires `identity.identity.provider` to be `google` or `apple`, read from the stored column and from nothing else. An anonymous caller is refused with the shared 403 `operation_not_allowed` through `ClaimantNotRegistered`, the fourth `ClaimRefused` leaf. **No Firebase read happens on this route at all**, so the confirmation point at `:73` is four points and not five, and `verification_required` is still not a registered code — Phase 41 D-11 already declined to register it, and this phase adds no producer for it. **The ground:** the claim never writes the identity binding, so there is no drift for a live read to prevent, and account uniqueness rests on `ix_external_identities_provider_account`, which constrains the stored value. A live read would add nothing to uniqueness. **The cost, stated plainly:** a stored binding that no longer matches the live Firebase account is not detected here. The identity row is re-read inside the locked transaction by a plain read and re-checked, so a row that flipped inside the window is refused; a row that is stale in Firebase and unchanged in this database is served.
  >
  > **FLAGGED, NOT COUNTED (Phase 42 D-16, 2026-09-03). Every eligible new-grant claim makes an unbounded pair of Apple round trips.** With the backend rate-limit engine deleted from the product (Phase 35 D-05) and the Envoy gateway contract deferred to v2.1 (Phase 35 D-08), a caller holding a valid ID token for an **eligible** registered account can make the backend call Apple as often as it can prepare challenges. **Accepted and flagged, on the Phase 41 D-20 and Phase 40 D-22 precedent.** The mitigating facts: the caller must already hold a valid token for an existing linked `google` or `apple` account, so this is one subject looping on itself; the preflight refuses an ineligible account **before** Apple is reached, so the loop is open only to an account that has not yet claimed; and each turn costs a fresh challenge from `POST /auth/challenge`. **This route's exposure is narrower than the anonymous route's**, because the conversion destination reaches Apple not at all (D-02, under REGGRANT-02) — an account holding an active anonymous grant cannot use this route to reach Apple even once. **Closes with:** the v2.1 gateway contract. **Not counted as a new conflict**, because it is a consequence of the pre-existing Phase 35 D-05 `limits` override recorded at `:50` and under FOUND-06.
  >
  > **What the brief asks for on this route that this phase did not build, because it was already deleted from the product.** Read `07-claim-registered-grant.md` without this paragraph and every item below reads as unmet work by this phase. None of it is. **Rate limiting and every vendor budget — none of it exists.** `:7` puts rate-limit config entries in scope, `:19` describes the engine as foundation-provided, `:28` names the two new entries for this route, `:29` names eight reused entries, `:43` makes the prepare entry an admission gate, `:49` makes the completion entry one, `:52` and `:53` put five vendor budgets immediately before their vendor calls with four dedicated exhaustion results, `:17` registers `rate_limited`, and `:74` states the whole policy. **Phase 35 D-05 deleted the backend rate-limit engine from the product rather than deferring it**, so those entries have nowhere to live and the 429 at `:49` is unreachable on this route. What survives of `:55`'s retry policy is `tenacity`: three attempts total, in `auth/devicecheck.py`. **The `audit.auth_events` row and the internal-result vocabulary — the subsystem is gone.** `:18` describes the audit writer, `:27` enumerates three new `core.auth_event_result` values this phase would add and eighteen it would reuse, `:41`, `:47`, `:49`, `:51`, `:53`, `:58` and `:61` each name an audited result, `:62` (step 6) requires the success audit inside the consuming transaction, `:67` maps every class to an internal result, and `:75` states the one-row-per-attempt contract in full. **Phase 37.1 D-01 deleted the table, its writer and every call site, and Phase 38 D-03 struck § "Audit" from `SHARED-INVARIANTS.md` outright.** The distinct-result obligation survives only as structured-log event names, produced from exception class names — which is why this route's four refusals are four classes behind one byte-identical 403 body. **The mode signal and its classifier** (`:7`, `:41`, `:43`) went with **Phase 37.2**; `classify_mode_signal` does not exist. **`claim_attempt_id`** (`:50`'s claim stamp and `:64`'s same-attempt retry) went with **Phase 37.4 D-03**; the consume condition here is `claimed_at IS NOT NULL AND consumed_at IS NULL`, and a transient commit failure is not retried under an attempt identity. **The HMAC keyring** behind `:31`, `:48` (step 3), `:72`, `:74` and `:75` went with **Phase 37.4 D-11**, and D-08 here declines to rebuild it: a hash of `provider_uid` protects nothing while `provider_uid`, `subject` and `email` sit in plain text beside it. **The route registry and its startup route-enumeration assertion** (`:7`, `:16`, `:26`) went with **Phase 37.1 D-06**; route categorisation is carried by `tests/unit/test_app_wiring.py` alone, and the conflict that creates is the one already flagged under FOUND-01. **The two Firebase confirmation points** (`:51` and `:73`) are not built here: **Phase 41 D-08** established that no claim route performs a Firebase read, and D-05 above keeps that rule on this route.
  >
  > **The two counts, re-derived rather than inherited (2026-09-03).** Six sections of `SHARED-INVARIANTS.md` were re-read against what this phase shipped before any number was stated. **The result: all six new conflicts are against the brief or the schema reference, and not one of the six invariant sections produced a new divergence.** A null result is only useful if it says what it examined, so: **§ "Identity and ownership" — none.** The stored `provider` column is the sole classifier on this route, and nothing rederives it; usage rows stay keyed by `core.access_grants.id`, including the new row a conversion inserts; no identity row is deleted or retired. The section's five-read-point clause is about where a live read may happen, not where one must, so performing none is not a divergence from it — the divergence is from the brief, and is recorded above. **§ "The barrier" — none.** The route sits behind `get_linked_identity`, the handler re-verifies nothing, and the route is categorised through the surviving `test_app_wiring.py` mechanism, which is the FOUND-01 conflict rather than a new one. **§ "Fail-closed defaults" — none.** An exhausted DeviceCheck retry answers 503 with no grant; a divergent stored row is refused and never converged; the four refusals return a byte-identical one-field 403, asserted by raw-byte equality at two depths; each refusal leaves one structured line named for its own class. **§ "Locks and transactions" — none for the code.** The brief's own step 11 conflicts with this section; that is treated under REGGRANT-02 and is not a divergence of the code from either text. **§ "Grants and evaluation time" — none.** Exactly one active grant per user is held by expiring the anonymous row before the registered row is inserted, and by `ix_access_grants_one_active_per_user`; the shared effective-grant predicate is used; the source set is unchanged and exhaustive, with one writer per free source; nothing is allocated implicitly and no usage row is lazily minted; one captured instant per request is structural through `get_evaluated_at`. **§ "Global deletions" — none.** No `promo` source, no cleanup or healer job, no device-check component in a rate-limit key (vacuous, there are no rate-limit keys), no backend token minting, no wildcard callback membership and no distributed lock. **A named null result worth stating: `SHARED-INVARIANTS.md` names no anti-abuse row anywhere.** The phase's largest deletion therefore diverges from `06-schema-reference.md` and from the brief, and from no invariant text. **The result: six new flagged conflicts, taking the header's count from ten to sixteen, and one new uncounted divergence, taking the set of known divergences from sixteen to twenty-three.** The gap between the two numbers is therefore **seven**, and every item in it is listed in the header. The gap is the difference between a counted conflict and a recorded override, forward flag or precedence resolution, and it has been kept deliberately since Phase 37.5.

- [x] **REGGRANT-02**: Supersession conversion of an active anonymous device grant happens inside one transaction under the same fixed global lock order, never leaving two active grants
  > **Met as written, and proven against a real database rather than by reading the writer (Phase 42, 2026-09-03).** The SQL `crud/grants.py::activate_registered_account_grant` actually emits is captured at `before_cursor_execute`. It locks `core.access_grants` ascending by id, then `core.user_monthly_usage`, with **exactly two distinct lock tiers** on the conversion and neither `core.external_identities` nor `core.users` among them; the identity row is revalidated by a plain non-locking re-read. A third tier added by a future writer fails a named case, which a mirrored literal could not detect. The `UPDATE` that expires the anonymous row is asserted to precede the `INSERT` of the registered row, from captured SQL. Two simultaneous conversions on two independent connections against real PostgreSQL leave one active registered grant, one expired anonymous row and two grant rows in total, with `monthly_period` and `monthly_used` carried across unchanged. **Marked met on the same suite run quoted under REGGRANT-01** — 1001 unit, 237 e2e and 147 schema passed with `ruff check src tests` clean — and specifically on `tests/schema/test_grant_locks.py::TestTheConversionExpiresBeforeItInserts`, `::TestTheRegisteredWriterAddsNoThirdLockTier`, `tests/schema/test_claim_race.py::TestTwoSimultaneousConversionsSupersedeOnce` and `tests/unit/test_claim_ordering.py::TestBothVendorCallsPrecedeTheRegisteredActivation`.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 42 D-02, 2026-09-03). The conversion path makes no Apple call.** The brief gates every iOS claim and is explicit about it: `:52` (step 7) makes the DeviceCheck material *"unconditionally mandatory on every iOS claim, no availability gate"*; `:53` (step 8) reads bit1 *"before the DB preflight"*; `:55` (step 10) makes the bit write load-bearing after the preflight on every device-checked kind. **What shipped calls Apple on the new-grant destination only.** A caller who holds an active `anonymous_device_grant` already paid one device slot, because bit0 is the only way that grant can exist. The conversion issues no new allowance, so it spends no new slot, and the device token in the body goes unused on that path. Both e2e conversion cases assert that the read list and the write list are empty. **The consequence, stated plainly and accepted:** one device can pay for two allowances — one anonymous and one registered-new — instead of one. **The mitigating fact:** the second allowance is not a second entitlement. A conversion moves the one allowance to a higher tier and leaves one active grant, and a device that has already paid for an anonymous grant on one account cannot use that grant to obtain a registered grant on another, because the conversion reads the caller's own grant rows.
  >
  > **FLAGGED CONFLICT against `06-schema-reference.md` and the brief — recorded, NOT resolved (Phase 42 D-07 and D-08, 2026-09-03). Three tables left the single migration.** `06-schema-reference.md:287` creates `core.access_grants_anti_abuse`, `:349` creates `core.provider_accounts`, `:357` creates `core.provider_account_gate_consumptions` and `:347` creates the `core.gate_consumption_kind` enum; their semantics at `:1127` and `:1257`, and the schema-specific invariants at `:1538`, `:1546` and `:1562`, describe rules over them at length. The brief writes them: `:48` (step 3) derives `idp_account_hash`, `:59` (transaction step 3) inserts the new grant's anti-abuse row, `:60` (step 4) does the same for a new grant, and `:61` (step 5) resolves the `core.provider_accounts` row and inserts the gate-consumption row. **All of it is deleted.** The three tables left `migrations/20260818_01_initial-release.sql` with everything only they need: the enum, two generated columns on `core.access_grants`, both deferred foreign keys, `UNIQUE (id, source)` and two indexes. `AccessGrantAntiAbuse` left the model layer, and Phase 41's writer stopped writing the receipt row. **The ground:** the anti-abuse row was a receipt and decided nothing. Every fact on it existed elsewhere — the grant's own `source`, the identity row's `native_claim_platform` and the identity row's `provider_uid` — and the two provider tables had no writer anywhere in `src/`. **What is kept, and checked one by one:** `core.native_claim_provider` survives, because `external_identities.native_claim_platform` binds it and that column stays the record of the device platform; both partial unique indexes over `core.access_grants` survive verbatim with their predicates; `ix_external_identities_provider_account` survives. **D-08 is recorded in the same entry, because it is the same decision seen from the other end:** no hash column, no key and no key version were introduced anywhere, and `provider_uid` stays raw. A hash of `provider_uid` protects nothing while `provider_uid`, `subject` and `email` are stored in plain text beside it, and a key would become a single point of failure for identity matching. **What this gives up:** the web branch, when it exists, needs a table that records the IdP account of an anonymous claimant. That is recorded under Deferred Ideas in `42-CONTEXT.md`, and it is rebuilt there on its own terms. **One-way:** this is an in-place edit of the one migration under SCHEMA-01, authorised at a blocking decision gate, and cheap today because there are no users.
  >
  > **The brief's own lock order conflicts with the invariants, and is resolved by precedence — the same item ANONGRANT-02 records, applied to a second route, and NOT a new divergence.** `07-claim-registered-grant.md:56` orders the completion transaction *"lock target user first, then every live `core.access_grants` row + corresponding anti-abuse row FOR UPDATE in ascending grant-id order, then usage rows"*, and `:76` repeats it as a hardening. `SHARED-INVARIANTS.md` § "Locks and transactions" forbids *"an account/user-row lock tier ahead of the grant locks"*. **The invariants win**, by the rule at the top of that file. The writer locks grant rows ascending by id, then their usage rows, and revalidates the identity row by a plain re-read taking no lock. It is counted neither among this phase's six conflicts nor as a new divergence: it is one item, recorded when Phase 41 met it, and Phase 42 applies the same resolution to the same rule.
  >
  > **Two facts about the conversion, measured in this phase and recorded because both contradict what was predicted.** **(1) The explicit flush boundary between the expiry and the insert is redundant, and is kept anyway.** Research assumption A1 said SQLAlchemy orders INSERTs before UPDATEs within one flush. Measured at the statement level with the boundary removed, and again under contention with it removed, SQLAlchemy 2.0.46 emits the `UPDATE` before the `INSERT` on its own. The boundary is kept as the guard against an ORM upgrade inverting that default. The ordering case asserts the emitted **order**, not the mechanism, so it stays the detector whichever mechanism produces the order. **(2) The conversion race loser raises no `IntegrityError`.** It blocks on `FOR UPDATE`; when the winner commits, the row it locked is `expired` and no longer matches the statement's predicate, so its lock read returns nothing and the writer's in-lock re-decision refuses it, having emitted no write. The unique indexes arbitrate the **new-grant** destination, where there is no row to lock. Both outcomes satisfy D-13, and the phase's prose must not claim one mechanism for both.

- [x] **REGGRANT-03**: The one-free-grant-per-account interplay with an existing anonymous device grant resolves without double-allocating entitlement
  > **Met as written, with the interplay proven at three depths (Phase 42, 2026-09-03).** The rule is enforced by a grant history read by **source and status**, at the preflight and again inside the locked transaction. Ten post-claim outcomes are named cases against a stub session, each asserting its own outcome and a challenge-consumption count of exactly one; the four refusals are asserted byte-identical on the wire at both depths, so the route is no account-state oracle; and both destinations are raced on two real connections. **Marked met on the same suite run quoted under REGGRANT-01** — 1001 unit, 237 e2e and 147 schema passed with `ruff check src tests` clean — and specifically on `tests/unit/test_claim_precedence_registered.py`, `tests/e2e/test_claim_registered_grant.py::TestTheFourRefusals` and `tests/schema/test_claim_race.py::TestTwoSimultaneousRegisteredClaimsAllocateOnce` with `::TestTwoSimultaneousConversionsSupersedeOnce`.
  >
  > **The guard is the history read, and neither spent signal may replace it — recorded because either one used as a blanket test breaks the destination this phase exists to add.** On the conversion path `free_grant_consumed_at` is already set **and** `has_prior_free_grant` is already true. A blanket guard on either would refuse the conversion with 403. A named case sets both signals true and asserts the conversion still happens, and a mutation that hoisted a `free_grant_consumed_at` guard above the destination decision failed exactly that case.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 42 D-06, 2026-09-03). Account uniqueness rests on the indexes the schema already carries.** The brief builds a global registry: `:61` (transaction step 5) resolves or creates a `core.provider_accounts` row and inserts a `registered_account_grant` gate-consumption row, unique per provider account, and makes a conflict there roll back and return the new class **`account_already_claimed`**, which `:25` registers and `:67` describes as terminal for the provider account and never to be conflated with `device_grant_exhausted`. `:72` states the same rule as a uniqueness hardening. **What shipped adds nothing.** `ix_external_identities_provider_account` — `UNIQUE (issuer, provider, provider_uid)`, no state predicate — allows one identity row per Google or Apple account, ever. `UNIQUE (user_id)` ties that row to one user. `ix_access_grants_one_free_grant_per_user_source` and `free_grant_consumed_at` allow that user one free grant per source. So one provider account cannot reach a second registered grant, and `account_already_claimed` is not added; `ErrorCode` stays at 18 members. **What is given up:** the brief's registry survives a user row's deletion, and these indexes do not. Identity rows are never deleted in this product (§ "Identity and ownership"), so the two bounds coincide today. **What a reader must not conclude:** the deleted `core.provider_accounts` (D-07, under REGGRANT-02) was never the live control here — it had no writer in `src/` at any point.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 42 D-09(b), 2026-09-03). The refusal carries no end date.** `:58` (transaction step 11.2(b)) requires that an account holding any other active grant be refused `operation_not_allowed` *"carrying machine-readable `held_grant_ends_at` (the held grant's `ends_at`, or `null` when open-ended; nothing else about the held grant disclosed)"*, and calls the refusal *"a wait, not a forfeit"*. `:25` registers the field as a class-specific field of the shared response shape and `:67` repeats it. **What shipped is the bare 403.** `OtherActiveGrantHeld` declares no field, no status and no code of its own, and the body is the one-field `{"code":"operation_not_allowed"}` every other refusal on this route returns. **The ground:** § "Errors" requires that within a class the body be identical across every triggering branch, and a field present on one refusal and absent on three would let a client distinguish which internal condition fired. **What is given up:** a client cannot tell the user when to retry, and must poll. `held_grant_ends_at` is recorded under Deferred Ideas in `42-CONTEXT.md`; reopen it if a client needs the date.
  >
  > **The five destinations, so a later reader does not re-derive them from the brief.** In this order: an active `registered_account_grant` is an idempotent 200 that writes nothing; any other active grant that is not an `anonymous_device_grant` is refused through `OtherActiveGrantHeld`; an active `anonymous_device_grant` is converted; no free-grant history and no active grant is a new grant; and free-grant history with no active free grant — a revoked or expired anonymous grant — is refused through `FreeGrantAlreadyConsumed`. The same five are decided in the preflight and again inside the lock. Every one of them consumes the challenge exactly once, and nothing consumes before the claim.

### APPLEHOOK — Phase 43 (`08-webhook-app-store.md`) — `POST /webhooks/app-store`

- [x] **APPLEHOOK-01**: The endpoint ingests Apple App Store Server Notifications outside the auth dependency, authenticated solely by verifying Apple's signed `signedPayload` JWS
  > **Adapter seam flagged forward by Phase 37.2 (D-09), 2026-08-25 — the loss is recorded; the replacement is not designed here.** `StoreAdapter`, `VerifiedNotification`, `VerifiedTransaction` and `StoreState` — the store-ingestion Protocols Phase 35 declared under FOUND-08 — were deleted as zero-consumer code before this phase exists. Nothing regressed: they had no implementations and no callers. Phase 43 defines its store adapter interface **beside its first implementation** rather than inheriting a declared-only seam, the same treatment Phase 37.1 gave the provider-callback category under APPLEHOOK-02.
  >
  > **Met as written, and measured rather than read from the code (Phase 43, 2026-09-04).** `POST /webhooks/app-store` sits on a dedicated router outside the auth dependency and reads no `Authorization` header at all. Its only credential is Apple's `signedPayload`. `auth/app_store.py::AppStoreNotifications.verify` calls Apple's `SignedDataVerifier` three times — the envelope, the nested signed transaction and the nested signed renewal information — and each nested payload is verified on its own before it is used as purchase evidence. The seam holds no logger, so no code path can write a payload to a log line. Verification runs in the route's dependency, **before** `get_db`, so a refused payload never opens a database session. One transaction then upserts `core.subscriptions` by `(provider, external_id)`, writes one `core.store_purchases` row, appends one `audit.subscription_events` row keyed on `notification_uuid`, and writes the `core.access_grants` row with its fresh `core.user_monthly_usage` row under the fixed lock order; 200 is answered only after the one `commit()`. **The chain walk runs for real in the unit suite:** a throwaway root, intermediate and leaf carrying Apple's two required OIDs mint ES256 payloads the library accepts, and the vendored Apple Root CA G3 refuses the same payload — the control that makes the case non-vacuous. **Marked met on a suite run made in plan 43-06 itself, not copied from a plan or a summary:** `pytest -q` **1089 passed**, `pytest -m e2e -q` **272 passed**, `pytest -m schema -q` **182 passed**, `ruff check src tests` **exit 0**. What follows is what this phase decided **differently from what was asked for**, recorded here rather than by editing `08-webhook-app-store.md`, which stays verbatim (D-27).
  >
  > **Re-measured after gap closure, and the basis of "met" replaced (quick task 260904-u7t, 2026-09-04).** The `1089 / 272 / 182` measurement recorded in the paragraph above was taken in plan 43-06 **before** the four critical findings in `43-REVIEW.md` were fixed, and the phase's first verification (`43-VERIFICATION.md`, 2026-09-04) therefore read `gaps_found`, `4/6`: an out-of-order Apple delivery downgraded a paying buyer's grant (CR-01), a grace-period grant was written with an already-past term and was never effective (CR-02), a purchase first seen without an `appAccountToken` permanently 500'd once a real one arrived (CR-03), and the shipped `.env.example` crashed the whole service's boot rather than costing this one route its 503 (CR-04). All four are now closed — commits `29082dd`..`f9b2557`. The paragraph above is **kept unedited as the dated record of what was measured then**; APPLEHOOK-01's "met" now rests on `pytest -q` **1098 passed**, `pytest -m e2e -q` **272 passed**, `pytest -m schema -q` **189 passed**, `ruff check src tests` **exit 0**, re-run by the verifier rather than copied, and on re-verification `passed`, `6/6`, which supersedes the `4/6` report. **No divergence count changes**: the four were implementation defects, not decisions against the brief. **One advisory residual is recorded, not fixed** — `core.store_purchases` is never backfilled, so a purchase first recorded unattributed keeps its server-minted `identity_value` and NULL `resolved_token_value` forever; the subscription row gains the owner and the buyer gets the grant, so the route recovers, but a reader of `core.store_purchases` must not mistake a stale `identity_value` for a live one. Backfill belongs to Phase 45.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 43 D-02, 2026-09-04). The router is always registered, and an incomplete configuration answers 503.** The brief asks for two things this phase did not build. `:26` requires that *"the route is not registered at all while Apple's store integration is unconfigured, and startup fails closed if the registered route lacks configuration its verifier requires"*, and `:49` repeats both as a hardening. **What shipped:** `routers/webhooks.py` is included in `app/main.py` like every other router, in every environment. When `config.app_store` is incomplete, lifespan logs one `app_store_configuration_absent` warning, `build_app_store_verifier` answers `None`, and the route answers **503 `verification_temporarily_unavailable`** on use. The application boots. **The ground:** the route set is then identical in every environment, which is what the wiring test assumes — `tests/unit/test_app_wiring.py` imports the app object, and configuration loads in lifespan **after** the routes exist, so a configuration-dependent route set would make the partition uncountable at import time and take APPLEHOOK-02's own mechanism with it. **What is given up:** a misconfigured deployment serves a registered route that can never accept a notification, rather than refusing to start. **The mitigating fact:** 503 is a retryable status, so Apple's schedule covers the window while an operator supplies the three values, whereas an unregistered route answers 404 and Apple abandons it. `production` with no `app_apple_id` yields no verifier rather than the library's `ValueError`, so an incomplete configuration cannot kill the pod at boot; that is an executed case.
  >
  > **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 43 D-04, 2026-09-04). The body shape is the shared one.** The brief says it three times and no passage is amended: `:7` (*"never emits the shared client-visible error classes"*), `:21` (*"the route answers with plain HTTP status codes only, never the shared client-visible error classes"*) and `:32` (*"answers with plain HTTP status codes, never the shared client-visible error classes"*). **What shipped, split into the half that complies and the half that does not.** The **classes are the route's own**: `NotificationRejected` (401) is one class for every verification failure, `UnmappedStoreProduct` (500) and `AttributionConflict` (500) are its two internal leaves, and **no `ErrorCode` member was added** — the vocabulary is unchanged. The **body is the shared one-field shape**, `{"code": "auth_required"}` at 401 and `{"code": "internal_error"}` at 500, because every status this application answers is mapped to a class and reaches one response builder. **The brief's "plain status" cannot be answered any other way here:** since Phase 37.4 D-08 there is one exception tree behind one handler, and even a bare `HTTPException` is mapped to the class its status declares, so a body-less status would need a second response path — the exact thing `SHARED-INVARIANTS.md` § "Errors" forbids. **What is given up:** Apple reads the status and ignores the body, so the cost is a four-field-less JSON object on the wire and nothing else. **What is kept, and it is the property the brief was protecting:** the route is no oracle. All seven reachable verification statuses answer the **byte-identical** `b'{"code":"auth_required"}'`, compared against `response.content` rather than parsed, and the failing stage goes to the server-side log only, as `stage`.
  >
  > **FLAGGED DEFERRAL, counted as a conflict — recorded, NOT resolved (Phase 43 D-06, 2026-09-04). The gateway carries no limit on this path.** The brief requires per-IP and per-URL gateway limits on this exact path three times: `:24` scopes them into the phase, `:50` states them as a hardening and `:51` restates the policy. **What shipped:** `k8s/templates/httproute-webhooks.yaml` had its path literal renamed from `/webhooks/apple` to `/webhooks/app-store`, so the gateway rule matches the literal the application registers, and **nothing else was added** — one `Exact` match, one `POST`, one backend, no rate-limit entry, no `BackendTrafficPolicy` reference and no filter chain. `k8s/templates/security-policy.yaml` was not touched, so the route stays outside the JWT `SecurityPolicy` by the same absence that already made it credential-free, and the template now carries one line saying why. **Recorded as a flagged deferral to the v2.1 gateway contract, on the Phase 35 D-05 and D-08 precedent, and not as an omission:** Phase 35 deleted the backend rate-limit engine from the product and deferred every gateway limit to v2.1, so a limit added here would be the only one in the chart and would still not be the contract the deferral names. **It compounds with the residual recorded further below** — this route is credential-free at the backend and unlimited at the gateway — and the same v2.1 limit closes both. They are one item seen from two ends, not two.
  >
  > **A DIVERGENCE FROM APPLE'S PRODUCTION GUIDANCE — not from the binding specification, and therefore in neither count (Phase 43 D-09, 2026-09-04).** `enable_online_checks=False` is passed to `SignedDataVerifier`. Verification is then pure local computation on the `x5c` chain, with no per-request network call on the admission path, which is what the Phase 35 barrier rule requires and what makes the whole route reachable without a round trip to Apple. **The honest cost is larger than a revoked Apple intermediate going unnoticed, and it was measured, not reasoned about (43-RESEARCH P-09).** With online checks off, the library sets the certificate store's clock to the payload's own claimed `signedDate` before it walks the chain. **The certificate-validity window is therefore evaluated at the payload's own claimed signing date, so leaf expiry is not enforced against wall-clock time.** An attacker holding an Apple-issued signing leaf's private key could keep using it after that leaf expired, by backdating that date. Measured with a deliberately expired leaf: an expired leaf with a backdated `signedDate` verifies, and the same expired leaf with a current `signedDate` fails. **The decision stands, and the reason it stands is the size of the prerequisite:** the attacker must first hold a leaf key chaining to Apple Root CA G3, which no ordinary attacker has, and the prize is one wrongly applied sub-$5 subscription. `enable_strict_checks` is left at its default `True`. **Why this is in neither count:** the two numbers in this file's header measure divergences from the binding specification, and neither `08-webhook-app-store.md` nor `SHARED-INVARIANTS.md` mandates an online revocation check — `:48` mandates only that the chain be walked with Apple's official library, which it is. A reader auditing divergences must therefore read the counts **and** this entry.
  >
  > **The two library environments that skip signature verification are refused at configuration load, and the reason is recorded here because it is written down nowhere else.** `Environment.XCODE` and `Environment.LOCAL_TESTING` make Apple's library return the decoded payload **without verifying the signature at all** — the library says so in its own comment. D-11 types `app_store.environment` as a two-member `StoreEnvironment` with no default, and that restriction is **load-bearing security, not tidiness**: a free-text field with a case transformation would accept `"xcode"` and turn a credential-free public route into an open endpoint. `Xcode` and `LocalTesting` are each refused at load with a `ValidationError`, asserted with string literals rather than the library's enum, so the case catches a library rename instead of following it.
  >
  > **What the brief asks for on this route that this phase did not build, because it was already deleted from the product.** Read `08-webhook-app-store.md` without this paragraph and every item below reads as unmet work by this phase. None of it is. **Rate limiting and every vendor budget — none of it exists.** `:23` says the route adds no backend limit and `:24`, `:50` and `:51` require the gateway ones; **Phase 35 D-05 deleted the backend rate-limit engine from the product** rather than deferring it, and **Phase 35 D-08 deferred the whole Envoy contract to v2.1**, so the gateway entries have no contract to live in. That is D-06 above, counted once. **The `audit.auth_events` row and the operation metadata — the subsystem is gone.** `:7`, `:22`, `:32` and `:52` each state that this route writes no `audit.auth_events` row, which is trivially satisfied because **Phase 37.1 D-01 deleted the table, its writer and every call site**, and **Phase 38 D-03 struck § "Audit" from `SHARED-INVARIANTS.md` outright**, taking the route→operation-metadata obligation with it. `:22` also says the route adds no operation enum value, and `core.auth_operation` has carried only its four challenge-bearing labels since Phase 40 D-11. What survives is `audit.subscription_events`, which this route does write, one row per accepted observation. **The route registry with its `Category`, `RouteMetadata`, `VERIFIERS` and `NamedVerifier`** — required by `:7`, `:15`, `:32`, `:48` and `:49` — went with **Phase 37.1 D-06 and D-10**. Route categorisation is carried by the dedicated router and by `tests/unit/test_app_wiring.py` alone, which is D-01 and is recorded under APPLEHOOK-02; the conflict that creates with § "The barrier" is the one already flagged under FOUND-01, not a new one. **The foundation store-verification interface** at `:17` and `:26` went with **Phase 37.2 D-09**, which flagged it forward to this phase. This phase **replaced it by declaring its seam beside its first implementation** — `StoreNotificationVerifier` is a Protocol declared beside `AppStoreNotifications` in `auth/app_store.py`, with `VerifiedNotification` as the frozen value type — which is exactly what FOUND-08's forward flag directs, and is the same treatment Phase 41 gave `DeviceCheckAdapter`. The seam lives in its own module rather than in `auth/adapters.py`, whose import fence excludes the store library.
  >
  > **Two operational facts and one product consequence, recorded because none of them is a divergence and all three will otherwise be rediscovered.** **(1) Apple retries on 4xx as well as 5xx** — five times, at 1, 12, 24, 48 and 72 hours after the previous attempt, and then it stops. So a misconfigured `bundle_id`, `app_apple_id` or `environment` turns **every genuine notification into a 401** and loses about six days of subscription state silently, because a forged payload from a third party is never retried and looks identical on the wire. **The `stage` field on the `notification_rejected` warning is what tells the two apart:** two of the eight verification statuses — `INVALID_APP_IDENTIFIER` and `INVALID_ENVIRONMENT` — mean *this deployment is misconfigured*, and the rest mean *this payload is not Apple's*. An operator has about six days to read that field. **(2) The refusal arms are seven, not four.** `REFUSAL_STAGES` is derived from the library's whole `VerificationStatus` set less `OK`, with a control asserting the derivation, so a status the library adds arrives as a new parameter instead of a silent gap. **(3) D-18's product consequence: a lapsed subscriber holds no grant at all.** A buyer's active free grant is expired — never deleted — before the paid grant is inserted, and the free slot is lifetime-once, because `ix_access_grants_one_free_grant_per_user_source` carries no status predicate. When the subscription later leaves the entitled set, its grant is marked `expired` or `revoked` in the same transaction and nothing replaces it. Ingestion never reactivates a grant; **restore (Phase 45) is the only path back**. This is a consequence of rules the brief states at `:40` and `:42`, not a divergence from them. **A second consequence D-19 implies but does not state:** when the subscription is entitled, **every** grant the buyer holds is superseded, not only this subscription's — `ix_access_grants_one_active_per_user` allows exactly one active grant per user — so a `manual` comp grant ends when a store purchase lands. Refusing instead would answer 500 on every Apple retry for ever.
  >
  > **FLAGGED, NOT COUNTED (Phase 43, 2026-09-04). This route is reachable by anyone, with no credential and no limiter, and each request costs a certificate-path build plus up to three ES256 verifications.** With the backend rate-limit engine deleted from the product (Phase 35 D-05) and the Envoy gateway contract deferred to v2.1 (Phase 35 D-08), nothing bounds the volume at either layer. **Accepted and flagged, on the Phase 35 D-05 and D-08 precedent.** **This residual is wider than the three this project already records** — the unbounded Firebase read under UPGRADE-01 (Phase 40 D-22), and the unbounded Apple DeviceCheck reads under ANONGRANT-01 (Phase 41 D-20) and REGGRANT-01 (Phase 42 D-16). **Each of those three needs a valid Firebase ID token for an existing account first, so each is one subject looping on itself. This one needs nothing at all.** It is stated that way here deliberately, rather than filed as one more of the same, because reading it as a fourth instance of a known pattern would understate it. **Mitigating, and this is why it is a record and not a blocker:** the work is local and bounded — no network call, and verification runs and fails **before** `get_db`, so no database session is opened; nothing is written; and the answer is a constant-size body. It is a CPU-burn vector, not an amplification vector and not a data vector. **Closes with:** the v2.1 gateway contract, which D-06 above already flags as a deferral for this exact path — one limit closes both entries. **Not counted as a new conflict**, because it is a consequence of the pre-existing Phase 35 D-05 `limits` override recorded at `:50` and under FOUND-06, which is the same reason Phase 40, Phase 41 and Phase 42 left theirs uncounted.
  >
  > **The two counts, re-derived rather than inherited (2026-09-04).** Four sections of `SHARED-INVARIANTS.md` were re-read against what this phase shipped before any number was stated. **The result: all three new conflicts are against the brief, and not one of the four invariant sections produced a new divergence.** A null result is only useful if it says what it examined, so: **§ "Global deletions" — none.** The route is registered at an exact path, never by wildcard and never by prefix, and is not on the public allowlist; no `promo` source, no healer job, no `checkRevoked`, no backend token or session minting, and no claim-header identity. The device-check clause is vacuous here, there being no rate-limit keys at all. **§ "Locks and transactions" — none.** The ingestion writer takes exactly two distinct lock tiers, `core.access_grants` ascending by id and then `core.user_monthly_usage`, asserted over the SQL captured at `before_cursor_execute`, with neither `core.subscriptions`, `core.store_purchases` nor `core.users` among them; the attribution token read runs before every lock and Apple is never called at all inside the request, because verification finished in the dependency; the writer re-decides inside the lock and rolls the whole mutation back on any failure. **A named null result worth stating:** `:37`'s same-attempt retry rule is satisfied across requests rather than in-process — the lost race rolls back and answers 5xx, and Apple's own resend under the same `notification_uuid` finds the event row and answers 200, so the attempt identity is durable and store-supplied. `claim_attempt_id` itself died with Phase 37.4 D-03 and is not rebuilt here. **§ "Grants and evaluation time" — none.** Exactly one active grant per user is held by superseding every grant the buyer holds before the paid one is inserted, plus `ix_access_grants_one_active_per_user`; the shared effective predicate is used through `GrantsDB`'s own statements, and the additional status-only lock read is a lock set rather than an effective-grant selection — the same shape Phase 42's registered writer already takes, and it exists because a renewal's superseded grant is time-ended and outside the effective set; the source set is unchanged and `AccessGrantSource.subscription` has exactly one construction site in `src/`, proven by an AST walk that was mutation-tested; nothing is allocated implicitly and no usage row is lazily minted for an existing grant; one captured instant per request is structural through `get_evaluated_at`, and `datetime.now` is named in neither module. **§ "Fail-closed defaults" — none.** Every verification failure answers a byte-identical 401 with nothing written; divergent stored attribution is refused through `AttributionConflict` and never converged by retry; an unmapped product id and a lost race each answer 500 with nothing written; each refusal leaves exactly one structured line named for its own class; and an admission-phase refusal writes no database row, because it happens before `get_db`. **The result: three new flagged conflicts, taking the header's count from sixteen to nineteen, and one new uncounted divergence — the unbounded unauthenticated request cost — taking the set of known divergences from twenty-three to twenty-seven.** The gap between the two numbers is therefore **eight**, and every item in it is listed in the header. **D-09 is in neither number**, for the reason its own entry above gives: it diverges from Apple's guidance and from no text in the binding specification. The gap is the difference between a counted conflict and a recorded override, forward flag or precedence resolution, and it has been kept deliberately since Phase 37.5.

- [x] **APPLEHOOK-02** — **ANSWERED AND CLOSED by Phase 43 (D-01), 2026-09-04. The mechanism is a dedicated router; the membership is the set of routes on it.** As written: the route is enumerated individually by exact path in the closed provider-callback category — never by wildcard or prefix, never on the public allowlist
  > **Flagged forward to Phase 43 by Phase 37.1 (D-06/D-10/D-12), 2026-08-24. The loss is recorded; the replacement is explicitly NOT designed here.** `Category`, `RouteMetadata`, `VERIFIERS` and `NamedVerifier` were deleted with the route registry, before this phase exists. Nothing regressed on the way out — `VERIFIERS` had no members — but **the control this requirement names is real**: individual exact-path enumeration is what stops a wildcard or prefix accidentally admitting an unauthenticated route, and `SHARED-INVARIANTS.md` § "Global deletions" still forbids wildcard or path-prefix provider-callback membership. The prohibition binds; only the machinery that enforced it is gone.
  >
  > **A pointer, not a design:** under D-08 the natural shape is a dedicated `APIRouter` carrying a named-verifier dependency, whose membership is then simply the set of routes registered on it. Phase 43 evaluates that on its own terms and is free to reject it. This phase adds **no** requirement describing a replacement mechanism — designing the provider-callback seam from outside the phase that owns it is how the deleted registry got built in the first place. APPLEHOOK-01's "outside the barrier" was reworded to "outside the auth dependency" in the same pass; same mechanism change, no substantive difference.
  >
  > **Partly settled by Phase 38 (D-03/D-04), 2026-09-01 — settled exactly as far as the removal reaches, and no further. This requirement is NOT closed.**
  >
  > **What is settled.** Plan 38-04 deleted § "Audit" in full from `SHARED-INVARIANTS.md`, which took two obligations with it: the route→operation-metadata obligation the deleted route registry carried — *"readable BEFORE the barrier runs, so barrier rejections are audited with the operation set"* — and any `audit.auth_events` row obligation for this route. Phase 43 owes no audit row and inherits no audit decision.
  >
  > **What is NOT settled and still binds.** `SHARED-INVARIANTS.md` § "Global deletions" still forbids wildcard or path-prefix membership for the provider-callback category, and **Phase 38 removed no clause from § "Global deletions"**. This route is still enumerated individually by exact path, and is still never on the public allowlist. What Phase 43 still owes is the mechanism that makes that enumeration real once the registry is gone — the prohibition binds; only the machinery that enforced it is missing.
  >
  > **ANSWERED by Phase 43 (D-01), 2026-09-04 — this is the text Phase 44 reads instead of inventing a second partition.** The provider-callback partition is a dedicated `APIRouter` in `src/nativespeaker/api/routers/webhooks.py`. Its router-level dependency is `verify_app_store_notification`. **Membership in the partition is the set of routes registered on that router**, so there is no second table that can disagree with the first — which is what the deleted registry died of. **The partition is countable in one place:** `tests/unit/test_app_wiring.py` holds `PROVIDER_CALLBACK_PATHS = {"/webhooks/app-store"}` as a literal and compares it with `==` against the routes on the webhooks router. Widening the partition is therefore a visible one-line edit, and Phase 44 adds one route and one literal member. Four cases assert the mechanism: the literal equals the router's route set; each of those routes declares `verify_app_store_notification` by callable identity and declares neither `get_identity` nor `get_linked_identity`; no route outside the literal declares the verifier; and the three path literals are disjoint.
  >
  > **`SHARED-INVARIANTS.md` § "Global deletions" still binds and is satisfied.** The route is registered at the exact path `/webhooks/app-store` — `router.post("/webhooks/app-store")`, never a `/webhooks/*` wildcard and never a path prefix — and it is not on the public allowlist. Phase 43 removed no clause from that section and needed none removed.
  >
  > **What the mechanism cost, measured before it was built (43-RESEARCH P-05) and recorded because the requirement's own wording moved.** Two cases in `tests/unit/test_app_wiring.py::TestEveryRouteIsAuthenticated` compute their sets **structurally** over `app.routes`, so a route declaring neither identity accessor joins them by itself. Both broke the moment this route was registered — `test_every_route_but_the_two_exemptions_requires_a_linked_identity` and `test_the_public_allowlist_is_exactly_the_readiness_probe` — and both now take `PROVIDER_CALLBACK_PATHS` into their exemption union. **The consequence, stated plainly:** the structural case now asserts "declares no identity accessor", and the callback route satisfies that too, so that case alone no longer holds the public allowlist to one member. A **separate case now asserts `PUBLIC_PATHS == {"/health/ready"}` as a literal**, and that literal case is what keeps the true public allowlist to one member. D-01's phrasing — "the public allowlist is still exactly `/health/ready`" — stays true as intent and is false as the structural assertion, which is why the literal case exists.
  >
  > **PLAYHOOK-03 is the inheritor.** Its "second and last member" clause needs the partition to be countable in one place, and `PROVIDER_CALLBACK_PATHS` is that place. Phase 44 adds `"/webhooks/google-play/rtdn"` to the literal and its route to the same router; it defines no second partition mechanism.

### PLAYHOOK — Phase 44 (`09-webhook-google-play-rtdn.md`) — `POST /webhooks/google-play/rtdn`

- [ ] **PLAYHOOK-01**: The endpoint ingests Google Play RTDN via a Cloud Pub/Sub push subscription, authenticated solely by backend verification of Google's signed OIDC push token
- [ ] **PLAYHOOK-02**: The endpoint reuses the shared store-ingestion module owned by Phase 43 rather than forking it
  > **Adapter seam flagged forward by Phase 37.2 (D-09), 2026-08-25.** The store-ingestion Protocols Phase 35 declared under FOUND-08 were deleted as zero-consumer code before this phase exists. Phase 44 inherits **Phase 43's** adapter interface along with the shared module this requirement already binds it to, and defines no second store seam of its own — the same argument PLAYHOOK-03 makes about competing partition mechanisms applies to competing adapter seams.

- [ ] **PLAYHOOK-03** — **The category machinery this names was deleted by Phase 37.1. Phase 44 must answer it.** As written: the route is enumerated individually by exact path in the closed provider-callback category, as the second and last member of that partition
  > **Flagged forward to Phase 44 by Phase 37.1 (D-06/D-12), 2026-08-24 — not redesigned here.** The same loss as APPLEHOOK-02: the closed-category machinery is gone before this phase exists. Phase 44 should inherit whatever Phase 43 defines rather than inventing a second answer — PLAYHOOK-02 already binds it to Phase 43's shared module, and two competing partition mechanisms would recreate exactly the drift the registry died of. The **"second and last member"** clause is the part that needs care: it requires the partition to be *countable*, so whatever Phase 43 builds must make its membership enumerable in one place. This phase adds no replacement mechanism.
  >
  > **Partly settled by Phase 38 (D-03/D-04), 2026-09-01 — settled exactly as far as the removal reaches, and no further. This requirement is NOT closed.**
  >
  > **What is settled.** Plan 38-04 deleted § "Audit" in full from `SHARED-INVARIANTS.md`, taking with it the route→operation-metadata obligation the deleted route registry carried and any `audit.auth_events` row obligation for this route. Phase 44 owes no audit row and inherits no audit decision.
  >
  > **What is NOT settled and still binds.** `SHARED-INVARIANTS.md` § "Global deletions" still forbids wildcard or path-prefix membership for the provider-callback category, and **Phase 38 removed no clause from § "Global deletions"**. This route is still enumerated individually by exact path, is still never on the public allowlist, and the **"second and last member"** clause still requires the partition to be countable in one place. Phase 44 still owes all of that, inherited from whatever Phase 43 builds.

### RESTORE — Phase 45 (`10-restore-subscription.md`) — `POST /auth/restore-subscription`

- [ ] **RESTORE-01**: The endpoint verifies a native store artifact (`restore_proof`) directly against Apple or Google and attaches verified paid-subscription entitlement through one of two server-determined branches
  > **Adapter seam flagged forward by Phase 37.2 (D-09), 2026-08-25.** `VendorProofAdapter`, `ClaimKind` and `DeviceBitState` — the native-artifact verification Protocols Phase 35 declared under FOUND-08 — were deleted as zero-consumer code before this phase exists. Nothing regressed: they had no implementations and no callers. Phase 45 defines its verification interface **beside its first implementation**.
  >
  > **The `restore_subscription` operation label no longer exists — flagged forward by Phase 40 (D-11), 2026-09-02.** `core.auth_operation` was shrunk to its four challenge-bearing labels, so `10-restore-subscription.md:25`'s *"Operation enum value: `restore_subscription` (`core.auth_operation`)"* and `01-foundation.md:137`'s mapping name a value the type does not carry. **Nothing was lost that this phase could have used:** the label's only consumers were `audit.auth_events` (deleted by Phase 37.1 D-01) and the route registry's route→operation metadata (deleted by Phase 37.1 D-06), and restore is explicitly **not** challenge-bearing by its own brief, so it never belonged in the surviving consumer, `core.auth_challenges.operation`. Phase 45 decides for itself whether it needs an operation name at all; if it does, re-adding a label means editing the single migration in place under SCHEMA-01, where the full reasoning is recorded.

- [ ] **RESTORE-02**: The endpoint is a native-only surface; other surfaces receive `operation_not_allowed`

### SIGNOUT — Phase 46 (`11-sign-out-all.md`) — `POST /auth/sign-out-all`

- [ ] **SIGNOUT-01**: The endpoint revokes the verified subject's Firebase refresh tokens through the issuer-selected Firebase Admin client, returning success only on Firebase-confirmed revocation
  > **Adapter seam flagged forward by Phase 37.2 (D-09), 2026-08-25.** `FirebaseAdminAdapter`'s `revoke_refresh_tokens` method and the `RevocationOutcome` type were deleted as zero-consumer code before this phase exists; the surviving one-method `FirebaseAdminAdapter` carries `get_user_provider_data` only. Phase 46 adds the revocation call **beside its first implementation**. **The rule this requirement states is untouched** — `build_admin_apps` still builds one named Admin app per configured issuer, never a `[DEFAULT]` one, and revocation still goes through the client the request-verified issuer selects. What did change is where that client's credential comes from: see the flagged credential conflict under CREATE-01.
  >
  > **The `sign_out_all` operation label no longer exists — flagged forward by Phase 40 (D-11), 2026-09-02.** `core.auth_operation` was shrunk to its four challenge-bearing labels, so `11-sign-out-all.md:25`'s *"Operation enum value: `core.auth_operation` value `'sign_out_all'`"* and `01-foundation.md:137`'s mapping name a value the type does not carry. The same reasoning as RESTORE-01's matching note: the label's only consumers were `audit.auth_events` and the route registry, both deleted by Phase 37.1, and sign-out-all is **not** challenge-bearing by its own brief, so it never belonged in the surviving consumer. `11-sign-out-all.md:25` and `:37` additionally require that *"inventory membership is read from the inventory table, never inferred from the path name"* — that inventory table is the route registry Phase 37.1 D-06 deleted, which is the conflict already flagged under FOUND-01. Phase 46 decides for itself; re-adding a label means editing the single migration in place under SCHEMA-01.

- [ ] **SIGNOUT-02** — **Audit half settled by Phase 38 (2026-09-01): no durable row is owed and Phase 46 inherits no audit decision. Fail-closed half untouched and still binding.** As written: the endpoint is an audited state-changing operation writing exactly one `audit.auth_events` row; an indeterminate or failed revocation fails closed rather than reporting success
  > **Flagged forward to Phase 46 by Phase 37.1 (D-01/D-12), 2026-08-24 — recorded, NOT resolved here.** Only the audit half lost its mechanism. **The fail-closed half is unamended and fully binding**: an indeterminate or failed revocation must never report success, SIGNOUT-01 states the same rule independently, and `SHARED-INVARIANTS.md` § "Fail-closed defaults" binds it a third time.
  >
  > **Phase 46 owns the audit decision and must make it explicitly** — the same choice Phase 38 faces under SYNC-03 — but it must weigh it on this operation's own terms, because they are not the same case. This is a **state-changing** operation that fails closed on an indeterminate outcome: drop the durable-row obligation outright and a revocation that neither confirmed nor demonstrably failed leaves *nothing* behind recording that it was attempted, on the one endpoint whose whole purpose is revoking access. A read-only sync losing its attempt telemetry is a smaller loss than that. Rebuilding a record here does not require rebuilding the whole subsystem D-01 removed. See the flagged `SHARED-INVARIANTS.md` conflict under FOUND-05.
  >
  > **Amended by Phase 38 (D-03/D-04), 2026-09-01 — the audit half is settled by removal, and the paragraph directly above no longer stands as an open instruction to Phase 46.** Audit removal is a milestone-level decision, not a per-endpoint one: plan 38-04 deleted § "Audit" in full from `SHARED-INVARIANTS.md`, so **no durable `audit.auth_events` row is owed by this endpoint, the obligation is gone from the binding specification, and Phase 46 inherits no audit decision to make.** The record a revocation leaves is the one every other route leaves — the `request` line `RequestLoggingMiddleware` writes per attempt, plus one WARNING per rejection from the shared handler. **The accepted consequence, named rather than glossed:** the paragraph above was right that a revocation which neither confirmed nor demonstrably failed now leaves no durable record of the attempt, only those log lines. The developer accepted that when removing the invariants milestone-wide; Phase 46 may still choose to log more on its own terms, but it owes no row and re-deciding the audit question is not its work.
  >
  > **SIGNOUT-01 and SIGNOUT-02's fail-closed half are untouched by Phase 38 and stay fully binding: an indeterminate or failed revocation must never report success.** Neither requirement was reworded, SIGNOUT-01 states the same rule independently, and `SHARED-INVARIANTS.md` § "Fail-closed defaults" binds it a third time — that section is unremoved, and is in fact where plan 38-04 relocated the two live non-audit rules it kept, so it was read and extended rather than skipped.

## Phase 37.5 records (2026-08-31)

The reasoning, not the rules. The rules D-01, D-08 and D-11 state now live in `AGENTS.md`, written there by plan `37.5-01` and applied at write time by every plan after it. Nothing below restates one. What is here is the *grounds* — the part that stops a settled question being re-opened a fifth time, which is the failure this phase exists to end. Three sweeps preceded it (37.2 D-10, 37.3, 37.4 D-12) and each left the next one no record of what it had already examined.

### D-08 — the exhaustive single-caller classification

**Method, so the count can be re-derived rather than trusted:** an AST pass over all 40 files in `src/`, collecting every `FunctionDef`/`AsyncFunctionDef` at any nesting depth, then counting every `ast.Name`/`ast.Attribute`/`ast.alias` reference to each name across `src/` only. It counts *code* references and ignores docstrings and comments — a plain `grep` over-counts by four on `_should_retry` alone.

**Result: 21 module-private definitions, every one with exactly one call site.** Not a single dead private helper and not a single multi-caller one, which is itself worth recording. **Three are deletions and eighteen are keeps — 3 + 18 = 21.**

| # | Function | Verdict | Ground |
|---|---|---|---|
| 1 | `routers/auth.py::_complete` | **DELETE** | Step. The caller forwarded every argument and added one timestamp. Dissolved into D-03's move to `AuthService`. |
| 2 | `auth/create_user.py::_insert_account` | **DELETE** | Step. All that remained of `create_user` after one check. |
| 3 | `quota.py::consume_quota` + `charge_quota` | **DELETE (merge)** | The same subject split in two — one opened a session and called the other. Merged under D-06 into `QuotaService`'s one charge. Public, but single-caller and the same defect. |
| 4 | `resilience.py::_sleep_if_positive` | **KEEP** ⚠️ | **Corrects D-08's own deletion list.** See below. |
| 5 | `auth/create_user.py::_reject_existing_identity` | KEEP | Rule: which rejection an existing row earned. Moved with D-03; it did not dissolve. |
| 6 | `auth/firebase.py::_resolve_provider` | KEEP | Rule: the provider classification, four rejection branches. |
| 7 | `auth/firebase.py::_verified_email` | KEEP | Rule: copy the address only when non-empty and verified — the rule 37.3 put behind the seam so no consumer re-derives it. |
| 8 | `auth/firebase.py::_exhausted` | KEEP | Boundary: tenacity's `retry_error_callback` must be a separate callable. |
| 9 | `auth/jwt_verifier.py::_is_known_unknown` | KEEP | Boundary: takes `self._cache_lock`; the name marks where the critical section starts. |
| 10 | `auth/jwt_verifier.py::_record_unknown` | KEEP | Boundary: takes `self._cache_lock`. |
| 11 | `errors.py::_undeclared` | **DELETE from `src/`** | D-09, not D-08 — **rehosted** to `tests/unit/error_tree.py`, not dropped. Four test cases call it directly. |
| 12 | `errors.py::_tree_problems` | **DELETE from `src/`** | D-09; same rehost. |
| 13 | `errors.py::_family` | KEEP | Live dispatch, and it had **two** call sites — not single-caller at all when classified. It has one now, after the rehost, and **it is still not a step: it is recursive, so it cannot be inlined and attempting it produces a wrong walk.** That is now a line in `AGENTS.md`. |
| 14 | `auth/firebase.py::_application_default_credential` | KEEP | **Never classified by the context file.** Rule — its name *is* the contract: ADC if the environment supplies it, `None` if it does not, never a raise. Inlining puts a bare `try/except DefaultCredentialsError` in `build_admin_apps` and the never-raises property stops being stated. Also imported by `tests/e2e/conftest.py`. |
| 15 | `auth/firebase.py::FirebaseAdminLookup._read` | KEEP | **Never classified.** Boundary — the callable `starlette.concurrency.run_in_threadpool` requires, and the try/except boundary: everything that can raise happens there. |
| 16 | `auth/jwt_verifier.py::JWTVerifier._cache_key_for` | KEEP | **Never classified.** Rule — an absent or non-string `kid` shares one sentinel, the shared-sentinel behaviour stated once. |
| 17 | `resilience.py::_is_transient_error` | KEEP | **Never classified.** Rule — the OpenAI transient classifier, 13 lines over seven status codes. D-11 names it as one of the three surviving hand-rolled pieces and keeps it. |
| 18 | `resilience.py::_should_retry` | KEEP | **Never classified.** Boundary — tenacity's `retry_if_exception(...)` predicate. Same ground as `_exhausted`. |
| 19 | `resilience.py::LLMExecutionGate::_inflight_slot` | KEEP | **Never classified.** Boundary — an acquire/release critical section, and the **sole raise site of the queue-full rejection**. A-01 renamed the pair to `hold()`; it was not dissolved. |
| 20 | `resilience.py::attempt` (closure) | KEEP | Boundary — tenacity's retried callable. |
| 21 | `resilience.py::timed_op` (closure) | DELETE (incidental) | Step: two lines wrapping `wait_for`. A-01's seam dissolved it anyway when `gate.run` left `attempt()`. |

**`_sleep_if_positive` is a KEEP, and this corrects D-08's own deletion list (A-04, developer ruling).** D-08 listed it for deletion as *"three lines and an `if`"*, which contradicts D-08's own carve-out: it is **the callable tenacity's `sleep=` requires** — a boundary by the rule's own words — and it carries real behaviour, that a zero-length backoff issues no sleep at all, which `tests/unit/test_resilience_retry.py::TestBackoffSchedule::test_zero_backoff_records_no_sleep_at_all` pins. **D-08's deletions are three, not four.** The case survives and gained the `Admitted` token like every other call site; it was not weakened.

**The six the phase's own text never classified (#14–#19) are all keeps, and reporting them matters more than the verdicts (A-05).** Every one is a rule or a boundary and none was deletable. **Leaving no record of what was examined is precisely what 37.2 D-10's sweep failed to do, which is why D-08 was a second pass** — and a planner reading D-08 as exhaustive would either have left these six silently unexamined, guaranteeing a third sweep, or deleted them by extending the rule.

**Why the first sweep missed nine of the twenty-one.** Plan `37.2-06` landed as two refactors and one test: `f4cc09b` merged the classifier and the retry policy into the Firebase module, `d904050` inlined the rejection log into the function that raises it. **Both are cohesion merges — moving code between *modules* — not a general single-caller inline.** Its actual subject was module boundaries. Four of the six unreached functions live in `resilience.py` and `firebase.py`, which that sweep touched for a different reason; two were never in scope at all. **The implication, and the reason D-08's durable form is a rule in `AGENTS.md` rather than a list: a rule an agent applies at write time is what stops a fourth pass. A fifth sweep re-deriving the same eighteen keeps is the failure mode.**

**Public single-caller functions D-08 does not reach**, listed so a later sweep does not widen by accident: `get_config`, `get_session_factory`, `camel_to_snake`, `validation_error_handler` / `http_exception_handler` / `generic_error_handler` (all registered framework handlers, boundaries), `claims_from_payload`, `new_challenge_id`, `LLMService.create_chain`, `readiness`, and every `ChallengesDB` / `ChatsDB` / `GrantsDB` method — each called once from its one service, **which is the layering working, not a smell**.

### D-11 — the circuit breaker stays, with the corrected reasoning

`CircuitBreaker`, `LLMExecutionGate` and the OpenAI transient classifier stay hand-rolled. `resilience.py` was not a refactor target this phase beyond D-08 and D-12.

**The surviving arguments, which are the whole of the case:**

1. **The retry loop was already replaced by a library that is in the tree** — commit `2afa7fd`, *"run ainvoke's retry on tenacity, retiring the last hand-rolled loop"*. That is the library, and it is already in.
2. **That library bounds one request and nothing across requests.** At the configured values (`attempts: 3`, `timeout: 30`, `pool_size: 5`, `queue_size: 25`, `threshold: 5`, `reset: 60`), tenacity bounds a single request at roughly **91.5s**. It bounds nothing across requests, so **every** request during a provider outage pays that from scratch. The breaker is the cross-request control, and `CircuitOpenError` is re-raised unclassified so tenacity does not retry it. The two bound different things.
3. **Nothing installed replaces a circuit breaker.** All 110 distributions in `.venv` were enumerated: the only resilience-relevant ones are `tenacity` (in use) and `anyio` (transitive; it has `CapacityLimiter` and no circuit breaker). **Finishing the idea means adding a new dependency, not adopting a present one.**
4. **Without the breaker the client is told to retry in two seconds when the provider is down.** With 30 slots each held ~91.5s instead of released instantly, the gate saturates and the answer a caller gets is `Retry-After: 2` — wrong advice, on the one occasion the advice matters.

**The status-flip argument is WITHDRAWN as factually wrong (A-02/P-01).** D-07 and D-11 both said that removing the breaker would flip the client-visible answer from a 503 to a **429**. It would not. `QueueFullError` subclasses `ServiceUnavailable` and **declares no status of its own**, so the queue-full rejection has **always** been a 503. Both admission rejections have always been service-unavailable answers. What remains of that argument is argument 4 above — the `Retry-After`, not the status. **This withdrawal is stated rather than quietly dropped for one reason: a record carrying a wrong fact loses its authority on the first re-examination, and not being re-examined is the only job this record has.**

**The developer's opening expectation was correct as a reading of the specification, and that is why the question keeps coming back.** The discussion opened on the belief that a hand-rolled rate limiter in `resilience.py` was still awaiting replacement by a library already in the tree. `SHARED-INVARIANTS.md` § Rate limits does mandate the `limits` library — so the specification really does call for a library this project does not have, and never had: `git log -S'limits' -- pyproject.toml` is empty, because Phase 35 D-05 deleted the whole backend rate-limit engine before `limits` was ever added. **The expectation was not a misremembering; it was the spec.** What was hand-rolled turned out not to be rate limiting at all. See the `limits` bookkeeping item in the Phase 37.5 amendment above and the Phase 35 override at the FOUND scope note.

### What this phase surfaced and deliberately did not act on

Each item carries enough detail to decide from without re-deriving it. None is fixed here.

#### The database pool (A-15) — recorded, NOT fixed. The developer owns the choice.

`db.pool_size` defaults to **5** (`config.py:25`) and the engine is built with **`max_overflow=0`** (`app/lifespan.py:34`). A `POST /chats` already holds **two** connections, so **three concurrent chat posts exhaust the pool today** — before this phase, and independently of anything it did.

**What the admission seam changed is where the stall sits, not whether it exists.** The charge's database round trip now runs while an LLM concurrency permit is held (A-01), so a pool wait now also holds a provider permit. That is delta 3 below.

**The one-line change, named so it can be made without redoing the arithmetic:** raise `db.pool_size` to at least `resilience.pool_size × 2 + 2` — **12** at the configured `resilience.pool_size: 5` — in `config/config.yaml`, or leave it and accept the ceiling. This is a capacity decision about a pre-launch product with no users, not a defect, which is why it is recorded rather than changed.

#### The three costed behaviour deltas the admission seam introduced (plan `37.5-05`)

Real changes, all acceptable at the configured values, recorded so a later reader does not rediscover them as bugs.

| # | Delta | Cost at the configured values |
|---|---|---|
| 1 | The gate slot is held across the whole retry sequence rather than released between attempts | Worst case 3 × 30s timeout + 0.5s + 1.0s backoff ≈ **91.5s**, of which **90s** was already slot-held. The extra occupancy is the **1.5s** of backoff — **1.6%**, over 30 total slots. Not material. |
| 2 | The breaker is consulted once per request rather than once per attempt | A request whose own first attempt trips the breaker over threshold no longer sees `CircuitOpenError` on its retry; it spends its remaining attempts. **Cross-request behaviour — the thing the breaker exists for — is unchanged.** |
| 3 | The charge's database round trip now runs while an LLM permit is held | Does not create the pool hazard above; moves it inside the permit. See A-15. |

#### `ChallengeRequired` — zero name references, and load-bearing

Recorded here as well as under FOUND-04 because it is the discrepancy that **already produced one wrong deletion offer** during this phase's discussion, and the offer would be made again by anyone repeating the same method.

It had **zero references by name anywhere outside its own definition**. A reference-grep therefore reads it as dead. It is not: it is the only class declaring `answers_framework_status = True` at 409, so `class_answering_status(409)` resolves to it, and **deleting it turns a bare framework 409 into `error_registry_unmapped_status` and a 500**. Plan `37.5-04` added `test_the_409_answering_class_is_challenge_required_by_name` for exactly this reason — the class now has two references, the import and the assertion, so the same grep no longer reads it as dead. Note also that **one of D-10's two stated grounds was wrong** and is withdrawn (A-12): `ChallengeRejected` carries `challenge_required` too, so no mandated code would have been dropped. The dispatch ground alone carries the keep.

#### The consolidated deleted-test-case list

Gathered from the nine plan summaries so the developer reviews one list rather than six. Each plan's own summary names every case individually; this is the count and the ledger.

| Plan | Collected cases deleted | Subject |
|---|---|---|
| `37.5-01` | 0 | Rules into `AGENTS.md`; the docstring gate was added, not cut |
| `37.5-02` | 0 | The auth vertical — all 796 cases survive; three harness changes, no assertion weakened |
| `37.5-03` | 0 | `QuotaService` — re-pointed, nothing deleted |
| `37.5-04` | **7** (and **+1** added) | Six parametrized rows over the four deleted exception classes, plus one `CASES` row; added `test_the_409_answering_class_is_challenge_required_by_name` |
| `37.5-05` | **1** (and **+5** added) | `test_an_open_circuit_answers_503_after_the_charge_has_committed` — it asserted the defect; its inversion replaces it |
| `37.5-06` | **16** | `test_adapter_interfaces.py` 29 → 13: the three prose-asserting cases, nine source-text cases, four import-side-effect cases |
| `37.5-07` | **28** | The error, challenge and JWT clusters |
| `37.5-08` | **30** | The identity, create-user, quota and firebase clusters (19 case definitions) |
| `37.5-09` | 0 | `tests/e2e/` and `tests/schema/` — prose only, no case touched |
| **Total** | **82 deleted, 2 restored, 6 added** | **Net −74** |

**Two of the 82 were restored by the developer between waves 6 and 7, in commit `658895e`**, because plan `37.5-07`'s survivor argument did not hold for them: `test_rejects_token_signed_with_different_key` (a token signed with an attacker's own *valid* RSA key — the surviving `test_rejects_tampered_payload` covers a *broken* signature, a different path) and `test_the_module_logs_nothing` (the challenge store held no logger to leak a secret capability handle with). Both are present and passing.

**The count reconciles exactly against the measured suite.** 1090 with markers cleared after plan `37.5-01` → 1084 (plan 04) → 1088 (plan 05) → 1072 (06) → 1044 (07) → **1046 after `658895e` restored two** → 1016 (08) → 1016 (09). **The measured figure today is 1016 passing with markers cleared, 0 deselected.** The +2 step is the restoration and is the reason plan 08's summary opens from 1046 where plan 07's closed at 1044.

**Lines removed from `tests/`: 13,271 → 12,961, a net −310** across the phase, against D-12's starting figure (as corrected by A-08/P-09 — the tree was 59 files and 13,271 lines, not 39 files and 916 in `tests/schema/`). The net is **after** adding `tests/unit/error_tree.py` (73 lines, the rehosted checker) and `tests/unit/test_docstring_bar.py` (the gate), so the gross removal from pre-existing files is larger. `src/` went **2,886 → 2,758**, −128, with three files deleted (`auth/identity.py`, `auth/create_user.py`, `quota.py`) and four added (`schemas/auth.py`, `crud/identities.py`, `services/auth.py`, `services/quota.py`).

**One deletion is a real coverage loss and was NOT restored.** `test_foundation_calls_no_adapter_method_anywhere_in_src` (plan `37.5-06`, with its allow-list and its two controls, cases 10–12) was **the only enforcement in the repository** that `get_user_provider_data` is named nowhere in `src/` except `auth/adapters.py` and `auth/firebase.py`. It is a source-text scan and so a target by D-13's stated criterion, but unlike its eight siblings it carried an **architectural** guarantee rather than a language one. `grep -rl get_user_provider_data src/` returns exactly those two files today, so the property holds — but **nothing now fails if a third file starts calling the adapter**. Restoring cases 9–12 as a small class of their own costs about **25 lines**. This is open.

**Four further deletions whose survivors are arguments rather than cases**, each named by its plan and left standing: `test_the_one_401_the_responses_block_documents_is_the_answering_class` (the only cover for `class_answering_status(401).code == "auth_required"` and the `responses[401]` model — about two lines to restore inside `TestTheFrameworkStatusMap`); the challenge store's three other scans (no database lock, no raw SQL, no home-grown digest — nothing enforces any of them now); `test_accepts_token_with_extra_claims` (a future claim allow-list would ship green — three lines); and `test_a_non_bearer_scheme_answers_401_and_still_records_the_event`, whose survivor is a claim about the code path rather than a case.

#### What stays open

- **`tables/__init__.py` still re-exports the `schemas/` names.** Eleven of them — `ChatRequest`, `ChatResponse`, `ExamplesResponse`, `MessageRequest`, `MessageResponse`, `AnalyzeInput`, `AnalyzeResponse`, `FollowUpInput`, `FollowUpResponse`, `Issue`, `RejectResponse`. `37.5-PATTERNS.md` lists this as this phase's layering work; **no plan claimed it and none did it.** It is layering, not prose, so no sweep would have reached it. **Open.**
- **`tests/unit/test_app_wiring.py::TestEveryRouteIsAuthenticated` has no negative control against an injected undeclared route**, contrary to what FOUND-03's note claimed — corrected there. Reported by plan `37.5-08`, outside plan `37.5-09`'s scope, and **not fixed**. With it, **three vacuous cases stand in `tests/unit/test_quota_resolver.py`**: `TestMultipleEffectiveGrants::test_two_effective_grants_raise`, `TestMissingUsageRow::test_a_grant_with_no_usage_row_raises` and `TestUnknownTier::test_a_missing_tier_row_raises`, each driving the same call as a sibling in its own class and asserting nothing the sibling's `pytest.raises` has not already established. They were deliberately not deleted: plan `37.5-07` established that widening a deletion beyond its named targets is the deviation with the higher cost. **Both open.**
- **A runtime string still calls the database "a crud".** `tests/schema/conftest.py:56` reads `msg = f"refusing to interpolate {name!r} as a crud identifier"` inside `_check_identifier` — the same `crud`-for-`database` artefact from `d466a4b` that plan `37.5-09` fixed in 15 prose sites. It was left because it is a **string literal, not prose**: changing it would have changed the AST and broken that plan's own prose-only, no-logic-change proof. **One-line fix for a later phase:** `crud identifier` → `database identifier`. Nothing asserts on the text. **Open.**
- **`errors.py` at 397 lines** where 37.4's A-01 projected roughly 250. D-09 and D-10 took it from 495 to 399 and the D-12 sweep to 397. Whatever the sweep did not reach stays for a later look.
- **`37.4-REVIEW.md`'s warnings that fell outside moved or swept code**, re-checked against the tree at the phase's close: **WR-03 is closed by deletion, not by fix** — the false comment in `tables/identities.py` claiming the only plaintext external subject is gone with the sweep, so the falsehood no longer misleads, but the *correct* statement the review proposed was never written. **WR-04 is open and untouched:** the fourteen-line HMAC key-material block still sits in `config/config.yaml:18-31` above `chats_limit: 50`, describing keys that no longer exist and asserting *"the keys below are therefore committed"*. D-12's sweep is scoped to `.py` files, so nothing this phase did could reach a YAML comment. **WR-06 is open and moved:** the diagnostic-discarding `except Exception` is now at `services/auth.py:87`, relocated by D-03, still logging only `type(failure).__name__`. **WR-09 is open and moved:** `auth/firebase.py:86` still classifies every `ValueError` as retryable, so a permanently invalid uid burns all three attempts and answers a retryable 503.
- **The deferred list from `37.5-CONTEXT.md` carries unchanged:** the route template as a bounded telemetry label (dropped by 37.4 D-03, still gone); the provider-callback category and adapter seams for phases 43–46; FOUND-09 / the Envoy contract, still v2.1; and the unlimited auth surface — no `HTTPRoute` matches `/auth`, so no rate limit of any kind applies to it this milestone, knowingly accepted in `37.2-SECURITY.md` AR-01.
- **Two queued documentation todos are now unblocked**, both waiting on this phase landing: `/gsd:docs-update` for `README.md` (three stalenesses verified 2026-08-30) and the refresh of `.planning/codebase/*.md` (three milestones stale, predating `d466a4b` entirely). Neither was done here.

#### The A-17 line-number re-verification

The rule (inherited from 37.4 A-00): **re-verify line numbers after each wave, not facts.** Facts are what did not survive.

Every source line number cited in `37.5-CONTEXT.md` was re-verified against the tree at `0c6e54f` when the addenda were written, including all thirteen file line-counts, and **none had drifted**. It was re-checked wave by wave during execution:

- **Plan `37.5-02` (wave 2)** re-checked the citations its siblings depend on — `resilience.py:108` (`_sleep_if_positive`), `resilience.py:126-160` (`ainvoke` and its retry block), `errors.py:56-138` (`_family` through `class_answering_status`), `services/chats.py:92-94` and `:116-118` (the two charge sites), `app/lifespan.py:29-30` (the self-check call). **No drift in any of them** — none of those files was touched by that plan, which is why they held.
- **Drift was found in one place, and it is in a plan file rather than in the context file.** Every `auth/create_user.py` line number in `37.5-02-PLAN.md` is wrong: it cites `create_user` at `:72-90`, `resolve_existing_identity` at `:93-98`, `_reject_existing_identity` at `:101-110` and the insert block at `:113-148`, in a file that was **97 lines** — so `:113-148` does not exist at all. The measured positions at that plan's base were `:21-39`, `:42-47`, `:50-59` and `:62-97`. `37.5-PATTERNS.md` carried the correct numbers and is what the executor followed. **The file is now deleted, so the citations are moot going forward.**
- **Plans `37.5-03` through `37.5-09` each re-verified their own inherited citations** and reported them in their summaries; plan `37.5-03` explicitly handed forward three stale references, and plan `37.5-05` recorded all three as fixed.
- **What did not survive was never a line number.** It was the behavioural and count premises: the eleven corrections P-01…P-11, and three further count corrections found during execution — `tests/schema/` holds **90** comment lines and not the 71 the plan claimed (80 excluding `# ty: ignore` suppressions, still not 71); plan `37.5-08`'s wire-contract class held **eight** cases where the plan described seven; and plan `37.5-03` corrected the resolver case count in its own plan. Every one changed prose, not a decision.
- **A method note worth carrying:** three plans found `pytest -k` acceptance gates that collect **zero** cases, because `-k` matches node-id substrings case-sensitively and the subjects were CamelCase class names (`TestAntiOracle` against `-k anti_oracle`). Plans 07 and 08 each hit this; plan 09 audited both of its terms individually and was the first in the run for which both were non-vacuous. **A `-k` gate that collects nothing passes.** Verify by node id.

## Future Requirements

Tracked but deferred beyond v2.0.

### Gateway contract — deferred from Phase 35

- **FOUND-09**: The Envoy Gateway config forwards `Authorization` unchanged, injects no identity headers, pins `xff_num_trusted_hops`, overrides the shared 429 body, and the backend ignores every client- or proxy-supplied identity header (`01-foundation.md §9`). Deferred per Phase 35 D-08. Accepted consequences for v2.0: only the v1.6 chart's rate limiting ships, unverified against `§9`; Envoy's 429s keep their empty body, which does not satisfy the client error contract; and `xff_num_trusted_hops` stays unpinned. No backend correctness depends on this — `§9` is explicit that the backend is the sole authoritative verifier.
  > **Third accepted consequence amended by Phase 37.1 (D-01), 2026-08-24.** It used to end *"so the client address recorded in audit `details` is trusted rather than proven"*. There is no audit `details` any more, and `RequestContext` no longer carries a client-IP bucket field at all, so **the backend records no client address anywhere** — trusted or otherwise. The unpinned hop count is now purely a gateway-side concern, which narrows what v2.1 owes here rather than widening it.

### Observability

- **OBS-01**: Proactive quota warnings via an `X-RateLimit-Remaining` response header
- **OBS-02**: Grace-period transparency in `GET /users/me` — `core.subscriptions.status` models `grace_period` but `04-users-me.md` does not surface it
- **OBS-03**: Webhook retry reconciliation via App Store Server API polling
- **OBS-04**: Startup exhaustiveness check for quota config (QUOTA-06)

## Out of Scope

Explicitly excluded. `SHARED-INVARIANTS.md` "Global deletions" binds every phase — none of these may be built in any phase.

| Feature | Reason |
|---------|--------|
| Backend-minted tokens or sessions | Authentication is per-request via the Firebase ID token only; no cookie, no secondary auth state, no generation counter |
| `checkRevoked` / per-request revocation checks | Already-minted ID tokens are never force-expired |
| `promo` grant source | Deleted from the enum and from every rule, example, and remediation that referenced it |
| Scheduled cleanup, purge, reconciliation, or background-healer jobs | Indefinite retention; no recovery-scan of challenge rows, empty accounts, or provider state |
| Device-fingerprint or device-check components in rate-limit keys | Excluded in any form |
| Claim-header authentication or header-derived identity | The gateway injects no identity headers and the backend ignores every client/proxy identity header |
| Distributed locks, leases, or multi-phase-commit machinery | Short database-only transactions under a fixed global lock order suffice |
| Wildcard or path-prefix provider-callback membership | Callback routes are named individually by exact path, added only by a spec change |
| Identity row deletion | Rows are tombstones; retirement is permanent and no path reverses it |
| Data migration, backfill, compatibility shims, dual-write windows, deprecated aliases | Pre-launch database with disposable data |
| `display_name` population from auth context | Never sourced from the Admin record or token claims |
| Incremental migration files during v2.0 | One initial migration, renamed and replaced |
| Multi-IdP support | Exactly one configured Firebase integration; store providers are not IdPs |
| Admin user management | Own profile only (`GET /users/me`) |

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| SCHEMA-01 … SCHEMA-08 | Phase 34 | Complete (SCHEMA-06 withdrawn by Phase 37.1) |
| FOUND-01, FOUND-04 … FOUND-08 | Phase 35 | Pending (FOUND-01/02 amended and FOUND-05 withdrawn by Phase 37.1, which also deleted one further requirement outright — see the FOUND block; FOUND-08 amended and flagged forward to phases 43–46 by Phase 37.2, and **amended again by Phase 37.3** — the providerData result types became one raised hierarchy; FOUND-05's compensating-log mechanism corrected by Phase 37.3. **Phase 37.4 deleted FOUND-02 outright** — the wire contract it stated is recorded under FOUND-01 as a property given up — and **amended FOUND-01 and FOUND-04**, the credential extractor becoming `HTTPBearer` and the error registry becoming one exception tree behind one handler) |
| FOUND-09 | v2.1 backlog | Deferred (D-08) |
| REBIND-01 … REBIND-06 | Phase 36 | Complete (REBIND-03 partial by design, REBIND-04 void; REBIND-01/02 amended by Phase 37.1) |
| CREATE-01 … CREATE-04 | Phase 37 | Complete (CREATE-01/02 amended by Phase 37.2 — pre-auth admission is two enumerated routes, and the prepare/completion partition is by route; the credential conflict is flagged under CREATE-01) |
| SYNC-01 … SYNC-03 | Phase 38 | Pending (SYNC-03 **decided and amended by Phase 38, 2026-09-01** — option (b), the durable-row obligation dropped rather than rebuilt; it is no longer blocked on anything) |
| PROF-01 … PROF-02 | Phase 39 | Complete (PROF-02 amended by Phase 37.1; PROF-01 amended by Phase 39, 2026-09-01 — the detached-row profile read and the omitted rate-limit entry) |
| UPGRADE-01 … UPGRADE-02 | Phase 40 | Complete (both **amended by Phase 40, 2026-09-02** — UPGRADE-01 carries the flagged conflict for D-01's removed client declaration, the uncounted Firebase exposure and the D-18 credential-mechanism reversal; UPGRADE-02 records that the prepare/completion partition is by route, inherited from Phase 37.2, and which brief obligations were already dead before this phase began) |
| ANONGRANT-01 … ANONGRANT-03 | Phase 41 | Complete (all three **amended by Phase 41, 2026-09-03** — ANONGRANT-01 carries the iOS-only flagged conflict, the reword of its own mode-partition wording after Phase 37.2, the unregistered `verification_required`, the 422 collapse, the uncounted Apple exposure, the dead-obligation inventory and the re-derived counts; ANONGRANT-02 carries the database-before-Apple conflict and the brief-versus-invariants lock order resolved by precedence; ANONGRANT-03 carries the anonymous-only and idempotent-repeat conflicts) |
| REGGRANT-01 … REGGRANT-03 | Phase 42 | Complete (all three **amended by Phase 42, 2026-09-03** — REGGRANT-01 carries the single-token and stored-row-claimant conflicts, the reword of its own prepare/completion wording after Phase 37.2, the uncounted Apple exposure, the dead-obligation inventory and the re-derived counts; REGGRANT-02 carries the conversion's skipped device gate, the D-07/D-08 schema deletion against `06-schema-reference.md`, the brief-versus-invariants lock order resolved by precedence, and the two measured facts about the conversion; REGGRANT-03 carries the account-uniqueness and absent-end-date conflicts and the five destinations. Marked met on 1001 unit, 237 e2e and 147 schema passed with ruff clean, measured in plan 42-06) |
| APPLEHOOK-01 … APPLEHOOK-02 | Phase 43 | Complete (both **amended by Phase 43, 2026-09-04** — APPLEHOOK-01 carries the always-registered-router, shared-body and absent-gateway-limit conflicts, the D-09 divergence from Apple's guidance that is in neither count, the two library environments that skip signature verification, the dead-obligation inventory, Apple's 4xx retry schedule and the six-day silent loss, D-18's lapsed-subscriber consequence, the uncounted unauthenticated request cost recorded as wider than its three predecessors, and the re-derived counts; **APPLEHOOK-02 is ANSWERED AND CLOSED** — the dedicated `APIRouter`, the `PROVIDER_CALLBACK_PATHS` literal, the exact-path registration, and what the two structural wiring cases cost. The store adapter seam flagged forward under APPLEHOOK-01 by Phase 37.2 is answered by declaring the seam beside its first implementation. Marked met on 1089 unit, 272 e2e and 182 schema passed with ruff clean, measured in plan 43-06) |
| PLAYHOOK-01 … PLAYHOOK-03 | Phase 44 | Pending (PLAYHOOK-03 flagged forward to its own phase — Phase 37.1; the store adapter seam inherited from Phase 43 under PLAYHOOK-02 — Phase 37.2) |
| RESTORE-01 … RESTORE-02 | Phase 45 | Pending (the vendor-proof adapter seam flagged forward under RESTORE-01 — Phase 37.2; the `restore_subscription` operation label flagged forward under RESTORE-01 — **Phase 40, 2026-09-02**, the label no longer exists) |
| SIGNOUT-01 … SIGNOUT-02 | Phase 46 | Pending (the `sign_out_all` operation label flagged forward under SIGNOUT-01 — **Phase 40, 2026-09-02**, the label no longer exists; SIGNOUT-02's audit half **settled by Phase 38's removal, 2026-09-01** — no durable row is owed and Phase 46 inherits no audit decision, while its fail-closed half is untouched and still binding; the revocation adapter seam flagged forward under SIGNOUT-01 — Phase 37.2) |

**Coverage:**

- v2.0 requirements: 47 total (49 defined; two deleted outright — one by Phase 37.1, FOUND-02 by Phase 37.4)
- Mapped to phases: 47
- Unmapped: 0 ✓
- Prefixes spanning more than one phase: 0 ✓

**Standing after Phase 37.1's two deletions (2026-08-24), Phase 37.2's auth-module simplification (2026-08-25), Phase 37.3's returned-rejection refactor (2026-08-26), Phase 37.4's third machine-generated-code refactor (2026-08-30) and Phase 37.5's fourth (2026-08-31):**

| Treatment | Requirements |
|-----------|--------------|
| Deleted outright | **two FOUND requirements.** Phase 37.1: the route registry and its enumeration assertion (see the FOUND block). Phase 37.4: **FOUND-02**, the exactly-one-`Authorization` wire contract — deleted at the developer's direction in the same decision that removed the invariant asserting it from `SHARED-INVARIANTS.md`; the property given up is recorded under FOUND-01, and the same rule surviving in four unamended phase briefs is reported there |
| Withdrawn, subject no longer exists | SCHEMA-06, FOUND-05 |
| Amended, substance intact | Phase 37.1: FOUND-01, FOUND-02, REBIND-01, REBIND-02, PROF-02, APPLEHOOK-01, FOUND-09. Phase 37.2: **CREATE-01**, **CREATE-02**, **FOUND-08**. Phase 37.3: **FOUND-08** again. Phase 37.4: **FOUND-01** again (the credential extractor became `HTTPBearer`) and **FOUND-04** (the shared registry module became one exception tree in one module behind one handler). Phase 37.5: **FOUND-04** again (the startup totality walk became a test, giving up boot-time fail-closed). Phase 37.5 also **corrected** three prior notes without amending their requirements — FOUND-01's module names, FOUND-03's overstated negative control, and FOUND-04's startup-check paragraph. Phase 43 (2026-09-04): **APPLEHOOK-01** — the store adapter seam Phase 37.2 flagged forward is answered by declaring the Protocol beside its first implementation in `auth/app_store.py`, and the three flagged conflicts, the Apple-guidance divergence and the accepted residual are recorded under it |
| Flagged forward — the owning phase decides | Phase 37.1: SYNC-03 (38), SIGNOUT-02 (46), APPLEHOOK-02 (43), PLAYHOOK-03 (44) — **all four were flagged forward on the same deleted mechanism, and Phase 38 (2026-09-01) settles that mechanism for all four by removing the invariants rather than by four separate decisions.** SYNC-03 is decided and amended; SIGNOUT-02's audit half is settled and its fail-closed half untouched and still binding; APPLEHOOK-02 and PLAYHOOK-03 are settled only as to audit and route→operation metadata and are **not closed** — § "Global deletions" still forbids wildcard or path-prefix provider-callback membership, so exact-path enumeration is still owed by phases 43 and 44. ***APPLEHOOK-02 is now CLOSED by Phase 43 (D-01), 2026-09-04** — the partition is a dedicated `APIRouter`, membership is the set of routes on it, `PROVIDER_CALLBACK_PATHS` makes it countable, and the route is registered at an exact path and is not on the public allowlist. **PLAYHOOK-03 stays open and now inherits a mechanism instead of a question:** Phase 44 adds one route and one literal member rather than a second partition.* Phase 37.2: FOUND-08's adapter seam → 43, 44, 45 and 46, each defining its interface beside its first implementation, carried unchanged by Phase 37.3 with D-12's addition that those phases name their outcomes as exception classes rather than re-growing an outcome enum |
| Flagged conflicts against the binding specification, unresolved | **Nineteen.** *Count updated by Phase 43, 2026-09-04: this cell read **sixteen** from 2026-09-03. Phase 43 adds three, all against `08-webhook-app-store.md` and all under APPLEHOOK-01 — D-02's always-registered router with a 503 on an incomplete configuration (`:26`/`:49`), D-04's shared one-field body (`:7`/`:21`/`:32`), and D-06's absent per-IP and per-URL gateway limits (`:24`/`:50`/`:51`). Phase 43's own uncounted item is the unbounded unauthenticated request cost, and its D-09 departure from Apple's production guidance is in **neither** number, because the binding specification mandates no online revocation check. The cell was re-derived against four named `SHARED-INVARIANTS.md` sections — § "Global deletions", § "Locks and transactions", § "Grants and evaluation time" and § "Fail-closed defaults" — and not one produced a divergence.* *Count corrected by Phase 42, 2026-09-03: this cell read **six** from 2026-09-02, and stayed at six when Phase 41 added four on 2026-09-03 — the header paragraph was updated then and this cell was not. Phase 42 adds six more. The six named below are the six that predate both phases; **Phase 41's four are under ANONGRANT-01 … ANONGRANT-03 and Phase 42's six under REGGRANT-01 … REGGRANT-03**, each listed by line in the header.* **Phase 40 D-01 — the removed client-declared target provider vs `05-upgrade-anonymous.md:26`/`:46`/`:53`/`:56`/`:60`/`:61`/`:81` (under UPGRADE-01), added 2026-09-02.** 37.1 D-06 vs `SHARED-INVARIANTS.md` § "The barrier" (under FOUND-01); 37.2 D-08 — Application Default Credentials vs § "Wire contract"'s no-ambient-credential rule (under CREATE-01); **37.4 D-11 — the plaintext pre-auth subject vs `02-create-user.md:49`/`:56` (under CREATE-02)**; **37.4 D-03 — consumption no longer conditioned on this attempt's claim id vs `02-create-user.md:65` (under CREATE-02)**; **37.4 D-06 — the provider-account conflict losing its own answer on the race path vs `02-create-user.md:63`/`:74` (under CREATE-04)**. Phase 37.4's two planned wire-contract conflicts (D-10, A-09) are **not** in this table: the developer removed the invariant asserting them, so there is no surviving shared-invariant text to diverge from. The property given up is recorded under FOUND-01, together with the four phase briefs that still state the rule. **Nor is 37.1 D-01 vs § "Audit" (under FOUND-05), which was counted in this row until 2026-09-01 and is now resolved by removal:** Phase 38 (D-03) deleted § "Audit" from `SHARED-INVARIANTS.md`, leaving no invariant text to diverge from; the entry survives under FOUND-05 as a resolved record with the removed obligation still quoted. **Three further divergences were known and deliberately uncounted when this cell was written**, making the set of known divergences nine rather than six: the `limits` mandate (a Phase 35 override, at `:50` and under FOUND-06), and Phase 40's two forward flags — the `core.auth_operation` shrink (under SCHEMA-01, RESTORE-01 and SIGNOUT-01) and the unbounded Firebase read (under UPGRADE-01, a consequence of the `limits` override). *Corrected by Phase 42, 2026-09-03: the uncounted divergences are now **seven** and the whole set **twenty-three**. Phase 41 added the lock-order precedence resolution, the 422 collapse and its unbounded Apple read; Phase 42 added its own unbounded Apple read on the registered route. All seven are enumerated in the header* |
| Flagged forward — added by Phase 40 (2026-09-02) | The `core.auth_operation` shrink reaches two unbuilt phases: **RESTORE-01** (45) and **SIGNOUT-01** (46) each name an operation enum label that no longer exists. Neither phase inherits a mechanism to rebuild — the label's only consumers, `audit.auth_events` and the route registry, were both deleted by Phase 37.1, and neither operation is challenge-bearing — so each decides for itself whether it needs an operation name at all. Filed as forward flags rather than counted conflicts, on the precedent Phase 37.1 set when it deleted `audit.auth_events` against `00-schema.md` and recorded that by withdrawing SCHEMA-06 |

---
*Requirements defined: 2026-08-19*
*Last updated: 2026-09-04 — amended by Phase 43 plan 06 for `POST /webhooks/app-store`. **APPLEHOOK-01 and APPLEHOOK-02 are marked met**, on 1089 unit, 272 e2e and 182 schema passed with ruff clean, all four commands run in plan 43-06 rather than copied. **APPLEHOOK-02 is ANSWERED AND CLOSED** — the provider-callback partition is a dedicated `APIRouter`, membership is the set of routes on it, and `PROVIDER_CALLBACK_PATHS` makes it countable; the flag has been open since 2026-08-24 and PLAYHOOK-03 now inherits a mechanism rather than a question. Three divergences are recorded as **new flagged conflicts** under APPLEHOOK-01 — the always-registered router answering 503 on an incomplete configuration (D-02), the shared one-field body (D-04), and the absent per-IP and per-URL gateway limits (D-06) — taking the count from sixteen to nineteen. The unbounded unauthenticated request cost is **flagged but uncounted**, taking the set of known divergences from twenty-three to twenty-seven, and is recorded as **wider** than the three residuals under UPGRADE-01, ANONGRANT-01 and REGGRANT-01, each of which needs a valid token first. **D-09's departure from Apple's production guidance is in neither number**, because the binding specification mandates no online revocation check; it is recorded in full under APPLEHOOK-01 with its measured cost. No requirement deleted, withdrawn or unchecked, and no specification file edited **Amended again 2026-09-04 by quick task 260904-u7t**, which closed the four critical findings of `43-REVIEW.md` (CR-01 … CR-04) that the phase's first verification recorded as gaps. APPLEHOOK-01's measurement basis is replaced — 1098 unit, 272 e2e, 189 schema, ruff clean — and re-verification reads `passed`, `6/6`. No count changes and no requirement re-opened: the four were implementation defects, not divergences from the brief.*
*Previously updated: 2026-09-03 — amended by Phase 42 plan 06 for `POST /auth/claim-registered-grant`. **REGGRANT-01, REGGRANT-02 and REGGRANT-03 are marked met**, on 1001 unit, 237 e2e and 147 schema passed with ruff clean, all four commands run in plan 42-06 rather than copied. Six divergences are recorded as **new flagged conflicts** under those three requirements — the device gate skipped on the conversion path (D-02), the single device token (D-04), the stored-row claimant with no live provider confirmation (D-05), account uniqueness resting on the existing indexes (D-06), the deletion of three tables from the one migration (D-07 with D-08), and the refusal carrying no end date (D-09(b)) — taking the count from ten to sixteen. D-16's unbounded Apple read is **flagged but uncounted**, taking the set of known divergences from sixteen to twenty-three. Every prior entry naming the deleted receipt row is corrected in place with a dated note and none is deleted: ANONGRANT-01, ANONGRANT-03, three entries in `STATE.md` and the header of `.planning/auth-refactor-endpoint-changes.md`. Two stale counts in the traceability table, left when Phase 41 updated the header alone, are corrected in the same stroke. No requirement deleted, withdrawn or unchecked, and no specification file edited*
*Previously updated: 2026-09-03 — amended by Phase 41 plan 05 and by Phase 41's code review for `POST /auth/claim-anonymous-grant`, which added four flagged conflicts and three uncounted divergences and marked ANONGRANT-01 … ANONGRANT-03 met. Both amendments are in the header block; neither added a line here, which is why this line jumped from Phase 40 to Phase 42*
*Previously updated: 2026-09-02 — amended by Phase 40 plan 08 for `POST /auth/upgrade-anonymous`. **UPGRADE-01 and UPGRADE-02 are checked**, on 857 passed in the default suite and 337 in `e2e or schema` with ruff clean, and both carry dated notes: D-01's removal of the client-declared target provider is a **new flagged conflict** against unamended text in `05-upgrade-anonymous.md`, taking the count from five to six; D-11's `core.auth_operation` shrink is **flagged forward** to phases 45 and 46, whose briefs name labels that no longer exist, and noted under SCHEMA-01 as the migration's second in-place edit; D-22's unbounded Firebase read is **flagged but uncounted**, being a consequence of the Phase 35 D-05 `limits` override; and D-18's test-credential mechanism is recorded as **reversed** — no signer exists on this machine and no service-account key can be minted, so nothing is minted or signed and a stored Google refresh token is exchanged and linked per run, with `40-CONTEXT.md`'s D-18 text deliberately left as decided. The brief's rate-limit and audit obligations are stated to have died before this phase began (Phase 35 D-05, Phase 37.1 D-01, Phase 38 D-03). The set of known divergences rises from six to nine. No requirement deleted, withdrawn or unchecked, and no specification file edited*
*Previously updated: 2026-09-01 — amended by Phase 38 plan 05 for the milestone's audit removal: SYNC-03 decided as **option (b)** and amended to what is actually owed — one `request` line per attempt from the request middleware, one WARNING per rejection from the shared handler, no durable row and no new success event (D-01/D-02). The audit half of SIGNOUT-02, APPLEHOOK-02 and PLAYHOOK-03 is settled by the same removal, each with a dated note naming what still binds (D-04); SIGNOUT-01 and SIGNOUT-02's fail-closed half are untouched and still binding, and § "Global deletions" still binds phases 43 and 44. The § "Audit" conflict under FOUND-05 is **resolved by removal** and kept as a resolved record, so the flagged-conflicts count drops from six to five and the set of known divergences from seven to six. No requirement deleted, withdrawn, checked or unchecked*
*Previously updated: 2026-08-31 — amended by Phase 37.5 plan 10 for the fourth machine-generated-code refactor: the layering restored into `services/`, `crud/` and `schemas/`, the docstring bar driven to 0 on every root, the error-tree self-check rehosted into `tests/` (FOUND-04 amended), and the quota charge moved inside admission so an unserved request is not billed. No requirement deleted, withdrawn or unchecked; no new conflict flagged and none resolved. Three prior notes corrected, and six items recorded as open in § Phase 37.5 records*
