---
phase: 48-narrow-identity-to-the-verified-pair
plan: 04
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - tests/unit/test_upgrade_precedence.py
  - tests/unit/test_claim_precedence.py
  - tests/unit/test_claim_precedence_registered.py
autonomous: true
requirements: []
estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-04 and D-05: a route declaring both dependencies needs both overrides, because the account dependency now declares the token dependency and not the reverse"
    - "D-08: the three routes read claims for the token values and linked for the rows"
    - "D-11: every test builds a VerifiedClaims or a LinkedIdentity"
  artifacts:
    - tests/unit/test_upgrade_precedence.py
    - tests/unit/test_claim_precedence.py
    - tests/unit/test_claim_precedence_registered.py
  key_links:
    - "get_claims -> get_identity: the override chain is reversed, so one override is no longer enough"
---

<objective>
Give the three precedence suites two dependency overrides each.

Purpose: D-04, D-05, D-08 and D-11. RESEARCH names this one of the four sites where the rewrite
is not a rename.
Output: three suites whose rejection precedence is unchanged and proved.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-PATTERNS.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The upgrade precedence suite overrides both dependencies</name>
  <files>tests/unit/test_upgrade_precedence.py</files>
  <read_first>
    - tests/unit/test_upgrade_precedence.py
    - src/nativespeaker/api/routers/auth.py
    - src/nativespeaker/api/app/dependencies.py
    - tests/unit/conftest.py
  </read_first>
  <action>
Today the suite overrides one dependency at `:158` and receives the second for free, because the
second declared the first. After D-04 and D-05 that chain runs the other way, so the client
fixture sets two entries: `app.dependency_overrides[get_claims] = lambda: claims` and
`app.dependency_overrides[get_identity] = lambda: linked`. The fixture at `:137-140` builds a
`VerifiedClaims` carrying the same two token values and a `LinkedIdentity` carrying the same two
rows it builds today, so every case keeps the caller it had. The override style is
`tests/unit/conftest.py:186-188`. No rejection, no order and no assertion changes.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_upgrade_precedence.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>test "$(grep -c 'dependency_overrides\[get_claims\]' tests/unit/test_upgrade_precedence.py)" = "1"</automated>
    <fails_when>non-zero exit, which means the token dependency is not overridden exactly once</fails_when>
  </verify>
  <acceptance_criteria>
    - Both `get_claims` and `get_identity` appear in `dependency_overrides` assignments
    - `grep -n '\bAuthIdentity\b' tests/unit/test_upgrade_precedence.py` prints nothing
    - The same number of cases passes as before the phase
    - `.venv/bin/pytest -q tests/unit/test_upgrade_precedence.py` exits 0
  </acceptance_criteria>
  <done>The upgrade route's precedence is proved with both values supplied.</done>
</task>

<task type="auto">
  <name>Task 2: The two grant claim precedence suites override both dependencies</name>
  <files>tests/unit/test_claim_precedence.py, tests/unit/test_claim_precedence_registered.py</files>
  <read_first>
    - tests/unit/test_claim_precedence.py
    - tests/unit/test_claim_precedence_registered.py
    - src/nativespeaker/api/routers/auth.py
    - src/nativespeaker/api/services/auth.py
  </read_first>
  <action>
Repeat Task 1 in both files: two overrides at `test_claim_precedence.py:261` and
`test_claim_precedence_registered.py:141`, and the fixtures at `:232-235` and `:112-115` build a
`VerifiedClaims` and a `LinkedIdentity`. Both suites drive
`complete_claim_anonymous_grant` and `complete_claim_registered_grant`, which now take `claims`
and `linked`; any direct service call in the module follows those keywords. The grant writers
record `claims.issuer` and `claims.subject`, so a case that asserts the written values keeps its
expectation only if the `VerifiedClaims` carries the same two values as the rows.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_claim_precedence.py tests/unit/test_claim_precedence_registered.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Each file assigns both `dependency_overrides[get_claims]` and `dependency_overrides[get_identity]`
    - `grep -n '\bAuthIdentity\b' tests/unit/test_claim_precedence.py tests/unit/test_claim_precedence_registered.py` prints nothing
    - `.venv/bin/pytest -q tests/unit/test_claim_precedence.py tests/unit/test_claim_precedence_registered.py` exits 0
  </acceptance_criteria>
  <done>Both grant routes' precedence is proved with both values supplied.</done>
</task>

</tasks>

<!-- planner-discipline-allow: AuthIdentity -->

## Artifacts this phase produces

The `claims` and `linked` fixtures are new names in all three suites. The renamed source symbols
this plan drives are `AuthService.complete_upgrade`, `complete_claim_anonymous_grant` and
`complete_claim_registered_grant`; the full list is in 48-01 § Artifacts this phase produces.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> the three challenge-bearing routes | a claimed challenge buys a grant or a provider flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-04-01 | Elevation of privilege | the two overrides | high | mitigate | a suite that overrides one dependency only would run the real account dependency against no database, so both entries are asserted present |
| T-48-04-02 | Repudiation | the two grant writers | medium | mitigate | the recorded values come from `claims`, and the suites keep the cases that assert them |
| T-48-04-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/unit/test_upgrade_precedence.py tests/unit/test_claim_precedence.py tests/unit/test_claim_precedence_registered.py` exits 0
- Each of the three files assigns both overrides

</verification>

<success_criteria>
The reversed dependency chain is handled in all three suites, and no rejection precedence moves.
</success_criteria>

<output>
Create `.planning/phases/48-narrow-identity-to-the-verified-pair/48-04-SUMMARY.md` when done.
</output>
