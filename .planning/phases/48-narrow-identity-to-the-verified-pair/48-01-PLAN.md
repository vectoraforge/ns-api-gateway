---
phase: 48-narrow-identity-to-the-verified-pair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nativespeaker/api/schemas/auth.py
  - src/nativespeaker/api/crud/identities.py
  - src/nativespeaker/api/crud/challenges.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/services/auth.py
  - src/nativespeaker/api/services/restore.py
  - src/nativespeaker/api/routers/auth.py
  - src/nativespeaker/api/routers/users.py
  - src/nativespeaker/api/routers/chats.py
  - src/nativespeaker/api/routers/root.py
  - src/nativespeaker/api/routers/examples.py
  - tests/unit/conftest.py
  - tests/unit/test_identity_accessors.py
  - tests/unit/test_app_wiring.py
autonomous: false
requirements: []
estimate:
  tokens: 70000
  raw_tokens: 140000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-01: no module in src/ or tests/ names the deleted four-field class; the token result is VerifiedClaims from auth/jwt_verifier.py"
    - "D-02: LinkedIdentity has exactly the fields user and identity, both required, frozen, slotted, base object"
    - "D-03: IdentitiesDB.resolve takes issuer and subject only, answers LinkedIdentity or None, and keeps IdentityUnresolvable, HistoricalIdentity and BlockedUser"
    - "D-04: get_claims refuses the same five token arms and opens no session"
    - "D-05: get_identity answers PreAuthIdentityNotAllowed for a caller with no row, and resolves in its own short session"
    - "D-06: the challenge route declares get_claims and get_db, and calls resolve after the operation check"
    - "D-07: create-user declares get_claims only"
    - "D-08: services and the challenge store read claims for the two token values and linked for the two rows"
    - "D-10: no comment survives whose subject is the deleted fields, the deleted flag or the old return value"
  artifacts:
    - src/nativespeaker/api/schemas/auth.py
    - src/nativespeaker/api/app/dependencies.py
    - src/nativespeaker/api/crud/identities.py
    - tests/unit/test_identity_accessors.py
    - tests/unit/test_app_wiring.py
  key_links:
    - "get_claims -> get_identity: FastAPI caches the first, so a route declaring both verifies the token once"
    - "get_identity -> IdentitiesDB.resolve: one statement in one short session, closed before the handler"
    - "AuthService._complete -> ChallengesDB.verify_binding: the one place that carries LinkedIdentity | None in services/"
---

<objective>
Delete the four-field identity class. Ship the two value types, the two dependencies and every
`src/` caller in one commit, and prove the barrier end to end.

Purpose: D-01 to D-08 and D-10. The type change breaks every site at once, so `src/` moves
together.
Output: 11 rewritten `src/` files, the unit conftest, and the two suites that pin the barrier.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-PATTERNS.md
@AGENTS.md
</context>

<planner_notes>
Two planning decisions this phase's artifacts did not settle. Both were measured against the
working tree, not reasoned about.

**P-01 — the already-linked caller at create-user.** D-07 gives create-user `get_claims` only,
so the completion path holds no row. `ChallengesDB.verify_binding` raises
`ChallengeIdentityMismatch` when `row.bound_external_identity_id` is set and the presenter has
no row. A caller that already has an active row is issued a create-user challenge bound to that
row (`crud/challenges.py:53-54`), so after D-07 it would answer 409 `challenge_required` where
it answers 409 `identity_already_linked` today, and would not spend the challenge.
`tests/e2e/test_create_user.py:203-256` pins both of today's outcomes. Task 1 puts the three
options to the developer. Tasks 2 and 3, and plans 02 to 08, are written for option B.

**P-02 — the `PostClaim` alias (RESEARCH open question A3).** `_complete` loses the type
variable `I` and keeps `T`. Each public method binds `linked` into its own `partial`, so
`type PostClaim[T] = Callable[[VerifiedClaims], Awaitable[T]]` and no alias spells an optional.
`_complete` still holds `linked: LinkedIdentity | None` for `verify_binding`. ROADMAP criterion
3 was amended on 2026-09-16 to name `AuthService._complete` beside `crud/challenges.py`.

**Spec-less probe fallback: skipped, visibly.** There is no SPEC file and no requirement ID maps
to this phase, so no probe predicates were generated. `must_haves.truths` come from the five
ROADMAP criteria and from D-01 to D-11 instead.

**RESEARCH open question 2 is decided:** delete `TestNoClientAddressIsCarried`'s string-field
case at `tests/unit/test_identity_accessors.py:343-346`. `LinkedIdentity` has no `str` field
after D-02, and `tests/unit/test_jwt_security.py:207-227` already pins `VerifiedClaims`.
</planner_notes>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide what an already-linked caller gets at create-user</name>
  <files>none — nothing is edited until the answer is given</files>
  <read_first>
    - tests/e2e/test_create_user.py:197-256
    - src/nativespeaker/api/crud/challenges.py:90-104
    - src/nativespeaker/api/services/auth.py:331-366
    - tests/unit/test_create_user_precedence.py:58-84
    - .planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md (D-07)
  </read_first>
  <decision>What `POST /auth/create-user` answers a caller that already has an active identity row, after D-07 takes the account dependency off the route.</decision>
  <context>
Planning measured this; CONTEXT.md and RESEARCH.md do not record it. Today such a caller gets a
create-user challenge bound to `bound_external_identity_id` (`crud/challenges.py:53-54`),
presents it, and `AuthService._reject_existing_identity` answers 409 `identity_already_linked`
after the challenge is claimed and consumed. `tests/e2e/test_create_user.py:203-256` pins both
outcomes. After D-07 the completion path holds no row, so `ChallengesDB.verify_binding` reaches
the row-bound branch with nothing to compare and raises `ChallengeIdentityMismatch` first: 409
`challenge_required`, and the challenge stays issued. D-07 promised the same status and the same
code. It is the same status and a different code, and the client is told to fetch a new
challenge, which fails the same way every time.
  </context>
  <options>
    <option id="option-a">
      <name>Ship D-07 as written</name>
      <pros>No extra query. The smallest code change. The caller is refused either way.</pros>
      <cons>An already-linked client is answered `challenge_required` and loops. `tests/e2e/test_create_user.py:203-256` is rewritten to the new codes, and the phase stops being behavior-preserving.</cons>
    </option>
    <option id="option-b">
      <name>`AuthService.complete` resolves the caller itself (recommended)</name>
      <pros>Every wire outcome is byte-identical to today, including the three rejections and their precedence. The query count for create-user does not move, because the router-level dependency ran the same statement today. D-07's accepted cost of one spent challenge does not ship.</pros>
      <cons>One test guard changes: `_StubSession.exec` in `tests/unit/test_create_user_precedence.py:82-84` raises on any statement today, and becomes a recorder answering no row, plus a case pinning exactly one statement.</cons>
    </option>
    <option id="option-c">
      <name>`issue_challenge` refuses `create_user` for a caller that already has a row</name>
      <pros>No query at completion. The caller still gets `identity_already_linked`, and spends nothing.</pros>
      <cons>Adds the prepare-time pre-check `services/auth.py:337-340` calls racy, and rewrites the same two e2e cases.</cons>
    </option>
  </options>
  <resume-signal>Answer "A", "B" or "C". Plans 48-01 to 48-08 are written for B. On A or C, edit no source file, stop the phase and re-run `/gsd:plan-phase 48`.</resume-signal>
  <acceptance_criteria>
    - The developer answered A, B or C, and the answer is quoted in the plan summary
    - On B, execution continues with Task 2
    - On A or C, no source file is edited and the phase stops for re-planning
  </acceptance_criteria>
  <done>The developer chose one option, and it is recorded.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: The two types, the two dependencies and every src caller</name>
  <files>src/nativespeaker/api/schemas/auth.py, src/nativespeaker/api/crud/identities.py, src/nativespeaker/api/crud/challenges.py, src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/services/auth.py, src/nativespeaker/api/services/restore.py, src/nativespeaker/api/routers/auth.py, src/nativespeaker/api/routers/users.py, src/nativespeaker/api/routers/chats.py, src/nativespeaker/api/routers/root.py, src/nativespeaker/api/routers/examples.py, tests/unit/conftest.py, tests/unit/test_identity_accessors.py</files>
  <read_first>
    - src/nativespeaker/api/schemas/auth.py
    - src/nativespeaker/api/auth/jwt_verifier.py:55-70
    - src/nativespeaker/api/crud/identities.py
    - src/nativespeaker/api/crud/challenges.py
    - src/nativespeaker/api/app/dependencies.py
    - src/nativespeaker/api/services/auth.py
    - src/nativespeaker/api/services/restore.py
    - src/nativespeaker/api/routers/auth.py
    - src/nativespeaker/api/routers/users.py
    - src/nativespeaker/api/routers/chats.py
    - src/nativespeaker/api/routers/root.py
    - src/nativespeaker/api/routers/examples.py
    - tests/unit/conftest.py
    - tests/unit/test_identity_accessors.py
    - .planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md (the two `Inventory` sections carry every file:line)
    - .planning/phases/48-narrow-identity-to-the-verified-pair/48-PATTERNS.md
  </read_first>
  <reversibility rating="reversible">D-07 moves create-user's rejection site. Nothing is migrated, no client contract changes under option B, and a later phase can move the resolve call back to a dependency in one commit.</reversibility>
  <behavior>
    - A caller with no row on a route declaring `get_identity` answers 403 `preauth_identity_not_allowed`
    - A caller with both rows on the same route reaches the handler and reads `linked.user.id`
    - A caller with no row on a route declaring `get_claims` reaches the handler
    - An unverifiable token opens no session on either route
    - One request opens one session and issues one statement, closed before the handler runs
  </behavior>
  <action>
Rewrite `src/` in one commit. RESEARCH's inventory tables carry every file:line; the target state
is below.

`schemas/auth.py:94-108` — delete the four-field class. `LinkedIdentity` becomes a
`@dataclass(frozen=True, slots=True)` with no base class and two required fields, `user: User`
and `identity: ExternalIdentity`, and a one-line docstring. The imports at `:8-10` stay.

`crud/identities.py` — `resolve(self, *, issuer: str, subject: str) -> LinkedIdentity | None`.
The no-row branch returns `None`. The three rejections at `:45-53` are copied character for
character. The return becomes `LinkedIdentity(user=user, identity=identity)`. Drop the
`PreAuthIdentityNotAllowed` import. `insert_account` takes `claims: VerifiedClaims` and reads
`claims.issuer` and `claims.subject`. Import `VerifiedClaims` from
`nativespeaker.api.auth.jwt_verifier`; that module imports nothing from this project, so no
cycle is possible.

`app/dependencies.py:58-85` — split into two. `get_claims(request, credential=Depends(_bearer))
-> VerifiedClaims` holds today's lines 58-72 and returns `claims`. `get_identity(request,
claims: VerifiedClaims = Depends(get_claims)) -> LinkedIdentity` holds today's lines 74-76: it
opens its own session from `request.app.state.session_factory`, calls
`IdentitiesDB(session).resolve(issuer=claims.issuer, subject=claims.subject)`, and raises
`PreAuthIdentityNotAllowed` when that answers `None`. Delete the old narrowing dependency. Keep
the comment at `:79` above the new `get_identity`; it still states why nothing calls it directly.

`crud/challenges.py` — `issue(self, session, *, operation, claims: VerifiedClaims, linked:
LinkedIdentity | None)` and `verify_binding(self, row, claims: VerifiedClaims, linked:
LinkedIdentity | None)`. Each branch keeps its shape: `linked is not None` selects
`linked.identity.id`, otherwise the two values come from `claims`. This file and
`AuthService._complete` are the only two places in `src/` that spell the optional.

`services/auth.py` —
`Write = Callable[[VerifiedClaims, VerifiedProviderIdentity], Awaitable[IdentityProvider]]`;
`type PostClaim[T] = Callable[[VerifiedClaims], Awaitable[T]]`.
`complete(*, claims: VerifiedClaims, challenge_id)` resolves the caller itself, per option B:
`linked = await self.identities_db.resolve(issuer=claims.issuer, subject=claims.subject)`,
before `_complete`. `complete_upgrade`, `complete_claim_anonymous_grant` and
`complete_claim_registered_grant` take `claims` and `linked: LinkedIdentity`.
`_complete[T](self, *, claims, linked: LinkedIdentity | None, challenge_id, operation,
post_claim: PostClaim[T]) -> T` keeps its sequence unchanged, calls
`self.challenge_store.verify_binding(located, claims, linked)`, and calls `post_claim(claims)`.
The two grant methods receive `linked` through their `partial`:
`_claim_anonymous_grant(self, claims, *, linked: LinkedIdentity, device_token)` and
`_claim_registered_grant(self, claims, *, linked: LinkedIdentity, device_token)`, both annotated
non-optional. `_read_then_write(self, claims, *, write)`, `_apply_create_user(self, claims,
facts)`, `_apply_upgrade(self, claims, facts)`, `_settle(self, linked, ...)` and
`create_user(*, claims, provider, provider_uid, email)` follow. Every `identity.user.` read
becomes `linked.user.`, every `identity.identity.` becomes `linked.identity.`, and every
`identity.issuer` / `identity.subject` becomes `claims.issuer` / `claims.subject`, including the
two grant writers at `:198-199` and `:254-255` and the lock read at `:303-304`.
`_reject_existing_identity` is unchanged.

`services/restore.py:54-59` — the parameter is `linked: LinkedIdentity`, and the read is
`linked.user.id`.

`routers/auth.py` — the router-level dependency becomes `Depends(get_claims)`.
`issue_challenge` declares `claims=Depends(get_claims)`, keeps `session=Depends(get_db)`, and
calls `linked = await IdentitiesDB(session).resolve(issuer=claims.issuer,
subject=claims.subject)` **between** the operation check at `:56-59` and the row check at `:62`,
which becomes `linked is None`; `challenge_store.issue` is passed `claims=` and `linked=`. It
never declares `get_identity`. `create_user` declares `claims=Depends(get_claims)` only. The
remaining routes follow D-08's table: `upgrade_anonymous`, `claim_anonymous_grant`,
`claim_registered_grant` and `sign_out_all` declare `claims=Depends(get_claims)` and
`linked=Depends(get_identity)`; `restore_subscription` and `sync` declare `linked` only.
`sign_out_all` keeps `revoke_with_retry(adapter, claims.issuer, claims.subject)` and reads
`linked.identity.id`.

`routers/users.py`, `routers/chats.py`, `routers/root.py`, `routers/examples.py` — both the
router-level `Depends()` and every handler parameter take the new `get_identity`. Handler
parameters are named `linked`. `root.py:9` and `examples.py:8` change the router-level name
only, and RESEARCH A1 records them as in scope although CONTEXT's list omits them.

`tests/unit/conftest.py` — `TEST_IDENTITY` drops its two token-value keywords, the override at
`:188` keys on the new `get_identity`, and `FakeChallengeStore.verify_binding` follows the store's
new signature.

`tests/unit/test_identity_accessors.py` — `ACCESSORS = (get_claims, get_identity)`; `_linked()`
builds a `LinkedIdentity` and `_unlinked()` becomes a `VerifiedClaims` builder; the probe route
on `get_claims` returns `claims.subject` rather than a row test; the signature case expects
`["request", "credential"]` for `get_claims` and `["request", "claims"]` for `get_identity`;
`TestTheIdentityShape` is deleted and replaced by one case asserting
`sorted(LinkedIdentity.__dataclass_fields__) == ["identity", "user"]`, that an assignment raises,
that `__slots__` is present and that `LinkedIdentity.__mro__[1] is object`; the string-field case
at `:343-346` is deleted, per P-02's note above. Keep the two session pins at `:152-156` and
`:287-297` unchanged: they are criterion 2.

Comments, per D-10: delete `crud/identities.py:40-41`, the two class docstrings at
`schemas/auth.py:96` and `:105`, and every other comment whose subject is the deleted fields,
the deleted flag or the old return value. Write no comment that names a deleted identifier.

Expect `.venv/bin/pytest -q -m ''` to be red after this task: 21 test files still name the
deleted class. Plans 02 to 07 turn them green, one file group each.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_identity_accessors.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
    <automated>test -z "$(grep -rn '\bAuthIdentity\b' src)"</automated>
    <fails_when>non-zero exit, which means a src module still names the deleted class</fails_when>
    <automated>test "$(grep -rln 'LinkedIdentity | None' src | sort | tr '\n' ' ')" = "src/nativespeaker/api/crud/challenges.py src/nativespeaker/api/services/auth.py "</automated>
    <fails_when>non-zero exit, which means a third src file declares the optional</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn '\bAuthIdentity\b' src` prints nothing
    - `grep -c 'async def get_claims' src/nativespeaker/api/app/dependencies.py` prints 1
    - `grep -c 'async def get_identity' src/nativespeaker/api/app/dependencies.py` prints 1
    - `grep -rn 'get_linked_identity' src` prints nothing
    - `grep -rn 'allow_preauth' src` prints nothing
    - `.venv/bin/pytest -q tests/unit/test_identity_accessors.py` exits 0
    - `.venv/bin/ruff check src tests` prints "All checks passed!"
  </acceptance_criteria>
  <done>
The barrier works end to end on real routers: a caller with no row is refused 403 on a route
declaring `get_identity`, a caller with both rows reaches the handler, and a caller with no row
reaches a handler behind `get_claims`. One session, one statement, closed before the handler.
  </done>
</task>

<task type="auto">
  <name>Task 3: The declaration matrix follows the two names</name>
  <files>tests/unit/test_app_wiring.py</files>
  <read_first>
    - tests/unit/test_app_wiring.py
    - src/nativespeaker/api/app/dependencies.py
    - src/nativespeaker/api/routers/auth.py
  </read_first>
  <action>
Rewrite `tests/unit/test_app_wiring.py` against the two new names. `PREAUTH_CALLABLE_PATHS` at
`:19` stays `{"/auth/create-user", "/auth/challenge"}`: both declare `get_claims` alone. The
matrix at `:57-62` keys on `get_identity`, the exemption set at `:64-69` keys on `get_claims`,
the seven named routes at `:71-80` key on `get_identity`, and the four remaining cases at
`:92-113` and `:132-138` take both new names. Rewrite the doubly-declared probe at `:183-255`:
the handler declares `linked: LinkedIdentity = Depends(get_identity)` and
`admitted: VerifiedClaims = Depends(get_claims)`, and the identity comparison at `:241` becomes
`linked.identity.issuer == admitted.issuer` and `linked.identity.subject == admitted.subject`,
because a `VerifiedClaims` carries no row. Keep the one-verify one-query counts: they are what
prove the FastAPI cache still holds.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_app_wiring.py tests/unit/test_identity_accessors.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'get_linked_identity' tests/unit/test_app_wiring.py` prints nothing
    - `grep -c 'PREAUTH_CALLABLE_PATHS' tests/unit/test_app_wiring.py` prints 2 or more, and the set still holds the two auth paths
    - The doubly-declared probe still asserts one token verification and one identity query
    - `.venv/bin/pytest -q tests/unit/test_app_wiring.py` exits 0
  </acceptance_criteria>
  <done>Every route's declaration is read off the live app and matches D-08's table.</done>
</task>

</tasks>

<!-- planner-discipline-allow: AuthIdentity -->
<!-- planner-discipline-allow: get_linked_identity -->
<!-- planner-discipline-allow: allow_preauth -->

## Artifacts this phase produces

New or renamed symbols. A drift check must not read these as pre-existing names.

| Symbol | File | Shape |
|---|---|---|
| `get_claims` | `app/dependencies.py` | new dependency, `(request, credential) -> VerifiedClaims` |
| `get_identity` | `app/dependencies.py` | renamed, `(request, claims) -> LinkedIdentity` |
| `LinkedIdentity` | `schemas/auth.py` | re-declared: `user`, `identity`, no base class |
| `IdentitiesDB.resolve` | `crud/identities.py` | `(*, issuer, subject) -> LinkedIdentity \| None` |
| `IdentitiesDB.insert_account` | `crud/identities.py` | parameter `claims` |
| `ChallengesDB.issue`, `ChallengesDB.verify_binding` | `crud/challenges.py` | parameters `claims`, `linked` |
| `Write`, `PostClaim` | `services/auth.py` | `PostClaim[T]` takes `claims` alone |
| `AuthService.complete`, `complete_upgrade`, `complete_claim_anonymous_grant`, `complete_claim_registered_grant`, `_complete`, `_read_then_write`, `_claim_anonymous_grant`, `_claim_registered_grant`, `_settle`, `_apply_create_user`, `_apply_upgrade`, `create_user` | `services/auth.py` | parameters `claims` and `linked` |
| `RestoreService.restore` | `services/restore.py` | parameter `linked` |
| `TestTheLinkedIdentityShape` | `tests/unit/test_identity_accessors.py` | new case class |
| `test_resolve_answers_none_when_no_row_exists` | `tests/unit/test_identities_crud.py` | new case (plan 02) |
| `claims`, `linked`, `linked_client` fixtures | the unit suites | renamed fixtures |

## Source Coverage Audit

| Source | Item | Covered by |
|---|---|---|
| GOAL | delete the four-field class; two types; two dependencies; `resolve` loses the flag; the route table | 48-01 |
| REQ | none mapped | — |
| RESEARCH | 11 `src/` files, 23 `tests/` files, the 4 non-mechanical sites, the 6 pitfalls | 48-01 to 48-08 |
| RESEARCH | `routers/root.py`, `routers/examples.py` (A1) | 48-01 Task 2 |
| RESEARCH | `auth/google_play.py:240`, `tests/unit/test_google_play_notifications.py:843` — docstrings naming a name that still exists | no edit; verified in 48-08 |
| RESEARCH | open question A3 (`PostClaim`) | 48-01 § planner_notes P-02 |
| RESEARCH | open question 2 (the string-field case) | 48-01 Task 2 |
| CONTEXT | D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-10 | 48-01 |
| CONTEXT | D-11 | 48-02 to 48-07 |
| CONTEXT | D-09 (done in the discuss commit) | 48-08, verified, no edit |

No item is unplanned. The Deferred Ideas in CONTEXT.md are excluded by rule.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `get_claims` | an unverified bearer credential crosses here |
| `get_identity` -> `IdentitiesDB.resolve` | the verified token values become two database rows |
| challenge presenter -> `ChallengesDB.verify_binding` | a secret handle is claimed by a presenter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Elevation of privilege | `app/dependencies.py::get_identity` | high | mitigate | `None` from `resolve` raises `PreAuthIdentityNotAllowed`; pinned by `test_identity_accessors.py` and `tests/e2e/test_admission.py:78-87` |
| T-48-02 | Elevation of privilege | `crud/identities.py::resolve` | high | mitigate | the three rejections at `:45-53` are copied character for character and re-run in plan 02 |
| T-48-03 | Elevation of privilege | `routers/auth.py::issue_challenge` | high | mitigate | `linked is None` with any operation but `create_user` raises `PreAuthIdentityNotAllowed`, after the operation check |
| T-48-04 | Spoofing | `crud/challenges.py::verify_binding` | high | mitigate | option B keeps the row-bound branch reachable at create-user, so a handle bound to one row cannot be spent by another caller |
| T-48-05 | Repudiation | `routers/auth.py::sign_out_all`, the two grant writers | medium | mitigate | D-08: the request-verified `claims` are recorded, never `linked.identity.issuer` |
| T-48-06 | Denial of service | `app/dependencies.py::get_claims` | medium | mitigate | it reads no table; `test_identity_accessors.py:152-156` asserts a refused token opens no session |
| T-48-07 | Denial of service | the FastAPI dependency cache | low | mitigate | a route declaring both dependencies verifies the token once; `test_app_wiring.py` counts one verify and one query |
| T-48-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package; `pyproject.toml` is untouched |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/unit/test_identity_accessors.py tests/unit/test_app_wiring.py` exits 0
- `.venv/bin/ruff check src tests` prints "All checks passed!"
- `grep -rn '\bAuthIdentity\b' src` prints nothing
- `grep -rn 'get_linked_identity\|allow_preauth' src` prints nothing
- `grep -rln 'LinkedIdentity | None' src` prints `crud/challenges.py` and `services/auth.py` and nothing else
</verification>

<success_criteria>
The barrier is the two dependencies, the two types hold no optional field, and one real request
proves the whole path. `src/` carries no reference to the deleted class.
</success_criteria>

<output>
Create `.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md` when done.
</output>
