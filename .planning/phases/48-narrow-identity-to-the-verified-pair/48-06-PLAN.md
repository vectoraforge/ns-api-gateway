---
phase: 48-narrow-identity-to-the-verified-pair
plan: 06
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - tests/unit/test_restore_proof.py
  - tests/unit/test_users_me.py
  - tests/unit/test_auth_security.py
  - tests/unit/test_jwks_offload.py
autonomous: true
requirements: []
estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-02: every LinkedIdentity is built with two keywords, and none carries a token value"
    - "D-05: every probe route and every client fixture declares the account dependency by its new name"
    - "D-08: RestoreService.restore takes linked, and the users route reads linked.user and linked.identity"
  artifacts:
    - tests/unit/test_restore_proof.py
    - tests/unit/test_users_me.py
    - tests/unit/test_auth_security.py
    - tests/unit/test_jwks_offload.py
  key_links:
    - "get_identity -> the restore and users routes: the parameter is linked at every call site"
---

<objective>
Follow the rename in the restore service suite, the users route suite and the two probe-route
suites.

Purpose: D-02, D-05, D-08 and D-11.
Output: four suites that build the two-field type and declare the new dependency name.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The restore suite passes linked at every call site</name>
  <files>tests/unit/test_restore_proof.py</files>
  <read_first>
    - tests/unit/test_restore_proof.py
    - src/nativespeaker/api/services/restore.py
  </read_first>
  <action>
`_caller()` at `:26, 620-629` builds a `LinkedIdentity` with `user=` and `identity=` and no
token-value keywords. The 11 `service.restore(...)` call sites at `:640-1166` pass `linked=`. No
proof, store, term or refusal assertion changes: this suite's subject is the two store proofs,
not the barrier.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_restore_proof.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>test "$(grep -c 'linked=' tests/unit/test_restore_proof.py)" = "11"</automated>
    <fails_when>non-zero exit, which means a call site was missed or one was added</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'issuer=' tests/unit/test_restore_proof.py` counts no occurrence inside a `LinkedIdentity(` construction
    - All 11 restore call sites pass `linked=`
    - `.venv/bin/pytest -q tests/unit/test_restore_proof.py` exits 0
  </acceptance_criteria>
  <done>The restore service is driven by the renamed parameter, with every refusal unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: The users route suite and the two probe suites take the new name</name>
  <files>tests/unit/test_users_me.py, tests/unit/test_auth_security.py, tests/unit/test_jwks_offload.py</files>
  <read_first>
    - tests/unit/test_users_me.py
    - tests/unit/test_auth_security.py
    - tests/unit/test_jwks_offload.py
    - src/nativespeaker/api/routers/users.py
  </read_first>
  <action>
`test_users_me.py:14, 75-83, 87, 93, 107` builds a `LinkedIdentity` with two keywords and
overrides `get_identity`; the fixture and the helper keep their names or take `linked`, whichever
reads better against the handler's own parameter. `test_auth_security.py:8, 38` and
`test_jwks_offload.py:16, 105` declare their probe route as
`linked: LinkedIdentity = Depends(get_identity)`. Neither probe suite changes what it measures:
one is the security headers, the other is the JWKS fetch off the event loop.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_users_me.py tests/unit/test_auth_security.py tests/unit/test_jwks_offload.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'get_linked_identity\|\bAuthIdentity\b' tests/unit/test_users_me.py tests/unit/test_auth_security.py tests/unit/test_jwks_offload.py` prints nothing
    - `.venv/bin/pytest -q tests/unit/test_users_me.py tests/unit/test_auth_security.py tests/unit/test_jwks_offload.py` exits 0
  </acceptance_criteria>
  <done>Three suites declare the account dependency by its new name and pass.</done>
</task>

</tasks>

<!-- planner-discipline-allow: AuthIdentity -->
<!-- planner-discipline-allow: get_linked_identity -->

## Artifacts this phase produces

`RestoreService.restore`'s parameter `linked` and the renamed `get_identity` are what these four
suites drive; the full list is in 48-01 § Artifacts this phase produces.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `POST /auth/restore-subscription` | a store proof buys an entitlement |
| client -> `GET /users/me` | an account's own profile is read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-06-01 | Elevation of privilege | `services/restore.py::restore` | high | mitigate | the destination is still `linked.user.id`, and all 11 call sites are re-run |
| T-48-06-02 | Information disclosure | `GET /users/me` | medium | mitigate | the handler reads `linked.user` alone, and the suite keeps its body assertions |
| T-48-06-03 | Denial of service | the JWKS fetch | low | mitigate | the offload case is unchanged; only the probe route's declaration is renamed |
| T-48-06-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/unit/test_restore_proof.py tests/unit/test_users_me.py tests/unit/test_auth_security.py tests/unit/test_jwks_offload.py` exits 0
</verification>

<success_criteria>
Four suites build the two-field type, declare the new dependency, and keep every assertion.
</success_criteria>

<output>
Create `.planning/phases/48-narrow-identity-to-the-verified-pair/48-06-SUMMARY.md` when done.
</output>
