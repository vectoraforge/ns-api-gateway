---
phase: 48-narrow-identity-to-the-verified-pair
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - tests/unit/test_identities_crud.py
  - tests/unit/test_exception_handlers.py
autonomous: true
requirements: []
estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-03: resolve answers None for no row, and still raises IdentityUnresolvable, HistoricalIdentity and BlockedUser"
    - "D-11: no test passes the deleted flag, and no test asserts a None row field"
  artifacts:
    - tests/unit/test_identities_crud.py
    - tests/unit/test_exception_handlers.py
  key_links:
    - "resolve -> the three rejections: criterion 2 is what these two suites pin"
---

<objective>
Follow `resolve`'s new signature in the crud suite and in the admission fixture of the handler
suite.

Purpose: D-03 and D-11, and ROADMAP criterion 2.
Output: the crud suite drops the deleted flag and gains the no-row case.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The crud suite calls resolve with two keywords</name>
  <files>tests/unit/test_identities_crud.py</files>
  <read_first>
    - tests/unit/test_identities_crud.py
    - src/nativespeaker/api/crud/identities.py
  </read_first>
  <behavior>
    - `resolve` answers `None` when the joined query returns no row
    - `resolve` answers a `LinkedIdentity` holding both rows when the row is active and its user is active
    - An identity row with no user raises `IdentityUnresolvable`
    - A non-active identity row raises `HistoricalIdentity`
    - A non-active user raises `BlockedUser`
  </behavior>
  <action>
`_resolve`, `_rejected` and `_drive` at `:60-83` lose the flag argument and the
`preauth_callable` parametrization. Delete the two cases at `:89-98` that assert the unlinked
shape and the case at `:100-109` that expects `PreAuthIdentityNotAllowed`: that rejection now
belongs to `get_identity`, and `tests/unit/test_identity_accessors.py` pins it. Add one case,
`test_resolve_answers_none_when_no_row_exists`, asserting `await _resolve(row=None) is None`.
The three rejection cases at `:112-186` keep every assertion; only the call loses its argument.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_identities_crud.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>test -z "$(grep -n 'allow_preauth\|preauth_callable' tests/unit/test_identities_crud.py)"</automated>
    <fails_when>non-zero exit, which means the deleted flag is still named</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'is None' tests/unit/test_identities_crud.py` prints 1 or more, and the new no-row case is one of them
    - `grep -c 'IdentityUnresolvable\|HistoricalIdentity\|BlockedUser' tests/unit/test_identities_crud.py` prints 3 or more
    - `grep -n 'PreAuthIdentityNotAllowed' tests/unit/test_identities_crud.py` prints nothing
    - `.venv/bin/pytest -q tests/unit/test_identities_crud.py` exits 0
  </acceptance_criteria>
  <done>The crud suite proves the new return value and all three rejections.</done>
</task>

<task type="auto">
  <name>Task 2: The admission fixture drops the deleted argument</name>
  <files>tests/unit/test_exception_handlers.py</files>
  <read_first>
    - tests/unit/test_exception_handlers.py:340-380
    - src/nativespeaker/api/crud/identities.py
  </read_first>
  <action>
The admission fixture at `:356-362` calls `resolve` with the flag. Drop the argument. The
fixture's caller expects the rejection the row earns, so no assertion changes. Leave the rest of
the module untouched: the 409 and 403 tables are error-registry facts, not identity facts.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_exception_handlers.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'allow_preauth' tests/unit/test_exception_handlers.py` prints nothing
    - `.venv/bin/pytest -q tests/unit/test_exception_handlers.py` exits 0
  </acceptance_criteria>
  <done>The handler suite drives the new signature and keeps every status and code.</done>
</task>

</tasks>

<!-- planner-discipline-allow: allow_preauth -->
<!-- planner-discipline-allow: preauth_callable -->

## Artifacts this phase produces

`test_resolve_answers_none_when_no_row_exists` is new in `tests/unit/test_identities_crud.py`.
`IdentitiesDB.resolve` is the renamed-signature symbol this plan drives; the full list is in
48-01 § Artifacts this phase produces.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `get_identity` -> `IdentitiesDB.resolve` | the verified token values become two database rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-02-01 | Elevation of privilege | `crud/identities.py::resolve` | high | mitigate | the three rejection cases are re-run against the new signature, unchanged in every assertion |
| T-48-02-02 | Elevation of privilege | the deleted no-row rejection | high | mitigate | the rejection moves to `get_identity` and is pinned there; this suite proves `resolve` answers `None` rather than a usable value |
| T-48-02-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/unit/test_identities_crud.py tests/unit/test_exception_handlers.py` exits 0
- `grep -rn 'allow_preauth\|preauth_callable' tests/unit` prints nothing for these two files
</verification>

<success_criteria>
`resolve`'s three rejections and its new `None` answer are both proved by a run, not by prose.
</success_criteria>

<output>
Create `.planning/phases/48-narrow-identity-to-the-verified-pair/48-02-SUMMARY.md` when done.
</output>
