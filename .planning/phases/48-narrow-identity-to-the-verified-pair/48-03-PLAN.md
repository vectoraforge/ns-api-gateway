---
phase: 48-narrow-identity-to-the-verified-pair
plan: 03
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - tests/unit/test_challenge_endpoint.py
  - tests/unit/test_challenge_ids.py
  - tests/e2e/test_challenge_store.py
autonomous: true
requirements: []
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-06: the challenge route resolves the caller itself, after the operation check, and still refuses a caller with no row for every operation but create_user"
    - "D-08: issue and verify_binding bind to linked.identity.id when a row exists, and to claims otherwise"
    - "D-11: every test builds a VerifiedClaims or a LinkedIdentity"
  artifacts:
    - tests/unit/test_challenge_endpoint.py
    - tests/unit/test_challenge_ids.py
    - tests/e2e/test_challenge_store.py
  key_links:
    - "the operation check -> resolve: a body refusal must still issue no statement"
---

<objective>
Follow the challenge store's two new parameters in the three suites that drive it, and give the
challenge route's recording session a row.

Purpose: D-06, D-08 and D-11.
Output: the route suite proves the new resolve call, and the store suites pass both values.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-PATTERNS.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The challenge route suite resolves from its own session</name>
  <files>tests/unit/test_challenge_endpoint.py</files>
  <read_first>
    - tests/unit/test_challenge_endpoint.py
    - tests/unit/test_identities_crud.py:24-55
    - src/nativespeaker/api/routers/auth.py
  </read_first>
  <behavior>
    - A caller with no row is issued a create_user challenge
    - A caller with no row asking for any other operation answers 403 `preauth_identity_not_allowed`
    - A caller with a row is issued a challenge bound to that row
    - Every body-validation refusal issues no statement at all
  </behavior>
  <action>
`_RecordingSession` at `:50-64` answers an empty result for every statement, so after D-06 the
linked fixture reads as a caller with no row. Give it a row, copying `_StubSession(row)` and its
`(identity, user)` builder from `tests/unit/test_identities_crud.py:24-55`: the constructor takes
the row, `exec` appends the statement and returns a result carrying it. `linked_client` at `:104`
and `:235-245` overrides `get_claims` with a `VerifiedClaims` and supplies a session whose row is
the `(ExternalIdentity, User)` pair, rather than overriding the account dependency. The unlinked
client supplies a session with no row. The unlinked override at `:17, 22, 85, 97` keys on
`get_claims`. Keep the assertion at `:197-206` as it stands: a body refusal still issues no
statement, because the route resolves after the operation check.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_challenge_endpoint.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>test -z "$(grep -n 'get_linked_identity' tests/unit/test_challenge_endpoint.py)"</automated>
    <fails_when>non-zero exit, which means the deleted dependency name survives</fails_when>
  </verify>
  <acceptance_criteria>
    - The case at `:197-206` still asserts an empty statement list, and passes
    - A case proves a caller with a row is issued a challenge bound to `linked.identity.id`
    - A case proves a caller with no row is refused every operation but `create_user`
    - `.venv/bin/pytest -q tests/unit/test_challenge_endpoint.py` exits 0
  </acceptance_criteria>
  <done>The route's own resolve call is driven by the suite, in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: The challenge id suite passes both values</name>
  <files>tests/unit/test_challenge_ids.py</files>
  <read_first>
    - tests/unit/test_challenge_ids.py
    - src/nativespeaker/api/crud/challenges.py
  </read_first>
  <action>
`linked_identity()` and `preauth_identity()` at `:77-90` return a `LinkedIdentity` and a
`VerifiedClaims`. Every `issue(session, operation=..., ...)` call at `:93-100, 167, 220-294` and
every `verify_binding(row, ...)` call passes `claims=` and `linked=`, with `linked=None` for the
caller that has no row. No assertion about the issued handle, its entropy or its TTL changes.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_challenge_ids.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '\bAuthIdentity\b' tests/unit/test_challenge_ids.py` prints nothing
    - Both binding branches are still driven: one call passes a `LinkedIdentity`, one passes `None`
    - `.venv/bin/pytest -q tests/unit/test_challenge_ids.py` exits 0
  </acceptance_criteria>
  <done>Both binding branches are proved through the new signature.</done>
</task>

<task type="auto">
  <name>Task 3: The challenge store e2e suite passes both values</name>
  <files>tests/e2e/test_challenge_store.py</files>
  <precondition>PostgreSQL answers on localhost:5432; `-m e2e` collects rather than deselects.</precondition>
  <read_first>
    - tests/e2e/test_challenge_store.py
    - src/nativespeaker/api/crud/challenges.py
  </read_first>
  <action>
`preauth(...)` at `:16, 35-36` returns a `VerifiedClaims`. The three construction sites at
`:298, 311-312, 328-329` build a `LinkedIdentity` with both rows and no token-value keywords.
Every `issue` and `verify_binding` call passes `claims=` and `linked=`. The cross-presenter
cases at `:306-335` keep their assertions: a handle bound to one row must stay unspendable by
another caller.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m e2e tests/e2e/test_challenge_store.py</automated>
    <fails_when>non-zero exit, a summary line reporting any failed case, or a run that collects 0 cases</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '\bAuthIdentity\b' tests/e2e/test_challenge_store.py` prints nothing
    - The run collects more than 0 cases and exits 0
    - The cross-presenter rejection cases still pass
  </acceptance_criteria>
  <done>The store's binding rule is proved against a live database through the new signature.</done>
</task>

</tasks>

<!-- planner-discipline-allow: AuthIdentity -->
<!-- planner-discipline-allow: get_linked_identity -->

## Artifacts this phase produces

`linked_client` and the unlinked client fixtures are rewritten around `get_claims`;
`_RecordingSession` gains a row. The renamed source symbols this plan drives are
`ChallengesDB.issue` and `ChallengesDB.verify_binding`; the full list is in 48-01 § Artifacts
this phase produces.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| challenge presenter -> `ChallengesDB.verify_binding` | a secret handle is claimed by a presenter |
| client -> `routers/auth.py::issue_challenge` | a caller with no row asks for an operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-03-01 | Spoofing | `crud/challenges.py::verify_binding` | high | mitigate | the cross-presenter cases at `tests/e2e/test_challenge_store.py:306-335` are re-run against the new signature |
| T-48-03-02 | Elevation of privilege | `routers/auth.py::issue_challenge` | high | mitigate | a case proves a caller with no row is refused every operation but `create_user` |
| T-48-03-03 | Information disclosure | the handle in a log | medium | mitigate | no assertion about the handle's logging changes; the module holds no logger |
| T-48-03-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/unit/test_challenge_endpoint.py tests/unit/test_challenge_ids.py` exits 0
- `.venv/bin/pytest -q -m e2e tests/e2e/test_challenge_store.py` exits 0 and collects more than 0 cases
</verification>

<success_criteria>
The route resolves its own caller, and both binding branches of the store are driven by a run.
</success_criteria>

<output>
Create `.planning/phases/48-narrow-identity-to-the-verified-pair/48-03-SUMMARY.md` when done.
</output>
