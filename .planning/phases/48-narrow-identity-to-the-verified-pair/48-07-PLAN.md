---
phase: 48-narrow-identity-to-the-verified-pair
plan: 07
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - tests/schema/test_claim_race.py
  - tests/schema/test_create_atomicity.py
  - tests/schema/test_create_race.py
  - tests/schema/test_restore_race.py
autonomous: true
requirements: []
estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-02: the restore race builder carries both rows, because neither field has a default any more"
    - "D-03: no schema test passes the deleted flag"
    - "D-11: every schema test builds a VerifiedClaims or a LinkedIdentity"
  artifacts:
    - tests/schema/test_claim_race.py
    - tests/schema/test_create_atomicity.py
    - tests/schema/test_create_race.py
    - tests/schema/test_restore_race.py
  key_links:
    - "the two-connection races -> the new constructors: a race case must still commit exactly one row"
---

<objective>
Follow the two types in the four schema suites, and give the restore race builder the row it
never had.

Purpose: D-02, D-03 and D-11. RESEARCH names the restore race builder one of the four sites where
the rewrite is not a rename.
Output: four race suites that run against a live database and commit what they committed before.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-PATTERNS.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The claim race and the two create suites take the two types</name>
  <files>tests/schema/test_claim_race.py, tests/schema/test_create_atomicity.py, tests/schema/test_create_race.py</files>
  <precondition>PostgreSQL answers on localhost:5432; `-m schema` collects rather than deselects.</precondition>
  <read_first>
    - tests/schema/test_claim_race.py
    - tests/schema/test_create_atomicity.py
    - tests/schema/test_create_race.py
    - src/nativespeaker/api/crud/identities.py
  </read_first>
  <action>
`test_claim_race.py:246-251` calls `resolve` without the flag, and `:261-262` reads `linked.user`
and `linked.identity`. `test_create_atomicity.py:16, 69-71, 166` returns a `VerifiedClaims` from
`identity_for(...)`, and the attempt record's field takes the same type; the `create_user` and
`insert_account` calls pass `claims=`. `test_create_race.py:16, 144, 158` builds a
`VerifiedClaims` on the attempt record. Every assertion about what one connection commits and
what the loser writes stays as it is: these suites measure the database, not the types.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m schema tests/schema/test_claim_race.py tests/schema/test_create_atomicity.py tests/schema/test_create_race.py</automated>
    <fails_when>non-zero exit, a summary line reporting any failed case, or a run that collects 0 cases</fails_when>
    <automated>test -z "$(grep -rn 'allow_preauth' tests/schema)"</automated>
    <fails_when>non-zero exit, which means the deleted flag is still named in a schema suite</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn '\bAuthIdentity\b' tests/schema/test_claim_race.py tests/schema/test_create_atomicity.py tests/schema/test_create_race.py` prints nothing
    - The run collects more than 0 cases and exits 0
    - Every race case still asserts exactly one committed row
  </acceptance_criteria>
  <done>Three schema suites drive the new types against a live database.</done>
</task>

<task type="auto">
  <name>Task 2: The restore race builder carries both rows</name>
  <files>tests/schema/test_restore_race.py</files>
  <precondition>PostgreSQL answers on localhost:5432; `-m schema` collects rather than deselects.</precondition>
  <read_first>
    - tests/schema/test_restore_race.py:200-240
    - tests/unit/conftest.py:115-126
    - src/nativespeaker/api/schemas/auth.py
  </read_first>
  <action>
`identity_of(user_id)` at `:17, 220-222` builds the caller with a `User` and no second row, which
the two-field type refuses. Return a `LinkedIdentity` carrying both: the `User` it builds today,
and an `ExternalIdentity` whose `user_id` is that user's id, copying the field set from
`tests/unit/conftest.py:115-126`. The seeded row for that user is what the builder must mirror,
so read the seeding helper in this module before choosing `provider` and `provider_uid`. The two
racing connections and every count assertion stay as they are.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m schema tests/schema/test_restore_race.py</automated>
    <fails_when>non-zero exit, a summary line reporting any failed case, or a run that collects 0 cases</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '\bAuthIdentity\b' tests/schema/test_restore_race.py` prints nothing
    - The builder passes both `user=` and `identity=`
    - The run collects more than 0 cases and exits 0
  </acceptance_criteria>
  <done>The restore race builds a complete caller, and the race still commits exactly one grant.</done>
</task>

</tasks>

<!-- planner-discipline-allow: AuthIdentity -->
<!-- planner-discipline-allow: allow_preauth -->

## Artifacts this phase produces

`identity_of` in `tests/schema/test_restore_race.py` returns the two-field type; the renamed
source symbols these suites drive are `IdentitiesDB.resolve`, `IdentitiesDB.insert_account` and
`AuthService.create_user`. The full list is in 48-01 § Artifacts this phase produces.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two connections -> one row | a race decides which caller owns an account or a grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-07-01 | Tampering | the create and claim races | high | mitigate | each race case still asserts exactly one committed row, so a type change that loosened a lock would fail here |
| T-48-07-02 | Elevation of privilege | `resolve` inside a transaction | high | mitigate | the claim race reads both rows off the returned value, which now cannot be absent |
| T-48-07-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package |
</threat_model>

<verification>
- `.venv/bin/pytest -q -m schema tests/schema/test_claim_race.py tests/schema/test_create_atomicity.py tests/schema/test_create_race.py tests/schema/test_restore_race.py` exits 0 and collects more than 0 cases
- `grep -rn 'allow_preauth' tests/schema` prints nothing
</verification>

<success_criteria>
All four schema suites run against a live database and commit exactly what they committed before
the phase.
</success_criteria>

<output>
Create `.planning/phases/48-narrow-identity-to-the-verified-pair/48-07-SUMMARY.md` when done.
</output>
