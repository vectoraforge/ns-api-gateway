---
phase: 48-narrow-identity-to-the-verified-pair
plan: 05
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - tests/unit/test_create_user_body.py
  - tests/unit/test_create_user_precedence.py
  - tests/unit/test_create_user_rollback.py
  - tests/unit/test_conflict_classification.py
autonomous: true
requirements: []
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-07: create-user declares the token dependency only, and the suites override that name"
    - "D-08: create_user and the two apply methods take claims"
    - "D-11: every test builds a VerifiedClaims, and none passes the deleted flag"
    - "Option B: the completion path issues exactly one identity statement, and every rejection keeps its status, code and place in the precedence"
  artifacts:
    - tests/unit/test_create_user_precedence.py
    - tests/unit/test_create_user_rollback.py
    - tests/unit/test_conflict_classification.py
  key_links:
    - "AuthService.complete -> IdentitiesDB.resolve -> ChallengesDB.verify_binding: the one statement the completion path now issues"
---

<objective>
Follow `claims` through the four create-user suites, and re-pin the statement count the
completion path now issues.

Purpose: D-07, D-08 and D-11, and option B from plan 48-01 Task 1.
Output: the create-user precedence is unchanged and measured.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-PLAN.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The precedence suite answers the completion path's one statement</name>
  <files>tests/unit/test_create_user_precedence.py</files>
  <read_first>
    - tests/unit/test_create_user_precedence.py
    - src/nativespeaker/api/services/auth.py
    - src/nativespeaker/api/crud/identities.py
  </read_first>
  <behavior>
    - The completion path issues exactly one statement, the identity query
    - A caller with no row keeps every rejection it has today, in the same order
    - The five challenge rejections and the provider-stage rejections keep their status and code
  </behavior>
  <action>
Under option B, `AuthService.complete` resolves the caller before the sequence runs, so
`_StubSession.exec` at `:82-84` may no longer raise. Make it a recorder: it appends the statement
to a list and returns a result whose `first()` answers `None`, which is the caller with no row
every case in this module uses. Replace the guard it deleted with a stronger one: a case
asserting the completion path issues exactly one statement, so a second query is still a
failure. The fixture at `:131-132` returns a `VerifiedClaims`, and the override at `:141` keys on
`get_claims`. `AuthService.create_user` is monkeypatched at `:127-128`, so its recorder now
receives `claims=`. Every rejection case keeps its status, code, event name and place in the
precedence.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_create_user_precedence.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>test -z "$(grep -n 'get_linked_identity\|\bAuthIdentity\b' tests/unit/test_create_user_precedence.py)"</automated>
    <fails_when>non-zero exit, which means a deleted name survives in the suite</fails_when>
  </verify>
  <acceptance_criteria>
    - A case asserts the completion path issues exactly one statement
    - The suite still drives all five challenge rejections and all four provider-stage rejections
    - `grep -c 'dependency_overrides\[get_claims\]' tests/unit/test_create_user_precedence.py` prints 1
    - `.venv/bin/pytest -q tests/unit/test_create_user_precedence.py` exits 0
  </acceptance_criteria>
  <done>The precedence is unchanged, and the new statement is counted rather than forbidden.</done>
</task>

<task type="auto">
  <name>Task 2: The body suite and the rollback suite take claims</name>
  <files>tests/unit/test_create_user_body.py, tests/unit/test_create_user_rollback.py</files>
  <read_first>
    - tests/unit/test_create_user_body.py
    - tests/unit/test_create_user_rollback.py
    - src/nativespeaker/api/services/auth.py
  </read_first>
  <action>
`test_create_user_body.py:13, 17, 82-83` builds a `VerifiedClaims` and overrides `get_claims`.
Its `_UnlinkedSession` already answers an empty result, so the completion path's identity query
reads as a caller with no row and the 422 partition is unchanged.
`test_create_user_rollback.py:10, 71-72, 78` builds a `VerifiedClaims` and calls
`service.create_user(claims=...)`. Both suites keep every assertion about what is written and
what is rolled back.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_create_user_body.py tests/unit/test_create_user_rollback.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '\bAuthIdentity\b' tests/unit/test_create_user_body.py tests/unit/test_create_user_rollback.py` prints nothing
    - `grep -c 'claims=' tests/unit/test_create_user_rollback.py` prints 1 or more
    - `.venv/bin/pytest -q tests/unit/test_create_user_body.py tests/unit/test_create_user_rollback.py` exits 0
  </acceptance_criteria>
  <done>The 422 partition and the rollback arms run through the new keyword.</done>
</task>

<task type="auto">
  <name>Task 3: The conflict classification suite takes claims</name>
  <files>tests/unit/test_conflict_classification.py</files>
  <read_first>
    - tests/unit/test_conflict_classification.py
    - src/nativespeaker/api/crud/identities.py
  </read_first>
  <action>
`_identity()` at `:21, 111-112` returns a `VerifiedClaims` for a caller with no row, and every
`create_user` and `insert_account` call passes `claims=`. The SQLSTATE mapping cases keep every
class, status and code they assert today, including the 409 arm.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_conflict_classification.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '\bAuthIdentity\b' tests/unit/test_conflict_classification.py` prints nothing
    - Every conflict class the suite asserted before the phase is still asserted
    - `.venv/bin/pytest -q tests/unit/test_conflict_classification.py` exits 0
  </acceptance_criteria>
  <done>The conflict classification is unchanged under the new keyword.</done>
</task>

</tasks>

<!-- planner-discipline-allow: AuthIdentity -->
<!-- planner-discipline-allow: get_linked_identity -->

## Artifacts this phase produces

`_StubSession` becomes a recorder in `tests/unit/test_create_user_precedence.py`, and the
`claims` fixture replaces the old identity fixture in all four suites. The renamed source symbols
this plan drives are `AuthService.complete`, `AuthService.create_user` and
`IdentitiesDB.insert_account`; the full list is in 48-01 § Artifacts this phase produces.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `POST /auth/create-user` | a caller with no account spends a challenge and is given one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-05-01 | Elevation of privilege | `AuthService.create_user` | high | mitigate | `_reject_existing_identity` keeps all three arms, and the suites assert each one |
| T-48-05-02 | Spoofing | the completion path's statement count | medium | mitigate | the deleted guard is replaced by an exact count, so a second unplanned query is still a failure |
| T-48-05-03 | Information disclosure | the handle in a log | medium | mitigate | the two cases asserting the handle reaches neither logger are kept |
| T-48-05-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package |
</threat_model>

<verification>
- `.venv/bin/pytest -q tests/unit/test_create_user_body.py tests/unit/test_create_user_precedence.py tests/unit/test_create_user_rollback.py tests/unit/test_conflict_classification.py` exits 0
- A case pins the completion path's statement count at exactly one
</verification>

<success_criteria>
Create-user's rejections, their order and their codes are unchanged, and the one new statement is
measured.
</success_criteria>

<output>
Create `.planning/phases/48-narrow-identity-to-the-verified-pair/48-05-SUMMARY.md` when done.
</output>
