---
phase: 48-narrow-identity-to-the-verified-pair
plan: 08
type: execute
wave: 3
depends_on: ["48-01", "48-02", "48-03", "48-04", "48-05", "48-06", "48-07"]
files_modified:
  - .planning/WINDOWS.md
autonomous: true
requirements: []
estimate:
  tokens: 24000
  raw_tokens: 48000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Criterion 1: the deleted class is named nowhere in src or tests, and LinkedIdentity has exactly two required fields and no base class"
    - "Criterion 4: no test passes the deleted flag and none asserts a None row field"
    - "Criterion 5: the three suites are re-run rather than copied, and the only failing case is the pre-existing restore case"
    - "D-09: the ROADMAP entry was amended in the discuss commit; verified here, no edit"
    - "D-07: what actually shipped is recorded, including the planning decision that answered it"
  artifacts:
    - .planning/WINDOWS.md
  key_links:
    - "the four criterion greps -> the three suites: a green suite with a surviving name is not a pass"
---

<objective>
Measure the phase gate, and record what shipped.

Purpose: ROADMAP criteria 1, 4 and 5, and the records D-07 and D-09 ask for.
Output: three measured suite runs, the four criterion greps, and one `WINDOWS.md` entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-CONTEXT.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md
@.planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The four criterion greps</name>
  <files>none — this task measures and writes nothing but the summary</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 48 entry, criteria 1 to 5)
    - .planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md (§ Common Pitfalls, pitfall 1)
    - src/nativespeaker/api/schemas/auth.py
  </read_first>
  <action>
Run the four greps and record each result verbatim in the summary. The word-boundary form is
required: a plain search also matches the rejection class name, which stays in `errors.py` and is
raised from two places. Confirm that `src/nativespeaker/api/auth/google_play.py:240` and
`tests/unit/test_google_play_notifications.py:843` need no edit, because the name their
docstrings carry still exists. Confirm D-09: the Phase 48 entry already carries the amended goal
and criteria from the discuss commit, and `REQUIREMENTS.md` maps nothing to this phase and is not
edited.
  </action>
  <verify>
    <automated>test -z "$(grep -rn '\bAuthIdentity\b' src tests)"</automated>
    <fails_when>non-zero exit, which means a module still names the deleted class</fails_when>
    <automated>test -z "$(grep -rn 'get_linked_identity\|allow_preauth\|preauth_callable' src tests)"</automated>
    <fails_when>non-zero exit, which means a deleted dependency name or the deleted flag survives</fails_when>
    <automated>test "$(grep -rln 'LinkedIdentity | None' src | sort | tr '\n' ' ')" = "src/nativespeaker/api/crud/challenges.py src/nativespeaker/api/services/auth.py "</automated>
    <fails_when>non-zero exit, which means a third src file declares the optional</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_identity_accessors.py tests/unit/test_app_wiring.py</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored case</fails_when>
  </verify>
  <acceptance_criteria>
    - All four commands exit 0, and each result is quoted in the summary
    - The summary names the two files whose docstrings need no edit
    - The summary states that `REQUIREMENTS.md` was read and not edited
  </acceptance_criteria>
  <done>Criteria 1, 3 and 4 are measured by command, not asserted in prose.</done>
</task>

<task type="auto">
  <name>Task 2: The three suites, ruff and ty</name>
  <files>none — this task measures and writes nothing but the summary</files>
  <precondition>PostgreSQL answers on localhost:5432; `-m e2e` and `-m schema` collect rather than deselect.</precondition>
  <read_first>
    - .planning/phases/48-narrow-identity-to-the-verified-pair/48-VALIDATION.md
    - .planning/phases/48-narrow-identity-to-the-verified-pair/48-RESEARCH.md (§ Baseline)
  </read_first>
  <action>
Run each command in this task and quote its own output. Do not copy a count from any earlier
document. The baseline at HEAD `937864c` is 2579 passed and 1 failed for the bare marker, 360
passed and 1 failed for e2e, 297 passed for schema, `ruff` clean, and `ty` at 311 diagnostics.

Criterion 5 is met when the failing set is exactly one case,
`tests/e2e/test_restore_subscription.py::TestEveryRejectedProofOfBothStoresAnswersOneBody::test_the_four_arms_answer_bodies_equal_to_each_other_and_write_nothing`,
which is pre-existing and is recorded in STATE.md and in 47-VERIFICATION.md. Compare the short
test summary, not the exit code: both the bare marker and e2e exit 1 on that one case today. Any
second failing case is this phase's and must be fixed at its root, not worked around. `ty` is a
control: the count must not rise above 311. If it rises, name the new diagnostic and its file in
the summary.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>any failing case other than the named pre-existing restore case appears in the short test summary, or the run collects fewer than 2570 cases</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>any failing case other than the named pre-existing restore case appears in the short test summary, or the run collects 0 cases</fails_when>
    <automated>.venv/bin/pytest -q -m schema</automated>
    <fails_when>non-zero exit, or the run collects 0 cases</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>any line other than "All checks passed!", or non-zero exit</fails_when>
    <automated>.venv/bin/ty check</automated>
    <fails_when>a diagnostic count above 311</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the five commands was run in this task, and its output is quoted in the summary
    - The bare-marker and e2e runs report exactly one failing case, and it is the named restore case
    - The schema run exits 0
    - `ruff` prints "All checks passed!"
    - `ty` reads 311 diagnostics or fewer
  </acceptance_criteria>
  <done>Criterion 5 is measured at this phase's HEAD, with the one pre-existing failure named.</done>
</task>

<task type="auto">
  <name>Task 3: Record what shipped</name>
  <files>.planning/WINDOWS.md</files>
  <reversibility rating="reversible">The create-user ordering and the `_complete` signature are both a code shape. A later phase can move the resolve call or split the method without a migration or a client change.</reversibility>
  <read_first>
    - .planning/WINDOWS.md (the last entry, for the id and the field set)
    - .planning/phases/48-narrow-identity-to-the-verified-pair/48-01-SUMMARY.md
    - .planning/ROADMAP.md (the Phase 48 entry, criterion 3)
  </read_first>
  <action>
Write entry 33 in both places `.planning/WINDOWS.md` carries an entry, as every prior entry
commit does: one row at the end of the markdown table, and one object at the end of the JSON
array, with the same field set the last entry carries. Its `kind` is `deviation`, its `phase` is
`48`, its `file` is `src/nativespeaker/api/services/auth.py`, and its `status` is `waived`, which
is this file's word for accepted rather than fixed. In the same edit raise `waived_count` in the
frontmatter from 1 to 2 and `total_count` from 32 to 33, and set `last_updated` to the commit's
UTC time, in the format the current value carries. `open_count` reads 20 and `fixed_count` reads
11, and neither moves: the three add up to `total_count`. The description
states three facts: D-07 gives create-user the token dependency only; planning found that a
caller with an active row presents a challenge bound to that row, which the completion path can
no longer confirm, so `ChallengesDB.verify_binding` would answer 409 `challenge_required` where
the route answers 409 `identity_already_linked` today; and the developer chose the option
recorded in `48-01-SUMMARY.md`. Under option B the reason field states that every wire outcome is
preserved, that the completion path issues one identity statement where the router-level
dependency issued it before, and that D-07's accepted cost of one spent challenge does not ship.
Record in the same entry that ROADMAP criterion 3 was amended on 2026-09-16 at plan time to name
`AuthService._complete` beside `crud/challenges.py`, because `_complete` serves create-user,
which has no row, and the three claim routes, which have one.

Do not edit `REQUIREMENTS.md`: nothing maps to this phase. Do not edit the ROADMAP goal or the
five criteria: D-09 amended them in the discuss commit and planning amended criterion 3.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json,re,pathlib; json.loads(re.search(r'\[[\s\S]*\]', pathlib.Path('.planning/WINDOWS.md').read_text()).group(0))"</automated>
    <fails_when>non-zero exit, which means the appended entry left the array unparseable</fails_when>
    <automated>test "$(grep -c '"phase": "48"' .planning/WINDOWS.md)" = "1"</automated>
    <fails_when>non-zero exit, which means no entry or more than one entry was appended</fails_when>
    <automated>test "$(grep -c 'waived_count: 2' .planning/WINDOWS.md)" = "1"</automated>
    <fails_when>non-zero exit, which means the counter was not raised with the entry</fails_when>
    <automated>test "$(grep -c 'total_count: 33' .planning/WINDOWS.md)" = "1"</automated>
    <fails_when>non-zero exit, which means the entry total still reads the count before the entry</fails_when>
    <automated>test "$(grep -c '^| 33 | 48 |' .planning/WINDOWS.md)" = "1"</automated>
    <fails_when>non-zero exit, which means the markdown table did not get the row the JSON array got</fails_when>
    <automated>git -C . diff --name-only HEAD -- .planning/REQUIREMENTS.md</automated>
    <fails_when>any file name in the output, which means the requirements file was edited</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` parses as JSON after the code fence is stripped
    - Exactly one entry carries `"phase": "48"`, and its `status` is `waived`
    - The markdown table carries the same entry as one row, id 33
    - The frontmatter reads `waived_count: 2` and `total_count: 33`, and `last_updated` carries the commit date
    - Its description names the option the developer chose
    - `.planning/REQUIREMENTS.md` is unchanged
  </acceptance_criteria>
  <done>The one behavior question this phase raised is recorded with the answer it was given.</done>
</task>

</tasks>

<!-- planner-discipline-allow: AuthIdentity -->
<!-- planner-discipline-allow: get_linked_identity -->
<!-- planner-discipline-allow: allow_preauth -->
<!-- planner-discipline-allow: preauth_callable -->

## Artifacts this phase produces

This plan writes no source. It measures the symbols listed in 48-01 § Artifacts this phase
produces, and appends one `WINDOWS.md` entry.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase gate -> the record | a copied count would hide a regression the phase caused |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-08-01 | Repudiation | the gate counts | high | mitigate | every command is run in this task and its output quoted; a copied number is a failure of the task |
| T-48-08-02 | Elevation of privilege | a surviving deleted name | high | mitigate | the four greps run over `src` and `tests` together, and the word-boundary form is required |
| T-48-08-03 | Denial of service | an all-deselected run | medium | mitigate | `addopts` deselects e2e and schema, so each run's collected count is checked against 0 |
| T-48-08-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package |
</threat_model>

<verification>
- The five gate commands were run in Task 2 and their output is quoted
- The four criterion greps exit 0
- `.planning/WINDOWS.md` parses, and carries exactly one Phase 48 entry
</verification>

<success_criteria>
The three suites, `ruff` and `ty` are measured at this phase's HEAD. The only failing case is the
pre-existing restore case. The one behavior question is recorded with its answer.
</success_criteria>

<output>
Create `.planning/phases/48-narrow-identity-to-the-verified-pair/48-08-SUMMARY.md` when done.
</output>
