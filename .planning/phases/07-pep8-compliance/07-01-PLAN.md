---
phase: 07-pep8-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/routers/__init__.py
  - app/routers/prompts.py
  - tests/unit/test_exception_handlers.py
autonomous: true
requirements: [STYLE-01]

must_haves:
  truths:
    - "`ruff check .` exits 0 with zero violations"
    - "`ruff format --check .` exits 0 with no formatting changes needed"
    - "`ruff` and `ty` are in `[dependency-groups] dev`, not in `[project] dependencies`"
    - "All tests pass after formatting changes"
  artifacts:
    - path: "pyproject.toml"
      provides: "ruff configuration and corrected dependency groups"
      contains: "[tool.ruff]"
  key_links:
    - from: "pyproject.toml"
      to: "ruff check / ruff format"
      via: "[tool.ruff] and [tool.ruff.lint] config sections"
      pattern: "select.*E.*W.*F.*I.*UP"
---

<objective>
Add ruff configuration, fix all lint violations, format the codebase, and move dev tools to the correct dependency group.

Purpose: Enforce consistent PEP8-compliant code style across the codebase with automated tooling.
Output: Zero-violation `ruff check` and `ruff format --check`, dev tools properly separated from runtime dependencies.
</objective>

<execution_context>
@/Users/otto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/otto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pep8-compliance/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ruff config, fix manual violations, and run auto-fix + format</name>
  <files>pyproject.toml, app/routers/__init__.py, app/routers/prompts.py, tests/unit/test_exception_handlers.py</files>
  <action>
**Step 1 — Add `[tool.ruff]` config to `pyproject.toml`:**

Append these sections at the end of `pyproject.toml`:

```toml
[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]
```

**Step 2 — Fix 6 manual violations BEFORE running auto-fix:**

2a. `app/routers/__init__.py` — Fix F401 re-export warnings (4 violations). Add `__all__` to declare intentional re-exports:

```python
__all__ = ["chats_router", "health_router", "prompts_router", "root_router"]

from routers.health import router as health_router
from routers import chats_router
from routers import router as prompts_router
from routers.root import router as root_router
```

2b. `app/routers/prompts.py:82` — Fix F841 unused variable (1 violation). The `chat = await service.chats.get_chat_owned(...)` call is made for its side effect (raises `ChatOwnershipError`). Remove the variable assignment but keep the call:

```python
# Before:
chat = await service.chats.get_chat_owned(db, chat_id, user_id)

# After:
await service.chats.get_chat_owned(db, chat_id, user_id)
```

2c. `tests/unit/test_exception_handlers.py` — Fix E402 mid-file import (1 violation). Move the `from app.auth import UnsafeBase64Verifier, get_user_id` import from line ~94 to the top of the file alongside other imports. The import has no dependency on the `CASES` list or fixtures.

**Step 3 — Run `ruff check --fix .`** to auto-fix the remaining ~30 violations (I001 import sorting, F401 unused imports, W292 missing newline, UP035 deprecated imports).

**Step 4 — Run `ruff format .`** to reformat all files to consistent style. This MUST run after `--fix` because import sorting changes line content that the formatter then wraps.

**Step 5 — Verify zero violations:**

```bash
ruff check .
ruff format --check .
```

Both commands must exit 0.
  </action>
  <verify>
    <automated>cd /Users/otto/Work/nativespeaker/sn-api-gateway && ruff check . && ruff format --check .</automated>
  </verify>
  <done>`ruff check .` exits 0 (no lint violations) and `ruff format --check .` exits 0 (no formatting changes needed). `pyproject.toml` has `[tool.ruff]` with `line-length = 120`, `target-version = "py312"`, and `select = ["E", "W", "F", "I", "UP"]`.</done>
</task>

<task type="auto">
  <name>Task 2: Move ruff/ty to dev dependencies, regenerate lock file, run tests</name>
  <files>pyproject.toml</files>
  <action>
**Step 1 — Move `ruff` and `ty` from `[project] dependencies` to `[dependency-groups] dev`:**

Remove these two lines from the `[project] dependencies` list:
```
    "ruff>=0.15.2",
    "ty>=0.0.17",
```

Add them to `[dependency-groups] dev`:
```
    "ruff >=0.15.2",
    "ty >=0.0.17",
```

Note the space before `>=` to match the existing style in the dev group.

**Step 2 — Regenerate the lock file:**

```bash
uv lock
```

**Step 3 — Run the full test suite:**

```bash
python -m pytest tests/ -x
```

All tests must pass. If any fail, diagnose whether the failure is caused by formatting changes (check the specific assertion) and fix.
  </action>
  <verify>
    <automated>cd /Users/otto/Work/nativespeaker/sn-api-gateway && python -m pytest tests/ -x</automated>
  </verify>
  <done>`ruff` and `ty` appear only in `[dependency-groups] dev`, not in `[project] dependencies`. `uv.lock` is regenerated. All tests pass.</done>
</task>

</tasks>

<verification>
1. `ruff check .` — exits 0, no violations
2. `ruff format --check .` — exits 0, no formatting changes needed
3. `grep -A5 '\[tool.ruff\]' pyproject.toml` — shows `line-length = 120` and `target-version = "py312"`
4. `grep -A3 '\[tool.ruff.lint\]' pyproject.toml` — shows `select = ["E", "W", "F", "I", "UP"]`
5. `ruff` and `ty` NOT in `[project] dependencies` section
6. `ruff` and `ty` IN `[dependency-groups] dev` section
7. `python -m pytest tests/ -x` — all tests pass
</verification>

<success_criteria>
- `ruff check .` exits 0
- `ruff format --check .` exits 0
- `pyproject.toml` contains `[tool.ruff]` with rules E, W, F, I, UP and line-length=120
- `ruff` and `ty` listed under `[dependency-groups] dev`, not under `[project] dependencies`
- All tests pass after formatting changes
</success_criteria>

<output>
After completion, create `.planning/phases/07-pep8-compliance/07-01-SUMMARY.md`
</output>
