---
phase: 37.1-refactor-machine-generated-code
plan: 08
type: execute
wave: 3
depends_on: ["37.1-01", "37.1-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-12, D-11, D-02, D-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "FOUND-03 is deleted from REQUIREMENTS.md — the route registry and the enumeration assertion it requires no longer exist"
    - "FOUND-01 and FOUND-02 keep their substance and name the router-level dependency instead of the barrier"
    - "SCHEMA-06 and FOUND-05 are marked as withdrawn with the D-01 deletion named as the cause, not silently edited away"
    - "SYNC-03 and SIGNOUT-02 are marked as requiring a mechanism this phase deleted, so phases 38 and 46 decide for themselves rather than inheriting a broken requirement"
    - "Both conflicts against SHARED-INVARIANTS.md are recorded as flagged, not resolved: its §4 audit obligation against D-01, and its pre-handler-barrier rule against D-06"
    - "APPLEHOOK-02 and PLAYHOOK-03 are flagged forward to phases 43 and 44 as those phases' own design problem, explicitly not redesigned here"
    - "Every surviving reference to the audit trail, the barrier or the route registry in ROADMAP.md sits either inside a historical plan checkbox line or inside the Phase 37.1 section"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "REQUIREMENTS.md FOUND-01 -> SHARED-INVARIANTS.md's pre-handler-barrier rule: the requirement is amended, the binding spec is not, so the divergence must be visible in the requirement text"
    - "REQUIREMENTS.md SYNC-03 and SIGNOUT-02 -> phases 38 and 46: both require an audit row from a subsystem that no longer exists, and both phases are unbuilt"
  prohibitions:
    - statement: "No conflict against SHARED-INVARIANTS.md may be silently resolved. Both must be recorded as flagged, following the precedent set in 37-CONTEXT.md and in SCHEMA-01's amendment note."
      status: required
      verification: "REQUIREMENTS.md contains an explicit flagged-conflict note naming SHARED-INVARIANTS.md for each of D-01 and D-06"
    - statement: "The provider-callback category for phases 43 and 44 must not be redesigned here. APPLEHOOK-02 and PLAYHOOK-03 are flagged forward only."
      status: required
      verification: "The amended APPLEHOOK-02 and PLAYHOOK-03 text names the loss and defers the replacement to its own phase, and no new requirement describing a replacement mechanism is added"
    - statement: "ROADMAP.md line 190's historical plan checkbox (`35-09-PLAN.md — audit.auth_events writer`) and every other completed-plan checkbox must be left alone. They record what was built, and rewriting history is not an amendment."
      status: required
      verification: "git diff .planning/ROADMAP.md shows no change to any line matching '^- \\[x\\] [0-9]'"
    - statement: "audit.subscription_events and the audit schema survive the deletion, so any requirement text about them must not be amended away with the auth-event text."
      status: required
      verification: "grep -n 'subscription_events' .planning/REQUIREMENTS.md .planning/ROADMAP.md still resolves to statements that are true after this phase"
    - statement: "No requirement may be deleted merely because it became inconvenient. Only FOUND-03 is deleted, because the thing it requires no longer exists; every other affected requirement is amended, withdrawn with a reason, or flagged forward."
      status: required
      verification: "The count of requirement ids in REQUIREMENTS.md drops by exactly one, and that one is FOUND-03"
---

<objective>
Reconcile `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` with the two deletions (D-12).

Purpose: deleting a subsystem strands every criterion that names it. Two shipped requirements describe an audit trail that no longer exists, two unbuilt phases *require* a row from it, one requirement describes a route registry that is gone, and nine roadmap success criteria name a barrier. Left alone, the next phase to read these documents builds against a system that is not there.

The standing rule from 37-CONTEXT.md applies: **conflicts against `SHARED-INVARIANTS.md` are flagged, never silently resolved.** That document binds every phase and wins over any phase brief; this phase contradicts it twice, and the contradiction must be visible in the requirement text rather than smoothed away.

Output: two planning documents that describe the system as it now is, with every conflict and every forward-flagged loss recorded.

## How to find the work

**Line numbers drift.** CONTEXT.md's citations were verified against the working copy on 2026-08-24, and this planner has since written the Phase 37.1 plan list into `ROADMAP.md`, shifting everything below it. Locate by content, not by line number:

```
grep -niE 'audit\.auth_events|audit row|auth_events|barrier|route registry|enumeration|registry' .planning/REQUIREMENTS.md
grep -niE 'audit\.auth_events|audit row|auth_events|barrier|route registry|enumeration assertion|registry' .planning/ROADMAP.md
```

Those two greps are the work list. At the start they return 13 and 20 hits respectively. At the end, every remaining hit in `ROADMAP.md` must sit either inside a completed-plan checkbox line (`- [x] NN-NN-PLAN.md — …`) or inside the Phase 37.1 section, and every remaining hit in `REQUIREMENTS.md` must be a statement that is true of the system after this phase — including the amendment notes this plan writes, which legitimately name what was deleted.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.1-refactor-machine-generated-code/37.1-CONTEXT.md
@.planning/phases/37.1-refactor-machine-generated-code/37.1-01-SUMMARY.md
@.planning/phases/37.1-refactor-machine-generated-code/37.1-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Produced by this plan

Amendment notes in `.planning/REQUIREMENTS.md`, in the same form SCHEMA-01's existing note uses: a blockquote under the requirement naming what changed, when, why, and what the consequence is for later phases.

## Deleted by this plan

- **FOUND-03** — the only requirement deleted. It requires a route registry placing every registered route in exactly one of three categories plus a startup enumeration assertion; D-06 deletes both. Nothing survives of it to amend.

No other requirement id is removed.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: amend REQUIREMENTS.md — thirteen references, one deletion, two flagged conflicts</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the whole file; SCHEMA-01's existing amendment note is the exact form to copy
    - `.planning/phases/37.1-refactor-machine-generated-code/37.1-CONTEXT.md` — the `<decisions>` block, D-11 and D-12 in particular
    - `.planning/phases/37.1-refactor-machine-generated-code/37.1-01-SUMMARY.md` and `37.1-02-SUMMARY.md` — what actually shipped, which may differ in detail from what was planned
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` — §4 (the audit obligation) and its pre-handler-barrier rule; read only those two sections
  </read_first>
  <action>
Work through the grep hit list. Each requirement gets one of four treatments, and which one is not a judgement call:

**Deleted — FOUND-03 only.** It requires a route registry and a startup enumeration assertion. D-06 deletes both, and nothing of the requirement survives. Remove the requirement line entirely and leave a one-line note in its place recording that it was withdrawn by this phase and why, so a reader of the FOUND block does not wonder where 03 went.

**Amended, substance unchanged — FOUND-01, FOUND-02.** FOUND-01 requires "a mandatory, default-on pre-handler barrier [as] the only place JWT acceptance and identity resolution happen". Every property still holds of a router-level dependency: it is mandatory, it is default-on, it runs before the handler, and it is the only place resolution happens. Reword it to name the dependency and keep the `identity_state='active'` AND `users.active` exactly TRUE admission rule verbatim. FOUND-02's wire contract is untouched; reword only the word "barrier". Also amend the note about the startup assertion in the FOUND preamble.

**Withdrawn with a reason — SCHEMA-06, FOUND-05, and the audit halves of REBIND-02, PROF-02 and FOUND-09.** These describe an audit trail that no longer exists. Do not silently edit the sentence into something true; mark the requirement withdrawn, name D-01 as the cause, and state the consequence. `SCHEMA-06` also names `core.auth_operation`, which survives — say so, rather than withdrawing the whole line as if both enums had gone. REBIND-02's substantive half is that these routes write no audit row ever and keep their rejection in the structured security log; that half became *trivially* true rather than false, and D-03 keeps `record_rejection` — say that too.

**Flagged forward, not redesigned here — SYNC-03, SIGNOUT-02, APPLEHOOK-02, PLAYHOOK-03.** SYNC-03 (Phase 38) and SIGNOUT-02 (Phase 46) each *require* an `audit.auth_events` row from a phase that has not been built, and the mechanism is gone. Mark both as blocked on a decision their own phase must make: either rebuild a durable record or drop the obligation. Do not decide it here. APPLEHOOK-02 and PLAYHOOK-03 both require enumeration in the closed provider-callback category, whose machinery D-06 deleted before either phase exists; record the loss and note that the natural replacement is a dedicated router carrying a named-verifier dependency — as a pointer for those phases, explicitly not as a design this phase adopts.

**Also amend REBIND-01**'s enumeration half. Its partition-membership half is satisfied structurally now: the router a route is registered on *is* its declaration, so the two cannot disagree.

**Both SHARED-INVARIANTS conflicts get an explicit flagged-conflict note**, in SCHEMA-01's blockquote form: one under FOUND-05 for D-01 against §4's audit obligation, one under FOUND-01 for D-06 against the pre-handler-barrier rule. Each note states that the binding specification is unamended and that this project has knowingly diverged, so a later reader finds the divergence rather than discovering it.
  </action>
  <verify>
    <automated>grep -c "FOUND-03" .planning/REQUIREMENTS.md returns 1 or fewer, and every remaining audit or barrier reference resolves to a statement true after this phase</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\- \[[ x]\] \*\*FOUND-03\*\*' .planning/REQUIREMENTS.md` returns 0 — the requirement is gone from the list
    - The FOUND block contains a one-line withdrawal note for FOUND-03 naming this phase
    - `grep -c 'SHARED-INVARIANTS' .planning/REQUIREMENTS.md` returns 2 or more, and both hits are flagged-conflict notes attached to FOUND-01 and FOUND-05
    - The count of lines matching `^\- \[[ x]\] \*\*[A-Z]+-[0-9]+\*\*` drops by exactly 1 from before the task
    - `grep -nE 'SYNC-03|SIGNOUT-02|APPLEHOOK-02|PLAYHOOK-03' .planning/REQUIREMENTS.md` shows each of the four carrying an explicit forward-flag note naming its owning phase
    - `grep -n 'auth_operation' .planning/REQUIREMENTS.md` shows SCHEMA-06's note distinguishing the enum that survives from the one that was dropped
    - No line matching `^\- \[x\] \*\*SCHEMA-0[1-5]\*\*` or `^\- \[x\] \*\*CREATE-0[1-4]\*\*` is modified
  </acceptance_criteria>
  <done>REQUIREMENTS.md describes the system as it now is: FOUND-03 gone with a note, FOUND-01 and FOUND-02 reworded with their substance intact, five requirements withdrawn with reasons, four flagged forward to their own phases, and both SHARED-INVARIANTS conflicts recorded as flagged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: amend ROADMAP.md — fifteen live references, five historical checkboxes left alone</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — the Phase 35, 36, 37.1, 38, 39, 43 and 46 sections; the whole file is long, so read the sections the grep hits land in
    - `.planning/phases/37.1-refactor-machine-generated-code/37.1-CONTEXT.md` D-12's ROADMAP list
    - `.planning/REQUIREMENTS.md` as Task 1 left it — the roadmap must agree with it
  </read_first>
  <action>
Run the grep from the objective and work the hit list. Two hits are already correct and need no change beyond confirmation: the two success criteria asserting that *no* audit row is ever written by a route (Phase 36's and Phase 39's) became trivially true rather than false.

**Leave alone entirely:** every completed-plan checkbox line, `- [x] 35-09-PLAN.md — audit.auth_events writer …` included. Those record what was built. Rewriting them is not an amendment, it is falsifying history.

**Leave alone:** the Phase 37.1 section, whose goal line and plan list correctly describe this phase's own work.

**Amend the Phase 35 and Phase 36 success criteria** that name the barrier, the route registry or the enumeration assertion: the criteria that a route in zero or two categories fails the assertion, that the assertion executes at real startup, that the barrier admits only active-and-active, that a barrier rejection produces an audit row, and that the registry cross-check enforces the quota flag. Each is either restated against the dependency, marked as satisfied structurally, or withdrawn with the phase that withdrew it named. The admission rule itself survives everywhere it appears; only the noun changes.

**Amend the Phase 38, 39, 43 and 46 criteria** that require or forbid an audit row, in step with what Task 1 did to the matching requirement. Where Task 1 flagged a requirement forward, the roadmap criterion says the same thing in the same words — the two documents must not disagree.

**Amend the Phase 35 goal line and the Phase 36 goal line** where they enumerate the shared machinery: the barrier, the route registry and the audit writer are no longer part of what this system contains.

Update nothing about phases 40, 41, 42 or 45, which the grep does not reach.
  </action>
  <verify>
    <automated>Every remaining grep hit in .planning/ROADMAP.md sits inside a completed-plan checkbox line or inside the Phase 37.1 section</automated>
  </verify>
  <acceptance_criteria>
    - `git diff .planning/ROADMAP.md | grep -E '^[-+]- \[x\] [0-9]'` returns nothing — no completed-plan checkbox was touched
    - Every hit from `grep -niE 'audit\.auth_events|audit row|auth_events|barrier|route registry|enumeration assertion' .planning/ROADMAP.md` is either a line matching `^- \[[ x]\] [0-9]+.*PLAN\.md`, or a line inside the Phase 37.1 section, or a line that explicitly records the amendment
    - The Phase 38 and Phase 46 success criteria about an audit row carry the same forward-flag wording as their requirements in `.planning/REQUIREMENTS.md`
    - `grep -c 'Phase 37.1' .planning/ROADMAP.md` is unchanged and the Phase 37.1 goal line is byte-identical to before the task
    - `.venv/bin/pytest -q -m ""` still exits 0 — this task touches no code, and the criterion exists to catch an accidental edit outside `.planning/`
  </acceptance_criteria>
  <done>ROADMAP.md agrees with REQUIREMENTS.md on every amended, withdrawn and forward-flagged item; no completed-plan checkbox was rewritten; the two trivially-true criteria are confirmed rather than deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → the next phase's planner | The only boundary this plan touches. A requirement describing a deleted subsystem is a phase built against something that is not there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.1-29 | Repudiation | the two SHARED-INVARIANTS conflicts | high | mitigate | Silently editing a requirement so it no longer contradicts the binding specification hides a knowing divergence from every later reader — the exact failure the flag-never-resolve rule exists to prevent. Mitigated by requiring an explicit flagged-conflict note attached to FOUND-01 and FOUND-05, in SCHEMA-01's established form. |
| T-37.1-30 | Repudiation | completed-plan checkbox lines | medium | mitigate | Rewriting a checkbox that records a subsystem which really was built erases the project's own history and makes a later forensic read impossible. Mitigated by a diff-scoped grep asserting no `- [x] NN-…-PLAN.md` line changed. |
| T-37.1-31 | Elevation of privilege | SYNC-03 and SIGNOUT-02 | high | mitigate | Both are audit obligations on *unbuilt* phases, and SIGNOUT-02 in particular pairs its row with a fail-closed revocation. Quietly deleting either would let phase 46 ship a sign-out-all that reports success on an indeterminate revocation with nothing recording it. Flagged forward as a decision each phase must make, never resolved here. |
| T-37.1-32 | Spoofing | APPLEHOOK-02 and PLAYHOOK-03 | high | mitigate | Both require the webhook routes to be enumerated individually by exact path in a closed category — the control that stops a wildcard or prefix accidentally admitting an unauthenticated route. The category machinery is gone before either phase exists. Recorded as a loss those phases must answer, with a pointer and no redesign. |
| T-37.1-33 | Tampering | `audit.subscription_events` references | medium | mitigate | Amending audit-trail text wholesale would sweep up the subscription event log D-02 deliberately preserved, leaving phases 43 and 44 with no recorded home for store notifications. Asserted by a targeted grep. |
| T-37.1-SC | Tampering | npm/pip/cargo installs | high | n/a | No packages installed, no dependency added; this plan touches only markdown. |
</threat_model>

<verification>
- `grep -niE 'audit\.auth_events|audit row|auth_events|barrier|route registry|enumeration' .planning/REQUIREMENTS.md` returns only statements true after this phase, amendment notes included
- Every remaining hit of the same grep in `.planning/ROADMAP.md` sits inside a completed-plan checkbox, inside the Phase 37.1 section, or inside an amendment note
- Exactly one requirement id was removed, and it is FOUND-03
- Both SHARED-INVARIANTS conflicts carry an explicit flagged-conflict note
- No completed-plan checkbox line changed
</verification>

<success_criteria>
A reader opening `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md` after this phase finds a description of the system that exists, learns where this project knowingly diverged from its binding specification and why, and finds the four stranded obligations flagged for the phases that own them rather than deleted.
</success_criteria>

<output>
Create `.planning/phases/37.1-refactor-machine-generated-code/37.1-08-SUMMARY.md` when done. Record: the two flagged conflicts verbatim, the four forward-flagged requirements, and the single deletion.
</output>
