---
phase: 37.1-refactor-machine-generated-code
plan: 04
type: execute
wave: 3
depends_on: ["37.1-01", "37.1-02"]
files_modified:
  - src/nativespeaker/api/auth/firebase.py
  - src/nativespeaker/api/auth/keys.py
  - src/nativespeaker/api/auth/wire.py
  - src/nativespeaker/api/auth/identity.py
  - src/nativespeaker/api/auth/context.py
  - src/nativespeaker/api/auth/__init__.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/app/lifespan.py
  - src/nativespeaker/api/app/main.py
  - src/nativespeaker/api/app/errors.py
  - src/nativespeaker/api/errors.py
autonomous: true
requirements: [D-13, D-14, D-15, D-16, D-17]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "None of the eleven files contains a multi-line docstring"
    - "None of the eleven files contains a run of two or more consecutive own-line comments"
    - "No surviving comment or docstring in these files cites a spec section symbol, a ruling number, a decision id, a planning filename, a review ticket id, or a research pitfall number"
    - "src/nativespeaker/api/app/errors.py logs a ServiceError through a single logger.log(exc.log_level, ...) call and the level-to-method-name dict no longer exists (D-17)"
    - "Every one of the eleven files has a prose ratio at or below 18%, and the eleven together sit at or below 15%"
    - ".venv/bin/pytest -q -m '', -m e2e and -m schema all exit 0"
  artifacts:
    - src/nativespeaker/api/app/errors.py
    - src/nativespeaker/api/app/dependencies.py
    - src/nativespeaker/api/auth/wire.py
  key_links:
    - "app/errors.py::service_error_handler -> logs.py's structlog filtering bound logger: the replacement must be logger.log on the structlog bound logger, never the stdlib logging.log, or the contextvars request correlation is lost"
    - "app/dependencies.py's session-source note -> the no-lock-across-a-provider-call rule: this is the comment that stops a later phase routing the identity query through the database yield dependency"
  prohibitions:
    - statement: "Except in app/errors.py, no executable line may change. Everywhere else this plan edits comments, docstrings and blank lines only."
      status: required
      verification: "The AST-equality check reports SAME for all ten files other than src/nativespeaker/api/app/errors.py"
    - statement: "D-17's replacement must call the module-level structlog logger's .log method, not the stdlib logging module's log function. Routing through the logging module would bypass the structlog pipeline and lose the contextvars request correlation."
      status: required
      verification: "grep -n 'logging.log(' src/nativespeaker/api/app/errors.py returns nothing, and grep -c 'logger.log(' src/nativespeaker/api/app/errors.py returns 1"
    - statement: "Type-checker and linter directive comments (# type: ignore, # ty: ignore, # noqa) must not be deleted as prose."
      status: required
      verification: "The directive count over the eleven files is unchanged; .venv/bin/ruff check and .venv/bin/ty check stay clean"
    - statement: "The two invariants app/dependencies.py's prose is currently the only record of must survive as one plain sentence each: the auth dependency opens its own short session rather than using the database yield dependency, and QuotaGate does the same, because either would hold a transaction across the LLM provider round trip."
      status: required
      verification: backstop
---

<objective>
Rewrite the prose in the remaining `auth/` modules, the whole `app/` package, and `errors.py` (D-13, D-14, D-15, D-16), and replace the level-to-method-name dispatch table in `app/errors.py` with a direct `logger.log` call (D-17).

Purpose: `app/dependencies.py` carries four banner comment blocks fenced in rule lines, 43 of its 73 comment lines, and one of them documents code that no longer exists — an audit trail of a past deletion, kept in the source. `app/lifespan.py` is 44.4% prose with a twelve-line comment that opens by amending a previous version of itself. `errors.py` is 526 lines with nine multi-line docstrings.

Output: the composition root and the error registry readable cold, and one dict of five entries gone.

## Pinned definitions

A **multi-line comment block is a run of two or more consecutive own-line comment lines** — the threshold for this phase is two, not three. Surviving notes must fit one line within the 120-character limit.

The three commands used verbatim in every acceptance criterion below — **the measurement command**, **the AST-equality check**, and **the cross-reference grep** — are written out in full in the `<objective>` of `.planning/phases/37.1-refactor-machine-generated-code/37.1-03-PLAN.md`. Read that section and use them exactly as written.

## The two poles

**Target style:** `src/nativespeaker/api/logs.py` (2.6% prose), `src/nativespeaker/api/database/chats.py` (1.9%), `src/nativespeaker/api/routers/health.py` and `routers/root.py` (0%), `src/nativespeaker/api/models/chats.py` (0%).

**Anti-pattern, in this plan's own files:** `app/dependencies.py`'s four rule-fenced banner blocks, one of which describes two dependencies that were deleted phases ago; `app/lifespan.py:80-91`, a comment about a previous version of the same comment.

What plain English rules out: spec and plan cross-references as load-bearing text, arguing with a hypothetical reader, instructions to future maintainers, and markdown emphasis or section symbols inside docstrings. What it must not destroy: the non-obvious invariants this prose is currently the only record of.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.1-refactor-machine-generated-code/37.1-CONTEXT.md
@.planning/phases/37.1-refactor-machine-generated-code/37.1-PATTERNS.md
@.planning/phases/37.1-refactor-machine-generated-code/37.1-03-PLAN.md
</context>

<artifacts_this_phase_produces>
## Produced by this plan

No new files. One changed implementation: `service_error_handler` in `src/nativespeaker/api/app/errors.py` logs through one `logger.log(exc.log_level, ...)` call.

## Deleted by this plan

- `_LEVEL_TO_METHOD` — the five-entry dict at `src/nativespeaker/api/app/errors.py:19-25`, together with the `getattr(logger, method_name)` dispatch at `:31-33` (D-17)

Everything else deleted is a comment line or a docstring line.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: the remaining auth/ modules — firebase, keys, wire, identity, context, __init__</name>
  <files>src/nativespeaker/api/auth/firebase.py, src/nativespeaker/api/auth/keys.py, src/nativespeaker/api/auth/wire.py, src/nativespeaker/api/auth/identity.py, src/nativespeaker/api/auth/context.py, src/nativespeaker/api/auth/__init__.py</files>
  <read_first>
    - `src/nativespeaker/api/auth/wire.py` — whole file; its opening note is why the raw header list is read instead of a folding accessor, and that fact is load-bearing
    - `src/nativespeaker/api/auth/identity.py` — whole file as plan 02 left it; its docstring holds the anti-oracle rationale and the reason both comparisons are written in their strict form
    - `src/nativespeaker/api/auth/context.py` — whole file as plan 02 left it
    - `src/nativespeaker/api/auth/keys.py` — whole file; plan 01 already rewrote the field description at `:86-88`
    - `src/nativespeaker/api/auth/firebase.py` and `src/nativespeaker/api/auth/__init__.py` — whole files
    - `src/nativespeaker/api/logs.py` — the target-style register
  </read_first>
  <action>
Collapse every multi-line docstring and remove every two-or-more-line comment block in the six files. Preserve, as one plain sentence each, the facts a reader cannot recover from the code:

`wire.py` — that the raw header list is read rather than a first-value-wins accessor, because a duplicated or comma-joined `Authorization` field is its own rejection and a folding accessor cannot see it. This is the single most important sentence in the six files; it is the difference between enforcing the wire contract and appearing to.

`identity.py` — that admission is a positive test, so every unrecognized or future state fails closed; that the join is an outer one, so a broken link is a distinct outcome from an unlinked pair rather than being collapsed into it; and that the two `account_unavailable` branches are reached from the same statement so no observable work distinguishes them. Delete the paragraph arguing about timing normalization and the decision-id citations.

`context.py` — that the captured evaluation time is the single one for the whole request and nothing downstream recomputes it, and that the pre-auth variant deliberately carries nothing but the verified issuer and subject so an unlinked caller cannot be read as linked. Delete the phase-import inventory and the client-address paragraph, which describes a field plan 02 removed.

`keys.py`, `firebase.py` — collapse; keep any bound or failure-mode sentence that is not visible in the signature. `auth/__init__.py` — its 36-line module docstring collapses to at most two lines; keep only the one fact the code does not state, that the three Firebase names resolve lazily so importing the adapters seam does not pull in the provider SDK. Do not touch `__all__`, the import blocks, the lazy-name mapping, or the module-level attribute hook.

Change no executable line in any of the six.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m "" && .venv/bin/pytest -q -m e2e</automated>
  </verify>
  <acceptance_criteria>
    - The measurement command over the six files reports `multiline_docstrings=0` and `comment_blocks=0` for each, and `ratio` at or below 18.0 for each
    - The AST-equality check reports `SAME` for all six files
    - The cross-reference grep over the six files returns nothing
    - `.venv/bin/python -c "import nativespeaker.api.auth as a; assert a.build_admin_apps is not None"` exits 0 and `.venv/bin/pytest -q tests/unit/test_adapter_interfaces.py` exits 0 — the lazy re-export still works and the seam still imports no provider SDK
    - `.venv/bin/pytest -q -m ""` and `-m e2e` exit 0; `.venv/bin/ruff check src/` exits 0
  </acceptance_criteria>
  <done>Six modules read plainly, the wire-contract and admission-matrix invariants survive as single sentences, and no executable line changed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: the app/ composition root — dependencies, lifespan, main</name>
  <files>src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/app/main.py</files>
  <read_first>
    - `src/nativespeaker/api/app/dependencies.py` — whole file as plan 02 left it; the four rule-fenced banner blocks and the new auth dependency
    - `.planning/phases/37.1-refactor-machine-generated-code/37.1-02-SUMMARY.md` — what plan 02 changed here and why
    - `src/nativespeaker/api/app/lifespan.py` — whole file as plans 01 and 02 left it
    - `src/nativespeaker/api/app/main.py` — whole file as plan 02 left it
    - `src/nativespeaker/api/routers/root.py` — the 0%-prose pole, and the module `main.py` composes
  </read_first>
  <action>
`dependencies.py` — delete all four rule-fenced banner blocks. Any banner that survives at all survives as one line, and the rule lines themselves go. The block documenting deleted dependencies goes entirely: a record of a past deletion belongs in git history, not in the file. From the quota banner keep exactly one sentence — that consumption travels as `QuotaGate`, which the chat service calls at the resilience layer's admission callback rather than as a route-level dependency, because a pre-handler charge bills callers for requests the service then refuses.

Preserve, as one plain sentence each, the two session-source facts: the auth dependency opens its own short session and closes it before the handler runs, and `QuotaGate` takes the session factory directly — both because the alternative holds a transaction open across the LLM provider round trip. These two sentences are the only thing standing between a future edit and a real production bug; they are not optional prose.

Every accessor docstring collapses to one line or goes. Keep one sentence on the raw-query-string accessor explaining why the unparsed bytes are handed over: a duplicated parameter is its own rejection and a folding accessor cannot see it.

`lifespan.py` — it has zero docstrings and 55 comment lines in seven blocks; nearly all of it goes. Keep at most one line per app-state object stating what it is. Delete the twelve-line note that amends a previous version of itself outright, and delete the phase-history paragraph about the Firebase credential; the one fact worth a sentence is that the named per-issuer Firebase apps must be given back on shutdown, because the SDK keeps them in a process-global registry and a second boot in the same process would otherwise fail — the e2e suite does exactly that.

`main.py` — has no docstrings and 14 comment lines. Keep one line on why the four documentation routes are off (they register on the app router directly and would bypass the router-level auth dependency) and one line on why slash redirection is disabled. Delete the middleware-ordering commentary, which plan 02 made mostly obsolete.

Change no executable line in any of the three.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m "" && .venv/bin/pytest -q -m e2e</automated>
  </verify>
  <acceptance_criteria>
    - The measurement command over the three files reports `multiline_docstrings=0` and `comment_blocks=0` for each, and `ratio` at or below 18.0 for each
    - The AST-equality check reports `SAME` for all three files
    - The cross-reference grep over the three files returns nothing
    - `grep -c '^# ---' src/nativespeaker/api/app/dependencies.py` returns 0 — every rule-fenced banner is gone
    - `src/nativespeaker/api/app/dependencies.py` contains a one-line note stating that the auth dependency opens its own session rather than using the database yield dependency, and another stating the same for `QuotaGate`
    - `.venv/bin/pytest -q -m ""` and `-m e2e` exit 0; `tests/unit/test_app_wiring.py` passes unchanged
    - `.venv/bin/ruff check src/` exits 0
  </acceptance_criteria>
  <done>The composition root carries no banner blocks and no self-amending notes, both session-source invariants survive as single sentences, and no executable line changed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: errors.py prose, and D-17's logger.log replacement in app/errors.py</name>
  <files>src/nativespeaker/api/errors.py, src/nativespeaker/api/app/errors.py</files>
  <read_first>
    - `src/nativespeaker/api/app/errors.py` — whole file; `_LEVEL_TO_METHOD` at `:19-25`, the dispatch at `:30-34`, and the docstring at `:44-49`
    - `src/nativespeaker/api/logs.py:31` — `wrapper_class=structlog.make_filtering_bound_logger(...)`, the enabling fact: that bound logger exposes `.log(level, event, **kw)` taking the stdlib integer level
    - `src/nativespeaker/api/errors.py` — whole file; nine multi-line docstrings and 97 own-line comments over 526 lines, including the `AuthRejectionError` plan 02 added
    - `tests/unit/test_exception_handlers.py` and `tests/unit/test_error_registry.py` — the behavior that must not move
  </read_first>
  <action>
**D-17 first.** In `app/errors.py`, delete `_LEVEL_TO_METHOD` and replace the three-line dispatch inside `service_error_handler` with one call: `logger.log(exc.log_level, str(exc), error_type=type(exc).__name__, exc_info=(exc.log_level >= logging.ERROR))`. This is the module-level structlog logger's `log` method — **not** the stdlib `logging` module's `log` function, which would bypass the structlog pipeline and lose the contextvars request correlation. The surrounding `if exc.log_level is not None:` guard stays; the behavior for every level, including the previous fallback to error on an unmapped level, must be preserved or the change is not a refactor. Verify with the existing exception-handler tests, and add one case if none covers a level outside the old dict's five keys.

Then the prose. `app/errors.py` is already at 11.6%; collapse its one multi-line docstring at `:44-49` to a single line and leave the rest. `errors.py` — collapse all nine multi-line docstrings, delete the rule-fenced banner blocks, and keep one sentence per registered class group at most. Preserve as one plain sentence each: that error copy is neutral by construction so no branch within a class is distinguishable from another, that `register_class` rejects a duplicate name or code rather than merging, that the response body carries exactly one field, and that `AuthRejectionError` carries its error class per instance and logs nothing itself because the rejection is already recorded by the security log. Delete the v1.3 and v1.6 migration history and every decision-id citation.

Outside `app/errors.py`, change no executable line.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m "" && .venv/bin/pytest -q -m e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_LEVEL_TO_METHOD' src/nativespeaker/api/app/errors.py` returns 0 <!-- planner-discipline-allow: _LEVEL_TO_METHOD -->
    - `grep -c 'logger.log(' src/nativespeaker/api/app/errors.py` returns 1 and `grep -n 'logging.log(' src/nativespeaker/api/app/errors.py` returns nothing
    - `grep -c 'getattr(logger' src/nativespeaker/api/app/errors.py` returns 0
    - `.venv/bin/pytest -q tests/unit/test_exception_handlers.py tests/unit/test_error_registry.py tests/unit/test_error_contract.py tests/unit/test_logging.py` exits 0, including a case for a `log_level` outside the five levels the deleted dict mapped
    - The measurement command over both files reports `multiline_docstrings=0` and `comment_blocks=0` for each, and `ratio` at or below 18.0 for each
    - The AST-equality check reports `SAME` for `src/nativespeaker/api/errors.py` (it reports `CHANGED` for `app/errors.py`, which is expected and is the only file in this plan permitted to change)
    - The cross-reference grep over both files returns nothing
    - Across all eleven files in this plan, the aggregate ratio is at or below 15.0
    - `.venv/bin/pytest -q -m ""`, `-m e2e` and `-m schema` all exit 0; `.venv/bin/ruff check src/` exits 0
  </acceptance_criteria>
  <done>The five-entry level dict is gone and replaced by one structlog `logger.log` call with identical behavior at every level, both error modules read plainly, and every other file in this plan is byte-identical in its executable lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose → future maintainer | A deleted warning is a vulnerability a later phase reintroduces without resistance |
| exception → client response | `app/errors.py` is the single point where an internal exception becomes a client-visible body; D-17 edits it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.1-13 | Tampering | the ten prose-only files | high | mitigate | AST-equality check against `HEAD` in every task; only `app/errors.py` is permitted to change, and it is named explicitly. |
| T-37.1-07 | Denial of service | `app/dependencies.py`'s session-source notes | high | mitigate | These two sentences are the only in-code record of why neither the auth dependency nor `QuotaGate` uses the database yield dependency. Losing them makes the lock-across-a-provider-call regression a natural-looking future simplification. Required to survive verbatim in substance; asserted by a presence criterion in Task 2. |
| T-37.1-17 | Repudiation | D-17's replacement in `app/errors.py` | high | mitigate | Routing through the stdlib `logging` module instead of the structlog bound logger would silently drop the contextvars request correlation from every error line — losing the ability to tie an error to a request. Asserted by a negative grep on `logging.log(` plus the logging tests. |
| T-37.1-18 | Information disclosure | `errors.py`'s neutral-copy rule | medium | mitigate | The anti-oracle property depends on every branch within an error class returning identical copy. The sentence recording that must survive, or a later phase adds a helpful, distinguishing message. Asserted by the error-contract tests staying green plus the backstop. |
| T-37.1-14 | Tampering | tool directive comments | medium | mitigate | Before/after directive count plus clean `ruff` and `ty` runs. |
| T-37.1-SC | Tampering | npm/pip/cargo installs | high | n/a | No packages installed, no dependency added. |
</threat_model>

<verification>
- The measurement command over all eleven files reports `multiline_docstrings=0`, `comment_blocks=0` and `ratio` at or below 18.0 for each
- The aggregate ratio over the eleven files is at or below 15.0
- The AST-equality check reports `SAME` for all files except `src/nativespeaker/api/app/errors.py`
- The cross-reference grep over all eleven files returns nothing
- `.venv/bin/pytest -q -m ""`, `-m e2e` and `-m schema` all exit 0
</verification>

<success_criteria>
The composition root, the auth support modules and the error registry read cold; `_LEVEL_TO_METHOD` is one `logger.log` call with identical behavior; both session-source invariants and the wire-contract invariant survive as single plain sentences.
</success_criteria>

<output>
Create `.planning/phases/37.1-refactor-machine-generated-code/37.1-04-SUMMARY.md` when done. Record the before/after ratio per file, the D-17 replacement, and which invariants you kept.
</output>
