---
phase: 39-get-users-me
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PROF-01, PROF-02]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "AGENTS.md § Package layout states that a router may call crud/ directly and that a services/ class is introduced when the router body would otherwise become too big or complicated (D-05)"
      verification: backstop
    - statement: "AGENTS.md § Package layout still states that routers take Depends() only — D-05 removes the service requirement, not that rule (Pitfall 6)"
      verification: backstop
    - ".planning/REQUIREMENTS.md PROF-01 carries a dated Phase 39 amendment block recording D-02's rate-limit omission and D-03's divergence from the brief's literal handler step 1"
    - "The four numbered exceptions under AGENTS.md § Package layout survive the amendment, exception 4 included — it is what places the fail-closed raise in crud/"
  artifacts:
    - AGENTS.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "AGENTS.md § Package layout is read by every implementing agent in every later phase — this amendment is the phase's repo-wide deliverable, not commentary on it"
  prohibitions:
    - "No file under specs/auth-refactor-phases/ is edited — the briefs are marked verbatim and divergences are recorded as dated amendments instead"
    - "The existing PROF-02 amendment block is not rewritten, reworded, or removed"
    - "No existing service is refactored under the new rule (Deferred Ideas)"
    - "The Depends()-only rule for routers is not weakened or deleted"
    - "No requirement checkbox is ticked by this plan — the phase is not complete at this point"
---

<objective>
Land the phase's two documentation deliverables: the `AGENTS.md` § "Package layout" amendment D-05
requires, and the dated `.planning/REQUIREMENTS.md` amendments recording where this phase knowingly
diverges from the brief.

Purpose: D-05 is a repo-wide convention change binding every later phase's layering, and the project's
recorded practice is that a divergence from a verbatim brief is written down where the requirement
lives rather than edited into the brief. Both are deliverables in their own right.
Output: an amended layering rule and two dated amendment blocks.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-get-users-me/39-CONTEXT.md
@AGENTS.md
</context>

## Artifacts this phase produces

This plan creates no code symbols. The phase-wide symbol inventory — `MissingPurchaseTokenError`,
`PurchasesDB`, `get_purchases_db`, `Profile`, `MeResponse`, `users_router`, `seed_purchase_tokens`
and the three new test files — is listed in `39-01-PLAN.md` § "Artifacts this phase produces".

Probe-edge assumptions: all six unresolved edges are recorded in `39-01-PLAN.md` § "Assumptions".
None applies to this plan's files; none is dropped.

<tasks>

<task type="auto">
  <name>Task 1: Amend the layering rule (D-05)</name>
  <reversibility rating="costly">A repo-wide convention every later phase's routers are written under; reversing it means re-deciding the rule and revisiting whatever routers were written against it. Flagged, not gated — 39-CONTEXT.md records the developer stating the rule directly.</reversibility>
  <files>AGENTS.md</files>
  <read_first>
    - `AGENTS.md:24-47` — § "Package layout": the bullet list of homes and the four numbered exceptions
    - `AGENTS.md:1-22` — § "Comments and docstrings", for the register the whole file is written in
    - `AGENTS.md` § "Function shape" — the rule D-05 leans on when it says a one-read service is the shape to inline
    - `.planning/phases/39-get-users-me/39-CONTEXT.md` D-05 — the developer's wording of the rule
  </read_first>
  <action>
    Amend § "Package layout" so it states the rule the developer gave: a router may call `crud/`
    directly, and a `services/` class is introduced when the router body would otherwise become too big
    or complicated. A service is earned by complexity, not assumed by category. This supersedes the
    current text's implication that business logic always routes through `services/`.

    Concretely, the two bullets to touch are `services/` — "business logic: orchestration, rules,
    transaction boundaries" — and `routers/` — "HTTP handlers, `Depends()` only". Exact wording is
    yours; the constraints are:
    - The `Depends()`-only clause on the `routers/` bullet **stays**. D-05 removes the service
      requirement, not that rule; a handler that constructs a database class inline would violate the
      layering the same amendment is written to describe.
    - All four numbered exceptions survive unchanged. Exception 4 — a fail-closed read raises its own
      rejection, so the rejection stays with the query in `crud/` — is what places the raise in this
      phase's new crud module.
    - Match the file's register: imperative, no hedging, roughly 70-character wrap, the ground stated
      after a comma or a colon. The existing exceptions are the model — a rule and not a story.
    - The amendment binds new code. Do not add any instruction to refactor the existing services.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run ruff check</automated>
    <human-check>Read the amended § "Package layout" end to end: it states the router-to-crud rule, keeps the Depends()-only clause, and still carries all four numbered exceptions.</human-check>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` § "Package layout" contains a sentence stating that a router may call `crud/` directly
    - `AGENTS.md` § "Package layout" contains a sentence naming the condition under which a `services/` class is introduced (the router body becoming too big or complicated)
    - `grep -n 'routers/' AGENTS.md | grep -cF 'Depends()'` equals 1 — the routers bullet keeps its clause
    - `grep -cE '^[1-4]\. ' AGENTS.md` equals 4 — all four numbered exceptions survive
    - `grep -c 'the rejection stays with' AGENTS.md` equals 1 — exception 4 is intact
    - `git diff --name-only` for this commit lists `AGENTS.md` and no other file
    - `uv run pytest -q` exits 0 (nothing in the suite reads this file, so a red suite here means an unrelated break)
  </acceptance_criteria>
  <done>§ "Package layout" states the router-to-crud rule in the file's own register, the `Depends()`-only clause and all four exceptions are intact, and no existing service was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Record this phase's two divergences under PROF-01</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:200-209` — the Phase 38 SYNC-03 amendment block, the exact format to copy
    - `.planning/REQUIREMENTS.md:211-217` — § PROF: PROF-01, PROF-02 and PROF-02's existing Phase 37.1 amendment block
    - `.planning/phases/39-get-users-me/39-CONTEXT.md` D-02, D-03 and § "Carried forward"
    - `/home/init/native-speaker/specs/auth-refactor-phases/04-users-me.md` — the brief being diverged from; **read only, never edited**
  </read_first>
  <action>
    Append one dated amendment block under the **PROF-01** bullet, in the Phase 38 format: a blockquote
    nested under the requirement, opening
    `> **Amended by Phase 39 (D-02/D-03), 2026-09-01 — <one-clause summary>.**`, then bolded lead
    sentences per paragraph with a `>` blank line between paragraphs.

    Two divergences to record, each stating what the brief says, what this phase does instead, and the
    ground:

    1. **D-03 diverges from the brief's literal handler step 1**, "Load the resolved user's profile
       fields from `core.users`". The step is satisfied in substance — the row *was* loaded from
       `core.users`, by the barrier, in this request, and `app/lifespan.py:36` sets
       `expire_on_commit=False` so the detached row stays readable. Re-reading would issue a second
       query for a row already in memory, and the freshness it buys is empty: no path in this milestone
       edits either field. The purchase-token read is therefore the request's only query.

    2. **The brief's `users_me` rate-limit entry is omitted (D-02's other half)** because there is no
       engine to register it with. Phase 35 D-05 deleted the backend `limits` engine from the product
       rather than deferring it; `AGENTS.md` § Resilience states this and `limits` is absent from
       `pyproject.toml`. The auth surface is knowingly unlimited this milestone (Phase 38 D-08), and the
       gateway contract is deferred to v2.1.

    Leave PROF-02's existing Phase 37.1 amendment block untouched — it already records that this phase
    inherits no audit-writer and no counter obligation, and rewriting it would erase that history. Do
    not tick either PROF checkbox; the phase is not complete at this point. Do not edit any file under
    `specs/auth-refactor-phases/`: those briefs are marked verbatim and this block is the recorded
    alternative to editing them.
  </action>
  <verify>
    <automated>grep -c "Amended by Phase 39" .planning/REQUIREMENTS.md && git diff --name-only -- specs/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Amended by Phase 39" .planning/REQUIREMENTS.md` equals 1
    - The new block sits between the `**PROF-01**` bullet and the `**PROF-02**` bullet: `awk '/\*\*PROF-01\*\*/,/\*\*PROF-02\*\*/' .planning/REQUIREMENTS.md | grep -c "Amended by Phase 39"` equals 1
    - `grep -c "Amended by Phase 37.1" .planning/REQUIREMENTS.md` equals 1 — the existing PROF-02 block survives
    - `grep -c "expire_on_commit" .planning/REQUIREMENTS.md` is at least 1 — the D-03 ground is stated, not just asserted
    - `git status --porcelain specs/` produces no output — no brief was edited
    - `awk '/### PROF/,/### UPGRADE/' .planning/REQUIREMENTS.md | grep -c '^- \[x\]'` equals 0 — neither PROF checkbox was ticked
  </acceptance_criteria>
  <done>PROF-01 carries a dated Phase 39 amendment recording both divergences with their grounds, PROF-02's existing block is byte-identical, and no brief under `specs/` was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future implementing agents | `AGENTS.md` is read as binding instruction by every later phase's agent; a wrong or ambiguous rule here propagates into code that has not been written yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-08 | Tampering (instruction drift) | `AGENTS.md` § Package layout | medium | mitigate | The amendment keeps the `Depends()`-only clause and all four numbered exceptions; task 1's acceptance criteria assert both survive, so a rewrite that drops the router constraint fails before commit |
| T-39-09 | Repudiation (undocumented divergence) | `.planning/REQUIREMENTS.md` | medium | mitigate | Both divergences are recorded as dated amendments naming the decision ids, so a later reader can tell a deliberate omission from a missed obligation |
| T-39-10 | Tampering (spec corruption) | `specs/auth-refactor-phases/` | high | mitigate | The briefs are read-only in this phase; `git status --porcelain specs/` is asserted empty in task 2's acceptance criteria |

No code, no dependency, and no runtime surface changes in this plan, so the ASVS categories that apply
to the endpoint itself are carried by `39-01-PLAN.md`.
</threat_model>

<verification>
- `uv run pytest -q` green and `uv run ruff check` clean — this plan changes no code, so any failure is unrelated and must be reported rather than absorbed
- `git status --porcelain specs/` empty
- The amended § "Package layout" read end to end by a human against D-05's wording
</verification>

<success_criteria>
`AGENTS.md` states the router-to-crud rule while keeping the `Depends()`-only constraint and all four
exceptions; `.planning/REQUIREMENTS.md` carries one dated Phase 39 amendment under PROF-01 covering
both divergences; nothing under `specs/` changed.
</success_criteria>

<output>
Create `.planning/phases/39-get-users-me/39-02-SUMMARY.md` when done
</output>
