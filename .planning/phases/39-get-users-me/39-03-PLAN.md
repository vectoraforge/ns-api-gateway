---
phase: 39-get-users-me
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - tests/unit/test_users_me.py
  - tests/unit/test_purchases_crud.py
autonomous: true
requirements: [PROF-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The 200 body equals a whole literal — a fourth top-level key or a fifth profile field fails the assertion (D-01)"
    - "set(purchase_tokens) equals {p.value for p in PurchaseProvider} on every 200 (PROF-01, D-07)"
    - "The body is identical across a differing User-Agent, an unknown platform header, and an unknown query parameter — the executable form of roadmap criterion 1 (D-02)"
    - "The handler issues exactly one statement and none of them names the users table (D-03)"
    - "The 200 carries Cache-Control: no-store, by equality and not containment (D-09)"
    - "Zero token rows raises, one row raises, and both rows return the mapping — the one-row case is what an emptiness check would wrongly pass (D-07)"
    - "The purchase-token statement compiles under the PostgreSQL dialect with no lock clause"
    - "The raised exception's message names the user id and the missing provider values and never the token column value (D-06, invariant 10)"
  artifacts:
    - tests/unit/test_users_me.py
    - tests/unit/test_purchases_crud.py
  key_links:
    - "The unit stub's result rows must be (provider, identity_value) tuples, matching the two-column select the production code unpacks (Pitfall 7)"
  prohibitions:
    - "No branching on User-Agent, on any platform header, or on any query parameter is introduced into the route to make a test pass — a failing invariance case is a source bug, not a test to soften"
    - "No test asserts a purchase token is absent from a log line (D-10 — no code path carries a body into a log, so such a test would assert something no code attempts)"
    - "No test seeds one token row and expects a 200"
    - "No null or partial purchase_tokens entry is accepted by any assertion"
    - "The route's source is not modified by this plan — if a case goes red, report it rather than edit src/"
---

<objective>
Prove at unit level what the end-to-end tracer only demonstrated once: the body is closed and
unconditional, the read takes one unlocked query, and the completeness rule refuses a partial account.

Purpose: the tracer proved the happy path exists. These cases pin the properties that would otherwise
drift silently — a fourth field creeping into the payload, a platform branch appearing, a second query
appearing, or the completeness check degrading into an emptiness check.
Output: two new unit test files, each runnable in under a second.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-get-users-me/39-PATTERNS.md
@.planning/phases/39-get-users-me/39-VALIDATION.md
@src/nativespeaker/api/routers/users.py
@src/nativespeaker/api/crud/purchases.py
</context>

## Artifacts this phase produces

This plan creates `tests/unit/test_users_me.py` and `tests/unit/test_purchases_crud.py`. The
phase-wide symbol inventory is in `39-01-PLAN.md` § "Artifacts this phase produces"; every production
symbol these tests import (`MissingPurchaseTokenError`, `PurchasesDB`, `get_purchases_db`, `Profile`,
`MeResponse`, `users_router`) is created by plan 39-01 and is not drift.

Probe-edge assumptions: all six unresolved edges are recorded in `39-01-PLAN.md` § "Assumptions".
E2 (empty), E3 (ordering) and E5 (concurrency) are the ones this plan makes executable — E2 by the
zero-row and one-row cases, E3 by asserting the key **set** and never the key order, E5 by the
no-lock assertion. None is dropped.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The route's closed, unconditional body</name>
  <files>tests/unit/test_users_me.py</files>
  <read_first>
    - `tests/unit/test_challenge_endpoint.py:31-61` (the `_Recording*` doubles), `:64-88` (the client fixture: bare `FastAPI()`, `include_router`, `register_exception_handlers`, `dependency_overrides`, `raise_server_exceptions=False`), `:110-119` (key-set and `cache-control` assertions), `:130-132` (the parametrise register)
    - `tests/unit/conftest.py:103-114` — `TEST_IDENTITY`, whose `User` sets only `id` and `active`, which is why a local `Identity` carrying `email` and `display_name` is needed
    - `src/nativespeaker/api/routers/users.py` — the handler as landed by plan 39-01
    - `src/nativespeaker/api/schemas/auth.py` — `Profile`, `MeResponse`, `Identity`
    - `src/nativespeaker/api/tables/purchases.py:10-14` — `PurchaseProvider`
    - `.planning/phases/39-get-users-me/39-PATTERNS.md` § `tests/unit/test_users_me.py`
  </read_first>
  <behavior>
    - Whole-body equality: a 200 body equals `{"profile": {"email": <seeded>, "display_name": <seeded>},
      "identity_provider": <seeded provider value>, "purchase_tokens": {"apple": <t1>, "google_play": <t2>}}`
      as a single literal, so a fourth top-level key or a fifth profile field fails.
    - Key set: `set(body["purchase_tokens"]) == {p.value for p in PurchaseProvider}`.
    - Header: `response.headers["cache-control"] == "no-store"` — lowercase key, equality not containment.
    - One query: the recording double saw exactly one statement, and no statement's compiled text names
      the users table.
    - Client-signal invariance, parametrised over at least a differing `User-Agent` value, an unknown
      platform request header, and an unknown query parameter: each response body is byte-identical to
      the baseline body and each status is 200.
    - A caller whose `email` and `display_name` are both `None` still gets 200 with `null` for both
      inside `profile`, and both store keys present — a nullable profile column is not the null entry
      criterion 4 forbids.
  </behavior>
  <action>
    Build the client fixture exactly as `tests/unit/test_challenge_endpoint.py:74-88` does — a bare
    `FastAPI()`, `include_router(users_router)`, `register_exception_handlers(app)`, then
    `dependency_overrides` for `get_linked_identity` and `get_purchases_db`, and
    `TestClient(app, raise_server_exceptions=False)`. `raise_server_exceptions=False` is required so the
    500 arm renders through the shared handler rather than propagating.

    Construct a local linked `Identity` carrying a `User` with a known `email` and `display_name` and an
    `ExternalIdentity` with a known provider; the shared `TEST_IDENTITY` sets only `id` and `active` and
    is not sufficient. Substitute a recording double for the crud class rather than a real session, in
    the `_Recording*` naming register: it records the statements it was asked for on a `self.statements`
    list and returns a complete mapping, and it raises `AssertionError` on any method the route must
    never call. The `self.statements` list is how the one-query property is asserted.

    For the invariance cases, capture a baseline response with no extra headers or parameters, then
    parametrise over the request variations and assert each `response.content` equals the baseline's.
    Assert on bytes, not on a re-parsed dict, so a reordering or a formatting change is also visible.
    If any invariance case fails, that is a source bug in the handler — report it; do not soften the
    assertion and do not edit `src/`.

    Class names are full sentences with a one-line docstring stating the rule the class holds, and
    method names spell the behaviour, following `tests/unit/test_sync_resolver.py`. Docstrings are three
    lines maximum and the docstring-bar baseline for `tests/unit` is 0 and must stay 0.
  </action>
  <verify>
    <automated>uv run pytest -q tests/unit/test_users_me.py && uv run pytest -q tests/unit/test_docstring_bar.py && uv run ruff check</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q tests/unit/test_users_me.py` exits 0
    - `grep -c 'response.json() ==' tests/unit/test_users_me.py` is at least 1 — the closed payload is asserted as a whole literal
    - `grep -cF 'headers["cache-control"] == "no-store"' tests/unit/test_users_me.py` is at least 1
    - `grep -cF 'set(PurchaseProvider)' tests/unit/test_users_me.py` is at least 1 — the key set is derived from the enum, never hand-listed
    - `grep -c 'parametrize' tests/unit/test_users_me.py` is at least 1 — the invariance cases are parametrised
    - `uv run pytest -q tests/unit/test_users_me.py --collect-only -q 2>&1 | tail -1` reports at least 8 collected cases
    - `git diff --name-only` for this commit lists only paths under `tests/`
    - `uv run pytest -q tests/unit/test_docstring_bar.py` exits 0
  </acceptance_criteria>
  <done>The route's body is proven closed, its key set proven to be the enum, its header proven `no-store`, its query count proven to be one, and its body proven identical across three client-supplied signals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The completeness rule and the unlocked read</name>
  <files>tests/unit/test_purchases_crud.py</files>
  <read_first>
    - `tests/unit/test_identities_crud.py:24-70` — `_StubResult`, `_StubSession` with its `statements` list and `executed` property, and the `_rejected` raise-assertion helper
    - `tests/unit/test_sync_resolver.py:35-45` — the `_StubResult` shape carrying `.all()` for a multi-row read; `:31-32` and `:106-108` — `LOCK_CLAUSE` and the `_compiled()` helper; `:137-142` and `:206-209` — the no-lock and reads-no-other-table assertions
    - `src/nativespeaker/api/crud/purchases.py` — the statement and the completeness branch as landed by plan 39-01
    - `src/nativespeaker/api/errors.py` — the `MissingPurchaseTokenError` signature
    - `.planning/phases/39-get-users-me/39-PATTERNS.md` § `tests/unit/test_purchases_crud.py`, including Pitfall 7
  </read_first>
  <behavior>
    - Both rows present: `read_tokens` returns a mapping whose keys are exactly `set(PurchaseProvider)`
      and whose values are the stubbed token strings.
    - Zero rows: raises `MissingPurchaseTokenError`.
    - **One row present, one missing: raises `MissingPurchaseTokenError`** — this is the case an
      emptiness check would wrongly pass, and it is the reason the check is written against the enum.
    - The raised instance names the missing provider and the user id, and its `str()` contains neither
      of the stubbed token values.
    - The statement compiles under the PostgreSQL dialect with no lock clause, and exactly one statement
      is issued per call.
  </behavior>
  <action>
    Stub the session rather than touching a database. Copy the `_StubSession` shape from
    `tests/unit/test_identities_crud.py:24-70` — a `self.statements` list appended on every `exec` — and
    the multi-row `_StubResult` from `tests/unit/test_sync_resolver.py:35-45` so `.all()` is available.

    Pitfall 7 is load-bearing: the production statement selects two columns, so its results are row
    tuples and not model instances. The stub must yield `(provider, identity_value)` tuples matching the
    unpacking the production code does. Do not copy the `column_descriptions[0]["entity"]` dispatch from
    `tests/unit/test_sync_resolver.py:77-80` — there is one statement here and nothing to dispatch on.

    Parametrise the raising cases over the seeded subsets so the zero-row case and each one-row case are
    separate reported cases rather than one merged assertion. Use the `pytest.raises(...) as caught`
    form from `tests/unit/test_identities_crud.py:67-70` so the raised instance is available, then assert
    on it: the missing provider is named, the user id is named, and neither stubbed token value appears
    in `str(caught.value)`. That last assertion is the executable trace of D-06's redaction constraint —
    the token column value must never reach the message, the traceback, or any log field.

    For the lock assertion, reuse the `_compiled()` helper and the `LOCK_CLAUSE` constant register from
    `tests/unit/test_sync_resolver.py:31-32, 106-108` (needs `from sqlalchemy.dialects import postgresql`),
    and assert the compiled statement carries no lock clause. Add the companion assertion that exactly
    one statement was issued.

    Do not test that a missing row is minted, backfilled, or repaired — the read path repairs nothing,
    and a test expecting a 200 from a single seeded row would encode the exact bug the enum check exists
    to prevent.
  </action>
  <verify>
    <automated>uv run pytest -q tests/unit/test_purchases_crud.py && uv run pytest -q && uv run ruff check</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q tests/unit/test_purchases_crud.py` exits 0
    - `grep -cF 'MissingPurchaseTokenError' tests/unit/test_purchases_crud.py` is at least 2
    - `grep -cF 'set(PurchaseProvider)' tests/unit/test_purchases_crud.py` is at least 1
    - `grep -cF 'postgresql.dialect()' tests/unit/test_purchases_crud.py` is at least 1 — the lock check compiles under the dialect that actually runs it
    - `uv run pytest -q tests/unit/test_purchases_crud.py --collect-only -q 2>&1 | tail -1` reports at least 5 collected cases, including a case for a single-provider seed
    - `uv run pytest -q` exits 0
    - `git diff --name-only` for this commit lists only paths under `tests/`
  </acceptance_criteria>
  <done>Both rows return the mapping, zero rows and one row each raise with the user id and missing providers named and no token value in the message, and the statement is proven lock-free and singular.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | The request variations the invariance cases send are the untrusted-input surface being proven inert |
| API → client | The response body and headers, asserted to carry no server-side identifier |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering / Integrity | `crud/purchases.py::read_tokens` | high | mitigate | Task 2's one-row case is the executable guard on the completeness rule; without it an emptiness check would pass and a partial account would be served |
| T-39-02 | Information disclosure | `errors.py::MissingPurchaseTokenError` | high | mitigate | Task 2 asserts neither stubbed token value appears in `str(caught.value)`, so a future edit that formats the token column into the message fails here |
| T-39-05 | Tampering (client-signal steering) | `routers/users.py::me` | medium | mitigate | Task 1's parametrised invariance cases compare response bytes against a baseline across three client-supplied signals; a branch introduced later fails immediately |
| T-39-06 | Information disclosure (cache retention) | `routers/users.py::me` | medium | mitigate | Task 1 asserts the header by equality, so `no-cache` or a dropped header fails |
| T-39-11 | Information disclosure (test-only leakage) | `tests/unit/*` | low | accept | The stubbed token strings are fixtures, not real credentials; no live secret enters either test file |
</threat_model>

<verification>
- `uv run pytest -q` green (both new files run inside the quick suite; measured budget ~29s)
- `uv run ruff check` clean
- `uv run pytest -q tests/unit/test_docstring_bar.py` green — `tests/unit` still at a baseline of 0
- `git diff --name-only` for this plan's commits lists only paths under `tests/`
</verification>

<success_criteria>
Both new unit files are green inside the quick suite; the closed body, the enum key set, the
`no-store` header, the single unlocked query, the client-signal invariance and the one-row fail-closed
case each have a named, reported case.
</success_criteria>

<output>
Create `.planning/phases/39-get-users-me/39-03-SUMMARY.md` when done
</output>
