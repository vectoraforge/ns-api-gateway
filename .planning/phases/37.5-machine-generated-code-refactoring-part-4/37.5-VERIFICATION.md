---
phase: 37.5-machine-generated-code-refactoring-part-4
verified: 2026-08-31T22:18:30Z
status: gaps_found
score: 11/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "The docstring and comment rules apply to every file in `src/` and `tests/` (D-12, A-08)"
    status: partial
    reason: >-
      The docstring half is complete and mechanically gated at 0 on all five roots with a real
      negative control. The comment half has measurable residue in files the phase swept, and
      the phase's own record names 1 of 13 instances of the largest defect class.
    artifacts:
      - path: "src/nativespeaker/api/tables/grants.py"
        issue: "Lines 56, 71, 74 call the database \"a crud\" (`# A crud CHECK enforces…`, `the crud enforces no format`, `no crud DEFAULT`)"
      - path: "src/nativespeaker/api/tables/purchases.py"
        issue: "Line 27: `The table has no crud primary key`"
      - path: "tests/unit/test_users.py"
        issue: "Lines 8 and 55 call the database \"the live crud\" / \"rows the crud accepts\""
      - path: "tests/unit/test_models.py"
        issue: "Lines 249 and 272: `the pre-existing crud enum type`, `no crud primary key`"
      - path: "tests/unit/test_identities_crud.py"
        issue: "Line 1 module docstring: `the branches a real crud cannot produce`"
      - path: "tests/unit/test_quota_resolver.py"
        issue: "Line 1 module docstring: `the branches a real crud cannot produce`"
      - path: "tests/unit/test_conflict_classification.py"
        issue: "Line 1 module docstring: `durability needs a real crud`"
      - path: "tests/e2e/test_admission.py"
        issue: "Line 163: `a bad token never reaches the crud`"
      - path: "tests/schema/conftest.py"
        issue: "Line 55 runtime string: `refusing to interpolate {name!r} as a crud identifier` (the one instance the record names, cited there as :56)"
      - path: "tests/unit/test_firebase_retry.py"
        issue: "Lines 63 and 163 cite `budgets.py:49-50` and `budgets.py:65-66` — a module Phase 37 D-04 retired. This is exactly P-10 / the `auth/adapters.py` defect class the phase set out to eliminate."
      - path: "tests/unit/test_firebase_adapter.py"
        issue: "A docstring cites \"`adapters.py`'s preamble: a fixed configured per-attempt timeout on the order of 5-10 s\" — the swept `adapters.py` docstring no longer says that."
      - path: "src/nativespeaker/api/errors.py"
        issue: "Lines 147-148: a two-line comment block explaining an absence in the registry design, with no code line below it. AGENTS.md requires one line each, explaining the line or lines below."
      - path: "tests/unit/test_create_user_precedence.py"
        issue: "Lines 498-501: a four-line comment describing where a deleted case moved to — 'describes what lives somewhere else'. Line 173-174 is a two-line comment restating a rule enforced in `app/dependencies.py`."
      - path: "tests/unit/test_create_user_body.py"
        issue: "Lines 83-84: the same two-line comment restating `app/dependencies.py::get_db`'s design"
    missing:
      - "Replace the 13 remaining 'crud'-for-database prose occurrences across 9 files in `src/` and `tests/` (plan 09 fixed 2 of 15 under the header 'name the database, not the crud package'; 13 survive)"
      - "Retire the two `budgets.py` citations in `tests/unit/test_firebase_retry.py` and the stale `adapters.py` preamble citation in `tests/unit/test_firebase_adapter.py`"
      - "Reduce the 5 remaining multi-line comment blocks and the 3 design/absence comments in `errors.py` to the AGENTS.md one-line bar, or record them as a deliberate carve-out"
deferred: []
human_verification:
  - test: "Decide whether `tables/__init__.py` re-exporting 11 `schemas/` names is acceptable residue or closes as layering work"
    expected: "Either the 11 re-exports are dropped (no consumer in `src/` or `tests/` uses them today) or the goal's 'bodies in `schemas/`' clause is amended to permit the facade"
    why_human: "A scope call between the ROADMAP goal's layering clause and the phase's own recorded 'did NOT resolve' list — not a code defect a checker can settle"
  - test: "Decide whether `routers/auth.py::issue_challenge` must move behind a service"
    expected: "Either the issuable-operation rule and the direct `ChallengesDB.issue()` call move into `services/`, or `AGENTS.md`'s `routers/` clause gains a carve-out"
    why_human: "AGENTS.md now states `routers/` is `Depends()`-only with no business logic and no queries; this handler retains one rule and calls `crud/` directly. The phase recorded it as 'unchanged in substance' rather than as an exception."
---

# Phase 37.5: Machine-generated code refactoring, part 4 — Verification Report

**Phase Goal:** Restore the layered architecture and write it down — business logic in `services/`, database access in `crud/`, bodies in `schemas/`, tables in `tables/`, handlers in `routers/`, external-SDK seams in `auth/` — then apply the docstring and comment rules to every file in `src/` and `tests/`, inline the functions that are only a step, move the error-tree self-check out of production code, and move the quota charge so a request that never reached the provider is never billed.

**Verified:** 2026-08-31T22:18:30Z
**Status:** gaps_found
**Re-verification:** No — initial verification

Requirement set: D-01…D-15 and A-01…A-17 in `37.5-CONTEXT.md`. Every claim below was checked by reading the file or running the command; no SUMMARY assertion was accepted on its own.

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | The layered architecture is restored: business logic in `services/`, DB access in `crud/`, bodies in `schemas/`, tables in `tables/`, handlers in `routers/`, SDK seams in `auth/` (D-01…D-06) | ✓ VERIFIED (2 warnings) | `grep -rn "select(\|\.add(\|\.flush(\|\.exec(" src/ \| grep -v crud/` returns **zero lines** — every database call in `src/` lives in `crud/`. `auth/` holds exactly `__init__.py`, `adapters.py`, `firebase.py`, `jwt_verifier.py`. `auth/identity.py`, `auth/create_user.py`, `api/quota.py` all absent; `grep -rn 'auth\.identity\|auth\.create_user\|api\.quota\b' src tests` returns zero. `services/auth.py` (135 L), `services/quota.py` (82 L), `crud/identities.py` (98 L), `schemas/auth.py` are all substantive and wired through `app/dependencies.py::get_auth_service`/`get_quota_service`. `crud/grants.py` is **byte-identical** to `0817c6f` (D-06). Warnings below. |
| 2 | The layering, function-shape and resilience rules are written into `AGENTS.md` (D-01, D-08, D-11, A-02, A-06, A-07) | ✓ VERIFIED | `AGENTS.md` carries the six-package layering rule with all four exceptions — `errors.py` owning the response shape (A-07), `BoundedReason` staying in `auth/jwt_verifier.py`, `commit()`/`rollback()` in `services/`, and "a fail-closed read may raise its own rejection" (A-06). D-08's rule is present with the inline-then-read-the-call-site check and the recursive-function case. The D-11 note carries the corrected reasoning (tenacity bounds one request at ~91.5s, nothing across requests; no installed package replaces a breaker) and **contains no 429-vs-503 status-flip claim** — A-02's binding correction. |
| 3 | The docstring and comment rules apply to every file in `src/` and `tests/` (D-12, A-08) | ⚠️ PARTIAL | Docstring half **complete and gated**: `tests/unit/test_docstring_bar.py` **9 passed**, baselines `{src: 0, tests: 0, tests/e2e: 0, tests/schema: 0, tests/unit: 0}` asserted with `==` not `<=`, and four `TestTheMeasurementFires` controls prove the walk fires (4-line reported, 3-line not, method-inside-class reached). An AST scan finds **zero** docstrings in `src/` that describe what lives elsewhere. Comment half has residue: **13 prose occurrences across 9 files call the database "a crud"** (the record names 1); 5 multi-line comment blocks against AGENTS.md's "one line each"; 8 comments in `errors.py` not above a code line; 2 test docstrings cite `budgets.py`, retired in Phase 37. See Gaps. |
| 4 | Functions that are only a step are inlined; the keeps are recorded (D-08, A-04, A-05) | ✓ VERIFIED | `grep -rn "_complete\|_insert_account\|consume_quota\|charge_quota" src/` returns **zero** — all three D-08 deletions landed. `_sleep_if_positive` is **kept** at `resilience.py:113` with a docstring stating the zero-backoff behaviour (A-04). `REQUIREMENTS.md:264-290` carries the 21-function classification table (3 deletions + 18 keeps), including A-05's six previously unclassified helpers. |
| 5 | The error-tree self-check left production code and is a test (D-09, A-11) | ✓ VERIFIED (behavioural) | `grep -rn 'assert_tree_total\|_tree_problems\|_undeclared' src/` returns **zero**. The checker is `tests/unit/error_tree.py` (73 L). `_family` (`errors.py:55`) and `class_answering_status` (`:64`) survive and are still called at `app/error_handlers.py:56` — live dispatch, not deleted. The five subprocess controls each assert a **specific message substring** in stderr (an `ImportError` would not match), and `test_the_same_checks_report_nothing_when_no_synthetic_class_exists` asserts `returncode == 0` and `stderr == ""` — that positive control is what would fail if the `PYTHONPATH` rehost broke. All pass. |
| 6 | Four dead exception classes deleted; `ChallengeRequired` kept with a name-level assertion (D-10, A-12) | ✓ VERIFIED | `InvalidCursorError`, `PageSizeLimitError`, `WebhookVerificationError`, `DatabaseNotInitializedError` return **zero hits** across `src/` and `tests/`. `ChallengeRequired` survives at `errors.py:107`; `test_error_registry.py:201` asserts `class_answering_status(409) is ChallengeRequired` by name — A-12's requirement that a reference-grep cannot read it as dead. |
| 7 | A request that never reached the provider is never billed (D-07, A-01, A-02, A-03) | ✓ VERIFIED (behavioural) | `ResiliencePolicy.admission()` consults the breaker, holds a gate slot, and yields a frozen `Admitted` token; `ainvoke(operation, admitted)` takes it as a required parameter. In `services/chats.py:91-93` and `:114-116` the charge is a plain statement **inside** `admission()` and **outside** `attempt()`'s retry. `tests/unit/test_quota_seam.py` **20 passed**, including `test_a_full_queue_answers_503_and_spends_nothing` and `test_an_open_circuit_answers_503_and_spends_nothing` (both assert `usage.monthly_used == 0`, `llm.calls == 0`, `events == []` and status/code `(503, "service_unavailable")` — A-02's corrected status), `test_two_transient_failures_then_success_spends_one_unit` (`llm.calls == 3`, `usage.monthly_used == 1`), and `test_the_charge_is_not_refunded_when_the_provider_call_fails`. Positive controls exist on both admitted paths. Validation rejections still precede the charge (four cases). `grep -rn 'on_admitted\|QuotaGate\|_AdmissionRejected' src tests` returns **zero** — no callback chain. |
| 8 | The circuit breaker and gate stay; `resilience.py` is otherwise not a refactor target (D-11) | ✓ VERIFIED | `CircuitBreaker` (`:39`) and `LLMExecutionGate` (`:73`) both present with `_inflight_slot`, `hold`, and the transient classifier. `resilience.py` is 169 lines. `QuotaExceededError` and `charge_quota` appear nowhere in it. |
| 9 | Out-of-proportion test files are cut; every deleted case is named; no assertion weakened in place (D-13, A-08, A-09, A-13) | ✓ VERIFIED | 60 test-case names disappear between `0817c6f` and `HEAD`; **all 60 appear by name in the phase SUMMARY files** (checked mechanically). `test_challenge_ids.py` 415→341, `test_jwt_security.py` 587→527, `test_identity_accessors.py` 430→383, `test_adapter_interfaces.py` 257→128. A-09's two keeps (`test_a_padded_credential_value_is_accepted…`, `test_a_duplicate_authorization_field…`) survive; a third survived and the deviation is disclosed in `37.5-08-SUMMARY.md` flag 1. **`tests/schema/`: `git diff 0817c6f HEAD -- tests/schema/` contains zero changed `def test_`, `class Test` or `assert` lines** — swept, not pruned (A-08). Anti-oracle controls survive in `test_jwt_security.py:200`, `test_rejection_vocabulary.py:167`, `test_exception_handlers.py:396`, `e2e/test_admission.py:114`. `test_startup_assertion.py` renamed to `test_unauthenticated_access.py` (P-10). All 31 added assertions map to new subjects (the D-07 seam, the docstring gate, the A-12 name assertion, the two restored security cases) — no weakening in place. |
| 10 | Project records amended (D-14, D-15, A-16) | ✓ VERIFIED (1 defect) | `ROADMAP.md:430-433` carries a real goal, a **Delivered** paragraph and a real Requirements line — no placeholder survives. `REQUIREMENTS.md:20-28` carries the dated Phase 37.5 amendment; `:28` names **six** things the phase did not resolve. D-15's verdict is restated with the flagged-conflict count held at six and the `limits` mandate recorded as an override rather than a new conflict (A-16). Defect: `REQUIREMENTS.md:104` says `errors.py` is "495 → **399** lines"; the file measures **397**, which is what `:28` and the ROADMAP both say. |
| 11 | The phase gates pass | ✓ VERIFIED | `uv run python -m pytest -q -m ""` → **1016 passed**, 0 deselected, 77.6s. `uv run ruff check .` → **All checks passed!**. `uv run python -m pytest -q tests/unit/test_docstring_bar.py` → **9 passed**, gate at 0 on all five roots. `pytest -m schema --collect-only` → 114 collected, zero errors (A-10's trap cleared). No `TODO`/`FIXME`/`XXX`/`TBD`/`HACK`/`PLACEHOLDER` anywhere in `src/`, `tests/` or `AGENTS.md`. |
| 12 | The two security cases restored in `658895e` are present and non-vacuous | ✓ VERIFIED (behavioural) | Both **pass** when run by node id. `test_rejects_token_signed_with_different_key` (`test_jwt_security.py:109`) generates a real second 2048-bit RSA key and asserts `BoundedReason.bad_signature` — distinct from the tampered-payload case above it. `test_the_module_logs_nothing` (`test_challenge_ids.py:332`) parses 4,869 characters of `crud/challenges.py` and reads a real, non-empty import set `['base64','datetime','nativespeaker','secrets','sqlalchemy','sqlmodel']` before asserting `structlog` and `logging` are absent. |

**Score:** 11/12 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `AGENTS.md` | Layering, function-shape, resilience rules | ✓ VERIFIED | All three present with every named carve-out; D-11 note free of the withdrawn 429/503 claim |
| `tests/unit/test_docstring_bar.py` | Gate at 0 on five roots, with control | ✓ VERIFIED | 9 passed; `==` not `<=`; four controls; runs in the default unit selection so the ratchet is live |
| `src/nativespeaker/api/schemas/auth.py` | Four Pydantic bodies + `Identity` | ✓ VERIFIED | 4 bodies + `Identity` dataclass; `CreateUserRequest.challenge_id` keeps `min_length=1` |
| `src/nativespeaker/api/crud/identities.py` | `IdentitiesDB` with the four queries | ✓ VERIFIED | `resolve`, `resolve_existing`, `user_by_id`, `insert_account`; the four A-06 rejections stay with the query |
| `src/nativespeaker/api/services/auth.py` | `AuthService.complete` | ✓ VERIFIED | 135 L; rejection precedence, claim, commit, provider read, account creation, consume; no logger takes the handle |
| `src/nativespeaker/api/services/quota.py` | `QuotaService.charge`, one merged method | ✓ VERIFIED | One `charge()`, its own short session, commit on success / rollback on failure; all five fail-closed branches raise |
| `src/nativespeaker/api/resilience.py::Admitted` | Frozen token | ✓ VERIFIED | `@dataclass(frozen=True, slots=True)`; only `admission()` mints one |
| `src/nativespeaker/api/resilience.py::ResiliencePolicy.admission` | Breaker + slot, yields token | ✓ VERIFIED | `before_call()` then `gate.hold()` then `yield Admitted()` |
| `src/nativespeaker/api/resilience.py::LLMExecutionGate.hold` | Slot + semaphore | ✓ VERIFIED | Present, wrapping `_inflight_slot` |
| `tests/unit/error_tree.py` | Rehosted checker | ✓ VERIFIED | 73 L; `undeclared`, `tree_problems`, `assert_tree_total`; five subprocess controls + one positive control |
| `.planning/REQUIREMENTS.md` § Phase 37.5 | Dated amendment + D-08 table | ✓ VERIFIED | Amendment at `:20-28`; 21-function table at `:264-290`; one line-count typo at `:104` |
| `src/nativespeaker/api/auth/identity.py` | MUST NOT exist | ✓ VERIFIED | Absent |
| `src/nativespeaker/api/auth/create_user.py` | MUST NOT exist | ✓ VERIFIED | Absent |
| `src/nativespeaker/api/quota.py` | MUST NOT exist | ✓ VERIFIED | Absent |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `app/dependencies.py::get_identity` | `crud/identities.py::IdentitiesDB.resolve` | Depends chain | ✓ WIRED | Returns `schemas.auth.Identity`; 9 of 10 real routes declare `get_linked_identity`, `/health/ready` declares nothing |
| `routers/auth.py::create_user` | `services/auth.py::AuthService.complete` | `Depends(get_auth_service)` | ✓ WIRED | Handler takes no session, store or adapter; builds `CompletionResponse` from a returned domain fact |
| `app/dependencies.py::get_quota_service` | `services/quota.py::QuotaService` → `crud/grants.py::GrantsDB` | DI | ✓ WIRED | Same shape as `get_chat_service` |
| `ChatService` | `admission()` → charge → `ask_llm(…, admitted)` | `services/chats.py:91-93`, `:114-116` | ✓ WIRED | Charge is a plain statement between two plain statements, inside admission and outside the retry |
| `Admitted` | both `ainvoke` signatures | required parameter | ⚠️ PARTIAL | Enforced at **runtime** (`test_invoking_the_policy_without_a_token_raises` passes). The plan's stated `ty`-level enforcement does not hold: `uv run ty check` reports 90 diagnostics repo-wide, 5 in `src/`. `37.5-05-SUMMARY.md:383` honestly records the criterion as unsatisfiable on this tree; the baseline moved 93 → 90, so nothing regressed. |
| `app/error_handlers.py:56` | `class_answering_status` → `_family` | direct call | ✓ WIRED | Both survive in `errors.py`; live dispatch intact |
| `tests/unit/error_tree.py` | five `python -c` subprocesses | explicit `PYTHONPATH` | ✓ WIRED | `_fresh_interpreter` injects `_TESTS_ROOT`; the positive control asserts `returncode == 0` and empty stderr |

### Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
| --------- | ------- | ------ | ------ |
| Full suite, markers cleared | `uv run python -m pytest -q -m ""` | 1016 passed, 0 deselected | ✓ PASS |
| Lint | `uv run ruff check .` | All checks passed! | ✓ PASS |
| Docstring gate at 0 on five roots | `uv run python -m pytest -q tests/unit/test_docstring_bar.py` | 9 passed | ✓ PASS |
| Schema suite still collects (A-10 trap) | `pytest -m schema --collect-only -q` | 114 collected, 0 errors | ✓ PASS |
| Billing fix behaviour | `pytest -q tests/unit/test_quota_seam.py` | 20 passed | ✓ PASS |
| Restored security cases | `pytest -q <two node ids>` | 2 passed | ✓ PASS |
| Type check | `uv run ty check` | 90 diagnostics (5 in `src/`, pre-existing `Identity.user` optionality) | ✗ FAIL vs plan 05's literal criterion; no regression |
| DB access confined to `crud/` | `grep -rn "select(\|\.add(\|\.flush(\|\.exec(" src/ \| grep -v crud/` | zero lines | ✓ PASS |
| Adapter-method containment (D-06's deleted test) | `grep -rn get_user_provider_data src/` | only `auth/adapters.py:24`, `auth/firebase.py:64,147` | ✓ PASS (property holds; nothing enforces it) |
| Every deleted case named in a summary | 60 names cross-checked against `*-SUMMARY.md` | 0 unlisted | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` exist and no plan declares a probe. Step skipped.

### Requirements Coverage

| Requirement | Status | Evidence |
| ----------- | ------ | -------- |
| D-01 layering rule in `AGENTS.md` | ✓ SATISFIED | Present with all four exceptions |
| D-02 `auth/` = three seam files | ✓ SATISFIED | Exactly `adapters.py`, `firebase.py`, `jwt_verifier.py`, `__init__.py` |
| D-03 business logic → `services/auth.py` | ✓ SATISFIED | `AuthService.complete` + `create_user` + `_reject_existing_identity` |
| D-04 DB access → `crud/identities.py` | ✓ SATISFIED | `IdentitiesDB` with all four operations |
| D-05 bodies → `schemas/auth.py` | ✓ SATISFIED | Four bodies moved; `tables/auth.py` keeps only `AuthChallenge`, `AuthOperation` |
| D-06 `quota.py` → `services/quota.py`, `crud/grants.py` unchanged | ✓ SATISFIED | Top-level module deleted; `grants.py` byte-identical |
| D-07 never bill an unreached provider | ✓ SATISFIED | Behavioural tests on both admission rejections; validation order and no-refund preserved |
| D-08 delete steps, keep rules/boundaries | ✓ SATISFIED | Three deletions; `_sleep_if_positive` kept per A-04; 21-function table recorded |
| D-09 `assert_tree_total` out of production | ✓ SATISFIED | Zero hits in `src/`; live dispatch survives; controls non-vacuous |
| D-10 four dead classes deleted, `ChallengeRequired` kept | ✓ SATISFIED | Zero hits for the four; name-level assertion added |
| D-11 breaker stays, reasoning in `AGENTS.md` | ✓ SATISFIED | Both classes present; note carries A-02's corrected reasoning |
| D-12 rules applied to every file | ⚠️ PARTIAL | Docstring half gated at 0; comment half has ~20 residual items |
| D-13 test files cut proportionally, deletions listed | ✓ SATISFIED | 60/60 named; `tests/schema/` unpruned |
| D-14 ROADMAP amended | ✓ SATISFIED | Real goal, Delivered paragraph, real requirement line |
| D-15 no new flagged conflicts | ✓ SATISFIED | Count held at six; two adjacent items recorded, not counted |
| A-01 explicit `admission()` + `Admitted` | ✓ SATISFIED | Charge outside `attempt()`; no probe-then-charge, no guarded-inside-retry |
| A-02 `QueueFullError` is 503; D-11 note corrected | ✓ SATISFIED | Tests assert `(503, "service_unavailable")`; `AGENTS.md` carries no status-flip claim |
| A-03 the property is structural | ✓ SATISFIED | Charge sits outside `attempt()`'s `except Exception` |
| A-04 `_sleep_if_positive` kept | ✓ SATISFIED | Present and recorded as a correction to D-08 |
| A-05 six helpers classified | ✓ SATISFIED | All six in the `REQUIREMENTS.md` table |
| A-06 four rejections stay in `crud/` | ✓ SATISFIED | All four raise from `IdentitiesDB.resolve` |
| A-07 `ErrorResponse` stays in `errors.py` | ✓ SATISFIED | `errors.py:29`; `AGENTS.md` carve-out 1 |
| A-08 `tests/schema/` swept not pruned | ✓ SATISFIED | Zero case/assertion changes in that tree |
| A-09 keep two wire-contract cases | ✓ SATISFIED (deviation disclosed) | Both keeps present; a third kept and flagged in `37.5-08-SUMMARY.md` |
| A-10 schema collection gate | ✓ SATISFIED | 114 collected, zero errors |
| A-11 rehost, `PYTHONPATH`, fail-first | ✓ SATISFIED | Mechanism present; positive control would fail if it broke |
| A-12 `ChallengeRequired` name assertion | ✓ SATISFIED | `test_error_registry.py:201` |
| A-13 adapter cut before `src/` sweep | ✓ SATISFIED | `test_adapter_interfaces.py` 257→128; `adapters.py` docstring is 2 lines |
| A-14 PostgreSQL available | ✓ SATISFIED | Full suite ran with 0 deselected |
| A-15 pool finding recorded not fixed | ✓ SATISFIED | Recorded in `REQUIREMENTS.md` |
| A-16 flagged-conflict bookkeeping | ✓ SATISFIED | Count reconciled at six |
| A-17 line numbers re-verified | ✓ SATISFIED | Recorded per wave in `REQUIREMENTS.md:384` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `src/.../tables/grants.py` | 56, 71, 74 | Comment calls the database "a crud" | ⚠️ Warning | A comment that creates the misreading AGENTS.md says comments exist to prevent |
| `src/.../tables/purchases.py` | 27 | Same | ⚠️ Warning | Same |
| `tests/unit/test_users.py` | 8, 55 | Same | ⚠️ Warning | Same |
| `tests/unit/test_models.py` | 249, 272 | Same | ⚠️ Warning | Same |
| `tests/unit/test_identities_crud.py` | 1 | Same, in a module docstring | ⚠️ Warning | The docstring does not state what the module does |
| `tests/unit/test_quota_resolver.py` | 1 | Same | ⚠️ Warning | Same |
| `tests/unit/test_conflict_classification.py` | 1 | Same | ⚠️ Warning | Same |
| `tests/e2e/test_admission.py` | 163 | Same | ⚠️ Warning | Same |
| `tests/schema/conftest.py` | 55 | Same, in a **runtime string** | ⚠️ Warning | The only instance the phase record names; a raised `ValueError` message reaching a developer |
| `tests/unit/test_firebase_retry.py` | 63, 163 | Docstring cites `budgets.py`, retired in Phase 37 | ⚠️ Warning | Exactly the P-10 / `auth/adapters.py` defect class this phase set out to eliminate |
| `tests/unit/test_firebase_adapter.py` | — | Docstring cites an `adapters.py` preamble the sweep removed | ⚠️ Warning | Same class |
| `src/.../errors.py` | 147-148 | Two-line comment explaining an absence, no code below | ⚠️ Warning | Against "one line each" and "explains the line or lines below it" |
| `src/.../errors.py` | 72, 151, 267, 319, 328, 369 | Section-divider comments | ℹ️ Info | Structural labels; defensible, but not covered by any AGENTS.md carve-out |
| `tests/unit/test_create_user_precedence.py` | 173-174, 498-501 | Multi-line comments; the second describes where a deleted case moved | ⚠️ Warning | "Describes what lives somewhere else" |
| `tests/unit/test_create_user_body.py` | 83-84 | Multi-line comment restating `app/dependencies.py`'s design | ⚠️ Warning | "A rule enforced in another module" |
| `src/.../routers/auth.py` | 43-47 | The issuable-operation rule in a handler | ⚠️ Warning | `AGENTS.md` now states `routers/` is `Depends()`-only with no business logic; the handler also calls `ChallengesDB.issue()` directly, skipping `services/` |
| `src/.../tables/__init__.py` | 2-24 | 11 `schemas/` names re-exported from `tables` | ⚠️ Warning | Contradicts the goal's "bodies in `schemas/`" at the package surface. **No consumer uses them** — every `src/` and `tests/` importer of these names imports from `schemas.*` directly |
| `.planning/REQUIREMENTS.md` | 104 | "495 → 399 lines" | ℹ️ Info | `errors.py` measures 397; the same document says 397 at `:28` |

### The Five Recorded-Open Items — Independently Confirmed

Each was checked against the codebase rather than against the record.

1. **`tables/__init__.py` re-exports `schemas/` names — GENUINELY OPEN.** 11 names (`ChatRequest`, `ChatResponse`, `ExamplesResponse`, `MessageRequest`, `MessageResponse`, `AnalyzeInput`, `AnalyzeResponse`, `FollowUpInput`, `FollowUpResponse`, `Issue`, `RejectResponse`). `37.5-PATTERNS.md:339` demanded exactly **four** names be dropped (`ChallengeRequest`, `CompletionResponse`, `CreateUserRequest`, `PrepareResponse`) and all four **were** dropped, so plan 02's stated key link is satisfied literally. The other 11 predate the phase (present identically at `0817c6f`) and no plan claimed them. It is a genuine gap against the goal's layering clause, and it is dead surface — no importer in `src/` or `tests/` uses those re-exports. Routed to human decision, not counted as a failed truth.

2. **`test_foundation_calls_no_adapter_method_anywhere_in_src` — GENUINELY DELETED; the property still HOLDS.** `grep -rn get_user_provider_data src/` returns exactly three lines in two files: `auth/adapters.py:24` (the Protocol declaration) and `auth/firebase.py:64,147`. No third `src/` file names it. Nothing now fails if one starts to. The record's characterization is accurate and the ~25-line restoration cost it quotes is realistic.

3. **`test_app_wiring.py` negative control — GENUINELY ABSENT; three redundant resolver cases — CONFIRMED.** `TestEveryRouteIsAuthenticated` has no case injecting a route without `get_linked_identity`. It is not fully vacuous: `test_the_public_allowlist_is_exactly_the_readiness_probe` asserts set equality against `{"/health/ready"}` and would fail on an empty route list, and the real app has 10 `APIRoute`s. The three named cases in `test_quota_resolver.py` — `TestMultipleEffectiveGrants::test_two_effective_grants_raise`, `TestMissingUsageRow::test_a_grant_with_no_usage_row_raises`, `TestUnknownTier::test_a_missing_tier_row_raises` — each duplicate a sibling in their own class that makes the same `pytest.raises` plus an extra assertion. "Redundant" is more precise than "vacuous", but the record's judgment is sound.

4. **`tests/schema/conftest.py` "a crud" — CONFIRMED, and the record UNDERSTATES it.** The cited line is `:55`, not `:56`. More importantly this is **1 of 13** surviving occurrences across 9 files in `src/` and `tests/`. The defect predates the phase (present at `0817c6f`, including a docstring at `conftest.py:53` that plan 09 **did** fix), and plan 09's own commit `6dfefe2` is titled *"name the database, not the crud package, in two e2e docstrings"* — so the sweep recognized the class and cleared 2 of 15 instances. This is the substance of the D-12 gap above and is the one finding this verification adds beyond the record.

5. **`errors.py` at 397 lines — CONFIRMED.** 397 lines, 41 classes, down from 495 and 45. D-09 removed ~66 lines and D-10 four classes, which matches the ~110 the context projected. The ~250 figure was 37.4 A-01's projection carried in the **deferred/flagged** section, not a must-have of this phase. Not a gap; a standing observation. `REQUIREMENTS.md:104` states 399 where the other two records state 397.

### Human Verification Required

#### 1. `tables/__init__.py`'s 11 `schemas/` re-exports

**Test:** Decide whether these re-exports close as layering work or stand as accepted residue.
**Expected:** Either the 11 names leave `tables/__init__.py`'s `__all__` and import block — no consumer in `src/` or `tests/` uses them, so the change is mechanical — or the goal's "bodies in `schemas/`" clause gains an explicit facade carve-out in `AGENTS.md`.
**Why human:** This is a scope decision between the ROADMAP goal's layering clause and the phase's own recorded "did NOT resolve" list, not a defect a checker can settle.

#### 2. `routers/auth.py::issue_challenge`

**Test:** Read the handler at `src/nativespeaker/api/routers/auth.py:35-56` against `AGENTS.md`'s `routers/` clause.
**Expected:** Either the issuable-operation rule and the direct `ChallengesDB.issue()` call move behind a service, or `AGENTS.md` gains a carve-out naming this handler.
**Why human:** The handler retains one business rule, its own instant capture, and a direct `crud/` call — while `AGENTS.md`, written by this phase, states `routers/` is "`Depends()` only" with "no business logic, no queries". `37.5-02-SUMMARY.md:159` records it as "unchanged in substance" rather than as an exception, so the developer never chose between fixing it and exempting it. This item is **not** among the six the phase recorded as unresolved.

### Gaps Summary

The phase substantially achieved its goal. Every structural claim in the ROADMAP's Delivered paragraph is true in the codebase: the four new modules exist and are substantive, `auth/` is down to three seam files, the three deleted modules are gone with no dangling references, **every database call in `src/` is inside `crud/`**, the error-tree self-check is a test with non-vacuous controls, four dead exception classes are gone while `ChallengeRequired` gained a name-level assertion, and the billing fix is real and behaviourally proven — a full queue and an open circuit each answer 503 having spent nothing, while a request retried three times spends exactly one unit. All three stated gates pass exactly as claimed: 1016 passed with 0 deselected, ruff clean, and the docstring gate at 0 on all five roots with a working negative control.

One goal clause is short of its own wording. "Apply the docstring and comment rules to every file in `src/` and `tests/`" is complete and mechanically ratcheted on the docstring half — the AST scan finds zero `src/` docstrings that describe what lives elsewhere, and the gate asserts equality rather than an upper bound. The comment half has roughly twenty residual items. The largest is that **13 comments and docstrings across 9 swept files call the database "a crud"** — a pre-existing bad rename that plan 09 recognized and partly fixed under a commit titled *"name the database, not the crud package"*, clearing 2 of 15 instances. The phase record names only one of the 13. Two test docstrings still cite `budgets.py`, a module retired three phases ago — the exact defect class (`auth/adapters.py` naming a deleted module) that this phase's own context file used as its motivating example. Five multi-line comment blocks stand against AGENTS.md's "one line each", one of which describes where a deleted test case moved to.

Two layering residues stand outside that gap. `tables/__init__.py` still re-exports 11 `schemas/` names — genuinely open, correctly recorded, and dead surface no importer uses. `routers/auth.py::issue_challenge` retains a business rule and calls `crud/` directly, which the phase recorded as "unchanged in substance" but did not surface as an exception to the `routers/` rule it had just written into `AGENTS.md`; that one is not on the record's list.

The phase's own records are unusually honest and, in every case checked, accurate: plan 05's summary discloses that its `ty check` criterion is unsatisfiable on this tree rather than claiming a pass; plan 08's summary flags that it kept a third wire-contract case beyond A-09's two; `REQUIREMENTS.md` names the deleted architectural test as a real coverage loss and the resolver cases as redundant. The only inaccuracies found are a line-count typo (399 for 397 at `REQUIREMENTS.md:104`), a line-number drift (`conftest.py:55`, cited as `:56`), and the material understatement of the "crud" prose defect at 1 instance rather than 13.

None of this is a blocker for proceeding. No behaviour regressed, no client-visible contract moved, and no debt marker was left anywhere in `src/`, `tests/` or `AGENTS.md`.

---

_Verified: 2026-08-31T22:18:30Z_
_Verifier: Claude (gsd-verifier)_
