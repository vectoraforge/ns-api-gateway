---
phase: 37.5-machine-generated-code-refactoring-part-4
plan: 03
type: execute
wave: 3
depends_on: ["37.5-02"]
files_modified:
  - src/nativespeaker/api/services/quota.py
  - src/nativespeaker/api/quota.py
  - src/nativespeaker/api/services/chats.py
  - src/nativespeaker/api/services/__init__.py
  - src/nativespeaker/api/app/dependencies.py
  - tests/unit/test_quota_resolver.py
  - tests/unit/test_quota_seam.py
  - tests/unit/conftest.py
  - tests/unit/test_app_wiring.py
autonomous: true
requirements: [D-01, D-06, D-08, A-05, A-10]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`src/nativespeaker/api/quota.py` does not exist; `src/nativespeaker/api/services/quota.py` does"
    - "`consume_quota` and `charge_quota` are one method on `QuotaService`, not two functions where one calls the other"
    - "`test_quota_resolver.py`'s 34 resolver cases pass against the merged method — the positive control that the merge lost no branch"
    - "`crud/grants.py` is byte-identical to its pre-phase state; `GrantsDB` keeps its name (renaming to `QuotaDB` was offered and declined)"
    - "The charge still opens its own short session, commits on success and rolls back on failure, so no grant or usage row lock is held across the provider round trip"
    - "The service's own validation rejections still precede the charge — unsupported language, chats limit, chat-not-found, messages limit"
    - "Every fail-closed branch still raises rather than minting: no effective grant, multiple effective grants, missing usage row, unknown tier, exhausted allowance"
    - "`grep -rn 'api\\.quota\\b' src tests` returns zero lines"
    - "`.venv/bin/python -m pytest -m schema --collect-only` collects with zero errors (A-10)"
  artifacts:
    - src/nativespeaker/api/services/quota.py
  key_links:
    - "`get_quota_service` → `QuotaService` → `GrantsDB` — the DI shape `get_chat_service` established"
    - "`ChatService` holds the `QuotaService`; plan `37.5-05` moves only *where* it calls it, never *whether*"
---

<objective>
Move quota out of the top level and into the service layer, merging its two halves into one.

Purpose: the top-level `quota.py` is D-01's second violation. `crud/grants.py::GrantsDB` already holds
every database call quota makes, so only the business half moves — and D-08's rule dissolves the
`consume_quota`/`charge_quota` split, which is the same subject written twice.

Output: `services/quota.py::QuotaService` with one charge method; the top-level module deleted;
`GrantsDB` untouched.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md
@.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-PATTERNS.md
@AGENTS.md
</context>

<preconditions>
- Plan `37.5-02` has landed: `services/auth.py` exists and `app/dependencies.py` already carries
  `get_auth_service`, so this plan adds an accessor beside two others rather than inventing the shape.
- `crud/grants.py` is in scope for reading only. If this plan finds itself editing it, the plan is
  wrong: D-06 says the CRUD half was already satisfied.
</preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `services/quota.py::QuotaService`, one merged charge method</name>
  <files>src/nativespeaker/api/services/quota.py, src/nativespeaker/api/quota.py, src/nativespeaker/api/services/chats.py, src/nativespeaker/api/services/__init__.py, src/nativespeaker/api/app/dependencies.py</files>
  <read_first>
    - `src/nativespeaker/api/quota.py` (84 lines) — the module being moved and merged; every branch in `consume_quota` must survive
    - `src/nativespeaker/api/services/chats.py` — the analog for the class shape, and the current call sites at `:93` and `:117`
    - `src/nativespeaker/api/crud/grants.py` — read only; `GrantsDB` is unchanged by this phase
    - `src/nativespeaker/api/app/dependencies.py` — `get_chat_service` and the `get_auth_service` plan `37.5-02` added
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-PATTERNS.md:150-195` — the pattern assignment for this file
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md:69-78` (D-06), `:107-134` (D-08's merge)
  </read_first>
  <behavior>
    - No effective grant raises `QuotaExceededError` and logs the closed-set branch label
    - More than one effective grant raises `MultipleEffectiveGrantsError` — a tripwire, never a
      tie-break
    - A missing usage row raises `MissingUsageRowError` and never mints one
    - A period mismatch resets the counter in the same transaction as the charge, so a reset never
      commits uncharged
    - A missing tier row raises `UnknownTierError` — not a zero allowance and not an unbounded one
    - A used-up allowance raises before the increment, so a refused request is never charged
    - A successful charge increments by exactly one and stamps `updated_at` from the request's captured
      instant, never from a clock read
    - The grant rows are locked before the usage row, always
  </behavior>
  <action>
    Create `src/nativespeaker/api/services/quota.py` holding `QuotaService`, shaped like
    `services/chats.py::ChatService`: the session factory is a constructor argument, and the class
    exposes one public method that spends one unit of a user's allowance or raises.

    Merge `quota.py:22 consume_quota` and `:72 charge_quota` into that one method, per D-08 — today one
    opens a session and calls the other, which is the same subject split in two. The merged body opens
    its own short session, runs the resolution and the spend inside it, commits on success and rolls
    back on any exception. That short session is the property `SHARED-INVARIANTS.md` § Locks depends on:
    no grant or usage row lock is held across the provider round trip. It is not an implementation
    detail and must not be replaced by the request-scoped session.

    Every branch listed in the behaviour block moves unchanged, including its log event and its
    closed-set `branch` label. Labels come from a fixed set only — never an id, never a raw path.

    Delete `src/nativespeaker/api/quota.py`.

    Add `get_quota_service` to `app/dependencies.py` beside `get_chat_service` and `get_auth_service`,
    and have `get_chat_service` construct `ChatService` with the `QuotaService` rather than with a
    module-level function import. `ChatService` calls `self.quota_service.<charge method>(...)` at the
    two sites now at `services/chats.py:93` and `:117` — the same two visible points, in the same
    order relative to the four validation rejections. This plan moves the call's *owner*, not its
    position; plan `37.5-05` moves its position.

    Add `QuotaService` to `services/__init__.py`.

    Do not touch `crud/grants.py` and do not rename `GrantsDB`.
  </action>
  <acceptance_criteria>
    - `test -f src/nativespeaker/api/quota.py` is false; `test -f src/nativespeaker/api/services/quota.py` is true
    - `git diff --stat HEAD~1 -- src/nativespeaker/api/crud/grants.py` shows no change
    - `grep -rn 'api\.quota\b' src tests` returns zero lines (grep scoped to `src` and `tests`)
    - `grep -c 'async def' src/nativespeaker/api/services/quota.py` returns 1 — the two halves are one method, not two
    - `grep -n 'session_factory' src/nativespeaker/api/services/quota.py` shows the short session is still opened from a factory inside the charge, not taken as a parameter
    - `.venv/bin/python -c "import nativespeaker.api.app.main"` succeeds
    - `.venv/bin/python -m pytest -q` passes
    - `.venv/bin/python -m pytest -m schema --collect-only -q` collects with zero errors
    - `.venv/bin/ruff check` and `.venv/bin/ty check` pass
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import nativespeaker.api.app.main" && .venv/bin/python -m pytest -q && .venv/bin/python -m pytest -m schema --collect-only -q && .venv/bin/ruff check && .venv/bin/ty check</automated>
  </verify>
  <done>`QuotaService` owns the merged charge, the top-level module is gone, `GrantsDB` is untouched, and the DI accessor sits beside its two siblings.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the resolver suite and the seam fixtures at the merged method</name>
  <files>tests/unit/test_quota_resolver.py, tests/unit/test_quota_seam.py, tests/unit/conftest.py, tests/unit/test_app_wiring.py</files>
  <read_first>
    - `tests/unit/test_quota_resolver.py` (346 lines, 34 cases) — the largest single test rewrite in the phase; its stub-session harness at `:31-104` is what must be re-pointed
    - `tests/unit/conftest.py:117-172` — the `charge_calls` fixture (`:132-142`) that monkeypatches the charge, and the `service` fixture (`:144-157`)
    - `tests/unit/test_quota_seam.py:178-225` — `TestAServiceRejectionPrecedesAnyCharge`, six cases that are also the positive control that the charge fires at all
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:944-962` (§3.3, the disposition table for each test file)
  </read_first>
  <action>
    `test_quota_resolver.py`'s 34 cases target `consume_quota`, which ceases to exist as a separate
    function. Re-point the stub-session harness at the merged method and keep every case as written.
    This is a subject re-point, not a prune: D-13 forbids weakening an assertion in place, and any case
    that genuinely cannot be re-pointed is deleted outright and listed in this plan's summary.

    Passing all 34 against the merged method is the positive control D-06 needs — it is what proves the
    merge lost no branch. If a branch has no case after the re-point, that is the finding, not a
    rounding error.

    Update `conftest.py`'s `charge_calls` fixture to patch the charge on the `QuotaService` the
    `ChatService` holds, rather than a module attribute on `services.chats`. Update the `service`
    fixture to construct `ChatService` with a `QuotaService` (or its double). Keep the recording
    behaviour identical — the fixture unblocks about ten cases across two files and a fixture that
    lies is the failure mode `37.4-VERIFICATION.md` recorded seven times.

    Update `test_quota_seam.py` only where the patch target or the constructor changed. Do not touch
    `TestAnExhaustedAllowanceAnswers429AndNeverReachesTheProvider`, `TestNoSessionIsHeldAcrossTheProviderCall`
    or `TestTheAcceptedRegression` in this plan — plan `37.5-05` owns them, and editing them here would
    split one behaviour change across two waves.

    Update `test_app_wiring.py` if it enumerates DI accessors; the new `get_quota_service` is a third
    service accessor.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/unit/test_quota_resolver.py` passes with **34** cases collected — the count is the control that no branch lost its case
    - `.venv/bin/python -m pytest -q tests/unit/test_quota_seam.py` passes with its pre-plan case count (16)
    - `git diff -U0 HEAD~1 -- tests/unit/test_quota_seam.py | grep -c '^+.*assert'` shows only additions that are re-points, and any case deleted is listed in the summary
    - `.venv/bin/python -m pytest -q` passes
    - `.venv/bin/python -m pytest -m schema --collect-only -q` collects with zero errors
    - `.venv/bin/ruff check` and `.venv/bin/ty check` pass
    - The summary lists every deleted test case, or states explicitly that none was deleted
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_quota_resolver.py tests/unit/test_quota_seam.py && .venv/bin/python -m pytest -q && .venv/bin/python -m pytest -m schema --collect-only -q</automated>
  </verify>
  <done>All 34 resolver cases pass against the merged method, the seam fixtures construct the new shape, and the deleted-case list (or its absence) is stated.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Produced by this plan:

| Artifact | Kind | Decision |
|---|---|---|
| `src/nativespeaker/api/services/quota.py` | new module | D-06 |
| `QuotaService` | new service class, one merged charge method | D-06, D-08 |
| `get_quota_service` | new DI accessor | D-01, D-06 |
| Deleted: `src/nativespeaker/api/quota.py`, `consume_quota` as a separate function | — | D-06, D-08 |
| Unchanged by design: `crud/grants.py`, `GrantsDB` | — | D-06 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ChatService` → `QuotaService` | The only place an allowance is resolved and spent |
| `QuotaService` → its own short session | A second database connection per in-flight chat request, opened and closed inside the charge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.5-03-01 | Elevation of privilege | the merged charge method | **high** | mitigate | A branch lost in the merge is free service or an unbounded allowance. Every fail-closed branch is named in the `<behavior>` block, and the 34-case count on `test_quota_resolver.py` is the gate. |
| T-37.5-03-02 | Denial of service | the short session | medium | mitigate | Replacing the factory-opened short session with the request-scoped one would hold a grant lock across the provider call, violating `SHARED-INVARIANTS.md` § Locks. Grepped, and pinned by `test_quota_seam.py:290-330`'s event ordering. |
| T-37.5-03-03 | Tampering | log labels | low | accept | Labels are a closed set of branch names carried over unchanged; no id and no raw path is logged. Accepted as unchanged behaviour. |
| T-37.5-03-04 | Denial of service | `db.pool_size` | medium | transfer | A `POST /chats` holds two connections and `pool_size` defaults to 5 with no overflow, so three concurrent chat posts exhaust the pool **today** (A-15). This plan does not create it and does not fix it; it is recorded in plan `37.5-10` as a finding for the developer. |
| T-37.5-03-SC | Tampering | package installs | high | mitigate | No package installed by this phase. |
</threat_model>

<verification>
- `.venv/bin/python -c "import nativespeaker.api.app.main"` after every commit
- `.venv/bin/python -m pytest -q` after every commit; `-m schema --collect-only` after the import-path change
- `.venv/bin/ruff check`, `.venv/bin/ty check`
- 34 resolver cases collected and passing
</verification>

<success_criteria>
Quota lives in `services/`, its two halves are one method, and every fail-closed branch still has a
passing case.
</success_criteria>

<output>
Create `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-03-SUMMARY.md` when done.
List every deleted test case, or state that none was deleted. Record the resolver case count.
</output>
