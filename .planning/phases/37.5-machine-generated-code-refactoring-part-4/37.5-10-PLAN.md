---
phase: 37.5-machine-generated-code-refactoring-part-4
plan: 10
type: execute
wave: 9
depends_on: ["37.5-09"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [D-08, D-11, D-14, D-15, A-01, A-02, A-04, A-05, A-15, A-16, A-17, P-01, P-02, P-03, P-04, P-05, P-06, P-07, P-08, P-09, P-10, P-11]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/ROADMAP.md`'s Phase 37.5 entry carries a real goal and a real requirement line — no placeholder text survives"
    - "`.planning/REQUIREMENTS.md` carries a dated Phase 37.5 amendment in the established 37.1–37.4 style, naming what the phase did NOT resolve"
    - "The flagged-conflict count and the divergence set agree, or the disagreement is stated: the `limits` override is recorded as an override and is not one of the counted conflicts (A-16)"
    - "No new conflict against `SHARED-INVARIANTS.md` is claimed or resolved; D-15's verdict is restated with A-07's carve-out and A-16's bookkeeping item as the only adjacent findings"
    - "All twenty-one private single-caller functions in `src/` are recorded with a verdict and a ground, including the six the phase's own text never classified (A-05)"
    - "`_sleep_if_positive`'s keep is recorded as a correction to D-08's deletion list, with the reason (A-04)"
    - "D-11's record carries the corrected reasoning: the status-flip argument is withdrawn as factually wrong and the surviving arguments are named (A-02 / P-01)"
    - "The database pool finding is recorded and explicitly not fixed, with the one-line change named for the developer to choose (A-15)"
    - "`ChallengeRequired`'s zero-name-references-but-load-bearing discrepancy is recorded, so the wrong deletion offer is not made a second time (P-05, A-12)"
    - "The A-17 line-number re-verification for the phase is recorded, naming what drifted"
  artifacts:
    - ".planning/REQUIREMENTS.md § Phase 37.5 amendment"
    - "The D-08 classification table (21 functions, verdict and ground each)"
  key_links:
    - "The amendment is what a later reader meets instead of discovering the divergence; the 37.1–37.4 convention is that it names what it did not resolve"
---

<objective>
Close the phase's records: the roadmap entry that was still a placeholder when the discussion opened,
the dated requirements amendment, and the three records this phase exists to leave behind — D-08's
exhaustive classification, D-11's corrected reasoning, and the findings the phase deliberately did not
act on.

Purpose: the pattern was broken by agents who had no written rule to follow, and the durable half of
this phase is the writing. `AGENTS.md` carries the rules; this plan carries the reasoning, so the
questions this phase settled are not re-opened a fifth time.

Output: `ROADMAP.md` and `REQUIREMENTS.md` amended; the classification table, the corrected resilience
reasoning, and the unactioned findings all on record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<preconditions>
- Every code plan has landed and its summary exists. This plan reports what happened, so it must not run
  before there is something to report.
- Editing `ROADMAP.md`, `REQUIREMENTS.md` and `STATE.md` is scoped replacement only. A whole-file
  rewrite of any of them destroys entries outside the edit window; these files carry eighteen phases and
  four prior amendments.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Amend the roadmap entry and write the dated requirements amendment</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md:429-438` — the Phase 37.5 entry; its goal was still a placeholder and its requirement line still read as unset when the discussion opened
    - `.planning/ROADMAP.md:400-428` — the Phase 37.4 entry, for the established shape of a completed phase's entry
    - `.planning/REQUIREMENTS.md:10-20` — the four prior dated amendments (37.1, 37.2, 37.3, 37.4) whose style this one follows, and `:20` where the flagged-conflict count is stated
    - `.planning/REQUIREMENTS.md:48` — where the `limits` override is already recorded, as an override rather than as a counted conflict
    - `.planning/REQUIREMENTS.md:73` — where the two wire-contract divergences plan `37.5-08` kept a test for are recorded as losses
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` — read § Errors, § Rate limits, § Locks and § The barrier; the phase claims no new conflict and that claim must be checked, not assumed
    - Every `37.5-NN-SUMMARY.md` in this phase directory
  </read_first>
  <action>
    Amend `ROADMAP.md`'s Phase 37.5 entry with scoped edits only. Give it the real goal — the layered
    architecture restored and written down, the docstring and comment rules applied repo-wide,
    step-functions inlined, the startup self-check moved to a test, and the quota charge moved so that a
    request that never reached the provider is not billed — and a real requirement line naming the
    decision and addendum set (D-01…D-15, A-01…A-17), since this phase has no REQ-ID prefix. Mark the
    plans complete in the established form.

    Write a dated Phase 37.5 amendment into `REQUIREMENTS.md`, in the same register as the four before
    it. It states what the phase did, which requirements it amended and which it left alone, and — per
    the 37.1–37.4 convention — **what it did not resolve**. Include:

    - The eleven premise corrections this phase's research made against its own context file, by number
      and in one line each. Three changed the shape of the plan: the queue-full rejection has always
      been a 503 and not a 429; the property protecting a quota rejection from reading as a provider
      outage is structural rather than located at a line; and the replacement test for the error-tree
      self-check already existed, so the work was rehosting a checker, not writing a test.
    - **A-16's bookkeeping item.** The binding specification mandates the `limits` library, and this
      project has none — but that divergence is already on record as an override from Phase 35, not as
      one of the counted flagged conflicts. The count at `:20` and the divergence set therefore
      disagree. Reconcile the two, or state the disagreement explicitly. Do not add it as a new conflict:
      it is pre-existing and recorded.
    - **D-15's verdict, checked rather than assumed.** No new conflict was found. Two adjacent items
      stand: the layering rule, read literally, would move the error response shape out of the one module
      the specification requires to own it — which is why `AGENTS.md` carries a carve-out rather than a
      conflict; and the `limits` item above. If reading the specification now finds a conflict this
      phase's reading missed, flag it. Never resolve one silently.
    - The two wire-contract divergences whose only executable trace is now the two cases plan `37.5-08`
      kept, cross-referenced to `:73`.
    - The consequence D-09 accepted: a defective error tree no longer refuses to boot, and deploying
      without running the tests turns a refused start into served 500s.
  </action>
  <acceptance_criteria>
    - `grep -n 'to be planned\|Requirements.*TBD' .planning/ROADMAP.md` returns no line inside the Phase 37.5 entry
    - `grep -c 'Phase 37.5' .planning/REQUIREMENTS.md` returns at least one — the dated amendment exists
    - `grep -n 'P-01\|P-06\|P-09' .planning/REQUIREMENTS.md` returns lines inside the new amendment — the premise corrections are recorded by number
    - `grep -n 'limits' .planning/REQUIREMENTS.md` shows the reconciliation, and the flagged-conflict count at `:20` either matches the divergence set or the mismatch is stated in the amendment
    - `git diff --stat .planning/ROADMAP.md` shows changes confined to the Phase 37.5 entry — no other phase entry is touched
    - `git diff .planning/REQUIREMENTS.md | grep -c '^-'` shows only additions and the count line, if it changed — no prior amendment was rewritten
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(sed -n '/#### Phase 37.5/,/#### Phase 38/p' .planning/ROADMAP.md | grep -i 'to be planned\|TBD')" && grep -q 'Phase 37.5' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The roadmap entry carries a real goal and requirement line, and the requirements amendment is dated, in style, and names what it did not resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Record D-08's exhaustive classification and D-11's corrected reasoning</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:775-855` (§2, the complete twenty-one-function classification with each verdict's ground, the six the context file never reached, the public single-caller functions D-08 does not reach, and why the first sweep missed what it missed)
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md:107-134` (D-08 as written), `:503-516` (A-04 and A-05, the developer's corrections)
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md:180-206` (D-11 as written), `:485-499` (A-02, the withdrawn argument)
    - `AGENTS.md` — the rules as plan `37.5-01` wrote them, so this record does not duplicate them
  </read_first>
  <action>
    Two records, both in `REQUIREMENTS.md` under the Phase 37.5 amendment. Neither restates a rule that
    now lives in `AGENTS.md`; both record the reasoning, which is the part that stops a question being
    re-opened.

    **The D-08 classification.** All twenty-one module-private single-caller definitions in `src/`, each
    with its verdict and its one-line ground. Three deletions landed — the completion step that forwarded
    every argument and added a timestamp, the insert step that was all that remained of its caller after
    one check, and the two-function quota split that merged. Eighteen are keeps. Record specifically:

    - `_sleep_if_positive` is a **keep**, correcting D-08's own deletion list, which contradicted D-08's
      own rule: it is the callable tenacity's `sleep=` requires, and a zero-length backoff issuing no
      sleep at all is real behaviour a test pins. The deletions are three, not four.
    - The six the phase's own text never classified are all keeps, each with its ground: the credential
      resolver whose name is its contract, the threadpool callable that is also the try/except boundary,
      the cache-key rule that states the shared-sentinel behaviour once, the transient classifier, the
      retry predicate, and the acquire/release critical section that is the sole raise site of the
      queue-full rejection. Leaving no record of what was examined is precisely what the first sweep
      failed to do, which is why this was a second pass.
    - The recursive family walk is a keep with a distinct ground: after the self-check left, it has one
      caller and reads as a step. It is recursive, so it cannot be inlined, and attempting it produces a
      wrong walk. That is now a line in `AGENTS.md` as well.
    - Why the first sweep missed nine of these: its two commits were cohesion merges between modules, not
      a general single-caller inline, so its actual subject was module boundaries.

    **D-11's corrected reasoning.** The circuit breaker and the execution gate stay. Record the surviving
    arguments — the retry loop was already replaced by a library that is in the tree; that library bounds
    one request and nothing across requests; nothing installed replaces a circuit breaker, so finishing
    the idea means a new dependency; and without the breaker the client is told to retry in two seconds
    when the provider is down. Record explicitly that the status-flip argument is **withdrawn as
    factually wrong**: both admission rejections have always been service-unavailable answers, because
    the queue-full class inherits its status and declares none of its own. Record that the developer's
    opening expectation was correct as a reading of the specification, which is why the question keeps
    coming back — the specification mandates a library this project deleted.
  </action>
  <acceptance_criteria>
    - The amendment contains a table or list of exactly 21 entries, each with a verdict and a ground
    - `grep -n '_sleep_if_positive' .planning/REQUIREMENTS.md` returns a line recording it as a keep and as a correction to D-08's list
    - `grep -in 'withdrawn' .planning/REQUIREMENTS.md` returns a line in the D-11 record naming the status-flip argument
    - `grep -in 'recursive' .planning/REQUIREMENTS.md` returns the family-walk ground
    - The three deletions and the eighteen keeps sum to 21, and the summary states that sum
    - `git diff .planning/REQUIREMENTS.md` touches only the Phase 37.5 amendment section
  </acceptance_criteria>
  <verify>
    <automated>grep -q '_sleep_if_positive' .planning/REQUIREMENTS.md && grep -qi 'withdrawn' .planning/REQUIREMENTS.md && grep -qi 'recursive' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>All 21 functions are on record with verdicts and grounds, `_sleep_if_positive`'s keep is recorded as a correction, and D-11's reasoning is the corrected reasoning.</done>
</task>

<task type="auto">
  <name>Task 3: Record what the phase deliberately did not do, and update project state</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md:574-598` (A-14 through A-17), `:430-455` (the deferred list)
    - `.planning/STATE.md:95-115` — the pending-todo section and its established form
    - Every `37.5-NN-SUMMARY.md` — the deleted-case lists, the control observations and the behaviour deltas each plan recorded
    - `.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-REVIEW.md` — the warnings and info items; several sat in code this phase moved or swept, and the rest stay open
  </read_first>
  <action>
    Record the findings this phase surfaced and deliberately did not act on, each with enough detail that
    the developer can decide without re-deriving it:

    - **The database pool.** The pool defaults to five connections with no overflow, and a chat post
      already holds two, so three concurrent chat posts exhaust it **today**. The new admission seam does
      not create this, but it moves the stall inside the LLM permit, so a pool wait now also holds a
      provider permit. Name the one-line configuration change that would fix it and state that the
      developer owns the choice.
    - **The three behaviour deltas the admission seam introduced**, with their costed values, copied from
      plan `37.5-05`'s summary: the slot held across the retry sequence rather than per attempt, the
      breaker consulted once per request rather than once per attempt, and the charge's round trip now
      inside the permit.
    - **The name-versus-behaviour discrepancy on `ChallengeRequired`.** It has zero references by name
      anywhere outside its own definition and is load-bearing: it is the only class answering a bare
      framework 409, and without it that becomes an unmapped status and a 500. A name-grep reads it as
      dead, which is exactly what produced the wrong deletion offer during this phase's discussion. Plan
      `37.5-04` added a name-level assertion; record why.
    - **The consolidated deleted-test-case list.** Gather the per-plan lists from the summaries into one
      place with their totals, so the developer reviews one list rather than six. State the total lines
      removed from `tests/` against the phase's starting figure.
    - **What stays open.** The warnings from the prior phase's review that fell outside moved or swept
      code; the error module's remaining size against the projection an earlier phase made; the deferred
      items the context file listed. Whatever survived this phase's rewrites stays open, as before.
    - **The A-17 re-verification.** State which line-number citations were re-checked after each wave and
      which drifted. Line numbers are re-verified after each wave; facts are what did not survive.

    Then update `.planning/STATE.md` with scoped edits: the phase's position, its decisions carried
    forward, and any new pending todo. Do not rewrite the file; it carries eighteen phases of accumulated
    context and a plan-counter note that has been corrected twice.
  </action>
  <acceptance_criteria>
    - `grep -in 'pool_size' .planning/REQUIREMENTS.md` returns the recorded pool finding with the one-line change named
    - `grep -in 'ChallengeRequired' .planning/REQUIREMENTS.md` returns the name-versus-behaviour record
    - The consolidated deleted-case list is present with a total, and its total matches the sum of the per-plan lists in the summaries
    - `grep -n 'current_phase' .planning/STATE.md` reflects the phase's completion state
    - `git diff --stat .planning/STATE.md` shows a scoped edit, not a rewrite: the accumulated-context and performance-metrics sections are intact
    - `.venv/bin/python -m pytest -m "" -q` passes against PostgreSQL — the phase's final gate
    - `.venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py` passes with every baseline at 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -m "" -q && .venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py && .venv/bin/ruff check && .venv/bin/ty check</automated>
  </verify>
  <done>Every finding the phase surfaced and did not act on is recorded with enough detail to decide from, the deleted-case lists are consolidated, and project state reflects the phase's close.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Produced by this plan:

| Artifact | Kind | Decision |
|---|---|---|
| `.planning/ROADMAP.md` § Phase 37.5 — real goal and requirement line | record | D-14 |
| `.planning/REQUIREMENTS.md` § Phase 37.5 amendment (dated) | record | D-14, D-15, A-16 |
| The D-08 classification table — 21 functions, 3 deletions, 18 keeps, each with a ground | record | D-08, A-04, A-05 |
| D-11's corrected reasoning, with the status-flip argument withdrawn | record | D-11, A-02, P-01 |
| The database-pool finding, recorded and not fixed | record | A-15 |
| The consolidated deleted-test-case list | record | D-13 |
| The A-17 line-number re-verification for the phase | record | A-17 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan edits planning records only. The risk is to the project's memory, not to the running service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.5-10-01 | Repudiation | `.planning/REQUIREMENTS.md` | medium | mitigate | A whole-file rewrite destroys four prior amendments and the flagged-conflict record a later reader depends on. Scoped edits only, gated by a diff check that no prior amendment line was removed. |
| T-37.5-10-02 | Repudiation | the D-11 record | medium | mitigate | A record carrying the withdrawn 429/503 fact loses its authority on first re-examination, and the breaker question returns a fifth time. The withdrawal is explicit and grepped. |
| T-37.5-10-03 | Repudiation | the `ChallengeRequired` discrepancy | **high** | mitigate | Without the record, a future reference-grep reads a load-bearing class as dead and deleting it turns a bare framework 409 into a 500. Recorded here and asserted in code by plan `37.5-04`. |
| T-37.5-10-04 | Denial of service | the database pool finding | medium | transfer | Recorded, not fixed. The developer owns the decision; the one-line change is named so it can be made without re-deriving the arithmetic. |
| T-37.5-10-05 | Repudiation | `.planning/STATE.md` | low | mitigate | Eighteen phases of accumulated context and a twice-corrected counter note. Scoped edits, gated by a diff-stat check. |
| T-37.5-10-SC | Tampering | package installs | high | mitigate | No package installed by this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -m "" -q` against PostgreSQL — the phase's final gate, 0 deselected
- `.venv/bin/python -m pytest -m schema --collect-only -q` — zero errors
- `.venv/bin/python -c "import nativespeaker.api.app.main"` succeeds
- `.venv/bin/ruff check`, `.venv/bin/ty check`
- The docstring gate at 0 across every root
- Diff-scope checks on all three planning files
</verification>

<success_criteria>
The phase's reasoning is on record where a later reader meets it, the roadmap no longer carries a
placeholder, and every question this phase settled has its grounds written down rather than its answer
alone.
</success_criteria>

<output>
Create `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-10-SUMMARY.md` when done.
State the 3-deletions/18-keeps sum, the consolidated deleted-case total and the lines removed from
`tests/`, and confirm the final gate results.
</output>
