---
phase: 37.5-machine-generated-code-refactoring-part-4
plan: 07
type: execute
wave: 6
depends_on: ["37.5-06"]
files_modified:
  - tests/unit/test_error_registry.py
  - tests/unit/test_exception_handlers.py
  - tests/unit/test_rejection_vocabulary.py
  - tests/unit/test_error_contract.py
  - tests/unit/error_tree.py
  - tests/unit/test_challenge_ids.py
  - tests/unit/test_challenge_endpoint.py
  - tests/unit/test_jwt_security.py
  - tests/unit/test_jwks_offload.py
  - tests/unit/test_auth_package_shape.py
  - tests/unit/test_docstring_bar.py
autonomous: true
requirements: [D-12, D-13]
user_setup: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every file in this set clears the three-line docstring bar; the gate's `tests/unit` baseline drops by this set's share and the drop is reported"
    - "No assertion is weakened in place: `git diff -U0 -- tests/ | grep '^+.*assert'` is empty for every pure-deletion commit in this plan"
    - "Every deleted test case is named in the plan summary for the developer to review"
    - "The anti-oracle control survives: the challenge rejections' indistinguishability case is not deleted"
    - "The JWT anti-oracle case survives, and every arm it makes redundant is deleted rather than weakened"
    - "The five error-tree totality properties still each have a case with unchanged assertion text"
    - "`test_auth_package_shape.py`'s 14-line docstring, which describes the history of a deleted ratchet, is gone; its measured triple and its `TestTheMeasurementFires` control are untouched"
    - "The full suite is green against PostgreSQL with 0 deselected"
  artifacts:
    - "tests/unit/test_docstring_bar.py (this set's share of the `tests/unit` baseline retired)"
  key_links:
    - "`test_rejection_vocabulary.py`'s `EVENT_NAMES` and `CONSTRUCTOR_ARGUMENTS` literals are ratchets; a prune that touches them must keep `:104` and `:149` failing on a stale entry"
---

<objective>
Sweep the error, challenge and JWT test files against `AGENTS.md`'s rules, and cut three of the four
clusters D-13 reaches whose subjects this plan owns.

Purpose: 13,271 lines of the old register is what the next agent reads as precedent, which is why
leaving `tests/` exempt was rejected. `test_jwt_security.py` is 587 lines and 53 cases over twelve
classes for a 167-line module — the highest case-to-behaviour ratio in the repository.

Output: eleven files swept; the challenge, JWT and error clusters cut to the behaviour that matters,
with every deletion named.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md
</context>

<preconditions>
- Plan `37.5-04` has landed, so the error files already carry the rehosted checker and the D-10 prunes;
  this plan sweeps and cuts what remains rather than colliding with that work.
- Plan `37.5-06` has landed, so the gate's `src` baseline is already 0 and this plan lowers only the
  test-side keys it owns.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: The error cluster — sweep four files and cut what re-asserts itself</name>
  <files>tests/unit/test_error_registry.py, tests/unit/test_exception_handlers.py, tests/unit/test_rejection_vocabulary.py, tests/unit/test_error_contract.py, tests/unit/error_tree.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `AGENTS.md` — the rules being applied
    - `tests/unit/test_error_registry.py` (385 lines) — including `TestRetired401Code`, five cases for a string that has not existed for several phases
    - `tests/unit/test_exception_handlers.py` (551 lines, 33 docstrings, 6 comment lines)
    - `tests/unit/test_rejection_vocabulary.py` (288 lines) — two over-long docstrings; `:104` and `:149` are ratchet controls and stay
    - `tests/unit/test_error_contract.py` (137 lines)
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1164-1195` (§6.2 cluster 8), `:1196-1263` (§7's per-file counts)
  </read_first>
  <action>
    Apply the docstring and comment rules to all five files, reading each; no codemod.

    Then cut on D-13's criterion. Cluster 8 is 1,361 test lines against 571 source lines — a 2.4 ratio,
    the least out of proportion of the eight, so the cut here is narrow and specific rather than broad:
    `TestRetired401Code`'s five cases assert the absence of a string that has not existed for several
    phases, and `test_error_contract.py` overlaps the registry file on the response-shape assertions.

    Four things in this set are controls and are not targets, in any reading:
    - `test_rejection_vocabulary.py:104` and `:149` — the two ratchets that catch a stale `EVENT_NAMES`
      member or a stale `CONSTRUCTOR_ARGUMENTS` entry.
    - `test_rejection_vocabulary.py:263` — the declaring-intermediate acceptance, the case that catches
      an over-simplified rehost of the tree checker.
    - The seven fresh-interpreter defect cases in `test_error_registry.py` — they are the negative
      controls for the five totality properties.
    - `test_error_registry.py:203, 208, 214, 220, 224` — live dispatch, not self-check.

    Every case is kept as written or deleted outright. Name every deletion in the summary.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/unit/test_error_registry.py tests/unit/test_exception_handlers.py tests/unit/test_rejection_vocabulary.py tests/unit/test_error_contract.py` passes
    - `grep -c 'class TestRetired401Code' tests/unit/test_error_registry.py` returns 0
    - `.venv/bin/python -m pytest -q -k "declaring_intermediate or spells_exactly_the_recorded_event_names"` passes — the two controls survive
    - `git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert'` is empty
    - `.venv/bin/python -m pytest -q` passes
    - The summary names every deleted case
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_error_registry.py tests/unit/test_exception_handlers.py tests/unit/test_rejection_vocabulary.py tests/unit/test_error_contract.py && .venv/bin/python -m pytest -q && test -z "$(git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert')"</automated>
  </verify>
  <done>Five files swept, the narrow error-cluster cut made, all four control sets intact, every deletion named.</done>
</task>

<task type="auto">
  <name>Task 2: The challenge cluster — sweep two files and cut the source-text and resampled cases</name>
  <files>tests/unit/test_challenge_ids.py, tests/unit/test_challenge_endpoint.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `tests/unit/test_challenge_ids.py` (415 lines, already pruned once from 490 by 37.4 D-14) — `:120 TestTheOpaqueHandle` is eight cases for a two-line function; `:129` and `:133` are the same alphabet assertion at two sample sizes; `:146` and `:151` infer from the source; `:361 TestTheStoreBuildsNoMachineryTheDesignForbids` is source-text grepping
    - `tests/unit/test_challenge_endpoint.py` (183 lines)
    - `src/nativespeaker/api/crud/challenges.py:21-23` — the two-line subject; `secrets.token_bytes` is never hand-rolled and its use is not what these cases should be re-asserting
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1164-1195` (§6.2 cluster 4)
  </read_first>
  <action>
    Sweep both files, then cut. D-13 names `test_challenge_ids.py` explicitly: 415 lines for a two-line
    function, already pruned once.

    The concrete targets: the alphabet assertion appearing twice at two sample sizes (keep one), the two
    cases that infer a property from reading the source rather than exercising behaviour, and the
    class that greps the store's source text for machinery the design forbids.

    Two things stay. The handle is a secret — any case asserting it is never logged and never appears in
    error text is behaviour, not prose, and stays. So does any case pinning that a padded handle is a
    not-found rather than a match, because byte-for-byte comparison is a rule.

    Name every deletion in the summary.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/unit/test_challenge_ids.py tests/unit/test_challenge_endpoint.py` passes with the surviving case count reported
    - `git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert'` is empty
    - `.venv/bin/python -m pytest -q -k "handle and (log or secret or padded)"` still collects at least one case — the secrecy and byte-comparison properties survive
    - `.venv/bin/python -m pytest -q` passes
    - The summary names every deleted case
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_challenge_ids.py tests/unit/test_challenge_endpoint.py && .venv/bin/python -m pytest -q && test -z "$(git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert')"</automated>
  </verify>
  <done>Both files swept, the resampled and source-text cases cut, the secrecy properties kept, every deletion named.</done>
</task>

<task type="auto">
  <name>Task 3: The JWT cluster and the two ratchet files</name>
  <files>tests/unit/test_jwt_security.py, tests/unit/test_jwks_offload.py, tests/unit/test_auth_package_shape.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `tests/unit/test_jwt_security.py` (587 lines, 53 cases, 12 classes) — `:67 TestAlgorithmSecurity`, `:107 TestSignatureVerification`, `:133 TestTokenExpiry`, `:149 TestClaimValidation`, `:203 TestMalformedTokens` all funnel into one function returning the same pair; `:260 TestAntiOracle` then asserts they are indistinguishable; `:214 TestCrossUserIsolation` and `:223 TestValidToken` overlap
    - `tests/unit/test_jwks_offload.py` (190 lines)
    - `src/nativespeaker/api/auth/jwt_verifier.py` (167 lines) — the subject
    - `tests/unit/test_auth_package_shape.py:1-14` — the 14-line docstring describing the history of a deleted ratchet; `:23` the measured triple; `:50-57` `TestTheMeasurementFires`, the control
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1164-1195` (§6.2 cluster 6)
  </read_first>
  <action>
    Sweep all four files, then cut cluster 6.

    The anti-oracle case is the load-bearing one: it asserts that every rejection arm is
    indistinguishable to a caller. That is precisely what makes most of the individual arms redundant —
    if the answers cannot be told apart, a case per arm re-asserts one property fifty times. Keep the
    anti-oracle case and keep one representative case per genuinely distinct code path (a bad algorithm,
    a bad signature, an expired token, a missing claim, a malformed token), and delete the resampled
    arms. Collapse the overlap between the cross-user isolation and valid-token classes to one case each.

    Two properties must survive as executable cases and are not targets: that a rejection carries a
    bounded reason from a closed set, and that an unknown key identifier costs one bounded, off-loop
    fetch rather than an unbounded per-request one. Both are rules this project decided deliberately.

    In `test_auth_package_shape.py`, delete the 14-line module docstring — it describes the history of a
    ratchet that no longer exists, which fails the bar on every count — and write a docstring within the
    three-line bar. Do not touch the measured triple plan `37.5-02` wrote and do not touch
    `TestTheMeasurementFires`: it is the control proving the measurement walks nested definitions rather
    than only top-level ones.

    Lower the gate's `tests/unit` baseline by this plan's share and report both the old and new values.
    If the remaining count does not match the files still unswept, that is a finding.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/unit/test_jwt_security.py tests/unit/test_jwks_offload.py tests/unit/test_auth_package_shape.py` passes with the surviving case counts reported
    - `.venv/bin/python -m pytest -q -k "anti_oracle or bounded_reason"` still collects and passes — the two kept properties survive
    - `.venv/bin/python -m pytest -q -k "measurement_fires"` passes — the ratchet's own control is untouched
    - `git diff HEAD~1 -- tests/unit/test_auth_package_shape.py | grep -c '^[-+].*CURRENT'` returns 0 — the measured triple is unchanged
    - `git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert'` is empty
    - The gate passes at its lowered `tests/unit` baseline, and both values are reported
    - `.venv/bin/python -m pytest -m "" -q` passes against PostgreSQL with 0 deselected
    - The summary names every deleted case
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -m "" -q && .venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py</automated>
  </verify>
  <done>The JWT cluster is proportionate to its 167-line subject, the anti-oracle and bounded-reason properties are kept, the package-shape ratchet's docstring clears the bar while its triple and control are untouched, and every deletion is named.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Produced by this plan:

| Artifact | Kind | Decision |
|---|---|---|
| `tests/unit/test_docstring_bar.py` — `tests/unit` baseline lowered by this set's share | ratchet value | D-12 |
| A named deleted-case list for the error, challenge and JWT clusters | record | D-13 |
| Deleted: `TestRetired401Code`, the resampled challenge-handle cases, the source-text challenge cases, the redundant JWT rejection arms | — | D-13 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → any auth rejection | What a caller can learn by comparing rejection responses is decided by the anti-oracle properties these files pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.5-07-01 | Information disclosure | the anti-oracle cases | **high** | mitigate | Deleting the JWT anti-oracle case or the challenge-rejection indistinguishability case removes the only executable proof that rejections cannot be told apart. Both are named as non-targets and grepped for after the cut. |
| T-37.5-07-02 | Information disclosure | challenge-handle secrecy cases | **high** | mitigate | The handle is never logged and never in error text. Any case pinning that is behaviour and is kept; the `-k` gate collects at least one. |
| T-37.5-07-03 | Tampering | `test_rejection_vocabulary.py` ratchets | medium | mitigate | `:104` and `:149` fail on a stale literal; deleting them would let a stale vocabulary entry pass unnoticed. Named as non-targets and exercised. |
| T-37.5-07-04 | Tampering | assertions weakened rather than deleted | **high** | mitigate | D-13 forbids weakening in place. Gated mechanically: a pure-deletion commit adds no assertion line. |
| T-37.5-07-05 | Denial of service | the unknown-key-identifier bound | medium | mitigate | The bounded off-loop fetch is a decided rule; its case is a non-target and is collected by the `-k` gate. |
| T-37.5-07-SC | Tampering | package installs | high | mitigate | No package installed by this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` after every commit; `-m ""` at the plan's end
- `git diff -U0 -- tests/ | grep '^+.*assert'` empty on every pure-deletion commit
- The gate at its lowered `tests/unit` baseline
- `.venv/bin/ruff check`
</verification>

<success_criteria>
Eleven test files clear the bar, three clusters are proportionate to their subjects, every control and
anti-oracle property survives, and every deleted case is on a list the developer can read.
</success_criteria>

<output>
Create `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-07-SUMMARY.md` when done.
Name every deleted case grouped by cluster, and report the old and new `tests/unit` baseline values.
</output>
