---
phase: 37.5-machine-generated-code-refactoring-part-4
plan: 09
type: execute
wave: 8
depends_on: ["37.5-08"]
files_modified:
  - tests/conftest.py
  - tests/e2e/conftest.py
  - tests/e2e/test_admission.py
  - tests/e2e/test_challenge_store.py
  - tests/e2e/test_chat_queries.py
  - tests/e2e/test_chats.py
  - tests/e2e/test_create_user.py
  - tests/e2e/test_error_cases.py
  - tests/e2e/test_examples.py
  - tests/e2e/test_flows.py
  - tests/e2e/test_health.py
  - tests/e2e/test_isolation.py
  - tests/e2e/test_llm_schema.py
  - tests/e2e/test_model_queries.py
  - tests/e2e/test_quota.py
  - tests/e2e/test_root.py
  - tests/e2e/test_startup_assertion.py
  - tests/e2e/test_unauthenticated_access.py
  - tests/schema/conftest.py
  - tests/schema/helpers.py
  - tests/schema/test_apply_rollback.py
  - tests/schema/test_constraints.py
  - tests/schema/test_create_atomicity.py
  - tests/schema/test_create_race.py
  - tests/schema/test_grant_locks.py
  - tests/schema/test_inventory.py
  - tests/schema/test_store_purchase_tokens.py
  - tests/unit/test_docstring_bar.py
autonomous: true
requirements: [D-12, A-08, P-10]
user_setup: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every root in the docstring gate's baseline is 0, so the phase-wide total is 0 against the measured pre-sweep figure plan `37.5-01` recorded"
    - "`tests/schema/` is swept but not pruned: zero cases are deleted from it (A-08 — D-13's 'untouched' governs pruning only)"
    - "`tests/e2e/test_startup_assertion.py` is renamed to a filename naming its actual subject; a filename naming a deleted subject is the same defect as a docstring naming a deleted module (P-10)"
    - "The anti-oracle e2e control survives: the case asserting a historical and a blocked identity are identical in status, body and headers is not deleted"
    - "No assertion is weakened in place across `tests/e2e/` or `tests/schema/`"
    - "The full suite is green against PostgreSQL with 0 deselected, and `pytest -m schema --collect-only` collects every case"
  artifacts:
    - "tests/unit/test_docstring_bar.py (every baseline at 0)"
    - tests/e2e/test_unauthenticated_access.py
  key_links:
    - "`tests/schema/` guards the migration; a sweep that deletes a case there removes a guard on work this phase did not do"
---

<objective>
Sweep `tests/e2e/`, `tests/schema/` and the root conftest, rename one file whose name records a deleted
subject, and drive every docstring-gate baseline to zero.

Purpose: A-08 settles the one contradiction in the phase's own text — D-12 says no file is exempt, D-13
says `tests/schema/` is untouched. The developer ruled: the sweep includes it, D-13's "untouched"
governs pruning only. The sweep is cheap there — zero over-long docstrings and 71 comment lines — and
those 2,297 lines are part of what the next agent reads as precedent.

Output: the last twenty-seven test files swept; `tests/schema/` unpruned; the gate at 0 everywhere.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md
</context>

<preconditions>
- Plan `37.5-08` has landed, so `src` and `tests/unit` are already at 0 in the gate and this plan owns
  the remaining three roots.
- PostgreSQL is reachable: every case this plan touches is deselected by default, so a green quick run
  proves nothing here.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Sweep `tests/e2e/` and rename the file that names a deleted subject</name>
  <files>tests/e2e/conftest.py, tests/e2e/test_admission.py, tests/e2e/test_challenge_store.py, tests/e2e/test_chat_queries.py, tests/e2e/test_chats.py, tests/e2e/test_create_user.py, tests/e2e/test_error_cases.py, tests/e2e/test_examples.py, tests/e2e/test_flows.py, tests/e2e/test_health.py, tests/e2e/test_isolation.py, tests/e2e/test_llm_schema.py, tests/e2e/test_model_queries.py, tests/e2e/test_quota.py, tests/e2e/test_root.py, tests/e2e/test_startup_assertion.py, tests/e2e/test_unauthenticated_access.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `AGENTS.md` — the rules
    - `tests/e2e/test_startup_assertion.py:1-45` — its module docstring and its four cases; the name is a leftover from the route-enumeration assertion Phase 37.1 deleted, and the cases are unauthenticated-access, doc-route-absence and trailing-slash cases
    - `tests/e2e/test_create_user.py` (643 lines, 50 comment lines — the highest comment count in the tree)
    - `tests/e2e/test_llm_schema.py` (96 lines, three over-long docstrings)
    - `tests/e2e/test_admission.py` — holds the case asserting a historical and a blocked identity are indistinguishable in status, body and headers; it is the anti-oracle control and is not a target
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1196-1263` (§7's per-file table for `tests/e2e/`), `:479-489` (P-10)
  </read_first>
  <action>
    Sweep all sixteen e2e files against the rules, reading each; no codemod. This set carries the
    heaviest comment load in the tree — 84 comment lines including 50 in one file — and five of the 22
    over-long docstrings.

    Rename `test_startup_assertion.py` to `test_unauthenticated_access.py` with `git mv`, and rewrite its
    module docstring to name what it actually tests. The current name records an assertion Phase 37.1
    deleted; a filename naming a deleted subject is the same defect as a module docstring naming a
    deleted module, in a different register. Nothing about the four cases changes.

    This plan sweeps `tests/e2e/`; it does not prune it. The e2e clusters that D-13 reaches were cut in
    plans `37.5-07` and `37.5-08` alongside their unit twins; anything still standing here is either
    proportionate or its cut has already been made. If a case's subject no longer exists, delete it and
    name it — otherwise keep it as written.

    Lower the gate's `tests/e2e` baseline to its measured value.
  </action>
  <acceptance_criteria>
    - `test -f tests/e2e/test_startup_assertion.py` is false; `test -f tests/e2e/test_unauthenticated_access.py` is true, and `git log --follow --oneline -1 -- tests/e2e/test_unauthenticated_access.py` shows the rename preserved history
    - `.venv/bin/python -m pytest -m e2e -q` passes against PostgreSQL with the same case count as before this task, minus any case named in the summary as deleted
    - `.venv/bin/python -m pytest -m e2e -q -k "identical_in_status_body_and_headers or two_responses_are_identical"` collects and passes — the anti-oracle control survives
    - The gate's `tests/e2e` baseline is 0 and `.venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py` passes
    - `git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert'` is empty
    - `.venv/bin/ruff check` passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -m e2e -q && .venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py && .venv/bin/ruff check</automated>
  </verify>
  <done>Sixteen e2e files swept, the misnamed file renamed with history preserved, the anti-oracle control intact, the `tests/e2e` baseline at 0.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep `tests/schema/` and the root conftest — no case deleted</name>
  <files>tests/conftest.py, tests/schema/conftest.py, tests/schema/helpers.py, tests/schema/test_apply_rollback.py, tests/schema/test_constraints.py, tests/schema/test_create_atomicity.py, tests/schema/test_create_race.py, tests/schema/test_grant_locks.py, tests/schema/test_inventory.py, tests/schema/test_store_purchase_tokens.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `AGENTS.md` — the rules
    - `tests/schema/test_constraints.py` (635 lines, 46 docstrings, 31 comment lines — the heaviest file in this set, and zero over-long docstrings)
    - `tests/schema/test_inventory.py` (286 lines, 8 comment lines)
    - `tests/conftest.py` — the root fixtures
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md:534-539` (A-08, the developer ruling and the corrected counts)
  </read_first>
  <action>
    Sweep all ten `tests/schema/` files and the root conftest against the rules. This set is 2,297 lines
    with zero over-long docstrings and 71 comment lines, so the work is entirely comment judgement and
    docstring quality, not length.

    **Delete no case.** A-08's ruling is exact: D-13's "untouched" governs pruning only. These files
    guard the migration, and this phase makes no migration edit — deleting a case here removes a guard on
    work this phase did not do. If a case looks disproportionate, that is a finding for a later phase and
    goes in the summary, not a deletion.

    Two cautions. Several comments here state why a constraint is asserted the way it is — that a named
    check is subsumed by a broader one and unreachable as a reported violation, that a savepoint-scoped
    helper exists because a rejected statement aborts the whole transaction, that an index predicate is
    pinned to a specific search path rather than normalised. Those are rules and stay. And
    `test_create_atomicity.py` and `test_create_race.py` carry the import lines plan `37.5-02` re-pointed;
    a sweep must not touch them.

    Lower the gate's `tests/schema` and `tests` baselines to their measured values, which should now be 0
    everywhere. Report the phase-wide total against plan `37.5-01`'s measured pre-sweep figure.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -m schema -q` passes against PostgreSQL with **exactly** the same case count as before this task — zero deletions
    - `.venv/bin/python -m pytest -m schema --collect-only -q` collects with zero errors
    - `git diff -U0 HEAD~1 -- tests/schema/ | grep -c '^-.*def test_'` returns 0 — no case was removed
    - `git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert'` is empty
    - Every gate baseline is 0 and `.venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py` passes
    - The summary reports the phase-wide docstring total: 0 now, against the figure plan `37.5-01` measured
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -m schema -q && .venv/bin/python -m pytest -m schema --collect-only -q && .venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py</automated>
  </verify>
  <done>Eleven files swept, no case deleted from `tests/schema/`, every gate baseline at 0.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the sweep — the gate's own control, then the full suite</name>
  <files>tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `tests/unit/test_docstring_bar.py` — the gate as plan `37.5-01` built it and plans `37.5-06` through this one lowered it
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-VALIDATION.md` — the D-12 / A-08 row: the gate must have reported the pre-sweep total, and a gate reporting 0 before the sweep measures nothing
  </read_first>
  <action>
    No permanent change beyond what the controls require. Execute two controls and record both
    observations.

    1. **The gate still fires at zero.** Add a module with a four-line docstring under one swept root,
       confirm the baseline case fails naming that file, then remove it. A ratchet at 0 that cannot
       report a new violation is a ratchet that has stopped working, and 0 is exactly the value at which
       that failure is invisible.
    2. **The pre-sweep total was real.** Quote plan `37.5-01`'s recorded pre-sweep figure and confirm the
       current measurement is 0, so the gate's trajectory is a measured before and a measured after
       rather than a value that always agreed with the tree.

    Then run the full suite with markers cleared against PostgreSQL, plus the collection gate, the lint
    gate and the type gate. This is the last plan that touches code; the next one touches only records.
  </action>
  <acceptance_criteria>
    - Control 1 executed and observed: a four-line docstring added under a swept root makes the baseline case fail and names the file; after removal the case passes
    - Control 2 recorded: plan `37.5-01`'s pre-sweep total and the current total of 0 are both stated
    - `.venv/bin/python -m pytest -m "" -q` passes against PostgreSQL with 0 deselected
    - `.venv/bin/python -m pytest -m schema --collect-only -q` collects with zero errors
    - `.venv/bin/python -c "import nativespeaker.api.app.main"` succeeds
    - `.venv/bin/ruff check` and `.venv/bin/ty check` pass
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -m "" -q && .venv/bin/python -m pytest -m schema --collect-only -q && .venv/bin/python -c "import nativespeaker.api.app.main" && .venv/bin/ruff check && .venv/bin/ty check</automated>
  </verify>
  <done>The gate reports 0 everywhere and was proved still able to report a violation at that value; the full suite is green against PostgreSQL.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Produced by this plan:

| Artifact | Kind | Decision |
|---|---|---|
| `tests/e2e/test_unauthenticated_access.py` | renamed file (history preserved) | D-12, P-10 |
| `tests/unit/test_docstring_bar.py` — every baseline at 0 | ratchet value | D-12, A-08 |
| A recorded before-and-after for the phase-wide docstring count | record | D-12 |
| Deleted: `tests/e2e/test_startup_assertion.py` (renamed, not removed) | — | P-10 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → every wire-level arm | The e2e files are the only place the client-visible contract is exercised end to end |
| migration → database | `tests/schema/` is the only guard on the shipped schema; this phase makes no migration edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.5-09-01 | Information disclosure | `tests/e2e/test_admission.py` | **high** | mitigate | The case asserting a historical and a blocked identity are identical in status, body and headers is the anti-oracle control. Named as a non-target and collected by an explicit `-k` gate. |
| T-37.5-09-02 | Tampering | `tests/schema/` | **high** | mitigate | Deleting a case there removes a migration guard for work this phase did not do. A-08 forbids it; gated by a diff check that no test definition was removed and by an unchanged case count. |
| T-37.5-09-03 | Tampering | the docstring gate at 0 | medium | mitigate | A ratchet that has stopped working is invisible at 0. Control 1 adds a violation and observes the case fail. |
| T-37.5-09-04 | Tampering | `tests/schema/` import lines | medium | mitigate | The two re-pointed import lines from plan `37.5-02` must survive the sweep; the collection gate runs in two of three tasks. |
| T-37.5-09-SC | Tampering | package installs | high | mitigate | No package installed by this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -m e2e -q` and `-m schema -q` against PostgreSQL after their respective tasks
- `.venv/bin/python -m pytest -m "" -q` and `-m schema --collect-only -q` at the plan's end
- `.venv/bin/ruff check`, `.venv/bin/ty check`
- Both gate controls executed and observed
</verification>

<success_criteria>
Every file in `src/` and `tests/` clears `AGENTS.md`'s bar, `tests/schema/` kept every case it had, and
the gate that proves it was shown still able to fail.
</success_criteria>

<output>
Create `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-09-SUMMARY.md` when done.
Record the phase-wide before-and-after docstring totals, both control observations, and any e2e case
deleted (with its name) or explicitly none.
</output>
