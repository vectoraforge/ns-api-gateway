---
phase: 37.5-machine-generated-code-refactoring-part-4
plan: 08
type: execute
wave: 7
depends_on: ["37.5-07"]
files_modified:
  - tests/unit/test_create_user_precedence.py
  - tests/unit/test_create_user_body.py
  - tests/unit/test_create_user_rollback.py
  - tests/unit/test_conflict_classification.py
  - tests/unit/test_identity_accessors.py
  - tests/unit/test_identities_crud.py
  - tests/unit/test_auth_security.py
  - tests/unit/test_firebase_adapter.py
  - tests/unit/test_firebase_retry.py
  - tests/unit/test_quota_seam.py
  - tests/unit/test_quota_resolver.py
  - tests/unit/test_resilience_retry.py
  - tests/unit/test_services.py
  - tests/unit/test_models.py
  - tests/unit/test_app_wiring.py
  - tests/unit/test_config.py
  - tests/unit/test_logging.py
  - tests/unit/test_users.py
  - tests/unit/test_llm_chain_schema.py
  - tests/unit/conftest.py
  - tests/unit/test_docstring_bar.py
autonomous: true
requirements: [D-12, D-13, A-09]
user_setup: []

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The gate's `tests/unit` baseline reaches 0 and the case passes at that value"
    - "`test_identity_accessors.py:399` and `:407` survive — they are the only executable record of two wire-contract divergences four phase briefs still contradict (A-09)"
    - "The other five wire-contract cases at `:364-430` are deleted, named individually, because they assert the framework's behaviour rather than this codebase's"
    - "The rejection-precedence walk survives: `test_create_user_precedence.py` still exercises every arm in order"
    - "`test_resilience_retry.py:257` — the zero-backoff case A-04 keeps `_sleep_if_positive` for — is not edited and not deleted"
    - "The seam's controls survive: the service-rejection-precedes-the-charge class, the exhausted-allowance controls, and the session-ordering pair"
    - "No assertion is weakened in place: `git diff -U0 -- tests/ | grep '^+.*assert'` is empty for every pure-deletion commit"
    - "Every deleted test case is named in the plan summary for the developer to review"
    - "The full suite is green against PostgreSQL with 0 deselected"
  artifacts:
    - "tests/unit/test_docstring_bar.py (`tests/unit` baseline at 0)"
  key_links:
    - "`test_quota_seam.py`'s kept controls are what plan `37.5-05`'s seam rests on; a D-13 cut that reaches them undoes that plan's proof"
---

<objective>
Sweep the remaining twenty-one files in `tests/unit/` and cut the create-user, identity, firebase and
quota clusters — the four D-13 reaches that this plan owns, including the largest by absolute mass.

Purpose: clusters 1, 3 and 4 alone are more test lines than the whole of `src/`. `37.5-CONTEXT.md` named
the JWT cluster and part of the challenge cluster and missed the four largest; the same treatment
applies to what it missed.

Output: `tests/unit/` clearing the bar with the gate's baseline at 0; four clusters cut with every
deletion named; two wire-contract cases kept as the only surviving record of a flagged divergence.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md
</context>

<preconditions>
- Plans `37.5-02`, `37.5-03` and `37.5-05` have landed, so the quota, resilience and create-user files
  already carry their re-pointed subjects and their new seam. This plan sweeps and cuts final code.
- Plan `37.5-07` has landed, so the gate's `tests/unit` baseline already reflects that plan's share and
  this plan drives the remainder to 0.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: The create-user and identity clusters — sweep and cut, keeping A-09's two</name>
  <files>tests/unit/test_create_user_precedence.py, tests/unit/test_create_user_body.py, tests/unit/test_create_user_rollback.py, tests/unit/test_conflict_classification.py, tests/unit/test_identity_accessors.py, tests/unit/test_identities_crud.py, tests/unit/test_auth_security.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `AGENTS.md` — the rules
    - `tests/unit/test_identity_accessors.py:364-430` — `TestTheWireContractTheFrameworkNowEnforces`, seven cases; `:399` (a padded credential value is accepted, which loosens the wire contract) and `:407` (a duplicate field is resolved by taking the first) are the two that survive; `:326 TestExternalIdentityModel` duplicates `test_models.py`
    - `tests/unit/test_create_user_body.py` (209 lines, 9 cases for one Pydantic model with one non-empty-string field)
    - `tests/unit/test_conflict_classification.py` (291 lines, 16 cases) and `tests/unit/test_create_user_rollback.py` (154 lines, 7 cases) — both target the already-linked classification
    - `tests/unit/test_create_user_precedence.py` (558 lines, 23 cases) — the stub-side rejection-precedence walk; its duplication with the end-to-end walk is defensible and is not a target
    - `.planning/REQUIREMENTS.md:73` — where the two surviving divergences are recorded as losses, naming four phase-brief files that still contradict them
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1164-1195` (§6.2 clusters 3 and 5)
  </read_first>
  <action>
    Sweep all seven files against the rules, then cut.

    **A-09 is a developer ruling and is not open.** Of the seven wire-contract cases, keep exactly two —
    the padded-credential case and the duplicate-field case. They record properties 37.4 deliberately
    gave up, and they are the only executable trace of two divergences that four phase briefs still
    assert. Delete the other five: they assert the framework's bearer-extraction behaviour rather than
    this codebase's, and they are listed individually in the summary rather than described as a group.

    Then the ordinary cuts. The external-identity model class duplicates the model tests; the body test
    file is nine cases for one model with one constrained field; the conflict-classification and
    rollback files overlap on the already-linked classification. Keep one case per genuinely distinct
    outcome.

    Two things are not targets. The rejection-precedence walk stays whole: it and the end-to-end walk
    exercise the same precedence against stubs and against the database respectively, and that
    duplication is the point. Any case asserting that the challenge handle never reaches a log or an
    error body stays.

    Every case is kept as written or deleted outright. Name every deletion in the summary; the developer
    reviews the list.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/unit/test_identity_accessors.py -k "padded or duplicate"` collects and passes exactly the two A-09 keeps
    - `grep -c 'def test_' tests/unit/test_identity_accessors.py` shows the wire-contract class reduced by five, and each of the five is named in the summary
    - `.venv/bin/python -m pytest -q tests/unit/test_create_user_precedence.py` passes with 23 cases — the precedence walk is untouched
    - `git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert'` is empty
    - `.venv/bin/python -m pytest -q` passes
    - The summary names every deleted case, with the five A-09 deletions listed individually
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_create_user_precedence.py && .venv/bin/python -m pytest -q && test -z "$(git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert')"</automated>
  </verify>
  <done>Seven files swept, both clusters cut, A-09's two kept and its five deletions named individually, the precedence walk intact.</done>
</task>

<task type="auto">
  <name>Task 2: The quota, resilience and firebase clusters — sweep and cut, keeping every seam control</name>
  <files>tests/unit/test_quota_seam.py, tests/unit/test_quota_resolver.py, tests/unit/test_resilience_retry.py, tests/unit/test_firebase_adapter.py, tests/unit/test_firebase_retry.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `AGENTS.md` — the rules
    - `tests/unit/test_quota_resolver.py:251` (`TestTheLockingStatements`, seven compiled-SQL grep cases) and `:293` (`TestGrantThenUsageOrder`, five cases re-asserting the same order on four paths)
    - `tests/unit/test_quota_seam.py` — as rewritten by plan `37.5-05`; its service-rejection-precedes-the-charge class, its two exhausted-allowance controls and its session-ordering pair are all non-targets
    - `tests/unit/test_resilience_retry.py:257` — the zero-backoff case; A-04 keeps `_sleep_if_positive` because of it, so it is neither edited nor deleted
    - `tests/unit/test_firebase_adapter.py` (492 lines, 36 cases, two over-long docstrings) and `tests/unit/test_firebase_retry.py` (185 lines, 13 cases)
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1164-1195` (§6.2 clusters 1 and 7)
  </read_first>
  <action>
    Sweep all five files, then cut cluster 1 and cluster 7.

    Cluster 1 is the largest by absolute mass — 1,194 test lines against 130 source lines. The concrete
    targets are the overlap between the resolver file and the wire-level file: the compiled-SQL grep
    cases duplicate the effective-grant statement class at the wire level, and the grant-then-usage
    order class re-asserts one order on four paths. Keep one case per distinct lock-order claim.

    Cluster 7's reducible part is the classifier's shape cases in the firebase adapter file; the two
    files otherwise split cleanly by subject and are not a broad target.

    **Six things in this set are non-targets and a cut that reaches any of them undoes plan `37.5-05`'s
    proof:** the class asserting a service rejection precedes any charge (also the positive control that
    the charge fires at all); the case that an exhausted allowance does not degrade into a provider-
    outage answer; the case that a quota rejection records no breaker failure; both cases of the
    session-ordering pair, including the one proving the recorded session actually spent a unit; the
    exactly-once-under-retry case; and the queue-full-is-not-billed case. Also untouchable: the
    zero-backoff case, and the 22 breaker references across five files that D-11 rests on.

    Every case is kept as written or deleted outright. Name every deletion.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q -k "precedes_any_charge or never_degrades or records_no_failure or exactly_once or queue_full or zero_backoff"` collects and passes every named control
    - `git diff HEAD~1 -- tests/unit/test_resilience_retry.py | grep -c 'zero_backoff'` returns 0 — A-04's case is untouched
    - `.venv/bin/python -m pytest -m "" -q -k "circuit or breaker"` is green — D-11's 22 references still pass
    - `git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert'` is empty
    - `.venv/bin/python -m pytest -q` passes
    - The summary names every deleted case
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/python -m pytest -q -k "precedes_any_charge or never_degrades or records_no_failure or exactly_once or queue_full or zero_backoff" && test -z "$(git diff -U0 HEAD~1 -- tests/ | grep '^+.*assert')"</automated>
  </verify>
  <done>Five files swept, the quota overlap and the firebase shape cases cut, every seam control and every breaker reference intact, deletions named.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the remaining `tests/unit/` files and drive the baseline to 0</name>
  <files>tests/unit/test_services.py, tests/unit/test_models.py, tests/unit/test_app_wiring.py, tests/unit/test_config.py, tests/unit/test_logging.py, tests/unit/test_users.py, tests/unit/test_llm_chain_schema.py, tests/unit/conftest.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `AGENTS.md` — the rules
    - `tests/unit/test_llm_chain_schema.py` (97 lines, three over-long docstrings — the densest violation rate in the tree)
    - `tests/unit/conftest.py` (211 lines) — the shared fixtures; sweeping a fixture's docstring must not change what the fixture does
    - `tests/unit/test_app_wiring.py` — holds the every-route-is-authenticated case, which is the structural replacement for a deleted route-registry assertion and is not a target
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1196-1263` (§7's per-file table)
  </read_first>
  <action>
    Sweep the remaining nine files. These are proportionate to their subjects — the chat service, model
    and configuration files are at or below a 2:1 ratio — so this is a sweep, not a cut. Delete a case
    here only if its subject no longer exists.

    `test_app_wiring.py`'s every-route-is-authenticated case is the structural replacement for the route
    registry Phase 37.1 deleted, negative-controlled against an injected undeclared route. It is not a
    target under any reading.

    Then lower the gate's `tests/unit` baseline to 0 and confirm the case passes there. If the measured
    count is not 0, name the remaining files and their docstrings rather than lowering the baseline to
    match them — a baseline written to match an unswept tree is the failure this gate exists to prevent.

    Report the running total against plan `37.5-01`'s measured pre-sweep figure: `src` and `tests/unit`
    are now 0, and what remains is `tests/e2e`, `tests/schema` and the root conftest, which plan
    `37.5-09` owns.
  </action>
  <acceptance_criteria>
    - The gate's `tests/unit` baseline is 0 and `.venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py` passes
    - `.venv/bin/python -m pytest -q -k "every_route_is_authenticated or EveryRouteIsAuthenticated"` collects and passes
    - `.venv/bin/python -m pytest -m "" -q` passes against PostgreSQL with 0 deselected
    - `.venv/bin/ruff check` passes
    - The summary reports the running total: which roots are at 0 and what remains for plan `37.5-09`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -m "" -q && .venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py && .venv/bin/ruff check</automated>
  </verify>
  <done>All of `tests/unit/` clears the bar, the gate's baseline for it is 0, and the remaining roots are named.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Produced by this plan:

| Artifact | Kind | Decision |
|---|---|---|
| `tests/unit/test_docstring_bar.py` — `tests/unit` baseline at 0 | ratchet value | D-12 |
| A named deleted-case list for the create-user, identity, quota and firebase clusters | record | D-13 |
| Five individually named A-09 deletions, with the two keeps recorded as the surviving trace of a flagged divergence | record | D-13, A-09 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `POST /auth/create-user` | The rejection precedence these files pin is what a caller can and cannot learn |
| `ChatService` → the charge | The seam controls in this set are the only proof that a request that never reached the provider is not billed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.5-08-01 | Elevation of privilege | the quota seam controls | **high** | mitigate | Cutting the service-rejection-precedes-the-charge class, the exactly-once case or the queue-full case removes the proof that plan `37.5-05` shipped. All six are named non-targets and collected by an explicit `-k` gate. |
| T-37.5-08-02 | Information disclosure | the rejection-precedence walk | **high** | mitigate | Cutting arms out of the precedence walk could let a rejection change status or code unnoticed, turning completion into an enumeration oracle. The walk is a non-target and its 23-case count is gated. |
| T-37.5-08-03 | Repudiation | the two A-09 keeps | medium | mitigate | They are the only executable record of two divergences four phase briefs still assert. Deleting them would erase the trace; kept and gated by name. |
| T-37.5-08-04 | Denial of service | D-11's breaker references | medium | mitigate | The 22 references across five files are the evidence the breaker still works. Gated by a `-k` run against PostgreSQL. |
| T-37.5-08-05 | Tampering | assertions weakened rather than deleted | **high** | mitigate | Gated mechanically: a pure-deletion commit adds no assertion line. |
| T-37.5-08-SC | Tampering | package installs | high | mitigate | No package installed by this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` after every commit; `-m ""` at the plan's end
- `git diff -U0 -- tests/ | grep '^+.*assert'` empty on every pure-deletion commit
- The gate's `tests/unit` baseline at 0
- `.venv/bin/ruff check`
</verification>

<success_criteria>
`tests/unit/` clears the bar mechanically and by reading, the four largest clusters are proportionate to
their subjects, every seam and anti-oracle control survives, and every deleted case is on a list the
developer can read.
</success_criteria>

<output>
Create `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-08-SUMMARY.md` when done.
Name every deleted case grouped by cluster, list the five A-09 deletions individually, and report the
running docstring-gate total.
</output>
