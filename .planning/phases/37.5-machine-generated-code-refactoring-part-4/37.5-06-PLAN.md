---
phase: 37.5-machine-generated-code-refactoring-part-4
plan: 06
type: execute
wave: 5
depends_on: ["37.5-04", "37.5-05"]
files_modified:
  - tests/unit/test_adapter_interfaces.py
  - tests/unit/test_docstring_bar.py
  - src/nativespeaker/api/auth/adapters.py
  - src/nativespeaker/api/auth/firebase.py
  - src/nativespeaker/api/auth/jwt_verifier.py
  - src/nativespeaker/api/auth/__init__.py
  - src/nativespeaker/api/resilience.py
  - src/nativespeaker/api/errors.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/app/error_handlers.py
  - src/nativespeaker/api/app/lifespan.py
  - src/nativespeaker/api/app/main.py
  - src/nativespeaker/api/services/auth.py
  - src/nativespeaker/api/services/quota.py
  - src/nativespeaker/api/services/chats.py
  - src/nativespeaker/api/services/llm.py
  - src/nativespeaker/api/services/__init__.py
  - src/nativespeaker/api/crud/identities.py
  - src/nativespeaker/api/crud/challenges.py
  - src/nativespeaker/api/crud/chats.py
  - src/nativespeaker/api/crud/grants.py
  - src/nativespeaker/api/crud/__init__.py
  - src/nativespeaker/api/routers/auth.py
  - src/nativespeaker/api/routers/chats.py
  - src/nativespeaker/api/routers/examples.py
  - src/nativespeaker/api/routers/health.py
  - src/nativespeaker/api/routers/root.py
  - src/nativespeaker/api/routers/__init__.py
  - src/nativespeaker/api/schemas/auth.py
  - src/nativespeaker/api/schemas/api.py
  - src/nativespeaker/api/schemas/llm.py
  - src/nativespeaker/api/schemas/__init__.py
  - src/nativespeaker/api/tables/auth.py
  - src/nativespeaker/api/tables/chats.py
  - src/nativespeaker/api/tables/grants.py
  - src/nativespeaker/api/tables/identities.py
  - src/nativespeaker/api/tables/purchases.py
  - src/nativespeaker/api/tables/users.py
  - src/nativespeaker/api/tables/__init__.py
  - src/nativespeaker/api/config.py
  - src/nativespeaker/api/logs.py
  - src/nativespeaker/api/__init__.py
autonomous: true
requirements: [D-12, D-13, A-13, A-05]
user_setup: []

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The docstring gate's `src` baseline is 0 and the case passes at that value"
    - "No docstring in `src/` describes what lives somewhere else, what the entity is not, or how the application works in general"
    - "`auth/adapters.py`'s module docstring no longer names a module 37.4 deleted"
    - "Comments in `src/` are one line each and explain the line or lines below them, never the design or a rule enforced in another module"
    - "`test_adapter_interfaces.py`'s docstring-phrase cases are deleted before the docstring they assert is rewritten, so neither decision breaks the other (A-13)"
    - "No production behaviour changed: `git diff` over `src/` shows only docstring, comment and blank-line changes"
    - "The full suite is green against PostgreSQL with 0 deselected"
  artifacts:
    - "tests/unit/test_docstring_bar.py (src baseline driven to 0)"
  key_links:
    - "The `src` baseline in the gate is the only mechanical proof D-12 has for this half of the sweep"
---

<objective>
Apply `AGENTS.md`'s docstring and comment rules to all 40 files in `src/`, and cut the adapter test
cases that assert the prose of a docstring this sweep rewrites.

Purpose: 37.4 scoped the rules to the lines it rewrote, which left most of the repository untouched.
`auth/adapters.py:1-6` still carries a six-line module docstring naming `auth/exceptions.py` — a module
37.4 itself deleted. This sweep runs last over `src/` so it sweeps final code rather than code about to
move.

Output: zero over-long docstrings in `src/`; comments that clear the necessity bar; the adapter cluster
cut from 27 cases for a 35-line file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-CONTEXT.md
</context>

<preconditions>
- Plans `37.5-02` through `37.5-05` have landed. This sweep must not run before them: sweeping a file in
  one wave and moving its contents in the next wastes the sweep and produces a diff no reviewer can
  read.
- The docstring gate from plan `37.5-01` exists and its `src` baseline is the measured pre-sweep value.
- The rules are in `AGENTS.md`. This plan does not restate them; it applies them.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Cut the adapter test cluster, before the docstring it asserts is rewritten</name>
  <files>tests/unit/test_adapter_interfaces.py</files>
  <read_first>
    - `tests/unit/test_adapter_interfaces.py` (257 lines, 27 cases) — the file being cut; `:81-137` `TestZeroImplementations` (9 cases), `:165-189` `TestImportIsSideEffectFree` (4 cases), `:248-257` `TestSharedAdapterRules`
    - `src/nativespeaker/api/auth/adapters.py` (35 lines) — the subject: one frozen dataclass and one Protocol
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` — the source of the rules `TestSharedAdapterRules`'s phrases quote
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1140-1195` (§6, the cluster ranking and the concrete targets), `:1371-1377` (Pitfall 6)
  </read_first>
  <action>
    A-13 sequences this before task 2 for a specific reason: `TestSharedAdapterRules` asserts that
    `auth/adapters.py`'s module docstring contains named phrases, and that docstring is what task 2 must
    cut. Whichever runs first breaks the other unless this one does.

    Delete `TestSharedAdapterRules`. The rules its phrases quote are `SHARED-INVARIANTS.md`'s, and
    asserting that a docstring quotes them tests prose, not behaviour.

    Then cut the rest of the cluster on D-13's criterion — a case is a target when its subject is
    deleted or trivial, when it re-asserts an earlier case's assertion at a different sample size, or
    when it asserts a property of the source text rather than of the behaviour. Twenty-seven cases for a
    35-line file of one frozen dataclass and one Protocol is the second-worst ratio in the repository.
    `TestZeroImplementations` and `TestImportIsSideEffectFree` assert source-text properties (no
    function definitions, no module-level mutable state) and are the concrete targets.

    Every case is kept as written or deleted outright. No assertion is weakened in place — that is what
    makes "deleted, never weakened" checkable rather than asserted. Name every deleted case in the
    summary; coverage that leaves must leave visibly, and the developer reviews the list.

    Apply the docstring and comment rules to whatever remains of this file in the same commit.
  </action>
  <acceptance_criteria>
    - `grep -c 'class TestSharedAdapterRules' tests/unit/test_adapter_interfaces.py` returns 0
    - `.venv/bin/python -m pytest -q tests/unit/test_adapter_interfaces.py` passes, with the surviving case count reported
    - `git diff -U0 HEAD~1 -- tests/unit/test_adapter_interfaces.py | grep '^+.*assert'` is empty — a pure-deletion diff adds no assertion
    - `.venv/bin/python -m pytest -q` passes
    - The summary names every deleted case
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_adapter_interfaces.py && .venv/bin/python -m pytest -q && test -z "$(git diff -U0 HEAD~1 -- tests/unit/test_adapter_interfaces.py | grep '^+.*assert')"</automated>
  </verify>
  <done>The docstring-phrase cases are gone before the docstring changes, the cluster is cut, and every deletion is named.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep `src/` — `auth/`, `resilience.py`, `errors.py`, `app/`</name>
  <files>src/nativespeaker/api/auth/adapters.py, src/nativespeaker/api/auth/firebase.py, src/nativespeaker/api/auth/jwt_verifier.py, src/nativespeaker/api/auth/__init__.py, src/nativespeaker/api/resilience.py, src/nativespeaker/api/errors.py, src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/app/error_handlers.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/app/main.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `AGENTS.md` — the four rules being applied; the file states them and this plan does not repeat them
    - `src/nativespeaker/api/auth/adapters.py:1-6` and `:15-21` — the two over-long docstrings; `:1-6` names a module that no longer exists
    - `src/nativespeaker/api/auth/firebase.py:1-8` (module), `:66`, `:94`, `:146-151` — the module docstring, the one-line docstring `AGENTS.md` quotes as its example of failure, the developer's named example comment, and `_exhausted`'s docstring
    - `src/nativespeaker/api/app/error_handlers.py:27-31` — `camel_to_snake`'s five-line docstring, a rare case where the length may be earned
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1196-1263` (§7, the per-file counts: this set holds six of the seven over-long docstrings in `src/` and 91 of its 162 comment lines)
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-PATTERNS.md:777-785` — the bar, restated once with an example
  </read_first>
  <action>
    Read each file and apply the rules. Do not use `sed` or a codemod: a regular expression cannot tell
    a necessary comment from a redundant one, and this is judgement work.

    Length is the mechanical half and this set holds six of `src/`'s seven violations. The real workload
    is the other half of the bar — a three-line docstring can fail on every count. `firebase.py:66` is
    one line and is the exact sentence `AGENTS.md` quotes as its example of failure.

    Comments: default to none. The developer's own example of one that fails the bar is
    `firebase.py:94`, a sentence above a log call that says what the log event already says. Keep a
    comment only where it resolves a genuine ambiguity or prevents a misreading of the specific lines
    below it. Several comments in this set carry a real rule and stay — the positive-test comments that
    explain why a NULL fails closed, and the ones marking why a commit or a rollback happens where it
    does.

    `errors.py` has the highest docstring count in `src/` and zero over-long ones, so it is judgement
    only. `resilience.py` gained the admission seam in plan `37.5-05`; sweep what that plan wrote as
    well as what preceded it.

    Change no behaviour. If a docstring cannot be shortened without dropping a fact that a reader needs,
    that fact belongs in the code or in `AGENTS.md`, not in a longer docstring.

    Lower the docstring gate's `src` baseline to the value it now measures. If that value is not 0, the
    sweep is incomplete — report the remaining files rather than lowering the baseline to match them.
  </action>
  <acceptance_criteria>
    - `grep -rn 'exceptions' src/nativespeaker/api/auth/adapters.py` returns zero lines naming a deleted module
    - The gate's `src` baseline is 0 and `.venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py` passes
    - `git diff HEAD~1 -- src/ | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vc '^\s*[-+]\s*\(#\|"""\|'"'''"'\|$\)'` shows no non-comment, non-docstring source line changed, or every such line is named and justified in the summary
    - `.venv/bin/python -m pytest -q` passes with the same case count as before this task
    - `.venv/bin/python -c "import nativespeaker.api.app.main"` succeeds
    - `.venv/bin/ruff check` and `.venv/bin/ty check` pass
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/python -c "import nativespeaker.api.app.main" && .venv/bin/ruff check && .venv/bin/ty check</automated>
  </verify>
  <done>Ten files swept, the `src` half of the gate reports 0 for this set, and no source line changed.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the rest of `src/` — `services/`, `crud/`, `routers/`, `schemas/`, `tables/`, `config.py`, `logs.py`</name>
  <files>src/nativespeaker/api/services/auth.py, src/nativespeaker/api/services/quota.py, src/nativespeaker/api/services/chats.py, src/nativespeaker/api/services/llm.py, src/nativespeaker/api/services/__init__.py, src/nativespeaker/api/crud/identities.py, src/nativespeaker/api/crud/challenges.py, src/nativespeaker/api/crud/chats.py, src/nativespeaker/api/crud/grants.py, src/nativespeaker/api/crud/__init__.py, src/nativespeaker/api/routers/auth.py, src/nativespeaker/api/routers/chats.py, src/nativespeaker/api/routers/examples.py, src/nativespeaker/api/routers/health.py, src/nativespeaker/api/routers/root.py, src/nativespeaker/api/routers/__init__.py, src/nativespeaker/api/schemas/auth.py, src/nativespeaker/api/schemas/api.py, src/nativespeaker/api/schemas/llm.py, src/nativespeaker/api/schemas/__init__.py, src/nativespeaker/api/tables/auth.py, src/nativespeaker/api/tables/chats.py, src/nativespeaker/api/tables/grants.py, src/nativespeaker/api/tables/identities.py, src/nativespeaker/api/tables/purchases.py, src/nativespeaker/api/tables/users.py, src/nativespeaker/api/tables/__init__.py, src/nativespeaker/api/config.py, src/nativespeaker/api/logs.py, src/nativespeaker/api/__init__.py, tests/unit/test_docstring_bar.py</files>
  <read_first>
    - `AGENTS.md` — the rules
    - `src/nativespeaker/api/schemas/llm.py` — holds the one remaining over-long docstring in this set
    - `src/nativespeaker/api/services/auth.py` — written by plan `37.5-02` from a body carrying a seven-line docstring and eighteen comment lines; this is where the sweep pays most
    - `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-RESEARCH.md:1196-1263` (§7's per-file table; this set is ~50 docstrings, 71 comment lines and 1 over-long docstring)
  </read_first>
  <action>
    Same treatment, remaining thirty files. Read each and apply the rules; no codemod.

    Three cautions specific to this set:
    - `services/auth.py` and `services/quota.py` are new files this phase wrote from moved bodies. The
      prose that came with them is the prose the sweep exists for, and it should be rewritten against
      the bar rather than preserved because it was inherited.
    - The comments that state *why* a commit happens where it does, and the positive-test comments that
      say why a NULL or a future enum member fails closed, are rules and stay. Deleting them removes the
      only statement of a fail-closed property.
    - `tables/` docstrings describe rows; keep them to what the table is.

    Lower the docstring gate's `src` baseline to its measured value, which should now be 0 across all
    forty files. A non-zero remainder is a finding, not a baseline.
  </action>
  <acceptance_criteria>
    - The gate's `src` baseline is 0 and `.venv/bin/python -m pytest -q tests/unit/test_docstring_bar.py` passes
    - `.venv/bin/python -m pytest -m "" -q` passes against PostgreSQL with 0 deselected
    - `.venv/bin/python -c "import nativespeaker.api.app.main"` succeeds
    - `.venv/bin/ruff check` and `.venv/bin/ty check` pass
    - The summary names any comment that was kept on the grounds that it states a rule, so the developer
      can judge the necessity call that only a reader can make
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -m "" -q && .venv/bin/python -c "import nativespeaker.api.app.main" && .venv/bin/ruff check && .venv/bin/ty check</automated>
  </verify>
  <done>All 40 `src/` files are swept, the gate's `src` baseline is 0, and the kept-comment judgements are listed for review.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Produced by this plan:

| Artifact | Kind | Decision |
|---|---|---|
| `tests/unit/test_docstring_bar.py` — `src` baseline at 0 | ratchet value | D-12 |
| A rewritten `auth/adapters.py` module docstring | prose | D-12 |
| Deleted: `TestSharedAdapterRules` and the adapter cluster's source-text cases | — | D-13, A-13 |
| A named list of kept comments and their grounds | record | D-12 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none newly crossed | A prose sweep. The risk is that a "prose-only" edit silently changes behaviour, or that deleting a comment removes the only statement of a fail-closed rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.5-06-01 | Tampering | every swept `src/` file | **high** | mitigate | A sweep that changes a source line changes behaviour under cover of a prose diff. Gated by a diff filter that shows only comment, docstring and blank-line changes, and by the full suite with 0 deselected. |
| T-37.5-06-02 | Information disclosure | `crud/identities.py`, `services/auth.py` comments | medium | mitigate | The positive-test comments state why a NULL or a future enum member fails closed on the admission branches. Deleting them leaves the property undocumented; keeping them is named in the summary for review. |
| T-37.5-06-03 | Tampering | `test_adapter_interfaces.py` cut | medium | mitigate | Deleting cases in the same commit that rewrites the docstring they assert would hide which change caused which failure. A-13 sequences the cut first, in its own commit. |
| T-37.5-06-SC | Tampering | package installs | high | mitigate | No package installed by this phase. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` after every commit; `-m ""` at the plan's end
- `.venv/bin/python -c "import nativespeaker.api.app.main"` after every commit
- `.venv/bin/ruff check`, `.venv/bin/ty check`
- The gate's `src` baseline at 0
- The prose-only diff filter, run and its output recorded
</verification>

<success_criteria>
Every file in `src/` clears the bar mechanically and by reading, no source line changed, and the adapter
cluster is proportionate to its 35-line subject.
</success_criteria>

<output>
Create `.planning/phases/37.5-machine-generated-code-refactoring-part-4/37.5-06-SUMMARY.md` when done.
Name every deleted test case, every comment kept on rule grounds, and the measured `src` baseline.
</output>
