---
phase: 38-post-auth-sync
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md
autonomous: false
requirements: [SYNC-03]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SHARED-INVARIANTS.md contains no clause requiring an audit.auth_events row, an audited attempt path, route→operation metadata readable before the barrier, an actor_subject_hash, or a details shape"
    - "The non-audit substance of the two mixed clauses survives the edit: a rejection still leaves exactly one structured security-log line, and admission-phase rejections still write no per-rejection database row"
    - "The phase briefs under specs/auth-refactor-phases/ are unedited — they are marked verbatim, and the removed invariants plus the REQUIREMENTS.md amendment carry the decision instead"
    - "No new entry is added to the flagged-conflicts table for this edit: after it there is no surviving invariant text to conflict with (D-03)"
  artifacts:
    - path: "/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md"
      provides: "The binding specification with the audit obligations removed"
      contains: "Fail-closed defaults"
---

<objective>
Land the milestone's audit removal in the binding specification: strike § "Audit" and every other
clause in `SHARED-INVARIANTS.md` that requires an audit row (D-03).

Purpose: SYNC-03 is satisfied by removing an obligation, not by building something. Leaving the
invariant text in place would leave a standing conflict that phases 43, 44 and 46 would each
re-discover and re-decide.
Output: an edited binding specification, and the deliberate absence of a new flagged-conflict entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-post-auth-sync/38-CONTEXT.md
@.planning/phases/38-post-auth-sync/38-PATTERNS.md
</context>

## Artifacts this phase produces

This plan produces no code symbol. It edits one specification document. The full phase artifact
inventory is in `38-01-PLAN.md` § "Artifacts this phase produces".

<repository_boundary>
`/home/init/native-speaker/specs/` is in the parent working tree, outside the `ns-api-gateway`
submodule. Edit the file, then run no git command against the parent repository — no add, no commit,
no push. Report the edit in the summary so the developer commits it themselves. Only files inside
`ns-api-gateway` are committed by this phase.
</repository_boundary>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>The exact clause set struck from `SHARED-INVARIANTS.md`, and where the two mixed clauses' surviving non-audit substance lands</decision>
  <context>
D-03 is settled: the audit invariants are removed rather than diverged from, and this is rated
one-way in practice — `SHARED-INVARIANTS.md` overrides every phase brief, and restoring it would
mean re-deciding the milestone's audit posture. What is not settled is the exact cut.

Six of the eight clauses in scope are pure audit machinery and go without question: the § "Audit"
heading itself, the audited-attempt-path and route→operation-metadata clause, the one-durable-row
clause, the first-class-rejection-rows clause, the `succeeded` / `actor_subject_hash` clause, and the
`details` shape and redaction clause.

Three clauses need a call, because they carry substance that is still live:

- The clause stating routine authenticated routes are off the audited path also states that a
  rejection keeps its stable internal result in the structured security log, and mandates a
  bounded-cardinality counter metric. Phase 36 D-15 already removed the counter.
- The clause on admission-phase rejections also states that they write no per-rejection database row
  at all — a live fail-closed rule independent of auditing.
- The § "Errors" clause stating that internal `core.auth_event_result` values are never
  client-visible and that the audited internal result is never less specific than the returned class.
  The enum was deleted by Phase 37.3, and the anti-oracle rule directly above it already states that
  body, status and copy are identical across every triggering branch of a class.

Undo cost: the file binds every remaining phase (39–46) and is the document that wins over any phase
brief. Restoring the struck text means re-deciding the audit posture for the whole milestone, not
reverting a commit in this repository.
  </context>
  <options>
    <option id="option-a">
      <name>Delete § "Audit" entirely; relocate the two surviving non-audit rules into § "Fail-closed defaults"; delete the § "Errors" auth_event_result clause</name>
      <pros>No section survives whose name promises a mechanism that does not exist. The two live rules keep a home in a section whose subject they actually belong to. The deleted § "Errors" clause names a type that no longer exists and its live half is already stated one line above it.</pros>
      <cons>Moves text between sections, so a reader diffing the file sees relocation as well as deletion.</cons>
    </option>
    <option id="option-b">
      <name>Delete the six pure-audit clauses, keep the § "Audit" heading carrying only the two rewritten rules; delete the § "Errors" auth_event_result clause</name>
      <pros>The smallest diff; no text moves between sections.</pros>
      <cons>Leaves a section titled "Audit" that mandates no audit, which is the kind of stale heading this project has repeatedly had to correct.</cons>
    </option>
    <option id="option-c">
      <name>Delete § "Audit" entirely including both mixed clauses, and delete the § "Errors" auth_event_result clause</name>
      <pros>The cleanest cut; nothing about auditing survives anywhere.</pros>
      <cons>Loses two live rules that have nothing to do with auditing — that a rejection leaves exactly one structured log line, and that an admission-phase rejection writes no per-rejection database row. Neither is stated elsewhere in the file.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c — and say separately whether the § "Errors" `core.auth_event_result` clause goes or stays.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Strike the audit invariants from the binding specification</name>
  <reversibility rating="one-way">`SHARED-INVARIANTS.md` overrides every phase brief and binds phases 39–46; restoring the struck text means re-deciding the milestone's audit posture, not reverting a commit.</reversibility>
  <files>/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md</files>
  <read_first>
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` in full — the whole file is 73 lines and the edit must not disturb anything outside the named clauses
    - `.planning/phases/38-post-auth-sync/38-CONTEXT.md` § D-03, § D-05
    - `.planning/REQUIREMENTS.md` FOUND-05's flagged-conflict paragraph — the text this edit makes void
  </read_first>
  <action>
Apply the option the checkpoint selected, editing only the clauses it named.

The six pure-audit clauses go in every option: the § "Audit" heading, the audited-attempt-path and
route→operation-metadata clause, the exactly-one-durable-row clause, the first-class-rejection-rows
clause with its bounded-reason enumeration, the `succeeded` / `actor_subject_hash` clause, and the
`details` shape and redaction clause.

Treat the two mixed clauses and the § "Errors" clause exactly as the checkpoint decided — relocated,
rewritten in place, or deleted. Where a clause is rewritten, keep only its non-audit substance and do
not add any new rule: this edit removes obligations, it does not create them.

Do not touch anything else in the file. § "Rate limits" is already dead by Phase 35 D-05 and is not
this phase's subject; § "Global deletions", § "Fail-closed defaults", § "The barrier", § "Locks and
transactions" and § "Grants and evaluation time" keep every clause they have except where the
selected option relocates text into one of them.

Do not edit any phase brief under `specs/auth-refactor-phases/` — `03-sync.md` and its siblings are
marked verbatim (D-05). The removed invariants and the `REQUIREMENTS.md` amendment written by plan
38-05 carry the decision instead.

Do not add a flagged-conflict entry for this edit (D-03): after it there is no surviving invariant
text to conflict with, which is the whole point of removing rather than diverging. Resolving the
already-recorded conflict under FOUND-05 is plan 38-05's work, not this plan's.

Run no git command against the parent repository.
  </action>
  <verify>
    <automated>grep -inE 'auth_events|actor_subject_hash|audited attempt|audit row|auth_event_result|schema_version, context, verification' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md; test $(grep -icE 'auth_events|actor_subject_hash|audited attempt|audit row|auth_event_result' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -icE 'auth_events|actor_subject_hash|audited attempt|audit row|auth_event_result'` against the file outputs `0`.
    - `grep -c '^## ' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` outputs one fewer than before the edit under options a and c, and the same number under option b — state the before and after values in the summary.
    - The surviving substance is present unless option-c was selected: the file still states that a rejection keeps its stable internal result in the structured security log, and that admission-phase rejections write no per-rejection database row. Quote both surviving lines in the summary.
    - `git -C /home/init/native-speaker/specs diff --stat` (or `git -C /home/init/native-speaker diff --stat -- specs/`) shows exactly one changed file, `SHARED-INVARIANTS.md`, and no phase brief.
    - No git command was run against the parent repository beyond the read-only `diff --stat` above — no add, no commit, no push.
    - The diff removes lines and does not add any new rule: `git -C /home/init/native-speaker diff -- specs/auth-refactor-phases/SHARED-INVARIANTS.md | grep -c '^+.*MUST\|^+.*must '` is reported in the summary with a one-line justification for each added line, if any.
  </acceptance_criteria>
  <done>The binding specification mandates no audit row anywhere, the two live non-audit rules survive as the checkpoint decided, no phase brief is touched, and no new flagged-conflict entry exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| specification → every future phase | This document overrides phase briefs; what it says binds phases 39–46 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-12 | Repudiation | removal of the durable auth-attempt record | medium | accept | Accepted milestone-wide by the developer (37.1 D-01, restated as 38 D-01). The compensating record is the request middleware's one line per attempt plus one WARNING per rejection from the shared error handler. Recorded under FOUND-05 rather than mitigated: this is a pre-launch sub-$5/month product whose threat model does not ask for a durable auth-attempt trail. |
| T-38-13 | Tampering | an over-broad edit silently removing a live security rule | high | mitigate | The checkpoint names the exact three clauses whose substance is contested; the acceptance criteria require the surviving lines to be quoted, require the diff to touch exactly one file, and require every added line to be justified — an edit removing a fail-closed rule cannot pass silently. |

No package-manager install task exists in this plan, so no package-legitimacy gate applies.
</threat_model>

<verification>
- The audit-mandating vocabulary is absent from `SHARED-INVARIANTS.md`.
- Exactly one file changed in the parent working tree, and no phase brief among them.
- No flagged-conflict entry was added.
- No git command was run against the parent repository.
</verification>

<success_criteria>
- No clause in the binding specification requires an audit row, an audited attempt path, or audit actor fields.
- The two live non-audit rules are handled exactly as the checkpoint decided, with the surviving text quoted in the summary.
- The phase briefs are byte-identical.
</success_criteria>

<output>
Create `.planning/phases/38-post-auth-sync/38-04-SUMMARY.md` when done.
</output>
