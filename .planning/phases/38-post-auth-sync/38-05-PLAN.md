---
phase: 38-post-auth-sync
plan: 05
type: execute
wave: 2
depends_on: ["38-04"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SYNC-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md SYNC-03 no longer describes a durable audit.auth_events row as an outstanding obligation; it records the decision, names option (b), dates it, and names what actually exists in its place"
    - "The three sibling entries APPLEHOOK-02, PLAYHOOK-03 and SIGNOUT-02 each carry a dated Phase 38 note stating exactly which half is settled and which half still binds"
    - "SIGNOUT-01 and SIGNOUT-02's fail-closed half are untouched and still binding: an indeterminate or failed revocation must never report success"
    - "The flagged-conflicts count drops from six to five and the FOUND-05 § Audit entry is recorded as resolved by removal rather than deleted without trace"
    - "ROADMAP.md Phase 38 success criterion 4 describes what is built rather than a blocked obligation, and criteria 1 to 3 are unchanged"
    - "No phase brief under specs/auth-refactor-phases/ is edited, and no migration file is touched"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The dated Phase 38 amendment, the SYNC-03 decision record, and the three sibling notes"
      contains: "Amended again by Phase 38"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 38 success criterion 4 rewritten to what is built"
      contains: "Phase 38: POST /auth/sync"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md"
      via: "the FOUND-05 flagged conflict is resolved by the removal plan 38-04 performed"
      pattern: "FOUND-05"
---

<objective>
Write the decision down where the project's readers look for it: the dated SYNC-03 amendment, the
three sibling requirement entries D-04 settles, the flagged-conflicts arithmetic the removal changes,
and ROADMAP success criterion 4 (D-01, D-04, D-05).

Purpose: SYNC-03 is satisfied by removing an obligation and recording that removal. Without the
record, three later phases re-decide a settled question and a reader auditing divergences counts a
conflict that no longer exists.
Output: an amended `REQUIREMENTS.md` and a corrected Phase 38 entry in `ROADMAP.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-post-auth-sync/38-CONTEXT.md
@.planning/phases/38-post-auth-sync/38-PATTERNS.md
@.planning/phases/38-post-auth-sync/38-04-SUMMARY.md
</context>

## Artifacts this phase produces

This plan produces no code symbol. It edits two planning documents. The full phase artifact inventory
is in `38-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto">
  <name>Task 1: The SYNC-03 amendment and the three sibling entries</name>
  <reversibility rating="costly">D-01 records the audit subsystem as dropped rather than rebuilt; rebuilding it means rewriting the single initial migration under a new id, which the v2.0 one-migration constraint makes a replacement rather than an addition.</reversibility>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the header amendment blockquotes at :12-32 for the established format, SYNC at :190-197, APPLEHOOK-02 at :228-231, PLAYHOOK-03 at :238-239, SIGNOUT-01/02 at :249-254, FOUND-05 at :111 and its flagged-conflict paragraph
    - `.planning/phases/38-post-auth-sync/38-CONTEXT.md` § D-01, § D-02, § D-04, § D-05
    - `.planning/phases/38-post-auth-sync/38-04-SUMMARY.md` — which clauses were actually struck
  </read_first>
  <action>
Rewrite the SYNC-03 entry and add a dated amendment note beneath it, in the blockquote shape Phase
37.1 established and 37.2 through 37.5 followed — a bolded lead sentence naming the phase and the
date, and the three-word vocabulary (**amended**, **withdrawn**, **flagged forward**).

SYNC-03's treatment is **amended**, not withdrawn: its subject was attempt telemetry, and attempt
telemetry still exists — it is the middleware line. The rewritten requirement states what is actually
owed: every `/auth/sync` attempt earns exactly one `request` line from `RequestLoggingMiddleware`
carrying the request id, method, path and status code, and every rejection additionally earns one
WARNING named for its exception class from the shared error handler. It writes no durable row and no
per-attempt telemetry beyond those lines. Remove the **BLOCKED** lead and the two-option choice: the
choice has been made.

The amendment note records the decision and its grounds, in plain concrete language:
- Option **(b)** was chosen, dated 2026-09-01. The durable-row obligation is dropped, not rebuilt.
- The ground: rebuilding means the table back in the migration, HMAC-SHA-256 actor hashing with key
  versioning, the six-key `details` shape, and route→operation metadata readable before the barrier —
  which is not warranted for a read-only endpoint's attempt telemetry, and reopens the cost argument
  that removed it. SYNC-02 makes the endpoint strictly read-only, so the row was never a mutation
  record.
- No success event was added either (D-02): the middleware already writes one line per attempt, and a
  second line saying a 200 succeeded is duplication.
- The obligation was removed at its source: plan 38-04 struck the audit clauses from
  `SHARED-INVARIANTS.md` rather than carrying a permanent flagged conflict, following the precedent
  the developer set in Phase 37.4.

Then amend the three siblings D-04 names, each with its own dated Phase 38 note, and be exact about
what the removal does and does not settle:

- **SIGNOUT-02** — the **audit half is settled** by the same stroke: no durable row is owed, and the
  obligation is gone from the binding specification, so Phase 46 inherits no audit decision. State
  plainly, in the same note, that **SIGNOUT-01 and SIGNOUT-02's fail-closed half are untouched and
  stay fully binding**: an indeterminate or failed revocation must never report success. Do not
  reword either.
- **APPLEHOOK-02** and **PLAYHOOK-03** — settled only as far as the removal actually reaches. What is
  settled: the route→operation-metadata obligation that the deleted route registry carried, and any
  audit-row obligation for those two routes. What is **not** settled and still binds: the § "Global
  deletions" prohibition on wildcard or path-prefix membership for the provider-callback category —
  callback routes are still named individually by exact path and are still never on the public
  allowlist. Phase 38 removes no clause from § "Global deletions". Say both halves; do not write a
  note that reads as though the whole requirement is closed.

Add the Phase 38 amendment blockquote to the header block at the top of the file, after the Phase
37.5 one, in the same register: what this phase settled, which requirements were amended, and that no
requirement was deleted or withdrawn.

Change no requirement checkbox in this task — the boxes are handled by plan 38-06 once the suite is
green.
  </action>
  <verify>
    <automated>grep -c 'BLOCKED' .planning/REQUIREMENTS.md; grep -c 'Amended again by Phase 38' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'SYNC-03' .planning/REQUIREMENTS.md` shows the entry no longer carries the string `BLOCKED` and no longer offers a choice between options (a) and (b).
    - `grep -c 'Amended again by Phase 38' .planning/REQUIREMENTS.md` outputs at least `1`.
    - The SYNC-03 amendment names the date `2026-09-01`, the word `amended`, and option `(b)` explicitly.
    - `grep -n 'SIGNOUT-01' .planning/REQUIREMENTS.md` shows its requirement line byte-identical to before the edit — confirm with `git diff .planning/REQUIREMENTS.md | grep '^-' | grep -c 'SIGNOUT-01'` outputting `0`.
    - The SIGNOUT-02 note contains a sentence stating the fail-closed half is untouched and still binding.
    - The APPLEHOOK-02 and PLAYHOOK-03 notes each contain a sentence naming what is still binding — the exact-path enumeration prohibition under § "Global deletions" — and neither note claims the requirement is fully closed.
    - `git diff --stat .planning/ROADMAP.md` shows no changes in this task.
  </acceptance_criteria>
  <done>SYNC-03 records a made decision rather than a pending one, and the three siblings each state precisely which half is settled and which half still binds.</done>
</task>

<task type="auto">
  <name>Task 2: The flagged-conflicts arithmetic and the FOUND-05 resolution</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the six-conflicts blockquote at :32, FOUND-05's entry and its FLAGGED CONFLICT paragraph at :111 onward, the treatment table at :455-461, the footer date lines at :464-466
    - `.planning/phases/38-post-auth-sync/38-04-SUMMARY.md` — the clauses actually struck
    - `.planning/phases/38-post-auth-sync/38-CONTEXT.md` § D-03
  </read_first>
  <action>
The removal plan 38-04 performed makes one of the six recorded conflicts void. Update the arithmetic
in all three places it is stated, and resolve rather than delete the entry — the project's convention
is that a divergence leaves a trace a later reader can find.

- **FOUND-05's FLAGGED CONFLICT paragraph.** Rewrite it to record that the conflict is **resolved by
  removal**, dated 2026-09-01 by Phase 38 (D-03): the clauses it quoted no longer exist in
  `SHARED-INVARIANTS.md`, so there is no surviving invariant text to diverge from. Keep the paragraph
  rather than deleting it, and keep it stating what the removed text used to require, so the history
  of the divergence survives. Note that `01-foundation.md §4` still states the same obligation in
  phase form and is deliberately unedited, being a phase brief marked verbatim — so a reader who
  reaches the brief first sees an obligation the binding specification no longer carries.
- **The six-conflicts blockquote in the header block.** The count becomes **five**, and the § "Audit"
  entry moves out of the flagged list into a sentence recording it as resolved by removal on
  2026-09-01. Keep the paragraph's existing observation that the set of known divergences is one
  larger than the count because of the Phase 35 `limits` override, and re-derive that arithmetic
  correctly against the new count rather than copying the old sentence.
- **The treatment table row "Flagged conflicts against the binding specification, unresolved".** The
  bolded count becomes **Five**, the § "Audit" item is removed from the list, and the row gains a
  short trailing sentence naming the resolved-by-removal item and its date, in the same style the row
  already uses for Phase 37.4's two non-entries.

Update the footer date lines at the end of the file the way each previous amending phase did: the
current `Last updated` line moves to `Previously updated`, and a new `Last updated: 2026-09-01` line
names Phase 38 and what it settled.

Do not touch any other requirement, table row or count.
  </action>
  <verify>
    <automated>grep -nE 'Flagged conflicts against the binding specification' .planning/REQUIREMENTS.md; grep -c 'Last updated: 2026-09-01' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The treatment-table row reads `**Five.**` and no longer lists the § "Audit" item among the flagged conflicts.
    - The header blockquote states five conflicts and names the § "Audit" item as resolved by removal on `2026-09-01`.
    - FOUND-05's paragraph still describes what the removed text used to require — `git diff .planning/REQUIREMENTS.md | grep '^-' | grep -c 'auth_events'` is reported in the summary, and any removed occurrence is accounted for by a surviving restatement.
    - `grep -c 'Last updated: 2026-09-01' .planning/REQUIREMENTS.md` outputs `1` and the previous `Last updated` line survives as `Previously updated`.
    - The count appears consistently in all three places: no occurrence of the word `Six` remains in a sentence counting flagged conflicts — `grep -inE '\*\*six\.?\*\*|count is six' .planning/REQUIREMENTS.md` outputs nothing.
  </acceptance_criteria>
  <done>The conflicts count is five everywhere it appears, and the removed conflict is recorded as resolved with its history intact.</done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP Phase 38 success criterion 4</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` Phase 38 at :475-485 — criterion 4 is the long BLOCKED paragraph
    - `.planning/ROADMAP.md` Phase 39 at :487-498 — criterion 3's shape, the precedent for an audit criterion rewritten to what actually holds
    - `.planning/phases/38-post-auth-sync/38-CONTEXT.md` § D-01, § D-05
  </read_first>
  <action>
Replace Phase 38's success criterion 4 — the whole **BLOCKED** paragraph offering options (a) and
(b) — with a criterion describing what is built, matching the register of the other three criteria:
one short testable sentence.

The criterion states that the endpoint writes no durable audit row on any path and adds no per-attempt
telemetry beyond what already exists — one `request` line per attempt from the request middleware, and
one WARNING per rejection from the shared error handler — and that the decision to drop the durable-row
obligation is recorded in `REQUIREMENTS.md` under SYNC-03 with the matching removal from
`SHARED-INVARIANTS.md`.

Criteria 1, 2 and 3 are unchanged, byte for byte. The Goal line, the Requirements line and the
Depends-on line are unchanged. Every other phase entry in the file is unchanged — edit only the
criterion-4 paragraph, using a scoped replacement, never a whole-file rewrite.
  </action>
  <verify>
    <automated>grep -c 'BLOCKED' .planning/ROADMAP.md; git diff --stat .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 38 entry no longer contains the string `BLOCKED` — `sed -n '/#### Phase 38/,/#### Phase 39/p' .planning/ROADMAP.md | grep -c 'BLOCKED'` outputs `0`.
    - The Phase 38 entry still lists exactly four numbered success criteria.
    - `sed -n '/#### Phase 38/,/#### Phase 39/p' .planning/ROADMAP.md | sed -n '1,8p'` shows the Goal, Requirements and Depends-on lines unchanged from before the edit.
    - `git diff .planning/ROADMAP.md | grep -c '^-'` is small and every removed line belongs to criterion 4 or to the plan-list block — report the count and the ownership in the summary.
    - Phase 39's criterion 3 is unchanged: `git diff .planning/ROADMAP.md | grep -c 'PROF-02'` outputs `0`.
  </acceptance_criteria>
  <done>Phase 38's fourth success criterion states what is built and is checkable, and no other roadmap entry moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → every future phase's planner | These documents are read at plan time by phases 39 through 46 and determine what those phases believe they owe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-14 | Tampering | the SIGNOUT-02 amendment | high | mitigate | The audit half and the fail-closed half are amended separately and the note must state the fail-closed half is untouched; the acceptance criteria assert SIGNOUT-01's line is byte-identical and that the surviving-binding sentence is present, so a broad edit cannot silently relax a revocation rule. |
| T-38-15 | Repudiation | resolving a flagged conflict by deleting its record | medium | mitigate | FOUND-05's paragraph is rewritten as resolved-by-removal rather than deleted, and keeps what the removed text used to require; the acceptance criteria account for every removed occurrence. |
| T-38-16 | Tampering | an over-broad amendment closing APPLEHOOK-02 and PLAYHOOK-03 entirely | medium | mitigate | Both notes must name the surviving § "Global deletions" prohibition and must not claim the requirement is closed; asserted per note in the acceptance criteria. |

No package-manager install task exists in this plan, so no package-legitimacy gate applies.
</threat_model>

<verification>
- `grep -c 'BLOCKED' .planning/REQUIREMENTS.md .planning/ROADMAP.md` — no occurrence remains in the SYNC-03 entry or the Phase 38 entry.
- The flagged-conflicts count reads five in all three places it is stated.
- `git diff --stat` shows exactly two changed files, both inside `ns-api-gateway/.planning/`.
- `git -C /home/init/native-speaker diff --stat -- specs/` shows only the file plan 38-04 edited, and no phase brief.
</verification>

<success_criteria>
- SYNC-03 records the made decision, its date, its grounds and what exists in its place.
- The three siblings each state exactly which half is settled and which half still binds.
- The conflicts count is five and the resolved conflict keeps its history.
- ROADMAP criterion 4 describes what is built, with criteria 1 to 3 untouched.
</success_criteria>

<output>
Create `.planning/phases/38-post-auth-sync/38-05-SUMMARY.md` when done.
</output>
