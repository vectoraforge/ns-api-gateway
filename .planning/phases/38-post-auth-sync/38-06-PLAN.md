---
phase: 38-post-auth-sync
plan: 06
type: execute
wave: 4
depends_on: ["38-01", "38-02", "38-03", "38-05"]
files_modified:
  - tests/unit/test_sync_audit_removal.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SYNC-02, SYNC-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The single initial migration is unmodified by this phase and names no auth-event table: git shows no change under migrations/ and the schema audit-table expectation is still the one-member set it was"
    - "The sync service emits no structured-log event of its own on any path, success or failure, and imports no logging library"
    - "No new log event name is introduced anywhere under src/ by this phase"
    - statement: "Concurrent /auth/sync attempts each earn exactly one request line from the middleware carrying their own request id, and no attempt writes a durable per-attempt record anywhere — there is no shared per-attempt row for concurrent attempts to interleave on, because none is written"
      verification: backstop
    - "The full test suite, the e2e suite and the schema suite are green and ruff is clean at phase close"
    - "SYNC-01, SYNC-02 and SYNC-03 are each marked complete in REQUIREMENTS.md only after the suites are green"
  artifacts:
    - path: "tests/unit/test_sync_audit_removal.py"
      provides: "The executable guards that SYNC-03 was satisfied by removal, not by rebuilding"
      contains: "EXPECTED_AUDIT_TABLES"
  key_links:
    - from: "tests/unit/test_sync_audit_removal.py"
      to: "src/nativespeaker/api/services/sync.py"
      via: "an AST walk asserting the module makes no structured-logging call"
      pattern: "ast"
  prohibitions:
    - statement: "MUST NOT put a user id, an external subject, a provider value or any other per-account identifier into a structured-log label — labels come from a closed set of fixed branch names, and this endpoint's account state must not become queryable through the log pipeline"
      category: privacy
      verification: judgment
    - statement: "MUST NOT reintroduce per-attempt durable telemetry under another name — the milestone removed the auth-audit subsystem deliberately, and rebuilding it as a table, a counter, or a per-attempt event under a different label would restore the cost the developer removed while appearing to comply"
      category: values
      verification: judgment
---

<objective>
Prove SYNC-03 held: the obligation was removed, nothing was rebuilt, and nothing new is written or
logged — then close the phase against green suites (D-01, D-02).

Purpose: a decision to remove something is only durable if something fails when it comes back. These
guards are what make D-01 and D-02 enforceable rather than remembered.
Output: `tests/unit/test_sync_audit_removal.py`, green suites, and three checked requirement boxes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-post-auth-sync/38-CONTEXT.md
@.planning/phases/38-post-auth-sync/38-PATTERNS.md
@.planning/phases/38-post-auth-sync/38-01-SUMMARY.md
@.planning/phases/38-post-auth-sync/38-02-SUMMARY.md
@.planning/phases/38-post-auth-sync/38-03-SUMMARY.md
@.planning/phases/38-post-auth-sync/38-05-SUMMARY.md
@AGENTS.md
</context>

## Artifacts this phase produces

This plan creates `tests/unit/test_sync_audit_removal.py`. The full phase artifact inventory is in
`38-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The executable guards that nothing was rebuilt</name>
  <reversibility rating="costly">D-01 records the audit subsystem as dropped; rebuilding it means rewriting the single initial migration under a new id, because the v2.0 one-migration constraint forbids incremental migration files.</reversibility>
  <files>tests/unit/test_sync_audit_removal.py</files>
  <read_first>
    - `tests/schema/test_inventory.py` around :104 and :207 — the audit-table expectation and the assertion that consumes it
    - `src/nativespeaker/api/services/sync.py` (as left by plan 38-02)
    - `src/nativespeaker/api/routers/auth.py` (as left by plan 38-01)
    - `src/nativespeaker/api/logs.py` — the middleware that already writes one line per attempt, and its excluded-path set
    - `src/nativespeaker/api/errors.py` — the three `InternalError` subclasses whose declared level already produces a line per occurrence
    - `tests/unit/error_tree.py` — the in-repo precedent for a test module that reasons about source rather than behaviour
    - `.planning/phases/38-post-auth-sync/38-CONTEXT.md` § D-01, § D-02
  </read_first>
  <behavior>
    - Test 1: `tests/schema/test_inventory.py` still expects exactly one audit table. Parse that file with `ast`, find the `EXPECTED_AUDIT_TABLES` assignment, and assert its set literal equals `{"subscription_events"}`. It must fail if a second table name is added.
    - Test 2: no SQL file under `migrations/` mentions `auth_events` or `auth_event_result`, case-insensitively. It must fail if the deleted table is reintroduced.
    - Test 3: exactly one `.sql` file exists under `migrations/` — the v2.0 one-migration constraint, restated here so a rebuild that adds an incremental file fails in this phase's own guard rather than only in the schema suite.
    - Test 4: `src/nativespeaker/api/services/sync.py` makes no structured-logging call. Parse it with `ast` and assert there is no `Call` whose function is an attribute access on a name bound to a logger, and no import of `structlog` or of the standard `logging` module.
    - Test 5: no string constant anywhere under `src/` contains `auth_sync`, and no string constant anywhere under `src/` contains `auth_events`. Walk every `.py` file under `src/` with `ast` and inspect `ast.Constant` string values. This is what fails if a success event is added under an obvious name.
    - Test 6 (control, so the walk is proven non-vacuous): the same `src/` walk finds at least one existing structured-log event name that is known to be present — assert a known event string from `services/quota.py` is found. A guard that scans nothing passes vacuously; this proves it scans.
  </behavior>
  <action>
Create the guard module. Each behaviour above becomes one test in a class whose one-line docstring
states the rule it defends. Use the standard library's syntax-tree module for every source
assertion — never a shell-out and never a plain substring scan of raw file text, because a comment or
a docstring mentioning a name would otherwise satisfy or break a check for the wrong reason. The
control test is not optional: it is what distinguishes a guard that found nothing from a guard that
looked nowhere.

Keep the module in `tests/unit/` so it runs in the default invocation — the deselected markers are
the end-to-end and schema ones, and a guard that only runs behind a marker is a guard that does not
run. Follow AGENTS.md: docstrings three lines maximum, comments default to none and one line each.

Record in the summary, as a short paragraph, what this phase deliberately did not build: no table, no
writer, no call site, no per-attempt event, no counter metric, and no route-to-operation metadata —
and name the two records that do exist in their place, the middleware line per attempt and the one
warning per rejection from the shared error handler.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_sync_audit_removal.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_sync_audit_removal.py -v` passes and lists exactly 6 node ids — a zero-collection or partial-collection run fails this criterion.
    - Fault injection recorded in the summary, one per guard: add a second table name to the schema expectation, add a logging import to the sync service, and add a string constant naming a sync success event; observe the matching test fail each time; revert each and observe it pass.
    - The control test is proven meaningful: temporarily narrow the walk to an empty directory and observe the control test fail, then revert.
    - `git diff --stat migrations/` is empty and `git log --oneline -1 -- migrations/` shows no commit from this phase.
    - `git diff --stat tests/schema/` is empty.
    - `uv run pytest tests/unit/test_docstring_bar.py` passes.
  </acceptance_criteria>
  <done>Six guards fail if the audit subsystem, the migration, or a new per-attempt event returns, and each has been proven to fail by injecting the thing it forbids.</done>
</task>

<task type="auto">
  <name>Task 2: Close the phase against green suites</name>
  <precondition>A PostgreSQL 17 instance is reachable at the `DB_*` values in `.env` and the e2e Firebase fixtures resolve — both the end-to-end and the schema suites in this task need real infrastructure; halt and report rather than reporting a skipped suite as green.</precondition>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` § SYNC at :190-197 as left by plan 38-05
    - `.planning/ROADMAP.md` Phase 38 success criteria as left by plan 38-05
    - `pyproject.toml` — the pytest markers and the default deselection
    - the four prior summaries in this phase directory
  </read_first>
  <action>
Run the three suites and the linter, and record the numbers rather than the verdict: the default
invocation, the end-to-end suite, the schema suite, and the linter over both source roots. Compare
the default suite's passing count against the count recorded in `STATE.md` for the last phase, and
state the delta and what accounts for it.

Then check the three SYNC requirement boxes in `.planning/REQUIREMENTS.md`, and only then — a box
checked before the suites are green is the failure mode this task exists to prevent. Check exactly
those three and nothing else in the file.

Walk the four ROADMAP success criteria for Phase 38 one at a time and name, for each, the specific
test node id or command output that proves it. Criterion 1 is the one-instant derivation and the
agreement with what quota would act on; criterion 2 is the byte-identical equivalence; criterion 3 is
the unchanged table state; criterion 4 is the recorded decision and the absence of a durable row.
Where a criterion has no single proving node id, say so plainly instead of assigning one loosely.

Do not edit any source file in this task. If a suite fails, stop and report it — do not repair code
under a closing task, because a repair made here has no plan, no review and no test of its own.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
    <automated>uv run pytest -m e2e -q</automated>
    <automated>uv run pytest -m schema -q</automated>
    <automated>uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` passes; the summary records the passing count and its delta against the 1016 recorded in `STATE.md`, with the delta accounted for by the tests this phase added.
    - `uv run pytest -m e2e -q` passes with a non-zero collected count.
    - `uv run pytest -m schema -q` passes with a non-zero collected count.
    - `uv run ruff check src tests` exits 0.
    - `grep -n 'SYNC-0' .planning/REQUIREMENTS.md` shows all three entries checked, and `git diff .planning/REQUIREMENTS.md | grep -c '^+- \[x\]'` outputs `3` with no other checkbox changed.
    - Each of the four ROADMAP success criteria is mapped in the summary to a named node id or command output, or is explicitly stated as unproven.
    - `git diff --stat src/` is empty for this task.
  </acceptance_criteria>
  <done>Three green suites, a clean linter, three checked requirement boxes, and each ROADMAP criterion mapped to the evidence that proves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → log pipeline | Whatever this phase emits leaves the process and is retained by the deployment's log pipeline |
| repository → future phases | These guards are what stop a later phase reintroducing a subsystem the milestone removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-17 | Information Disclosure | structured-log labels emitted by the sync path | medium | mitigate | The sync service emits no event of its own (D-02), so no per-account value can reach a label from it; the syntax-tree guard fails if a logging call or import appears in the module. The repository's closed-label-set rule covers the paths that do log. |
| T-38-18 | Repudiation | the absence of a durable per-attempt record | low | accept | Accepted milestone-wide (D-01). The request middleware writes one line per attempt with the request id, method, path and status, and `/auth/sync` is not in its excluded-path set; each rejection additionally writes one warning. Recorded, not mitigated. |
| T-38-19 | Tampering | a later phase reintroducing the audit table through the single migration | medium | mitigate | The guards assert the audit-table expectation is a one-member set, that no migration file names the deleted table, and that exactly one migration file exists — each proven to fail by injection before being trusted. |

No package-manager install task exists in this plan, so no package-legitimacy gate applies.
</threat_model>

<verification>
- `uv run pytest tests/unit/test_sync_audit_removal.py` — six guards, each proven to fail by injection.
- `uv run pytest -q`, `uv run pytest -m e2e -q`, `uv run pytest -m schema -q` — all green with non-zero collection.
- `uv run ruff check src tests` — exits 0.
- `git diff --stat migrations/ tests/schema/ src/nativespeaker/api/errors.py` — all empty across the whole phase.
</verification>

<success_criteria>
- Nothing was rebuilt: no table, no writer, no call site, no per-attempt event, no counter, no route metadata — each guarded by a test proven to fail if it returns.
- The sync service emits no event of its own on any path.
- Three suites green, linter clean, three requirement boxes checked in that order.
- Every ROADMAP success criterion is mapped to named evidence or is explicitly declared unproven.
</success_criteria>

<output>
Create `.planning/phases/38-post-auth-sync/38-06-SUMMARY.md` when done.
</output>
