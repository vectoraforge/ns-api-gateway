---
phase: 46-post-auth-sign-out-all
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - tests/unit/test_adapter_interfaces.py
  - tests/unit/test_auth_package_shape.py
  - tests/unit/test_rejection_vocabulary.py
  - tests/unit/test_app_wiring.py
autonomous: true
requirements: [SIGNOUT-01, SIGNOUT-02]

estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The recorded Protocol method set names both seam calls, so the second Firebase Admin call is declared, not smuggled (D-01)."
    - "The recorded rejection vocabulary names revocation_unconfirmed, so the new refusal is an admitted one (D-05)."
    - "The recorded route inventory names /auth/sign-out-all in both wiring lists, so the route is pinned as narrowed (D-04)."
    - "A case proves the handler declares no database session, which is the whole proof of D-04."
    - "The whole unit suite is green after the four hand-written literals are re-written."
  prohibitions:
    - statement: "A ratchet number must be read from the failing run, never guessed and never derived from a plan."
      status: unverified
      verification: judgment
    - statement: "A case name or a docstring must not survive the edit still claiming the pre-phase shape."
      status: unverified
      verification: judgment
    - statement: "No source file under src/ is edited by this plan; every change is a test literal."
      status: unverified
      verification: judgment
  artifacts:
    - tests/unit/test_adapter_interfaces.py
    - tests/unit/test_auth_package_shape.py
    - tests/unit/test_rejection_vocabulary.py
    - tests/unit/test_app_wiring.py
  key_links:
    - "tests/unit/test_app_wiring.py _declared(/auth/sign-out-all) -> get_db absent -> D-04"
    - "tests/unit/test_rejection_vocabulary.py CONSTRUCTOR_ARGUMENTS -> RevocationUnconfirmed(stage=...)"
---

<objective>
Re-write the four hand-written unit literals plan 46-01 invalidated by construction, and add the one
case that proves the handler opens no database session.

Purpose: these four files record the shape of the auth package by hand. Plan 46-01 changed that
shape, so the unit suite is red until each literal states the new one. This plan is separate from
the tracer so the tracer stays at six files and this work runs the moment the tracer lands.
Output: a green unit suite, and a one-line case pinning D-04.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-post-auth-sign-out-all/46-CONTEXT.md
@.planning/phases/46-post-auth-sign-out-all/46-RESEARCH.md
@.planning/phases/46-post-auth-sign-out-all/46-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The three recorded-shape literals</name>
  <files>tests/unit/test_adapter_interfaces.py, tests/unit/test_auth_package_shape.py, tests/unit/test_rejection_vocabulary.py</files>
  <read_first>
    - tests/unit/test_adapter_interfaces.py:24 and :107-120 — the import allowlist and `TestFirebaseAdminAdapter`.
    - tests/unit/test_auth_package_shape.py:1-30 — `CURRENT` and `_measure`.
    - tests/unit/test_rejection_vocabulary.py:67-115 — `EVENT_NAMES`; :175-190 — `CONSTRUCTOR_ARGUMENTS` and its `Unavailable` entry.
    - src/nativespeaker/api/errors.py — `RevocationUnconfirmed` as plan 46-01 wrote it.
    - .planning/phases/46-post-auth-sign-out-all/46-RESEARCH.md § "Tests that break by construction".
  </read_first>
  <action>
Each of these is a hand-written literal that plan 46-01 invalidated. Re-write each one, and re-write
the surrounding name and docstring where they now state something false.

`tests/unit/test_adapter_interfaces.py` — the case at :110-111 asserts the Protocol's method set
equals `{"get_user_provider_data"}`. The set is now two names. Rename that case so its name no
longer claims one surviving method, change `TestFirebaseAdminAdapter`'s class docstring, which
reads "One method, one issuer-selected client", and add a case asserting
`FirebaseAdminAdapter.revoke_refresh_tokens`'s return annotation is `None` and its parameters
include `issuer`, on the model of the two cases that already do this for the read.

`tests/unit/test_auth_package_shape.py` — `CURRENT` holds the measured triple of modules, classes
and functions. Plan 46-01 added functions to the auth package, so the third element moved. Run the
suite, read the number `_measure` reports in the failure text, and write that number down. Do not
guess it and do not derive it from this plan: the case docstring says a later phase has to come here
and write the new number.

`tests/unit/test_rejection_vocabulary.py` — add `revocation_unconfirmed` to `EVENT_NAMES`, beside
`user_not_found`, `unavailable` and `not_linked`. Add a `CONSTRUCTOR_ARGUMENTS` entry for
`errors_module.RevocationUnconfirmed` on the `Unavailable` model, which is `((), {"stage": ...})`:
without an entry the walk constructs it with `()` and the keyword-only `stage` raises.

`tests/unit/test_app_wiring.py` is Task 2's file and is left alone here, so this task's gate is the
three files it touches rather than the whole suite.

Every comment written in this task is ASD-STE100 and one line (D-11).
  </action>
  <acceptance_criteria>
    - `tests/unit/test_adapter_interfaces.py` asserts the Protocol's method set is exactly `{"get_user_provider_data", "revoke_refresh_tokens"}`, and neither the case name nor the class docstring still claims one method.
    - A case asserts `revoke_refresh_tokens`'s return annotation is `None` and its parameters include `issuer`.
    - `tests/unit/test_auth_package_shape.py`'s `CURRENT` third element equals what `_measure` returns for the auth package.
    - `tests/unit/test_rejection_vocabulary.py` contains `revocation_unconfirmed` in `EVENT_NAMES` and a `RevocationUnconfirmed` entry in `CONSTRUCTOR_ARGUMENTS`.
    - No file under `src/` is edited by this task.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_adapter_interfaces.py tests/unit/test_auth_package_shape.py tests/unit/test_rejection_vocabulary.py -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED", or a summary line reporting "no tests ran"</fails_when>
    <automated>test "$(grep -c 'revocation_unconfirmed' tests/unit/test_rejection_vocabulary.py)" = "1"</automated>
    <fails_when>non-zero exit, which means the new event name is absent from the recorded vocabulary</fails_when>
    <automated>test "$(grep -c 'revoke_refresh_tokens' tests/unit/test_adapter_interfaces.py)" -ge "2"</automated>
    <fails_when>non-zero exit, which means the Protocol's method-set literal and its new return-annotation case were not written</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any output line beginning "src/" or "tests/"</fails_when>
  </verify>
  <done>The three recorded-shape files describe the auth package as plan 46-01 left it, and each is green.</done>
</task>

<task type="auto">
  <name>Task 2: The route inventory and the no-session case</name>
  <files>tests/unit/test_app_wiring.py</files>
  <read_first>
    - tests/unit/test_app_wiring.py:30-90 — `_declared`, the generic case, and the two parametrize literals at :70-73 and :80-83.
    - src/nativespeaker/api/routers/auth.py — the `sign_out_all` handler as plan 46-01 wrote it, for the dependency set it declares.
    - src/nativespeaker/api/app/dependencies.py — `get_linked_identity`, `get_firebase_adapter`, `get_db`.
    - .planning/phases/46-post-auth-sign-out-all/46-RESEARCH.md § Open Questions Q3, on why the one-line case replaces a session stand-in.
  </read_first>
  <action>
Add `/auth/sign-out-all` to both parametrize literals, at :70-73 and :80-83. They are identical
tuples on two cases and both must change.

Then add one case proving D-04: `get_db not in _declared(_route_at("/auth/sign-out-all"))`.
`_declared` names exactly the callables FastAPI resolved, so this is the whole proof that the
handler opens no session, and it needs no fixture. Research Q3 rejected a session stand-in for this
reason.

This is the task that closes the phase's unit suite, so its gate is `uv run pytest -q`.

Every comment written in this task is ASD-STE100 and one line (D-11).
  </action>
  <acceptance_criteria>
    - `uv run pytest -q` reports 0 failures.
    - `/auth/sign-out-all` appears in both parametrize tuples in `tests/unit/test_app_wiring.py`.
    - A case asserts `get_db` is not among the declared callables of `/auth/sign-out-all`.
    - No file under `src/` is edited by this task.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED" in the output</fails_when>
    <automated>test "$(grep -c '/auth/sign-out-all' tests/unit/test_app_wiring.py)" -ge "3"</automated>
    <fails_when>non-zero exit, which means the path is missing from one of the two parametrize tuples or from the no-session case</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any output line beginning "src/" or "tests/"</fails_when>
  </verify>
  <done>The whole unit suite is green with the new seam method, the new leaf and the new route present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the recorded shape -> a later reader | These four literals are the project's own inventory of the auth package; a stale one hides a change rather than reporting it. |
| the route -> the database session | A handler that declared `get_db` would open a session for a call that issues no statement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-05 | Elevation of privilege | the barrier on this route | high | mitigate | Task 2 adds `/auth/sign-out-all` to both wiring lists, so the route is pinned as narrowed by the same cases that pin `/auth/sync`. |
| T-46-11 | Tampering | the auth-package ratchet | low | mitigate | Task 1 writes the number `_measure` reports from a real failing run, so the ratchet keeps counting rather than being switched off. |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and edits no source file. |
</threat_model>

<verification>
- `uv run pytest -q` green
- `uv run ruff check src tests` clean
- No file under `src/` appears in this plan's commits
</verification>

<success_criteria>
The unit suite records the auth package as plan 46-01 left it, and one case proves the sign-out
handler declares no database session.
</success_criteria>

## Artifacts this phase produces

Created by this plan:
- The re-written Protocol method-set literal and the new return-annotation case in `tests/unit/test_adapter_interfaces.py`
- The re-measured `CURRENT` triple in `tests/unit/test_auth_package_shape.py`
- `revocation_unconfirmed` in `EVENT_NAMES` and the `RevocationUnconfirmed` entry in `CONSTRUCTOR_ARGUMENTS` in `tests/unit/test_rejection_vocabulary.py`
- `/auth/sign-out-all` in both wiring parametrize tuples, and the no-session case, in `tests/unit/test_app_wiring.py`

No source symbol is created by this plan.

<output>
Create `.planning/phases/46-post-auth-sign-out-all/46-02-SUMMARY.md` when done
</output>
