---
phase: 46-post-auth-sign-out-all
plan: 04
type: execute
wave: 3
depends_on: ["46-02", "46-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [SIGNOUT-01, SIGNOUT-02]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The one departure this phase makes from 11-sign-out-all.md, D-06's 401 on a Firebase 'no such user', is recorded as a dated flagged conflict by brief line."
    - "Both forward flags standing under SIGNOUT-01 are closed: the adapter seam by D-01, and the sign_out_all operation label by D-08."
    - "Every obligation the brief states that was already dead before this phase is listed by brief line and by the phase that removed its mechanism."
    - "The unbounded Firebase revocation write per attempt is recorded as an accepted, uncounted divergence, not filed as a defect."
    - "SIGNOUT-01 and SIGNOUT-02 are marked met on suite counts measured in this plan, not copied from Phase 45."
    - "ROADMAP success criterion 3 states what was built, and no longer says Phase 46 must decide."
    - "The files under /home/init/native-speaker/specs are byte-identical to their pre-phase state (D-10)."
  prohibitions:
    - statement: "A divergence from the binding brief must not be resolved by editing the brief; the record shows what the specification says and what this project chose."
      status: unverified
      verification: judgment
    - statement: "No operation label is added to core.auth_operation and the single migration is not edited (D-08)."
      status: unverified
      verification: judgment
    - statement: "No durable audit row and no audit writer is reintroduced by this record; Phase 38 D-03 settled that half."
      status: unverified
      verification: judgment
    - statement: "Suite counts must not be carried forward from an earlier phase's paragraph; every number is read from a run made in this plan."
      status: unverified
      verification: judgment
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "REQUIREMENTS.md SIGNOUT-01/02 -> 11-sign-out-all.md by line number"
    - "REQUIREMENTS.md header conflict counts -> the new D-06 conflict and the uncounted revocation-write divergence"
    - "ROADMAP.md Phase 46 criterion 3 -> D-05, D-07 and the middleware request line"
---

<objective>
Record what this phase decided, what it departed from, what was already dead, and what it knowingly
accepted.

Purpose: D-09 makes this a phase deliverable. `11-sign-out-all.md` and `SHARED-INVARIANTS.md` are
not edited (D-10), so `.planning/REQUIREMENTS.md` is the only place a later reader finds the
departures.
Output: the amended SIGNOUT block, the re-derived header counts, the STATE.md decisions and outcome
paragraph, and the rewritten ROADMAP criteria.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-post-auth-sign-out-all/46-CONTEXT.md
@.planning/phases/46-post-auth-sign-out-all/46-RESEARCH.md
@.planning/phases/45-post-auth-restore-subscription/45-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend SIGNOUT-01 and SIGNOUT-02 and re-derive the header counts</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the SIGNOUT block, the RESTORE block as the format model for a phase's own amendment, and the header's amendment paragraphs with their conflict and divergence counts.
    - /home/init/native-speaker/specs/auth-refactor-phases/11-sign-out-all.md — the brief, read for the line number of each obligation named below. Read only; it is not edited.
    - .planning/phases/46-post-auth-sign-out-all/46-CONTEXT.md — D-09's list, which is the outline of this entry.
    - .planning/phases/46-post-auth-sign-out-all/46-RESEARCH.md — the Assumptions Log, in particular A1.
    - .planning/phases/46-post-auth-sign-out-all/46-01-SUMMARY.md through 46-03-SUMMARY.md — what actually shipped.
  </read_first>
  <action>
Write dated entries under SIGNOUT-01 and SIGNOUT-02 in the blockquote style the RESTORE and
PLAYHOOK blocks already use. Do not edit `11-sign-out-all.md` or `SHARED-INVARIANTS.md` (D-10).

Record as **two forward flags now closed**:
  - The adapter seam flag Phase 37.2 D-09 raised. D-01 answers it the way FOUND-08 directs: the
    revocation method is declared beside its first implementation, so `FirebaseAdminAdapter` now
    carries two methods. No `RevocationOutcome` value type came back; the seam returns nothing on
    success and raises otherwise.
  - The `sign_out_all` operation label flag Phase 40 D-11 raised. D-08 answers it: no label is
    needed. Sign-out is not challenge-bearing and writes no durable row, so the surviving consumer
    `core.auth_challenges.operation` never wanted it. `core.auth_operation` keeps its four values
    and the single migration is not edited. State that this is decided on this route's own terms,
    as the flag required, and that Phase 45's matching decision is a precedent and not the answer.

Record as **one new flagged conflict against `11-sign-out-all.md`, by line**:
  - D-06 — a Firebase "no such user" answers **401 `auth_required`**. Against § "Error classes and
    triggers", which lists `auth_required` for barrier failures only and puts every non-confirmed
    revocation on the server-error surface. State the ground: the token verified and the identity
    row is linked, but the IDP has no account behind the uid, so nothing exists to revoke and no
    token can be minted for it again. State the rejected alternative: a 503 on every attempt for up
    to an hour until the ID token expires.

Record as **obligations already dead before this phase began**, each by the brief's line and by the
phase that removed its mechanism:
  - The route registry and its inventory table — Phase 37.1 D-06, already flagged under FOUND-01.
  - The durable record per attempt, and the two result values the brief names for it — Phase 37.1
    D-01 and Phase 38 D-03. Name both values as the brief spells them. **One of the two collides
    textually with a name this phase now uses for something else, and the entry must say so:** the
    brief's dead result value and this phase's new error class both read `revocation_unconfirmed`.
    The living one is the log event name derived from `RevocationUnconfirmed`, written at WARNING by
    the shared handler. Nothing durable is written.
  - The backend rate-limit exemption clauses — Phase 35 D-05, which deleted the engine from the
    product rather than deferring it.
  - Coalescing — Phase 35 D-05.
  - The exactly-one-`Authorization` wire contract at `:42` — removed by the developer on
    2026-08-30, already recorded under FOUND-01.
  - The gateway per-IP and per-user entries — the v2.1 gateway contract; `/auth` paths are in no
    HTTPRoute today.

Record as **accepted, not as a new defect**:
  - The unbounded Firebase revocation write per attempt, on the Phase 40 D-22 precedent. State what
    bounds it: the caller must already hold a valid Firebase ID token for a linked account, so this
    is one subject looping on itself, and it is narrower than the two unauthenticated webhook cases.
    State what closes it: the v2.1 gateway contract. Count it at the divergences end only.

Record as **a standing fact about the world, not a gap this phase left**:
  - Assumption A1. The mapping from the Identity Toolkit's `accounts:update` answer for a
    non-existent account to `USER_NOT_FOUND` is verified from the installed SDK source and
    corroborated by an upstream issue report, not by a probe against a live project. Both suites
    pass either way, because the case scripts the exception directly, so the exposure is
    production-only. Say what would settle it: one real-credential call against a deleted uid.

**Mark SIGNOUT-01 and SIGNOUT-02 met**, with the suite counts measured by Task 2's run written into
the entry. Do not copy Phase 45's numbers. State plainly which half of SIGNOUT-02 this phase
answers: the fail-closed half, which SIGNOUT-01 states independently and `SHARED-INVARIANTS.md`
§ "Fail-closed defaults" binds a third time. The audit half was settled by removal in Phase 38 and
this phase inherits no decision on it.

**Re-derive the header's counts** and add this phase's amendment paragraph in the same voice the
earlier ones use. Count from the file rather than incrementing blind: if D-06 is the only new
conflict and the revocation write is the only uncounted item, the flagged-conflict count moves from
thirty to thirty-one and the set of known divergences from forty to forty-two, and the list of
uncounted divergences gains an eleventh member. Verify each of those three numbers against the file
before writing it.

Every sentence is ASD-STE100: short, one idea each, no metaphors.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` carries a dated 2026 entry under SIGNOUT-01 naming D-01 and D-08 as the two closed forward flags.
    - A dated entry names D-06 as a flagged conflict against `11-sign-out-all.md`, with the brief's line reference.
    - The six dead obligations each appear once, each with a brief line reference and the phase that removed the mechanism.
    - The entry states that the brief's dead result value and this phase's error class share the text `revocation_unconfirmed`, and says which of the two exists.
    - The accepted revocation-write exposure names the v2.1 gateway contract as what closes it.
    - Assumption A1 is recorded with what would settle it.
    - SIGNOUT-01 and SIGNOUT-02 are checked, and the suite counts in their entries match Task 2's measured run.
    - The header's flagged-conflict count, divergence count and uncounted-divergence list each include this phase.
    - `/home/init/native-speaker/specs/auth-refactor-phases/11-sign-out-all.md` and `SHARED-INVARIANTS.md` are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'SIGNOUT-01' .planning/REQUIREMENTS.md)" -ge "12"</automated>
    <fails_when>non-zero exit, which means no amendment entry was written under the requirement</fails_when>
    <automated>test "$(grep -c '11-sign-out-all.md' .planning/REQUIREMENTS.md)" -ge "10"</automated>
    <fails_when>non-zero exit, which means the conflict and the dead obligations were not recorded against the brief by line</fails_when>
    <automated>test "$(grep -c 'D-06' .planning/REQUIREMENTS.md)" -ge "1"</automated>
    <fails_when>non-zero exit, which means the one new flagged conflict is missing from the requirement record</fails_when>
    <automated>test "$(grep -c '\- \[ \] \*\*SIGNOUT-01' .planning/REQUIREMENTS.md)" = "0"</automated>
    <fails_when>non-zero exit, which means SIGNOUT-01 is still an unchecked checkbox after the phase shipped</fails_when>
    <automated>echo "9de772ba5f5c42217cf0dbe596a6c264a239fa56ab10a190dddd08caac6153b7  /home/init/native-speaker/specs/auth-refactor-phases/11-sign-out-all.md
c04c58c2305a828dc1fceeae0e9aef03485d7efa49c72a76a10acd90482a919b  /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md" | sha256sum -c --status</automated>
    <fails_when>non-zero exit, which means the brief or the shared invariants were edited, against D-10</fails_when>
  </verify>
  <done>Every departure, every closed flag, every dead obligation and every accepted exposure of this phase is written into REQUIREMENTS.md, and the specification is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Record the decisions in STATE.md and rewrite the ROADMAP criteria</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md — § Accumulated Context, the v2.0 decisions block, and the Phase 44 and Phase 45 outcome paragraphs as the format model.
    - .planning/ROADMAP.md — the Phase 46 section at :754-765, its four success criteria, and the Progress table.
    - .planning/phases/46-post-auth-sign-out-all/46-01-SUMMARY.md through 46-03-SUMMARY.md.
  </read_first>
  <action>
Run the three suites and `ruff` first, in this plan, and use those numbers everywhere below. The
baseline this phase started from is unit 1255, e2e 333, schema 229. Schema is untouched by this
phase and must still read 229; a different number means something unrelated moved and must be
explained rather than written down silently.

`.planning/STATE.md` — add to § Accumulated Context, the v2.0 decisions block:
  - A second Firebase Admin call now lives on the same seam: `revoke_refresh_tokens` beside
    `get_user_provider_data`, each with its own retry wrapper and its own exhaustion leaf (D-01,
    D-02). A later phase adding a third call follows the same shape.
  - `RevocationUnconfirmed` shares 503 `verification_temporarily_unavailable` with `Unavailable` on
    purpose, so the two are told apart by class and by log event name, never by the wire (D-05).
  - `POST /auth/sign-out-all` answers 401 on a Firebase "no such user" (D-06). This is a departure
    from the brief and is recorded under SIGNOUT-01; a later reader must not read it as a bug.
  - `/auth/sign-out-all` writes one INFO line, `sign_out_all_confirmed`, carrying `identity_row_id`.
    This narrows Phase 38 D-02's "no success log line" to `/auth/sync`; it does not reopen it there
    (D-07).

Write the Phase 46 outcome paragraph in the voice the Phase 44 and 45 paragraphs use: what shipped,
what was measured rather than assumed, and what is recorded as accepted rather than fixed. Name the
route, the seam method, the leaf, the two orderings that were the phase's known mistakes and are now
pinned by cases, and the four hand-written literals that were re-written. Record the three suite
counts and `ruff check src tests` clean, all read from the run made in this plan. Do not increment
any frontmatter counter: read the plan and summary file counts from disk and write what you counted,
with a note saying so, as the existing counting notes in this file do.

`.planning/ROADMAP.md` — Phase 46:
  - **Rewrite success criterion 3 in full.** It currently says the phase is blocked and must decide
    the audit question. That question was settled by removal in Phase 38 D-03. The criterion now
    states what was built: every unconfirmed outcome is one 503 leaf, `RevocationUnconfirmed`,
    whose class name is its WARNING event name; one INFO line records a confirmed revocation with
    the identity row id; the middleware writes one `request` line per attempt; and no durable row
    is written. Remove the word BLOCKED and the instruction to decide.
  - Mark criteria 1, 2 and 4 against what answers each: criterion 1 by the confirmed-204 case and
    the explicit `app=` assertion; criterion 2 by the exhaustion case that asserts the class and by
    the refusal cases at the wire; criterion 4 by the absence of any token, session or counter, and
    by the case proving the handler declares no database session.
  - Update the Phase 46 row of the Progress table and the plan checklist for this phase. Use the
    marking convention the earlier completed phases already use; do not invent a new one.
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` § Accumulated Context carries four new v2.0 decision lines for D-01/D-02, D-05, D-06 and D-07.
    - `.planning/STATE.md` carries a Phase 46 outcome paragraph naming the route, the leaf and the four re-written literals.
    - The three suite counts in that paragraph come from a run made in this plan; the schema count reads 229.
    - `.planning/ROADMAP.md` Phase 46 criterion 3 no longer contains the word BLOCKED and states what was built.
    - `.planning/ROADMAP.md` marks criteria 1, 2 and 4, each naming what answers it.
    - The Phase 46 row of the ROADMAP Progress table shows the phase complete with its plan count.
    - `.planning/STATE.md` frontmatter counters are consistent with the plan and summary files on disk.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run pytest -m e2e -q &amp;&amp; uv run pytest -m schema -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED" — the counts recorded in STATE.md must come from a green run</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any output line beginning "src/" or "tests/"</fails_when>
    <automated>test "$(grep -c 'Phase 46 outcome' .planning/STATE.md)" = "1"</automated>
    <fails_when>non-zero exit, which means the outcome paragraph is missing or was written twice</fails_when>
    <automated>test "$(grep -c 'BLOCKED: requires a mechanism Phase 37.1 deleted' .planning/ROADMAP.md)" = "0"</automated>
    <fails_when>non-zero exit, which means success criterion 3 still tells this phase to decide a question Phase 38 settled</fails_when>
    <automated>test "$(grep -c 'sign_out_all_confirmed' .planning/STATE.md)" -ge "1"</automated>
    <fails_when>non-zero exit, which means D-07's narrowing of Phase 38 D-02 is not recorded where a later phase reads decisions</fails_when>
  </verify>
  <done>STATE.md carries the four decisions and the Phase 46 outcome with measured counts, and the ROADMAP criteria state what was built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's record -> a later phase's planner | A departure that is not written down is a departure a later phase silently re-decides. |
| the measured suite counts -> the requirement record | A count copied from an earlier phase would assert a green run that never happened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-08 | Repudiation | the phase record | medium | mitigate | D-06 is a dated, line-referenced entry against the brief; the brief is unedited, so the record shows both what the specification says and what this project chose. |
| T-46-09 | Tampering | the recorded suite counts | medium | mitigate | Task 2 runs the three suites in this plan and writes what it read; the schema count is pinned at 229 as the control. |
| T-46-06 | Denial of service | the unbounded revocation write | medium | accept | Recorded under SIGNOUT-01 with what bounds it and what closes it, on the Phase 40 D-22 precedent. |
| T-46-10 | Information disclosure | the production-only A1 exposure | low | transfer | Raised to the developer in the requirement record with what would settle it: one real-credential call against a deleted uid. |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and touches no source file. |
</threat_model>

<verification>
- `uv run pytest -q`, `uv run pytest -m e2e -q`, `uv run pytest -m schema -q` green
- `uv run ruff check src tests` clean
- The two files under `/home/init/native-speaker/specs/auth-refactor-phases/` are byte-identical to their pre-phase state.
</verification>

<success_criteria>
A reader who opens REQUIREMENTS.md after this phase sees the one departure from the brief, both
closed forward flags, every obligation that was already dead, and the one exposure accepted rather
than fixed.
</success_criteria>

## Artifacts this phase produces

Created by this plan:
- Dated amendment entries under SIGNOUT-01 and SIGNOUT-02 in `.planning/REQUIREMENTS.md`, and the re-derived header counts
- Four v2.0 decision lines and one Phase 46 outcome paragraph in `.planning/STATE.md`
- A rewritten success criterion 3 and marked criteria 1, 2 and 4 in the Phase 46 section of `.planning/ROADMAP.md`

No source symbol is created by this plan. It records the symbols plan 46-01 created:
`POST /auth/sign-out-all`, `revoke_refresh_tokens` on the seam and the Protocol,
`RevocationUnconfirmed`, `revoke_with_retry`, `_revocation_exhausted`, and the event names
`sign_out_all_confirmed` and `revocation_unconfirmed`.

<output>
Create `.planning/phases/46-post-auth-sign-out-all/46-04-SUMMARY.md` when done
</output>
