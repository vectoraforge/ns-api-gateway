---
phase: 34-schema
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - migrations/20260818_01_initial-release.sql
  - migrations/20260322_01_initial-release.sql
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: false
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05, SCHEMA-06, SCHEMA-07]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pogo apply` against the empty PostgreSQL 17 from plan 34-01 exits 0 and creates schemas `core` and `audit` in a single transaction"
    - "`migrations/` holds exactly one `.sql` file and its name is `20260818_01_initial-release.sql` (per D-02 and orchestrator DIRECTIVE-1)"
    - "`pogo rollback --count 1` leaves neither `core` nor `audit` in `pg_namespace`, and a second `pogo apply` afterwards succeeds and recreates all 17 tables"
    - "The applied database has 11 enum types in `core`, 15 tables in `core`, and 2 tables in `audit`"
    - "`core.users` has exactly seven columns — id, email, display_name, registered_at, active, created_at, updated_at — taken from the `00-schema.md §2` target-shape table at lines 84-94, not from the baseline `CREATE TABLE`"
    - "`to_regtype('core.subscription_plan')`, `to_regclass('core.usage_monthly')` and `to_regclass('core.subscription_events')` all return NULL against the applied database, and `information_schema.columns` has no row for (core, users, jwt_sub)"
    - "`core.access_grant_source` has exactly four labels and none of them is `promo` (§9.1)"
    - "Running `pogo apply` a second time against an already-migrated database is a no-op: pogo finds the id in `public._pogo_migration` and skips the file, exiting 0 without re-executing any DDL"
    - statement: "An interrupted or failed apply leaves no partial schema — `core`, `audit`, and the `public._pogo_migration` row are either all present or all absent, never mixed, because pogo wraps the whole file in one transaction"
      verification: backstop
    - "Applied against a genuinely empty database the migration succeeds with no pre-existing object present, and it seeds zero rows into `core.access_tiers` (§4: Phase 00 seeds NO tier rows), so the object inventory holds for a schema with no data at all"
    - "`.planning/REQUIREMENTS.md` SCHEMA-01, `.planning/ROADMAP.md` Phase 34 goal, and the `.planning/PROJECT.md` Key Decisions migration row all name `migrations/20260818_01_initial-release.sql` and describe a rename-and-replace, not a rewrite in place"
    - "No migration-related statement anywhere in `.planning/` still describes an in-place rewrite: the six locations are REQUIREMENTS.md SCHEMA-01 and its global-exclusions row, ROADMAP.md's Phase 34 goal, PROJECT.md's Constraints bullet and Key Decisions row, and STATE.md's v2.0 bullet — and each retains its own surviving invariant, in particular 'never add incremental migrations during v2.0'"
    - "`.planning/PROJECT.md` 'Known areas for future work' contains a bullet naming the `REVOKE DELETE ON core.external_identities` requirement (D-11)"
  prohibitions:
    - requirement_id: SCHEMA-06
      category: privacy
      status: unverified
      flagged: true
      statement: "MUST NOT add any column to `audit.auth_events` or `core.auth_challenges` that stores a raw external subject, a raw token, or any other plaintext credential material. `core.external_identities` is the sole plaintext reservation store — a deliberate, user-disclosed exception per §8 — and every other surface carries only the keyed BYTEA hash. A convenience plaintext column added 'for debugging' would silently turn the audit log into a credential store."
    - requirement_id: SCHEMA-07
      category: values
      status: unverified
      flagged: true
      statement: "MUST NOT weaken, relax, or retain any schema structure in order to keep the v1.6 application code importable or its tests green. The phase boundary accepts a knowingly-broken intermediate commit; a retained legacy column or a softened CHECK that hides the breakage is a silent scope reversal, not a kindness."
    - requirement_id: SCHEMA-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT ship a migration whose apply silently succeeds without producing the v2.0 schema. A database that already recorded a prior migration id must fail loudly rather than skip, so no developer or CI run can report green against a stale schema."
  artifacts:
    - path: "migrations/20260818_01_initial-release.sql"
      provides: "The single initial migration delivering the complete v2.0 auth schema in one apply from empty"
      contains: "-- migrate: rollback"
      min_lines: 400
  key_links:
    - from: "migrations/20260818_01_initial-release.sql"
      to: "pyproject.toml"
      via: "[tool.pogo] migrations = './migrations' — pogo reads every .sql in this directory; the file stem is the tracked migration id"
      pattern: "migrations = './migrations'"
    - from: "migrations/20260818_01_initial-release.sql"
      to: "core.access_grants_anti_abuse"
      via: "the two back-pointing deferrable FKs on core.access_grants added by ALTER TABLE after the anti-abuse table exists — the circular pair"
      pattern: "ALTER TABLE core\\.access_grants"
---

<objective>
**Phase tracer.** Deliver the complete v2.0 auth schema as one pogo migration and prove it end to end — applied against the empty PostgreSQL 17 from plan 34-01, rolled back, and re-applied — before a single line of the conformance suite is written.

Purpose: this is the thinnest path that touches every layer this phase modifies (migration file → pogo parser → PostgreSQL 17 → rollback → re-apply) and it carries the phase's real architectural risk. If the assembled single file does not apply, or the two `DROP SCHEMA … CASCADE` rollback statements do not fully unwind it, every downstream test is built on sand. RESEARCH.md already ran this exact assembly against a live PostgreSQL 16.2 with 0 failures across 32 conformance cases, so the file is expected to work — but PG 17 has never been exercised (assumption A1), and the seven-object gap in `§3–§7` (pitfall P-1) is invisible to anyone reading those sections in isolation.

This is a production-quality tracer, not a prototype. The file it produces is the file that ships. There are no stubs and no partial sections: a partial migration would hide exactly the failures this task exists to surface — the circular deferrable FKs, the STORED generated columns on both sides of an FK, and the seven baseline survivors.

Output: `migrations/20260818_01_initial-release.sql` (new), `migrations/20260322_01_initial-release.sql` (deleted), and four planning documents amended at six locations so CONFLICT-1 is closed rather than left dangling.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-schema/34-CONTEXT.md
@.planning/phases/34-schema/34-RESEARCH.md
@.planning/phases/34-schema/34-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way door — one migration file rewritten from scratch</name>
  <read_first>
    - `.planning/phases/34-schema/34-CONTEXT.md` decisions D-01, D-02, D-03, D-05 — the migration-file-shape decisions and their recorded reversibility ratings
    - `/home/init/native-speaker/specs/auth-refactor-phases/00-schema.md` lines 32-48 (`§1`) — the six-file sequence this decision overrides, and the non-negotiable statement ordering plus prohibition list that survive the override
    - `.planning/phases/34-schema/34-RESEARCH.md` section "Runtime State Inventory" — the verified consequence of each filename branch
  </read_first>
  <action>Halt and put the one-way door in front of the developer before the agent walks through it. D-01 is rated `one-way` in CONTEXT.md: once this file is the sole migration and the development database has been rebuilt from it, restoring a six-file incremental sequence means reconstructing a baseline state that no longer exists anywhere. Orchestrator DIRECTIVE-1 has already resolved the related filename question (CONFLICT-1) in favour of D-02's rename; that part is settled and is not re-opened here. What this gate confirms is the file-count decision itself.</action>
  <decision>Deliver the v2.0 schema as ONE migration file written from scratch against an empty database, deleting the existing `20260322_01_initial-release.sql`, rather than as the six incremental files `00-schema.md §1` specifies.</decision>
  <context>
    D-01 is rated one-way. After this task the repository has a single migration whose id is `20260818_01_initial-release`, and plan 34-01 has already dropped and recreated the development database. There is then no artifact anywhere — not in git, not in any database — expressing the v1.6 baseline that `§1`'s six-file delta sequence was written against. Undoing this is not a revert; it is reconstructing a state that no longer exists.

    The decision is not new: PROJECT.md's Key Decisions table already records it, Phase 27 in v1.6 set the precedent on this same file, and CONTEXT.md D-01 restates it and flags it per the SHARED-INVARIANTS conflict rule. RESEARCH.md CONFLICT-2 confirms `§1`'s statement ordering and prohibition list still bind either way. This gate exists because the reversibility rating says a human confirms one-way doors, not because the decision is in doubt.
  </context>
  <options>
    <option id="one-file">
      <name>One migration file, written from scratch (D-01 — the recorded decision)</name>
      <pros>The apply target is an empty database, so `§2`'s teardown has nothing to tear down and its `ALTER TABLE core.users` collapses into a direct `CREATE TABLE`. One file, one transaction, one reviewable artifact. Matches PROJECT.md, the Phase 27 precedent, and the v2.0 constraint "one initial migration, never add incremental migrations". RESEARCH.md verified this exact assembly applies cleanly.</pros>
      <cons>One-way as described above. The reviewer loses the six-step narrative of `§1` and must review a single ~500-line file — which D-06's five banner-commented sections are specifically designed to mitigate.</cons>
    </option>
    <option id="six-files">
      <name>Six incremental files exactly as `00-schema.md §1` specifies</name>
      <pros>Literal spec conformance; each file's rollback is independently exercisable; reversible in the sense that any single step can be rolled back.</pros>
      <cons>Requires a v1.6 baseline database to migrate *from*, which plan 34-01 has already destroyed and `§9.13` mandates destroying. File 1 would be a teardown of objects that do not exist. Directly contradicts PROJECT.md's recorded decision and the v2.0 "no incremental migrations" constraint, both of which later phases assume.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The developer has selected `one-file` or `six-files`, or the run is halted
    - If `six-files` is selected, this plan is abandoned and re-planned — do not attempt a hybrid
  </acceptance_criteria>
  <resume-signal>Select: one-file, six-files, or describe a concern.</resume-signal>
  <done>The one-way file-count decision is confirmed by a human and recorded in the plan summary.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end "the v2.0 schema applies from empty" — one migration file, one apply path</name>
  <precondition>A PostgreSQL 17 server is reachable using the five DB_* values in `.env`, and the database named by `DB_NAME` holds no `core` schema, no `audit` schema, and no `public._pogo_migration` row (plan 34-01 complete). Re-assert this before writing SQL; if it is false, halt rather than authoring a file that cannot be applied.</precondition>
  <reversibility rating="costly">D-02 renames the file, and the filename stem *is* pogo's tracked migration id. Any database that already applied the old id must be dropped and rebuilt — which `00-schema.md §9.13` mandates for the disposable development database regardless, and which plan 34-01 has already done.</reversibility>
  <files>migrations/20260818_01_initial-release.sql, migrations/20260322_01_initial-release.sql</files>
  <read_first>
    - `/home/init/native-speaker/specs/auth-refactor-phases/00-schema.md` — the binding specification. Read `§2` (lines 49-95) for the `core.users` TARGET shape at lines 84-94; `§3` (96-191) for the enums; `§4` (192-250) for identity and tiers; `§5` (251-340) for subscriptions and store tables; `§6` (341-501) for grants, anti-abuse, provider-account gates and usage; `§7` (502-609) for challenges and the auth audit log; `§8` (610-623) for retention, delete behavior and the cascade list; `§9` (624-642) for the fourteen rulings that override contrary prose; `§10` (643-648) for the object inventory this file must produce; and `§1` (32-48) for the non-negotiable statement ordering and the prohibition list
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` — binds every phase and wins over any phase brief on conflict. Flag conflicts, never resolve them silently
    - `migrations/20260322_01_initial-release.sql` — the file being replaced. Source of the pogo header shape (lines 1-4) and of six of the seven baseline survivors: `CREATE SCHEMA core` (line 6), `core.chat_role` (line 8), `core.subscription_status` (line 11), `core.chats` (25-31), `ix_chats_user_id` (33), `core.messages` (35-41). Do NOT copy its `core.users` (lines 13-21) — that is the baseline shape, not the target shape
    - `.planning/phases/34-schema/34-RESEARCH.md` — Pattern 1 (the seven-object table and the `core.users` target-shape transcription), Pattern 2 (the pogo header regex), Pattern 3 (comment handling in the apply body), "Anti-Patterns to Avoid", pitfalls P-1 through P-11, and Code Example 1 (the file skeleton with banners and rollback)
    - `.planning/phases/34-schema/34-PATTERNS.md` — the `migrations/…` section: header pattern, schema+enum block pattern, table+index pattern, partial-unique-index formatting, and the rollback pattern D-05 replaces
    - `.planning/phases/34-schema/34-CONTEXT.md` — decisions D-03 through D-10, which fix the `depends:` line, the description header, the rollback body, the five banner sections, the transcription rule, the `§9` supremacy rule, the prohibition list, and the comment-only REVOKE branch
    - `pyproject.toml` lines 70-73 — `[tool.pogo]` `migrations`, `database_config`, and `schema = 'api'`
  </read_first>
  <action>
Write `migrations/20260818_01_initial-release.sql` as the complete, single, from-empty v2.0 migration, then delete `migrations/20260322_01_initial-release.sql` with `git rm` in the same change.

**File name and header.** The file is `migrations/20260818_01_initial-release.sql` per D-02 and orchestrator DIRECTIVE-1 — renamed and replaced, never edited in place under the old id. Line 1 is a single-line description rewritten per D-04 to describe the v2.0 schema; the current line names tables this migration no longer creates. Line 2 is `-- depends:` with nothing after the colon (D-03 — this is the root migration). Nothing may sit before line 1 and nothing between lines 1 and 2: the parser at `pogo_core/migration.py:49` is `re.match(r".*--(.*)\s-- depends:(.*)[\s]?", …)` without DOTALL, so a two-line description, a banner above the description, or a blank line between them all raise `BadMigrationError` before any SQL runs (P-2). Additional prose is legal only *after* the `-- depends:` line, and the literal `-- depends:` must appear exactly once in the metadata.

**Section layout (D-06, and orchestrator DIRECTIVE-5).** The apply body carries five banner-commented sections in the spec's order: enums → identity/tiers → subscriptions/store → grants/anti-abuse/usage → challenges/audit. `CREATE SCHEMA IF NOT EXISTS core` and `CREATE SCHEMA IF NOT EXISTS audit` belong to none of those five, so author them as an explicit **unnumbered preamble** above the enums banner, and label it as such in the banner text. State in the file that the preamble is deliberate and not a sixth section. This closes CONFLICT-4 so a reviewer does not read it as a D-06 violation. Banner comments at column 0 are stripped by `pogo_core/migration.py:22` before the statement reaches PostgreSQL and banner-only blocks collapse to an empty statement that pogo skips, so they are free — write them, and write inline `§9` ruling citations at the points where a more natural-looking alternative was deliberately rejected.

**The delta gap — this is the phase's highest-probability failure (P-1, orchestrator DIRECTIVE-6).** `§3–§7` is a delta against a baseline that does not exist at apply time; it is NOT a complete file. Seven objects that `§10`'s inventory requires are never created by `§3–§7` and must be written by hand:
  - `CREATE SCHEMA core` and `CREATE SCHEMA audit` (the preamble above);
  - enums `core.chat_role` and `core.subscription_status`, which `00-schema.md:181` says "survive from the baseline unchanged and are not recreated" — true of the six-file sequence, false here. Take them verbatim from the old migration at lines 8 and 11;
  - table `core.users`, taken from the `§2` **TARGET shape table at `00-schema.md:84-94`** — seven columns: `id UUID PRIMARY KEY`, `email TEXT` (nullable — `00-schema.md:80` makes it nullable because it is copied only from a Firebase Admin record whose `emailVerified` is TRUE), `display_name TEXT` (the renamed `name`), `registered_at TIMESTAMPTZ`, `active BOOLEAN NOT NULL DEFAULT TRUE`, `created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`. Copying the baseline `core.users` at `20260322_01_initial-release.sql:13-21` instead would reintroduce `jwt_sub` and `subscription_plan`, violate SCHEMA-07, and still let the apply succeed — a silent failure. Do not add `NOT NULL` to `email`; do not recreate `ix_users_jwt_sub`;
  - tables `core.chats` and `core.messages`, verbatim from the old migration at lines 25-31 and 35-41 (`00-schema.md:82`: they "keep their existing definitions");
  - index `ix_chats_user_id`, verbatim from the old migration line 33.

**Statement ordering (`§1`, non-negotiable).** Schemas, then all eleven enums, then `core.users` → `core.chats` → `core.messages` with `ix_users_registered_at`, `ix_chats_user_id`, `ix_messages_chat_id`, then `core.external_identities` and its four indexes, then `core.access_tiers`; then `core.subscriptions` → `core.store_purchase_tokens` → `core.store_purchases` → `audit.subscription_events`; then `core.access_grants` → `core.access_grants_anti_abuse` → the `ALTER TABLE core.access_grants` statements adding the two back-pointing deferrable FKs (the circular pair, which is why they are an ALTER and not inline) → `ix_access_grants_one_free_grant_per_user_source` → `core.manual_grant_issuances` → `core.provider_accounts` → `core.provider_account_gate_consumptions` → `core.user_monthly_usage`; then `core.auth_challenges` → `audit.auth_events`.

**Baseline indexes that must NOT appear:** `ix_users_jwt_sub` (its column is gone), `ix_subscriptions_external_id` and `ix_subscriptions_user_provider_active` (`00-schema.md:333` says both are not recreated), and `ix_usage_monthly_user_month` (its table is gone). `ix_subscription_events_subscription_id` **is** recreated, but on `audit.subscription_events` — `00-schema.md:339`: "The index keeps its baseline name but now lives in `audit`."

**Transcription, not design (D-07).** Every table, column, enum label and its order, CHECK, foreign key, delete behavior, and index is dictated by `§3–§8`. Invent nothing. Where `§9` and other spec prose disagree, `§9` wins (D-08): `promo` is deleted from `core.access_grant_source`, leaving four labels; `invalid_attestation_or_integrity_proof` is deleted from `core.auth_event_result`, leaving 44; the product-entitled set in the `core.subscriptions` generated column is fixed at `('active','grace_period')` and is never a runtime toggle; `ends_at` is nullable because open-ended grants are legal.

**The prohibition list (`§1`, D-09) is absolute.** No triggers, stored procedures, rules, views, materialized views, extensions, partitioning, `NULLS NOT DISTINCT`, invented format CHECKs, or scheduled-job scaffolding, and no `ON UPDATE`/`ON DELETE` clause beyond the ones `§8` enumerates. `updated_at` is maintained by application writes, never by a trigger. Do not seed `core.access_tiers` — `00-schema.md:249` says Phase 00 seeds no tier rows. Do not add a format CHECK to `core.user_monthly_usage.monthly_period` (`00-schema.md:500`: free text, no format CHECK). Do not make `ix_access_grants_anti_abuse_idp_account_hash` unique (`00-schema.md:497`). Do not add a key-version column to `core.auth_challenges` (`00-schema.md:595`). Do not add an FK on `audit.auth_events.challenge_row_id` (`00-schema.md:608`).

**Do not name the constraints the spec leaves unnamed.** Auto-generated CHECK names are positional and order-fragile (P-8); explicit naming would deviate from the verbatim DDL D-07 mandates. Index names, which `§3–§7` states explicitly, are stable and must match exactly.

**The REVOKE comment (D-10).** Take the comment-only branch of `§8`'s final bullet: place a prominent comment at the `core.external_identities` definition stating that whoever provisions database roles must add `REVOKE DELETE ON core.external_identities`. Do not invent a database role — `§8` warns against it and this repository defines none, only a `{DB_USER}` placeholder in `[tool.pogo] database_config` and `docker-compose.yml`.

**Rollback (D-05).** Exactly two statements after the `-- migrate: rollback` marker: `DROP SCHEMA IF EXISTS audit CASCADE;` then `DROP SCHEMA IF EXISTS core CASCADE;`. This deliberately replaces the old file's eleven-statement reverse-order drop list because that list drifts out of sync with the apply section; the two-statement form cannot.

**Delete the old file.** `git rm migrations/20260322_01_initial-release.sql` in the same change. With both files present, pogo's `read_migrations` iterates every `.sql` in the directory, and because `-- depends:` is empty there is no ordering constraint between them: verified behaviour is that pogo applies the old file first and then fails the new one with `type "chat_role" already exists` (P-3).

**Apply it.** Run `pogo apply` against the database from plan 34-01. Do NOT pass `--create-schema`: `[tool.pogo] schema = 'api'` only sets `search_path`, pogo's own bookkeeping table is hard-coded to `public`, and PostgreSQL accepts a `search_path` naming a non-existent schema without error (P-10). Every statement in the file is schema-qualified, so the file applies correctly under `search_path = api` — which also means no unqualified object reference may enter the DDL, or it would silently target `api`. Do NOT use `pogo validate` as a pre-check: it crashes with a `TypeError` from `sqlparse` on the existing working baseline and exits 0 on that crash, so it is worse than useless as a gate (P-9). The gate is `pogo apply` against a real empty database and nothing else.
  </action>
  <verify>
    <automated>set -a && . ./.env && set +a && test "$(ls -1 migrations/*.sql | wc -l)" = "1" && test -f migrations/20260818_01_initial-release.sql && .venv/bin/pogo apply && .venv/bin/pogo rollback --count 1 && .venv/bin/pogo apply && .venv/bin/python - <<'PY'
import asyncio, os, asyncpg
DSN = "postgres://%s:%s@%s:%s/%s" % (os.environ["DB_USER"], os.environ["DB_PASSWORD"], os.environ["DB_HOST"], os.environ["DB_PORT"], os.environ["DB_NAME"])
async def main():
    c = await asyncpg.connect(DSN)
    enums = await c.fetchval("SELECT count(*) FROM pg_type t JOIN pg_namespace n ON n.oid=t.typnamespace WHERE n.nspname='core' AND t.typtype='e'")
    core_t = await c.fetchval("SELECT count(*) FROM pg_tables WHERE schemaname='core'")
    audit_t = await c.fetchval("SELECT count(*) FROM pg_tables WHERE schemaname='audit'")
    users_cols = await c.fetchval("SELECT count(*) FROM information_schema.columns WHERE table_schema='core' AND table_name='users'")
    gone = await c.fetchrow("SELECT to_regtype('core.subscription_plan') IS NULL a, to_regclass('core.usage_monthly') IS NULL b, to_regclass('core.subscription_events') IS NULL d, NOT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_schema='core' AND table_name='users' AND column_name='jwt_sub') e")
    sources = await c.fetchval("SELECT count(*) FROM pg_enum e JOIN pg_type t ON t.oid=e.enumtypid JOIN pg_namespace n ON n.oid=t.typnamespace WHERE n.nspname='core' AND t.typname='access_grant_source'")
    await c.close()
    assert enums == 11, "expected 11 core enums, got %d" % enums
    assert core_t == 15, "expected 15 core tables, got %d" % core_t
    assert audit_t == 2, "expected 2 audit tables, got %d" % audit_t
    assert users_cols == 7, "expected 7 columns on core.users (the section 2 target shape), got %d" % users_cols
    assert all(gone), "a legacy structure survived: %r" % (dict(gone),)
    assert sources == 4, "expected 4 access_grant_source labels, got %d" % sources
    print("OK apply/rollback/re-apply clean; 11 enums, 15 core tables, 2 audit tables, 7 user columns")
asyncio.run(main())
PY</automated>
  </verify>
  <acceptance_criteria>
    - `ls -1 migrations/*.sql | wc -l` outputs `1`, and that file is `migrations/20260818_01_initial-release.sql`
    - `git status --porcelain migrations/` shows `20260322_01_initial-release.sql` as deleted (staged `D`), not merely untracked or renamed-in-place
    - `.venv/bin/pogo apply` against the plan-34-01 database exits 0
    - `.venv/bin/pogo rollback --count 1` exits 0, after which `SELECT count(*) FROM pg_namespace WHERE nspname IN ('core','audit')` returns 0 and `SELECT count(*) FROM public._pogo_migration` returns 0
    - A second `.venv/bin/pogo apply` after that rollback exits 0 and recreates all 17 tables
    - A third `.venv/bin/pogo apply` with the migration already applied exits 0 and its output contains no `Applying` line (the idempotent-skip path)
    - Against the applied database: 11 enum types in `core`; 15 tables in `core`; 2 tables in `audit`
    - Against the applied database: `information_schema.columns` reports exactly 7 columns for `(core, users)`, and no row for `(core, users, jwt_sub)`
    - Against the applied database: `to_regtype('core.subscription_plan')`, `to_regclass('core.usage_monthly')`, and `to_regclass('core.subscription_events')` all return NULL
    - Against the applied database: `core.access_grant_source` has exactly 4 labels; `core.auth_event_result` has exactly 44; `core.auth_operation` has exactly 7; `core.subscription_provider` has exactly 2
    - Against the applied database: `SELECT count(*) FROM core.access_tiers` returns 0 (the migration seeds no tier rows)
    - Against the applied database: `SELECT count(*) FROM pg_trigger t JOIN pg_class c ON c.oid=t.tgrelid JOIN pg_namespace n ON n.oid=c.relnamespace WHERE n.nspname IN ('core','audit') AND NOT t.tgisinternal` returns 0, and `pg_views` and `pg_matviews` both return 0 rows for schemas `core` and `audit`
    - The file's line 2 is exactly `-- depends:` and line 1 is a single-line description; the literal `-- depends:` appears exactly once in the metadata block above `-- migrate: apply`
    - The rollback body between `-- migrate: rollback` and end-of-file contains exactly two executable statements, dropping `audit` then `core`, both `CASCADE`
    - The file contains a comment at the `core.external_identities` definition naming the `REVOKE DELETE` requirement for role provisioners (D-10)
  </acceptance_criteria>
  <done>`migrations/20260818_01_initial-release.sql` is the only migration in the repository, it applies cleanly to an empty PostgreSQL 17, its rollback removes both schemas completely, it re-applies cleanly afterwards, and a fourth apply is a silent no-op. The applied schema has the exact object counts `§10` requires and none of the legacy structures SCHEMA-07 deletes.</done>
</task>

<task type="auto">
  <name>Task 3: Close CONFLICT-1 in every planning document that still describes an in-place rewrite, and record the REVOKE requirement</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` line 16 — the SCHEMA-01 bullet, which currently names `migrations/20260322_01_initial-release.sql`
    - `.planning/REQUIREMENTS.md` line 134 — the global-exclusions table row `| Incremental migration files during v2.0 | One initial migration, rewritten in place |`
    - `.planning/ROADMAP.md` line 117 — the Phase 34 `**Goal:**` line, which currently says "Rewrite `migrations/20260322_01_initial-release.sql` in place"
    - `.planning/PROJECT.md` lines 161-165 — the "Known areas for future work" bullet list; line 174 — the `## Constraints` bullet beginning "**Migrations**: pre-launch DB with no data. One initial migration file, rewritten in place"; and line 212 — the Key Decisions row that currently reads "Rewrite `20260322_01_initial-release.sql` in place instead of the six-migration sequence…"
    - `.planning/STATE.md` line 62 — the v2.0 bullet "One initial migration rewritten in place; never add incremental migrations during v2.0 (overrides 00-schema.md §1/§2)"
    - `.planning/phases/34-schema/34-CONTEXT.md` D-02, D-10, D-11 — the rename decision, the comment-only REVOKE branch, and the requirement that the REVOKE note also reach PROJECT.md
    - `.planning/phases/34-schema/34-PATTERNS.md` section `.planning/PROJECT.md (docs)` — the exact bullet convention for the "Known areas for future work" list
  </read_first>
  <action>
Make six surgical amendments so CONFLICT-1 is closed rather than left dangling, and add one new bullet.

Orchestrator DIRECTIVE-1 resolved CONFLICT-1 in favour of CONTEXT.md D-02: the migration is renamed to `migrations/20260818_01_initial-release.sql` and the old file is deleted, not edited in place. Six locations across four documents still name the old file or still describe an in-place rewrite. Amend each so the corpus is internally consistent — a half-corrected corpus, where three statements say renamed-and-replaced and three still say "in place", is worse than either, because a later reader cannot tell which is current.

Two distinct things are being fixed and they must not be conflated. Locations 1-3 name the wrong *file*; locations 4-6 state the wrong *mechanism* in a general project constraint that never names a file at all. In locations 4-6, only the "in place" framing changes — the surviving invariant "never add incremental migrations during v2.0" is still true, still binding, and must be preserved word for word. Do not weaken it, do not drop it, do not merge it into the rename clause.

Keep every edit minimal and scoped — replace only the affected clause. Use `Edit`, never `Write`; a whole-file write on `ROADMAP.md`, `PROJECT.md`, or `STATE.md` destroys everything outside the diff window.

  1. `.planning/REQUIREMENTS.md` line 16 (SCHEMA-01): change the migration path it names, and change "The single initial migration … creates the complete v2.0 schema" so it reads as a rename-and-replace of the prior file. Keep the trailing clause "no incremental migration files are added" exactly as it is — that is a separate, still-true constraint. Do not renumber, reorder, or reword any other requirement; do not touch the Traceability table.
  2. `.planning/ROADMAP.md` line 117 (the Phase 34 `**Goal:**` line): replace "Rewrite `migrations/20260322_01_initial-release.sql` in place" with a phrasing that names `migrations/20260818_01_initial-release.sql` and states it replaces the deleted `20260322_01_initial-release.sql`. Leave the `**Requirements:**`, `**Depends on:**`, and `**Success criteria:**` lines and the trailing note about Phase 36 untouched. This is the only line in `ROADMAP.md` that changes — lines 198 and 202 also contain the words "in place", but they describe Phase 40 flipping an identity row's provider and have nothing to do with migrations. Do not touch them.
  3. `.planning/PROJECT.md` line 212 (the Key Decisions row beginning "Rewrite `20260322_01_initial-release.sql` in place"): amend the decision cell to describe the rename to `20260818_01_initial-release.sql` with the old file deleted. Keep the rationale cell's existing reasoning and append the reason DIRECTIVE-1 gives for preferring the rename: an in-place rewrite under the old pogo id is silently skipped on any database that already applied it, whereas the rename fails loudly. Leave the third cell's status marker as it is; do not touch any other row.
  4. `.planning/PROJECT.md` line 174 (the `## Constraints` bullet "**Migrations**: pre-launch DB with no data. One initial migration file, rewritten in place — never add incremental migrations during v2.0"): replace "rewritten in place" with wording that reflects the rename — the single initial migration is replaced by a renamed file, the superseded one deleted. Keep "pre-launch DB with no data" and keep the trailing "never add incremental migrations during v2.0" verbatim. The bullet names no file today; it does not need to start naming one.
  5. `.planning/REQUIREMENTS.md` line 134 (the global-exclusions row `| Incremental migration files during v2.0 | One initial migration, rewritten in place |`): amend only the right-hand rationale cell so it no longer says "rewritten in place" — the exclusion in the left-hand cell is unchanged and still binding. Keep it to one short cell; this is a table, not prose.
  6. `.planning/STATE.md` line 62 ("One initial migration rewritten in place; never add incremental migrations during v2.0 (overrides 00-schema.md §1/§2)"): replace the "rewritten in place" clause the same way. Keep the "never add incremental migrations during v2.0" clause and keep the trailing `(overrides 00-schema.md §1/§2)` parenthetical — that override is still exactly what this bullet records. Touch no other line in `STATE.md`; the file is live workflow state and everything else in it belongs to the orchestrator.

Then, per D-11, append one bullet to `.planning/PROJECT.md`'s "Known areas for future work" list (currently four bullets at lines 161-165). It records that normal application and cleanup roles must have no permission to delete `core.external_identities` rows, that the corresponding `REVOKE DELETE ON core.external_identities` is not in the migration because this repository defines no database role, and that it becomes actionable when role provisioning lands in the Kubernetes deployment. Follow the list's existing convention exactly: a single line, backticked identifiers, an em-dash before the "but here is the catch" clause. The migration comment from task 2 reaches whoever reads the SQL; this bullet reaches whoever provisions the cluster — different people, which is why both exist.
  </action>
  <verify>
    <automated>.venv/bin/python - <<'PY'
import pathlib, sys
OLD = "20260322_01_initial-release"
NEW = "20260818_01_initial-release"
CORPUS = [".planning/REQUIREMENTS.md", ".planning/ROADMAP.md", ".planning/PROJECT.md", ".planning/STATE.md"]
checks = []

req = pathlib.Path(".planning/REQUIREMENTS.md").read_text()
schema01 = [ln for ln in req.splitlines() if "SCHEMA-01" in ln and ln.strip().startswith("- [")][0]
checks.append((NEW in schema01, "REQUIREMENTS.md SCHEMA-01 names the new migration"))
checks.append((OLD not in schema01, "REQUIREMENTS.md SCHEMA-01 no longer names the superseded migration"))

road = pathlib.Path(".planning/ROADMAP.md").read_text()
goal = [ln for ln in road.splitlines() if ln.startswith("**Goal:**") and ("migration" in ln or "schema" in ln.lower()) and ("initial-release" in ln)][0]
checks.append((NEW in goal, "ROADMAP.md Phase 34 goal names the new migration"))
checks.append(("in place" not in goal, "ROADMAP.md Phase 34 goal no longer claims an in-place rewrite"))

proj = pathlib.Path(".planning/PROJECT.md").read_text()
row = [ln for ln in proj.splitlines() if ln.startswith("|") and "initial-release" in ln][0]
checks.append((NEW in row, "PROJECT.md Key Decisions row names the new migration"))
checks.append(("REVOKE DELETE ON core.external_identities" in proj, "PROJECT.md records the REVOKE requirement (D-11)"))

# Corpus sweep: no migration-related statement in any of the four documents may still say
# "in place". Scoped to lines that mention a migration, so the legitimate unrelated uses
# survive -- REQUIREMENTS.md UPGRADE-01 and ROADMAP.md Phase 40 both flip an identity row's
# provider "in place", and PROJECT.md has a JWT row about a skeleton being "in place".
stale = []
for path in CORPUS:
    for n, ln in enumerate(pathlib.Path(path).read_text().splitlines(), 1):
        low = ln.lower()
        if "in place" in low and ("migration" in low or "initial-release" in low):
            stale.append("%s:%d" % (path, n))
checks.append((not stale, "no migration statement in .planning/ still says 'in place' (stale: %s)" % ", ".join(stale)))

# The "in place" framing changes; each location's own surviving invariant must not.
never = "never add incremental migrations during v2.0"
constraints = [ln for ln in proj.splitlines() if ln.startswith("- **Migrations**")]
checks.append((len(constraints) == 1 and never in constraints[0].lower(),
               "PROJECT.md Constraints migration bullet survives and keeps its no-incrementals rule"))
excl = [ln for ln in req.splitlines() if ln.startswith("|") and "Incremental migration files during v2.0" in ln]
checks.append((len(excl) == 1, "REQUIREMENTS.md global-exclusions row still excludes incremental migration files"))
state = pathlib.Path(".planning/STATE.md").read_text()
sline = [ln for ln in state.splitlines() if "initial migration" in ln.lower()]
checks.append((len(sline) == 1 and never in sline[0].lower() and "overrides 00-schema.md" in sline[0],
               "STATE.md v2.0 bullet keeps its no-incrementals rule and its override parenthetical"))

bad = [m for ok, m in checks if not ok]
if bad:
    sys.exit("FAILED:\n  " + "\n  ".join(bad))
print("OK all ten document assertions hold")
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s SCHEMA-01 bullet names `migrations/20260818_01_initial-release.sql` and does not name `20260322_01_initial-release`
    - `.planning/REQUIREMENTS.md`'s SCHEMA-01 bullet still ends with the clause about no incremental migration files being added
    - `.planning/ROADMAP.md`'s Phase 34 `**Goal:**` line names `migrations/20260818_01_initial-release.sql` and does not contain the phrase "in place"
    - `.planning/ROADMAP.md`'s Phase 34 `**Requirements:**`, `**Depends on:**`, and `**Success criteria:**` lines are byte-identical to before (`git diff .planning/ROADMAP.md` shows a single changed line)
    - `.planning/PROJECT.md`'s Key Decisions row for the migration names `20260818_01_initial-release.sql` and states the old file is deleted
    - `.planning/PROJECT.md` contains the string `REVOKE DELETE ON core.external_identities`, in the "Known areas for future work" list, as a fifth bullet following the existing four
    - Across `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`, and `.planning/STATE.md`, no line that mentions a migration also contains the phrase "in place" — the corpus sweep in the verification script reports zero stale locations
    - `.planning/PROJECT.md`'s `## Constraints` migration bullet still exists as exactly one line and still ends with "never add incremental migrations during v2.0"
    - `.planning/REQUIREMENTS.md`'s global-exclusions table still carries exactly one row excluding "Incremental migration files during v2.0"
    - `.planning/STATE.md`'s v2.0 migration bullet still exists as exactly one line, still carries "never add incremental migrations during v2.0", and still ends with the `(overrides 00-schema.md §1/§2)` parenthetical
    - The four legitimate non-migration uses of "in place" are untouched and still present: `.planning/PROJECT.md:180` (the JWT-skeleton Key Decisions row), `.planning/REQUIREMENTS.md:70` (UPGRADE-01), and `.planning/ROADMAP.md:198` and `:202` (the Phase 40 identity-provider flip) — `grep -n "in place"` over the four documents returns exactly these four lines and nothing else
    - `git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md .planning/STATE.md` shows changes to exactly those four files and no others under `.planning/`. Do not assert a diff-line count on `.planning/STATE.md` — it is live workflow state the orchestrator writes to during the run, so a line count there is not a stable gate; the content assertions in the verification script cover it instead
    - The task's automated verification script exits 0 and prints `OK all ten document assertions hold`
  </acceptance_criteria>
  <done>No document in `.planning/` names the deleted migration or describes an in-place rewrite — all six locations across the four documents are corrected and each retains its own surviving invariant — and PROJECT.md records the deferred `REVOKE DELETE` requirement for whoever provisions database roles.</done>
</task>

</tasks>

<assumption_delta_decision>
The deterministic detector fired `pluralization` on the phrase "no second migration file present". That literal signal is about file count, not identity — but the phase does carry a real assumption delta underneath it, and it is worth naming because it is exactly the seam this checkpoint exists to catch.

**The noun that is now primary:** the verified external identity `(issuer, subject)`, resolved through `core.external_identities` → `core.users`. In v1.6 identity was singular and inline: one `core.users.jwt_sub` column holding one Firebase subject, with the user row itself doubling as the identity record.

**The decision: `promote`.** `core.external_identities` becomes the primary identity representation and `core.users` is demoted to the business-data row it always should have been — `jwt_sub` is deleted, not kept alongside. This is the correct move and it is already what `§4` and SHARED-INVARIANTS specify ("Identity is only the backend-verified `(issuer, subject)` resolved through `core.external_identities` → `core.users`; `core.users.id` is never an authentication key"). No accepted debt, because nothing was added alongside.

The same promote happens twice more in this phase: `core.subscription_provider` goes from one label to two and `core.store_purchases` is keyed per provider rather than assuming Apple; and monthly usage moves from `core.usage_monthly` keyed by `(user_id, month)` to `core.user_monthly_usage` keyed by `grant_id`, promoting the grant to the entitlement identity. In every case the old singular representation is deleted rather than retained.

**On the file-count signal itself: `no-change`.** One migration file is D-01's decision, confirmed at this plan's task 1, and success criterion 1's "no second migration file present" is an assertion that the singular stays singular — the opposite of a pluralization.

**Companion invariant test (suggested, already covered):** the invariant "no path resolves a principal from `core.users` alone" cannot be encoded in DDL and is Phase 35's barrier work. What this phase *can* encode is already in the plan: the `(issuer, subject)` UNIQUE constraint, the `ON DELETE RESTRICT` that makes identity rows undeletable, and the SCHEMA-07 introspection assertions proving `jwt_sub` is gone rather than merely unused. No extra task is added.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → PostgreSQL | The DDL is the entire enforcement layer for entitlement, identity, and audit integrity; anything it fails to constrain is unconstrained forever |
| application (future phases) → schema | Every later phase writes through these constraints; a missing constraint becomes an application-layer assumption nobody wrote down |
| developer database → repository | A stale applied migration id can make a wrong schema report as correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-02-01 | Elevation of Privilege | `core.access_grants` — free-grant farming | high | mitigate | `ix_access_grants_one_free_grant_per_user_source` is a lifetime partial unique index with **no status predicate**, so an expired first grant still occupies the slot. Task 2's acceptance criteria require the index to exist with exactly that predicate; plan 34-04 proves case R8 (a second free grant of the same source rejected after the first is expired). |
| T-34-02-02 | Elevation of Privilege | `core.access_grants` — two concurrent active grants (entitlement double-spend) | high | mitigate | `ix_access_grants_one_active_per_user`, a non-deferrable partial unique index evaluated per statement, so concurrent inserts cannot both win. `§6` explicitly forbids an application-side rejection path for this. Proven by case R7 in plan 34-04. |
| T-34-02-03 | Elevation of Privilege | `core.subscriptions` — entitlement without payment | high | mitigate | The deferred FK from `core.access_grants` onto the STORED generated `product_entitled_subscription_id`, whose expression is fixed at `('active','grace_period')` per `§9.14`. `billing_retry` is deliberately not entitled. Never a runtime toggle. Proven by cases E1/E2 in plan 34-04. |
| T-34-02-04 | Elevation of Privilege | `core.access_grants` — cross-account entitlement theft | high | mitigate | The deferred composite FK on the generated `(subscription_id, user_id)` pair rejects a grant pointing at another user's subscription at COMMIT. Proven by case OWN in plan 34-04. |
| T-34-02-05 | Repudiation | `audit.auth_events` — audit forgery / actor spoofing | high | mitigate | All-or-nothing actor CHECK (issuer + subject hash + key version together or none), the keyed BYTEA hash with its version column, and no free-text outcome column. Proven by cases A1-A6 in plan 34-04. |
| T-34-02-06 | Information Disclosure | `audit.auth_events`, `core.auth_challenges` — raw subject leakage | high | mitigate | Only `BYTEA` hash columns ship on these tables; `core.external_identities` is the sole plaintext reservation store, a disclosed exception per `§8`. Task 2's action forbids adding a plaintext-subject column, and the prohibition is recorded in `must_haves.prohibitions`. `§9.3` keeps `preauth_issuer` plaintext deliberately and `§9.4` forbids a key-version column on the challenge row. |
| T-34-02-07 | Tampering / Repudiation | `core.external_identities` — identity-row deletion to free a uniqueness reservation | high | mitigate | `core.external_identities.user_id` is explicitly `ON DELETE RESTRICT` (`§8`), so a user row with an identity row cannot be deleted. The role-level `REVOKE DELETE` is deferred per D-10 — recorded in the migration comment (task 2) **and** in PROJECT.md (task 3), because a SQL comment does not reach whoever provisions the cluster. |
| T-34-02-08 | Tampering | the schema as a whole — an unreviewed extra object | medium | mitigate | D-18's exact-set inventory assertions in plan 34-03 (name-set equality for enums, tables, and indexes, plus zero user triggers/views/matviews) fail on any object added later that is not in the file. Task 2's acceptance criteria assert the counts immediately. |
| T-34-02-09 | Tampering | `public._pogo_migration` — a green apply against a stale schema | high | mitigate | The rename (D-02 / DIRECTIVE-1) presents the file as a new, unapplied id, so a database holding the old id fails loudly instead of silently skipping. Plan 34-01 dropped and recreated the database per `§9.13`. Recorded as a prohibition in `must_haves.prohibitions`. |
| T-34-02-10 | Elevation of Privilege | `core.access_grants_anti_abuse` — a free grant with no anti-abuse evidence | high | mitigate | The declarative quartet in `§6`: the deferred FK on `anti_abuse_required_grant_id` (lower bound), the anti-abuse primary key (upper bound), the composite FK on `(grant_id, grant_source)`, and the per-source CHECK. `§6` states this is fully declarative — no trigger, no application check. Proven by cases LB and R1-R6 in plan 34-04. |
| T-34-02-11 | Spoofing | `core.external_identities` — re-registration of a retired Google/Apple account | medium | mitigate | The partial unique index on `(issuer, provider, provider_uid)` carries `WHERE provider_uid IS NOT NULL` and **no state predicate**, so it covers `historical` tombstone rows as well as `active` ones. Present by construction; the predicate is asserted verbatim in plan 34-03. |
| T-34-02-12 | Denial of Service | the migration's single transaction | low | accept | The whole apply runs in one transaction, so a very large schema briefly holds many locks. On an empty pre-launch database with no concurrent traffic this is irrelevant, and the alternative (per-statement autocommit) trades it for partial-apply states, which is strictly worse. |
| T-34-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Attack surface eliminated by construction, not residual risk tolerated.** Three verified controls: (1) this plan has zero install surface — no task runs a package manager, and every tool it uses (`pogo-migrate` 0.4.2, `asyncpg` 0.31.0) is already declared and installed; (2) `pyproject.toml` dependencies are unmodified, which plan 34-01's acceptance criteria assert directly (`git diff --stat -- pyproject.toml` empty); (3) RESEARCH.md's Package Legitimacy Audit forbids the `[SUS]` packages `pgserver`, `pytest-postgresql`, and `testing.postgresql` project-wide. There is therefore no `[ASSUMED]`/`[SUS]` package to gate. Should an executor find it needs a package after all, that is a scope change requiring a new blocking human checkpoint before installing. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Produced by **this plan (34-02)**. Every symbol below is new — none of it exists before this plan runs, so drift checks must treat all of it as created, not missing.

**Files**

| Action | Path |
|--------|------|
| create | `migrations/20260818_01_initial-release.sql` |
| delete | `migrations/20260322_01_initial-release.sql` |
| modify (six clauses across four files) | `.planning/REQUIREMENTS.md` (SCHEMA-01 + global-exclusions row), `.planning/ROADMAP.md` (Phase 34 goal), `.planning/PROJECT.md` (Constraints bullet + Key Decisions row + new future-work bullet), `.planning/STATE.md` (v2.0 migration bullet) |

**Schemas:** `core`, `audit`

**Enum types (11, all in `core`):** `chat_role` (2 labels), `subscription_status` (5), `subscription_provider` (2), `identity_provider` (3), `identity_state` (2), `auth_operation` (7), `access_grant_source` (4), `access_grant_status` (3), `auth_event_result` (44), `native_claim_provider` (2), `gate_consumption_kind` (2)

**Tables in `core` (15):** `users`, `external_identities`, `access_tiers`, `chats`, `messages`, `subscriptions`, `store_purchase_tokens`, `store_purchases`, `access_grants`, `access_grants_anti_abuse`, `manual_grant_issuances`, `provider_accounts`, `provider_account_gate_consumptions`, `user_monthly_usage`, `auth_challenges`

**Tables in `audit` (2):** `subscription_events`, `auth_events`

**Columns worth naming individually** (each is new, or new in this shape): `core.users.display_name` (the renamed `name`), `core.users.active`, `core.users.registered_at`, `core.users.email` (now nullable); `core.external_identities.issuer`, `.subject`, `.provider`, `.provider_uid`, `.identity_state`, `.historical_at`, `.free_grant_consumed_at`; `core.subscriptions.product_entitled_subscription_id` (STORED generated over `('active','grace_period')`); the four STORED generated columns on `core.access_grants`; `core.access_grants.anti_abuse_required_grant_id`, `.ends_at` (nullable), `.source`, `.status`; `core.access_grants_anti_abuse.native_claim_provider`, `.idp_account_hash`; `core.user_monthly_usage.grant_id` (the primary key), `.monthly_period` (free text, no format CHECK); `core.auth_challenges.preauth_issuer` (plaintext by `§9.3`), `.preauth_subject_hash` (BYTEA, no key-version column by `§9.4`); `audit.auth_events.actor_subject_hash` (BYTEA), `.actor_subject_hash_key_version`, `.challenge_row_id` (no FK, by `00-schema.md:608`), `.details` (JSONB with the six-key default skeleton)

**Explicitly named indexes (18):** `ix_users_registered_at`, `ix_chats_user_id`, `ix_messages_chat_id`, `ix_external_identities_user_id`, `ix_external_identities_provider`, `ix_external_identities_provider_account` (UNIQUE, `WHERE provider_uid IS NOT NULL`), `ix_external_identities_user_active`, `ix_subscriptions_user_id`, `ix_subscriptions_provider_external_id` (UNIQUE, no predicate), `ix_store_purchase_tokens_user_id`, `ix_store_purchases_provider_identity_value`, `ix_store_purchases_purchase_user_id`, `ix_access_grants_user_active`, `ix_access_grants_subscription`, `ix_access_grants_one_per_subscription` (UNIQUE), `ix_access_grants_one_active_per_user` (UNIQUE), `ix_access_grants_one_free_grant_per_user_source` (UNIQUE, no status predicate), `ix_access_grants_anti_abuse_idp_account_hash` (deliberately NOT unique), `ix_gate_consumptions_grant_id`, `ix_auth_challenges_expires_at`, `ix_auth_events_actor_issuer_subject_hash`, `ix_auth_events_challenge_row_id`, `ix_auth_events_operation_created_at`, `ix_auth_events_result_created_at`, `ix_subscription_events_subscription_id` (baseline name, now in `audit`). PostgreSQL adds 25 more auto-named constraint indexes, for 54 total across both schemas — plan 34-03 captures the exact set.

**Constraints created but deliberately left unnamed** (auto-named, positional, never asserted by name per P-8): the identity `provider`/`provider_uid` agreement CHECK on `core.external_identities`; the four-arm anti-abuse evidence CHECK; the `subscription_id` CHECK and the source CHECKs on `core.access_grants`; the three lifecycle/binding CHECKs and the operation CHECK on `core.auth_challenges`; the actor-triple CHECK, the operation CHECK, and the seven `details` shape CHECKs on `audit.auth_events`.

**Deletions (SCHEMA-07):** enum `core.subscription_plan`; columns `core.users.jwt_sub` and `core.users.subscription_plan`; tables `core.usage_monthly` and `core.subscription_events` (the latter reborn in `audit`); indexes `ix_users_jwt_sub`, `ix_subscriptions_external_id`, `ix_subscriptions_user_provider_active`, `ix_usage_monthly_user_month`; enum labels `promo` (from `access_grant_source`, `§9.1`) and `invalid_attestation_or_integrity_proof` (from `auth_event_result`, `§9.7`).

**Not produced, deliberately:** no triggers, stored procedures, rules, views, materialized views, extensions, partitions, or scheduled jobs (`§1`); no database role and no `REVOKE` statement (D-10 — comment only); no seed rows in `core.access_tiers` (`00-schema.md:249`).
</artifacts_this_phase_produces>

<edge_probe_disposition>
The deterministic edge probe surfaced 11 applicable rows for this phase, all initially `unresolved`; `.edge-coverage.json` holds the report. Seven were `unclassified` and required planner judgement. All 11 are dispositioned below with no dismissals and no silent drops; each is authored into the `must_haves.truths` of the plan that proves it, so (11 surfaced) == (11 authored + 0 flagged assumptions).

| # | Requirement | Category | Resolution | Verification | Authored in |
|---|-------------|----------|-----------|--------------|-------------|
| 1 | SCHEMA-01 | idempotency | resolved | explicit | 34-02 — "a second `pogo apply` is a no-op: pogo finds the id in `public._pogo_migration` and skips" |
| 2 | SCHEMA-01 | concurrency | resolved | **backstop** | 34-02 — an interrupted apply leaves no partial schema. Authored as a structured `{statement, verification: backstop}` marker because killing an apply mid-transaction is not a check this suite runs; the verifier abstains to `human_needed` rather than passing silently |
| 3 | SCHEMA-02 | unclassified → identity uniqueness | resolved | explicit | 34-04 — a duplicate `(issuer, subject)` is rejected whether the existing row is `active` or `historical` |
| 4 | SCHEMA-03 | unclassified → single-active-grant | resolved | explicit | 34-04 — the second active grant and the second free grant of the same source are both rejected |
| 5 | SCHEMA-04 | unclassified → generated-column integrity | resolved | explicit | 34-04 — explicit write raises `GeneratedAlwaysError`; a grant on an `expired` or `billing_retry` subscription is rejected at COMMIT |
| 6 | SCHEMA-05 | unclassified → challenge operation partition | resolved | explicit | 34-04 — `operation='restore_subscription'` is rejected by the challenge CHECK |
| 7 | SCHEMA-06 | unclassified → audit actor completeness | resolved | explicit | 34-04 — five malformed audit shapes rejected, the minimal valid triple accepted |
| 8 | SCHEMA-07 | unclassified → legacy absence | resolved | explicit | 34-02 and 34-03 — `to_regtype`/`to_regclass` negatives and the absent `jwt_sub` column |
| 9 | SCHEMA-08 | adjacency | resolved | explicit | 34-03 — inventory assertions are exact-set equality, not superset: one extra index, table, or enum fails |
| 10 | SCHEMA-08 | empty | resolved | explicit | 34-02 — applies against a genuinely empty database, and seeds zero `core.access_tiers` rows |
| 11 | SCHEMA-08 | ordering | resolved | explicit | 34-03 — enum labels asserted in `enumsortorder`, matching `§3`'s declared order, not as an unordered set |

**Prohibitions.** The two-stage recall/precision pass over the eight SCHEMA requirements over-produced fourteen must-NOT candidates; twelve were dropped as routine engineering or as constraints already written into `§1`, `§8`, `§9`, or CONTEXT.md (retaining `jwt_sub`, adding triggers or sweepers, seeding tiers, inventing a role, a `monthly_period` format CHECK, making the anti-abuse hash index unique, `NOT NULL` on `email`, restoring `promo`, making the entitled set a toggle, adding an FK on `challenge_row_id`, adding a key-version to challenges, deleting identity rows). Three survived precision and are authored descriptor-less into `must_haves.prohibitions` above, where each disposes flagged-unverified.

Canon-referral breadcrumbs (surfaced, deliberately not minted): SQL injection in the test harness's seed SQL is canon — covered by `/gsd:secure-phase` and by asyncpg `$1` parameter binding, which plan 34-03 mandates; not minted here. Indefinite retention of identity tombstones and audit rows raises GDPR/data-retention questions that are canon — covered by `/gsd:secure-phase`; `§8` and `§9.12` make the retention deliberate and disclosed, so not minted here.
</edge_probe_disposition>

<verification>
- The tracer's `<automated>` block is the phase's real gate: it asserts one migration file, a clean apply, a clean rollback, a clean re-apply, and the six inventory counts, all against the real PostgreSQL 17 from plan 34-01.
- `pogo validate` is not a gate and must not be run — it crashes on the existing working baseline and exits 0 on that crash (P-9).
- `pytest tests/schema` cannot run in this plan: the package and the `schema` marker do not exist until plan 34-03. `pytest tests/unit -q` should still pass, because the unit suite is mock-based and touches no database — RESEARCH.md assumption A6 records that the observed post-Phase-34 breakage is runtime, not import, so a failing unit suite here means something outside this plan's scope broke.
- Success criterion 1 of the ROADMAP ("a fresh `pogo apply` against an empty database produces the full schema with no error and no second migration file present") is fully discharged by this plan. Criteria 2-5 are proven by plans 34-03 and 34-04.
</verification>

<success_criteria>
1. `migrations/` holds exactly one `.sql` file, named `20260818_01_initial-release.sql`; the old file is deleted in git
2. `pogo apply` against an empty PostgreSQL 17 exits 0; `pogo rollback --count 1` leaves no `core` and no `audit`; a subsequent `pogo apply` exits 0 and recreates all 17 tables; a further `pogo apply` is a silent no-op
3. The applied database has 11 `core` enums, 15 `core` tables, 2 `audit` tables, and a `core.users` of exactly seven columns
4. `to_regtype('core.subscription_plan')`, `to_regclass('core.usage_monthly')`, `to_regclass('core.subscription_events')` are all NULL and `(core, users, jwt_sub)` has no row in `information_schema.columns`
5. Zero user triggers, zero views, zero materialized views in `core` and `audit`
6. `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`, and `.planning/STATE.md` name the new migration where they name one at all, no migration statement in any of them still says "in place", each location keeps its own surviving invariant (notably "never add incremental migrations during v2.0"), and PROJECT.md records the `REVOKE DELETE` requirement
</success_criteria>

<output>
Create `.planning/phases/34-schema/34-02-SUMMARY.md` when done. Record: the option chosen at the task 1 checkpoint; the final line count of the migration file; any place where the spec's DDL had to be adapted for the from-empty context beyond the seven documented survivors (there should be none — if there is, it is a spec conflict and must be flagged, not resolved silently); and any observed difference between PostgreSQL 17's behaviour and the PostgreSQL 16.2 findings in RESEARCH.md.
</output>
