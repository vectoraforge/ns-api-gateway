---
phase: 37.4-machine-generated-code-refactoring-part-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nativespeaker/api/crud/__init__.py
  - src/nativespeaker/api/crud/challenges.py
  - src/nativespeaker/api/app/lifespan.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/auth/create_user.py
  - src/nativespeaker/api/routers/auth.py
  - src/nativespeaker/api/quota.py
  - src/nativespeaker/api/services/chats.py
  - tests/unit/conftest.py
  - tests/unit/test_challenge_ids.py
  - tests/unit/test_create_user_precedence.py
  - tests/unit/test_auth_package_shape.py
  - tests/schema/test_create_atomicity.py
  - tests/schema/test_create_race.py
  - tests/e2e/test_quota.py
autonomous: true
requirements: [A-00]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 17000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.venv/bin/python -c \"import nativespeaker.api.app.main\"` exits 0 — the application tree imports (A-00)."
    - "`.venv/bin/python -m pytest -q` collects with zero collection errors and runs a non-zero number of tests (A-00)."
    - "`.venv/bin/ruff check .` exits 0 (A-00)."
    - "`.venv/bin/ty check`'s diagnostic count is measured and written into the SUMMARY as the ceiling every later 37.4 wave is measured against (A-00)."
    - "`tests/unit/test_auth_package_shape.py` passes against a freshly measured triple, not a guessed one (A-00)."
  artifacts:
    - src/nativespeaker/api/crud/challenges.py
    - tests/unit/test_auth_package_shape.py
  key_links:
    - "every importer of the challenge store -> `nativespeaker.api.crud.challenges.ChallengesDB` (one module path, one class name)"
    - "`app/lifespan.py` -> `ChallengesDB(...)` -> `app.state.challenge_store` -> `app/dependencies.py::get_challenge_store`"
  prohibitions:
    - statement: "This plan changes no behaviour. It repairs commit `d466a4b`'s incomplete rename and nothing else."
      status: required
      verification: "Every hunk in the diff is an import path, a class name, a `__repr__` string literal, a ruff import-order fix, or the one recorded triple in test_auth_package_shape.py. No `<action>` here removes, adds or reshapes a function, class, column or status code."
    - statement: "No 37.4 decision (D-01 … D-16, A-01 … A-10) is implemented in this plan."
      status: required
      verification: "`git show --stat` for this plan's commit touches none of: migrations/, config/config.yaml, auth/hmac_keyring.py, auth/extract_bearer.py, auth/context.py, errors.py, auth/exceptions.py, app/error_handlers.py."
---

<objective>
Repair the pre-existing breakage commit `d466a4b` left behind, so that the rest of Phase 37.4 is
verifiable at all. RESEARCH.md and VALIDATION.md call this **Wave 0**; it is `wave: 1` in frontmatter
because GSD wave numbering is 1-based.

**This is a precondition, not 37.4 scope creep.** `d466a4b` moved `api/database/` to `api/crud/` and
renamed `ChallengeStore` to `ChallengesDB` without updating call sites. At HEAD (`1c63214`) the tree
does not import, `pytest -q` gives 4 collection errors and runs 0 tests, `ruff` reports 22 errors,
`ty` reports 151 diagnostics, and `tests/unit/test_auth_package_shape.py:23`'s recorded triple
`(11, 32, 49)` no longer matches the measured `(10, 31, 41)`. Nothing in Phase 37.4 can be sampled,
gated or verified until this lands.

Purpose: restore a green, measurable baseline and record the `ty` ceiling every later wave is
measured against.
Output: an importable tree, a collecting-and-passing unit suite, a clean `ruff`, a recorded `ty`
baseline, and a re-recorded package-shape triple — as one repair commit that says so.

**Line-number warning for every downstream plan.** Every `file:line` citation in `37.4-RESEARCH.md`
and `37.4-PATTERNS.md` is against HEAD (`1c63214`). This plan's commit shifts them. Downstream
executors must re-verify **line numbers**, never the facts — the facts were read from the working
tree this session and are sound.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-CONTEXT.md
@.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-VALIDATION.md
@AGENTS.md
</context>

## Planning provenance

**Spec-less probe fallback: SKIPPED (visible skip, recorded not dropped).** This phase has no `REQ-`
style requirement IDs to probe — its requirement set is the decisions `D-01 … D-16` and the addenda
`A-00 … A-10` in `37.4-CONTEXT.md`. No probe-derived predicates are generated this run.
`must_haves.truths` are derived goal-backward from the phase goal and those 27 decisions.

**Detectors run at planning time, both negative:** API coverage → `detected: false` (no external API
integration is introduced). Assumption-delta → `detected: false`. Neither is raised; no coverage
matrix is fabricated.

**Schema push gate:** not applicable to this plan. It is `[BLOCKING]` in plan `37.4-05`, which owns
both in-place migration edits.

**D-15 (no standing deletion rule) applies to this plan too:** the repair fixes exactly the 13 stale
imports and 9 stale class names A-00 enumerates. Anything else found stranded is reported as a
question in the SUMMARY, never swept.

## Multi-source coverage audit (plan-local slice)

| Source | Item | Coverage |
|--------|------|----------|
| CONTEXT | A-00 — `d466a4b` left the tree unimportable; Wave 0 repairs it before any 37.4 work | COVERED — tasks 01-T1, 01-T2 |
| GOAL | ROADMAP 37.4 goal (written by plan `37.4-07` under D-16) | N/A for this plan — precondition repair only |
| REQ | none — phase carries no `REQ-` IDs | N/A (visible skip, above) |
| RESEARCH | Finding 0 (0a/0b/0c/0d) | COVERED — 0a+0b in 01-T1; 0c+0d in 01-T2 |

No gaps. No item deferred.

<tasks>

<task type="auto">
  <name>Task 1 (01-T1): Repair the 13 stale module paths and the 9 stale class names</name>
  <files>src/nativespeaker/api/crud/__init__.py, src/nativespeaker/api/crud/challenges.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/auth/create_user.py, src/nativespeaker/api/routers/auth.py, src/nativespeaker/api/quota.py, src/nativespeaker/api/services/chats.py, tests/unit/conftest.py, tests/unit/test_challenge_ids.py, tests/unit/test_create_user_precedence.py, tests/schema/test_create_atomicity.py, tests/schema/test_create_race.py, tests/e2e/test_quota.py</files>
  <read_first>
    - `37.4-RESEARCH.md` §"FINDING 0 (BLOCKING)" sub-sections **0a** and **0b** — the two verbatim site tables (13 import sites, 9 name sites). This is the authoritative enumeration; do not re-derive it, but DO re-confirm each site with `rg` before editing, because the tables were captured at `1c63214`.
    - `37.4-PATTERNS.md` §"Wave 0: do not copy a broken import forward" — names the five excerpt sites elsewhere in PATTERNS.md that quote the *broken* import, so no downstream plan replicates it.
    - `src/nativespeaker/api/crud/challenges.py` — the class is already named `ChallengesDB` at its definition; only importers and annotations lag.
    - `src/nativespeaker/api/crud/__init__.py` — the package facade whose two re-exports are broken.
  </read_first>
  <action>
Replace every occurrence of the non-existent package path `nativespeaker.api.database` with
`nativespeaker.api.crud` across `src/` and `tests/`. There are 13 such statements; run
`rg -n "nativespeaker\.api\.database" src tests` first and edit exactly what it reports. Two shapes
occur: submodule imports naming `challenges`, and package-level imports naming `ChatsDB` or
`GrantsDB` off `nativespeaker.api.database` directly. `tests/e2e/test_quota.py` holds a
function-local import, not a module-level one — fix it in place where it sits.

Settle the class name on `ChallengesDB`, matching the sibling `ChatsDB` / `GrantsDB` naming
`d466a4b` established, and update all 9 lagging sites: the import and annotation in `app/lifespan.py`,
the two in `app/dependencies.py`, the one in `auth/create_user.py`, the four in `routers/auth.py`,
and the test-side sites in `tests/unit/test_create_user_precedence.py`,
`tests/unit/test_challenge_ids.py`, `tests/schema/test_create_atomicity.py` and
`tests/schema/test_create_race.py`. Run `rg -n "ChallengeStore" src tests` and edit exactly what it
reports, including the string literal returned by `ChallengesDB.__repr__` in
`src/nativespeaker/api/crud/challenges.py`, which still spells the old name.

Change nothing else. No signature, no behaviour, no docstring rewrite, no D-12 sweep — D-12 applies
to files a *37.4 decision* rewrites, and this plan rewrites none. Commit as one atomic commit whose
message states that it repairs `d466a4b`'s incomplete rename and is a precondition of Phase 37.4,
not part of it.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import nativespeaker.api.app.main" && .venv/bin/python -m pytest -q --collect-only >/dev/null && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import nativespeaker.api.app.main"` exits 0 (it currently raises `ModuleNotFoundError`).
    - `rg -c "nativespeaker\.api\.database" src tests` reports no matches (exit status 1).
    - `rg -c "ChallengeStore" src tests` reports no matches (exit status 1).
    - `.venv/bin/python -m pytest -q --collect-only` reports 0 errors (it currently reports 4).
    - `.venv/bin/python -m pytest -q` exits 0 and runs more than 0 tests.
    - `.venv/bin/python -c "from nativespeaker.api.crud import ChatsDB, GrantsDB"` exits 0.
    - `.venv/bin/python -c "from nativespeaker.api.crud.challenges import ChallengesDB"` exits 0.
    - The `__repr__` of `ChallengesDB` yields a string beginning with the class's own name — assert with `.venv/bin/python -c` constructing nothing, by reading the literal in the source.
    - `git show --stat HEAD` for this commit touches only the 15 files in this task's `<files>` list.
  </acceptance_criteria>
  <done>The application tree imports, the unit suite collects with zero errors and passes, and no reference to the old package path or the old class name survives in `src/` or `tests/`.</done>
</task>

<task type="auto">
  <name>Task 2 (01-T2): Clean lint, record the `ty` ceiling, re-record the package-shape triple</name>
  <files>tests/unit/test_auth_package_shape.py, plus whatever `ruff check --fix .` rewrites</files>
  <read_first>
    - `37.4-RESEARCH.md` §"0c" and §"0d" — the measured triple `(10, 31, 41)` and the lint/typecheck counts, each captured at `1c63214`. Task 1 has since moved lines, so **re-measure rather than copy**.
    - `tests/unit/test_auth_package_shape.py` — read its `_measure()` implementation and the `CURRENT` literal near the top of the file, so the re-recorded value is produced by the file's own measurement function and not by hand-counting.
    - `37.4-VALIDATION.md` §"Wave 0 Requirements" and the "Re-record ×2 note" — this is the first of two deliberate re-recordings; the second happens in plan `37.4-07` after the phase's deletions land.
  </read_first>
  <action>
Run `.venv/bin/ruff check --fix .`. At HEAD this clears 17 of 22 errors automatically; fix the
residual by hand. Every residual is import-order (`I001`) or an unused import left by `d466a4b` —
if any residual is not one of those, stop and report it in the SUMMARY as a D-15 question rather
than changing behaviour to satisfy the linter.

Re-record `tests/unit/test_auth_package_shape.py`'s `CURRENT` triple. Do it by executing the file's
own `_measure()` against `src/nativespeaker/api/auth` and writing the returned tuple in, verbatim.
Do not write `(10, 31, 41)` because RESEARCH.md says so — RESEARCH.md measured it before Task 1.
Add nothing to the file beyond the changed literal; the ratchet's whole value is that the number is
measured, not chosen.

Run `.venv/bin/ty check` and record the exact diagnostic count in the plan SUMMARY under a heading
that names it the Phase 37.4 baseline ceiling. Every later 37.4 task's gate is
"`ruff check .` is clean AND the `ty` count has not increased above this number". Note in the SUMMARY
that roughly 48 of the current 151 diagnostics live in files this phase deletes or rewrites
(`test_hmac_keys.py`, `test_create_atomicity.py`, `test_challenge_store.py`, `test_error_registry.py`,
`app/error_handlers.py`, `test_conflict_classification.py`, `routers/auth.py`, `quota.py`), so the
count is expected to fall, never rise.
  </action>
  <verify>
    <automated>.venv/bin/ruff check . && .venv/bin/python -m pytest -q tests/unit/test_auth_package_shape.py && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/ruff check .` exits 0 and prints no error lines.
    - `.venv/bin/python -m pytest -q tests/unit/test_auth_package_shape.py` exits 0.
    - The `CURRENT` triple in `tests/unit/test_auth_package_shape.py` equals the tuple returned by that file's own `_measure()` when run against `src/nativespeaker/api/auth` at this commit — demonstrate the equality in the SUMMARY by pasting the command and its output.
    - `.venv/bin/ty check` completes and its diagnostic count is written into the SUMMARY as an integer under a heading naming it the Phase 37.4 baseline ceiling.
    - The recorded `ty` count is less than or equal to 151.
    - `.venv/bin/python -m pytest -q` still exits 0.
  </acceptance_criteria>
  <done>`ruff` is clean, the package-shape ratchet is green against a freshly measured triple, and the `ty` ceiling is a written number in the SUMMARY that later waves can be compared against.</done>
</task>

</tasks>

## Artifacts this phase produces

Listed once per plan so the drift pass can see both halves. **This plan produces none of them** — it
only restores the baseline. The full phase-level list:

**Created:** `Identity` (class), `get_identity` / `get_linked_identity` (accessors), `charge_quota`
(function), `AppError` (merged tree base), `AccountUnavailable` / `HistoricalIdentity` / `BlockedUser`,
`assert_tree_total` (registry-free totality walk), `get_session_factory` (dependency), new files
`src/nativespeaker/api/auth/identity.py` and `tests/unit/test_quota_seam.py`.

**Deleted:** `LinkedIdentity`, `PreAuthIdentity`, `IdentityKind`, `RequestContext`, `QuotaGate`,
`ErrorClass`, `REGISTRY`, `register_class`, `STATUS_TO_CLASS`, `assert_registry_total`, `AuthRejected`,
`ServiceError`, `ProviderAccountAlreadyLinked`, `_AdmissionRejected`, `HmacKeyring`, `HmacConfig`,
`extract_bearer`, `get_request_context`, `get_quota_gate`, `_insert_account`/`_flush_account` (merged),
three `BoundedReason` members (`missing_token`, `malformed`, `duplicate_authorization`), the
`claim_attempt_id` column, the `preauth_subject_hash` column (retyped to `preauth_subject`); files
`auth/context.py`, `auth/resolve_identity.py`, `auth/extract_bearer.py`, `auth/hmac_keyring.py`,
`auth/exceptions.py`, `tests/unit/test_hmac_keys.py`, `tests/unit/test_wire_contract.py`,
`tests/e2e/test_wire_contract.py`.

**This plan deletes exactly one thing:** nothing. It renames references only.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted bearer credential crosses here; unchanged by this plan |
| API → PostgreSQL | Parameterised SQLAlchemy only; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.4-01 | Elevation of privilege | identity type collapse | high | n/a here | Owned by plan `37.4-03`; this plan does not touch the identity types |
| T-37.4-02 | Information disclosure | historical vs blocked indistinguishability | medium | n/a here | Owned by plan `37.4-02` |
| T-37.4-03 | Tampering | startup totality walk | high | n/a here | Owned by plan `37.4-02` |
| T-37.4-04 | Tampering | undeclared class must answer 500 | high | n/a here | Owned by plan `37.4-02` |
| T-37.4-05 | Information disclosure | secret in a log line | high | n/a here | Owned by plan `37.4-02`; this plan adds no logging |
| T-37.4-06 | Spoofing | duplicate/folded Authorization header | medium | n/a here | Owned by plan `37.4-06` |
| T-37.4-07 | Information disclosure | plaintext `preauth_subject` | medium | n/a here | Owned by plan `37.4-05` |
| T-37.4-W0 | Tampering | a repair that changes behaviour while claiming not to | medium | mitigate | The two `prohibitions` above, enforced by a `git show --stat` file-list check and by the unit suite going from 0 tests run to a full pass with zero assertion edits |

No package-manager install occurs in this plan, so no package-legitimacy checkpoint applies. No new
control is added and none is removed.
</threat_model>

<verification>
- `.venv/bin/python -c "import nativespeaker.api.app.main"` exits 0.
- `.venv/bin/python -m pytest -q` exits 0 with zero collection errors.
- `.venv/bin/python -m pytest -q -m ""` runs; record which of `e2e` / `schema` were skipped for want
  of credentials, and why, in the SUMMARY.
- `.venv/bin/ruff check .` exits 0.
- `.venv/bin/ty check` count recorded as the phase baseline.
- `rg -c "nativespeaker\.api\.database" src tests` and `rg -c "ChallengeStore" src tests` both report
  no matches.
</verification>

<success_criteria>
Phase 37.4 has a green, measurable baseline: the tree imports, the unit suite passes, lint is clean,
the typecheck ceiling is a recorded integer, and the package-shape ratchet holds against a measured
triple. The commit message names `d466a4b` as the thing being repaired and states that this is a
precondition of Phase 37.4 rather than part of its scope.
</success_criteria>

<output>
Create `.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-01-SUMMARY.md` when done.
It MUST contain: the recorded `ty` baseline integer; the freshly measured package-shape triple with
the command that produced it; and a one-line note to downstream executors that every `file:line` in
`37.4-RESEARCH.md` and `37.4-PATTERNS.md` predates this commit and must be re-verified as a line
number, not as a fact.
</output>
