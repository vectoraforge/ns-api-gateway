---
phase: 37.4
slug: machine-generated-code-refactoring-part-3
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-08-29
---

# Phase 37.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `37.4-RESEARCH.md` §"Validation architecture". The planner fills the
> Task ID column in the map below as it authors tasks.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest ≥9.0 with pytest-asyncio ≥1.3 (`asyncio_mode = "auto"`) |
| **Config file** | `pyproject.toml:56-68` |
| **Quick run command** | `.venv/bin/python -m pytest -q` |
| **Full suite command** | `.venv/bin/python -m pytest -q -m ""` |
| **Estimated runtime** | ~60 seconds (unknown at HEAD — the suite does not currently collect; re-measure after Wave 0) |

Markers `e2e` and `schema` are deselected by default via `addopts`; `-m ""` selects everything.
Lint is `.venv/bin/ruff check .`; typecheck is `.venv/bin/ty check`.

**⚠ Baseline was red at HEAD (`1c63214`)** — 4 collection errors, 0 tests run, 22 `ruff` errors, 151
`ty` diagnostics (A-00 in `37.4-CONTEXT.md`). **Resolved by plan `37.4-01`** before any 37.4
requirement was sampled. Measured at phase end (2026-08-30): `pytest -q` **782 passed** in 32s,
`pytest -q -m ""` **1076 passed** with nothing skipped, `ruff check .` clean, `ty check` **100**,
`assert_tree_total()` exits 0.

---

## Sampling Rate

- **After every task commit:** `.venv/bin/python -m pytest -q` + `.venv/bin/ruff check .`
- **After every plan wave:** `.venv/bin/python -m pytest -q -m ""` (unit + schema; e2e where
  credentials allow) + `.venv/bin/ty check`, with the Wave 0 diagnostic count as the ceiling
- **Before `/gsd:verify-work`:** full suite green, `ty` at or below the Wave 0 baseline,
  `pogo apply` clean from an empty database
- **Max feedback latency:** 60 seconds

### `ty` diagnostic count, recorded per wave (final, 2026-08-30)

| Point | `ty` | Note |
|---|---|---|
| RESEARCH at HEAD `1c63214` | 151 | pre-repair; never used as the ceiling |
| plan `37.4-01` baseline | **136** | measured after the A-00 repair — the ceiling later waves are held to |
| plan `37.4-02` | 135 | `app/error_handlers.py` lost the two-tree double dispatch |
| plan `37.4-03` | **145** | +10, all one diagnostic: a handler reading `identity.user` behind a router-level declaration `ty` cannot see. Left **unsuppressed** on purpose — ten `# ty: ignore` comments would violate D-12 and erase the one static signal marking where residual hazard T-37.4-01 lives. **Ceiling rebaselined to 145 by the orchestrator** |
| plan `37.4-04` | 145 | at the rebaselined ceiling; two new seam diagnostics removed structurally, not suppressed |
| plan `37.4-05` | 108 | fell by 37 with the keyring's deletion |
| **plan `37.4-06`, final** | **100** | **36 below the `37.4-01` baseline of 136 and 45 below the 145 ceiling** |

**No `# ty: ignore` was added anywhere in this phase** (D-12). Every diagnostic that would have
pushed a wave above its ceiling was removed at source instead.

---

## Per-Task Verification Map

> **Wave numbering.** Prose in this file and in `37.4-RESEARCH.md` calls the repair wave "Wave 0".
> GSD wave numbers are 1-based, so the repair plan carries `wave: 1` in frontmatter and the columns
> below use the frontmatter numbers. "Wave 0" in prose = `wave: 1` = plan `37.4-01`.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 01 | 1 | A-00 | T-37.4-W0 | N/A | smoke | `.venv/bin/python -c "import nativespeaker.api.app.main"` | ❌ W0 | ✅ green |
| 01-T1 | 01 | 1 | A-00 | T-37.4-W0 | N/A | collect | `.venv/bin/python -m pytest -q --collect-only` | ❌ W0 | ✅ green |
| 01-T2 | 01 | 1 | A-00 | T-37.4-W0 | N/A | lint | `.venv/bin/ruff check .` | ❌ W0 | ✅ green |
| 02-T1 | 02 | 2 | D-05/D-08/D-09 | T-37.4-02/03/04/05 | the tracer: a raise travels the stack to one 403 with a one-field body and one named WARNING | unit | `.venv/bin/python -m pytest tests/unit/test_exception_handlers.py` | ✅ rewrite | ✅ green |
| 03-T1, 03-T3 | 03 | 3 | D-01/D-02 | T-37.4-01 | unlinked identity cannot be read as linked | unit | `.venv/bin/python -m pytest tests/unit/test_identity_accessors.py tests/unit/test_identity_resolution.py` | ✅ rewrite | ✅ green |
| 03-T2 | 03 | 3 | D-02 | T-37.4-01 | every route but the two exemptions requires a linked identity | unit | `.venv/bin/python -m pytest tests/unit/test_app_wiring.py::TestEveryRouteIsAuthenticated` | ✅ keep green | ✅ green |
| 03-T2 | 03 | 3 | D-03 | T-37.4-13 | `RequestContext` is gone; `route` is not re-derived anywhere | unit | `.venv/bin/python -m pytest tests/unit/test_app_wiring.py tests/unit/test_identity_accessors.py` | ✅ rewrite | ✅ green |
| 05-T4 | 05 | 4 | D-03 | T-37.4-23 | consume's WHERE no longer keys on an attempt id | e2e | `.venv/bin/python -m pytest -m e2e tests/e2e/test_challenge_store.py` | ✅ rewrite | ✅ green |
| 05-T2, 05-T3 | 05 | 4 | D-03 | T-37.4-20/21 | the migration applies and the lifecycle CHECK still bites | schema | `.venv/bin/python -m pytest -m schema tests/schema/test_constraints.py::TestAuthChallengeConstraints` | ✅ edit (A-06) | ✅ green |
| 02-T1 | 02 | 2 | D-05 | T-37.4-02 | the two 403 arms are byte-identical to a client | e2e | `.venv/bin/python -m pytest -m e2e tests/e2e/test_admission.py::TestOutcomesTwoAndThreeAreIndistinguishable` | ✅ keep green | ✅ green |
| 02-T1 | 02 | 2 | D-05 | — | the two arms are distinct log events | unit | `.venv/bin/python -m pytest tests/unit/test_exception_handlers.py` | ✅ rewrite | ✅ green |
| 06-T1 | 06 | 5 | D-06 | T-37.4-25/26 | the race loser answers 409 and rolls back all mutation | schema | `.venv/bin/python -m pytest -m schema tests/schema/test_create_race.py` | ✅ edit (A-02) | ✅ green |
| 04-T3 | 04 | 4 | D-07/A-03 | T-37.4-17 | a service rejection precedes any charge | unit | `.venv/bin/python -m pytest tests/unit/test_quota_seam.py` | ❌ W0 (new file) | ✅ green |
| 04-T3 | 04 | 4 | D-07/A-03 | T-37.4-14/16 | an exhausted allowance answers 429 and the LLM is never called | unit | `.venv/bin/python -m pytest tests/unit/test_quota_seam.py` | ❌ W0 (new file) | ✅ green |
| 04-T1, 04-T3 | 04 | 4 | D-07/A-03 | T-37.4-15 | no session or lock is held across the provider call | unit | `.venv/bin/python -m pytest tests/unit/test_quota_seam.py` | ❌ W0 (new file) | ✅ green |
| 02-T2 | 02 | 2 | D-08/A-01 | T-37.4-03 | a duplicate code fails startup | unit | `.venv/bin/python -m pytest tests/unit/test_error_registry.py` | ✅ rebuild | ✅ green |
| 02-T2 | 02 | 2 | D-08/A-01 | T-37.4-03 | an undeclared leaf fails startup | unit | `.venv/bin/python -m pytest tests/unit/test_rejection_vocabulary.py` | ✅ rebuild | ✅ green |
| 02-T2 | 02 | 2 | D-08 | T-37.4-08 | a class declares `status` and `code` together or neither | unit | `.venv/bin/python -m pytest tests/unit/test_error_registry.py` | ✅ rebuild | ✅ green |
| 02-T3 | 02 | 2 | D-08 | T-37.4-04 | 404/405/422 map; an unmapped status fails loudly; `Allow` survives a 405 | unit | `.venv/bin/python -m pytest tests/unit/test_error_contract.py tests/unit/test_error_registry.py` | ✅ rebuild | ✅ green |
| 02-T3 | 02 | 2 | D-09 | T-37.4-05 | one WARNING per rejection, named for the class, no secret in it | unit | `.venv/bin/python -m pytest tests/unit/test_exception_handlers.py` | ✅ rewrite | ✅ green |
| 06-T2, 06-T3 | 06 | 5 | D-10/A-05 | T-37.4-06/28 | no credential → 401 `auth_required`; the log event survives | e2e + unit | `.venv/bin/python -m pytest -m e2e tests/e2e/test_startup_assertion.py tests/unit/test_identity_accessors.py` | ✅ keep green | ✅ green |
| 06-T3 | 06 | 5 | A-09 | T-37.4-24 | the padding loosening is written into the suite as a named case, not left implicit | unit | `.venv/bin/python -m pytest tests/unit/test_identity_accessors.py` | ✅ rewrite | ✅ green |
| 06-T2 | 06 | 5 | D-10/A-04 | — | the verifier's five bounded reasons still classify | unit | `.venv/bin/python -m pytest tests/unit/test_jwt_security.py` | ✅ **green; 3 lines changed, all inside the import block — see the reading note below** | ✅ green |
| 05-T3, 05-T4 | 05 | 4 | D-11 | T-37.4-07 | consumption clears the plaintext subject | e2e + schema | `.venv/bin/python -m pytest -m e2e tests/e2e/test_challenge_store.py -m schema tests/schema/test_create_atomicity.py` | ✅ edit | ✅ green |
| 01-T2, 07-T3 | 01, 07 | 1, 6 | D-13 | — | the auth package measures its recorded shape | unit | `.venv/bin/python -m pytest tests/unit/test_auth_package_shape.py` | ✅ re-record ×2 | ✅ green |
| 07-T1, 07-T2 | 07 | 6 | D-16/A-09 | T-37.4-29 | **three** flagged conflicts recorded and two wire losses recorded as losses, none resolved | doc | `rg -c "FLAGGED CONFLICT" .planning/REQUIREMENTS.md` reports **6** (was to report 8 — see the count note below) | ✅ edit | ✅ green |
| 07-T3 | 07 | 6 | D-15/A-10 | T-37.4-30/31 | every stranded item is a written question; the README is queued, not edited | doc | `git diff --stat README.md` shows zero changed lines | ✅ edit | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Every row green, verified at phase end 2026-08-30** by `.venv/bin/python -m pytest -q -m ""` —
**1076 passed, 0 failed, 0 skipped**, so the `e2e` and `schema` layers really executed rather than
being deselected. Both "Manual-Only Verifications" below therefore ran automatically: the dev
database was dropped and re-applied from empty by plan `37.4-05` (`pogo rollback` / `pogo apply`,
both exit 0, with the column list and constraints read back from `information_schema` and
`pg_constraint`), and the e2e suite reached real Firebase and provider credentials because
`pytest-dotenv` resolves `.env` by walking *up* out of a worktree into the parent checkout.

**Count note for row `07-T1, 07-T2`.** The seeded expectation of **8** assumed the five flagged
conflicts D-16 and A-09 planned. Mid-phase the developer deleted the exactly-one-`Authorization`
wire contract from `SHARED-INVARIANTS.md` and deleted `FOUND-02` from `.planning/REQUIREMENTS.md`,
which removes the text D-10 and A-09 would have diverged *from*. Those two are therefore recorded
under FOUND-01 as **properties knowingly given up** rather than flagged as conflicts against text
that no longer exists, and three remain flagged: D-11 and D-03 under CREATE-02, D-06 under
CREATE-04, all against `02-create-user.md`, which is unamended. 3 new + 3 pre-existing = **6**. The
requirement the row actually protects — T-37.4-29, that no divergence goes unrecorded — is met:
nothing was resolved silently, and the four phase briefs still stating the deleted rule are reported
under FOUND-01 rather than left to be discovered.

**Reading note for row `06-T2`** (the tension this file carried into plan `37.4-06`). This file
requires `tests/unit/test_jwt_security.py` to stay green **unmodified**, while A-04 relocates the
`BoundedReason` enum that file imports. Both cannot hold literally. The reading adopted was A-06's —
mechanical edits permitted, **no assertion weakens** — and the outcome is 3 changed lines (2
deletions, 1 insertion), entirely inside the import block, because repointing the import made two
`from nativespeaker.api.auth.jwt_verifier import …` statements adjacent and ruff's `I001` requires
them merged. **No assertion was added, removed or altered**, verified by reading the whole diff.
Recorded so a later reader sees the reading rather than only the diff.

**Task ID column filled by the planner, 2026-08-29.** Task IDs are `{plan}-T{n}`, matching the
`Task N (NN-Tn): …` names in each `37.4-NN-PLAN.md`. Rows beyond the seeded set were added for tasks
the seed did not anticipate: the tracer slice (02-T1), the declare-both-or-neither control (02-T2),
the lock-not-held-across-the-provider-call control (04-T1/04-T3), the `RequestContext` retirement
(03-T2), the A-09 loosening record (06-T3), and the two documentation obligations (07-T1/07-T2,
07-T3). Plan `37.4-05` task 05-T1 is a blocking `checkpoint:decision` and has no automated command,
so it carries no row.

**Re-record ×2 note (D-13) — closed 2026-08-30.** `test_auth_package_shape.py`'s recorded triple was
to move twice: once in Wave 0 to match HEAD's real `(10,31,41)`, and again once D-13's folding and
D-08/D-10/D-11's deletions changed it. It moved **six** times, not twice, because four separate plans
each deleted or folded part of the package and each ratchet literal is rewritten in the commit that
changes it rather than batched: `(11,32,49)` → `(10,31,41)` (01) → `(10,30,39)` → `(9,14,34)` (02) →
`(8,12,34)` → `(8,11,34)` (03) → `(7,9,25)` (05) → **`(6,9,23)`** (06). Every one came from the file's
own `_measure()`, never from a document; the final triple was re-measured at phase end by plan
`37.4-07` and already matched, so that plan wrote nothing. All six are deliberate re-recordings
against a measurement, not test weakening — the ratchet is meant to make package growth a visible
edit, and this phase only shrank it.

---

## Wave 0 Requirements

- [x] Repair `d466a4b`: 13 stale `nativespeaker.api.database` imports → `nativespeaker.api.crud`
      *(plan `37.4-01`, `9e9e273`; `rg -n "nativespeaker\.api\.database" src tests` → no matches)*
- [x] Repair `d466a4b`: 9 sites naming `ChallengeStore` → `ChallengesDB`
      *(`9e9e273`; `rg -nw "ChallengeStore" src tests` → no matches. Three test-local **fake** classes
      still spell the old name and were left per D-15 — reported, never swept)*
- [x] `.venv/bin/python -c "import nativespeaker.api.app.main"` succeeds *(exits 0)*
- [x] `.venv/bin/python -m pytest -q` collects *(0 errors; 788 collected at Wave 0, **782 passed** at
      phase end after 827 lines of test whose subjects were deleted went with them)*
- [x] `.venv/bin/ruff check .` clean *(22 errors → **All checks passed**, and clean at every wave since)*
- [x] `.venv/bin/ty check` baseline recorded — **136**, measured after the repair rather than carrying
      RESEARCH.md's pre-repair 151 forward. Rebaselined twice since; see the sampling note below
- [x] `tests/unit/test_auth_package_shape.py`'s recorded triple re-recorded from `(11,32,49)` to the
      measured `(10,31,41)` *(`55b5963`)*
- [x] New file `tests/unit/test_quota_seam.py` seeded for D-07/A-03 — authored in the quota wave as
      stated *(plan `37.4-04`, `c9cd48e`, 16 cases)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The dev/test database re-applies cleanly after both in-place migration edits | D-03, D-11 | The established v2.0 pattern edits the single initial migration in place rather than adding an incremental one; there is no automated check that a *from-empty* apply succeeds | Drop and re-apply from empty, then run `.venv/bin/python -m pytest -q -m schema` |
| The e2e suite against real provider credentials | D-10, D-11 | `tests/e2e/` needs credentials the CI environment may not hold; the schema suite uses a scratch database while e2e uses the dev one (RESEARCH.md Pitfall 8) | Run `.venv/bin/python -m pytest -q -m e2e` in an environment with credentials |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — every row above carries an
      automated command; the one task without one (`05-T1`) is a blocking `checkpoint:decision` and
      correctly carries no row
- [x] Task ID column filled by the planner for every row (2026-08-29)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — every task in all seven
      plans ran `pytest -q` + `ruff check .` before its commit, per the sampling rate above
- [x] Wave 0 covers all MISSING references — the three `❌ W0` rows are closed: the two `01-T1`/`01-T2`
      smoke and lint rows by plan `37.4-01`, and `tests/unit/test_quota_seam.py` by plan `37.4-04`
- [x] No watch-mode flags — no command in this file or in any plan's `<verify>` uses one
- [x] Feedback latency < 60s — `.venv/bin/python -m pytest -q` measured at **32s** at phase end
- [ ] `nyquist_compliant: true` set in frontmatter — **left for `/gsd:validate-phase`; not
      self-certified by the executing plan**

**Approval:** pending — `status` and `nyquist_compliant` are `/gsd:validate-phase`'s to set.
