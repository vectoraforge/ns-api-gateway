---
phase: 37.4-machine-generated-code-refactoring-part-3
plan: 07
type: execute
wave: 6
depends_on: ["37.4-06"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-VALIDATION.md
  - tests/unit/test_auth_package_shape.py
autonomous: true
requirements: [D-13, D-15, D-16, A-09, A-10]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/ROADMAP.md`'s Phase 37.4 entry carries a real goal in place of the placeholder, a real requirements line in place of `TBD`, and a plan list naming all six plans with their waves (D-16)."
    - "`.planning/REQUIREMENTS.md` records **five** flagged conflicts against the binding specification, each attached to the requirement whose subject it is, each in the established dated style, each explicitly recorded and not resolved (D-16, A-09)."
    - "The five are distinct entries with distinct causes: the wire contract's first/last resolution and its reason vocabulary (D-10); the padding loosening (A-09); subject hashing (D-11); the attempt-id consumption condition (D-03); and the provider-account collapse (D-06)."
    - "`tests/unit/test_auth_package_shape.py`'s recorded triple is re-measured against the final `auth/` package and passes (D-13)."
    - "A `/gsd:docs-update` is queued for the stale README; the README itself is not edited in this phase (A-10)."
    - "Every stranded item any plan reported under D-15 is surfaced to the developer as a question, and none was swept."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - tests/unit/test_auth_package_shape.py
  key_links:
    - "`37.4-CONTEXT.md` D-01…D-16 and A-00…A-10 -> the ROADMAP requirements line (this phase's requirement set has no `REQ-` IDs)"
    - "each flagged conflict -> the `REQUIREMENTS.md` requirement block whose subject it is -> the standing table at the foot of the file"
    - "the final `auth/` package -> `_measure()` -> the recorded triple"
  prohibitions:
    - statement: "No conflict against `SHARED-INVARIANTS.md` is silently resolved. Each is recorded so a later reader finds the divergence rather than discovering it."
      status: required
      verification: "Each of the five entries contains the words that mark the established style — that the document binds every phase, wins over any conflicting phase brief, and has not been amended — and states what was knowingly given up."
    - statement: "`README.md` is not edited by this phase."
      status: required
      verification: "`git diff --stat README.md` across the whole phase branch shows zero changed lines."
    - statement: "`.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are edited by scoped replacement, never rewritten whole."
      status: required
      verification: "`git diff .planning/ROADMAP.md` touches only the Phase 37.4 entry; `git diff .planning/REQUIREMENTS.md` touches only the header amendment paragraph, the five requirement blocks receiving a conflict, the traceability rows, the standing tables and the footer date."
---

<objective>
Write the phase's record: the ROADMAP goal D-16 replaces, the five flagged conflicts D-16 and A-09
require in `REQUIREMENTS.md`, the final package-shape re-recording D-13 owes, the queued documentation
work A-10 defers, and the D-15 questions every earlier plan collected.

Purpose: Phase 37.4's roadmap goal was still `[Urgent work - to be planned]` with `Requirements: TBD`
when the discussion that produced this phase opened. The three directives that opened that discussion
are the phase's spec, and D-16 writes them back. And this phase gives up five things the binding
specification requires; a phase that takes those and does not write them down leaves a later reader to
discover the divergence by hitting it.

Output: an amended ROADMAP entry; five dated flagged-conflict entries in the established style; a
re-measured package-shape ratchet; a queued documentation task; and a written list of everything found
stranded and deliberately not touched.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-CONTEXT.md
@.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-VALIDATION.md
@.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-05-SUMMARY.md
@.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-06-SUMMARY.md
@AGENTS.md
</context>

## Planning provenance

**Spec-less probe fallback: SKIPPED (visible skip).** No SPEC and no `REQ-` IDs; `must_haves` derived
goal-backward.

**A-09 makes it five conflicts, not four.** CONTEXT.md D-16 says four and its canonical-references
section lists four. A-09 adds the padding loosening as a **fifth**, recorded as its own entry rather
than folded into the existing wire-contract text, because its cause differs from the
first/last-resolution divergence D-10 names. Where an addendum contradicts a decision, the addendum
wins. Five.

**A tension carried forward from plan `37.4-06`, to be recorded here:**
`37.4-VALIDATION.md` says `tests/unit/test_jwt_security.py` must stay green **unmodified**, while A-04
relocates the enum that file imports. The reading adopted was A-06's: mechanical edits permitted, no
assertion weakens, exactly one import line moved. Record it so a later reader sees the reading rather
than only the diff.

**D-15 is a collection obligation here, not a deletion licence.** This plan deletes nothing from the
source tree. It gathers the questions plans `37.4-01` through `37.4-06` each recorded and puts them in
front of the developer.

## Multi-source coverage audit (plan-local slice)

| Source | Item | Coverage |
|--------|------|----------|
| CONTEXT | D-13 — the package-shape triple re-recorded once, at the end, against the final package | COVERED — 07-T3 |
| CONTEXT | D-15 — anything else found stranded comes back as a question | COVERED — 07-T3 |
| CONTEXT | D-16 — the ROADMAP entry amended; the flagged conflicts recorded dated | COVERED — 07-T1, 07-T2 |
| CONTEXT | A-09 — the fifth flagged conflict, its own entry, its own cause | COVERED — 07-T2 |
| CONTEXT | A-10 — the README is queued, not fixed; the phase is not widened | COVERED — 07-T3, recorded as a prohibition |
| GOAL | the ROADMAP goal this phase must write | COVERED — 07-T1 |

No gaps. No item deferred.

<tasks>

<task type="auto">
  <name>Task 1 (07-T1): Write Phase 37.4's real goal, requirements line and plan list into the ROADMAP</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — the Phase 37.4 entry with its placeholder goal and `Requirements: TBD`, and the Phase 37.3 entry immediately above it, whose goal/requirements/plans formatting is the house style to match exactly.
    - `37.4-CONTEXT.md` `<domain>` — the four fronts, stated in the developer's own framing, and the out-of-scope list.
    - `37.4-CONTEXT.md` `<decisions>` and `<addenda>` — so the requirements line names the real requirement set.
  </read_first>
  <action>
Replace Phase 37.4's placeholder goal with a real one, in the same register and roughly the same
length as the Phase 37.3 entry above it. It must name all four fronts: the identity union collapsing
to one `Identity` class and one accessor pair; the two exception trees, the `ErrorClass` indirection
and the module-level registry collapsing into one tree served by one handler with a registry-free
startup totality walk; the code-shape rules landing (docstrings capped at three lines, comments cut to
the necessary ones and collapsed to a line each, one-function modules folded, the quota gate demoted
to a plain function); and the deletion of named disproportionate machinery — the HMAC keyring, the
hand-rolled bearer extractor and `claim_attempt_id` — each against an explicit spec requirement, each
flagged. State the client-visible scope honestly: no client-visible change except the two movements
D-06 and D-10 sanction and the sanctioned log-event vocabulary change.

Replace `Requirements: TBD` with a line saying there are no new requirement IDs — this phase's
requirement set is decisions D-01…D-16 and addenda A-00…A-10 in `37.4-CONTEXT.md` — and that five
conflicts against the binding specification are recorded as FLAGGED in `.planning/REQUIREMENTS.md`,
not resolved.

Replace the plan placeholder with the real plan list, grouped by wave in the Phase 37.3 entry's
format, and update the plan count to 7. All seven plan files are counted, including the Wave 1
precondition repair — Phase 37.3's entry counts every plan file it lists, and there is no house-style
precedent for excluding a precondition wave from the total:
- Wave 1: `37.4-01-PLAN.md` — repair `d466a4b` and re-baseline lint, typecheck and the package-shape
  ratchet (the A-00 precondition, not 37.4 scope)
- Wave 2: `37.4-02-PLAN.md` — the tracer: one exception tree in `errors.py`, one handler, a
  registry-free startup totality walk, and D-05's two account classes
- Wave 3: `37.4-03-PLAN.md` — the identity collapse: one `Identity`, two accessors, `RequestContext`
  retired, `auth/identity.py` folded
- Wave 4: `37.4-04-PLAN.md` — the quota seam: `charge_quota()` called inside the two service methods,
  the callback chain deleted
- Wave 4: `37.4-05-PLAN.md` — the two in-place migration edits, the HMAC keyring's deletion, and the
  dev-database re-apply
- Wave 5: `37.4-06-PLAN.md` — the create-user conflict collapse and the `HTTPBearer` substitution
- Wave 6: `37.4-07-PLAN.md` — the ROADMAP goal, the five flagged conflicts, and the phase's records

Edit by scoped replacement. Do not rewrite the file; a whole-file write destroys every phase entry
outside the diff window.
  </action>
  <verify>
    <automated>rg -c "Urgent work - to be planned" .planning/ROADMAP.md; rg -n "Requirements.*TBD" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Urgent work - to be planned" .planning/ROADMAP.md` reports no matches.
    - `rg -n "TBD" .planning/ROADMAP.md` reports no match inside the Phase 37.4 entry.
    - The Phase 37.4 entry's plans line reads `**Plans:** 7 plans` (or the equivalent completed form the house style uses at phase end).
    - The plan list names all seven plan files with their waves — the Wave 1 precondition repair plus six work plans — and the count is seven, matching the plans line.
    - The goal text names all four fronts and states the client-visible scope.
    - `git diff .planning/ROADMAP.md` touches only lines inside the Phase 37.4 entry; the Phase 37.3 and Phase 38 entries are byte-identical.
  </acceptance_criteria>
  <done>Phase 37.4's roadmap entry states what the phase actually did, what its requirement set is, and which plans delivered it — with every other phase entry in the file untouched.</done>
  <reversibility rating="reversible">Documentation edit.</reversibility>
</task>

<task type="auto">
  <name>Task 2 (07-T2): Record the five flagged conflicts in `REQUIREMENTS.md`, dated, in the established style</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read the three existing flagged-conflict entries in full. They are the style template: each quotes the binding text it diverges from, states that the document binds every phase and wins over any conflicting phase brief and has not been amended, states that this project has knowingly diverged and why, and says it is recorded so a later reader finds the divergence rather than discovering it. Also read the header amendment paragraphs for phases 37.1, 37.2 and 37.3, the traceability table, and the two standing tables at the foot of the file.
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` § "Wire contract" — the exact sentences the D-10 and A-09 conflicts diverge from.
    - `/home/init/native-speaker/specs/auth-refactor-phases/02-create-user.md` — the prepare-mode subject-hash requirement, the completion-mode consumption condition, and the provider-account uniqueness step.
    - `37.4-CONTEXT.md` D-03, D-06, D-10, D-11, A-08 and A-09 — the reasoning to write down, in the developer's own terms, so a later reader can re-examine it rather than rediscover it.
    - The `37.4-05-SUMMARY.md` and `37.4-06-SUMMARY.md` flagged-conflict statements handed forward by those plans.
  </read_first>
  <action>
Add a header amendment paragraph for Phase 37.4, dated, in the shape of the three that precede it,
saying what this phase amended and that it records five conflicts rather than resolving any.

Then write five entries, each attached to the requirement block whose subject it is — identify each by
reading, not by guessing: the wire-contract requirement owns the D-10 and A-09 entries, and the
create-user requirements own the D-03, D-06 and D-11 entries. Each entry follows the established style
exactly: quote the binding text, state that the document binds every phase and wins over any
conflicting phase brief and has not been amended, state that this project has knowingly diverged and
why, and state that it is recorded per the standing rule so a later reader finds the divergence.

The five, with their distinct causes:

1. **D-10, against `SHARED-INVARIANTS.md:18` and `:55`.** The hand-rolled extractor is replaced by the
   framework's bearer dependency. Duplicate `Authorization` field instances stop being a distinct
   rejection because the framework reads the header through a mapping that returns the first value and
   hides the rest — the resolution `:18` forbids by name. Comma-joined and line-folded values stop
   being distinctly rejected and degrade to a signature failure. Three members of `:55`'s named reason
   vocabulary are deleted; five survive with the verifier. Record the developer's framing verbatim in
   spirit: *"I would rather sacrifice a requirement than bloat my code."* Record the mitigating factor
   the developer named — Envoy sits in front and normalises headers, and `:20` already treats Envoy's
   JWT filter as defence-in-depth — **and record that this mitigation is unverified**, because no
   Envoy configuration exists in this repository and FOUND-09 is deferred to v2.1.
2. **A-09, against `SHARED-INVARIANTS.md:18`, as its own entry.** The framework's scheme/parameter
   split strips the parameter, so a value carrying padding around the token is now *accepted* where the
   extractor rejected it as malformed. This is a **loosening** of the same line that D-10 does not
   name, with a different cause, and the two tests that pinned the old behaviour die with D-10 so
   nothing will catch it. Note that plan `37.4-06` wrote the new behaviour into the unit suite as a
   named case precisely so the divergence is visible in the tests as well as here.
3. **D-11, against `02-create-user.md:49` and `SHARED-INVARIANTS.md:56`.** The subject verifier is
   stored in plaintext; the keyring, the key-version machinery and the constant-time comparison (A-08)
   are all deleted. Record the finding that drove it: `core.external_identities.subject` already stores
   the same raw subject permanently in plaintext with the stable provider account UID beside it, so
   432 lines protected a value for 300 seconds that the schema keeps forever one table over. Record
   that the clear-on-consume half of the rule **survives**, keyed on the column being NULL.
4. **D-03, against `02-create-user.md:65`.** Consumption is no longer conditioned on still-claimed
   under this attempt's claim id; the column is dropped. Record why: the condition could never fail,
   because nothing anywhere un-claims a challenge, so it was a compare-and-swap guard with an
   always-true predicate. Record what survives: the WHERE still keys on the challenge id, a non-null
   claim time and a null consumption time, so a double consumption still matches no row.
5. **D-06, the provider-account collapse.** Any integrity error from the insert becomes one
   already-linked answer, so the provider-account conflict stops earning its own
   `operation_not_allowed` on the race path. Record both accepted consequences: the distinct answer is
   lost **only** on the race path, because the pre-check that D-04 keeps still earns it in the common
   case; and the unmapped-constraint 500 tripwire dies, so a constraint rename or a new constraint will
   read as a benign 409 rather than surfacing — which was 37.3 D-03's explicitly preserved fail-loudly
   property, knowingly given up.

Update the traceability rows for the requirements that received an entry, the "Amended, substance
intact" standing table, the "Flagged conflicts against `SHARED-INVARIANTS.md`, unresolved" standing
table — which grows from three to eight — and the footer's last-updated line.

Edit by scoped replacement throughout. Do not rewrite the file.
  </action>
  <verify>
    <automated>rg -c "Phase 37.4" .planning/REQUIREMENTS.md && rg -c "FLAGGED CONFLICT" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "FLAGGED CONFLICT" .planning/REQUIREMENTS.md` reports 8 — the three that existed plus five new.
    - Each of the five new entries names its spec line or step, states that the binding document has not been amended, and states what was knowingly given up.
    - The five entries have five distinct causes; no two are folded into one paragraph. In particular the D-10 entry and the A-09 entry are separate.
    - The D-10 entry records that the Envoy mitigating factor is unverified.
    - The D-11 entry records that the clear-on-consume half survives.
    - The D-03 entry records that a double consumption still matches no row.
    - The D-06 entry records both accepted consequences.
    - The standing "Flagged conflicts" table lists eight, with the five new ones attributed to Phase 37.4 and dated.
    - The footer's last-updated line names Phase 37.4 and the date.
    - `git diff .planning/REQUIREMENTS.md` adds no requirement ID and removes none; the coverage counts at the foot of the file are unchanged.
  </acceptance_criteria>
  <done>Five divergences from the binding specification are written down where a later reader will find them, each with its own cause, its own reasoning and its own record of what survived — and none of them resolved.</done>
  <reversibility rating="reversible">Documentation edit.</reversibility>
</task>

<task type="auto">
  <name>Task 3 (07-T3): Re-record the package shape, queue the documentation work, and surface every D-15 question</name>
  <files>tests/unit/test_auth_package_shape.py, .planning/STATE.md, .planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-VALIDATION.md</files>
  <read_first>
    - `tests/unit/test_auth_package_shape.py` — its `_measure()` function and the recorded triple, which plan `37.4-01` already moved once.
    - `37.4-VALIDATION.md` — the "Re-record ×2 note", the Wave 0 requirements checklist, and the validation sign-off checklist.
    - Every `37.4-0N-SUMMARY.md` from plans 01 through 06 — specifically each one's D-15 question section and its recorded `ty` count.
    - `37.4-CONTEXT.md` A-10 — the README's staleness and the instruction to queue rather than fix.
    - `37.4-CONTEXT.md` `<deferred>` — the items that stay deferred, so nothing is re-opened by accident.
  </read_first>
  <action>
Re-record `tests/unit/test_auth_package_shape.py`'s triple against the final `auth/` package, by
executing the file's own `_measure()` and writing the returned tuple in verbatim. This is the second
of the two deliberate re-recordings the validation strategy names; both are re-recordings against a
measurement, never a widening to make a test pass. Paste the command and its output into the SUMMARY.
The package should now hold fewer modules than it did at the phase's start, because two were folded
into one and two were deleted — if the measurement disagrees with that expectation, stop and report
it rather than writing the number in.

Queue the documentation work A-10 defers: add a pending todo to `.planning/STATE.md` naming
`/gsd:docs-update` and the README's specific staleness — endpoints that no longer exist, a package
layout that no longer exists, and a Python version that no longer applies. **Do not edit `README.md`.**
Also record there that `.planning/codebase/*.md` remains stale and is best refreshed after this phase,
which is what `37.4-CONTEXT.md`'s deferred list already says.

Collect every D-15 question from the six plan SUMMARYs into one list in this plan's SUMMARY, each with
the plan that found it and what was found. These are things that looked stranded and were deliberately
**not** deleted, because this phase deletes exactly the named instances. Present them to the developer
as questions. Add to that list the two items plan `37.4-06` carried forward: the reading applied to
`tests/unit/test_jwt_security.py` (mechanical import edit permitted, no assertion weakened), and that
37.3-REVIEW's WR-09, WR-10 and WR-11 stay open because no line this phase rewrote contained them.

Complete `37.4-VALIDATION.md`: tick the Wave 0 requirements now satisfied, tick the sign-off items this
phase can prove, set `wave_0_complete: true`, and record the final `ty` count against the plan
`37.4-01` baseline in the sampling section. Leave `nyquist_compliant` and `status` for
`/gsd:validate-phase` to set; do not self-certify them.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_auth_package_shape.py && .venv/bin/python -m pytest -q -m "" && .venv/bin/ruff check . && .venv/bin/ty check</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/unit/test_auth_package_shape.py` exits 0.
    - The recorded triple equals the tuple returned by that file's own `_measure()` at this commit, and the SUMMARY pastes the command and its output.
    - The module count in the recorded triple is lower than the count plan `37.4-01` recorded, and the SUMMARY names which modules were folded and which were deleted to account for the difference.
    - `git diff --stat README.md` shows zero changed lines across the whole phase branch.
    - `.planning/STATE.md`'s pending-todos section names `/gsd:docs-update` and the README's three specific staleness items.
    - This plan's SUMMARY contains a numbered list of every D-15 question from all six plan SUMMARYs, each attributed to the plan that found it.
    - `37.4-VALIDATION.md` has `wave_0_complete: true`, every Wave 0 requirement ticked, and the final `ty` count recorded; `nyquist_compliant` is left as it was.
    - `.venv/bin/python -m pytest -q -m ""` exits 0, or the `e2e` skip and its cause are recorded.
    - `.venv/bin/ruff check .` exits 0 and `.venv/bin/ty check` is at or below the plan `37.4-01` baseline.
  </acceptance_criteria>
  <done>The package-shape ratchet holds against a measurement of the final package, the README's staleness is queued rather than quietly fixed or quietly ignored, and every stranded item the phase declined to touch is in front of the developer as a question.</done>
  <reversibility rating="reversible">A recorded measurement and documentation edits.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

**Created by this plan:** the Phase 37.4 ROADMAP goal and requirements line; five dated flagged-conflict
entries in `REQUIREMENTS.md`; a queued documentation todo in `STATE.md`; the final recorded package-shape
triple.

**Deleted by this plan:** nothing. D-15 is a collection obligation here, not a deletion licence.

**Created across the phase:** `Identity`, `get_identity`, `get_linked_identity`, `charge_quota`,
`get_session_factory`, `AppError`, `assert_tree_total`, `AccountUnavailable`, `HistoricalIdentity`,
`BlockedUser`, `app_error_handler`, `answers_framework_status`, the `preauth_subject` column,
`HTTPBearer(auto_error=False)`, `BoundedReason` in its new home; files
`src/nativespeaker/api/auth/identity.py` and `tests/unit/test_quota_seam.py`.

**Deleted across the phase:** `LinkedIdentity`, `PreAuthIdentity`, `IdentityKind`, `RequestContext`,
`get_request_context`, `QuotaGate`, `get_quota_gate`, `_AdmissionRejected`, the `on_admitted` chain,
`ErrorClass`, `REGISTRY`, `register_class`, the 16 module-level error-class constants,
`STATUS_TO_CLASS`, `assert_registry_total`, `AuthRejected`, `ServiceError`,
`ProviderAccountAlreadyLinked`, `HmacKeyring`, `HmacConfig`, `extract_bearer`, three `BoundedReason`
members, the `claim_attempt_id` column and its CHECK conjuncts, the `preauth_subject_hash` column, the
savepoint and the second insert function, the cause-chain walk and the three-way constraint map; files
`auth/context.py`, `auth/resolve_identity.py`, `auth/exceptions.py`, `auth/extract_bearer.py`,
`auth/hmac_keyring.py`, `tests/unit/test_hmac_keys.py`, `tests/unit/test_wire_contract.py`,
`tests/e2e/test_wire_contract.py`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase → a later reader | The only boundary this plan governs. A divergence that is not written down is discovered by being hit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.4-29 | Repudiation | an unrecorded divergence from the binding specification | high | mitigate | Five flagged entries in the established style, each attached to the requirement whose subject it is, each stating what was given up and why, plus the standing table at the foot of the file growing from three to eight. Verified by count and by content |
| T-37.4-30 | Repudiation | a swept stranded item | medium | mitigate | D-15 forbids a standing deletion rule; every stranded item is collected from the six SUMMARYs and presented as a question rather than acted on |
| T-37.4-31 | Tampering | widening the phase through documentation | low | mitigate | A-10 queues the README rather than fixing it; a prohibition and a `git diff --stat` check enforce it |
| T-37.4-32 | Tampering | a whole-file write destroying unrelated ROADMAP or REQUIREMENTS content | medium | mitigate | Scoped replacement only, with a diff-locality acceptance criterion on both files |
| T-37.4-01 … T-37.4-07 | — | the phase's seven named threats | — | inherited | All are owned and dispositioned by plans `37.4-02` through `37.4-06`. This plan changes no code and adds no control; it records the three that were **accepted** (T-37.4-06 the duplicate-header loss, T-37.4-07 the plaintext subject, and the timing and tripwire losses beside them) into `REQUIREMENTS.md` so the acceptances are traceable to a decision rather than to a diff |

No package-manager install occurs in this plan, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `rg -c "Urgent work - to be planned" .planning/ROADMAP.md` reports no matches.
- `rg -c "FLAGGED CONFLICT" .planning/REQUIREMENTS.md` reports 8.
- `.venv/bin/python -m pytest -q -m ""` exits 0 (record any `e2e` skip and its cause).
- `.venv/bin/ruff check .` exits 0; `.venv/bin/ty check` at or below the plan `37.4-01` baseline.
- `git diff --stat README.md` shows zero changed lines across the phase.
- `.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-VALIDATION.md` has
  `wave_0_complete: true` and every Wave 0 requirement ticked.
</verification>

<success_criteria>
The roadmap says what Phase 37.4 did; the requirements file records five divergences from the binding
specification with five distinct causes and none of them resolved; the package-shape ratchet holds
against a measurement of the final package; the stale README is queued rather than quietly fixed; and
every stranded item the phase declined to touch is a written question in front of the developer.
</success_criteria>

<output>
Create `.planning/phases/37.4-machine-generated-code-refactoring-part-3/37.4-07-SUMMARY.md` when done.
It MUST record: the final measured package-shape triple with the command that produced it and the
account of which modules were folded and which deleted; the numbered list of every D-15 question from
all six plan SUMMARYs, attributed; the reading applied to `tests/unit/test_jwt_security.py`; the note
that 37.3-REVIEW WR-09, WR-10 and WR-11 stay open; and the final `ty` count against the plan `37.4-01`
baseline.
</output>
