# Phase 37.4: Machine-generated code refactoring, part 3 - Context

**Gathered:** 2026-08-29
**Status:** Ready for planning

<domain>
## Phase Boundary

Continue 37.1/37.2/37.3's de-complication, on four fronts the developer named directly:

1. **The identity types collapse.** `LinkedIdentity`/`PreAuthIdentity`/`IdentityKind` and the
   `RequestContext` carrier all die, replaced by one `Identity` class and one accessor pair.
2. **The error architecture unifies.** The two parallel exception trees, the `ErrorClass`
   indirection and the module-level registry between exceptions and responses collapse into one
   tree whose classes carry their own status and code, served by one handler.
3. **Code shape rules land.** Docstrings capped at three lines, comments cut to the necessary
   ones and collapsed to a line each, one-function modules folded, the quota gate demoted to a
   plain function.
4. **Named disproportionate machinery is deleted** — the HMAC keyring, the hand-rolled bearer
   extractor, `claim_attempt_id` — each against an explicit spec requirement, each flagged.

**Withdrawn during discussion.** An earlier directive in this conversation asked to remove all
code whose purpose is distinguishing between kinds of errors. The developer reversed it:
`_reject_existing_identity` and its like stay, and distinguishing between errors is correct **so
long as the distinction is carried by an exception class**. D-04 records this; nothing in the
phase sweeps a distinction merely for being one.

**Out of scope:** which routes are authenticated, phases 38-46's own work, the schema tests
(`tests/schema/`), and `secret-manager-integration` (reviewed, not folded - fourth time).

</domain>

<decisions>
## Implementation Decisions

### The identity types

- **D-01: One `Identity` class replaces the union.**
  `Identity(user: User | None, identity: ExternalIdentity | None, issuer: str, subject: str)`,
  both object fields nullable together - unlinked means both `None`. It carries the
  `ExternalIdentity` row and not just the `User`, because the challenge binding keys on
  `bound_external_identity_id UUID REFERENCES core.external_identities (id)`
  (`migrations/20260818_01_initial-release.sql:379`) with a two-arm CHECK at `:407-411`. No
  schema change to that column. `IdentityKind` dies with the union it discriminated.
  `ChallengeStore.issue`/`verify_binding` stop branching on `isinstance` and read the nullable
  fields instead.

- **D-02: `get_identity` admits, `get_linked_identity` narrows.** `get_identity` returns the
  class and admits unlinked callers; `get_linked_identity` depends on it and raises when
  `user is None`. `POST /auth/challenge` and `POST /auth/create-user` declare `get_identity`;
  `chats`, `root` and `examples` declare `get_linked_identity` at router level. The narrowing
  survives the type split's removal - **reversibility:** a handler that reads `identity.user`
  without the narrowing dependency reads unlinked as linked, which is the hazard the two
  dataclasses existed to make unrepresentable. The router-level declaration is the replacement
  control.

- **D-03: `RequestContext` dies, field by field.**
  - `identity` -> `get_identity()`.
  - `evaluated_at` -> generated at the start of each endpoint that needs it.
  - `attempt_id` -> **leaves the code path entirely, and the `claim_attempt_id` column goes with
    it.** Dropped from `tables/auth.py:76` and from the initial migration, edited in place with
    the dev/test database re-applied (the established v2.0 pattern; never an incremental
    migration). `consume`'s WHERE keys on `challenge_id` + `claimed_at IS NOT NULL` +
    `consumed_at IS NULL`. It reads like a compare-and-swap guard whose condition can never
    fail, because nothing anywhere un-claims a challenge. **Flagged conflict 1** against
    `02-create-user.md:65`, which mandates consumption "conditioned on still-claimed under this
    attempt's `claim_attempt_id`".
  - `route` -> **gone, and not re-derived.** Neither the rejection handler nor the quota logs
    carry a route field; `RequestLoggingMiddleware` already binds `request_id`, `method` and
    `path` onto every line of a request via contextvars. **Accepted:** the middleware's `path`
    is the concrete URL, not the matched template, so telemetry label cardinality becomes
    unbounded (one series per `chat_id`). Correlate on `request_id`.

### Error distinctions - the withdrawn sweep

- **D-04: Distinguishing between errors is kept.** `_reject_existing_identity` stays, and so
  does every similar function whose job is to decide which rejection was earned. The developer's
  rule: distinctions are fine as long as exceptions are what carry them. No plan removes a
  distinction on the grounds that it exists only to tell clients apart - except where D-06 and
  D-10 name a specific collapse.

- **D-05: `AccountUnavailable`'s `cause` string becomes two classes.** `HistoricalIdentity` and
  `BlockedUser`, both under one base that declares the single 403 **once**. The `cause` field
  dies - the class name carries it. Same shape as the five challenge rejections. Clients still
  cannot tell the two apart, which `SHARED-INVARIANTS.md:14` requires ("mutually
  indistinguishable").

### The create-user conflict path

- **D-06: The `IntegrityError` arm collapses.** `_insert_account` and `_flush_account` merge
  into one function; the `begin_nested()` savepoint goes, since `create_user`'s own consume
  moves entirely to the route's `except` arm. The cause-chain walk for `constraint_name` and its
  three-way mapping collapse: **any `IntegrityError` from the insert becomes one already-linked
  exception.** Two consequences the developer accepted when choosing this:
  - `ProviderAccountAlreadyLinked` stops answering its own 403 `operation_not_allowed` and
    answers the already-linked 409 instead. **Flagged conflict 4.**
  - The unmapped-constraint 500 tripwire dies. A constraint rename or a new constraint reads as
    a benign 409 rather than surfacing. This was 37.3 D-03's explicitly preserved
    fail-loudly property and 37.3-REVIEW WR-12's subject; it is knowingly given up.
  The pre-check via `resolve_existing_identity` + `_reject_existing_identity` is untouched (D-04),
  so the common already-linked case still earns its distinct answer before the insert. Only the
  **race** path loses the distinction.

### The quota gate

- **D-07: `QuotaGate` becomes a plain function the endpoint calls.** The class, the
  `on_admitted` callback wiring in `ChatService.ask_llm` (`services/chats.py:52-61`) and the
  resilience layer's `_AdmissionRejected` re-raise plumbing all go.
  - **Invariant to preserve:** the service's own validation rejections still precede the charge -
    unsupported language, chats limit, chat-not-found, messages limit. Nobody is billed for a
    request the service refuses on its own terms.
  - **Accepted regression:** queue-full (429) and circuit-open (503) become billable, because the
    charge no longer waits on admission.
  - **Left to planning:** the exact seam. An endpoint cannot literally interleave with `ask_llm`
    without splitting the service method or moving its validation outward; the planner picks the
    shape that satisfies the invariant above.

### One exception tree

- **D-08: Collapse `ServiceError`, `AuthRejected`, `ErrorClass`, `REGISTRY` and `STATUS_TO_CLASS`
  into one tree, one handler.** Classes carry their own status and code. The registry module, the
  `ErrorClass` indirection and the two-tree split all go. Requirements that must survive the
  merge, drawn from both trees:
  - A class declaring no status still answers **500**. The default is a fail-closed control, not
    a convenience.
  - Where several classes deliberately share one client-visible answer - the five challenge
    rejections' one 409, and D-05's two account-unavailable classes' one 403 - that collapse is
    **declared once on a base and inherited, never restated per class.** It denies clients an
    enumeration oracle, and duplicated declarations drift apart.
  - The response body stays **exactly one field**, and an exception's message never reaches it.
    Several classes carry diagnostic detail (grant ids, tier ids, counts) that is server-side only.
  - **Per-class logging policy survives on both sides:** a service-layer failure decides its own
    level and whether it logs at all, with tracebacks on the 500-class failures; a rejection
    contributes structured per-exception fields and must never log anything secret it holds (the
    challenge handle above all).
  - **Response headers survive:** `Retry-After`, `WWW-Authenticate`, and the `Allow` header on a 405.
  - **Framework-raised errors still need mapped answers** - 404, 405, 422 - and an unmapped status
    must fail loudly rather than defaulting silently.
  - **The startup check survives with a new mechanism:** no two classes claim the same code, and
    the declared code vocabulary is exactly total. `assert_registry_total` walked the registry;
    without one it walks the tree.

- **D-09: The merged handler logs class-name-as-event.** `logger.warning(camel_to_snake(
  type(exc).__name__), **fields)` - `camel_to_snake` survives. The service-layer format loses:
  its message becomes a field rather than the event name, and `error_type` is redundant once the
  event *is* the class. One greppable name per outcome. **Risk the developer named:** external
  log queries or alerts referencing the losing format break. 37.3 recorded that no deployed
  dashboard keys on these event names, which bounds the exposure. Note the handler's `route`
  field does **not** survive - see D-03.

### Deleted machinery, each against a spec requirement

- **D-10: `extract_bearer` is replaced by `fastapi.security.HTTPBearer`.** `auth/extract_bearer.py`
  dies, `BoundedReason`'s eight members die, and `tests/unit/test_wire_contract.py` (110) plus
  `tests/e2e/test_wire_contract.py` (107) die with them. **Flagged conflict 2** against
  `SHARED-INVARIANTS.md:18` and `:55`. What is given up, precisely:
  - `HTTPBearer` reads `request.headers.get("Authorization")`, which returns the **first** value
    and hides the rest - the resolution `:18` forbids by name ("never resolved by taking
    first/last"). Duplicate Authorization headers stop being a distinct rejection.
  - Comma-joined and line-folded values stop being distinctly rejected; they degrade to a
    signature failure.
  - `HTTPBearer` answers **403**; the contract requires **401 `auth_required`**. The planner must
    re-map it, not inherit it.
  - `InvalidExternalJwt` loses the `bounded_reason` field it carries today, and `:55`'s named
    reason vocabulary goes with it.
  The developer's position, recorded verbatim in spirit: *"I would rather sacrifice a requirement
  than bloat my code."*

- **D-11: `auth/hmac_keyring.py` is deleted entirely.** `preauth_subject_hash BYTEA` becomes
  `preauth_subject TEXT` plaintext, edited into the initial migration alongside D-03's
  `claim_attempt_id` drop. Going with it: `hmac_keyring.py` (118), `tests/unit/test_hmac_keys.py`
  (314), `HmacConfig`, the `config/config.yaml` `hmac:` block, the lifespan wiring and
  `app.state.hmac_keyring`, `ChallengeStore`'s keyring constructor dependency, both message
  prefixes, the key-version machinery and `warn_missing_older`. **Flagged conflict 3** against
  `02-create-user.md:49` ("raw subject never stored") and `SHARED-INVARIANTS.md:56`.
  **The finding that drove it:** `core.external_identities.subject` stores the same raw subject
  **permanently, in plaintext** (`tables/identities.py:47`), with `provider_uid` - the stable
  Google/Apple account UID - in plaintext beside it. The keyring protected that value for the
  300 seconds a challenge row lives, and cleared it on consume. 432 lines for a five-minute
  window on a value the schema keeps forever one table over.
  Two pieces were already dead on their own terms and go regardless: `idp_account_hash` and
  `IDP_ACCOUNT_PREFIX` (zero production consumers; the target table
  `core.access_grants_anti_abuse` has no model at all), and the key versioning, which
  `02-create-user.md:49` itself renders inert by storing **no** key version on the challenge row.
  `verify_binding`'s pre-auth arm becomes a plaintext comparison, and its cleared-verifier rule
  keys on `preauth_subject IS NULL` instead of a cleared hash.

### Code shape

- **D-12: Docstring and comment rules, written to `ns-api-gateway/AGENTS.md`.** Docstrings three
  lines maximum, stating what the entity does; no description of what lives elsewhere or what the
  entity is not. Comments only where necessary to resolve a genuine ambiguity, one line each,
  explaining the specific lines below them and never the design or the request lifecycle.
  **This supersedes 37.1 D-16's prose register**, which the last three phases wrote to. Applying
  it is a sweep across every file the phase touches. The file nests under the parent repo's
  `/home/init/native-speaker/AGENTS.md` (which is shared with `ns-ios` and stays as it is) and
  commits with this repository.

- **D-13: Module folding, on the naming scheme already applied in `d466a4b`.** Modules are named
  after their principal function unless they mainly hold classes. Remaining work:
  `resolve_identity()` folds into `auth/context.py`, which - now holding the one `Identity` class
  - becomes `auth/identity.py` (class-mainly, so not function-named). `auth/extract_bearer.py`
  dies under D-10. `auth/adapters.py` stays separate: it is the seam that imports no provider SDK,
  and folding it into `firebase.py` destroys that property.

- **D-14: Test pruning is scoped to what this phase touches.** `tests/unit/test_challenge_ids.py`
  (490 lines for a two-line `new_challenge_id()` plus comparison rules) and
  `tests/unit/test_hmac_keys.py` (314) are pruned to the behaviour that survives;
  `test_wire_contract.py` x2 die with D-10. **`tests/schema/` is left alone** -
  `test_constraints.py` (630) and `test_inventory.py` (286) guard the migration this phase edits
  twice, so they are the check on D-03 and D-11, not a target.

- **D-15: No standing deletion rule.** Unlike 37.3's D-13, this phase deletes exactly the named
  instances. Anything else the planner finds stranded comes back to the developer as a question.

- **D-16: Amend `.planning/ROADMAP.md`'s Phase 37.4 entry** - its goal is still the placeholder
  `[Urgent work - to be planned]` at `:397` with `Requirements: TBD` - and record the four
  flagged conflicts in `.planning/REQUIREMENTS.md` in the established dated style.

### Claude's Discretion

- The `Identity` class's module-level shape and field names; whether `get_linked_identity`
  returns the same class or a narrowed view.
- The quota seam that satisfies D-07's invariant.
- The mechanism for D-08's startup totality check without a registry, and how status/code are
  declared on classes.
- Which comments in touched files clear D-12's necessity bar.
- Whether 37.3-REVIEW's remaining warnings live in lines this phase rewrites - fix in place if so
  (the standing 37.1/37.2/37.3 note). WR-01, WR-02, WR-03, WR-04, WR-06, WR-07, WR-08 and WR-13
  all sit in code this phase touches; WR-12's AST scan dies with D-06.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Binding specification - four new conflicts expected

- `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` - binds every phase
  and overrides any conflicting phase brief. Three conflicts already flagged (37.1 D-01/D-06,
  37.2 D-08). This phase adds four: `:18`/`:55` (D-10, the wire contract), `:56` (D-11, subject
  hashing), plus `02-create-user.md:65` (D-03, `claim_attempt_id`) and the provider-account
  collapse (D-06). Flag them; never resolve one silently.
- `/home/init/native-speaker/specs/auth-refactor-phases/02-create-user.md` - `:17` (the closed
  registry D-08 replaces), `:49` (the HMAC binding D-11 deletes), `:56` (the binding comparison),
  `:65` (the `claim_attempt_id` condition D-03 drops), `:70` (account-unavailable
  indistinguishability D-05 preserves).

### Project planning - amended by this phase (D-16)

- `.planning/ROADMAP.md:395-404` - the Phase 37.4 stub entry gets its real goal.
- `.planning/REQUIREMENTS.md` - the four flagged conflicts, dated.
- `ns-api-gateway/AGENTS.md` - **written during this discussion**; D-12's rules, binding on every
  file the phase touches.
- `.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-CONTEXT.md` - D-01/D-02
  (the exception family and log-event shape D-08/D-09 rebuild), D-13's sweep rule, D-16's
  precedence warning.
- `.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-REVIEW.md` - 13 warnings
  and 7 info, unaddressed. Most sit in code this phase rewrites.
- `.planning/phases/37.1-refactor-machine-generated-code/37.1-CONTEXT.md` - D-06/D-07/D-09 (the
  auth dependency mechanism), D-16 (the prose register D-12 now supersedes).

### Code this phase reshapes

- `src/nativespeaker/api/auth/context.py` (43) - D-01/D-13; becomes `auth/identity.py` holding
  `Identity` and `resolve_identity`.
- `src/nativespeaker/api/auth/resolve_identity.py` (40) - D-13; folds into the above.
- `src/nativespeaker/api/auth/extract_bearer.py` (43) - D-10; deleted.
- `src/nativespeaker/api/auth/hmac_keyring.py` (118) - D-11; deleted.
- `src/nativespeaker/api/app/dependencies.py` (112) - D-02/D-03; `get_request_context` becomes
  `get_identity`, `get_linked_identity` narrows.
- `src/nativespeaker/api/auth/create_user.py` (151) - D-06; the savepoint, the function split and
  the constraint mapping collapse. `_reject_existing_identity` stays (D-04).
- `src/nativespeaker/api/crud/challenges.py` (120) - D-01/D-03/D-11; no keyring, no
  `claim_attempt_id`, no `isinstance` branching.
- `src/nativespeaker/api/errors.py` (370) and `src/nativespeaker/api/auth/exceptions.py` (215) -
  D-08; both collapse into one tree.
- `src/nativespeaker/api/app/error_handlers.py` (84) - D-08/D-09; two handlers become one.
- `src/nativespeaker/api/quota.py` (97) - D-07; `QuotaGate` becomes a function.
- `src/nativespeaker/api/services/chats.py` (143) - D-07; the `on_admitted` charge goes.
- `src/nativespeaker/api/resilience.py` (190) - D-07; `_AdmissionRejected` and the callback
  parameter go.
- `src/nativespeaker/api/routers/auth.py` (163), `chats.py` (88), `root.py` (22),
  `examples.py` (19) - D-02/D-03/D-07.
- `src/nativespeaker/api/tables/auth.py` (78) and
  `migrations/20260818_01_initial-release.sql` - D-03/D-11; two column changes, edited in place.
- `src/nativespeaker/api/config.py` (101) and `config/config.yaml` - D-11; the `hmac:` block.
- `src/nativespeaker/api/app/lifespan.py` (68) - D-08/D-11; keyring wiring and the totality check.

### Tests that move, prune, or die

- `tests/unit/test_wire_contract.py` (110), `tests/e2e/test_wire_contract.py` (107) - die (D-10).
- `tests/unit/test_hmac_keys.py` (314) - dies (D-11).
- `tests/unit/test_challenge_ids.py` (490) - pruned (D-14).
- `tests/unit/test_create_user_precedence.py` (596) - the precedence spec. Statuses move only
  where D-06 and D-10 sanction it; every other literal holds.
- `tests/e2e/test_admission.py` (240) - the admission matrix. D-05's split and D-10's re-mapping
  are the only sanctioned movements.
- `tests/unit/test_error_registry.py` (346), `test_exception_handlers.py` (309),
  `test_rejection_vocabulary.py` (236) - rebuilt on D-08's tree.
- `tests/unit/test_identity_accessors.py` (382), `test_identity_resolution.py` (274) - D-01/D-02.
- `tests/schema/` - **untouched** (D-14), and the check on D-03's and D-11's migration edits.

### Stale - do not trust

- `.planning/codebase/*.md` - captured 2026-02-24, three milestones behind, and predates the
  `d466a4b` renames entirely. Read the source.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`d466a4b` already applied the naming scheme** (2026-08-29): `models/` split into `tables/` and
  `schemas/`, `database/` -> `crud/`, `creation.py` -> `create_user.py`, `wire.py` ->
  `extract_bearer.py`, `identity.py` -> `resolve_identity.py`, `verification.py` ->
  `jwt_verifier.py`, `keys.py` -> `hmac_keyring.py`, `challenges.py` -> `crud/challenges.py`,
  `app/errors.py` -> `app/error_handlers.py`. It is renames plus import fixups only. D-13 finishes
  the job by folding, not renaming.
- **The exception-handler MRO walk** - one `add_exception_handler` entry covers a whole tree, which
  is what makes D-08's single registration possible.
- **`get_db` already rolls back on exception** - pre-claim rejections still escape safely.
- **`camel_to_snake` already exists** in `app/error_handlers.py:27` and survives D-09 unchanged.
- **`ChallengeRejected` already demonstrates D-08's declare-once rule**: the 409 is on the base and
  no leaf re-declares it. D-05's two account classes copy that shape exactly.

### Established Patterns

- `Depends()`-only routes, DI in `app/dependencies.py`; handlers never take `Request`. The merged
  handler is the one place a `Request` is read.
- Delete machinery whose justification fails re-examination - this is the sixth consecutive phase.
- The challenge handle is a secret: never logged, never in error text. D-08's merged handler and
  D-11's plaintext `preauth_subject` both inherit that rule.
- Migration edits go into the single initial file in place; the disposable dev/test database is
  re-applied. Never an incremental migration during v2.0.

### Integration Points

- `app/lifespan.py:35` - `warn_missing_older` call site, deleted with D-11; `assert_registry_total`
  at the same layer needs D-08's new mechanism.
- `resilience.py:141-161` - the `on_admitted` parameter threaded through `run`, and the
  `_AdmissionRejected` wrapper at `:73-79` that keeps a quota 429 from reading as a provider 503.
  D-07 removes the thread; the 429-not-503 property must not regress for whatever raises in its place.
- `migrations/20260818_01_initial-release.sql:379,407-411` - the challenge binding column and its
  two-arm CHECK. D-01 leaves the column; D-11 changes `preauth_subject_hash`'s type and name, so the
  CHECK's pre-auth arm is rewritten in the same edit.

</code_context>

<specifics>
## Specific Ideas

- The developer's framing on the spec, stated when choosing D-10: **"I would rather sacrifice a
  requirement than bloat my code."** They then asked for other candidates where a pedantic
  invariant produces disproportionate code, which is how D-11 was found. Both were accepted with
  the consequences named.
- The reversal recorded in D-04 matters for tone as much as content: the phase is not hostile to
  error distinctions. It is hostile to indirection *between* an error and its answer - registries,
  mapping dicts, `ErrorClass` values, two trees - which is exactly what D-08 removes.
- On docstrings, the developer's objection was to prose that defines a subject by what it is not
  and by what lives elsewhere. D-12 is written against that failure specifically.
- Phase 37.4's roadmap goal was still `[Urgent work - to be planned]` when this discussion opened;
  the three directives that opened it are the phase's spec, and D-16 writes them back to the roadmap.

</specifics>

<deferred>
## Deferred Ideas

- **37.3-REVIEW.md's 13 warnings and 7 info** - unaddressed as a set. This phase rewrites most of
  the code they sit in; fix in place where the touched lines contain them (Claude's Discretion).
  Whatever survives the rewrite stays open.
- **`tests/schema/`'s 916 lines** - reviewed and deliberately left alone this phase (D-14), because
  they guard the two migration edits it makes. Revisit once the migration settles.
- **The route template as a bounded telemetry label** - dropped by D-03. If anything later needs
  aggregation by endpoint, it comes back as a deliberate choice rather than a leftover.
- **Provider-callback category and adapter seams for phases 43-46** - unchanged flags from
  37.1/37.2/37.3.
- **Refreshing `.planning/codebase/*.md`** - now three milestones stale and behind the `d466a4b`
  renames. Best done after this phase lands.
- **Envoy gateway contract (section 9 / FOUND-09)** - still v2.1.

### Reviewed Todos (not folded)

- **`secret-manager-integration.md`** - matched at 0.2 on the word "phase" alone; config/secrets
  work unrelated to this refactor. Fourth consecutive review-without-fold.

</deferred>

<addenda>
## Post-Research Addenda (2026-08-29)

`37.4-RESEARCH.md` contradicted three of this file's premises with the installed code, and surfaced
nine stranded items under D-15. The developer answered the four that change the plan's shape; the
remaining five take the researcher's recommendation and are recorded here rather than left to the
planner. **These addenda bind exactly as the D-NN decisions above do.** Where an addendum contradicts
a decision above, the addendum wins and says so.

### Blocking precondition

- **A-00: `d466a4b` left the tree unimportable; Wave 0 repairs it before any 37.4 work.**
  That commit moved `api/database/` -> `api/crud/` and renamed `ChallengeStore` -> `ChallengesDB`
  without updating call sites: **13 stale `nativespeaker.api.database` imports** and **9 sites still
  naming `ChallengeStore`**. `pytest -q` collects 4 errors and runs 0 tests; `import
  nativespeaker.api.app.main` raises `ModuleNotFoundError`; `ruff` reports 22 errors and `ty` 151;
  `tests/unit/test_auth_package_shape.py:23`'s recorded triple `(11,32,49)` no longer matches the
  measured `(10,31,41)`. This is pre-existing breakage, not 37.4 scope, but nothing in the phase is
  verifiable until it lands. Repair is mechanical: fix the imports, fix the name, re-record the
  triple. **Every line number in RESEARCH.md is against HEAD (`1c63214`) and shifts once this
  lands — re-verify line numbers, not facts, after the repair commit.**

### Answered by the developer

- **A-01: One module. `auth/exceptions.py` is deleted; the whole tree lives in `errors.py`.**
  Answers OQ-2. ~37 classes, ~250 lines once D-12 trims the prose.
  `tests/unit/test_rejection_vocabulary.py:106`'s `FAMILY_MODULE ==
  "nativespeaker.api.auth.exceptions"` assertion flips to `nativespeaker.api.errors` — it is kept,
  not dropped, because its job is to stop the family acquiring a second home and that job survives
  the move. Confirms the maximal reading of D-08: nothing is left to drift.

- **A-02: The four stranded test files are pruned to surviving behaviour, not rewritten and not
  deleted.** Answers OQ-3, and extends D-14's named prune list — which did not reach them — with the
  same treatment D-14 gives `test_challenge_ids.py`. Keep the cases that still describe real
  behaviour; delete the cases whose subject the phase removes.
  - `tests/e2e/test_challenge_store.py` (446) — 30 `claim_attempt_id` sites, 8
    `preauth_subject_hash` assertions, the `keyring` fixture at `:30-36`, 8 identity-type
    references. Prune all four groups; the store's real behaviour stays covered.
  - `tests/unit/test_conflict_classification.py` (464) — this is *specifically* the test of the
    classification D-06 deletes, so most of it goes. What survives is whatever still describes the
    pre-check path, which D-04 keeps.
  - `tests/unit/test_create_user_rollback.py` (226) — prune to what holds once the `begin_nested()`
    savepoint goes.
  - `tests/unit/test_config.py::TestHmacConfigSurface` (`:134-176`) — dies with the `hmac:` block
    (D-11).

- **A-03: D-07's quota seam is Option A2 — `charge_quota()` is called from inside `create_chat`
  and `send_message`, immediately before `ask_llm`.** Answers OQ-6 and settles D-07's
  "Left to planning" clause. "The endpoint calls" in D-07 is **not** load-bearing; the requirement is
  that quota is a plain function called at a visible point with the callback chain gone. A2 adds
  **zero** methods where A1 needs two (splitting `ask_llm` or hoisting its validation outward), and
  satisfies every invariant D-07 states: the service's own validation rejections — unsupported
  language, chats limit, chat-not-found, messages limit — still precede the charge, because they run
  earlier in the same method. D-07's deletions are unchanged: the `QuotaGate` class, the
  `on_admitted` wiring at `services/chats.py:52-61`, and `resilience.py`'s `_AdmissionRejected`
  re-raise plumbing all go. The 429-not-503 property at `resilience.py:73-79` must not regress in
  whatever raises in its place.

- **A-04: `BoundedReason` moves to `jwt_verifier.py`; only three of its eight members die.**
  Answers OQ-7 and **corrects D-10**, which asserted all eight die. Five members belong to
  `jwt_verifier.py`, which this phase does not touch, and `dependencies.py:50`'s verification arm
  still produces one. Delete exactly `missing_token`, `malformed` and `duplicate_authorization` —
  the three `extract_bearer` owned. **`InvalidExternalJwt` keeps its `bounded_reason` field** for
  the verification arm, contra D-10's "loses the `bounded_reason` field it carries today", which
  applied only to the extraction arm. This keeps `tests/unit/test_jwt_security.py` (588 lines, on no
  prune list, 30+ assertion sites) and `test_identity_accessors.py:208-224` meaningful, and keeps
  `SHARED-INVARIANTS.md:55`'s closed vocabulary alive for the reasons that still exist.

### Taken on the researcher's recommendation

- **A-05: D-10 needs no 403->401 re-map; the premise was wrong.** Answers RESEARCH.md's first
  premise correction. FastAPI 0.135.1's `HTTPBase.make_not_authenticated_error()` returns **401**
  with `WWW-Authenticate: Bearer`, not the 403 D-10 anticipated — verified by reading the installed
  source and by executing six header shapes against a live `TestClient`. Use `auto_error=False`,
  which keeps the `invalid_external_jwt` log event. D-10's bullet "**`HTTPBearer` answers 403**; the
  contract requires **401 `auth_required`**. The planner must re-map it" is **withdrawn as
  factually wrong**; the rest of D-10 stands.

- **A-06: `tests/schema/` reading (a) — mechanical column edits allowed, no assertion weakens.**
  Answers OQ-1 and narrows D-14's "`tests/schema/` is left alone", which cannot hold literally.
  `test_inventory.py` is genuinely untouched (verified). `test_constraints.py` breaks at `:27`,
  `:66-71`, `:159-185`, `:599-603` and `:618-627` — edit **only** the column list, the two default
  parameters, and the two cases naming `claim_attempt_id`. **Hard rule: no assertion in
  `test_constraints.py` weakens.** It remains the check on D-03's and D-11's migration edits, which
  is D-14's actual intent. `tests/schema/test_create_atomicity.py` and `test_create_race.py`
  construct `RequestContext`, `PreAuthIdentity`, `ChallengeStore`, `HmacKeyring` and `HmacConfig`
  from application code; they are ordinary collateral and get A-02's prune treatment.

- **A-07: `ProviderAccountAlreadyLinked` is deleted.** Answers OQ-4. D-06 removes its only raise
  site (`create_user.py:112`), so under D-04's rule — a distinction is fine as long as an exception
  carries it — this class now carries nothing. The `operation_not_allowed` code survives on
  `NotLinked` (`exceptions.py:166-173`), so no code drops out of the vocabulary.

- **A-08: D-11's loss of constant-time comparison is accepted.** Answers OQ-5. `hmac_keyring.py:118`
  uses `hmac.compare_digest`; the plaintext replacement is a plain `!=`. D-11 named the
  plaintext-at-rest tradeoff but not the timing one. Accepted on the same grounds: the compared
  value is a Firebase uid that an attacker holding the token already knows, and
  `core.external_identities.subject` stores it permanently in plaintext one table over.

- **A-09: the `param.strip()` loosening is recorded as a fifth flagged conflict under D-16.**
  Answers OQ-8. `get_authorization_scheme_param` calls `param.strip()`, so `"Bearer  tok "` is
  accepted as `tok` where `extract_bearer` rejected it as `malformed` — verified live (200 OK).
  It is a *loosening* of `SHARED-INVARIANTS.md:18` that D-10 does not name, and the two tests
  pinning the old behaviour (`test_wire_contract.py:72,95`) die under D-10, so nothing will catch
  it. Record it as its own entry rather than folding it into the existing `:18` text, because it has
  a different cause from the first/last-resolution divergence D-10 does name. **D-16 therefore
  records five flagged conflicts, not four.**

- **A-10: `README.md`'s staleness is out of scope and queued, not fixed.** Answers OQ-9. It
  documents endpoints (`POST /prompts/analyze`), a package layout (`app/models.py`) and a Python
  version that have not existed for several milestones. Queue a `/gsd:docs-update`; do not widen
  this phase.

</addenda>

---

*Phase: 37.4-machine-generated-code-refactoring-part-3*
*Context gathered: 2026-08-29*
