# 37.4-06 checkpoint 06-C1 — reconnaissance

Task 06-T1 is committed (`8857cdb`). Execution paused at the blocking decision checkpoint
06-C1. This file records the read-only reconnaissance run before the checkpoint was raised,
so a continuation agent in a fresh worktree does not have to repeat it.

## Baselines

| Gate | Before plan 06 | After 06-T1 | Ceiling |
|------|----------------|-------------|---------|
| `pytest -q` | 817 | 805 | — |
| `pytest -q -m ""` | 1129 | 1109 | — |
| `pytest -q -m schema` | 117 | 114 | — |
| `ruff check .` | clean | clean | clean |
| `ty check` | 108 | **100** | 145 |

Test count fell because 06-T1 deleted the classification suite whose subject was deleted.

## Envoy: RESEARCH assumption A5 is false

A5 records that "no Envoy configuration exists in this repository". It does exist:

- `k8s/templates/security-policy.yaml` — a `gateway.envoyproxy.io/v1alpha1 SecurityPolicy`
  declaring a Firebase JWT provider (`issuer`, `remoteJWKS`, `claimToHeaders: plan -> x-user-plan`)
  bound to the app-routes and llm-routes `HTTPRoute`s.
- No `RequestHeaderModifier` or any header-normalisation filter anywhere in the chart.
- `k8s/values.yaml` — `service.type: ClusterIP`, so the pod is not externally addressable.
- No `NetworkPolicy` ships in the chart, so any in-cluster pod can reach the API pod directly.

The config's existence does **not** confirm the mitigation. `SHARED-INVARIANTS.md:20` says
verbatim: *"Envoy's JWT filter is defense-in-depth only; no backend correctness depends on it."*
Relying on Envoy to normalise duplicate `Authorization` fields is the exact reliance that line
forbids. What Envoy does with a duplicate field is still unspecified anywhere in this repo.

## Measured `HTTPBearer(auto_error=False)` behaviour

fastapi 0.135.1, starlette 0.52.1. Probe run against the installed packages.

| Header shape | Extractor today | `HTTPBearer(auto_error=False)` |
|---|---|---|
| `Bearer TOKEN` | accept `TOKEN` | accept `TOKEN` — unchanged |
| absent | `missing_token` | `None` -> our `InvalidExternalJwt` -> 401 `auth_required` |
| two `Authorization` fields | `duplicate_authorization` | **first value silently used** |
| `Bearer FIRST, Bearer SECOND` | `duplicate_authorization` | accepted as `"FIRST, Bearer SECOND"` -> signature failure |
| `Bearer␣␣␣TOKEN␣␣` | `malformed` | **accepted as `TOKEN`** — A-09's loosening, confirmed |
| `Bearer TOKEN extra` | `malformed` | accepted as `"TOKEN extra"` -> signature failure |
| `bearer TOKEN` | accept | accept — unchanged |
| `Bearer ` | `malformed` | `None` -> 401 `auth_required` |

`auto_error=True` answers **401** with `WWW-Authenticate: Bearer`. **A-05's premise correction
is confirmed**; D-10's anticipated 403 does not occur. No re-map is needed either way.

## Blast radius is wider than the plan budgets for

The plan names `tests/unit/test_wire_contract.py` and `tests/e2e/test_wire_contract.py`.
Two further sites pin the dying behaviour and are not in `files_modified`:

1. `tests/unit/test_jwks_offload.py::test_a_duplicate_authorization_never_reaches_the_jwks_transport`
   — asserts a duplicate rejects before any JWKS fetch. After the swap the first value is used
   and verification proceeds, so this fails. The underlying protection is not materially
   load-bearing: a single well-formed header with an unknown `kid` already reaches the transport,
   and the verifier's negative `kid` cache bounds repeats. It needs deleting or repointing.
2. `tests/unit/test_identity_accessors.py:193` — the parametrized `missing_token` case
   (the plan anticipates this one).

Also: `SHARED-INVARIANTS.md`'s wire-contract bullet names **"trailing content"** explicitly, and
the probe shows it becomes `"TOKEN extra"`. That is a fifth loss neither D-10 nor A-09 enumerated,
and it is "resolved by concatenation" — the resolution that bullet forbids by name.

## Deletion size

`extract_bearer.py` 43 + `tests/unit/test_wire_contract.py` 110 +
`tests/e2e/test_wire_contract.py` 107 = **260 lines**.
