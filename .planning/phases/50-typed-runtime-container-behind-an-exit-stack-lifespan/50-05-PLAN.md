---
phase: 50-typed-runtime-container-behind-an-exit-stack-lifespan
plan: 05
type: execute
wave: 5
depends_on: ["50-04"]
files_modified:
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/app/lifespan.py
  - src/nativespeaker/api/routers/auth.py
  - tests/unit/test_create_user_body.py
  - tests/unit/test_create_user_precedence.py
  - tests/unit/test_claim_precedence.py
  - tests/unit/test_claim_precedence_registered.py
  - tests/unit/test_upgrade_precedence.py
  - tests/unit/test_challenge_endpoint.py
  - tests/unit/test_app_wiring.py
  - tests/e2e/conftest.py
autonomous: true
requirements: []

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-05: `sign_out_all` declares `runtime: Runtime = Depends(get_runtime)` and passes `runtime.firebase_adapter` to `revoke_with_retry`; it still declares no database session."
    - "D-05: no route other than `sign_out_all` declares `get_runtime` directly."
    - "`get_firebase_adapter` and `get_devicecheck_adapter` are deleted, and `get_auth_service` reads both adapters off the container."
    - "49 D-06 stands: `AuthService.__init__` and `get_auth_service` still annotate the two adapters `FirebaseAdminLookup` and `AppleDeviceCheck`."
  artifacts:
    - src/nativespeaker/api/routers/auth.py
    - src/nativespeaker/api/app/dependencies.py
    - tests/unit/test_app_wiring.py
  key_links:
    - "`sign_out_all` → `runtime.firebase_adapter` → `revoke_with_retry`"
    - "`get_auth_service` → `runtime.firebase_adapter`, `runtime.devicecheck_adapter`"
    - "six unit files → one `app.dependency_overrides[get_runtime]` each"
---

<objective>
Move the Firebase and DeviceCheck adapters onto the container, delete their two getters, and point
the one route that reads a lifespan object at `get_runtime`.

Purpose: this is the only clause of criterion 5 that reaches outside `dependencies.py`, and the six
unit files that override a deleted getter all change with it.
Output: two more `app.state` attributes gone, and criterion 5's route clause measured.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

This directory is a git submodule: its `.git` is a file. It is NOT a worktree — do not treat it as
one. Do not create, rename or switch git branches.

`.venv/bin/pytest -q` is unit-only. The schema and e2e suites need `-m e2e`, `-m schema` or `-m ''`.

Task 1 is one commit across ten files, for the same reason plan 50-04's task 1 was: deleting a
getter breaks every override of it at once.
</execution_context>

<context>
@.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-CONTEXT.md
@.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-RESEARCH.md
@.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-PATTERNS.md
@.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-04-SUMMARY.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-CONTEXT.md
@AGENTS.md
</context>

<!-- planner-discipline-allow: get_firebase_adapter -->
<!-- planner-discipline-allow: get_devicecheck_adapter -->
<!-- planner-discipline-allow: state.firebase_adapter -->
<!-- planner-discipline-allow: state.devicecheck_adapter -->

<tasks>

<task type="auto">
  <name>Task 1: the two adapters move into the container, and `sign_out_all` with them</name>
  <files>src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/routers/auth.py, tests/e2e/conftest.py, tests/unit/test_create_user_body.py, tests/unit/test_create_user_precedence.py, tests/unit/test_claim_precedence.py, tests/unit/test_claim_precedence_registered.py, tests/unit/test_upgrade_precedence.py, tests/unit/test_challenge_endpoint.py</files>
  <read_first>
    - src/nativespeaker/api/app/dependencies.py lines 100-135 (as plan 50-04 left it)
    - src/nativespeaker/api/routers/auth.py lines 1-30 and 200-215
    - src/nativespeaker/api/app/lifespan.py (the two assignments and the `Runtime(...)` call)
    - tests/e2e/conftest.py lines 285-350 (`scripted_firebase_adapter` and the DeviceCheck fixture)
    - tests/unit/test_challenge_endpoint.py lines 120-145 (the override shape to follow)
    - tests/unit/test_create_user_body.py lines 95-115
    - tests/unit/test_create_user_precedence.py lines 165-185
    - tests/unit/test_claim_precedence.py lines 258-275
    - tests/unit/test_claim_precedence_registered.py lines 140-155
    - tests/unit/test_upgrade_precedence.py lines 165-180
    - tests/unit/conftest.py (`make_runtime`)
  </read_first>
  <action>
    Per D-05 and D-01.

    `app/dependencies.py`: delete `get_firebase_adapter` and `get_devicecheck_adapter`.
    `get_auth_service` takes `db: AsyncSession = Depends(get_db)` and
    `runtime: Runtime = Depends(get_runtime)`, passing `adapter=runtime.firebase_adapter` and
    `devicecheck=runtime.devicecheck_adapter`. 49 D-06 stands: `AuthService.__init__` keeps its
    `FirebaseAdminLookup` and `AppleDeviceCheck` annotations, so do not change `services/auth.py`.
    Drop the `FirebaseAdminLookup` and `AppleDeviceCheck` imports from `dependencies.py` if nothing
    else in the file names them. Delete the two docstrings that described the deleted getters.

    `routers/auth.py`: in `sign_out_all`, replace
    `adapter: FirebaseAdminLookup = Depends(get_firebase_adapter)` with
    `runtime: Runtime = Depends(get_runtime)` and call
    `revoke_with_retry(runtime.firebase_adapter, claims.issuer, claims.subject)`. The
    `Depends(get_claims)` and `Depends(get_identity)` parameters do not move, and the route still
    declares no session. Fix the imports: `get_runtime` and `Runtime` in, `get_firebase_adapter` and
    `FirebaseAdminLookup` out if unused.

    `app/lifespan.py`: delete the two `app.state` assignments for these adapters; the locals feeding
    `Runtime(...)` stay.

    `tests/e2e/conftest.py`: `scripted_firebase_adapter` and the DeviceCheck fixture save the field
    value and swap with `replace(_app_lifespan.state.runtime, firebase_adapter=...)` /
    `devicecheck_adapter=...`, both on the way in and on the way out — never the whole container.

    The six unit files: each replaces its `app.dependency_overrides` entries for the two deleted
    getters with ONE `app.dependency_overrides[get_runtime] = lambda: make_runtime(...)` naming every
    field that file needs. In the three precedence files this also absorbs the `app.state.runtime`
    assignment plan 50-04 left, because an override of `get_runtime` bypasses `app.state` entirely —
    the session factory and any other field those cases rely on must be named in the same
    `make_runtime(...)` call. Where a case overrode a getter with `lambda: None` to script an absent
    adapter, pass `None` as that field with an inline `# ty: ignore[invalid-argument-type]`.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q</automated>
    <fails_when>non-zero exit, or the summary reports 1 or more failed</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>non-zero exit, or the summary reports 1 or more failed</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>anything other than "All checks passed!" on exit 0</fails_when>
    <automated>test -z "$(grep -rn 'get_firebase_adapter\|get_devicecheck_adapter\|state.firebase_adapter\|state.devicecheck_adapter' src tests --include='*.py')"</automated>
    <fails_when>non-zero exit — a deleted getter or a direct attribute read is still named in a tracked Python file</fails_when>
  </verify>
  <acceptance_criteria>
    - Neither deleted getter nor either direct attribute read appears in `src` or `tests` (the `test -z` gate above).
    - `grep -c 'Depends(get_runtime)' src/nativespeaker/api/routers/auth.py` is 1.
    - `grep -c 'Depends(' src/nativespeaker/api/routers/auth.py` for the `sign_out_all` signature counts three parameters: claims, identity, runtime.
    - `services/auth.py` is byte-identical to its state before this task.
    - `.venv/bin/pytest -q` and `.venv/bin/pytest -q -m e2e` exit 0.
  </acceptance_criteria>
  <reversibility rating="reversible">One route, one parameter, two deleted getters — per CONTEXT D-05.</reversibility>
  <done>The revocation route reads `runtime.firebase_adapter`; both suites exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: the criterion 5 route clause</name>
  <files>tests/unit/test_app_wiring.py</files>
  <read_first>
    - tests/unit/test_app_wiring.py lines 25-60 (`_api_routes`, `_declared`, `_flattened`, `_route_at`)
    - tests/unit/test_app_wiring.py lines 110-125 (`TestTheSignOutRouteOpensNoSession`, the shape to follow)
    - .planning/ROADMAP.md (Phase 50 criterion 5)
    - src/nativespeaker/api/routers/auth.py lines 200-215
  </read_first>
  <action>
    Add one class to `tests/unit/test_app_wiring.py` pinning criterion 5's route clause, following
    `TestTheSignOutRouteOpensNoSession` at lines 116-121. Two cases:
    `get_runtime in _declared(_route_at("/auth/sign-out-all"))`, and — for every registered API route
    other than that one — `get_runtime not in _declared(route)`, listing the offending paths in the
    assertion message. Use `_declared` and not `_flattened`: after this phase every route reaches
    `get_runtime` through `get_db` or `get_claims`, so a flattened walk would be true of all of them
    and would measure nothing. Add a one-line docstring saying that, so a later reader does not
    "fix" the case by widening it. Import the real symbols; do not use string-based module
    references.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_app_wiring.py</automated>
    <fails_when>non-zero exit, or "no tests ran", or fewer than 2 cases collected from the new class</fails_when>
    <automated>.venv/bin/pytest -q</automated>
    <fails_when>non-zero exit, or the summary reports 1 or more failed</fails_when>
  </verify>
  <acceptance_criteria>
    - Adding `runtime: Runtime = Depends(get_runtime)` to a second route makes this file's case fail — confirm once with a throwaway edit and revert it.
    - The new class uses `_declared` and never `_flattened`.
    - `.venv/bin/pytest -q` exits 0.
  </acceptance_criteria>
  <done>Criterion 5's route clause is measured, in both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verified caller → Firebase revocation write | `sign_out_all` is the one route that writes at the provider |
| Verified caller → DeviceCheck | The anonymous grant claim's evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-05-01 | Elevation of privilege | `sign_out_all` | high | mitigate | The route keeps `Depends(get_claims)` and `Depends(get_identity)`; only the adapter parameter changes. ASVS V4 by preservation. The new `_declared` case in task 2 fails if the barrier parameters are dropped along with the adapter. |
| T-50-05-02 | Spoofing | `revoke_with_retry` arguments | high | mitigate | The issuer and subject still come from `claims`, the request-verified pair, never from `linked`. The task action names the call arguments explicitly and `tests/e2e` covers the route. |
| T-50-05-03 | Information disclosure | `get_runtime` on an unintended route | medium | mitigate | Task 2's second case fails if any other route declares `get_runtime`, so a future route cannot quietly reach the whole container. |
| T-50-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed or upgraded; `uv.lock` is not touched. |
</threat_model>

<verification>
- `.venv/bin/pytest -q` and `.venv/bin/pytest -q -m e2e` exit 0
- `.venv/bin/ruff check src tests` prints `All checks passed!`
- Neither deleted getter appears anywhere in `src` or `tests`
</verification>

<success_criteria>
All four getters criterion 5 names are gone, `sign_out_all` declares `get_runtime` and reads
`runtime.firebase_adapter`, and no other route declares `get_runtime`.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 50-05)

- `get_auth_service(db, runtime: Runtime = Depends(get_runtime))` — rewritten signature
- `sign_out_all(claims, linked, runtime: Runtime = Depends(get_runtime))` — rewritten signature
- A wiring class in `tests/unit/test_app_wiring.py` pinning criterion 5's route clause

Removed: `get_firebase_adapter`, `get_devicecheck_adapter`, `app.state.firebase_adapter`,
`app.state.devicecheck_adapter`.
</artifacts>

<output>
Create `.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-05-SUMMARY.md` when done
</output>
