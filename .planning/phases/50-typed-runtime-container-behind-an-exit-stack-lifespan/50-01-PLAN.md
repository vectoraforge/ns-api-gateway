---
phase: 50-typed-runtime-container-behind-an-exit-stack-lifespan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/test_restore_subscription.py
  - src/nativespeaker/api/auth/google_play.py
  - src/nativespeaker/api/app/lifespan.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/services/restore.py
  - tests/unit/test_google_play_notifications.py
  - tests/unit/test_auth_package_shape.py
  - tests/e2e/conftest.py
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-09: `GooglePlayNotifications` is the one Google Play class; it keeps `verify`, `read` and `read_for_restore`, each raising `Unavailable` with its current stage string when its value is `None`."
    - "D-10: no `build` parameter, rebuild lock, rebuild interval or `_credential_in_hand` survives; the lifespan passes each value once."
    - "The inherited Phase-47 e2e failure is fixed in its own commit, before anything else in this phase."
    - "`tests/unit/test_auth_package_shape.py` carries a re-measured `CURRENT`, read off the failing run's own text, in the same commit as the merge."
  artifacts:
    - src/nativespeaker/api/auth/google_play.py
    - tests/e2e/test_restore_subscription.py
    - tests/unit/test_auth_package_shape.py
  key_links:
    - "`dependencies.py` verify + read → the one merged class"
    - "`services/restore.py` `play` annotation → `GooglePlayNotifications`"
    - "`tests/e2e/conftest.py` Play fixtures → one swapped attribute"
---

<objective>
Clear the one failing e2e case this phase inherits from Phase 47, then merge the two Google Play
classes into `GooglePlayNotifications` and delete the rebuild machinery.

Purpose: criterion 7 cannot be measured while a foreign failure is red, and `Runtime`'s
`google_play_notifications` field (plan 50-03) needs the merged type to already exist.
Output: one Play class, a green e2e suite, a re-measured `CURRENT`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

This directory is a git submodule: its `.git` is a file. It is NOT a worktree — do not treat it as
one. Do not create, rename or switch git branches.

`.venv/bin/pytest -q` is unit-only. The schema and e2e suites need `-m e2e`, `-m schema` or `-m ''`
and a live localhost PostgreSQL.
</execution_context>

<context>
@.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-CONTEXT.md
@.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-RESEARCH.md
@.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-PATTERNS.md
@AGENTS.md
</context>

<!-- planner-discipline-allow: PubSubPushTokens -->
<!-- planner-discipline-allow: PlayDeveloperSubscriptions -->
<!-- planner-discipline-allow: _credential_in_hand -->
<!-- planner-discipline-allow: _rebuild_lock -->
<!-- planner-discipline-allow: PUSH_VERIFIER_REBUILD_INTERVAL_SECONDS -->
<!-- planner-discipline-allow: PLAY_CREDENTIAL_REBUILD_INTERVAL_SECONDS -->

<tasks>

<!--
Tracer placement, recorded rather than assumed: the phase's `type="tracer"` slice is task 1 of plan
50-03. It cannot lead the phase, because `Runtime.google_play_notifications` must be annotated with
the merged type, and the merge is task 2 below. This plan and plan 50-02 are its two prerequisites.
-->

<task type="auto">
  <name>Task 1: the Phase-47 log-event expectation, in its own commit</name>
  <files>tests/e2e/test_restore_subscription.py</files>
  <read_first>
    - tests/e2e/test_restore_subscription.py (lines 770-800, the four-arms case)
    - src/nativespeaker/api/errors.py (the `PurchaseProofRejected` class and its `code`)
    - .planning/STATE.md (the Phase 47 paragraph naming this case)
  </read_first>
  <action>
    In `tests/e2e/test_restore_subscription.py`, class
    `TestEveryRejectedProofOfBothStoresAnswersOneBody`, case
    `test_the_four_arms_answer_bodies_equal_to_each_other_and_write_nothing`: the two log-event
    literals in the `refusal_records.entries` comparison are the class name, not the response code.
    Change both to `purchase_proof_rejected`, matching `tests/unit/test_rejection_vocabulary.py:115`.
    Leave `PROOF_REJECTED_BODY` and every `answer.content` assertion untouched: the body code is
    correct and stays. Do NOT edit `src/nativespeaker/api/errors.py` — the rename in commit
    `275bd8e` is the intended state, and only this expectation was missed.
    Commit alone, message naming Phase 47 as the origin of the defect.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m e2e tests/e2e/test_restore_subscription.py</automated>
    <fails_when>non-zero exit, or any line matching "failed" in the summary, or "no tests ran"</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>non-zero exit, or the summary reports 1 or more failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD~1` for this commit lists exactly `tests/e2e/test_restore_subscription.py`.
    - `.venv/bin/pytest -q -m e2e` exits 0 and its summary line reports 0 failed.
    - `grep -c 'PROOF_REJECTED_BODY' tests/e2e/test_restore_subscription.py` is unchanged from before the edit.
    - `src/nativespeaker/api/errors.py` is byte-identical to its state at HEAD before this task.
  </acceptance_criteria>
  <done>The e2e suite exits 0 with no change to any source file.</done>
</task>

<task type="auto">
  <name>Task 2: one Google Play class, no rebuild machinery</name>
  <files>src/nativespeaker/api/auth/google_play.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/services/restore.py, tests/unit/test_google_play_notifications.py, tests/unit/test_auth_package_shape.py, tests/e2e/conftest.py</files>
  <read_first>
    - src/nativespeaker/api/auth/google_play.py (whole file; lines 192-340 are the two classes)
    - src/nativespeaker/api/auth/app_store.py lines 40-125 (the shape to follow)
    - src/nativespeaker/api/app/lifespan.py lines 194-217 (the two constructions)
    - src/nativespeaker/api/app/dependencies.py lines 139-186 (the three reads)
    - src/nativespeaker/api/services/restore.py lines 1-55
    - tests/unit/test_google_play_notifications.py lines 300-330 and 1000-1160
    - tests/e2e/conftest.py lines 470-580 (the Play fakes and fixtures)
    - tests/unit/test_auth_package_shape.py lines 1-30
  </read_first>
  <action>
    Per D-09 and D-10.

    In `auth/google_play.py`: replace the two classes with one class `GooglePlayNotifications`,
    shaped like `AppStoreNotifications`. Keyword-only constructor taking `verifier: JWTVerifier | None`,
    `credential`, `client: httpx.AsyncClient` and `products: dict[str, str]`, one `self._x = x` per
    parameter. Keep the three entry points `verify`, `read` and `read_for_restore` with their present
    bodies, each opening with its own `if self._verifier is None` / `if self._credential is None`
    guard raising `Unavailable` with the stage string it uses today: `google_push_verify`,
    `play_subscriptions_read`, `RESTORE_UNCONFIGURED_STAGE`. Delete the `build` parameters, the
    `rebuild_interval_seconds` parameters, `self._rebuild_lock`, `self._rebuild_interval`,
    `self._next_rebuild`, the `_credential_in_hand` helper and the two module constants
    `PUSH_VERIFIER_REBUILD_INTERVAL_SECONDS` and `PLAY_CREDENTIAL_REBUILD_INTERVAL_SECONDS`.
    Drop the now-unused `asyncio`, `time` and `Callable` imports. Keep `CappedRefreshRequest`
    unchanged: it is the refresh transport, not the rebuild. The class docstring is one line saying
    what the class does — Google Play's push token and the live subscription read — never what the
    token is.

    In `app/lifespan.py`: one construction in place of two, assigned to
    `app.state.google_play_notifications`, taking `verifier=google_push_verifier`,
    `credential=play_credential`, `client=play_client`, `products=config.google_play.products`.
    Leave the warnings and the boot rule exactly as they are; plan 50-02 changes those.

    In `app/dependencies.py`: `verify_google_play_notification` reads
    `request.app.state.google_play_notifications` for both the token check and the subscription read;
    `get_restore_service` passes the same object as `play=`.

    In `services/restore.py`: the import at line 13 and the `play` parameter annotation at line 44
    name `GooglePlayNotifications`.

    In `tests/e2e/conftest.py`: give `FakePlaySubscriptions` an `async def verify(self, bearer)` that
    records into a `push_calls` list, so one fake serves all three entry points. Collapse
    `scripted_play_subscriptions` and `scripted_google_play` into fixtures that swap the one
    attribute `app.state.google_play_notifications`; keep both fixture names and both yielded values
    so no test module changes. `unconfigured_google_play` builds one `GooglePlayNotifications` with
    `verifier=None`, `credential=None`, the mock-transport client and `products={}`. Delete
    `AcceptingPushTokens` if nothing still refers to it.

    In `tests/unit/test_google_play_notifications.py`: the `push_tokens` fixture builds the merged
    class; `_stub_request` keeps its `SimpleNamespace` shape for now but names the one merged
    attribute (plan 50-06 turns it into a `Runtime`). Delete the class
    `TestAWarmUpFailureIsRetriedRatherThanCachedForThePodsLife` in full — it tests the machinery this
    task removes.

    In `tests/unit/test_auth_package_shape.py`: run the suite, read the measured tuple off the
    failing assertion's own output, and write that tuple into `CURRENT` in this same commit. Do NOT
    compute it by arithmetic and do NOT copy a number from any planning document.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_google_play_notifications.py tests/unit/test_auth_package_shape.py</automated>
    <fails_when>non-zero exit, or "no tests ran", or an AssertionError naming CURRENT</fails_when>
    <automated>.venv/bin/pytest -q</automated>
    <fails_when>non-zero exit, or the summary reports 1 or more failed</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>non-zero exit, or the summary reports 1 or more failed</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>anything other than "All checks passed!" on exit 0</fails_when>
    <automated>test -z "$(grep -rn 'PubSubPushTokens\|PlayDeveloperSubscriptions\|_credential_in_hand\|_rebuild_lock\|_next_rebuild\|REBUILD_INTERVAL_SECONDS' src tests --include='*.py')"</automated>
    <fails_when>non-zero exit — any of those six names still appears in a tracked Python file</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^class GooglePlayNotifications' src/nativespeaker/api/auth/google_play.py` is 1.
    - `grep -c 'class CappedRefreshRequest' src/nativespeaker/api/auth/google_play.py` is 1.
    - The six deleted names are absent from `src` and `tests` (the `test -z` gate above).
    - `grep -c 'google_play_notifications' src/nativespeaker/api/app/dependencies.py` is 3 or more, and `play_subscriptions` and `google_push_tokens` appear nowhere in `src`.
    - `services/restore.py` imports `GooglePlayNotifications` and annotates `play` with it.
    - `.venv/bin/pytest -q` and `.venv/bin/pytest -q -m e2e` both exit 0.
    - `CURRENT` in `tests/unit/test_auth_package_shape.py` differs from `(7, 19, 60)` and the change lands in this same commit.
  </acceptance_criteria>
  <done>One Google Play class, three entry points, no rebuild code; both suites exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Pub/Sub push → `/webhooks/google-play/rtdn` | An unauthenticated caller presents a bearer the merged class verifies |
| Play Developer API → this pod | The subscription read is the only evidence a grant is written from |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01-01 | Spoofing | `GooglePlayNotifications.verify` | high | mitigate | The merged `verify` keeps the `if self._verifier is None: raise Unavailable(stage="google_push_verify")` guard as its first statement, so a deployment with no verifier refuses rather than admits. Pinned by the existing refusal cases in `tests/unit/test_google_play_notifications.py`. |
| T-50-01-02 | Elevation of privilege | `read_for_restore` | high | mitigate | The `RESTORE_UNCONFIGURED_STAGE` guard and both `_names_one_path_segment` checks are carried over unchanged; the e2e restore suite is a verify command of this task. |
| T-50-01-03 | Information disclosure | merged class logging | low | accept | Log labels stay the closed set the module docstring names; no new log call is added. |
| T-50-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed or upgraded in this plan; `uv.lock` is not touched. RESEARCH.md records the Package Legitimacy Audit as not applicable. |
</threat_model>

<verification>
- `.venv/bin/pytest -q` exits 0
- `.venv/bin/pytest -q -m e2e` exits 0
- `.venv/bin/ruff check src tests` prints `All checks passed!`
- Two commits: the e2e expectation alone, then the merge with its re-measured `CURRENT`
</verification>

<success_criteria>
ROADMAP criterion 8, first half: `GooglePlayNotifications` is the one Google Play class, with no
`build` callable, lock or rebuild interval. Criterion 7 is no longer blocked by a foreign failure.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 50-01)

- `GooglePlayNotifications` (class, `src/nativespeaker/api/auth/google_play.py`) — methods `verify`,
  `read`, `read_for_restore`; constructor keywords `verifier`, `credential`, `client`, `products`
- `app.state.google_play_notifications` (attribute, temporary — plan 50-06 moves it into `Runtime`)
- `FakePlaySubscriptions.verify` and `FakePlaySubscriptions.push_calls` (`tests/e2e/conftest.py`)

Removed: `PubSubPushTokens`, `PlayDeveloperSubscriptions`, `_credential_in_hand`,
`PUSH_VERIFIER_REBUILD_INTERVAL_SECONDS`, `PLAY_CREDENTIAL_REBUILD_INTERVAL_SECONDS`,
`AcceptingPushTokens`, `TestAWarmUpFailureIsRetriedRatherThanCachedForThePodsLife`,
`app.state.google_push_tokens`, `app.state.play_subscriptions`.
</artifacts>

<output>
Create `.planning/phases/50-typed-runtime-container-behind-an-exit-stack-lifespan/50-01-SUMMARY.md` when done
</output>
