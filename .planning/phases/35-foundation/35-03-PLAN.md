---
phase: 35-foundation
plan: 03
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - src/nativespeaker/api/auth/context.py
  - src/nativespeaker/api/auth/__init__.py
  - src/nativespeaker/api/auth/registry.py
  - src/nativespeaker/api/models/identities.py
  - src/nativespeaker/api/models/api.py
  - src/nativespeaker/api/models/__init__.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/app/lifespan.py
  - src/nativespeaker/api/app/main.py
  - src/nativespeaker/api/routers/__init__.py
  - src/nativespeaker/api/routers/chats.py
  - src/nativespeaker/api/routers/users.py
  - src/nativespeaker/api/routers/webhooks.py
  - src/nativespeaker/api/services/__init__.py
  - src/nativespeaker/api/services/chats.py
  - src/nativespeaker/api/services/firebase.py
  - src/nativespeaker/api/services/subscriptions.py
  - src/nativespeaker/api/services/users.py
  - src/nativespeaker/api/database/__init__.py
  - src/nativespeaker/api/database/subscriptions.py
  - src/nativespeaker/api/database/usage.py
  - src/nativespeaker/api/database/users.py
  - tests/unit/test_identity_accessors.py
  - tests/unit/test_subscriptions.py
  - tests/unit/test_usage.py
  - tests/unit/test_webhooks.py
  - tests/unit/test_users.py
  - tests/e2e/test_users.py
autonomous: true
requirements: [FOUND-01, FOUND-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No module in src/ imports SubscriptionService, UserService, FirebaseService, UsersDB, UsageDB, or SubscriptionDB, and none of those files exists"
    - "The application registers exactly eight routes — GET /, GET /examples, GET /health/ready, GET /chats, POST /chats, GET /chats/{chat_id}, POST /chats/{chat_id}, DELETE /chats/{chat_id} — and the registry declares exactly those eight, so set equality holds in both directions"
    - "Reading the identity context on a request where the barrier did not attach one raises and is answered auth_required — never a None a handler could treat as anonymous, and never a silently open route"
    - "The identity context carries the identity variant, the route metadata record, the client-IP bucket kind, one captured evaluation time, and a server-generated attempt id, so no later phase recomputes any of them"
    - "No route creates a core.users or core.external_identities row; just-in-time provisioning is gone and only POST /auth/create-user will create accounts"
    - "Startup no longer initializes firebase_admin or an Apple receipt verifier, and app.state carries neither"
    - "Chat handlers take a user id rather than a User row, and consume the barrier's identity context through Depends() accessors only"
  artifacts:
    - path: "src/nativespeaker/api/auth/context.py"
      provides: "§1.4 typed identity context — the seam phases 36-46 import verbatim"
      exports: ["IdentityKind", "LinkedIdentity", "PreAuthIdentity", "ClientIpBucketKind", "RequestContext", "REQUEST_CONTEXT_SCOPE_KEY"]
    - path: "src/nativespeaker/api/models/identities.py"
      provides: "core.external_identities model and its three native enums"
      exports: ["IdentityProvider", "IdentityState", "NativeClaimProvider", "ExternalIdentity"]
    - path: "src/nativespeaker/api/app/dependencies.py"
      provides: "D-02 typed Depends() accessors that fail loudly when the barrier did not run"
      exports: ["get_config", "get_db", "get_chat_service", "get_request_context", "get_linked_identity", "get_preauth_identity"]
  prohibitions:
    - statement: "MUST NOT create, link, repair, reassign, or merge a core.users or core.external_identities row from the barrier, an accessor, or any read path — an unlinked verified subject is rejected, never provisioned just-in-time the way v1.6 did"
      status: resolved
      verification: test
  key_links:
    - from: "src/nativespeaker/api/routers/chats.py"
      to: "src/nativespeaker/api/app/dependencies.py"
      via: "Depends(get_linked_identity) replaces Depends(get_current_user); require_quota is gone"
      pattern: "Depends\\(get_linked_identity\\)"
    - from: "src/nativespeaker/api/app/dependencies.py"
      to: "src/nativespeaker/api/auth/context.py"
      via: "accessors read REQUEST_CONTEXT_SCOPE_KEY off request.state and raise when absent"
      pattern: "REQUEST_CONTEXT_SCOPE_KEY"
---

<objective>
Delete every surface a later phase replaces, and put the typed identity context and its
`Depends()` accessors in their place.

Purpose: D-16 says delete rather than repair code with a known deletion date. Everything here
is either dead against the v2.0 schema (the usage and subscription layers, JIT provisioning,
the Firebase plan-claim sync) or scoped to a later phase (`/users/me` is Phase 39,
`/webhooks/app-store` is Phase 43). Deleting it now also leaves nothing stale for the
enumeration assertion to reconcile, which is why the registry drops to eight entries in the
same commit as the routers.

Output: an eight-route application, a `core.external_identities` model, and the `§1.4` context
seam that phases 36-46 import by name.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-foundation/35-CONTEXT.md
@.planning/phases/35-foundation/35-RESEARCH.md
@.planning/phases/35-foundation/35-PATTERNS.md
@.planning/phases/35-foundation/35-02-SUMMARY.md

<interfaces>
src/nativespeaker/api/auth/context.py — the §1.4 seam. Every later phase reads these and
recomputes none of them.

    REQUEST_CONTEXT_SCOPE_KEY = "ns_request_context"

    class IdentityKind(StrEnum): linked, preauth
    class ClientIpBucketKind(StrEnum): ipv4, ipv6, unresolved

    @dataclass(frozen=True, slots=True)
    class LinkedIdentity:
        kind: Literal[IdentityKind.linked]
        user: User                    # core.users row: id, active, registered_at, ...
        identity: ExternalIdentity    # core.external_identities row: id, issuer, identity_state,
                                      #   provider, provider_uid
        issuer: str
        subject: str

    @dataclass(frozen=True, slots=True)
    class PreAuthIdentity:
        kind: Literal[IdentityKind.preauth]
        issuer: str
        subject: str

    @dataclass(frozen=True, slots=True)
    class RequestContext:
        identity: LinkedIdentity | PreAuthIdentity
        route_metadata: RouteMetadata
        client_ip_bucket_kind: ClientIpBucketKind
        evaluated_at: datetime        # the ONE captured evaluation time for this request
        attempt_id: UUID              # server-generated, uuid7

src/nativespeaker/api/app/dependencies.py
    def get_request_context(request: Request) -> RequestContext        # raises when absent
    def get_linked_identity(request: Request) -> LinkedIdentity        # raises when absent or pre-auth
    def get_preauth_identity(request: Request) -> PreAuthIdentity      # raises when absent or linked
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typed identity context and the fail-loudly Depends() accessors</name>
  <reversibility rating="costly">D-02 — this is the seam phases 36-46 import verbatim; changing the accessor names or the context field set later means editing every one of those phases.</reversibility>
  <files>src/nativespeaker/api/auth/context.py, src/nativespeaker/api/models/identities.py, src/nativespeaker/api/auth/__init__.py, src/nativespeaker/api/app/dependencies.py, tests/unit/test_identity_accessors.py</files>
  <read_first>
    - `/home/init/native-speaker/specs/auth-refactor-phases/01-foundation.md` §1.4 — the two variants, the four request-scoped values, and the fail-loudly rule
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` § Identity and ownership, § The barrier
    - `migrations/20260818_01_initial-release.sql` lines 200-250 — the `core.external_identities` column list and CHECKs, and lines 48-58 for `core.identity_provider`, `core.identity_state`, `core.native_claim_provider`
    - `src/nativespeaker/api/models/subscriptions.py` lines 29-52 — the native-enum `cast(Any, Enum(..., name=..., schema='core'))` idiom and the SQLModel table shape `models/identities.py` copies
    - `src/nativespeaker/api/models/users.py` line 10 — the `DateTimeType = cast(Any, DateTime(timezone=True))` idiom
    - `src/nativespeaker/api/app/dependencies.py` lines 16-27 — `get_config` and `get_db`, which stay verbatim
    - `src/nativespeaker/api/errors.py` — `AuthenticationError` / `AUTH_REQUIRED`, what the accessors raise
    - `src/nativespeaker/api/auth/registry.py` — `RouteMetadata`, carried on the context
    - `.planning/phases/35-foundation/35-PATTERNS.md` § `auth/context.py`, § `models/identities.py`, § `Depends()`-only route signatures
  </read_first>
  <action>
Create `src/nativespeaker/api/models/identities.py` declaring the three native PostgreSQL enums
and the `core.external_identities` SQLModel table, following the enum-and-table idiom in
`models/subscriptions.py`. `IdentityProvider` has `anonymous`, `google`, `apple`;
`IdentityState` has exactly `active` and `historical`; `NativeClaimProvider` mirrors
`core.native_claim_provider`. `ExternalIdentity` maps every column in the migration:
`id`, `user_id`, `issuer`, `subject`, `provider`, `provider_uid` (nullable),
`identity_state` (NOT NULL, default `active`), `native_claim_platform` (nullable),
`free_grant_consumed_at` (nullable), `created_at`, `updated_at`, `historical_at` (nullable),
with `__table_args__` carrying `{"schema": "core"}`. Do not encode the table CHECKs in Python —
the database owns them.

Create `src/nativespeaker/api/auth/context.py` with the five §1.4 symbols. `LinkedIdentity`
carries the resolved `User` row, the matching `ExternalIdentity` row, and the verified
`(issuer, subject)`. `PreAuthIdentity` carries the verified `(issuer, subject)` and nothing
else — no user row, no identity row, no provider. `RequestContext` carries the identity
variant, the `RouteMetadata` record, the `ClientIpBucketKind`, one `evaluated_at`, and one
`attempt_id`. Every dataclass is `frozen=True, slots=True`. Add a module docstring recording
that the stored `ExternalIdentity.provider` column is the sole per-request classifier and that
`User.registered_at` is reporting-only and must never be used as a competing classifier.
`ClientIpBucketKind` records the bucket kind only — `ipv4`, `ipv6`, or `unresolved` — and the
context carries no client address, because §9 is deferred so `xff_num_trusted_hops` is unpinned
and a stored address would be trusted rather than proven. It is never recomputed from raw
forwarded headers.

Add `get_request_context`, `get_linked_identity`, and `get_preauth_identity` to
`src/nativespeaker/api/app/dependencies.py`. Each reads `REQUEST_CONTEXT_SCOPE_KEY` off
`request.state` and raises `AuthenticationError` when it is absent, so the response is
`auth_required` — that is §1.4's "fails loudly", and putting the check in one place stops each
of the seven later phases re-implementing it. `get_linked_identity` additionally raises when
the attached variant is pre-auth; `get_preauth_identity` raises when it is linked. Keep
`get_config`, `get_db`, and `get_chat_service` verbatim.

Add `context.py` exports to the `auth/__init__.py` barrel, keeping the `__all__`-first
alphabetized shape.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/unit/test_identity_accessors.py` exits 0
    - A request whose `state` carries no context makes each of the three accessors raise, and the raised class resolves to `AUTH_REQUIRED` (401, `{"code":"auth_required"}`)
    - `get_linked_identity` raises when the attached variant is `PreAuthIdentity`; `get_preauth_identity` raises when it is `LinkedIdentity`; neither returns `None` on any path
    - `.venv/bin/python -c "from nativespeaker.api.auth.context import RequestContext as R; print(sorted(R.__dataclass_fields__))"` prints `['attempt_id', 'client_ip_bucket_kind', 'evaluated_at', 'identity', 'route_metadata']`
    - `.venv/bin/python -c "from nativespeaker.api.auth.context import PreAuthIdentity as P; print(sorted(P.__dataclass_fields__))"` prints exactly `['issuer', 'kind', 'subject']`
    - `.venv/bin/python -c "from nativespeaker.api.models.identities import IdentityState; print([m.value for m in IdentityState])"` prints `['active', 'historical']`
    - `src/nativespeaker/api/auth/context.py` declares no field holding a client IP address
    - `.venv/bin/python -m pytest -q` exits 0 and `.venv/bin/ruff check src tests` prints `All checks passed!`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_identity_accessors.py && .venv/bin/python -m pytest -q && .venv/bin/ruff check src tests && .venv/bin/ty check src</automated>
  </verify>
  <done>The §1.4 context seam exists, the accessors fail loudly, and `tests/unit/test_identity_accessors.py` proves both.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the subscription, usage, webhook, and users surfaces in one import-clean commit</name>
  <reversibility rating="reversible">D-16 — the deleted code stays in git history and both replacements (`/users/me` in Phase 39, `/webhooks/app-store` in Phase 43) are already scoped to their own phases.</reversibility>
  <files>src/nativespeaker/api/routers/users.py, src/nativespeaker/api/routers/webhooks.py, src/nativespeaker/api/routers/__init__.py, src/nativespeaker/api/services/users.py, src/nativespeaker/api/services/subscriptions.py, src/nativespeaker/api/services/firebase.py, src/nativespeaker/api/services/__init__.py, src/nativespeaker/api/services/chats.py, src/nativespeaker/api/database/users.py, src/nativespeaker/api/database/usage.py, src/nativespeaker/api/database/subscriptions.py, src/nativespeaker/api/database/__init__.py, src/nativespeaker/api/routers/chats.py, src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/app/main.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/auth/registry.py, src/nativespeaker/api/models/api.py, tests/unit/test_subscriptions.py, tests/unit/test_usage.py, tests/unit/test_webhooks.py, tests/e2e/test_users.py, tests/unit/test_users.py</files>
  <read_first>
    - `.planning/phases/35-foundation/35-CONTEXT.md` D-15 and D-16 — what is deleted and why, and what "boots" means
    - `.planning/phases/35-foundation/35-RESEARCH.md` § Runtime State Inventory § The boot-repair surface — the module-by-module disposition table
    - `.planning/phases/35-foundation/35-PATTERNS.md` § Cross-Cutting Notes for the Planner items 2 and 7
    - `src/nativespeaker/api/routers/__init__.py`, `services/__init__.py`, `database/__init__.py` — the `__all__`-first barrel shape; the lint gate's `I` rule enforces import order
    - `src/nativespeaker/api/app/lifespan.py` — the `apple_verifier`, `firebase_admin.initialize_app`, `FirebaseService`, and shutdown `delete_app` lines being removed
    - `src/nativespeaker/api/app/main.py` — the `users_router` / `webhooks_router` registrations
    - `src/nativespeaker/api/routers/chats.py` — every `summary=` and `description=` kwarg is the OpenAPI contract and must survive unchanged
    - `src/nativespeaker/api/services/chats.py` — the `create_chat(user=...)` and `send_message(user=...)` signatures
    - `src/nativespeaker/api/auth/registry.py` — the ten declarations written by plan 01
    - `tests/unit/test_users.py` — the `/users/me` cases to remove and any surviving cases to keep
  </read_first>
  <action>
Delete, in one commit so the package imports at every point: `routers/users.py`,
`routers/webhooks.py`, `services/users.py`, `services/subscriptions.py`,
`services/firebase.py`, `database/users.py`, `database/usage.py`,
`database/subscriptions.py`.

`GET /users/me` cannot serve unchanged against the v2.0 schema — it reads `user.name`,
`user.subscription_plan`, and `UsageDB`, all three of which the schema dropped — so it is
deleted under D-16 rather than repaired, and Phase 39 rewrites it. `services/firebase.py` is
deleted rather than repaired: its only method syncs a subscription-plan custom claim that no
longer exists, and SHARED-INVARIANTS forbids rederiving provider from claims.
`database/users.py` goes with them because just-in-time provisioning is gone in v2.0 — only
`POST /auth/create-user` creates accounts, and the barrier's single `(issuer, subject)`
resolution query replaces `get_or_create`.

Update the barrels in the same commit — `models/__init__.py:34` and `database/__init__.py:5`
re-export symbols that are about to disappear, and the package will not import if the barrel
lags the deletion. `routers/__init__.py` drops `users_router` and `webhooks_router`;
`services/__init__.py` drops `FirebaseService`, `SubscriptionService`, `UserService`, and
`create_apple_verifier`; `database/__init__.py` keeps only `ChatsDB`. Delete
`UserProfileResponse` from `models/api.py` and from the `models/__init__.py` `__all__` and
imports.

Remove the `users_router` and `webhooks_router` `include_router` calls and imports from
`app/main.py`. In `app/lifespan.py` remove the `apple_verifier` assignment, the
`firebase_admin.initialize_app(credentials.ApplicationDefault())` call, the `FirebaseService`
construction, the matching shutdown `firebase_admin.delete_app(...)`, and the now-unused
imports. That also removes Google Application Default Credentials from the startup path.

In `app/dependencies.py` delete `get_subscription_service`, `get_current_user`, and
`require_quota`. `get_current_user` is deleted specifically because it reads the token through
FastAPI's `Header(None)` alias, which returns a single folded value and cannot see a duplicate
`Authorization` field — the exact case §1.1 requires rejecting.

In `routers/chats.py` replace every `user: User = Depends(get_current_user)` parameter with
`identity: LinkedIdentity = Depends(get_linked_identity)` and pass `identity.user.id` where
`user.id` was passed; remove `dependencies=[Depends(require_quota)]` from both POST routes.
Quota enforcement is Phase 36's REBIND-05, and the named `quota_checked_request` admission
entry §8.4 described is void because D-05 deleted backend rate limiting from the product. Keep
every `summary=`, `description=`, and `response_description=` kwarg byte-identical — they are
the published OpenAPI contract. In `services/chats.py` change `create_chat` and `send_message`
to take `user_id: UUID` instead of `user: User`, and keep every `raise` site pointing at the
registry classes.

In `auth/registry.py` delete the `GET /users/me` and `POST /webhooks/apple` declarations,
leaving exactly eight entries. The router and the registry change together because §2.3 is a
set-equality check on the live router; a commit that changes one without the other aborts boot.

Delete `tests/unit/test_subscriptions.py`, `tests/unit/test_usage.py`,
`tests/unit/test_webhooks.py`, and `tests/e2e/test_users.py` — their surfaces are gone, so per
D-18 the tests go with them rather than becoming xfail markers. Remove the `/users/me` cases
from `tests/unit/test_users.py`; if nothing survives, delete the module.
  </action>
  <acceptance_criteria>
    - `test ! -e src/nativespeaker/api/routers/users.py -a ! -e src/nativespeaker/api/routers/webhooks.py -a ! -e src/nativespeaker/api/services/users.py -a ! -e src/nativespeaker/api/services/subscriptions.py -a ! -e src/nativespeaker/api/services/firebase.py -a ! -e src/nativespeaker/api/database/users.py -a ! -e src/nativespeaker/api/database/usage.py -a ! -e src/nativespeaker/api/database/subscriptions.py` succeeds
    - `.venv/bin/python -c "import nativespeaker.api.app.main"` exits 0
    - `.venv/bin/python -c "from nativespeaker.api.app.main import app; from nativespeaker.api.auth.registry import enumerate_registered, REGISTRY; r,p=enumerate_registered(app); print(len(r), len(REGISTRY), sorted(r)==sorted({(e.method,e.path) for e in REGISTRY}), p)"` prints `8 8 True []`
    - `.venv/bin/python -m pytest -q -m e2e tests/e2e/test_startup_assertion.py` exits 0
    - `.venv/bin/python -c "from nativespeaker.api.app.main import app; import asyncio; asyncio.run((lambda: __import__('contextlib').AsyncExitStack())().__aenter__()) if False else None"` is not required; instead the e2e startup test above is the boot proof
    - `.venv/bin/python -c "import nativespeaker.api.app.lifespan as m, inspect; s=inspect.getsource(m); print('firebase_admin' in s, 'apple_verifier' in s)"` prints `False False`
    - `.venv/bin/python -c "import nativespeaker.api.app.dependencies as m; print(hasattr(m,'get_current_user'), hasattr(m,'require_quota'), hasattr(m,'get_subscription_service'))"` prints `False False False`
    - `.venv/bin/python -m pytest -q` exits 0
    - `.venv/bin/ruff check src tests` prints `All checks passed!` and `.venv/bin/ty check src` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "from nativespeaker.api.app.main import app; from nativespeaker.api.auth.registry import enumerate_registered, REGISTRY; r,p=enumerate_registered(app); assert len(r)==8 and p==[] and r=={(e.method,e.path) for e in REGISTRY}" && .venv/bin/python -m pytest -q && .venv/bin/python -m pytest -q -m e2e tests/e2e/test_startup_assertion.py && .venv/bin/ruff check src tests && .venv/bin/ty check src</automated>
  </verify>
  <done>Eight routes registered, eight declared, nothing in `src/` imports a deleted module, the app starts, and the dead tests are gone rather than skipped.</done>
</task>

<task type="auto">
  <name>Task 3: Narrow the surviving chat and isolation tests to what still holds</name>
  <files>tests/e2e/test_chats.py, tests/e2e/test_chat_queries.py, tests/e2e/test_isolation.py, tests/e2e/test_error_cases.py, tests/unit/test_auth_security.py, tests/unit/test_services.py</files>
  <read_first>
    - `.planning/phases/35-foundation/35-CONTEXT.md` D-15 and D-18 — what "boots" means and the phase-end bar
    - `.planning/phases/35-foundation/35-VALIDATION.md` § Wave 0 Requirements § Deletions — the exact narrowing list
    - `tests/e2e/test_chats.py`, `tests/e2e/test_chat_queries.py`, `tests/e2e/test_isolation.py`, `tests/e2e/test_error_cases.py` — the cases as they stand
    - `tests/unit/test_auth_security.py` — the cases that referenced `get_current_user`
    - `tests/unit/test_services.py` — the `ChatService` cases whose signatures changed
  </read_first>
  <action>
Bring the surviving suite back to green after task 2 without introducing a single xfail marker.
Phase 36 must start from a known-good baseline, not from an unknown number of pre-existing
failures.

Update `tests/unit/test_services.py` for the `create_chat(user_id=...)` /
`send_message(user_id=...)` signatures. Remove from `tests/unit/test_auth_security.py` every
case that exercised the deleted `get_current_user` dependency, keeping the cases that still
describe live behaviour.

The chat and isolation e2e modules authenticate with a real Firebase token whose subject has no
`core.external_identities` row yet, so with the barrier attached they will reject rather than
serve. Narrow each module to the assertions that hold in this state: keep and retarget the
cases that assert an unauthenticated or unlinked caller is rejected with the shared error body;
remove or move to plan 09 the cases that require a served chat response. Do not weaken an
assertion to make it pass — delete the case and record it in the plan SUMMARY so plan 09 can
restore it once `seed_identity` exists. Plan 05 adds the `seed_identity` and `stub_verifier`
harness that makes served-chat cases possible again.

Keep `tests/e2e/test_error_cases.py` cases that assert the shared body shape; retarget any that
asserted the retired 401 code onto `auth_required`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q -m ""` exits 0 with zero failures and zero errors
    - `grep -rn "xfail" tests/ | wc -l` prints `0`
    - `grep -rn "pytest.mark.skip" tests/ | wc -l` prints `0`
    - `.venv/bin/ruff check src tests` prints `All checks passed!`
    - The plan SUMMARY lists every removed e2e case by name for plan 09 to restore
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q -m "" && test "$(grep -rn 'xfail\|pytest.mark.skip' tests/ | wc -l)" -eq 0 && .venv/bin/ruff check src tests</automated>
  </verify>
  <done>The whole suite is green with no xfail and no skip, and every deliberately-removed case is named in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler → identity context | A handler that reads an absent context must not proceed as anonymous |
| router → registry | The registered set and the declared set must stay equal across every commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03-01 | Elevation of Privilege | `app/dependencies.py` accessors | high | mitigate | Each accessor raises when the context is absent and never returns `None`; the raised class resolves to `auth_required`, so a route registered outside the barrier fails closed instead of serving anonymously |
| T-35-03-02 | Elevation of Privilege | deletion of `get_current_user` | high | mitigate | The deleted dependency read the token through FastAPI's `Header(None)` alias, which folds duplicate `Authorization` fields into one value — exactly the desync §1.1 exists to reject. Removing it removes the second acceptance path |
| T-35-03-03 | Spoofing | `auth/context.py` classifier fields | medium | mitigate | Only the stored `ExternalIdentity.provider` column classifies; `registered_at` is documented reporting-only and no claim-derived field survives the move |
| T-35-03-04 | Information disclosure | `auth/context.py` client address | medium | mitigate | Bucket kind only (`ipv4`/`ipv6`/`unresolved`); no address is carried, because §9 is deferred so `xff_num_trusted_hops` is unpinned and any stored address would be trusted rather than proven (A3) |
| T-35-03-05 | Elevation of Privilege | registry / router divergence | high | mitigate | The two `include_router` removals and the two registry declaration removals land in one commit, and the e2e startup test proves set equality after it |
| T-35-03-06 | Tampering | deleted JIT provisioning path | medium | mitigate | No read path creates or repairs an identity row; the prohibition above is the standing statement, and `core.users` rows can only originate from `POST /auth/create-user` in Phase 37 |
| T-35-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package; the legitimacy gate is vacuous for Phase 35 |
</threat_model>

<artifacts_this_phase_produces>
Symbols created by **this plan**:

**New files** — `src/nativespeaker/api/auth/context.py`,
`src/nativespeaker/api/models/identities.py`, `tests/unit/test_identity_accessors.py`

**Deleted files** — `src/nativespeaker/api/routers/users.py`,
`src/nativespeaker/api/routers/webhooks.py`, `src/nativespeaker/api/services/users.py`,
`src/nativespeaker/api/services/subscriptions.py`, `src/nativespeaker/api/services/firebase.py`,
`src/nativespeaker/api/database/users.py`, `src/nativespeaker/api/database/usage.py`,
`src/nativespeaker/api/database/subscriptions.py`, `tests/unit/test_subscriptions.py`,
`tests/unit/test_usage.py`, `tests/unit/test_webhooks.py`, `tests/e2e/test_users.py`

**New symbols**
- `auth.context.REQUEST_CONTEXT_SCOPE_KEY`, `auth.context.IdentityKind`,
  `auth.context.ClientIpBucketKind`, `auth.context.LinkedIdentity`,
  `auth.context.PreAuthIdentity`, `auth.context.RequestContext`
- `models.identities.IdentityProvider`, `models.identities.IdentityState`,
  `models.identities.NativeClaimProvider`, `models.identities.ExternalIdentity`
- `app.dependencies.get_request_context`, `app.dependencies.get_linked_identity`,
  `app.dependencies.get_preauth_identity`

**Removed symbols** — `app.dependencies.get_current_user`, `app.dependencies.require_quota`,
`app.dependencies.get_subscription_service`, `services.UserService`,
`services.SubscriptionService`, `services.FirebaseService`, `services.create_apple_verifier`,
`database.UsersDB`, `database.UsageDB`, `database.SubscriptionDB`,
`models.api.UserProfileResponse`, `routers.users_router`, `routers.webhooks_router`
</artifacts_this_phase_produces>

<recorded_assumptions>
- **A3 (RESEARCH.md)** — the client IP is recorded as bucket kind only, read from
  `scope["client"]`, with the address itself omitted from the context and from audit `details`.
  §4.4 requires the kind; storing the address is optional and would be trusted-not-proven while
  §9 is deferred. One field to add if forensic value is later wanted.
- **A4 (RESEARCH.md)** — `services/firebase.py` is deleted rather than repaired. Recoverable
  from git; phases 37+ build the §7.1 adapter fresh.
- **Open question 2 (RESEARCH.md), resolved:** `GET /health/ready` returns a bare
  `JSONResponse` rather than a model. Leave it exactly as-is — only its registry declaration
  matters here. Do not "fix" it while touching neighbouring files.
- **Open question 3 (RESEARCH.md), flagged for Phase 36:** `core.access_tiers` is seeded empty
  by the migration, so REBIND-05 has no allowance to resolve against until tiers are
  configured. Out of scope here; record it in the SUMMARY.
</recorded_assumptions>

<verification>
- `.venv/bin/python -m pytest -q -m ""` green, zero xfail, zero skip
- `.venv/bin/ruff check src tests` and `.venv/bin/ty check src` clean
- Eight routes registered, eight declared, assertion passes at real startup
</verification>

<success_criteria>
- Every D-16 deletion is done and nothing in `src/` imports a deleted module
- The `§1.4` typed context exists with all five request-scoped values
- Reading the context where the barrier did not run raises and answers `auth_required`
- The suite is green with no xfail markers
</success_criteria>

<output>
Create `.planning/phases/35-foundation/35-03-SUMMARY.md` when done. Record: the final
eight-route registered set; every e2e case removed for plan 09 to restore; and the Phase 36
flag that `core.access_tiers` is empty.
</output>
