---
phase: 35-foundation
plan: 10
type: execute
wave: 9
depends_on: [35-06, 35-09]
files_modified:
  - src/nativespeaker/api/auth/__init__.py
  - tests/e2e/test_chats.py
  - tests/e2e/test_chat_queries.py
  - tests/e2e/test_isolation.py
  - tests/e2e/test_error_cases.py
  - .planning/phases/35-foundation/COVERAGE.md
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06, FOUND-07, FOUND-08]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "nativespeaker.api.auth exports the full public seam every later phase imports — barrier, wire contract, route registry, typed context, identity resolution, verification, audit writer, challenge store, mode-signal check, key derivation, budget gate, and adapter interfaces"
    - "The whole suite passes with zero failures, zero errors, zero xfail markers, and zero skip markers"
    - "ruff check src tests and ty check src both report clean"
    - "The application starts for real: the package imports, the lifespan runs to completion, the HMAC keyring validates, the registry self-check passes, and the route-enumeration assertion executes against the real router"
    - "Every e2e case removed during plan 03 is either restored against seeded identities or recorded in the SUMMARY with the phase that owns it"
    - "COVERAGE.md records the reasoned no-external-API declaration for this phase"
  artifacts:
    - path: "src/nativespeaker/api/auth/__init__.py"
      provides: "The stable import root phases 36-46 depend on"
    - path: ".planning/phases/35-foundation/COVERAGE.md"
      provides: "The api-coverage declaration required at seal time"
      contains: "No external API integration"
  key_links:
    - from: "src/nativespeaker/api/auth/__init__.py"
      to: "src/nativespeaker/api/auth/*.py"
      via: "__all__-first alphabetized re-export of every public symbol in the subpackage"
      pattern: "__all__"
---

<objective>
Close the phase: publish the `auth/` public seam, restore every test case the deletions took out
that now holds again, prove the whole suite green with no markers hiding anything, and write the
coverage declaration the seal-time gate reads.

Purpose: Phase 36 should start from a known-good baseline. D-18's bar is "delete dead tests,
everything else green" with no xfail markers, because an unknown number of pre-existing failures
is worse than a smaller suite.

Output: the finalized barrel, a green suite, and `COVERAGE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-foundation/35-CONTEXT.md
@.planning/phases/35-foundation/35-VALIDATION.md
@.planning/phases/35-foundation/35-03-SUMMARY.md
@.planning/phases/35-foundation/35-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the auth/ public seam and refresh the editable install</name>
  <reversibility rating="costly">D-23 — phases 36-46 import these module paths and these names; renaming an export after those phases land means editing every one of them.</reversibility>
  <files>src/nativespeaker/api/auth/__init__.py</files>
  <read_first>
    - `.planning/phases/35-foundation/35-CONTEXT.md` D-23 — the one stable import root and why the seam should be visibly one thing
    - `src/nativespeaker/api/models/__init__.py` — the `__all__`-first alphabetized barrel shape the lint gate's `I` rule enforces
    - Every module under `src/nativespeaker/api/auth/` — `adapters.py`, `audit.py`, `barrier.py`, `budgets.py`, `challenges.py`, `context.py`, `identity.py`, `keys.py`, `modesignal.py`, `registry.py`, `telemetry.py`, `verification.py`, `wire.py`
    - `.planning/phases/35-foundation/35-RESEARCH.md` § Runtime State Inventory — the build-artifact row on re-running the editable install
    - `pyproject.toml` lines 45-49 — the setuptools `packages.find` configuration
  </read_first>
  <action>
Rewrite `src/nativespeaker/api/auth/__init__.py` as the final public seam for the subpackage,
re-exporting every symbol phases 36-46 are expected to import by name. Follow the
`models/__init__.py` shape exactly: `__all__` first and alphabetized, then absolute imports
grouped by module, because ruff's `I` rule is on and enforces the ordering.

Export, at minimum: `AuthBarrierMiddleware`; `BoundedReason` and `extract_bearer`; `Category`,
`RouteMetadata`, `REGISTRY`, `lookup`, `enumerate_registered`, `assert_route_enumeration`;
`IdentityKind`, `ClientIpBucketKind`, `LinkedIdentity`, `PreAuthIdentity`, `RequestContext`,
`REQUEST_CONTEXT_SCOPE_KEY`; `Admit`, `Reject`, `AdmissionDecision`, `resolve_identity`;
`VerifiedClaims`, `TokenVerifier`, `JWTVerifier`; `RejectionCounter`, `record_rejection`;
`ACTOR_SUBJECT_PREFIX`, `IDP_ACCOUNT_PREFIX`, `HmacConfig`, `HmacKeyring`;
`DETAILS_SCHEMA_VERSION`, `build_details`, `redact`, `AuditWriter`; `CHALLENGE_TTL_SECONDS`,
`CHALLENGE_ID_BYTES`, `new_challenge_id`, `ChallengeRejection`, `ChallengeStore`; `ModeSignal`,
`classify_mode_signal`; `ADAPTER_FIREBASE_LOOKUP`, `FIREBASE_LOOKUP_ATTEMPTS`, `BudgetGate`,
`BudgetExhausted`; and every Protocol and result type in `adapters.py`.

The error registry stays at package root and is **not** re-exported here — D-10 puts it outside
`auth/` deliberately, because it owns every client-visible class in the service rather than only
the auth ones, and quota and LLM errors importing from an auth package would be misleading.

Re-run the editable install so setuptools `packages.find` discovers the `auth/` subpackage that
replaced the old `auth.py` module. Note that `nativespeaker/` and `nativespeaker/api/` are
implicit namespace packages with no `__init__.py`, while every subpackage has one — `auth/` needs
its explicit `__init__.py`, which this task writes.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import nativespeaker.api.auth as a; print(len(a.__all__) >= 40)"` prints `True`
    - `.venv/bin/python -c "import nativespeaker.api.auth as a; missing=[n for n in a.__all__ if not hasattr(a,n)]; print(missing)"` prints `[]`
    - `.venv/bin/python -c "import nativespeaker.api.auth as a; print(a.__all__ == sorted(a.__all__))"` prints `True`
    - `.venv/bin/python -c "import nativespeaker.api.auth as a; print(hasattr(a,'ErrorResponse'), hasattr(a,'ServiceError'))"` prints `False False`
    - `.venv/bin/python -c "from nativespeaker.api.auth import AuthBarrierMiddleware, ChallengeStore, AuditWriter, HmacKeyring, BudgetGate, resolve_identity, classify_mode_signal, assert_route_enumeration; print('seam ok')"` prints `seam ok`
    - `.venv/bin/ruff check src tests` prints `All checks passed!` and `.venv/bin/ty check src` reports no errors
    - `.venv/bin/python -m pytest -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import nativespeaker.api.auth as a; assert a.__all__ == sorted(a.__all__); assert not [n for n in a.__all__ if not hasattr(a,n)]" && .venv/bin/python -m pytest -q && .venv/bin/ruff check src tests && .venv/bin/ty check src</automated>
  </verify>
  <done>One import root exports the whole seam, the list is complete and alphabetized, and the editable install sees the subpackage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore the e2e coverage that holds again and gate the whole suite green</name>
  <precondition>PostgreSQL 17 is reachable at the `DB_*` coordinates in `.env` with the v2.0 schema applied, and `.planning/phases/35-foundation/35-03-SUMMARY.md` lists by name every e2e case plan 03 removed.</precondition>
  <files>tests/e2e/test_chats.py, tests/e2e/test_chat_queries.py, tests/e2e/test_isolation.py, tests/e2e/test_error_cases.py</files>
  <read_first>
    - `.planning/phases/35-foundation/35-03-SUMMARY.md` — the list of cases removed for restoration here
    - `.planning/phases/35-foundation/35-CONTEXT.md` D-15 and D-18 — what still cannot work (the chat quota path is Phase 36) and the no-xfail bar
    - `tests/e2e/conftest.py` — `seed_identity`, `stub_verifier`, `create_chat`, `_db_transaction`
    - `tests/e2e/test_barrier_admission.py` — the seeded-identity pattern to reuse
    - `tests/e2e/test_chats.py`, `tests/e2e/test_chat_queries.py`, `tests/e2e/test_isolation.py`, `tests/e2e/test_error_cases.py` — their current narrowed state
    - `.planning/phases/35-foundation/35-VALIDATION.md` § Sampling Rate — the pre-verify gate
  </read_first>
  <behavior>
    - A chat request from a seeded, linked, active identity is served: the barrier admits, the handler receives the context, and the response shape is unchanged from v1.6
    - A chat request from an unlinked identity rejects `preauth_identity_not_allowed`
    - A chat request from a historical identity or a blocked user rejects `account_unavailable`
    - Cross-user isolation still holds: a chat belonging to one seeded user is not readable by another
    - Existing non-auth business errors on the chat routes keep their current contract codes
    - No case asserts a quota outcome — the quota path is Phase 36 and no longer runs
  </behavior>
  <action>
Walk the list of cases plan 03 removed and restore each one that now holds, rewriting it against
`seed_identity` and `stub_verifier` rather than the real Firebase token where the case needs a
specific identity state. Restore cross-user isolation coverage, chat listing and message
retrieval, and the existing non-auth business-error cases.

Do not restore any case that asserts quota behaviour. The chat quota path reads a grant model
Phase 36 wires, so those routes still cannot enforce an allowance; D-15 draws the line there
deliberately. Record any case you do not restore, with the phase that owns it, in the plan
SUMMARY — do not leave it as an xfail, a skip, or a weakened assertion.

Do not weaken an assertion to make a case pass. If a case cannot hold in this phase, it is
deleted and recorded, not softened.

Then gate the whole phase: run the full suite, the lint gate, and the type gate, and confirm the
real application starts.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q -m ""` exits 0 with zero failures and zero errors
    - `grep -rn "xfail" tests/ | wc -l` prints `0`
    - `grep -rn "pytest.mark.skip" tests/ | wc -l` prints `0`
    - `.venv/bin/ruff check src tests` prints `All checks passed!`
    - `.venv/bin/ty check src` reports no errors
    - `.venv/bin/python -m pytest -q -m e2e tests/e2e/test_startup_assertion.py tests/e2e/test_barrier_admission.py tests/e2e/test_barrier_wire_contract.py tests/e2e/test_audit_writer.py tests/e2e/test_challenge_store.py tests/e2e/test_model_queries.py` exits 0
    - `tests/e2e/test_isolation.py` contains a case proving one seeded user cannot read another seeded user's chat
    - No test module asserts a quota outcome on a chat route
    - The plan SUMMARY names every case not restored and the phase that owns it
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q -m "" && test "$(grep -rn 'xfail\|pytest.mark.skip' tests/ | wc -l)" -eq 0 && .venv/bin/ruff check src tests && .venv/bin/ty check src</automated>
  </verify>
  <done>Every restorable case is restored against seeded identities, the full suite is green with no markers hiding anything, and both gates are clean.</done>
</task>

<task type="auto">
  <name>Task 3: Write the phase coverage declaration and record the accepted v2.0 gaps</name>
  <files>.planning/phases/35-foundation/COVERAGE.md</files>
  <read_first>
    - `/home/init/native-speaker/specs/auth-refactor-phases/01-foundation.md` §7 — the adapter interfaces this phase defines and calls zero times
    - `.planning/phases/35-foundation/35-CONTEXT.md` D-08 — the deferred gateway contract and its four accepted consequences
    - `.planning/phases/35-foundation/35-RESEARCH.md` § Pattern 4's note on `k8s/templates/backend-traffic-policy.yaml`
    - `.planning/phases/35-foundation/35-05-SUMMARY.md` — the missing metrics-exporter gap recorded there
    - `.planning/phases/35-foundation/35-06-SUMMARY.md` and `35-08-SUMMARY.md` — the zero-implementation and zero-production-row facts
  </read_first>
  <action>
Create `.planning/phases/35-foundation/COVERAGE.md`. Its first content line must be the reasoned
declaration exactly in this form, because the seal-time `api-coverage.verify-pre` gate reads it:

    No external API integration: this phase defines adapter interfaces only — no Firebase Admin,
    store, or device-check implementation ships (FOUND-08).

Do not fabricate a capability matrix for an API nothing calls. Below the declaration, add a short
"Accepted gaps carried into v2.0" section recording, each in one or two lines, the items this
phase deliberately leaves open so a later reviewer does not rediscover them as defects:

- The Envoy gateway contract (FOUND-09 / §9) is deferred to v2.1 per D-08. Consequences: only the
  v1.6 chart's rate limiting ships, unverified against §9; Envoy's 429s keep their empty body,
  which does not satisfy the client error contract; `xff_num_trusted_hops` stays unpinned, so the
  client address is trusted rather than proven — which is why only the IP bucket kind is
  recorded. No backend correctness depends on the gateway.
- Backend traffic limiting is removed from the product per D-05, not deferred. Envoy Gateway is
  the sole request-rate enforcement point.
- `k8s/templates/backend-traffic-policy.yaml:53` emits `'{"code":"quota_exceeded"}'` on a 429
  where §3.2 wants `rate_limited`. D-08 forbids touching `k8s/` this phase, so this is a known,
  accepted inconsistency rather than a defect.
- No metrics exporter ships. The bounded rejection counter required by §1.2 and §8.2 exists and
  carries the right labels, but nothing exports it, so the operational alert §1.2 calls for
  cannot fire yet.
- Anti-oracle enforcement is structural only per D-13; timing normalization is deliberately not
  implemented and no test asserts timing parity.
- Chat and quota routes still fail their quota path until Phase 36 rewires them onto the grant
  model per D-15, and `core.access_tiers` is seeded empty so REBIND-05 has nothing to resolve
  against until tiers are configured.
- REBIND-01 was satisfied early by Phase 35's route declarations. REBIND-02, REBIND-03,
  REBIND-05, and REBIND-06 remain Phase 36; REBIND-04 is void.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/35-foundation/COVERAGE.md` succeeds
    - `grep -c "No external API integration" .planning/phases/35-foundation/COVERAGE.md` prints at least `1`
    - `grep -c "FOUND-08" .planning/phases/35-foundation/COVERAGE.md` prints at least `1`
    - The file contains an "Accepted gaps carried into v2.0" section with an entry for each of the seven items listed in the action
    - The file contains no capability matrix and no table of API endpoints
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/35-foundation/COVERAGE.md && grep -q "No external API integration" .planning/phases/35-foundation/COVERAGE.md && grep -q "FOUND-08" .planning/phases/35-foundation/COVERAGE.md</automated>
  </verify>
  <done>The coverage declaration exists in the form the seal-time gate reads, and every accepted v2.0 gap is recorded in one place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase boundary → Phase 36 | The published `auth/` seam and the green suite are the contract Phase 36 builds on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-10-01 | Repudiation | suppressed test markers | high | mitigate | The gate greps for `xfail` and `pytest.mark.skip` and requires a count of zero, so a failing security assertion cannot be parked behind a marker and carried into Phase 36 unnoticed |
| T-35-10-02 | Elevation of Privilege | weakened assertions during restoration | medium | mitigate | The task forbids softening an assertion to make a case pass; a case that cannot hold is deleted and named in the SUMMARY with its owning phase, so the loss of coverage is visible |
| T-35-10-03 | Information disclosure | accepted gaps forgotten | medium | mitigate | `COVERAGE.md` records every deliberately-open item — the deferred gateway contract, the removed backend limiting, the k8s 429 body inconsistency, the missing exporter, and the un-normalized timing — so a later reviewer reads them as decisions rather than rediscovering them as defects |
| T-35-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package; it re-runs the existing editable install only. The legitimacy gate is vacuous for Phase 35 |
</threat_model>

<artifacts_this_phase_produces>
Consolidated list for the whole of Phase 35. The plan-review-convergence source-grounding pass
must treat every name here as newly created by this phase, not as drift against the existing
codebase.

**New source files**
`src/nativespeaker/api/errors.py` ·
`src/nativespeaker/api/models/auth.py` · `src/nativespeaker/api/models/identities.py` ·
`src/nativespeaker/api/auth/__init__.py` · `src/nativespeaker/api/auth/adapters.py` ·
`src/nativespeaker/api/auth/audit.py` · `src/nativespeaker/api/auth/barrier.py` ·
`src/nativespeaker/api/auth/budgets.py` · `src/nativespeaker/api/auth/challenges.py` ·
`src/nativespeaker/api/auth/context.py` · `src/nativespeaker/api/auth/identity.py` ·
`src/nativespeaker/api/auth/keys.py` · `src/nativespeaker/api/auth/modesignal.py` ·
`src/nativespeaker/api/auth/registry.py` · `src/nativespeaker/api/auth/telemetry.py` ·
`src/nativespeaker/api/auth/verification.py` · `src/nativespeaker/api/auth/wire.py`

**New test files**
`tests/unit/test_route_registry.py` · `tests/unit/test_app_wiring.py` ·
`tests/unit/test_error_registry.py` · `tests/unit/test_identity_accessors.py` ·
`tests/unit/test_barrier_wire_contract.py` · `tests/unit/test_budgets.py` ·
`tests/unit/test_adapter_interfaces.py` · `tests/unit/test_hmac_keys.py` ·
`tests/unit/test_audit_details.py` · `tests/unit/test_challenge_ids.py` ·
`tests/unit/test_mode_signal.py` · `tests/e2e/test_startup_assertion.py` ·
`tests/e2e/test_model_queries.py` · `tests/e2e/test_barrier_admission.py` ·
`tests/e2e/test_barrier_wire_contract.py` · `tests/e2e/test_audit_writer.py` ·
`tests/e2e/test_challenge_store.py`

**New non-source artifacts**
`.planning/phases/35-foundation/COVERAGE.md` · the `hmac:` block in `config/config.yaml`

**New symbols, by module**
- `errors`: `ErrorClass`, `ErrorCode`, `ErrorResponse`, `REGISTRY`, `STATUS_TO_CLASS`,
  `register_class`, `error_response`, `assert_registry_total`, `AUTH_REQUIRED`,
  `PREAUTH_IDENTITY_NOT_ALLOWED`, `ACCOUNT_UNAVAILABLE`, `CHALLENGE_REQUIRED`, `INVALID_REQUEST`,
  `VERIFICATION_TEMPORARILY_UNAVAILABLE`, `RATE_LIMITED`, `NOT_FOUND`, `METHOD_NOT_ALLOWED`,
  `VALIDATION_ERROR`, `INTERNAL_ERROR`, `SERVICE_UNAVAILABLE`, `QUOTA_EXCEEDED`, `OUT_OF_SCOPE`
- `models.auth`: `AuthOperation`, `AuthEventResult`, `AuthEvent`, `AuthChallenge`
- `models.identities`: `IdentityProvider`, `IdentityState`, `NativeClaimProvider`,
  `ExternalIdentity`
- `auth.wire`: `BoundedReason`, `extract_bearer`
- `auth.registry`: `Category`, `RouteMetadata`, `REGISTRY`, `lookup`, `enumerate_registered`,
  `assert_route_enumeration`
- `auth.barrier`: `AuthBarrierMiddleware`
- `auth.context`: `REQUEST_CONTEXT_SCOPE_KEY`, `IdentityKind`, `ClientIpBucketKind`,
  `LinkedIdentity`, `PreAuthIdentity`, `RequestContext`
- `auth.identity`: `Admit`, `Reject`, `AdmissionDecision`, `resolve_identity`
- `auth.verification`: `VerifiedClaims`, `TokenVerifier`, `JWTVerifier`
- `auth.telemetry`: `RejectionCounter`, `record_rejection`
- `auth.keys`: `ACTOR_SUBJECT_PREFIX`, `IDP_ACCOUNT_PREFIX`, `HmacConfig`, `HmacKeyring`
- `auth.audit`: `DETAILS_SCHEMA_VERSION`, `build_details`, `redact`, `AuditWriter`
- `auth.challenges`: `CHALLENGE_TTL_SECONDS`, `CHALLENGE_ID_BYTES`, `new_challenge_id`,
  `ChallengeRejection`, `ChallengeStore`
- `auth.modesignal`: `ModeSignal`, `classify_mode_signal`
- `auth.budgets`: `ADAPTER_FIREBASE_LOOKUP`, `FIREBASE_LOOKUP_ATTEMPTS`, `BudgetGate`,
  `BudgetExhausted`
- `auth.adapters`: `ProviderDataOutcome`, `ProviderDataResult`, `RevocationOutcome`,
  `FirebaseAdminAdapter`, `VerifiedNotification`, `VerifiedTransaction`, `StoreState`,
  `StoreAdapter`, `ClaimKind`, `DeviceBitState`, `VendorProofAdapter`
- `app.dependencies`: `get_request_context`, `get_linked_identity`, `get_preauth_identity`
- `config`: `AppConfig.hmac`
- `app.state` keys: `rejection_counter`, `hmac_keyring`, `audit_writer`, `route_registry`,
  `challenge_store`
- `tests.e2e.conftest`: `seed_identity`, `stub_verifier`

**Files deleted by this phase**
`src/nativespeaker/api/exceptions.py` · `src/nativespeaker/api/auth.py` ·
`src/nativespeaker/api/models/subscriptions.py` · `src/nativespeaker/api/routers/users.py` ·
`src/nativespeaker/api/routers/webhooks.py` · `src/nativespeaker/api/services/users.py` ·
`src/nativespeaker/api/services/subscriptions.py` · `src/nativespeaker/api/services/firebase.py` ·
`src/nativespeaker/api/database/users.py` · `src/nativespeaker/api/database/usage.py` ·
`src/nativespeaker/api/database/subscriptions.py` · `tests/unit/test_subscriptions.py` ·
`tests/unit/test_usage.py` · `tests/unit/test_webhooks.py` · `tests/e2e/test_users.py`
</artifacts_this_phase_produces>

<recorded_assumptions>
- **The error registry is deliberately not re-exported from `auth/`.** D-10 places it at package
  root because it owns every client-visible class in the service, not only the auth ones. A
  reviewer expecting `from nativespeaker.api.auth import ErrorResponse` should read D-10.
- **Every accepted v2.0 gap is recorded in `COVERAGE.md`** rather than scattered across plan
  SUMMARYs, so the milestone audit has one place to read them.
</recorded_assumptions>

<verification>
- `.venv/bin/python -m pytest -q -m ""` green, zero xfail, zero skip
- `.venv/bin/ruff check src tests` and `.venv/bin/ty check src` clean
- The real application starts: package imports, lifespan completes, keyring validates, registry
  self-check passes, and the enumeration assertion runs against the real router
- `COVERAGE.md` exists and carries the declaration the seal-time gate reads
</verification>

<success_criteria>
- One stable import root exports the full `auth/` seam
- The whole suite is green with nothing hidden behind a marker
- Every unrestored case is named with its owning phase
- The phase's accepted gaps are recorded in one place
</success_criteria>

<output>
Create `.planning/phases/35-foundation/35-10-SUMMARY.md` when done. Record the final suite
counts, the exported seam size, and every case not restored with the phase that owns it.
</output>
