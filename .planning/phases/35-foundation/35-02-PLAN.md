---
phase: 35-foundation
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/nativespeaker/api/errors.py
  - src/nativespeaker/api/exceptions.py
  - src/nativespeaker/api/app/errors.py
  - src/nativespeaker/api/app/main.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/app/lifespan.py
  - src/nativespeaker/api/models/api.py
  - src/nativespeaker/api/models/__init__.py
  - src/nativespeaker/api/resilience.py
  - src/nativespeaker/api/services/chats.py
  - src/nativespeaker/api/services/subscriptions.py
  - src/nativespeaker/api/routers/webhooks.py
  - src/nativespeaker/api/auth.py
  - src/nativespeaker/api/auth/__init__.py
  - src/nativespeaker/api/auth/verification.py
  - tests/unit/test_error_registry.py
  - tests/unit/test_error_contract.py
  - tests/unit/test_exception_handlers.py
  - tests/unit/test_models.py
  - tests/unit/test_services.py
  - tests/unit/test_jwt_security.py
  - tests/unit/test_auth_security.py
  - tests/unit/test_users.py
  - tests/unit/test_usage.py
  - tests/unit/test_webhooks.py
  - tests/unit/conftest.py
autonomous: true
requirements: [FOUND-01, FOUND-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Exactly one module — src/nativespeaker/api/errors.py — declares every client-visible error class in the service; src/nativespeaker/api/exceptions.py no longer exists"
    - "No two registered error classes share a code, and every class has exactly one status that never varies per branch or per endpoint"
    - "There is no status-folding table and no status→code fallback lookup: every status the service can emit maps to exactly one declared class, and an unmapped status logs error_registry_unmapped_status at ERROR and returns internal_error rather than silently defaulting"
    - "The exception handlers are registered in one place with a fixed specificity order, and within a class the status, body, and copy are identical regardless of which branch raised it"
    - "The 401 code retired by D-11 is absent from the registry, from the ErrorResponse Literal set, and from the OpenAPI responses block; auth_required is the only 401 the service emits"
    - "A framework 409 surfaces as challenge_required at 409, not as a 400 — the collision the deleted remap table caused is gone"
    - "A wrong-method request to a registered path surfaces as method_not_allowed at 405 with the router's Allow header preserved"
    - "No class is declared for 415: python-multipart is not installed and no Form or File parameter exists, so no branch can reach it"
    - "src/nativespeaker/api/auth.py no longer exists; TokenVerifier and JWTVerifier live at src/nativespeaker/api/auth/verification.py and every importer points there"
    - "Token verification pins iss to exactly https://securetoken.google.com/<configured project id> and aud to exactly the configured project id, verifies RS256 only, requires a non-empty sub, and performs no per-request network call"
    - "Every verification failure maps to exactly one BoundedReason and returns the identical auth_required status, body, and copy — the response names no issuer, no integration, and no failed check"
  artifacts:
    - path: "src/nativespeaker/api/errors.py"
      provides: "The complete single error registry: seven foundation classes plus every pre-existing business class, ErrorResponse, ServiceError, the framework-exception mapping, and the totality self-check"
      exports: ["ErrorClass", "ErrorCode", "ErrorResponse", "REGISTRY", "STATUS_TO_CLASS", "error_response", "register_class", "assert_registry_total", "ServiceError"]
    - path: "src/nativespeaker/api/auth/verification.py"
      provides: "§1.2 JWT verification rules layered onto the absorbed JWTVerifier"
      exports: ["VerifiedClaims", "TokenVerifier", "JWTVerifier", "verify_token"]
    - path: "tests/unit/test_error_registry.py"
      provides: "Registry totality, uniqueness, and framework-exception mapping coverage"
  prohibitions:
    - statement: "MUST NOT use accusatory, shaming, or fault-attributing copy in any client-visible error class — account_unavailable, rate_limited, and every acceptance failure state the condition and the remediation without telling the user they did something wrong or implying abuse"
      status: resolved
      verification: judgment
  key_links:
    - from: "src/nativespeaker/api/app/errors.py"
      to: "src/nativespeaker/api/errors.py"
      via: "handlers read HTTP metadata off the registry class instead of a status remap table"
      pattern: "from nativespeaker\\.api\\.errors import"
    - from: "src/nativespeaker/api/app/lifespan.py"
      to: "src/nativespeaker/api/errors.py"
      via: "assert_registry_total() called before yield so a duplicate code or unmapped status aborts boot"
      pattern: "assert_registry_total"
---

<objective>
Complete D-09: make `errors.py` the one and only home for every client-visible error class in
the service, delete `exceptions.py`, delete the status-folding table, retire the duplicate 401
code, and absorb `auth.py` into `auth/verification.py` with the §1.2 verification rules layered
on.

Purpose: the tracer proved the registry seam on one class. Everything downstream — the
admission matrix in plan 04, the audit writer's client-facing side in plan 06, the challenge
rejections in plan 07 — appends to this module. It has to be complete and total before they do.

Output: one registry module, one response model, one handler set, one verification module.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-foundation/35-CONTEXT.md
@.planning/phases/35-foundation/35-RESEARCH.md
@.planning/phases/35-foundation/35-PATTERNS.md
@.planning/phases/35-foundation/35-01-SUMMARY.md

<interfaces>
Final shape of the registry after this plan. Plans 04-07 append classes to `REGISTRY` through
`register_class` and never define a second response model or handler.

src/nativespeaker/api/errors.py
    ErrorCode = Literal[            # the closed set after this plan
        "auth_required", "preauth_identity_not_allowed", "account_unavailable",
        "challenge_required", "invalid_request", "verification_temporarily_unavailable",
        "rate_limited", "validation_error", "not_found", "method_not_allowed",
        "internal_error", "service_unavailable", "quota_exceeded", "out_of_scope",
    ]
    STATUS_TO_CLASS: dict[int, ErrorClass]      # closed; replaces the deleted remap+code tables
    def assert_registry_total() -> None         # lifespan self-check
    class ServiceError(Exception):              # moved verbatim from exceptions.py
        error_class: ErrorClass                 # replaces the status_code/error_code pair
        log_level: int | None
        def extra_headers(self) -> dict[str, str] | None

src/nativespeaker/api/auth/verification.py
    @dataclass(frozen=True, slots=True)
    class VerifiedClaims:
        issuer: str
        subject: str
    class TokenVerifier(Protocol):
        def verify(self, token: str) -> tuple[VerifiedClaims | None, BoundedReason | None]: ...
    class JWTVerifier:  # __init__(*, jwks_url, audience, issuer, leeway, cache_ttl_seconds)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Absorb every business class into the registry and delete exceptions.py</name>
  <reversibility rating="costly">D-09 — this replaces `exceptions.py` and every `raise` site in the v1.6 codebase; undoing it means re-splitting the module and repointing every importer again.</reversibility>
  <files>src/nativespeaker/api/errors.py, src/nativespeaker/api/exceptions.py, src/nativespeaker/api/app/errors.py, src/nativespeaker/api/app/main.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/models/api.py, src/nativespeaker/api/models/__init__.py, src/nativespeaker/api/resilience.py, src/nativespeaker/api/services/chats.py, src/nativespeaker/api/services/subscriptions.py, src/nativespeaker/api/routers/webhooks.py, tests/unit/test_error_contract.py, tests/unit/test_exception_handlers.py, tests/unit/test_models.py, tests/unit/test_services.py, tests/unit/test_users.py, tests/unit/test_usage.py, tests/unit/test_webhooks.py, tests/unit/conftest.py</files>
  <read_first>
    - `/home/init/native-speaker/specs/auth-refactor-phases/01-foundation.md` §3.1, §3.2, §3.3 — the invariants the handler enforces mechanically and the exact classes foundation may register
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` § Errors
    - `.planning/phases/35-foundation/35-RESEARCH.md` § Pattern 4 — the complete inventory of framework exceptions reachable in this deployment, and the 415/405 reasoning
    - `.planning/phases/35-foundation/35-PATTERNS.md` § `src/nativespeaker/api/errors.py`
    - `src/nativespeaker/api/exceptions.py` — all fourteen classes being moved, with their current statuses and codes
    - `src/nativespeaker/api/app/errors.py` — `_STATUS_REMAP`, `_CODE_MAP`, `_LEVEL_TO_METHOD`, the four handlers, and the registration order at lines 77-81
    - `src/nativespeaker/api/models/api.py` — the current `ErrorResponse` and its `ErrorCode` import
    - `src/nativespeaker/api/app/main.py` lines 25-33 — the `responses={}` OpenAPI block
    - `src/nativespeaker/api/errors.py` as written by plan 01 — the seven foundation classes and `register_class`
    - `tests/unit/test_error_contract.py` lines 9-11 — it asserts the `ErrorCode` literal contents and must be updated in this task
  </read_first>
  <action>
Move every remaining client-visible error class out of `exceptions.py` and into `errors.py`,
then delete `exceptions.py`.

In `errors.py`, keep the plan-01 registry mechanism and extend it. Register the classes that
the pre-existing business paths already emit, each keeping its current code and status verbatim
so §8.3's "existing non-auth error contracts unchanged" holds: `invalid_request` 400 (already
registered by plan 01 — reuse it, do not mint a near-duplicate), `not_found` 404,
`validation_error` 422, `internal_error` 500, `service_unavailable` 503, `quota_exceeded` 429,
`out_of_scope` 400. Register one new class per A1: `method_not_allowed` at 405. Register no
class for 415 — `python-multipart` is not installed so a `Form` parameter cannot be declared and
no branch can reach that status; declaring an unreachable class is the same error D-11 corrects.

Retire the 401 code the v1.3 contract used alongside `auth_required` per D-11. Delete it from
the `ErrorCode` `Literal` set, register no class for it, and update the `responses={401: ...}`
description in `app/main.py` to describe the surviving 401 class. Once the barrier owns
acceptance nothing else can produce a 401, so keeping both would leave a code no branch reaches.
Update `tests/unit/test_error_contract.py`, which asserts the literal contents directly.

Move `ServiceError` and its thirteen subclasses into `errors.py`, replacing the
`status_code: int` / `error_code: ErrorCode` class attribute pair with a single
`error_class: ErrorClass` attribute pointing at the registered class. Keep `log_level` and
`extra_headers()` unchanged — `QueueFullError` and `CircuitOpenError` still return
`Retry-After`. Keep `AuthenticationError` as a class for now but point it at
`AUTH_REQUIRED`; plan 03 deletes its last raise site.

Move `ErrorResponse` from `models/api.py` into `errors.py` and delete it from
`models/api.py` and from the `models/__init__.py` `__all__` and imports. `models/api.py` keeps
its request/response schemas; the error body belongs to the registry per D-10. Repoint
`app/main.py`'s `responses={}` block to import `ErrorResponse` from
`nativespeaker.api.errors`.

Delete `_STATUS_REMAP` and `_CODE_MAP` from `app/errors.py` outright per D-12. In their place add a
closed `STATUS_TO_CLASS: dict[int, ErrorClass]` in `errors.py` covering exactly the statuses
the framework can raise in this deployment — 404 → `not_found`, 405 → `method_not_allowed`,
422 → `validation_error`, 500 → `internal_error` — plus the statuses the service itself emits
through `ServiceError`. The remap table is deleted rather than trimmed because one of its
entries is now actively wrong: it folded 409 to 400, and the registry uses 409 for
`challenge_required`, so a framework 409 would have surfaced as `invalid_request`.

Rewrite `app/errors.py`'s handlers against the registry. `service_error_handler` reads
`exc.error_class` and calls `error_response(...)` with `exc.extra_headers()`, keeping the
existing `_LEVEL_TO_METHOD` structured-logging behaviour. `http_exception_handler` looks the
status up in `STATUS_TO_CLASS`; on a hit it returns that class's response and forwards the
exception's `headers` attribute so the router's `Allow` header survives a 405; on a miss it logs
`error_registry_unmapped_status` at ERROR with the unmapped status as a field and returns
`internal_error`. That miss branch is a loud programming-error path, not a silent table
fallback. Keep `validation_error_handler` and `generic_error_handler`, and keep the
`register_exception_handlers(app)` name and its four-handler registration order at lines 77-81
exactly as they are.

Add `assert_registry_total()` to `errors.py`: it asserts every registered class has exactly one
status, no two classes share a code, every value in `STATUS_TO_CLASS` is a registered class, and
the `ErrorCode` literal set equals the set of registered codes. Call it from `app/lifespan.py`
immediately before `assert_route_enumeration(app)` so a registry defect aborts boot.

Repoint every importer of `nativespeaker.api.exceptions` to `nativespeaker.api.errors`:
`resilience.py`, `services/chats.py`, `services/subscriptions.py`, `routers/webhooks.py`,
`app/errors.py`, `models/api.py`, and the test modules `test_models.py`, `test_services.py`,
`test_users.py`, `test_usage.py`, `test_webhooks.py`, `test_exception_handlers.py`,
`conftest.py`. `services/subscriptions.py` and `routers/webhooks.py` are deleted by plan 03;
repoint them here anyway so the tree is importable at every commit. Then delete
`src/nativespeaker/api/exceptions.py`.
  </action>
  <acceptance_criteria>
    - `test ! -e src/nativespeaker/api/exceptions.py` succeeds
    - `.venv/bin/python -c "import nativespeaker.api.exceptions"` exits non-zero with ModuleNotFoundError
    - `.venv/bin/python -c "from nativespeaker.api.errors import REGISTRY; c=[e.code for e in REGISTRY.values()]; assert len(c)==len(set(c)); print(sorted(c))"` prints the 14 codes with no duplicates
    - `.venv/bin/python -c "from nativespeaker.api.errors import REGISTRY; print(sorted({e.status for e in REGISTRY.values()}))"` includes 405 and does not include 415
    - `.venv/bin/python -c "from nativespeaker.api.errors import assert_registry_total; assert_registry_total(); print('total')"` prints `total`
    - `.venv/bin/python -c "from nativespeaker.api.errors import STATUS_TO_CLASS; print(STATUS_TO_CLASS[409].code)"` prints `challenge_required`
    - `.venv/bin/python -c "import nativespeaker.api.app.errors as m; print(hasattr(m,'_STATUS_REMAP'), hasattr(m,'_CODE_MAP'))"` prints `False False`
    - `.venv/bin/python -m pytest -q` exits 0
    - `.venv/bin/ruff check src tests` prints `All checks passed!` and `.venv/bin/ty check src` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "from nativespeaker.api.errors import assert_registry_total; assert_registry_total()" && .venv/bin/python -m pytest -q && .venv/bin/ruff check src tests && .venv/bin/ty check src</automated>
  </verify>
  <done>One module owns every client-visible class, `exceptions.py` is gone, the status-folding table is gone, the duplicate 401 code is retired, and the suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Absorb auth.py into auth/verification.py and layer the §1.2 rules on it</name>
  <reversibility rating="costly">D-23 — phases 36-46 import these module paths by name; moving them again after those phases land means editing every one of them.</reversibility>
  <files>src/nativespeaker/api/auth.py, src/nativespeaker/api/auth/verification.py, src/nativespeaker/api/auth/__init__.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/app/dependencies.py, tests/unit/test_jwt_security.py, tests/unit/test_auth_security.py, tests/unit/conftest.py</files>
  <read_first>
    - `/home/init/native-speaker/specs/auth-refactor-phases/01-foundation.md` §1.2 — every acceptance rule, the bounded reasons, the single-integration rule, and the anti-oracle requirement
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` § Wire contract, § Tokens and sessions
    - `src/nativespeaker/api/auth.py` — the file being moved; copy it, then layer, do not rewrite
    - `src/nativespeaker/api/auth/wire.py` — `BoundedReason`, whose members this module returns
    - `src/nativespeaker/api/config.py` `JWTConfig` — `project_id`, `jwks_url`, `leeway_seconds`, `jwks_cache_ttl_seconds`, and the `issuer` property
    - `src/nativespeaker/api/app/lifespan.py` — the `JWTVerifier` construction and its import
    - `tests/unit/conftest.py` lines 40-123 — the ephemeral RSA keypair, `make_token`, and `_FixedKeyVerifier`; keep this block verbatim, the plan-04 e2e stub verifier reuses it
    - `tests/unit/test_jwt_security.py` — the existing algorithm-confusion and signature coverage; keep every case
    - `.planning/phases/35-foundation/35-PATTERNS.md` § `src/nativespeaker/api/auth/verification.py`
  </read_first>
  <behavior>
    - A token signed with the ephemeral test key, carrying the configured `iss` and `aud`, a future `exp`, and a non-empty `sub`, verifies and yields `VerifiedClaims(issuer, subject)` with no second bounded reason
    - A token whose `iss` is any other value returns `(None, BoundedReason.issuer_mismatch)`
    - A token whose `aud` is any other value returns `(None, BoundedReason.audience_mismatch)`
    - An expired token returns `(None, BoundedReason.expired)`
    - A token with an absent or empty `sub` returns `(None, BoundedReason.empty_subject)`
    - A token signed with a different key, or with `alg` set to `none`, or with HS256 over the public key, returns `(None, BoundedReason.bad_signature)`
    - Every failure branch returns the same two-tuple shape; none raises, none carries issuer or check text
    - Two consecutive verifications of two valid tokens issue zero additional JWKS fetches
  </behavior>
  <action>
Create `src/nativespeaker/api/auth/verification.py` by copying `src/nativespeaker/api/auth.py`
verbatim and then layering §1.2 on top; delete `src/nativespeaker/api/auth.py`.

Replace `UserIdentity` with `VerifiedClaims`, a frozen slotted dataclass carrying exactly
`issuer: str` and `subject: str`. The v1.6 shape carried `email` and `name` read off the token;
§1.2 and SHARED-INVARIANTS both forbid deriving identity or classification from claims, so those
fields do not survive the move. `issuer` and `subject` are exactly the verified `iss` and `sub`
and are never reconstructed from transport metadata.

Change the `TokenVerifier` Protocol and `JWTVerifier.verify` to return
`tuple[VerifiedClaims | None, BoundedReason | None]` instead of raising. Raising is wrong at this
seam: the barrier is a pure-ASGI middleware outside Starlette's `ExceptionMiddleware`, so an
exception raised here would surface as a 500 rather than as `auth_required`. Map each acceptance
failure to exactly one `BoundedReason` member: an `InvalidIssuerError` to `issuer_mismatch`, an
`InvalidAudienceError` to `audience_mismatch`, an `ExpiredSignatureError` or
`ImmatureSignatureError` to `expired`, an absent or empty `sub` to `empty_subject`, and every
remaining verification exception — signature failure, algorithm confusion, malformed compact
form, unknown key id — to `bad_signature`. Catch the specific PyJWT exception types rather than
a bare `Exception` so a new failure mode surfaces loudly instead of being silently labelled a bad
signature.

Keep the existing construction contract intact: `PyJWKClient` with `cache_jwk_set=True` and
`lifespan=cache_ttl_seconds`, the fail-fast `get_signing_keys()` warm-up in `__init__`,
`algorithms=["RS256"]` passed explicitly, and
`options={"require": ["exp", "iat", "aud", "iss", "sub"]}`. Pin `audience` to the configured
`JWTConfig.project_id` and `issuer` to the configured `JWTConfig.issuer` property — exactly one
Firebase integration, selected by issuer match, with no ambient, default, global, or fallback
client anywhere in the module. Do not add `checkRevoked` or any per-request revocation check;
SHARED-INVARIANTS forbids one in every phase.

Add `auth/verification.py` exports to the `auth/__init__.py` barrel (`JWTVerifier`,
`TokenVerifier`, `VerifiedClaims`), keeping the `__all__`-first alphabetized shape. Repoint
`app/lifespan.py` and `app/dependencies.py`, which currently import from
`nativespeaker.api.auth`, at the new module path. Repoint `tests/unit/test_jwt_security.py`,
`tests/unit/test_auth_security.py`, and `tests/unit/conftest.py` and update their assertions
from "raises" to "returns a bounded reason" — keep every existing algorithm-confusion and
signature case, they are the standing mitigation for that threat.

Note for later plans: only plans 02, 03, and 04 may edit `auth/__init__.py`. Plans 05 and 06 run
in the same wave and must import by full module path; plan 08 writes the final barrel.
  </action>
  <acceptance_criteria>
    - `test ! -e src/nativespeaker/api/auth.py` succeeds and `test -f src/nativespeaker/api/auth/verification.py` succeeds
    - `.venv/bin/python -m pytest -q tests/unit/test_jwt_security.py tests/unit/test_auth_security.py` exits 0
    - `.venv/bin/python -c "from nativespeaker.api.auth.verification import VerifiedClaims; print(sorted(VerifiedClaims.__dataclass_fields__))"` prints `['issuer', 'subject']`
    - `.venv/bin/python -c "import inspect; from nativespeaker.api.auth.verification import JWTVerifier as V; print('RS256' in inspect.getsource(V))"` prints `True`
    - `.venv/bin/python -m pytest -q` exits 0 across the whole unit suite
    - `.venv/bin/ruff check src tests` prints `All checks passed!` and `.venv/bin/ty check src` reports no errors
    - `.venv/bin/python -m pytest -q -m e2e tests/e2e/test_startup_assertion.py` still exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/python -m pytest -q -m e2e tests/e2e/test_startup_assertion.py && .venv/bin/ruff check src tests && .venv/bin/ty check src</automated>
  </verify>
  <done>`auth.py` is gone, `auth/verification.py` implements every §1.2 rule and returns bounded reasons instead of raising, and every importer and test points at the new path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove registry totality and framework-exception mapping</name>
  <files>tests/unit/test_error_registry.py</files>
  <read_first>
    - `/home/init/native-speaker/specs/auth-refactor-phases/01-foundation.md` §3.1 and §3.2 — the four mechanically-enforced invariants and the pinned statuses
    - `src/nativespeaker/api/errors.py` — the module written in task 1
    - `src/nativespeaker/api/app/errors.py` — the rewritten handlers
    - `tests/unit/test_error_contract.py` and `tests/unit/test_exception_handlers.py` — existing coverage this module extends rather than duplicates
    - `.planning/phases/35-foundation/35-VALIDATION.md` § Per-Task Verification Map, FOUND-04 row
  </read_first>
  <behavior>
    - Every registered class has exactly one status and no two share a code
    - The `ErrorCode` literal set equals the set of registered codes exactly
    - The retired 401 code is absent from the registry, from the literal set, and from the app's OpenAPI `responses` block
    - `STATUS_TO_CLASS` maps 404, 405, 422, and 500 each to exactly one registered class
    - `STATUS_TO_CLASS[409]` is `challenge_required`, not `invalid_request`
    - No registered class carries status 415
    - `assert_registry_total()` raises when a duplicate code is registered, and when a status maps to an unregistered class
    - `http_exception_handler` on an unmapped status returns 500 `internal_error` and emits an ERROR-level log event named for the unmapped status
    - `http_exception_handler` on a 405 preserves the exception's `Allow` header in the response
    - Two different `ServiceError` subclasses that share a class produce byte-identical bodies and identical statuses
  </behavior>
  <action>
Write `tests/unit/test_error_registry.py` as a pure unit module covering the behaviours above.
Drive the handler cases by calling `http_exception_handler` and `service_error_handler`
directly with constructed exceptions rather than through a client, so no app startup is needed.
Assert the retired 401 code is absent by checking `REGISTRY`, the `ErrorCode` literal via
`typing.get_args`, and `app.openapi_schema`-free introspection of the `responses` mapping the
app was constructed with. Use `structlog`'s capturing test helper or `caplog` to assert the
unmapped-status branch logs at ERROR. Do not duplicate what `test_error_contract.py` and
`test_exception_handlers.py` already assert; extend them instead where they overlap.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q tests/unit/test_error_registry.py` exits 0 with at least one test per behaviour listed above
    - `.venv/bin/python -m pytest -q` exits 0
    - The module contains a test that fails if a second class is registered with an existing code
    - The module contains a test asserting `STATUS_TO_CLASS[409].code == "challenge_required"`
    - `.venv/bin/ruff check src tests` prints `All checks passed!`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_error_registry.py && .venv/bin/python -m pytest -q && .venv/bin/ruff check src tests</automated>
  </verify>
  <done>Registry totality, code uniqueness, the 409 fix, the retired 401 code, and the loud unmapped-status path all have failing-if-broken coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API error surface | Response bodies and statuses are the only channel an attacker can observe to distinguish internal conditions |
| client → Envoy → API token verification | Untrusted compact JWTs, arbitrary `alg`, arbitrary `iss`/`aud` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-02-01 | Spoofing | `auth/verification.py` `JWTVerifier.verify` | high | mitigate | `algorithms=["RS256"]` passed explicitly and `iss`/`aud` pinned to the one configured integration, so `alg: none` and HS256-over-public-key both land on `bad_signature`; the existing `test_jwt_security.py` cases are retained as the standing regression |
| T-35-02-02 | Spoofing | `auth/verification.py` integration selection | high | mitigate | Issuer equality against the single configured `JWTConfig.issuer`; no ambient, default, global, or fallback client is constructible, and a mismatch rejects before any Admin work |
| T-35-02-03 | Information disclosure | `errors.py` class bodies | medium | mitigate | One class per condition with identical status, body, and copy across every triggering branch; no free-text `message`/`detail`/`reason` field exists to leak an issuer, integration, or failed check |
| T-35-02-04 | Information disclosure | `app/errors.py` `http_exception_handler` | medium | mitigate | The deleted remap table's 409→400 entry would have surfaced `challenge_required` as `invalid_request`, letting a client distinguish a challenge condition from a shape error; `STATUS_TO_CLASS` gives each status its own honest class |
| T-35-02-05 | Repudiation | `app/errors.py` unmapped-status branch | medium | mitigate | Logs `error_registry_unmapped_status` at ERROR with the status as a field rather than silently defaulting, so a registry hole is visible in operations |
| T-35-02-06 | Information disclosure | timing differences between `account_unavailable` branches | low | accept | D-13 scopes anti-oracle enforcement to structural identity and explicitly rejects timing normalization; a timing oracle distinguishing retired from blocked buys nothing against a sub-$5/month subscription. Documented, not omitted |
| T-35-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package; the legitimacy gate is vacuous for Phase 35 per `35-RESEARCH.md` § Package Legitimacy Audit |
</threat_model>

<artifacts_this_phase_produces>
Symbols created or relocated by **this plan**:

**New files** — `src/nativespeaker/api/auth/verification.py`,
`tests/unit/test_error_registry.py`

**Deleted files** — `src/nativespeaker/api/exceptions.py`, `src/nativespeaker/api/auth.py`

**New symbols**
- `errors.STATUS_TO_CLASS`, `errors.assert_registry_total`
- `errors.NOT_FOUND`, `errors.METHOD_NOT_ALLOWED`, `errors.VALIDATION_ERROR`,
  `errors.INTERNAL_ERROR`, `errors.SERVICE_UNAVAILABLE`, `errors.QUOTA_EXCEEDED`,
  `errors.OUT_OF_SCOPE`
- `auth.verification.VerifiedClaims`, `auth.verification.TokenVerifier`,
  `auth.verification.JWTVerifier`

**Relocated symbols** (same names, new module — `nativespeaker.api.errors`)
`ServiceError`, `UnsupportedLanguageError`, `AnalysisError`, `TransientLLMError`,
`PermanentLLMError`, `InvalidChatError`, `InvalidCursorError`, `PageSizeLimitError`,
`QueueFullError`, `CircuitOpenError`, `QuotaExceededError`, `ChatHistoryLimitError`,
`OutOfScopeError`, `AuthenticationError`, `WebhookVerificationError`,
`DatabaseNotInitializedError`, `ErrorCode`, `ErrorResponse`

**Removed symbols** — `app.errors._STATUS_REMAP`, `app.errors._CODE_MAP`, the retired 401
`ErrorCode` member, `models.api.ErrorResponse`, `auth.UserIdentity`
</artifacts_this_phase_produces>

<recorded_assumptions>
- **A1 (RESEARCH.md)** — 405 gets its own `method_not_allowed` class at its own status. Low
  risk, one line to change if the developer prefers folding it.
- **A2 (RESEARCH.md)** — no 415 class, because it is unreachable without `python-multipart` or
  a `Form`/`File` parameter. Self-correcting: adding either would make the registry self-check
  fail loudly rather than mis-map silently.
- **D-11 is rated `one-way` and no `checkpoint:decision` is inserted.** The rating is carried on
  task 1 for the record. `35-CONTEXT.md` D-11 records the developer choosing exactly this
  option, with the rationale that it is cheap pre-launch and would not be later; re-asking it is
  the "gating a decision already made" anti-pattern in `planner-reversibility.md`. If the
  orchestrator wants the gate anyway, insert a `checkpoint:decision` before task 1 and set
  `autonomous: false`.
- **Edge probe FOUND-04 rows** are authored into `must_haves.truths` above as explicit
  criteria; none was dismissed.
</recorded_assumptions>

<verification>
- `.venv/bin/python -m pytest -q -m ""` — the full suite is green with no xfail
- `.venv/bin/ruff check src tests` and `.venv/bin/ty check src` both clean
- The app still boots: `assert_registry_total()` and `assert_route_enumeration(app)` both run in
  the lifespan and both pass
</verification>

<success_criteria>
- Exactly one error module exists in the service; `exceptions.py` and `auth.py` are gone
- No duplicate codes, no status-folding table, no unreachable class, no retired code
- Every §1.2 acceptance rule is implemented and returns a bounded reason rather than raising
</success_criteria>

<output>
Create `.planning/phases/35-foundation/35-02-SUMMARY.md` when done. Record the final `ErrorCode`
set, the `STATUS_TO_CLASS` contents, and confirmation that no class carries 415.
</output>
