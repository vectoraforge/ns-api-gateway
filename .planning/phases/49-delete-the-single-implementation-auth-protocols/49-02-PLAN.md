---
phase: 49-delete-the-single-implementation-auth-protocols
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - src/nativespeaker/api/auth/devicecheck.py
  - src/nativespeaker/api/auth/firebase.py
  - src/nativespeaker/api/auth/adapters.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/services/auth.py
  - tests/unit/test_devicecheck_adapter.py
  - tests/unit/test_claim_ordering.py
  - tests/unit/test_adapter_interfaces.py
  - tests/unit/test_firebase_adapter.py
  - tests/unit/test_firebase_retry.py
  - tests/unit/test_upgrade_precedence.py
  - tests/unit/test_create_user_precedence.py
  - tests/unit/conftest.py
  - tests/unit/test_auth_package_shape.py
  - tests/e2e/test_upgrade_anonymous.py
  - tests/e2e/test_create_user.py
  - tests/e2e/test_flows.py
  - tests/schema/test_create_atomicity.py
  - tests/schema/test_create_race.py
autonomous: true
requirements: []

estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No name of the DeviceCheck Protocol or the Firebase admin Protocol is left in src/ or tests/."
    - "auth/adapters.py does not exist, and VerifiedProviderIdentity is imported from auth/firebase.py everywhere."
    - "The package-wide guard that no auth module but firebase pulls the provider SDK still runs, from its new home."
    - "The frozen and slotted properties of VerifiedProviderIdentity are still asserted, from their new home."
    - "D-08: each of the two commits writes the re-measured CURRENT tuple into tests/unit/test_auth_package_shape.py in that same commit."
  artifacts:
    - src/nativespeaker/api/auth/devicecheck.py
    - src/nativespeaker/api/auth/firebase.py
    - src/nativespeaker/api/app/dependencies.py
    - tests/unit/test_firebase_adapter.py
    - tests/unit/test_auth_package_shape.py
  key_links:
    - "auth/firebase.py declares VerifiedProviderIdentity; 11 other modules import it from there."
    - "app/dependencies.py returns the concrete Firebase and DeviceCheck classes from its two accessors."
    - "tests/unit/test_firebase_adapter.py holds the relocated SDK-isolation guard and the relocated value-type guard."
---

<objective>
Delete the DeviceCheck Protocol and the Firebase admin Protocol, one per commit. Move
`VerifiedProviderIdentity` into `auth/firebase.py` and remove `auth/adapters.py`.

Purpose: finish the Protocol half of the phase and settle the fate of the test file built on the
removed module.
Output: two commits, each with its own re-measured package-shape tuple.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

**You are NOT in a git worktree.** This repository is a git submodule, so its `.git` is a file and
GSD worktree detection reports `IS_WORKTREE=true` wrongly. Treat it as false. Run every git command
from `/home/init/native-speaker/ns-api-gateway`. Do not push. Do not touch the parent repository.

**Test baseline (Phase 48 gate).** `.venv/bin/pytest -q -m ''` reads `1 failed, 2572 passed`;
`-m e2e` reads `1 failed, 360 passed`; `-m schema` reads `297 passed`; `.venv/bin/ruff check src tests`
prints `All checks passed!`; `.venv/bin/ty check` reads `Found 306 diagnostics`. The one failing case
is the pre-existing restore four-arms case. It is not this phase's. Do not write a fix for it.
"Green" means no failed case other than that one. This plan deletes tests, so the passed count falls.

**Suite markers.** `addopts` carries `-m 'not e2e and not schema'`. The e2e and schema suites are
silently deselected unless `-m` is given, and they use the live localhost PostgreSQL. Task 2 edits
e2e and schema files, so it runs all three suites.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-CONTEXT.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-RESEARCH.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-PATTERNS.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-01-SUMMARY.md
@AGENTS.md
</context>

<!-- planner-discipline-allow: DeviceCheckAdapter -->
<!-- planner-discipline-allow: FirebaseAdminAdapter -->

<tasks>

<task type="auto">
  <name>Task 1: Delete the DeviceCheck Protocol</name>
  <files>src/nativespeaker/api/auth/devicecheck.py, src/nativespeaker/api/app/dependencies.py, tests/unit/test_devicecheck_adapter.py, tests/unit/test_claim_ordering.py, tests/unit/test_auth_package_shape.py</files>
  <read_first>
    - `git show 37a5ac6` — the seam recipe, if it is not already in context
    - .planning/phases/49-delete-the-single-implementation-auth-protocols/49-01-SUMMARY.md (the two measured triples from wave 1)
    - src/nativespeaker/api/auth/devicecheck.py (the `class DeviceCheckAdapter(Protocol)` block at :46-57, `class AppleDeviceCheck` at :128, the two retry helpers at :182 and :187)
    - src/nativespeaker/api/app/dependencies.py (the import at :10, `get_devicecheck_adapter` return type at :123, the `get_auth_service` parameter at :131)
    - tests/unit/test_devicecheck_adapter.py (the import at :25, `class TestTheSeamIsTheAnnotation` at :433-444)
    - tests/unit/test_claim_ordering.py (the `SEAM_NAMES` frozenset at :36-39)
  </read_first>
  <action>
    In `src/nativespeaker/api/auth/devicecheck.py`: delete the whole `class DeviceCheckAdapter(Protocol)`
    block with its class docstring and both method docstrings. Change the `adapter:` annotation on
    `read_bits_with_retry` and on `write_bits_with_retry` to `AppleDeviceCheck`. `AppleDeviceCheck` is
    declared lower in the same module, so the annotations need no import; keep them as string-free
    annotations only if the module already defers evaluation — otherwise move nothing and let the
    helpers stay below the class. Drop `Protocol` from the `typing` import only when nothing else in
    the module uses it.

    In `src/nativespeaker/api/app/dependencies.py`: change the import at :10 to `AppleDeviceCheck`,
    change `get_devicecheck_adapter`'s return annotation to `AppleDeviceCheck`, and change the
    `devicecheck:` parameter of `get_auth_service` to `AppleDeviceCheck`. Leave
    `get_challenge_store`, `get_firebase_adapter` and the `challenge_store` parameter alone — plan
    49-03 owns them.

    In `tests/unit/test_devicecheck_adapter.py`: delete the whole class `TestTheSeamIsTheAnnotation`
    with its docstrings, and drop the deleted Protocol name from the import at :25. ROADMAP criterion
    3 deletes this class rather than rewriting it against the concrete class. Record in the summary
    that a guard recorded under WR-23 is retired, and that its member-set control goes with it.

    In `tests/unit/test_claim_ordering.py`: drop the deleted Protocol name from the `SEAM_NAMES`
    frozenset at :36-39. Keep `"AppleDeviceCheck"` and every other member.

    D-08: run the shape case, RE-MEASURE the triple from its output, and write it into `CURRENT`.
    The projection is `(8, 21, 62)` — an expectation only.

    Commit this task alone.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_auth_package_shape.py</automated>
    <fails_when>non-zero exit, or "failed" in the summary line</fails_when>
    <automated>test -z "$(grep -rn 'DeviceCheckAdapter' src tests)"</automated>
    <fails_when>non-zero exit — grep printed at least one remaining line</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_devicecheck_adapter.py tests/unit/test_claim_ordering.py tests/unit/test_app_wiring.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>non-zero exit, or any output other than "All checks passed!"</fails_when>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'DeviceCheckAdapter' src tests` prints nothing.
    - `grep -c 'AppleDeviceCheck' tests/unit/test_claim_ordering.py` prints at least `1`.
    - `inspect.get_annotations(read_bits_with_retry, eval_str=True)["adapter"]` resolves to `AppleDeviceCheck`; the same holds for `write_bits_with_retry`.
    - `get_devicecheck_adapter` in `app/dependencies.py` returns `AppleDeviceCheck`.
    - `grep -c 'challenge_store' src/nativespeaker/api/app/dependencies.py` is unchanged from before this task.
    - `tests/unit/test_auth_package_shape.py` `CURRENT` holds the triple the failing run printed.
  </acceptance_criteria>
  <done>The DeviceCheck Protocol does not exist, both retry helpers and both dependency sites name the concrete class, the retired guard is recorded, and the unit suite carries only the pre-existing failure.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the Firebase admin Protocol, move the value type, remove the adapter module</name>
  <files>src/nativespeaker/api/auth/adapters.py, src/nativespeaker/api/auth/firebase.py, src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/services/auth.py, tests/unit/test_adapter_interfaces.py, tests/unit/test_firebase_adapter.py, tests/unit/test_firebase_retry.py, tests/unit/test_upgrade_precedence.py, tests/unit/test_create_user_precedence.py, tests/unit/conftest.py, tests/e2e/test_upgrade_anonymous.py, tests/e2e/test_create_user.py, tests/e2e/test_flows.py, tests/schema/test_create_atomicity.py, tests/schema/test_create_race.py, tests/unit/test_auth_package_shape.py</files>
  <read_first>
    - src/nativespeaker/api/auth/adapters.py (the whole file — both classes and every comment)
    - src/nativespeaker/api/auth/firebase.py (the import at :14, `class FirebaseAdminLookup` at :62, `lookup_with_retry` at :178, `revoke_with_retry` at :198)
    - src/nativespeaker/api/app/dependencies.py (the import at :9, `get_firebase_adapter` at :117-120 with its two comments)
    - tests/unit/test_adapter_interfaces.py (the whole file — module-level `SOURCE_PATH`, `SOURCE`, `TREE`, `SDK_FREE_MODULES`, and all four test classes)
    - tests/unit/test_firebase_adapter.py (the import at :8, `class TestTheDeliberateNonImplementations` at :592-595)
    - .planning/phases/49-delete-the-single-implementation-auth-protocols/49-RESEARCH.md § Pitfall 1 and § "13 import lines across 13 files"
    - .planning/phases/49-delete-the-single-implementation-auth-protocols/49-PATTERNS.md § "Moving VerifiedProviderIdentity"
  </read_first>
  <precondition>PostgreSQL answers on 127.0.0.1:5432. This task edits e2e and schema test files and runs `-m e2e` and `-m schema`, which use that live database and are deselected without `-m`.</precondition>
  <reversibility rating="reversible">A module deletion and an import repoint inside one package; no migration, no published contract, and `git revert` restores every line.</reversibility>
  <action>
    Move the value type first. Copy the `@dataclass(frozen=True, slots=True) class VerifiedProviderIdentity`
    block out of `auth/adapters.py` into `src/nativespeaker/api/auth/firebase.py` verbatim, with its
    class docstring and both field comments. Place it above `class FirebaseAdminLookup`. Add
    `from dataclasses import dataclass` to `firebase.py`; `IdentityProvider` is already imported
    there. Delete the import at `firebase.py:14`.

    Then delete `src/nativespeaker/api/auth/adapters.py` with `git rm`. Its module docstring goes with
    it; move no docstring onto any concrete class.

    Repoint the annotations. In `firebase.py`, change the `adapter:` parameter of `lookup_with_retry`
    and of `revoke_with_retry` to `FirebaseAdminLookup`. In `app/dependencies.py`, change the import
    at :9 and the return annotation of `get_firebase_adapter` to `FirebaseAdminLookup`, and delete
    the one-line comment inside that function whose subject is the removed Protocol (48 D-10). Leave
    the two-accessor comment above `get_challenge_store` alone — plan 49-03 owns it.

    Repoint every remaining importer of the value type at `nativespeaker.api.auth.firebase`. The
    list is exactly: `src/nativespeaker/api/services/auth.py:10`, `tests/unit/conftest.py:20`,
    `tests/unit/test_firebase_retry.py:7`, `tests/unit/test_upgrade_precedence.py:18`,
    `tests/unit/test_firebase_adapter.py:8`, `tests/unit/test_create_user_precedence.py:16`,
    `tests/e2e/test_upgrade_anonymous.py:10`, `tests/e2e/test_create_user.py:9`,
    `tests/e2e/test_flows.py:7`, `tests/schema/test_create_atomicity.py:12`,
    `tests/schema/test_create_race.py:13`. Merge the name into an existing `auth.firebase` import
    line where the file already has one, so ruff's `I` rules stay satisfied.

    In `tests/unit/test_firebase_adapter.py`: delete the whole class
    `TestTheDeliberateNonImplementations` with its docstrings — criterion 3 deletes the inheritance
    assertion, and the class holds nothing else.

    Now settle `tests/unit/test_adapter_interfaces.py`. **Decision (Claude's discretion, Research
    Open Question 2): delete the file and relocate its two surviving properties into
    `tests/unit/test_firebase_adapter.py`.** The file reads its module source at import time, so it
    cannot collect once the module is gone, and three of its four classes describe only that module.
    `tests/unit/test_firebase_adapter.py` is the new home because `firebase` is the one auth module
    that may hold the provider SDK, and it is now also the module that declares the value type.

    Relocate, without weakening either property:
    - `TestNoProviderDependency` — carry the class, its `_run` subprocess helper, the `AUTH_PACKAGE`
      path, the `SDK_FREE_MODULES` tuple, the parametrized case, the "set is every auth module but
      the one excluded by design" control and the "excluded module really holds the SDK" control.
      Derive `AUTH_PACKAGE` from the `firebase` module's own file, not from the removed module.
      Rewrite the control line `assert "adapters" in SDK_FREE_MODULES` as
      `assert "jwt_verifier" in SDK_FREE_MODULES`, naming a module that still exists. Keep
      `assert len(SDK_FREE_MODULES) > 1`.
    - `TestTheValueTypeIsImmutable` — carry the `FROZEN` tuple, the frozen-and-slotted case, the
      reassignment case, the no-instance-dict case and the email-default case, with their
      `ty: ignore` comments unchanged.

    Do NOT carry these, and record each as a deliberate loss in the summary: the whole class
    `TestTheOutcomeVocabularyLeftTheSeam`, the case
    `test_every_public_class_is_a_protocol_or_a_frozen_dataclass`, and the two source-AST cases
    `test_the_source_names_firebase_admin_nowhere_as_an_import` and
    `test_the_source_imports_only_the_stdlib_and_this_project`. Each asserts a property of the
    deleted module's own source and has no subject after the deletion. The parametrized subprocess
    case already covers "no sibling auth module imports the SDK" for every module that survives.

    Then `git rm tests/unit/test_adapter_interfaces.py`.

    D-08: run the shape case, RE-MEASURE the triple from its output, and write it into `CURRENT`.
    The projection is `(7, 20, 60)` — one module fewer, and the value type re-landing in
    `firebase.py` — an expectation only.

    Commit this task alone. This is the widest commit of the phase.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_auth_package_shape.py</automated>
    <fails_when>non-zero exit, or "failed" in the summary line</fails_when>
    <automated>test -z "$(grep -rn 'FirebaseAdminAdapter' src tests)"</automated>
    <fails_when>non-zero exit — grep printed at least one remaining line</fails_when>
    <automated>test ! -e src/nativespeaker/api/auth/adapters.py</automated>
    <fails_when>non-zero exit — the module file is still on disk</fails_when>
    <automated>test -z "$(grep -rn 'nativespeaker.api.auth.adapters' src tests)"</automated>
    <fails_when>non-zero exit — an import of the removed module survives</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_firebase_adapter.py tests/unit/test_firebase_retry.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>non-zero exit, or any output other than "All checks passed!"</fails_when>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
    <automated>.venv/bin/pytest -q -m schema</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'FirebaseAdminAdapter' src tests` prints nothing.
    - `src/nativespeaker/api/auth/adapters.py` and `tests/unit/test_adapter_interfaces.py` are both absent, and `git log -1 --stat` shows both as deletions.
    - `grep -c 'class VerifiedProviderIdentity' src/nativespeaker/api/auth/firebase.py` prints `1`.
    - `grep -rln 'VerifiedProviderIdentity' src tests` lists 12 files, and every import of the name resolves to `nativespeaker.api.auth.firebase`.
    - `grep -c 'class TestNoProviderDependency' tests/unit/test_firebase_adapter.py` prints `1`, and `grep -c 'class TestTheValueTypeIsImmutable' tests/unit/test_firebase_adapter.py` prints `1`.
    - `grep -c 'jwt_verifier' tests/unit/test_firebase_adapter.py` prints at least `1` — the rewritten membership control.
    - `.venv/bin/pytest -q tests/unit/test_firebase_adapter.py` collects the relocated parametrized case once per surviving SDK-free auth module.
    - `tests/unit/test_auth_package_shape.py` `CURRENT` holds the triple the failing run printed.
  </acceptance_criteria>
  <done>The Firebase admin Protocol and its module do not exist, the value type is declared in and imported from `auth/firebase.py`, both relocated guards run from `tests/unit/test_firebase_adapter.py`, and all three suites carry only the pre-existing failure.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no new production symbol. It relocates existing ones:

- `VerifiedProviderIdentity` — the same frozen, slotted dataclass, now declared in
  `src/nativespeaker/api/auth/firebase.py` instead of the removed module. Fields `provider`,
  `provider_uid`, `email` are unchanged.
- `TestNoProviderDependency` — the same class, now in `tests/unit/test_firebase_adapter.py`, with
  module-level names `AUTH_PACKAGE`, `SDK_FREE_MODULES` and the `_run` helper carried with it.
- `TestTheValueTypeIsImmutable` — the same class, now in `tests/unit/test_firebase_adapter.py`,
  with the module-level `FROZEN` tuple carried with it.

`FirebaseAdminLookup`, `AppleDeviceCheck`, `read_bits_with_retry`, `write_bits_with_retry`,
`lookup_with_retry` and `revoke_with_retry` already exist and keep their names and signatures.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firebase Admin SDK → the auth service | An externally-owned identity record crosses here as `VerifiedProviderIdentity` |
| Apple DeviceCheck → the claim path | An externally-held bit pair crosses here and gates a free grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-04 | Tampering | `VerifiedProviderIdentity` after the move | high | mitigate | The relocated `TestTheValueTypeIsImmutable` must still assert frozen, slotted, no instance dict and the absent-email default. A mutable identity would let a caller rewrite a verified uid after the check |
| T-49-05 | Elevation of Privilege | the `auth/` package's SDK isolation | high | mitigate | The relocated `TestNoProviderDependency` must still walk every surviving auth module in a subprocess. Its membership control names `jwt_verifier`, a module that exists, so the case cannot go vacuous |
| T-49-06 | Spoofing | `lookup_with_retry` and `read_bits_with_retry` | medium | mitigate | Only annotations change. `tests/unit/test_firebase_retry.py` and `tests/unit/test_devicecheck_adapter.py` must stay green; they hold the retry budget and the rejection mapping |
| T-49-07 | Repudiation | the retired WR-23 guard | low | accept | ROADMAP criterion 3 retires it deliberately, and the summary records the loss |

No package manager runs in this plan, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
Run after both commits, from the repository root:

- `.venv/bin/pytest -q -m ''`, `.venv/bin/pytest -q -m e2e` — no failed case but the restore four-arms case
- `.venv/bin/pytest -q -m schema` — exit 0
- `.venv/bin/ruff check src tests` — `All checks passed!`
- `test -z "$(grep -rn 'DeviceCheckAdapter\|FirebaseAdminAdapter' src tests)"`
- `test ! -e src/nativespeaker/api/auth/adapters.py`
</verification>

<success_criteria>
Two commits exist, one per seam. All four Protocol names of this phase are now absent from `src/`
and `tests/`. `auth/adapters.py` is gone and the value type is imported from `auth/firebase.py`. The
SDK-isolation guard and the value-type guard both still run. Each commit carries its own re-measured
`CURRENT` tuple.
</success_criteria>

<output>
Create `.planning/phases/49-delete-the-single-implementation-auth-protocols/49-02-SUMMARY.md` when done.
Record the two measured `CURRENT` triples, the retired WR-23 guard, and every case dropped with
`tests/unit/test_adapter_interfaces.py`.
</output>
