---
phase: 49-delete-the-single-implementation-auth-protocols
plan: 03
type: execute
wave: 3
depends_on: ["49-02"]
files_modified:
  - src/nativespeaker/api/app/lifespan.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/services/auth.py
  - src/nativespeaker/api/routers/auth.py
  - tests/unit/conftest.py
  - tests/unit/test_create_user_precedence.py
  - tests/unit/test_upgrade_precedence.py
  - tests/unit/test_claim_precedence.py
  - tests/unit/test_claim_precedence_registered.py
  - tests/unit/test_challenge_endpoint.py
  - tests/unit/test_create_user_body.py
  - tests/unit/test_create_user_rollback.py
  - tests/unit/test_conflict_classification.py
  - tests/e2e/test_challenge_store.py
  - tests/schema/test_create_race.py
  - tests/schema/test_create_atomicity.py
  - tests/schema/test_claim_race.py
autonomous: true
requirements: []

estimate:
  tokens: 80000
  raw_tokens: 53000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-02: AuthService holds the crud object as self.challenges_db and builds it itself; the word store names no class in new code."
    - "D-03: the challenge handler builds its own crud object inline, as it already builds the identities one."
    - "D-04: one fixture in tests/unit/conftest.py serves the four precedence suites, by monkeypatching the crud class."
    - "D-05: the challenge-endpoint suite and the create-user-body suite each keep their own recorder and monkeypatch the issue method."
    - "D-06: AuthService.__init__ and get_auth_service annotate the provider adapter and the device gate with their concrete classes."
    - "The lifespan sets no challenge attribute on app.state, and the request-scoped accessor for it does not exist."
    - "The four rejection precedence orders are unchanged."
  artifacts:
    - src/nativespeaker/api/app/dependencies.py
    - src/nativespeaker/api/app/lifespan.py
    - src/nativespeaker/api/services/auth.py
    - src/nativespeaker/api/routers/auth.py
    - tests/unit/conftest.py
  key_links:
    - "One monkeypatch on the crud class reaches both binders: services/auth.py imports it from the package, routers/auth.py from the module, and both bind the same class object."
    - "AuthService builds its own crud object, so no route dependency supplies one."
---

<objective>
Take the challenge crud object out of the lifespan and out of the request-scoped dependencies.
`AuthService` and the challenge handler each build their own, and the six suites that replaced the
dependency monkeypatch the class instead.

Purpose: the runtime container in Phase 50 must not carry an object that a crud caller can build.
Output: one commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

**You are NOT in a git worktree.** This repository is a git submodule, so its `.git` is a file and
GSD worktree detection reports `IS_WORKTREE=true` wrongly. Treat it as false. Run every git command
from `/home/init/native-speaker/ns-api-gateway`. Do not push. Do not touch the parent repository.

**Test baseline (Phase 48 gate).** `.venv/bin/pytest -q -m ''` reads `1 failed, 2572 passed`;
`-m e2e` reads `1 failed, 360 passed`; `-m schema` reads `297 passed`; `.venv/bin/ruff check src tests`
prints `All checks passed!`; `.venv/bin/ty check` reads `Found 306 diagnostics`. The one failing case
is the pre-existing restore four-arms case. It is not this phase's. Do not write a fix for it.
"Green" means no failed case other than that one.

**Suite markers.** `addopts` carries `-m 'not e2e and not schema'`. The e2e and schema suites are
silently deselected unless `-m` is given, and they use the live localhost PostgreSQL.

**This plan does not change the `auth/` package**, so `tests/unit/test_auth_package_shape.py` is not
edited here and D-08 does not apply to this commit.

**Planner note, recorded as a visible choice:** Research left the split of this work to discretion.
It is two commits — this plan removes the dependency, and plan 49-04 gives the crud class its
constructor. The constructor cannot come first: the lifespan has no session at boot, so the object
must leave the lifespan before it can take one.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-CONTEXT.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-RESEARCH.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-PATTERNS.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-02-SUMMARY.md
@AGENTS.md
</context>

<!-- planner-discipline-allow: challenge_store -->

<tasks>

<task type="auto">
  <name>Task 1: The challenge dependency goes; every caller builds its own crud object</name>
  <files>src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/services/auth.py, src/nativespeaker/api/routers/auth.py, tests/unit/conftest.py, tests/unit/test_create_user_precedence.py, tests/unit/test_upgrade_precedence.py, tests/unit/test_claim_precedence.py, tests/unit/test_claim_precedence_registered.py, tests/unit/test_challenge_endpoint.py, tests/unit/test_create_user_body.py, tests/unit/test_create_user_rollback.py, tests/unit/test_conflict_classification.py, tests/e2e/test_challenge_store.py, tests/schema/test_create_race.py, tests/schema/test_create_atomicity.py, tests/schema/test_claim_race.py</files>
  <read_first>
    - src/nativespeaker/api/app/dependencies.py (:20 the crud import, :111-114 the accessor and its comment, :128-136 `get_auth_service`)
    - src/nativespeaker/api/app/lifespan.py (:42 the crud import, :162 the `app.state` assignment, :171 and :180 where both adapters are built)
    - src/nativespeaker/api/services/auth.py (:9-14 the imports, :64-76 `__init__`, :136, :142, :146 inside `_complete`, :400 inside `_consume_and_commit`)
    - src/nativespeaker/api/routers/auth.py (:9-18 the import block, :47-76 `issue_challenge`, and :63 — the analog this task copies)
    - tests/unit/conftest.py (`class FakeChallengeStore` at :236-274, including `self._binding` at :242 and the pass-through at :251-252)
    - tests/unit/test_conversion_carries_usage.py:63-104 — the in-repo analog for a fixture that monkeypatches a crud class and returns the object the cases read
    - tests/unit/test_challenge_endpoint.py (`_RecordingChallengeStore` at :46-56, the `store` fixture at :86-88, the override at :128)
    - tests/unit/test_create_user_body.py (`_RecordingChallengeStore` at :26-40, the `store` fixture at :66-68, the override at :96)
    - tests/e2e/test_challenge_store.py:30-34 — the module-scoped fixture that reads the lifespan attribute
  </read_first>
  <precondition>PostgreSQL answers on 127.0.0.1:5432. This task edits e2e and schema test files and runs `-m e2e` and `-m schema`, which use that live database and are deselected without `-m`.</precondition>
  <reversibility rating="reversible">Internal wiring only; no migration, no route contract change, and `git revert` restores every line.</reversibility>
  <action>
    One commit. The crud class keeps its current shape in this task — no constructor argument yet,
    and every method still takes the session. Plan 49-04 changes that. The no-argument construction
    this task writes is deliberate and lives for exactly one commit.

    **`src/nativespeaker/api/app/lifespan.py`** — delete the crud import at :42 and the
    `app.state.challenge_store` assignment at :162. Change nothing else in the lifespan; Phase 50
    owns the rest.

    **`src/nativespeaker/api/app/dependencies.py`** — delete the crud import at :20, delete the whole
    `get_challenge_store` function with its docstring, and delete the comment above it that explains
    why two accessors exist. `get_firebase_adapter` and `get_devicecheck_adapter` stay; rewrite that
    comment only if one line is still needed for the two that remain, otherwise delete it (48 D-10).
    In `get_auth_service`, delete the `challenge_store` parameter and the matching keyword argument,
    and annotate the provider parameter `adapter: FirebaseAdminLookup` (D-06). The device-gate
    parameter is already annotated `AppleDeviceCheck` from plan 49-02.

    **`src/nativespeaker/api/services/auth.py`** — in `__init__`, delete the `challenge_store`
    parameter, annotate `adapter: FirebaseAdminLookup` and `devicecheck: AppleDeviceCheck` (D-06, no
    `| None` — the lifespan always builds both), and replace the assignment with
    `self.challenges_db = ChallengesDB()` placed directly below `self.grants_db = GrantsDB(db)`
    (D-02). Add `FirebaseAdminLookup` to the existing `auth.firebase` import line and
    `AppleDeviceCheck` to the existing `auth.devicecheck` import line. Keep the vendor-API comment
    above the device-gate assignment verbatim. Rename the four read sites at :136, :142, :146 and
    :400 to `self.challenges_db`; each keeps the `self.session` argument it passes today. The word
    "store" names no class in any line this task writes.

    **`src/nativespeaker/api/routers/auth.py`** — remove the deleted accessor from the import block
    at :9-18 and the `challenge_store` parameter from `issue_challenge` at :54. Keep the crud import
    at :21. Change the issue call at :68-71 to build the crud object inline, exactly as :63 builds
    the identities one two lines above; the session argument and the three keyword arguments stay as
    they are. The `session.commit()` line and the `Cache-Control` line do not change (D-03). Add no
    method to `AuthService`: the handler would then resolve the provider and device-gate
    dependencies it does not use.

    **`tests/unit/conftest.py` (D-04)** — delete `self._binding` from `FakeChallengeStore.__init__`
    and delete the fake's `verify_binding` pass-through; the real method stays unpatched and the
    service still calls it. Keep the class itself, one copy, with its `locate`, `claim` and `consume`
    bodies unchanged, including the `session` parameter each takes today. Add one `store` fixture to
    `conftest.py` that takes `monkeypatch`, builds a `FakeChallengeStore`, defines three replacement
    functions that take `self` first and delegate to the fake, calls
    `monkeypatch.setattr(ChallengesDB, "locate", ...)`, `..."claim"...` and `..."consume"...`, and
    returns the fake. Copy the shape from `tests/unit/test_conversion_carries_usage.py:63-90`. One
    setattr on the class reaches both binders, because the package import in the service and the
    module import in the handler bind the same class object.

    **The four precedence suites** — `test_create_user_precedence.py`, `test_upgrade_precedence.py`,
    `test_claim_precedence.py`, `test_claim_precedence_registered.py`: delete each per-file `store`
    fixture and each `dependency_overrides` line that named the deleted accessor. The cases keep
    reading `store.row` and `store.consume_calls` off the fixture that now comes from `conftest.py`.
    Change no assertion and no case order.

    **`test_challenge_endpoint.py` and `test_create_user_body.py` (D-05)** — each keeps its own
    `_RecordingChallengeStore`; the two differ and are not merged. Change each file's own `store`
    fixture to take `monkeypatch`, build its recorder, and set the replacement on the crud class —
    `issue` in both files, plus `locate` in `test_create_user_body.py` if its recorder implements
    one. Delete the `dependency_overrides` line in each.

    **`test_create_user_rollback.py` and `test_conflict_classification.py`** — delete the
    `challenge_store=` argument from the `AuthService(...)` call, and delete the crud import that
    the argument was the file's only use of. Leaving it makes ruff F401 red.

    **`tests/schema/test_create_race.py`, `test_create_atomicity.py`, `test_claim_race.py`** — delete
    the `challenge_store=` argument AND the now-unused local it was bound from on the line above,
    AND the crud import that local was the file's only use of. Leaving the local makes ruff F841
    red; leaving the import makes F401 red. The docstring in `test_claim_race.py:136` that names the
    crud class stays as prose.

    **`tests/e2e/test_challenge_store.py`** — the module-scoped fixture stops reading the lifespan
    attribute and returns a crud object it builds itself; drop the `_app_lifespan` parameter it no
    longer needs, and replace its docstring, whose claim about lifespan wiring is no longer true.
    This file has no crud import today — it imports `_claim_statement` from that module — so add
    `ChallengesDB` to that existing import line.
    Every other line of that file stays as it is; plan 49-04 rewrites the call sites.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'challenge_store' src tests)"</automated>
    <fails_when>non-zero exit — grep printed at least one remaining line</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_create_user_precedence.py tests/unit/test_upgrade_precedence.py tests/unit/test_claim_precedence.py tests/unit/test_claim_precedence_registered.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_challenge_endpoint.py tests/unit/test_create_user_body.py tests/unit/test_create_user_rollback.py tests/unit/test_conflict_classification.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_app_wiring.py tests/unit/test_config.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>non-zero exit, or any output other than "All checks passed!"</fails_when>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
    <automated>.venv/bin/pytest -q -m schema</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'challenge_store' src tests` prints nothing.
    - `grep -c 'self.challenges_db' src/nativespeaker/api/services/auth.py` prints `5` — one assignment and four read sites.
    - `grep -c 'def get_auth_service' src/nativespeaker/api/app/dependencies.py` prints `1`, and that function takes exactly three parameters.
    - `grep -c 'app.state' src/nativespeaker/api/app/lifespan.py` is one lower than before this task.
    - `grep -c 'ChallengesDB' src/nativespeaker/api/routers/auth.py` prints `2` — the import and the inline build.
    - `grep -c 'def store' tests/unit/conftest.py` prints `1`, and `grep -c 'monkeypatch.setattr(ChallengesDB' tests/unit/conftest.py` prints `3`.
    - `grep -c 'def store' tests/unit/test_create_user_precedence.py` prints `0`; the same holds for the other three precedence suites.
    - `grep -c '_RecordingChallengeStore' tests/unit/test_challenge_endpoint.py` prints at least `2`, and the same holds for `tests/unit/test_create_user_body.py` — the two recorders are not merged.
    - `grep -c 'verify_binding' tests/unit/conftest.py` prints `0` — the real method is called.
    - `git log -1 --stat` shows one commit covering all seventeen files.
  </acceptance_criteria>
  <done>No lifespan attribute and no request-scoped accessor supplies the challenge crud object; the service and the handler each build one, the six suites patch the class, and all three suites carry only the pre-existing failure.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure the type gate after the new annotations</name>
  <files>.planning/phases/49-delete-the-single-implementation-auth-protocols/49-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/48-narrow-identity-to-the-verified-pair/48-VERIFICATION.md (the recorded 306-diagnostic baseline)
    - src/nativespeaker/api/services/auth.py (the `__init__` signature this task measures)
    - src/nativespeaker/api/app/dependencies.py (`get_auth_service`)
  </read_first>
  <action>
    D-06 gave two previously unannotated parameters a concrete type in each of two signatures, so the
    type checker now checks the calls that reach them. Run `.venv/bin/ty check` and read the reported
    diagnostic count out of its own output. Do not copy the baseline number.

    Record the measured count in the summary beside the 306 baseline. If the count is at or below
    306, the gate holds and no source edit follows. If the count is above 306, read each new
    diagnostic, and fix only the ones this phase introduced — a wrong argument type at a call site
    that reaches one of the two newly annotated parameters. Do not silence a diagnostic with an
    ignore comment when a real type mismatch is what it names, and do not repair a diagnostic that
    predates this phase.

    Test call sites that pass `None` for the provider adapter are not type-checked and do not change.
  </action>
  <verify>
    <automated>.venv/bin/ty check</automated>
    <fails_when>the reported diagnostic count is above 306</fails_when>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
  </verify>
  <acceptance_criteria>
    - The summary records the measured diagnostic count, read from the run's own output.
    - The measured count is at or below 306.
    - If any source edit was needed, `git log -1 --stat` shows it as its own commit and the summary names each diagnostic it closed.
  </acceptance_criteria>
  <done>The type-gate count is measured against the 306 baseline and recorded, and it has not risen.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols this plan creates:

- `AuthService.challenges_db` — the attribute that holds the challenge crud object, beside
  `AuthService.identities_db` and `AuthService.grants_db`.
- `store` — a new fixture in `tests/unit/conftest.py`. It takes `monkeypatch`, patches `locate`,
  `claim` and `consume` onto `ChallengesDB`, and returns the `FakeChallengeStore` the four
  precedence suites read.

Signatures this plan changes:

- `AuthService.__init__(self, db, adapter: FirebaseAdminLookup, devicecheck: AppleDeviceCheck)` —
  three parameters, both provider parameters now annotated.
- `get_auth_service(db, adapter: FirebaseAdminLookup, devicecheck: AppleDeviceCheck)` — three
  parameters.
- `issue_challenge(body, response, claims, session)` — four parameters.

Symbols this plan removes: `get_challenge_store`, and the `app.state` attribute it read.
`FakeChallengeStore` and both `_RecordingChallengeStore` classes keep their names.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `POST /auth/challenge` | An untrusted caller asks for a single-use capability here |
| client → the four completion routes | An untrusted caller presents a handle it claims to own here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-08 | Elevation of Privilege | `AuthService._complete` rejection order | high | mitigate | The four precedence suites are the control and must stay green with no assertion edited. The locate, binding, operation, claim order at :136-146 is moved in name only |
| T-49-09 | Elevation of Privilege | the binding check | high | mitigate | `verify_binding` stays the real method: D-04 removes the fake's pass-through rather than patching the method. `grep -c 'verify_binding' tests/unit/conftest.py` printing `0` is the proof |
| T-49-10 | Tampering | single-use consume | high | mitigate | `tests/e2e/test_challenge_store.py` and the three schema race suites run against live PostgreSQL in this task. No SQL statement is edited |
| T-49-11 | Information Disclosure | the handle in `issue_challenge` | medium | mitigate | The handler still logs only the rejected operation string and never the handle, and the `no-store` header line is untouched |
| T-49-12 | Spoofing | `get_auth_service` provider wiring | medium | mitigate | D-06 annotates the provider parameter, so the type gate now checks its call sites; Task 2 measures that gate |

No package manager runs in this plan, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
Run from the repository root:

- `.venv/bin/pytest -q -m ''`, `.venv/bin/pytest -q -m e2e` — no failed case but the restore four-arms case
- `.venv/bin/pytest -q -m schema` — exit 0
- `.venv/bin/ruff check src tests` — `All checks passed!`
- `.venv/bin/ty check` — at or below 306 diagnostics
- `test -z "$(grep -rn 'challenge_store' src tests)"`
</verification>

<success_criteria>
The lifespan builds no challenge crud object, the request-scoped accessor is gone, `AuthService`
holds `self.challenges_db`, the handler builds its own, the six suites monkeypatch the class, and
the type-gate count has not risen above 306.
</success_criteria>

<output>
Create `.planning/phases/49-delete-the-single-implementation-auth-protocols/49-03-SUMMARY.md` when done.
Record the measured type-gate count and the three suite summary lines.
</output>
