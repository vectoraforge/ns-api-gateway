---
phase: 49-delete-the-single-implementation-auth-protocols
plan: 04
type: execute
wave: 4
depends_on: ["49-03"]
files_modified:
  - src/nativespeaker/api/crud/challenges.py
  - src/nativespeaker/api/services/auth.py
  - src/nativespeaker/api/routers/auth.py
  - tests/unit/conftest.py
  - tests/unit/test_challenge_endpoint.py
  - tests/unit/test_create_user_body.py
  - tests/unit/test_challenge_ids.py
  - tests/e2e/test_challenge_store.py
  - .planning/phases/49-delete-the-single-implementation-auth-protocols/49-VALIDATION.md
autonomous: true
requirements: []

estimate:
  tokens: 95000
  raw_tokens: 63000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01: ChallengesDB takes the session in its constructor, as IdentitiesDB and GrantsDB do, and issue, locate, claim and consume read self.session."
    - "D-01: verify_binding keeps its three parameters and its body byte-for-byte."
    - "No ChallengesDB method takes a session parameter."
    - "The claim UPDATE, the consume UPDATE and the expiry predicate are unchanged, statement for statement."
    - "The three suites, ruff and ty are re-measured at the phase gate rather than copied."
  artifacts:
    - src/nativespeaker/api/crud/challenges.py
    - tests/unit/test_challenge_ids.py
    - tests/e2e/test_challenge_store.py
    - .planning/phases/49-delete-the-single-implementation-auth-protocols/49-VALIDATION.md
  key_links:
    - "AuthService builds ChallengesDB(db) from the session it already holds."
    - "The challenge handler builds ChallengesDB(session) from its own request session."
    - "A test that spans two engine factories builds one crud object per session."
---

<objective>
Give the challenge crud class the session-holding constructor its two sibling crud classes already
have, then measure the phase gate.

Purpose: the three crud classes `AuthService` builds have one shape.
Output: one commit for the constructor, plus a measured phase gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

**You are NOT in a git worktree.** This repository is a git submodule, so its `.git` is a file and
GSD worktree detection reports `IS_WORKTREE=true` wrongly. Treat it as false. Run every git command
from `/home/init/native-speaker/ns-api-gateway`. Do not push. Do not touch the parent repository.

**Test baseline (Phase 48 gate).** `.venv/bin/pytest -q -m ''` reads `1 failed, 2572 passed`;
`-m e2e` reads `1 failed, 360 passed`; `-m schema` reads `297 passed`; `.venv/bin/ruff check src tests`
prints `All checks passed!`; `.venv/bin/ty check` reads `Found 306 diagnostics`. The one failing case
is the pre-existing restore four-arms case — the log event name comes from the exception class name
while the case asserts the error code. It is not this phase's. Do not write a fix for it. "Green"
means no failed case other than that one. This phase deletes tests, so the passed counts fall.

**Suite markers.** `addopts` carries `-m 'not e2e and not schema'`. The e2e and schema suites are
silently deselected unless `-m` is given, and they use the live localhost PostgreSQL.

**This plan does not change the `auth/` package**, so `tests/unit/test_auth_package_shape.py` is not
edited here and D-08 does not apply to this commit.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-CONTEXT.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-RESEARCH.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-PATTERNS.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-VALIDATION.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-03-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The challenge crud class takes the session in its constructor</name>
  <files>src/nativespeaker/api/crud/challenges.py, src/nativespeaker/api/services/auth.py, src/nativespeaker/api/routers/auth.py, tests/unit/conftest.py, tests/unit/test_challenge_endpoint.py, tests/unit/test_create_user_body.py, tests/unit/test_challenge_ids.py, tests/e2e/test_challenge_store.py</files>
  <read_first>
    - src/nativespeaker/api/crud/challenges.py (the whole file — every method and `_claim_statement`)
    - src/nativespeaker/api/crud/identities.py:25-33 — the constructor shape to copy, byte-identical at src/nativespeaker/api/crud/grants.py:96-104
    - src/nativespeaker/api/services/auth.py (the `__init__` assignment and the four read sites, as plan 49-03 left them)
    - src/nativespeaker/api/routers/auth.py (`issue_challenge`, and :63 — the analog)
    - tests/unit/conftest.py (`FakeChallengeStore` and the `store` fixture plan 49-03 added)
    - tests/unit/test_challenge_ids.py (the `store()` helper at :39-40, `_RecordingSession`, and every call site — `grep -n 'store()' tests/unit/test_challenge_ids.py` reads 16 lines: the definition at :39 and 15 calls)
    - tests/e2e/test_challenge_store.py (the whole file — the module fixture, the helpers `issue`, `expire`, `read`, `row_count`, and the second engine factory built for the contended claim)
    - tests/unit/test_conversion_carries_usage.py:88 — the precedent for handing a stub session to a crud constructor with a `ty: ignore` comment
  </read_first>
  <precondition>PostgreSQL answers on 127.0.0.1:5432. This task edits e2e test files and runs `-m e2e` and `-m schema`, which use that live database and are deselected without `-m`.</precondition>
  <reversibility rating="reversible">A constructor signature inside one package, with no migration and no route contract change; `git revert` restores every line.</reversibility>
  <action>
    One commit. This is the widest mechanical edit of the phase; count the call sites before you
    start and check the count again at the end.

    **`src/nativespeaker/api/crud/challenges.py`** — add
    `def __init__(self, session: AsyncSession): self.session = session` in the shape
    `crud/identities.py:25-28` uses: a required positional parameter, no default, no `| None`, no
    docstring on the constructor, and the same blank line after the class statement. Give it no
    default of `None`; a default would make this class a different shape from its two siblings,
    which is the whole reason for the change. Remove the clause in the class docstring that says the
    session is a parameter on every method; keep the rest of that sentence. Keep `__repr__` and keep
    the TTL inside it — the sibling crud classes declare no `__repr__` at all, so there is nothing to
    copy, and the TTL is the one fact the repr carries (Research Open Question 3; CONTEXT D-01 does
    not forbid it).

    Drop the `session` parameter from `issue`, `locate`, `claim` and `consume`, and change every
    `session.add`, `session.flush` and `session.exec` in their bodies to `self.session`. The keyword
    parameters after the dropped one keep their names and their keyword-only marker. Leave
    `verify_binding` byte-for-byte as it is: it reads no session and D-01 keeps it. Leave
    `_claim_statement` untouched — it is the one serialization point and the only expiry check, so it
    moves not at all. Leave the module docstring's note that this module holds no logger.

    **`src/nativespeaker/api/services/auth.py`** — build the attribute as `ChallengesDB(db)`. Drop
    `self.session` from the `locate`, `claim` and `consume` calls at :136, :146 and :400. The
    `verify_binding` call keeps its three arguments exactly.

    **`src/nativespeaker/api/routers/auth.py`** — build the crud object with the request session and
    drop the session argument from the issue call, so the line reads like the identities line above
    it. The three keyword arguments stay.

    **`tests/unit/conftest.py`** — drop the `session` parameter from `FakeChallengeStore.locate`,
    `claim` and `consume`, and from the three replacement functions in the `store` fixture. Each
    replacement still takes `self` first, because it is set on the class.

    **`tests/unit/test_challenge_endpoint.py` and `tests/unit/test_create_user_body.py`** — drop the
    `session` parameter from each recorder's methods and from each replacement function.

    **`tests/unit/test_challenge_ids.py`** — change the `store()` helper to take the session the case
    is already building and to return the crud object constructed around it, following the
    `GrantsDB(_AddingSession())  # ty: ignore[invalid-argument-type]` precedent. Update every call
    site. Measure the count first with `grep -n 'store()' tests/unit/test_challenge_ids.py`; at
    planning time it read 16 lines — the definition at :39 and 15 calls at :98, 169, 222, 230, 239,
    247, 256, 266, 275, 286, 297, 308, 311, 320 and 327. Nine of the calls reach `verify_binding`,
    which needs no session — they still construct with the session the helper is given, because the
    constructor requires one.

    **`tests/e2e/test_challenge_store.py`** — delete the module-scoped fixture entirely. Drop the
    crud-object parameter from the module-level helpers `issue`, `read`, `row_count` and `expire`,
    and build one crud object inside each `async with factory() as session:` block. Do this in every
    case as well, including the contended-claim case, which opens its sessions from a second engine
    factory: each of those sessions builds its own object. One object must never be reused across
    two sessions. Update every call site of the four helpers to match their new signatures.

    Change no assertion, no status code, no SQL statement and no case order anywhere in this task.
  </action>
  <verify>
    <automated>test -z "$(grep -n 'async def issue(self, session\|async def locate(self, session\|async def claim(self, session\|async def consume(self, session' src/nativespeaker/api/crud/challenges.py)"</automated>
    <fails_when>non-zero exit — a crud method still declares a session parameter</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_challenge_ids.py tests/unit/test_challenge_endpoint.py tests/unit/test_create_user_body.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_create_user_precedence.py tests/unit/test_upgrade_precedence.py tests/unit/test_claim_precedence.py tests/unit/test_claim_precedence_registered.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/pytest -q -m e2e tests/e2e/test_challenge_store.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line, or fewer than 32 cases reported</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>non-zero exit, or any output other than "All checks passed!"</fails_when>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
    <automated>.venv/bin/pytest -q -m schema</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def __init__(self, session: AsyncSession)' src/nativespeaker/api/crud/challenges.py` prints `1`.
    - `grep -c 'self.session' src/nativespeaker/api/crud/challenges.py` prints at least `5` — the assignment and the four method bodies.
    - `grep -c 'def verify_binding(self, row: AuthChallenge, claims: VerifiedClaims,' src/nativespeaker/api/crud/challenges.py` prints `1`, and `git diff HEAD~1 -- src/nativespeaker/api/crud/challenges.py` shows no changed line inside `verify_binding` or `_claim_statement`.
    - `grep -c 'ttl_seconds' src/nativespeaker/api/crud/challenges.py` prints `1` — the repr keeps the TTL.
    - `grep -c 'store(' tests/unit/test_challenge_ids.py` prints `16` — the count the pre-edit measurement read, one definition plus 15 calls, each call now carrying a session argument.
    - `grep -c 'ChallengesDB(db)' src/nativespeaker/api/services/auth.py` prints `1`.
    - `grep -c 'ChallengesDB(session)' src/nativespeaker/api/routers/auth.py` prints `1`.
    - `grep -c 'def store' tests/e2e/test_challenge_store.py` prints `0` — the module-scoped fixture is gone.
    - `.venv/bin/pytest -q -m e2e tests/e2e/test_challenge_store.py` reports the same case count as the Phase 48 gate, 32.
    - `git log -1 --stat` shows one commit covering all eight files.
  </acceptance_criteria>
  <done>Every challenge crud method reads `self.session`, no method takes a session parameter, the binding check and both UPDATE statements are byte-unchanged, and all three suites carry only the pre-existing failure.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the phase gate and record it</name>
  <files>.planning/phases/49-delete-the-single-implementation-auth-protocols/49-VALIDATION.md</files>
  <read_first>
    - .planning/phases/49-delete-the-single-implementation-auth-protocols/49-VALIDATION.md (the Per-Task Verification Map and the sign-off list)
    - .planning/phases/49-delete-the-single-implementation-auth-protocols/49-03-SUMMARY.md (the measured type-gate count)
    - .planning/ROADMAP.md Phase 49 — the five success criteria
  </read_first>
  <precondition>PostgreSQL answers on 127.0.0.1:5432. Two of the three suites use that live database and are deselected without `-m`.</precondition>
  <action>
    Re-run every gate. RE-MEASURE each number from the run's own output; copy no number from any
    earlier plan, summary or research file. Phase 47 found a real lint regression only because a
    plan re-ran the command instead of copying the answer.

    Run, in this order: `.venv/bin/pytest -q -m ''`, `.venv/bin/pytest -q -m e2e`,
    `.venv/bin/pytest -q -m schema`, `.venv/bin/ruff check src tests`, `.venv/bin/ty check`.

    Then update `49-VALIDATION.md`: set each row of the Per-Task Verification Map to green or red
    from what the commands printed, set `nyquist_compliant: true` when every row is green, and tick
    the sign-off boxes that hold. Set the `Approval` line to the date and the measured result.

    State the one pre-existing restore four-arms case by name in the record, and state that it is not
    this phase's, as Phases 47 and 48 both did. Do not fix it.

    Also run the five phase-wide greps and record each result: the four Protocol names absent from
    `src/` and `tests/`, the removed adapter module absent from disk, and the challenge dependency
    name absent from `src/` and `tests/`.

    Commit the record.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
    <automated>.venv/bin/pytest -q -m schema</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>non-zero exit, or any output other than "All checks passed!"</fails_when>
    <automated>.venv/bin/ty check</automated>
    <fails_when>the reported diagnostic count is above 306</fails_when>
    <automated>test -z "$(grep -rn 'PlaySubscriptionSource\|FirebaseAdminAdapter\|DeviceCheckAdapter\|TokenVerifier\|challenge_store' src tests)"</automated>
    <fails_when>non-zero exit — grep printed at least one remaining line</fails_when>
    <automated>test ! -e src/nativespeaker/api/auth/adapters.py</automated>
    <fails_when>non-zero exit — the removed module is still on disk</fails_when>
  </verify>
  <acceptance_criteria>
    - `49-VALIDATION.md` carries no `⬜ pending` row.
    - `49-VALIDATION.md` front matter reads `nyquist_compliant: true`.
    - The record names the restore four-arms case and states it is not this phase's.
    - Every number in the record was read from a command run in this task, and the record says so.
    - `git log -1 --stat` shows the validation record as its own commit.
  </acceptance_criteria>
  <done>Every gate is re-measured, every verification-map row is green or explained, and the record names the one pre-existing failure.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols this plan creates:

- `ChallengesDB.__init__(self, session: AsyncSession)` — a required positional parameter, matching
  `IdentitiesDB.__init__` and `GrantsDB.__init__`.
- `ChallengesDB.session` — the attribute the four methods read.

Signatures this plan changes:

- `ChallengesDB.issue(self, *, operation, claims, linked)` — the session parameter is gone.
- `ChallengesDB.locate(self, challenge_id)` — the session parameter is gone.
- `ChallengesDB.claim(self, *, challenge_id)` — the session parameter is gone.
- `ChallengesDB.consume(self, *, challenge_id)` — the session parameter is gone.
- `ChallengesDB.verify_binding(self, row, claims, linked)` — unchanged.
- `FakeChallengeStore.locate`, `.claim`, `.consume` in `tests/unit/conftest.py` — the session
  parameter is gone.
- `store(session)` in `tests/unit/test_challenge_ids.py` — the helper now takes the session.
- `issue(factory, ...)`, `read(factory, ...)`, `row_count(factory)`, `expire(factory, ...)` in
  `tests/e2e/test_challenge_store.py` — the crud-object parameter is gone.

Symbols this plan removes: the module-scoped `store` fixture in `tests/e2e/test_challenge_store.py`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request session → `core.auth_challenges` | Every challenge read and write crosses here |
| concurrent requests → one challenge row | Two callers can race the same single-use handle here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-13 | Elevation of Privilege | `_claim_statement` | high | mitigate | It is the one serialization point and the only expiry check. It is not edited, and `git diff HEAD~1` showing no changed line inside it is an acceptance criterion. The 8-contender e2e claim race and the three schema race suites are the live controls |
| T-49-14 | Tampering | `consume` | high | mitigate | The UPDATE that clears the pre-auth subject is not edited. `tests/e2e/test_challenge_store.py` exercises the claimed-to-consumed move against live PostgreSQL in this task |
| T-49-15 | Elevation of Privilege | `verify_binding` | high | mitigate | Byte-unchanged by D-01, asserted by a diff criterion, and exercised by the four precedence suites and by nine cases in `tests/unit/test_challenge_ids.py` |
| T-49-16 | Tampering | one crud object shared by two sessions | medium | mitigate | The class now holds a session, so a shared object would write through the wrong one. The e2e fixture that spanned two engine factories is deleted, and each session builds its own object |
| T-49-17 | Information Disclosure | the handle | medium | mitigate | The crud module still declares no logger, and no handle is added to any log call in this task |

No package manager runs in this plan, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
Run from the repository root:

- `.venv/bin/pytest -q -m ''`, `.venv/bin/pytest -q -m e2e` — no failed case but the restore four-arms case
- `.venv/bin/pytest -q -m schema` — exit 0
- `.venv/bin/ruff check src tests` — `All checks passed!`
- `.venv/bin/ty check` — at or below 306 diagnostics
- `test -z "$(grep -rn 'PlaySubscriptionSource\|FirebaseAdminAdapter\|DeviceCheckAdapter\|TokenVerifier\|challenge_store' src tests)"`
</verification>

<success_criteria>
The challenge crud class holds its session, no method takes one, the binding check and both UPDATE
statements are byte-unchanged, and `49-VALIDATION.md` carries a re-measured, fully green
verification map with the one pre-existing failure named.
</success_criteria>

<output>
Create `.planning/phases/49-delete-the-single-implementation-auth-protocols/49-04-SUMMARY.md` when done.
Record the three suite summary lines, the ruff result, the measured type-gate count, and the five
grep results, all read from runs in this plan.
</output>
