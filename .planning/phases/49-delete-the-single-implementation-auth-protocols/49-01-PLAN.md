---
phase: 49-delete-the-single-implementation-auth-protocols
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nativespeaker/api/auth/google_play.py
  - src/nativespeaker/api/auth/jwt_verifier.py
  - src/nativespeaker/api/services/restore.py
  - tests/unit/test_google_play_notifications.py
  - tests/unit/test_auth_package_shape.py
autonomous: true
requirements: []

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No name of the Play subscription Protocol or the JWT Protocol is left in src/ or tests/."
    - "services/restore.py annotates play with the concrete Play subscriptions class."
    - "PubSubPushTokens.__init__ annotates verifier and build with the concrete JWT verifier class."
    - "D-08: each of the two commits writes the re-measured CURRENT tuple into tests/unit/test_auth_package_shape.py in that same commit."
    - "D-07: the ROADMAP Phase 49 entry already names D-01 and criterion 5; this plan edits no ROADMAP goal text."
  artifacts:
    - src/nativespeaker/api/auth/google_play.py
    - src/nativespeaker/api/auth/jwt_verifier.py
    - src/nativespeaker/api/services/restore.py
    - tests/unit/test_auth_package_shape.py
  key_links:
    - "services/restore.py imports the concrete class from auth/google_play.py."
    - "auth/google_play.py imports the concrete verifier class from auth/jwt_verifier.py."
    - "tests/unit/test_auth_package_shape.py compares a literal with a live AST walk of auth/."
---

<objective>
Delete the two smallest single-implementation Protocols in `auth/`, one per commit, following
commit 37a5ac6.

Purpose: prove the whole seam recipe end-to-end on the cheapest seam before the wide ones run.
Output: two commits. Each deletes one Protocol, repoints its annotations at the concrete class,
and writes the re-measured package-shape tuple.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md

**You are NOT in a git worktree.** This repository is a git submodule, so its `.git` is a file and
GSD worktree detection reports `IS_WORKTREE=true` wrongly. Treat it as false. Run every git command
from `/home/init/native-speaker/ns-api-gateway`. Do not push. Do not touch the parent repository.

**Test baseline (Phase 48 gate).** `.venv/bin/pytest -q -m ''` reads `1 failed, 2572 passed`;
`-m e2e` reads `1 failed, 360 passed`; `-m schema` reads `297 passed`; `.venv/bin/ruff check src tests`
prints `All checks passed!`; `.venv/bin/ty check` reads `Found 306 diagnostics`. The one failing case
is the pre-existing restore four-arms case. It is not this phase's. Do not plan or write a fix for
it. "Green" means no failed case other than that one. This plan deletes tests, so the passed count
falls. A falling passed count is expected; a new failed name is not.

**Suite markers.** `addopts` carries `-m 'not e2e and not schema'`. The e2e and schema suites are
silently deselected unless `-m` is given, and they use the live localhost PostgreSQL. This plan
touches no e2e and no schema file, so `-m ''` is the suite it runs.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-CONTEXT.md
@.planning/phases/49-delete-the-single-implementation-auth-protocols/49-PATTERNS.md
@AGENTS.md
</context>

<!-- planner-discipline-allow: PlaySubscriptionSource -->
<!-- planner-discipline-allow: TokenVerifier -->

<tasks>

<task type="tracer">
  <name>Task 1: Delete the Play subscription Protocol — one seam, end to end</name>
  <files>src/nativespeaker/api/auth/google_play.py, src/nativespeaker/api/services/restore.py, tests/unit/test_auth_package_shape.py</files>
  <read_first>
    - `git show 37a5ac6` — the model commit. Read all four hunks before editing anything.
    - src/nativespeaker/api/auth/google_play.py (the `class PlaySubscriptionSource(Protocol)` block at :124-135, and `class PlayDeveloperSubscriptions` at :248)
    - src/nativespeaker/api/services/restore.py (the import at :13, the `__init__` annotation at :44)
    - tests/unit/test_auth_package_shape.py (the `CURRENT` literal at :13 and `_measure`)
    - .planning/phases/49-delete-the-single-implementation-auth-protocols/49-PATTERNS.md § "The four Protocol deletions"
  </read_first>
  <reversibility rating="reversible">A deletion inside one package with no published contract and no migration; `git revert` restores it.</reversibility>
  <action>
    This task is the tracer. It runs the complete seam recipe on the smallest seam, and every later
    seam copies it exactly.

    In `src/nativespeaker/api/auth/google_play.py`: delete the whole `class PlaySubscriptionSource(Protocol)`
    block with its class docstring and both method docstrings. Move no docstring onto
    `PlayDeveloperSubscriptions`. Drop `Protocol` from the `typing` import only when no other name in
    that module still uses it — check first, because the module may declare another Protocol.

    In `src/nativespeaker/api/services/restore.py`: change the import at :13 to bring in
    `PlayDeveloperSubscriptions` from the same module, and change the `play:` annotation in
    `__init__` at :44 to `PlayDeveloperSubscriptions`. Change no other parameter.

    Then D-08. Run `.venv/bin/pytest -q tests/unit/test_auth_package_shape.py`. It goes red, and the
    assertion text prints the measured triple beside the recorded one. RE-MEASURE: copy the measured
    triple out of that output and write it into `CURRENT`. The projection is `(8, 23, 65)`, from
    `(8, 24, 67)` minus one class and two methods — treat it as an expectation only, never as the
    value to type in blind. If the measured triple differs from the projection, the measured triple
    wins and the summary records the difference.

    Write no comment about this change. Delete any comment whose subject is the deleted Protocol.

    Commit this task alone: the source edit, the annotation edit and the tuple edit in one commit.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_auth_package_shape.py</automated>
    <fails_when>non-zero exit, or "failed" in the summary line</fails_when>
    <automated>test -z "$(grep -rn 'PlaySubscriptionSource' src tests)"</automated>
    <fails_when>non-zero exit — grep printed at least one remaining line</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_restore_proof.py tests/unit/test_google_play_notifications.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>non-zero exit, or any output other than "All checks passed!"</fails_when>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'PlaySubscriptionSource' src tests` prints nothing.
    - `grep -n 'class PlayDeveloperSubscriptions' src/nativespeaker/api/auth/google_play.py` prints one line.
    - `services/restore.py` `__init__` annotates `play` as `PlayDeveloperSubscriptions`.
    - `tests/unit/test_auth_package_shape.py` `CURRENT` holds the triple the failing run printed, not a typed-in guess.
    - `git log -1 --stat` shows exactly three files: the two source files and the shape test.
    - `.venv/bin/pytest -q -m ''` names no failed case but the restore four-arms case.
  </acceptance_criteria>
  <done>The Play subscription Protocol does not exist, its one consumer names the concrete class, the shape tuple is re-measured, and the unit suite carries only the pre-existing failure.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the JWT Protocol and repoint the push-token consumer</name>
  <files>src/nativespeaker/api/auth/jwt_verifier.py, src/nativespeaker/api/auth/google_play.py, tests/unit/test_google_play_notifications.py, tests/unit/test_auth_package_shape.py</files>
  <read_first>
    - src/nativespeaker/api/auth/jwt_verifier.py (the `class TokenVerifier(Protocol)` block at :69-72, `class JWTVerifier` at :102)
    - src/nativespeaker/api/auth/google_play.py (the import at :18, `PubSubPushTokens.__init__` at :211-212)
    - tests/unit/test_google_play_notifications.py (the import at :44, `class TestThePushTokenSeamIsTheAnnotation` at :1064-1074)
    - tests/unit/test_auth_package_shape.py
    - .planning/ROADMAP.md Phase 49 criterion 3 — it deletes this guard class rather than rewriting it
  </read_first>
  <action>
    In `src/nativespeaker/api/auth/jwt_verifier.py`: delete the whole `class TokenVerifier(Protocol)`
    block with its docstrings. Drop `Protocol` from the `typing` import only when nothing else in the
    module uses it.

    In `src/nativespeaker/api/auth/google_play.py`: change the import at :18 to bring in `JWTVerifier`
    from `nativespeaker.api.auth.jwt_verifier`, and change both `PubSubPushTokens.__init__`
    annotations to `JWTVerifier | None` and `Callable[[], JWTVerifier | None] | None`. The default
    values do not change. The lifespan already supplies the concrete type: `build_google_push_verifier`
    returns `JWTVerifier | None`.

    In `tests/unit/test_google_play_notifications.py`: delete the whole class
    `TestThePushTokenSeamIsTheAnnotation` with its docstrings, and drop the deleted Protocol name
    from the import at :44. Keep `JWTVerifier` in that import if another case uses it. ROADMAP
    criterion 3 deletes this class; 37a5ac6 rewrote its own against the concrete class, and the
    ROADMAP wins. Record in the summary that a guard recorded under WR-24 is retired, and that the
    member-set control inside it goes with it.

    D-08: run the shape case, RE-MEASURE the triple from its output, and write it into `CURRENT`.
    The projection is `(8, 22, 64)` — an expectation only.

    Commit this task alone.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_auth_package_shape.py</automated>
    <fails_when>non-zero exit, or "failed" in the summary line</fails_when>
    <automated>test -z "$(grep -rn 'TokenVerifier' src tests)"</automated>
    <fails_when>non-zero exit — grep printed at least one remaining line</fails_when>
    <automated>.venv/bin/pytest -q tests/unit/test_google_play_notifications.py tests/unit/test_jwt_verifier.py</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the summary line</fails_when>
    <automated>.venv/bin/ruff check src tests</automated>
    <fails_when>non-zero exit, or any output other than "All checks passed!"</fails_when>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>the summary names any failed case other than the restore four-arms case, or the run reports a collection error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'TokenVerifier' src tests` prints nothing.
    - `grep -c 'TestThePushTokenSeamIsTheAnnotation' tests/unit/test_google_play_notifications.py` prints `0`.
    - `inspect.get_annotations(PubSubPushTokens.__init__, eval_str=True)["verifier"]` resolves to `JWTVerifier | None`.
    - `tests/unit/test_auth_package_shape.py` `CURRENT` holds the triple the failing run printed.
    - `git log -1 --stat` shows exactly four files.
  </acceptance_criteria>
  <done>The JWT Protocol does not exist, the push-token consumer names the concrete verifier class, its retired guard is recorded in the summary, and the unit suite carries only the pre-existing failure.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no new symbol. It deletes two Protocol classes and repoints three annotation
sites. `PlayDeveloperSubscriptions`, `JWTVerifier` and `PubSubPushTokens` already exist and keep
their names and signatures. The `CURRENT` name in `tests/unit/test_auth_package_shape.py` already
exists; only its value changes.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Pub/Sub push → the RTDN handler | An untrusted signed token crosses here and `JWTVerifier` checks it |
| Google Play Developer API → the restore service | An external subscription answer crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01 | Spoofing | `PubSubPushTokens.__init__` | high | mitigate | Only the annotation changes. `tests/unit/test_google_play_notifications.py` and `tests/unit/test_jwt_verifier.py` must stay green; they exercise signature checking, the audience claim and the required-claim list |
| T-49-02 | Tampering | `services/restore.py` `play` parameter | medium | mitigate | Only the annotation changes; no call site and no proof check is edited. `tests/unit/test_restore_proof.py` must stay green |
| T-49-03 | Repudiation | the retired WR-24 guard | low | accept | ROADMAP criterion 3 retires it deliberately. The member-set control goes with it, and the summary records the retirement so a later reader sees the loss |

No package manager runs in this plan, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
Run after both commits, from the repository root:

- `.venv/bin/pytest -q -m ''` — no failed case but the restore four-arms case
- `.venv/bin/ruff check src tests` — `All checks passed!`
- `test -z "$(grep -rn 'PlaySubscriptionSource\|TokenVerifier' src tests)"`
</verification>

<success_criteria>
Two commits exist, one per seam. Neither Protocol name is left in `src/` or `tests/`. Each commit
carries its own re-measured `CURRENT` tuple. The unit suite names only the pre-existing failure at
each commit.
</success_criteria>

<source_coverage_audit>
## Multi-Source Coverage Audit — Phase 49 (whole plan set)

**GOAL** (ROADMAP Phase 49 goal and its five criteria)

| Item | Plan |
|------|------|
| Four Protocols deleted, every annotation names the concrete class | 49-01, 49-02 |
| `VerifiedProviderIdentity` moves to `auth/firebase.py`; `auth/adapters.py` removed | 49-02 |
| Protocol-only guard tests deleted | 49-01 T2, 49-02 T1, 49-02 T2 |
| Lifespan stops building the challenge crud object; the accessor goes; `AuthService` holds `challenges_db`; the handler builds its own; the six suites monkeypatch the class | 49-03 |
| `ChallengesDB` takes the session in its constructor | 49-04 T1 |
| One seam per commit; the shape tuple changes in each seam commit | 49-01, 49-02 (all four tasks) |
| Concrete adapters, build callables, retry helpers and value types left alone | all — no plan edits their bodies |

**REQ** — none. The ROADMAP maps no requirement ID to this phase, so every plan's `requirements`
field is empty. Recorded, not omitted.

**RESEARCH** (49-RESEARCH.md)

| Item | Plan |
|------|------|
| Commit sequence, seams 1 to 6 | 49-01 T1/T2, 49-02 T1/T2, 49-03 T1, 49-04 T1 |
| Pitfall 1 — the file built on the removed module | 49-02 T2 (decided: delete and relocate) |
| Pitfall 2 — `verify_binding` needs no session | 49-04 T1 (the helper takes the session it builds) |
| Pitfall 3 — two recorded guards retired by criterion 3 | 49-01 T2, 49-02 T1 (both recorded in the summaries) |
| Pitfall 4 — the dead local in the three schema files | 49-03 T1 |
| Pitfall 5 — the e2e fixture spanning two engines | 49-03 T1 (fixture repointed), 49-04 T1 (fixture deleted) |
| Pitfall 6 — the type gate moves under D-06 | 49-03 T2, 49-04 T2 |
| Open Question 1 — the tuple, resolved by D-08 | 49-01, 49-02 (every seam task re-measures) |
| Open Question 2 — the adapter-interfaces file | 49-02 T2 (decided) |
| Open Question 3 — the TTL in the repr | 49-04 T1 (kept) |
| Runtime State Inventory — nothing stored, configured or secret | no task needed; the stale `.pyc` is harmless |

**CONTEXT** (D-01 to D-08) — D-01 and D-08 in 49-04 and 49-01; D-02 to D-06 in 49-03; D-07 in
49-01; D-08 also in 49-02. All eight covered.

**Excluded, not missing:** the Deferred Ideas section is empty; the two reviewed todos
(`message-ordering-is-unspecified`, `secret-manager-integration`) are not folded, as in Phases 46
and 48; the rest of the lifespan and `app.state` belong to Phase 50.

**No gaps found.** No item is unplanned and no item is reduced.
</source_coverage_audit>

<output>
Create `.planning/phases/49-delete-the-single-implementation-auth-protocols/49-01-SUMMARY.md` when done.
Record the two measured `CURRENT` triples and whether each matched its projection.
</output>
