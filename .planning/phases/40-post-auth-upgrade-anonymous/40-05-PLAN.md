---
phase: 40-post-auth-upgrade-anonymous
plan: 05
type: execute
wave: 3
depends_on: ["40-04"]
files_modified:
  - src/nativespeaker/api/services/auth.py
  - tests/unit/test_upgrade_precedence.py
  - tests/e2e/test_upgrade_anonymous.py
autonomous: true
requirements: [UPGRADE-01, UPGRADE-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A repeat call that changes nothing answers identically to the call that performed the flip — 200 with the same one-field body naming the stored provider, no changed flag and no distinct status (D-04)"
    - "A live read reporting anonymous against a stored anonymous row refuses with NotLinked carrying the bounded cause empty and a stage naming the upgrade confirmation — the first producer of that cause in this repository (D-05, RESEARCH P-03)"
    - "A stored registered row whose live read disagrees — different provider, different uid, or anonymous — refuses as ProviderTransitionNotAllowed, and nothing is rewritten (D-05)"
    - "A target provider-account triple already held by another identity row refuses as ProviderAccountAlreadyLinked, found by catching the database's refusal rather than by checking first, with no constraint name stored or compared and no error message parsed (D-08)"
    - "All three refusals answer one identical 403 body, and are told apart only by the structured-log event name the class produces (D-02, D-05)"
    - "Every outcome at or after the Firebase call spends the handle, including provider-not-linked, with no branch on which outcome fired; rejections before the Firebase call neither claim nor consume (D-14)"
    - "Exactly one providerData read happens per completion, including the idempotent repeat that changes nothing (D-22)"
  prohibitions:
    - "The Firebase providerData read must never be skipped as an optimisation for a row that already looks registered — the read is the only thing that detects a diverged binding, and skipping it would let a corrupted row report success forever."
    - "A refusal must never leave a partial write behind: the transaction that raises must leave the identity row, the user row and the purchase-attribution tokens exactly as it found them."
  artifacts:
    - path: "src/nativespeaker/api/services/auth.py"
      provides: "The complete case matrix over the stored row and the live read, each combination raising its own class or returning the provider the transaction settled on"
      contains: "complete_upgrade"
    - path: "tests/unit/test_upgrade_precedence.py"
      provides: "The rejection precedence, the consumption disposition per branch, and the one-lookup-per-completion assertion"
      contains: "consume"
    - path: "tests/e2e/test_upgrade_anonymous.py"
      provides: "The four scripted-fake cases the real account cannot produce on demand"
      contains: "operation_not_allowed"
  key_links:
    - from: "src/nativespeaker/api/services/auth.py"
      to: "src/nativespeaker/api/errors.py"
      via: "each case-matrix branch raises the class whose snake-cased name is the brief's internal result"
      pattern: "ProviderTransitionNotAllowed"
    - from: "src/nativespeaker/api/services/auth.py"
      to: "src/nativespeaker/api/app/error_handlers.py"
      via: "no logger call is written for a refusal; raising the class produces the one WARNING line"
      pattern: "camel_to_snake"
---

<objective>
Complete the case matrix the tracer left with one branch built: the idempotent repeat, the
called-too-early refusal, the two drift refusals, and the already-taken conflict — three distinct
internal results behind one identical client answer.

Purpose: the tracer proved the spine. What is left is the part a client actually meets most of the
time, because the endpoint is also the repair path for an upgrade whose backend call was lost: a
client that calls a moment early, a client that calls twice, and the two states that mean a person
must fix something by hand.
Output: every combination of the stored row and the live read either returns the provider the
transaction settled on or raises the class that names it, proven per branch in a unit file and
end to end against the scripted provider fake.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-post-auth-upgrade-anonymous/40-CONTEXT.md
@.planning/phases/40-post-auth-upgrade-anonymous/40-RESEARCH.md
@.planning/phases/40-post-auth-upgrade-anonymous/40-PATTERNS.md
@AGENTS.md
</context>

## Artifacts this phase produces

This plan creates `tests/unit/test_upgrade_precedence.py`, the `stage` literal
`"upgrade_confirmation"` and this repository's first producer of `NotLinked(cause="empty")`. The full
phase inventory is in `40-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The remaining branches of the case matrix</name>
  <files>src/nativespeaker/api/services/auth.py</files>
  <read_first>
    - `src/nativespeaker/api/services/auth.py` as plan 40-04 left it — the shared sequence, the upgrade write seam and the placeholder raise whose reaching combinations 40-04's summary lists by name
    - `.planning/phases/40-post-auth-upgrade-anonymous/40-04-SUMMARY.md` — that list
    - `src/nativespeaker/api/auth/firebase.py` :100-131 — `_resolve_provider`, whose closed classification means a shape outside the accept set never reaches this matrix, and `_verified_email`
    - `src/nativespeaker/api/errors.py` — `NotLinked` and its `stage`/`cause` pair, and the two classes plan 40-02 added with their three-argument constructor
    - `src/nativespeaker/api/services/auth.py` :114-122 — `_reject_existing_identity`, the `NoReturn` shape for a raise-what-this-state-earned branch
    - `src/nativespeaker/api/tables/identities.py` :11-16 — the three-member `IdentityProvider`, from which both logged provider names come
    - `.planning/phases/40-post-auth-upgrade-anonymous/40-RESEARCH.md` § Architecture Patterns 3 (the five-branch table), § Premise Corrections P-03, § Open Questions 2
    - `.planning/phases/40-post-auth-upgrade-anonymous/40-CONTEXT.md` § D-02, D-04, D-05, D-08, D-13, D-14, D-22
  </read_first>
  <behavior>
    - Test 1: stored anonymous, live anonymous → `NotLinked` with cause `empty` and the upgrade stage; nothing written; 403 with the one-field body.
    - Test 2: stored google, live google, same uid → 200 and the stored provider returned, with no write issued — the idempotent repeat.
    - Test 3: stored google, live google, different uid → `ProviderTransitionNotAllowed`; the stored uid is never overwritten.
    - Test 4: stored google, live apple → `ProviderTransitionNotAllowed`.
    - Test 5: stored google, live anonymous → `ProviderTransitionNotAllowed` — a registered row does not un-register.
    - Test 6: stored anonymous, live google whose triple another identity row already holds → `ProviderAccountAlreadyLinked`, raised from the write's conflict arm and not from a pre-flight lookup.
    - Test 7 (control): the three refusals carry three distinct log event names and one identical client body, asserted by equality on the body so a more helpful field fails here.
  </behavior>
  <action>
Replace the placeholder raise in the upgrade write seam with the full matrix over two inputs — the
stored row's provider and the live read's provider, plus the uid comparison where both are registered.
There is no third input: D-01 removed the client declaration, so nothing the caller sent contributes.

Branch as follows. Stored anonymous and live registered is the flip the tracer already built. Stored
anonymous and live anonymous raises `NotLinked` with cause `empty` and a `stage` naming this decision
point — this is the repository's first producer of that cause, so pick a stage value that says the
live read confirmed nothing to record rather than reusing one of the classification stages, which
would read as a classifier failure in the log when the classifier in fact succeeded. Stored registered
with the same provider and the same uid returns the stored provider and issues no write at all: D-04
requires the answer be indistinguishable from the flip's, so it is the same status and the same
one-field body. Every other stored-registered combination raises `ProviderTransitionNotAllowed`.

Both new classes are constructed with the identity row id and the stored and live provider names, and
with nothing else — no provider-account identifier goes into either.

Do not add a pre-flight lookup for the already-taken case: the conflict arm plan 40-04 put on the
write is the whole mechanism, and a pre-check cannot answer without a race, so it would be a second
path saying the same thing exercised only by a race that is hard to trigger (D-08).

Do not skip the providerData read for a row that already looks registered. The read is the only thing
that detects a diverged binding; skipping it would let a corrupted row report success indefinitely,
and the exposure of always reading is accepted and recorded (D-22).

Do not branch the handle's disposition on which outcome fired. Every outcome at or after the provider
call spends the handle, which the shared sequence already does — this task adds no consumption logic
and must remove none.

Write no `logger` call for any refusal: raising the class produces the one WARNING line, and D-07's
"all three at WARNING" is the inherited default.

Keep the branches flat. A `try` contains only the statement that can raise, and there is no nested
exception handling anywhere in the new code (D-17).
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` passes with a non-zero collected count.
    - `grep -c "NotImplementedError" src/nativespeaker/api/services/auth.py` returns `0` — no placeholder survives.
    - `grep -c "cause=\"empty\"" src/nativespeaker/api/services/auth.py` returns `1`, and `grep -rn 'cause="empty"' src/` reports that one site as the only producer in the tree.
    - `grep -c "logger.warning" src/nativespeaker/api/services/auth.py` returns `0` — refusals are raised, never logged at the raise site.
    - `grep -in "for update\|begin_nested\|advisory\|serializable" src/nativespeaker/api/services/auth.py` returns nothing.
    - `uv run ruff check src tests` exits 0 and `uv run pytest tests/unit/test_docstring_bar.py -q` passes.
    - The summary states the chosen `stage` literal and why it is not one of the existing three.
  </acceptance_criteria>
  <done>Every combination of the stored row and the live read reaches a named outcome, and no combination falls through.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The precedence and consumption-disposition unit file</name>
  <files>tests/unit/test_upgrade_precedence.py</files>
  <read_first>
    - `tests/unit/test_create_user_precedence.py` in full — the four fakes at :41-131 (`_FakeChallengeStore`, `_RejectionLog`, `_StubSession`, `_RecordingCreator`), the rejection spy at :139-146 wiring three loggers into one list, the client fixture at :166-188 with its async-generator database override, the row builder and the byte-identical-body assertion at :191-224, and the case shape at :229-273
    - `src/nativespeaker/api/services/auth.py` as this plan's Task 1 leaves it
    - `tests/unit/conftest.py` :192-210 — `FakeFirebaseAdapter`, whose `script()` sets the seam's answer and whose `calls` list is what the one-lookup assertion reads
    - `src/nativespeaker/api/app/dependencies.py` :58-62 — `get_linked_identity`, which the override must satisfy: the upgrade route needs an identity carrying both a user and an identity row, unlike the pre-auth identity the create-user precedence file builds
    - `src/nativespeaker/api/crud/challenges.py` :43-47 and :90-105 — `issue`'s binding to the identity row id and `verify_binding`, so the fake's issued row binds the way the real store does
    - `.planning/phases/40-post-auth-upgrade-anonymous/40-CONTEXT.md` § D-14, D-22
  </read_first>
  <behavior>
    - Test 1: no such handle → 409 `challenge_required`; the row is neither claimed nor consumed and the provider fake was not called.
    - Test 2: a handle bound to a different identity row → the same 409 with a byte-identical body; nothing claimed, nothing consumed, no provider call.
    - Test 3: a handle issued for another operation → the same 409, still before the claim; nothing claimed, nothing consumed, no provider call.
    - Test 4: an expired handle and an already-consumed handle each → the same 409, distinguished only in the log.
    - Test 5: the provider read raises the not-found outcome → 401, and the handle IS consumed, because the failure is at or after the Firebase call (D-14).
    - Test 6: the provider read reports anonymous against a stored anonymous row → 403, and the handle IS consumed — the case D-14 names explicitly as the one that still spends.
    - Test 7: the successful flip → 200, and the handle is consumed exactly once.
    - Test 8: the idempotent repeat → 200, and the provider fake's call list has exactly one entry, so the read is not skipped for a row that already looks registered (D-22).
    - Test 9 (control): every one of the five pre-claim rejections returns a body equal by `==` to the same one-field dictionary, so a more helpful field fails the test.
  </behavior>
  <action>
Create `tests/unit/test_upgrade_precedence.py` by copying the scaffolding of the create-user
precedence file rather than inventing one: the same in-memory challenge store whose claim and consume
mirror the real conditional updates clause for clause, the same stub session that records transaction
boundaries and refuses queries, the same rejection spy wiring all three loggers into one list, and the
same client fixture building the real router with dependency overrides — including the
async-generator database override, which is a generator and not a plain callable because the real
dependency releases the read transaction in its own `except`.

Two things differ from the analog and must be got right. The identity the override supplies is a
**linked** one carrying both rows, because the upgrade route narrows to linked callers; and the fake
store's issued row binds by the identity row id rather than by the pre-auth pair, because that is what
the real store's issuance does for a linked caller.

Each case asserts three things, the way the analog does: the client answer, the internal result the
spy recorded (the snake-cased class name), and the disposition — whether the row was claimed, whether
it was consumed, how many times consume was called, and what the provider fake's call list holds. The
pre-claim cases assert an empty call list, which is what proves the rejection happened before the
provider was reached.

Assert the rejection body by equality against the one-field dictionary, never by checking that a key
is present.

Three-line docstring maximum, one-line comments only.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_upgrade_precedence.py -q</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_upgrade_precedence.py -q` passes and collects at least nine cases.
    - `uv run pytest tests/unit/test_upgrade_precedence.py -q --collect-only | grep -c "test_"` returns a non-zero count, so the file is not vacuous.
    - Every pre-claim case asserts the provider fake's call list is empty; every at-or-after case asserts the row was consumed. Both facts are checkable by reading the file and are stated per case in the summary.
    - `uv run pytest -q` passes with a non-zero collected count.
    - `uv run ruff check src tests` exits 0 and `uv run pytest tests/unit/test_docstring_bar.py -q` passes.
  </acceptance_criteria>
  <done>Every rejection's precedence and every branch's handle disposition is pinned, and exactly one provider read per completion is a measured fact.</done>
</task>

<task type="auto">
  <name>Task 3: The four end-to-end cases the real account cannot produce on demand</name>
  <precondition>A live PostgreSQL 17 carrying the re-applied schema is reachable at the `DB_*` values in `.env`.</precondition>
  <files>tests/e2e/test_upgrade_anonymous.py</files>
  <read_first>
    - `tests/e2e/test_upgrade_anonymous.py` as plans 40-03 and 40-04 leave it — the credential canary and the happy-path class whose seeding and request shape these cases reuse
    - `tests/e2e/conftest.py` :153-194 — `scripted_firebase_adapter` and `seed_identity`, whose derived provider_uid is exactly the handle a second seeded identity needs to pre-reserve the target triple
    - `src/nativespeaker/api/auth/adapters.py` — `VerifiedProviderIdentity`, the value the fake's `script()` takes
    - `tests/e2e/test_create_user.py` :61-110 — the scripted-fake case shape, including reading rows back through the test transaction after the response
    - `.planning/phases/40-post-auth-upgrade-anonymous/40-CONTEXT.md` § D-19, D-04
  </read_first>
  <action>
Add the four cases D-19 assigns to the fake, each end to end through the real router:

The **not-yet-linked** refusal — a stored anonymous row and a scripted anonymous answer. Assert 403,
the one-field body, and that the identity row is untouched afterwards.

The **drift** conflict — a stored registered row and a scripted answer that disagrees with it. Assert
403, the same body, and that the stored provider and provider_uid are byte-identical afterwards to
what they were before the call. This is the case that proves a divergent binding is refused rather
than rewritten.

The **already-taken** conflict — seed a second identity for a different subject whose provider_uid is
the value the scripted answer will report, then upgrade the first. Assert 403, the same body, and that
neither identity row moved. Assert also that the challenge handle was spent, because this failure is
at the write and therefore after the provider call.

The **idempotent repeat** — run the happy path, then run it again with a fresh handle and the same
scripted answer. Assert the second call returns 200 with a body equal to the first's, that the
identity row id and every column the flip writes are unchanged between the two, and that the provider
fake was called once per completion rather than once in total.

Assert every refusal body by equality against the same one-field dictionary, so the three refusals
being indistinguishable to a client is asserted rather than assumed.

Three-line docstring maximum, one-line comments only.
  </action>
  <verify>
    <automated>uv run pytest -m e2e tests/e2e/test_upgrade_anonymous.py -q</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -m e2e tests/e2e/test_upgrade_anonymous.py -q` passes with zero skipped and collects the credential canary, the happy path and these four cases.
    - The drift case and the already-taken case each read the stored provider and provider_uid before and after the call and assert equality, so "nothing was rewritten" is measured.
    - The idempotent case asserts the two response bodies are equal by `==` and that the provider fake's call list length equals the number of completions.
    - `uv run pytest -q` passes with a non-zero collected count.
    - `uv run ruff check src tests` exits 0 and `uv run pytest tests/unit/test_docstring_bar.py -q` passes.
  </acceptance_criteria>
  <done>The three refusals and the repeat are proven end to end against the scripted seam, with the untouched-row property measured rather than argued.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `POST /auth/upgrade-anonymous` | a caller who holds a valid token for an existing linked account can drive every branch of this matrix at will |
| server → client | three internal results collapse to one answer here; what the answer discriminates is the whole question |
| server → structured log | the two new refusals put the identity row id and two provider names into a retained line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-05-01 | Information Disclosure | the three refusals' client answer | high | mitigate | Distinguishable refusals would let any token holder probe which provider accounts are already held. All three answer 403 with the same one-field body, declared once on a shared base, and the e2e and unit cases assert the body by equality rather than by key presence, so a more helpful field fails a test |
| T-40-05-02 | Spoofing | challenge replay to probe provider state | medium | mitigate | The handle is spent on every outcome at or after the provider call including the not-linked refusal, with no branch on which outcome fired (D-14); Tests 5-7 in the unit file assert the disposition per branch rather than assuming it |
| T-40-05-03 | Tampering | a diverged stored-versus-live binding | high | mitigate | The only permitted outcomes are the flip, the no-op and a refusal; the drift cases read the stored provider and uid before and after and assert equality, so an auto-rewrite would fail a test rather than pass silently |
| T-40-05-04 | Elevation of Privilege | claiming a provider account another user holds | high | mitigate | The partial unique index refuses the write and the conflict arm converts it, with no constraint name compared and no message parsed; the already-taken e2e case drives it through a genuinely pre-reserved triple rather than a simulated error |
| T-40-05-05 | Denial of Service | unbounded provider lookups from a looping client | medium | accept | Every completion makes one Firebase read, including a repeat that changes nothing, and no rate limiting exists in the product. Accepted and flagged by D-22 and recorded in `REQUIREMENTS.md` by plan 40-08. Mitigating facts: the caller must already hold a valid token for an existing linked account, so this is one subject looping on itself rather than a fan-out, and the exposure closes when the Envoy gateway contract lands in v2.1 |
| T-40-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not reachable: this plan installs nothing. `40-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; `pyproject.toml` is not in `files_modified` |
</threat_model>

<verification>
- `uv run pytest -q` green with a non-zero collected count, including the new unit file
- `uv run pytest -m e2e tests/e2e/test_upgrade_anonymous.py -q` green with zero skipped
- `uv run ruff check src tests` exits 0
- no placeholder raise and no refusal-site logger call survives in the service
</verification>

<success_criteria>
Every state a caller can put this endpoint into reaches a named outcome; the three refusals are one
answer to the client and three records in the log; and no refusal leaves a row changed.
</success_criteria>

<output>
Create `.planning/phases/40-post-auth-upgrade-anonymous/40-05-SUMMARY.md` when done
</output>
