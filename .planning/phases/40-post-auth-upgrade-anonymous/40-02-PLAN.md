---
phase: 40-post-auth-upgrade-anonymous
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nativespeaker/api/errors.py
  - src/nativespeaker/api/schemas/auth.py
  - src/nativespeaker/api/routers/auth.py
  - tests/unit/test_rejection_vocabulary.py
  - tests/unit/test_create_user_body.py
autonomous: true
requirements: [UPGRADE-01, UPGRADE-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All three upgrade refusals answer one identical client body — 403 with exactly {\"code\": \"operation_not_allowed\"} — because the status and code are declared once on a shared base and no leaf overrides either (D-02, D-05)"
    - "The two new refusals produce two distinct structured-log event names, provider_transition_not_allowed and provider_account_already_linked, derived from the class names by the existing handler and not from any added field (D-05)"
    - "Each new refusal carries the identity row id and the stored and live provider names into its log line as plain strings, and carries nothing else (D-06)"
    - "Both new refusals log at the AppError default WARNING level, with no log_level override anywhere in the new classes (D-07)"
    - "One request model serves both /auth/create-user and, from plan 40-04, /auth/upgrade-anonymous — there is no second model carrying the same shape (D-03)"
    - "ErrorCode and ErrorResponse are unchanged: no new client-visible error code is introduced and the response body stays exactly one field"
  prohibitions:
    - "The provider account uid — the user's real identifier at Google or Apple — must never be admissible into any structured-log field these classes emit, and no constructor parameter may carry it."
  # Prohibition-probe breadcrumbs, dropped as canon security rather than minted here:
  #   enumeration oracle across refusal bodies (OWASP information disclosure) — carried structurally by D-02's shared base
  #   challenge handle leaking into a log line (secret-in-logs) — carried by the store holding no logger at all
  artifacts:
    - path: "src/nativespeaker/api/errors.py"
      provides: "UpgradeRefused and its two leaves ProviderTransitionNotAllowed and ProviderAccountAlreadyLinked"
      contains: "class UpgradeRefused"
    - path: "src/nativespeaker/api/schemas/auth.py"
      provides: "CompletionRequest, the completion body both routes take"
      contains: "class CompletionRequest"
    - path: "tests/unit/test_rejection_vocabulary.py"
      provides: "The three new names in EVENT_NAMES, their CONSTRUCTOR_ARGUMENTS rows, and the group test for the new base"
      contains: "upgrade_refused"
  key_links:
    - from: "src/nativespeaker/api/errors.py"
      to: "src/nativespeaker/api/app/error_handlers.py"
      via: "camel_to_snake(type(exc).__name__) is the structured-log event name, so the class name is the internal result"
      pattern: "camel_to_snake"
    - from: "src/nativespeaker/api/routers/auth.py"
      to: "src/nativespeaker/api/schemas/auth.py"
      via: "the create-user handler's body annotation, retargeted to the renamed model"
      pattern: "CompletionRequest"
---

<objective>
Land the vocabulary the upgrade's case matrix raises into, and collapse the two identical completion
request bodies into one model — both before anything has a caller.

Purpose: D-05 makes the class name the internal result, so the classes must exist and be in the
written-down vocabulary before plan 40-04's service can raise them. D-03 is the same argument in the
other direction: two models that must stay identical are one model, and Phases 41 and 42 are
challenge-bearing completions that will want the same shape.
Output: `UpgradeRefused` with its two leaves in `errors.py`, `CompletionRequest` in
`schemas/auth.py`, and the vocabulary ratchets in `tests/unit/test_rejection_vocabulary.py` moved
with them.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-post-auth-upgrade-anonymous/40-CONTEXT.md
@.planning/phases/40-post-auth-upgrade-anonymous/40-RESEARCH.md
@.planning/phases/40-post-auth-upgrade-anonymous/40-PATTERNS.md
@AGENTS.md
</context>

## Artifacts this phase produces

This plan creates `CompletionRequest`, `UpgradeRefused`, `ProviderTransitionNotAllowed` and
`ProviderAccountAlreadyLinked`, and retires the name `CreateUserRequest`. The full phase inventory is
in `40-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The two upgrade refusals under one shared 403 base</name>
  <files>src/nativespeaker/api/errors.py, tests/unit/test_rejection_vocabulary.py</files>
  <read_first>
    - `src/nativespeaker/api/errors.py` :12-33 — `ErrorCode` and `ErrorResponse`, neither of which changes; :35-53 — `AppError`, its fail-closed 500 default and its `WARNING` `log_level`; :294-330 — `PreAuthIdentityNotAllowed` and the `AccountUnavailable` base-plus-two-leaves shape this task copies; :345-380 — `ProviderLookupError`'s constructor-carried scalars and its absent-key-rather-than-None rule, plus `NotLinked` declaring 403 `operation_not_allowed`; :386-391 — `ChallengeRejected` declaring its 409 once with five silent leaves
    - `src/nativespeaker/api/errors.py` :299-301 — `InvalidExternalJwt.log_fields`, the in-repo way a non-`str` value is stringified into a log field
    - `src/nativespeaker/api/app/error_handlers.py` — `camel_to_snake` and `app_error_handler`, which is why the class name *is* the event name and why no `logger.warning` call is written for a refusal
    - `tests/unit/test_rejection_vocabulary.py` :28-97 (`EVENT_NAMES` and the exact-set assertion), :105-127 (`CONSTRUCTOR_ARGUMENTS` and `_sample`), :130-144 (the scalars-only gate), :203-215 (`TestTheTwoAccountArmsDeclareNothingEither`, the group test to mirror)
    - `tests/unit/error_tree.py` — the totality walk requiring `status` and `code` declared together or neither, and forbidding one code at two statuses
    - `src/nativespeaker/api/tables/identities.py` :11-16 — `IdentityProvider`, the closed set both provider fields come from
    - `.planning/phases/40-post-auth-upgrade-anonymous/40-CONTEXT.md` § D-02, D-05, D-06, D-07
    - `.planning/phases/40-post-auth-upgrade-anonymous/40-RESEARCH.md` § Common Pitfalls 3
  </read_first>
  <behavior>
    - Test 1: `EVENT_NAMES` gains exactly three entries — `upgrade_refused`, `provider_transition_not_allowed`, `provider_account_already_linked` — and `test_the_tree_spells_exactly_the_recorded_event_names` passes. A base nothing raises still appears in the set, exactly as `provider_lookup_error` and `challenge_rejected` already do.
    - Test 2: both leaves answer `(403, "operation_not_allowed")`, and neither declares `status` nor `code` in its own `vars()` — the pair is declared once on the base, so making one leaf answer differently would take an override a reviewer sees. Mirror `TestTheTwoAccountArmsDeclareNothingEither`.
    - Test 3: the family under the new base is exactly those two leaves, so a third arm added later without coming here is a rejection nobody checked the answer of.
    - Test 4: `log_fields()` on either leaf returns exactly three keys — the identity row id and the two provider names — every value a `str`. Assert the exact dict, not a superset.
    - Test 5: the constructor admits no provider-account identifier — asserted by inspecting the signature's parameter names rather than by hoping none is passed.
    - Test 6: `camel_to_snake` of each leaf's name equals the internal result name the brief requires, asserted as two literal strings.
    - Test 7 (control): the two leaf event names are distinct from each other and from `not_linked`, so the three refusals really are three records in the log and one answer to the client.
  </behavior>
  <action>
Append one base and two leaves to `src/nativespeaker/api/errors.py`, in the lookup-arms neighbourhood
where `NotLinked` already lives.

The base is `UpgradeRefused(AppError)`. It declares `status = 403` and `code = "operation_not_allowed"`
once. That code already exists in the `ErrorCode` literal, so `ErrorCode` and `ErrorResponse` are not
touched and no new client-visible code appears. Its `__init__` is keyword-only and takes exactly
`identity_row_id`, `stored_provider` and `live_provider`; copy `ProviderLookupError`'s body shape —
assign the three, then call `super().__init__` with a short message built from the class name.

`log_fields()` returns those three under the keys `identity_row_id`, `stored_provider` and
`live_provider`, each value passed through `str()`. The row id is a `UUID`; the two provider values
are `IdentityProvider` members, which are `StrEnum` and therefore already `str`, but `str()` is the
convention this repository uses at this seam and it keeps the field's type in the log pipeline
unambiguous. The two provider values are members of a closed three-value enum, never free text from
Firebase.

The leaves are `ProviderTransitionNotAllowed(UpgradeRefused)` and
`ProviderAccountAlreadyLinked(UpgradeRefused)`. Each carries a one-line docstring naming which of
D-05's three cases it is and declares nothing else — no `status`, no `code`, no `log_level`. D-07's
"all three at WARNING" is satisfied by `AppError`'s default; overriding it here would be a second
convention.

Then extend `tests/unit/test_rejection_vocabulary.py`: add the three snake-cased names to
`EVENT_NAMES`, add one `CONSTRUCTOR_ARGUMENTS` row per class that `_sample` can build (the base
included, since the exact-set walk instantiates every class in the tree), and add a group test
mirroring `TestTheTwoAccountArmsDeclareNothingEither` for the new base. Write the seven behaviours
above as cases in that file; the log-field case asserts the exact three-key dict.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_rejection_vocabulary.py tests/unit/test_error_registry.py tests/unit/test_error_contract.py -q</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` passes with a non-zero collected count.
    - `uv run python -c "from nativespeaker.api.errors import ProviderTransitionNotAllowed as P, ProviderAccountAlreadyLinked as Q; print(P.status, P.code, Q.status, Q.code)"` prints `403 operation_not_allowed 403 operation_not_allowed`.
    - `uv run python -c "from nativespeaker.api import errors; print('status' in vars(errors.ProviderTransitionNotAllowed), 'code' in vars(errors.ProviderAccountAlreadyLinked), 'log_level' in vars(errors.UpgradeRefused))"` prints `False False False`.
    - `uv run python -c "import inspect; from nativespeaker.api.errors import UpgradeRefused as U; print(sorted(p for p in inspect.signature(U.__init__).parameters if p != 'self'))"` prints exactly `['identity_row_id', 'live_provider', 'stored_provider']`.
    - `grep -c "provider_uid" src/nativespeaker/api/errors.py` returns `0`.
    - `git diff src/nativespeaker/api/errors.py` shows no line touching `ErrorCode` or `ErrorResponse`.
    - `uv run ruff check src tests` exits 0, and `uv run pytest tests/unit/test_docstring_bar.py -q` passes.
  </acceptance_criteria>
  <done>The tree carries one new 403 base and two silent leaves; the vocabulary ratchets name all three; the log line of either leaf is exactly three stringified scalars.</done>
</task>

<task type="auto">
  <name>Task 2: One completion request model for both routes</name>
  <reversibility rating="reversible">A rename behind a single import site inside one repository with no published clients; undoing it is one editor operation.</reversibility>
  <files>src/nativespeaker/api/schemas/auth.py, src/nativespeaker/api/routers/auth.py, tests/unit/test_create_user_body.py</files>
  <read_first>
    - `src/nativespeaker/api/schemas/auth.py` :13-33 — `ChallengeRequest`, `CreateUserRequest` with its two comments and its `min_length=1`, and `CompletionResponse`
    - `src/nativespeaker/api/routers/auth.py` :20-27 (the schema import block) and :62-73 (the create-user handler whose body annotation moves)
    - `tests/unit/test_create_user_body.py` — every reference to the model by name, and what each case asserts about the 422 an unusable handle earns
    - `.planning/phases/40-post-auth-upgrade-anonymous/40-CONTEXT.md` § D-03
  </read_first>
  <action>
Rename `CreateUserRequest` to `CompletionRequest` in `src/nativespeaker/api/schemas/auth.py`, keeping
the class body, its docstring and both comments byte-identical — the `min_length=1` and the reason
for it are what make an unusable handle the framework's 422 rather than a not-found 409, and none of
that changes. Do not add a second model, do not alias the old name, and do not leave a backwards-
compatible re-export: a name kept alive for nobody is the duplication D-03 exists to remove.

Update the two importers: the schema import block and the body annotation in `routers/auth.py`, and
every reference in `tests/unit/test_create_user_body.py`. Nothing else in either file changes — this
task moves a name and no behaviour, so no status code, body literal or assertion may move with it.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_create_user_body.py tests/unit/test_create_user_precedence.py tests/unit/test_challenge_endpoint.py -q</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "CreateUserRequest" src tests` returns nothing.
    - `uv run python -c "from nativespeaker.api.schemas.auth import CompletionRequest; print(CompletionRequest.model_fields['challenge_id'].metadata)"` shows the `min_length=1` constraint surviving the rename.
    - `uv run pytest -q` passes with a non-zero collected count.
    - `git diff --stat` lists exactly the three files this task names.
    - `uv run ruff check src tests` exits 0.
  </acceptance_criteria>
  <done>One completion body model exists, both the create-user route and plan 40-04's upgrade route can take it, and no test assertion moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → structured log pipeline | the two new refusals put fields into a log line that is shipped and retained wherever the deployment sends logs |
| server → client | the refusal's status, code and body cross here; three internal results collapse to one answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-02-01 | Information Disclosure | `UpgradeRefused.log_fields` | medium | mitigate | D-06 excludes the provider account uid — the user's real identifier at Google or Apple — from the log line entirely. The constructor admits no such parameter, asserted by a signature check, and the two provider values come from the closed three-member `IdentityProvider` enum rather than from Firebase text |
| T-40-02-02 | Information Disclosure | the three refusals' client answer | high | mitigate | A body that differed between "not linked yet", "drift" and "already taken" would let any token holder probe which provider accounts are held. The status and code are declared once on the shared base and no leaf overrides either, so the anti-oracle property is structural rather than asserted; `ErrorResponse` stays exactly one field |
| T-40-02-03 | Tampering | the error tree's totality | medium | mitigate | A class declaring neither `status` nor `code` falls into `AppError`'s 500 default, so a forgotten declaration surfaces as an internal error rather than a refusal. `tests/unit/error_tree.py`'s walk plus the re-baselined `EVENT_NAMES` exact-set assertion make both the omission and the addition visible |
| T-40-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not reachable: this plan installs nothing. `40-RESEARCH.md` § Package Legitimacy Audit records "Not applicable"; `pyproject.toml` is not in `files_modified` |
</threat_model>

<verification>
- `uv run pytest -q` green with a non-zero collected count
- `uv run ruff check src tests` exits 0
- `tests/unit/test_docstring_bar.py` still measures 0 on every root
- no reference to the old request-model name survives anywhere in `src` or `tests`
</verification>

<success_criteria>
The upgrade's two new internal results exist as raisable classes with distinct log-event names and one
identical client answer, and a single request model carries the completion body for every
challenge-bearing route this milestone will add.
</success_criteria>

<output>
Create `.planning/phases/40-post-auth-upgrade-anonymous/40-02-SUMMARY.md` when done
</output>
