---
phase: 42-post-auth-claim-registered-grant
plan: 06
type: execute
wave: 4
depends_on: ["42-03", "42-04", "42-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REGGRANT-01, REGGRANT-02, REGGRANT-03]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every decision this phase made that differs from `07-claim-registered-grant.md` or `06-schema-reference.md` is recorded under the REGGRANT requirement it belongs to, as a dated entry naming what the brief asks and what shipped — the device gate skipped on conversion, the single device token, the registered-only claimant with no live provider confirmation, the account uniqueness resting on existing indexes, the schema deletion, and the refusal carrying no end date (D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-14)"
    - "`07-claim-registered-grant.md` and `06-schema-reference.md` are not edited by any task in this phase: a divergence is recorded in the ledger, never resolved by rewriting the specification (D-15)"
    - "The obligations that were already dead before this phase began are inventoried by line, so a later reader does not read them as obligations this phase failed to meet — the rate limits and vendor budgets, the audit row, the mode signal, the attempt-id consumption condition, the keyring and the two provider-confirmation points (D-14)"
    - "The unbounded Apple exposure on this route is recorded as accepted and flagged, on the Phase 41 D-20 and Phase 40 D-22 precedent, with the mitigating facts and what closes it (D-16)"
    - "Every prior entry describing Phase 41's anti-abuse row — under ANONGRANT and in the STATE.md decision list — is amended so a later reader does not look for a table that no longer exists (D-07, D-14)"
    - "The header's flagged-conflict count and the set of known divergences are re-derived against the named sections rather than inherited, and the gap between the two numbers is enumerated rather than reconciled away (D-14)"
    - "REGGRANT-01, REGGRANT-02 and REGGRANT-03 are marked met against named, green suite runs with their counts recorded, and the ROADMAP phase entry, its plan list and the progress table agree with what shipped"
  prohibitions:
    - "A divergence from the binding specification must never be resolved by editing the specification; the brief and the invariants stay verbatim and the divergence is recorded where a later reader hits it."
    - "A count in the ledger must never be inherited from the previous phase without re-deriving it against the sections it summarises; an inherited number reads as evidence and is not."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The dated REGGRANT amendments, the amended ANONGRANT entries, the re-derived counts and the three marked requirements"
      contains: "REGGRANT-01"
    - path: ".planning/STATE.md"
      provides: "The Phase 42 outcome, the phase's decisions, and the corrected Phase 41 entries describing the deleted receipt row"
      contains: "Phase 42"
    - path: ".planning/ROADMAP.md"
      provides: "The Phase 42 entry with its plan list, its success-criteria dispositions and its row in the progress table"
      contains: "Phase 42"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "each amended requirement names the ROADMAP criterion it answers, and each criterion names its matching requirement"
      pattern: "REGGRANT"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/STATE.md"
      via: "the ledger's amendment of the receipt row and the STATE decision list describing it must not disagree"
      pattern: "ANONGRANT"
---

<objective>
Close the phase in the ledger: record every divergence under the requirement it belongs to, correct
every prior entry that describes a table this phase deleted, re-derive the counts, and mark the three
requirements met against named green runs.

Purpose: this milestone's standing convention is that the specification stays verbatim and the
divergence is written where a later reader hits it. Six decisions in this phase differ from
`07-claim-registered-grant.md` or `06-schema-reference.md`, and one of them — the schema deletion —
also falsifies entries Phase 41 wrote. A reader who finds the brief before the ledger must find the
divergence, not discover it.

Output: an amended `REQUIREMENTS.md`, an amended `STATE.md`, and a `ROADMAP.md` whose Phase 42 entry
matches what shipped.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-post-auth-claim-registered-grant/42-CONTEXT.md
@.planning/phases/41-post-auth-claim-anonymous-grant/41-LEARNINGS.md
@AGENTS.md
</context>

## Artifacts this phase produces

Repeated in every plan of this phase so a drift check reading any one of them knows these symbols are
created here. Full text: see plan `42-01-PLAN.md` § "Artifacts this phase produces". This plan
produces no code symbol; it produces dated entries in three planning documents.

**Created by plans 42-01 … 42-05** — `ClaimantNotRegistered`; `GrantClaimRequest`, replacing
`AnonymousGrantClaimRequest` by rename; `REGISTERED_TIER_ID`;
`AuthService.complete_claim_registered_grant`; `AuthService._claim_registered_grant`;
`GrantsDB.activate_registered_account_grant`; the `claim_registered_grant` route; the modules
`tests/e2e/test_claim_registered_grant.py` and `tests/unit/test_claim_precedence_registered.py`; and
new classes in `tests/schema/test_grant_locks.py`, `tests/schema/test_claim_race.py`,
`tests/unit/test_grant_sources.py`, `tests/unit/test_claim_ordering.py`,
`tests/unit/test_app_wiring.py` and `tests/unit/test_rejection_vocabulary.py`.

**Removed by plan 42-01** — the SQL objects `core.access_grants_anti_abuse`,
`core.provider_accounts`, `core.provider_account_gate_consumptions`, `core.gate_consumption_kind` and
everything only they needed; the Python model `AccessGrantAntiAbuse`; and the `with_anti_abuse`
parameter of the e2e `seed_grant` fixture.

<tasks>

<task type="auto">
  <name>Task 1: The dated REGGRANT amendments and the inventory of already-dead obligations</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `42-CONTEXT.md` § D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-14, D-15, D-16 and § "Carried forward — decided earlier, binding here, do NOT rebuild"
    - `/home/init/native-speaker/specs/auth-refactor-phases/07-claim-registered-grant.md` — the brief, read for the exact line numbers each divergence is against; **not edited**
    - `/home/init/native-speaker/specs/auth-refactor/06-schema-reference.md` § the three deleted tables — read for the line numbers; **not edited**
    - `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` § "Identity and ownership", § "The barrier", § "Fail-closed defaults", § "Locks and transactions", § "Grants and evaluation time", § "Global deletions" — re-read section by section to determine whether any of them, and not just the brief, is diverged from
    - `.planning/REQUIREMENTS.md` § the header amendment block, § REGGRANT, and § ANONGRANT — the Phase 41 amendment is the model for form, length and tone
    - `41-LEARNINGS.md` § Decisions "Record the divergence in the ledger; leave the specification verbatim" and "Resolved-by-precedence is a third ledger category, not a softer conflict"
    - the summaries of plans 42-01 … 42-05 — the shipped behaviour each amendment describes must be what actually shipped, not what was planned
  </read_first>
  <action>
Append a dated Phase 42 amendment to `.planning/REQUIREMENTS.md`, following the form and the tone of
the Phase 41 amendment immediately above it: a header block summarising the phase, then per-requirement
entries under REGGRANT-01, REGGRANT-02 and REGGRANT-03.

**Record six divergences as flagged conflicts, each under the requirement it belongs to**, each
naming what the brief asks, what shipped, and why:
- the device gate is skipped entirely on the conversion path (D-02), against the brief's steps that
  gate every claim on this platform;
- the body carries one device token rather than a separate query token and update token (D-04),
  the same collapse Phase 41 recorded for its own route after two independent tokens proved
  unbindable to one device;
- claimants are registered identities read from the stored row, with no live provider confirmation
  and no `verification_required` code (D-05), against the brief's steps requiring both;
- account uniqueness rests on the indexes the schema already carries and no `account_already_claimed`
  code is added (D-06), against the brief's uniqueness hardening;
- three tables and everything only they need leave the single migration (D-07), against
  `06-schema-reference.md` and the brief's steps that write them, with D-08's refusal of any hash or
  key recorded in the same entry;
- the refusal for an account holding another active grant carries no end date (D-09(b)), against the
  brief's field.

**Record the Apple exposure as accepted and flagged, not counted as a conflict** (D-16), on the Phase
41 D-20 and Phase 40 D-22 precedent: an eligible registered account can make the backend call Apple
as often as it can prepare challenges; mitigating, the preflight refuses an ineligible account before
Apple is reached and each turn costs a fresh challenge; it closes with the v2.1 gateway contract.
State plainly that this route's exposure is narrower than the anonymous route's, because the
conversion destination reaches Apple not at all.

**Inventory the obligations that were already dead before this phase began**, by line where the brief
states them, so a later reader does not read them as obligations this phase failed to meet: the
rate-limit entries and vendor budgets (Phase 35 D-05), the audit row (Phase 37.1 D-01 with Phase 38
D-03), the mode signal and its classifier (Phase 37.2), `claim_attempt_id` (Phase 37.4 D-03), the
HMAC keyring (Phase 37.4 D-11, and D-08 here), the route registry (Phase 37.1 D-06), and the two
Firebase confirmation points (Phase 41 D-08 and D-05 here). This is the same service Phase 41's
ANONGRANT-01 entry performs, and it is what stops the next reader re-litigating settled deletions.

**Re-derive rather than inherit.** Walk the six named `SHARED-INVARIANTS.md` sections and state
whether each produced a divergence or none — a null result is worth stating, and Phase 41's
re-derivation produced exactly one. Then update the header's flagged-conflict count and the set of
known divergences, showing the arithmetic: the count rises by the number of new flagged conflicts,
the divergence set rises by that number plus the uncounted items this phase adds. Enumerate the gap
between the two numbers rather than reconciling it away — it is the difference between a counted
conflict and a recorded override, forward flag or precedence resolution, and it has been kept
deliberately since Phase 37.5.

**Do not edit `07-claim-registered-grant.md`, `06-schema-reference.md` or `SHARED-INVARIANTS.md`**
(D-15). If a passage in the invariants names an anti-abuse row, the divergence goes in this file and
the invariant text stays verbatim.

Write in Simplified Technical English: short sentences, one idea each, the same word for the same
thing, no metaphors.
  </action>
  <verify>
    <automated>git -C /home/init/native-speaker/ns-api-gateway status --porcelain -- .planning/REQUIREMENTS.md</automated>
    <fails_when>the output is empty — that means the ledger was not amended at all</fails_when>
    <automated>grep -c "2026-09-0" .planning/REQUIREMENTS.md</automated>
    <fails_when>a count no greater than the pre-task count, which would mean no dated entry was added</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or fewer passed than after plan 42-05, or any error during collection</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` § REGGRANT holds a dated Phase 42 entry under each of REGGRANT-01, REGGRANT-02 and REGGRANT-03.
    - Six divergences are recorded as flagged conflicts, one per decision named in the action, each stating what the brief asks and what shipped.
    - The Apple exposure entry exists, is marked accepted and flagged rather than counted as a conflict, and names what closes it.
    - The already-dead obligations are inventoried with the deleting decision named for each.
    - The header's flagged-conflict count and divergence-set size are updated, the arithmetic is shown, and the gap between the two numbers is enumerated item by item.
    - The six `SHARED-INVARIANTS.md` sections are each named with a verdict of "diverged" or "none".
    - `git -C /home/init/native-speaker status --porcelain -- specs/` is empty — no file under the specification directory was modified by this task.
    - `grep -c "held_grant_ends_at" .planning/REQUIREMENTS.md` returns at least `1` — the declined field is recorded rather than silently absent.
    - Every sentence added is one idea, and no metaphor appears in the added text.
  </acceptance_criteria>
  <done>Every difference between what `07-claim-registered-grant.md` asks for and what this phase shipped is written under the requirement it belongs to, with its date and its reason, and the specification is byte-identical to what it was before the phase began.</done>
</task>

<task type="auto">
  <name>Task 2: Correct every prior entry that describes the deleted receipt row</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `42-CONTEXT.md` § D-07 and D-14 — the instruction to amend the ANONGRANT entries and any STATE decision that describes Phase 41's anti-abuse row
    - `.planning/REQUIREMENTS.md` § ANONGRANT in full — the Phase 41 entries, including the four-row description of what one successful claim writes
    - `.planning/STATE.md` § "Phase 41 outcome" and § Decisions — the entries naming the receipt row, including the one describing the four rows written in one flush
    - the summary of plan 42-01 — what was actually deleted and what survived, including the surviving record of the device platform
    - `41-LEARNINGS.md` § Decisions — the Phase 41 entries that are still true and must not be rewritten
  </read_first>
  <action>
Find every prior entry that tells a reader to look for a table this phase deleted, and correct it in
place with a dated note rather than by rewriting history.

**In `.planning/REQUIREMENTS.md` § ANONGRANT.** Phase 41's entries describe one successful anonymous
claim as writing four rows, one of them the receipt row carrying the device platform with both hash
columns NULL. Add a dated Phase 42 note under the entry that says so: the receipt row and its table
were deleted by Phase 42 D-07, one successful claim now writes three rows, and the device platform is
recorded on the identity row's platform column, which survives. Keep the original text as written —
it records what Phase 41 delivered and was true until this date — and put the correction beside it,
which is the treatment this file has given every superseded claim since Phase 37.1.

**In `.planning/STATE.md`.** The Phase 41 outcome paragraph says four rows go in one flush and names
the receipt row among them; the decision list carries entries on the same subject. Correct each with
the same dated note. Do not delete a decision entry: a decision that was made and later superseded is
part of the record.

**Search rather than rely on this list.** Grep the planning directory for the deleted table's name
and for the model's name, and correct every hit outside the Phase 42 directory and outside archived
milestone files. Report the full list of hits found and the disposition of each — corrected, or left
because it is a historical artifact of a completed phase whose own summary must stay as written.

Write in Simplified Technical English.
  </action>
  <verify>
    <automated>grep -rln "access_grants_anti_abuse\|AccessGrantAntiAbuse" .planning --include=*.md</automated>
    <fails_when>the output names any file outside .planning/phases/41-*, .planning/phases/42-*, .planning/milestones/ and the two ledger files whose hits are dated corrections — a bare mention anywhere else is an uncorrected pointer to a table that no longer exists</fails_when>
    <automated>git -C /home/init/native-speaker/ns-api-gateway status --porcelain -- .planning/STATE.md</automated>
    <fails_when>the output is empty — that means the state file was not amended at all</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` § ANONGRANT carries a dated Phase 42 note stating that one successful anonymous claim now writes three rows and where the device platform is recorded instead.
    - `.planning/STATE.md`'s Phase 41 outcome paragraph and every decision entry naming the receipt row carry the same dated correction, and no decision entry was deleted.
    - The summary lists every hit the planning-directory grep found, with the disposition of each and the reason for any left uncorrected.
    - `git -C /home/init/native-speaker/ns-api-gateway diff -- .planning/phases/41-post-auth-claim-anonymous-grant/` is empty — Phase 41's own summaries are historical artifacts and are left as written.
    - No sentence added contradicts what Phase 41 delivered; each states what changed and when.
  </acceptance_criteria>
  <done>No entry in the active planning documents sends a reader looking for a table this phase deleted, and Phase 41's own record of what it delivered is intact beside the correction.</done>
</task>

<task type="auto">
  <name>Task 3: Mark the three requirements met and close the phase in the roadmap</name>
  <precondition>Plans 42-01 through 42-05 are all committed, and PostgreSQL is reachable so that the three suite runs quoted in the ledger can actually be made rather than copied.</precondition>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `41-LEARNINGS.md` § Lessons "`requirements.mark-complete` cannot address a range traceability row" — the row is a range the tool does not expand, and Phase 42 will hit the identical result on its own three IDs; and § Lessons "The shared-ID requirement gate holds IDs until the last declaring plan lands"; and § Patterns "Verify the state before acting on what the plan predicted"
    - `.planning/ROADMAP.md` § Phase 42 — the goal, the four success criteria and the progress table row
    - `.planning/REQUIREMENTS.md` § REGGRANT and the traceability table row covering the three IDs
    - the summaries of plans 42-01 … 42-05 — the source of the counts and the outcomes quoted here
    - `.planning/STATE.md` — the current position block, the progress counters, and the performance-metrics table
  </read_first>
  <action>
Close the phase against measured facts rather than predicted ones.

**Run the three suites and record the numbers.** `uv run pytest -q`, `uv run pytest -m e2e -q` and
`uv run pytest -m schema -q`, plus `uv run ruff check src tests`. Quote the actual passed counts in
the ledger. Do not copy a count from a plan or a summary — the number in the ledger is the number
observed at this moment.

**Mark REGGRANT-01, REGGRANT-02 and REGGRANT-03 met**, each with the evidence that meets it named:
the single-writer walk and the route for -01, the emitted-SQL lock and order proofs and the
conversion race for -02, the history-by-source guard, the precedence matrix and both races for -03.
Expect the requirements tool to report the traceability row as unmatched: that row is written as a
range covering all three IDs, and the tool does not expand a range. That is a known result, not a
failure — verify it, then edit the row by hand as every prior endpoint phase did, and report what the
tool returned rather than writing what this plan predicted.

**Update `.planning/ROADMAP.md` § Phase 42.** Fill in the plan count and the plan list with each
plan's one-line objective and its wave. Give each of the four success criteria a disposition: met, or
met-with-a-reword naming what moved and why. Criterion 1 and criterion 4 are the ones to check most
carefully — the first because "across prepare and completion modes" describes a mode partition this
project replaced with a dedicated issuance route in Phase 37.2, and the second because the account
holding an active anonymous grant is converted rather than refused, which is a different shape of
"does not receive a second free entitlement" than the criterion's wording implies. Reword rather than
withdraw where the property still holds, following the treatment Phase 40 and Phase 41 gave their own
criteria, and record the reword under the matching requirement as well. Update the progress table row
for Phase 42.

**Update `.planning/STATE.md`.** Write the Phase 42 outcome paragraph in the form the prior phases
use: what shipped, the suite counts, and what is recorded as accepted rather than fixed. Add this
phase's decisions to the decision list. Move the current position forward. Read the plan and summary
counts off disk rather than incrementing the stored counters — the counters have been wrong twice
before for exactly that reason, and disk is truth.

Write in Simplified Technical English.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or any failure</fails_when>
    <automated>uv run pytest -m e2e -q</automated>
    <fails_when>non-zero exit, or "0 passed" or "no tests ran" in the summary line, or any skipped case</fails_when>
    <automated>uv run pytest -m schema -q</automated>
    <fails_when>non-zero exit, or "0 passed" or "no tests ran" in the summary line</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any line matching "^src/" or "^tests/" in the output</fails_when>
    <automated>grep -c "REGGRANT" .planning/ROADMAP.md</automated>
    <fails_when>a count of 0, which would mean the roadmap phase entry lost its requirement reference</fails_when>
  </verify>
  <acceptance_criteria>
    - All three suite commands pass, and the counts written into `.planning/REQUIREMENTS.md` equal the counts those commands printed in this task.
    - `grep -c "\[x\] \*\*REGGRANT-0" .planning/REQUIREMENTS.md` returns `3`.
    - `.planning/ROADMAP.md` § Phase 42 lists every plan with its wave and a one-line objective, and its plan count equals the number of `42-*-PLAN.md` files on disk.
    - Each of the four Phase 42 success criteria carries an explicit disposition, and any reworded criterion names what moved, when, and which requirement carries the matching amendment.
    - The Phase 42 row in the progress table reads complete with the plan count and a date.
    - `.planning/STATE.md` carries a Phase 42 outcome paragraph with the three suite counts, and its progress counters match `ls .planning/phases/*/*-PLAN.md | wc -l` and the matching summary count read off disk.
    - The summary reports exactly what the requirements tool returned for the three IDs, including the unmatched traceability row, rather than what this plan predicted it would return.
  </acceptance_criteria>
  <done>The three requirements are marked met against suite counts observed in this task, the roadmap entry and progress table match what shipped, and the state file records the phase outcome with counters read off disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the binding specification → this project's ledger | a divergence crosses here; editing the specification instead would hide it |
| a prior phase's record → a later reader | an uncorrected entry sends a reader looking for a table that no longer exists |
| a quoted suite count → a reviewer | a count copied rather than measured is an unverifiable claim presented as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-06-01 | Repudiation | a divergence resolved by editing the specification | high | mitigate | D-15 forbids it, and an acceptance criterion requires `git status` over the specification directory to be empty after the amendment task |
| T-42-06-02 | Repudiation | a stale pointer to a deleted table left in the ledger | medium | mitigate | A grep over the planning directory drives the correction rather than a hand-written list, and every hit's disposition is reported |
| T-42-06-03 | Repudiation | a suite count copied rather than measured | medium | mitigate | The three suites are run in the closing task and the ledger quotes what those runs printed; the acceptance criterion ties the written number to the observed one |
| T-42-06-04 | Information disclosure | recording an accepted exposure as though it were mitigated | medium | mitigate | The Apple exposure is recorded as accepted and flagged with its mitigating facts and what closes it, on the two precedents that handled the same exposure before it |
| T-42-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed, added, moved or upgraded by this plan. `42-RESEARCH.md` § Package Legitimacy Audit records the phase as installing nothing, so no legitimacy checkpoint is warranted |
</threat_model>

<verification>
- `uv run pytest -q`, `uv run pytest -m e2e -q` and `uv run pytest -m schema -q` all green, with the counts recorded
- `uv run ruff check src tests` exits 0
- no file under the specification directory is modified
- the three REGGRANT requirements read as met
- the ROADMAP plan list and the progress table match the plans on disk

</verification>

<success_criteria>
The ledger records every difference between what the brief asks for and what this phase shipped, the
specification is untouched, no active planning document points at a table that no longer exists, and
the three requirements are marked met against suite counts measured in this plan rather than copied.
</success_criteria>

<output>
Create `.planning/phases/42-post-auth-claim-registered-grant/42-06-SUMMARY.md` when done
</output>
