---
phase: 45-post-auth-restore-subscription
plan: 06
type: execute
wave: 6
gap_closure: true
depends_on: ["45-05"]
files_modified:
  - src/nativespeaker/api/auth/google_play.py
  - src/nativespeaker/api/schemas/auth.py
  - tests/unit/test_restore_proof.py
  - tests/e2e/test_restore_subscription.py
autonomous: true
requirements: [RESTORE-01, RESTORE-02]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A `restore_proof` carrying `../` and `?` reaches Google as one path segment: the request the transport sees still has the path /androidpublisher/v3/applications/{package}/purchases/subscriptionsv2/tokens/... and an empty query string (VERIFICATION truth 7)."
    - "Both path segments of the Play read URL are escaped, so the `applications/{package_name}` guard the code relies on cannot be escaped by caller-supplied content."
    - "The webhook's own `read()` gets the same escaping, because both entry points share `_get`."
    - "A `provider` of 32 characters reaches the handler's 403 gate; one of 33 characters is the framework's 422 before the gate (RESTORE-02 boundary)."
    - "A `restore_proof` of 8192 characters reaches the store check; one of 8193 characters is the framework's 422, and no store call is made (RESTORE-02 boundary)."
    - "The framework's length 422 runs before both refusals of the two-refusal gate, so the refusal order is now: 422, then the 403 for an unserved store, then the 403 for a proof that does not verify (RESTORE-02 ordering)."
    - "An ordinary purchase token still produces exactly the URL the read produced before this change (the non-vacuous control)."
    - statement: "The surface gate holds no per-request state, so two restore attempts running in parallel cannot change each other's refusal; nothing beyond per-request behaviour is guaranteed here."
      verification: backstop
  prohibitions:
    - statement: "A refusal must not carry any part of the artifact the caller presented — not in the body, not in the stage, and not in a log field."
      status: unverified
      verification: judgment
    - statement: "A length bound must not be set so low that a legitimate StoreKit 2 signed transaction or a Play purchase token is refused; the bound exists to stop an abusive payload, never to filter a real one."
      status: unverified
      verification: judgment
  artifacts:
    - src/nativespeaker/api/auth/google_play.py
    - src/nativespeaker/api/schemas/auth.py
    - tests/unit/test_restore_proof.py
    - tests/e2e/test_restore_subscription.py
  key_links:
    - "schemas/auth.py RestoreRequest.restore_proof -> services/restore.py::_verify -> google_play.py::read_for_restore -> google_play.py::_get -> PLAY_URL"
    - "google_play.py::_get -> google_play.py::read() (the webhook entry point shares the same escaping)"
---

<objective>
Close VERIFICATION gap 3 (truth 7, 45-REVIEW CR-01): the Google verification request is built from
caller-supplied content with no escaping, so an authenticated caller chooses the path of a GET that
carries this deployment's `androidpublisher`-scoped bearer token.

Purpose: "verify... against Google" is only true while the request reaches the resource the server
named. Today it does not, and the 1246-case suite never asserted the request's shape.
Output: escaped path segments in `_get`, bounded request fields, and the two regression cases that
go red against the current source.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-post-auth-restore-subscription/45-VERIFICATION.md
@.planning/phases/45-post-auth-restore-subscription/45-REVIEW.md
@.planning/phases/45-post-auth-restore-subscription/45-CONTEXT.md
@.planning/phases/45-post-auth-restore-subscription/45-PATTERNS.md
@.planning/phases/45-post-auth-restore-subscription/45-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The Play read URL is confined to one resource, proved end to end</name>
  <files>src/nativespeaker/api/auth/google_play.py, tests/unit/test_restore_proof.py</files>
  <read_first>
    - src/nativespeaker/api/auth/google_play.py — `PLAY_URL` (lines 33-35), `PlayDeveloperSubscriptions.__init__`, `read` (the webhook entry point), `read_for_restore`, `_product_of`, and `_get` (lines 296-304); the comment at lines 261-262 that relies on the `applications/{package_name}` segment
    - src/nativespeaker/api/services/restore.py — `_verify`, lines 171-174, which hands `restore_proof` to `read_for_restore` unmodified
    - src/nativespeaker/api/schemas/auth.py — `RestoreRequest`, the source of that unmodified string
    - tests/unit/test_restore_proof.py — `_play_reader`, `_restore_through`, `_play_restore`, `_answering`, `_subscription_body`, `PACKAGE_NAME`, `PURCHASE_TOKEN`, `PLAY_PRODUCT_ID`, `PLAY_TIER_ID`, and `TestThePlayReadReportsTheRestoreValueType` for the class-and-docstring convention this module uses
    - .planning/phases/45-post-auth-restore-subscription/45-VERIFICATION.md — the third `gaps:` entry and its two `missing:` items
    - .planning/phases/45-post-auth-restore-subscription/45-REVIEW.md — CR-01, including the three reproduced URLs
  </read_first>
  <behavior>
    - A purchase token of `a/../../../../v3/applications/evil/edits` produces a request whose path is `/androidpublisher/v3/applications/com.nativespeaker.app/purchases/subscriptionsv2/tokens/` followed by one escaped segment, and never a path under `applications/evil`.
    - A purchase token of `x?alt=media` produces a request whose `url.query` is empty bytes.
    - A package name containing a structural URL character is escaped the same way, so the guard cannot be escaped from either side.
    - An ordinary purchase token produces exactly the path the read produced before this change, and `read_for_restore` still returns the same `RestoredSubscription` for it — the control that makes the two cases above non-vacuous.
  </behavior>
  <action>
Write the failing cases first and watch them go red against the current source; a green run before
the fix means the case is not reaching `_get`.

`tests/unit/test_restore_proof.py` — add one class beside `TestThePlayReadReportsTheRestoreValueType`,
named `TestThePlayRequestUrlIsConfinedToOneResource`, with a one-line ASD-STE100 docstring. Add one
module-level helper `_capturing_reader()` that builds a list and returns it together with a reader
from `_play_reader`, whose `httpx.MockTransport` handler appends the `httpx.Request` it is given
before answering with `_answering(_subscription_body("SUBSCRIPTION_STATE_ACTIVE"))`. The captured
`request.url` is what every case in the class asserts on: `request.url.path` and `request.url.query`,
never the string the code passed in, because httpx's own dot-segment normalisation is exactly what
CR-01 exploits and the assertion must be made after it.

Four cases:
  - `test_a_token_carrying_path_traversal_names_one_segment_and_no_other_path` — read with
    `purchase_token="a/../../../../v3/applications/evil/edits"`; assert `request.url.path` starts
    with `/androidpublisher/v3/applications/{PACKAGE_NAME}/purchases/subscriptionsv2/tokens/` and
    that `"evil"` is not a path element of `request.url.path.split("/")`.
  - `test_a_token_carrying_a_query_string_leaves_the_query_empty` — read with
    `purchase_token="x?alt=media"`; assert `request.url.query == b""`.
  - `test_a_package_name_carrying_a_separator_is_escaped_too` — read with a package name holding a
    `/`; assert the path still carries exactly one segment between `applications/` and `/purchases`.
  - `test_an_ordinary_token_reaches_the_expected_path_control` — read with `PURCHASE_TOKEN`; assert
    the path equals `/androidpublisher/v3/applications/{PACKAGE_NAME}/purchases/subscriptionsv2/tokens/{PURCHASE_TOKEN}`
    and that the returned value is still a `RestoredSubscription` whose `external_id` is
    `PURCHASE_TOKEN`. This is the control: without it the three cases above would pass against a
    `_get` that had stopped calling Play at all.

`src/nativespeaker/api/auth/google_play.py` — import `quote` from `urllib.parse` (the module's
import block is alphabetical; `urllib` is standard library and goes with `base64` and `datetime`).
In `_get`, escape both interpolated values with `quote(..., safe="")` before they reach
`PLAY_URL.format(...)`. `safe=""` is the whole point: the default `safe="/"` would leave the
separator the traversal needs. Do not change `PLAY_URL` itself, do not change the method signature,
and do not add a second escaping site in `read_for_restore` — `_get` is the single choke point both
entry points already share, which is why the webhook `read()` is fixed by the same line.

`external_id` stays the raw purchase token: escaping is for the URL only, and the value persisted to
`core.subscriptions.external_id` must remain the token Google gave (44 D-10). Assert that in the
control case rather than leaving it implied.

Replace the comment at `_get` with one ASD-STE100 line stating that every path segment is escaped, so
a caller-supplied token names one segment and never a path (D-15). Do not leave the old comment
standing beside the new one.
  </action>
  <acceptance_criteria>
    - `src/nativespeaker/api/auth/google_play.py` imports `quote` from `urllib.parse`.
    - Both interpolated values in `_get` pass through `quote` with `safe=""`; `grep -v '^ *#' src/nativespeaker/api/auth/google_play.py | grep -c 'quote('` returns `2` (it returns `0` today).
    - `PLAY_URL` is unchanged: `grep -c 'purchases/subscriptionsv2/tokens/{purchase_token}' src/nativespeaker/api/auth/google_play.py` returns `1`.
    - `tests/unit/test_restore_proof.py` defines `TestThePlayRequestUrlIsConfinedToOneResource` with the four cases named above.
    - Each of the three adversarial cases fails against the pre-fix source and passes after it; the executor records both runs in the SUMMARY.
    - The control case asserts the returned `RestoredSubscription.external_id` equals the raw `PURCHASE_TOKEN`, unescaped.
    - No new comment or docstring exceeds one line, and every one is ASD-STE100 (D-15).
  </acceptance_criteria>
  <reversibility rating="reversible">Escaping is one call per segment inside one private method; removing it is the same edit backwards, and nothing persisted depends on it.</reversibility>
  <verify>
    <automated>uv run pytest tests/unit/test_restore_proof.py -q</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any line beginning "FAILED"</fails_when>
    <automated>test "$(grep -v '^ *#' src/nativespeaker/api/auth/google_play.py | grep -c 'quote(')" = "2"</automated>
    <fails_when>non-zero exit, which means one of the two path segments still reaches the URL unescaped</fails_when>
    <automated>uv run pytest tests/unit/test_google_play_notifications.py -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED", which means the shared `_get` change broke the webhook entry point</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any line naming a file and a rule code</fails_when>
  </verify>
  <done>An adversarial `restore_proof` names one path segment of the intended resource and nothing else, proved by a case that was red before the fix, and the webhook's own read is fixed by the same line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The two request fields carry a length bound (45-REVIEW WR-02)</name>
  <files>src/nativespeaker/api/schemas/auth.py, tests/e2e/test_restore_subscription.py</files>
  <precondition>PostgreSQL 17 is reachable with this repo's `.env` DB_* credentials; the e2e suite opens real sessions against it.</precondition>
  <read_first>
    - src/nativespeaker/api/schemas/auth.py — `RestoreRequest` (lines 38-42), and `ChallengeRequest`/`GrantClaimRequest` above it for the `Field(..., min_length=1)` convention and the comment style
    - src/nativespeaker/api/routers/auth.py — the restore handler around lines 160-175, the `PurchaseProvider` membership check, and the `logger.warning("restore_provider_not_served", provider=body.provider)` line with the comment above it
    - tests/e2e/test_restore_subscription.py — `TestTheSurfaceGateIsTheStoreNameAndTheProof`, `_restore`, `_auth`, `REFUSED_BODY`, and `test_an_empty_store_name_is_the_frameworks_own_refusal` as the model for a framework-422 case
    - .planning/phases/45-post-auth-restore-subscription/45-REVIEW.md — WR-02 and its stated fix
    - .planning/phases/45-post-auth-restore-subscription/45-VERIFICATION.md — the closing paragraph naming WR-02 as the same input surface CR-01 exploits
  </read_first>
  <behavior>
    - `provider` of exactly 32 characters reaches the handler and answers 403 `operation_not_allowed` with the body the gate already answers.
    - `provider` of 33 characters answers 422 and reaches no store call.
    - `restore_proof` of exactly 8192 characters reaches the store check.
    - `restore_proof` of 8193 characters answers 422 and reaches no store call.
    - An empty field is still the framework's 422, unchanged from today.
  </behavior>
  <action>
`schemas/auth.py` — add `max_length=32` to `RestoreRequest.provider` and `max_length=8192` to
`RestoreRequest.restore_proof`, keeping `min_length=1` on both. `32` bounds a store name: the longest
`PurchaseProvider` member is far below it, so no served value is filtered. `8192` bounds an artifact:
a StoreKit 2 signed transaction and a Play purchase token are both well below it, so no legitimate
proof is refused. Replace the comment above each field with one ASD-STE100 line stating why the bound
exists — the refusal log for `provider` really is bounded now, and an unbounded proof reaches Apple's
JWS decoder and an outbound URL. Do not add a `pattern` to either field: the store name is checked
against `PurchaseProvider` in the handler (D-01) and the artifact's shape is the store's to judge,
not this schema's.

`routers/auth.py` is NOT modified. Its comment claims the logged value is bounded; that claim becomes
true with this change, which is the point. State that in the SUMMARY so a later reader does not
re-open WR-02 against a comment that is now correct.

`tests/e2e/test_restore_subscription.py` — add two module constants beside `RESTORE_PROOF`,
`MAX_PROVIDER_LENGTH = 32` and `MAX_PROOF_LENGTH = 8192`, each with a one-line comment saying it
restates the schema's bound so the boundary is pinned in one place. Add four cases to
`TestTheSurfaceGateIsTheStoreNameAndTheProof`, each seeding the identity the class's other cases seed:
  - `test_a_store_name_at_the_bound_still_reaches_the_gates_own_refusal` — `provider` of
    `MAX_PROVIDER_LENGTH` characters; assert 403 and `refused.text == REFUSED_BODY`.
  - `test_a_store_name_past_the_bound_is_the_frameworks_own_refusal` — one character more; assert 422
    and `scripted_app_store_notifications.restore_calls == []`.
  - `test_a_proof_at_the_bound_still_reaches_the_store_check` — `provider="apple"` and a
    `restore_proof` of `MAX_PROOF_LENGTH` characters against a scripted rejection; assert the answer
    is the gate's own 403 `proof_rejected` body and that the store was called exactly once.
  - `test_a_proof_past_the_bound_is_refused_before_any_store_call` — one character more; assert 422
    and `scripted_app_store_notifications.restore_calls == []`.

The two "at the bound" cases are the controls: without them a bound set one character too low would
pass the two "past the bound" cases.
  </action>
  <acceptance_criteria>
    - `grep -c 'max_length' src/nativespeaker/api/schemas/auth.py` returns `2` (it returns `0` today).
    - `RestoreRequest.provider` carries `min_length=1, max_length=32`; `RestoreRequest.restore_proof` carries `min_length=1, max_length=8192`.
    - No `pattern=` is added to either field.
    - `src/nativespeaker/api/routers/auth.py` is byte-unchanged by this task.
    - `tests/e2e/test_restore_subscription.py` defines `MAX_PROVIDER_LENGTH` and `MAX_PROOF_LENGTH` and the four cases named above, and the two past-the-bound cases assert the scripted store recorded no call.
    - `test_an_empty_store_name_is_the_frameworks_own_refusal` is unchanged and still passes.
  </acceptance_criteria>
  <reversibility rating="reversible">Two keyword arguments on two `Field(...)` declarations; the bound can be raised or removed in one edit and nothing persisted depends on it.</reversibility>
  <verify>
    <automated>uv run pytest -m e2e tests/e2e/test_restore_subscription.py -q</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any line beginning "FAILED"</fails_when>
    <automated>test "$(grep -c 'max_length' src/nativespeaker/api/schemas/auth.py)" = "2"</automated>
    <fails_when>non-zero exit, which means one of the two request fields is still unbounded</fails_when>
    <automated>git diff --quiet -- src/nativespeaker/api/routers/auth.py</automated>
    <fails_when>non-zero exit, which means the router was edited when this task requires it unchanged</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any line naming a file and a rule code</fails_when>
  </verify>
  <done>Both caller-controlled fields carry a bound that no legitimate value reaches, and the route's own refusal log is bounded in fact and not only in its comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `POST /auth/restore-subscription` | An authenticated but untrusted caller supplies `provider` and `restore_proof`. |
| this service -> `androidpublisher.googleapis.com` | The outbound GET carries this deployment's own OAuth bearer token. The caller's string reaches the URL of that request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-06-01 | Tampering | `auth/google_play.py::_get` | high | mitigate | Escape both path segments with `quote(..., safe="")` before interpolation, so a caller-supplied token names one segment and can never leave the `applications/{package_name}/purchases/subscriptionsv2/tokens/` resource. Proved by `TestThePlayRequestUrlIsConfinedToOneResource`. |
| T-45-06-02 | Information disclosure | the deployment's `androidpublisher` bearer token | high | mitigate | Same fix: the credential can no longer be pointed at a caller-chosen endpoint of the same host. No credential change and no scope change is made — the scope was never the defect. |
| T-45-06-03 | Denial of service | `routers/auth.py` refusal log, `auth/app_store.py` JWS decode | medium | mitigate | `max_length=32` and `max_length=8192` bound both caller-controlled fields, so a multi-megabyte payload is the framework's 422 before the handler, the log pipeline and the decoder. |
| T-45-06-04 | Information disclosure | the length 422 as an oracle | low | accept | A 422 tells a caller only that a field was too long, which the published bound already says. No store name and no proof content is echoed. |
| T-45-06-SC | Tampering | package-manager installs | low | accept | This plan adds no npm/pip/cargo dependency. `quote` is `urllib.parse`, standard library, already vendored with the interpreter; `uv.lock` is not touched. No package legitimacy checkpoint is required. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit — the 45-06 … 45-09 gap-closure plan set

Scope is the three failed truths of `45-VERIFICATION.md`. Truths 2, 3, 4 and 5 are VERIFIED and are
not re-planned; RESTORE-02's surface gate is SATISFIED and is touched here only where the length
bound genuinely changes it.

| Source | Item | Covered by |
|---|---|---|
| GOAL | "Verify a native store artifact directly against Apple or Google" — the request must reach the endpoint the server named | 45-06 Task 1 |
| GOAL | "attach verified paid entitlement" — the attached term must be one the store actually verified | 45-07 Task 1 |
| GOAL | "the old owner's subscription grant is expired in the same transaction" — that grant, and no other | 45-08 Task 1 |
| REQ | RESTORE-01 (BLOCKED by gaps 1 and 2; gap 3 bears on "verified") | 45-06, 45-07, 45-08, 45-09 |
| REQ | RESTORE-02 (SATISFIED; the length bound adds a refusal ahead of the two-refusal gate) | 45-06 Task 2, 45-09 |
| RESEARCH | The Play read is one `purchases.subscriptionsv2.get` whose handle travels in the path | 45-06 Task 1 |
| RESEARCH | The term model: `ends_at` is the paid period's end, the grace window during grace | 45-07 Task 1 |
| RESEARCH | Expire-then-insert under `ix_access_grants_one_active_per_user`; a 23505 is a lost race | 45-08 Task 1 (the narrowing must not change either) |
| CONTEXT | D-05 — Google's one call is the proof check and the live state; `package_name` from `GooglePlayConfig` | 45-06 Task 1 |
| CONTEXT | D-06 — the stored row's status decides where a row exists | 45-07 Task 1 (the status source is unchanged; only the term is reconciled against it) |
| CONTEXT | D-07 — the webhook's writer writes the grant, unchanged | 45-07 Task 1, 45-08 Task 1 (the writer is corrected, not replaced) |
| CONTEXT | D-10 — the move expires the old owner's subscription grant in the same transaction | 45-08 Task 1 |
| CONTEXT | D-11 — one refusal family, byte-identical bodies, no new code | 45-07 Task 1 |
| CONTEXT | D-01/D-02 — the two-refusal surface gate | 45-06 Task 2 |
| CONTEXT | D-13 — REQUIREMENTS.md carries the dated record | 45-09 Task 1 |
| CONTEXT | D-15 — every comment is ASD-STE100, one line, inline | every task |

No item is MISSING. Nothing in the CONTEXT `## Deferred Ideas` block is planned here.

## 45-REVIEW.md findings NOT incorporated, with the reason

Recorded so a later reviewer sees a decision rather than an omission (the review-incorporation
contract). None of these defeats a ROADMAP success criterion; the verifier classified all of them
as non-gaps.

- **WR-01** (`restore_bound_user_id` provisioned and never written; two stale migration comments) —
  DEFERRED. Its own fix is a product decision (implement the lifetime binding, or drop the column),
  and CONTEXT `## Deferred Ideas` already carries "Dropping `restore_bound_user_id`". Closing a
  verification gap must not decide a deferred product question. The stale comment on
  `last_cross_account_transfer_month` is recorded in 45-09's REQUIREMENTS.md entry instead.
- **WR-02** (unbounded `provider` and `restore_proof`) — INCORPORATED, in 45-06 Task 2. The
  verification report names it as the same input surface gap 3 exploits.
- **WR-03** (the `ingest` owner-read race) — DEFERRED. It is an ingestion-path race, not a restore
  defect, and it predates this phase's scope.
- **WR-04** (a move writes no audit row) — REJECTED for this phase. CONTEXT § "Carried forward"
  states no `audit.auth_events` row and no operation label, decided in 37.1 D-01 and 38 D-03. A
  gap-closure plan may not reverse a carried-forward decision.
- **WR-05** (`purchase_user_id` NULL on adoption) — DEFERRED. The column is written once and never
  updated (43 D-17/D-19); changing that is a schema-lifecycle decision, not a gap.
- **WR-06** (two devices of one account restoring at once answer 500) — DEFERRED. It is the
  documented lost-race answer (`InternalError`, a retry then finds the rows), not a wrong write.
- **WR-07** (the loser of an adoption race retries as a move and spends the D-10 cap) — DEFERRED.
  It is a product question about the cap, which D-10 owns.
- **IN-01 … IN-04** (a second clock source in the Play read, a `-> None` return value read,
  `identity.user.id` dereferenced without a guard, the eight-times-duplicated SQLSTATE block) —
  DEFERRED. All four are hygiene; none changes a written row or a wire answer.
</source_audit>

<artifacts_produced>
## Artifacts this plan produces

**Source symbols**
- `src/nativespeaker/api/auth/google_play.py` — `from urllib.parse import quote`; two `quote(..., safe="")` call sites inside `_get`. No new function, no new class, no signature change.
- `src/nativespeaker/api/schemas/auth.py` — `RestoreRequest.provider` gains `max_length=32`; `RestoreRequest.restore_proof` gains `max_length=8192`.

**Test symbols**
- `tests/unit/test_restore_proof.py` — module helper `_capturing_reader`; class `TestThePlayRequestUrlIsConfinedToOneResource` with `test_a_token_carrying_path_traversal_names_one_segment_and_no_other_path`, `test_a_token_carrying_a_query_string_leaves_the_query_empty`, `test_a_package_name_carrying_a_separator_is_escaped_too`, `test_an_ordinary_token_reaches_the_expected_path_control`.
- `tests/e2e/test_restore_subscription.py` — module constants `MAX_PROVIDER_LENGTH`, `MAX_PROOF_LENGTH`; four cases added to `TestTheSurfaceGateIsTheStoreNameAndTheProof`: `test_a_store_name_at_the_bound_still_reaches_the_gates_own_refusal`, `test_a_store_name_past_the_bound_is_the_frameworks_own_refusal`, `test_a_proof_at_the_bound_still_reaches_the_store_check`, `test_a_proof_past_the_bound_is_refused_before_any_store_call`.

**No new files. No new error leaf. No new dependency.**
</artifacts_produced>

<verification>
- `uv run pytest tests/unit/test_restore_proof.py tests/unit/test_google_play_notifications.py -q` passes.
- `uv run pytest -m e2e tests/e2e/test_restore_subscription.py -q` passes.
- `uv run ruff check src tests` is clean.
- The three adversarial URL cases and the two past-the-bound cases were each observed RED against the pre-fix source; both runs are recorded in the SUMMARY.
</verification>

<success_criteria>
VERIFICATION truth 7 can be re-run and answered VERIFIED: the Google verification request is sent to
the endpoint and package the server intends and is not redirectable by `restore_proof` content, and a
case in the suite now asserts it.
</success_criteria>

<output>
Create `.planning/phases/45-post-auth-restore-subscription/45-06-SUMMARY.md` when done.
</output>
