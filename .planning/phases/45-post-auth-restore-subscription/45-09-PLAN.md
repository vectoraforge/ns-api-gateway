---
phase: 45-post-auth-restore-subscription
plan: 09
type: execute
wave: 8
gap_closure: true
depends_on: ["45-06", "45-07", "45-08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [RESTORE-01, RESTORE-02]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The four suite commands the phase records — the whole suite, the e2e marker, the schema marker and ruff — are run in this plan and their real counts recorded, never copied from 45-05."
    - "The three closed gaps are recorded under RESTORE-01 as dated entries naming the defect, the fix and the case that now covers it, so a later reader finds why the phase was re-opened."
    - "RESTORE-01 is marked met only after all three gap-closure plans are executed and the four commands are green in this plan's own run."
    - "The 45-REVIEW findings this plan set did not incorporate are recorded by name with the reason, so a later reviewer sees a decision rather than an omission."
    - "The migration comment on last_cross_account_transfer_month, which D-10 made false, is recorded as a known-stale comment with the fix it needs (45-REVIEW WR-01)."
  prohibitions:
    - statement: "A requirement must not be marked met on evidence copied from an earlier run; the record must name commands this plan itself ran and the counts they printed."
      status: unverified
      verification: judgment
    - statement: "A divergence from the binding brief must not be resolved by editing the brief; the record must show what this project chose and what the specification says."
      status: unverified
      verification: judgment
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  key_links:
    - "45-VERIFICATION.md gaps -> REQUIREMENTS.md RESTORE-01's dated gap-closure entry -> the three cases that now cover them"
    - "45-REVIEW.md CR-01/CR-02/CR-03 -> the three closing plans; WR-01 … WR-07 and IN-01 … IN-04 -> the recorded deferrals"
---

<objective>
Prove the three fixes together on the whole suite, and record what was wrong, what was changed and
what was deliberately left open.

Purpose: each of 45-06, 45-07 and 45-08 verified its own files while the other two were mid-flight.
Nothing has yet run the four commands the phase reports against the three fixes at once. And
REQUIREMENTS.md currently carries RESTORE-01 reverted after the gaps were found — a later reader
needs the closing entry, not silence.
Output: one green run of the four commands, and the dated record under RESTORE-01 and RESTORE-02.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-post-auth-restore-subscription/45-VERIFICATION.md
@.planning/phases/45-post-auth-restore-subscription/45-REVIEW.md
@.planning/phases/45-post-auth-restore-subscription/45-CONTEXT.md
@.planning/phases/45-post-auth-restore-subscription/45-05-SUMMARY.md
@.planning/phases/45-post-auth-restore-subscription/45-06-SUMMARY.md
@.planning/phases/45-post-auth-restore-subscription/45-07-SUMMARY.md
@.planning/phases/45-post-auth-restore-subscription/45-08-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The four commands run once, against all three fixes together</name>
  <files>.planning/STATE.md</files>
  <precondition>All three of 45-06, 45-07 and 45-08 are committed on this branch, and PostgreSQL 17 is reachable with this repo's `.env` DB_* credentials — the e2e and schema markers both open real sessions.</precondition>
  <read_first>
    - .planning/phases/45-post-auth-restore-subscription/45-06-SUMMARY.md, 45-07-SUMMARY.md, 45-08-SUMMARY.md — the counts each plan reported and the RED-then-GREEN observations each recorded
    - .planning/phases/45-post-auth-restore-subscription/45-05-SUMMARY.md — the counts the phase reported before the gaps were found: 1246 unit, 321 e2e, 222 schema
    - .planning/STATE.md — the frontmatter counters and the § Current Position block, and the comment convention prior plans used when they read counts off disk rather than incrementing them
  </read_first>
  <action>
Run the four commands this phase reports on, in this order, from the repository root, and capture the
summary line of each. Do not copy a count from 45-05 or from any of the three gap-closure summaries:
those runs each saw a partial tree.

  - `uv run pytest -q`
  - `uv run pytest -m e2e -q`
  - `uv run pytest -m schema -q`
  - `uv run ruff check src tests`

Every count must be at least what 45-05 recorded plus the cases the three plans added: the unit total
rises by the four URL cases of 45-06, the e2e total by 45-06's four length cases and 45-07's seven
cases, and the schema total by 45-08's seven cases. A total that did not rise means a case was not
collected — investigate before recording anything, and do not record a number the run did not print.

If any command is not green, stop. Do not amend REQUIREMENTS.md in Task 2 and do not mark anything
met. Report which command failed and what it printed; a red suite at this point means one of the
three fixes interacts with another, which is precisely the interaction this plan exists to find.

`.planning/STATE.md` — update § Current Position to say the phase's three verification gaps are
closed and the phase is awaiting re-verification, and update `last_activity` and
`last_activity_desc`. Read the plan and summary counts off disk rather than incrementing the
frontmatter counters, as 41-05, 42-07, 43-06, 44-07 and 45-05 each did, and record in a comment what
was counted and when. Do not mark the phase complete: `/gsd:verify-phase` decides that, not this
plan.
  </action>
  <acceptance_criteria>
    - The SUMMARY records the exact summary line of each of the four commands, quoted from this plan's own run.
    - The unit, e2e and schema totals are each strictly greater than the corresponding 45-05 figure (1246, 321, 222).
    - `uv run ruff check src tests` printed no rule code.
    - `.planning/STATE.md` § Current Position names the three closed gaps and says the phase awaits re-verification; `status:` is not set to a completed value.
    - The frontmatter counters were read off disk, and the comment says what was counted and at what time.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED", or a "passed" count at or below 1246</fails_when>
    <automated>uv run pytest -m e2e -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED", or a "passed" count at or below 321</fails_when>
    <automated>uv run pytest -m schema -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED", or a "passed" count at or below 222</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any line naming a file and a rule code</fails_when>
  </verify>
  <done>The three fixes are green together on the whole suite, on real PostgreSQL, with counts this plan itself observed.</done>
</task>

<task type="auto">
  <name>Task 2: The record of what was wrong, what changed, and what was left open</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the RESTORE-01 and RESTORE-02 blocks (around lines 476 and 513), the header's conflict-count paragraph, the traceability row for RESTORE-01 … RESTORE-02, and the file's convention for a dated amendment that supersedes rather than deletes earlier text
    - .planning/phases/45-post-auth-restore-subscription/45-VERIFICATION.md — the three `gaps:` entries, the truth table and the requirements-coverage table
    - .planning/phases/45-post-auth-restore-subscription/45-REVIEW.md — CR-01, CR-02, CR-03, WR-01 … WR-07 and IN-01 … IN-04
    - .planning/phases/45-post-auth-restore-subscription/45-06-PLAN.md — its `<source_audit>` block, which already holds the reasoned disposition of every review finding this plan set did not incorporate
    - .planning/phases/45-post-auth-restore-subscription/45-CONTEXT.md — D-06, D-07, D-10, D-11, D-13, D-14, D-15
  </read_first>
  <action>
`.planning/REQUIREMENTS.md` — add one dated entry under RESTORE-01, headed as a gap-closure
amendment for 2026-09-08, holding:
  - The three defects by their VERIFICATION truth number and their review id: truth 7 / CR-01, the
    unescaped Play request URL; truth 1 / CR-02, the unreconciled status and term; truth 6 / CR-03,
    the unscoped superseded set. State for each what was reachable, in one sentence, in the file's
    own plain register.
  - The fix for each, named concretely: `quote(..., safe="")` on both path segments of `_get`; the
    term check before any write that refuses into the existing `restore_not_found` family; the
    narrowed `superseded` comprehension keyed on the destination's rows plus this subscription's row.
  - The case that now covers each, by test class name, so a later reader can run it.
  - The plain statement that the phase's own 1246 / 321 / 222 passing suite did not catch any of the
    three, and why: none of those cases set up a stored-status and proof-term mismatch, a source
    account with a second unrelated subscription, or an adversarial token value. The code review
    found all three, not the test run. This is the lesson worth recording.
  - The counts from this plan's Task 1 run, with the four commands named.
  - WR-02 recorded as fixed in the same change, with the two bounds and the note that the router's
    own comment about a bounded logged value is now true.
  - The findings deliberately not incorporated — WR-01, WR-03, WR-04, WR-05, WR-06, WR-07 and
    IN-01 … IN-04 — each by name with the one-line reason from 45-06's `<source_audit>` block. Two
    need a sentence more: WR-01, because the stale migration comment on
    `last_cross_account_transfer_month` actively misleads a reader of the schema and should read
    that the column is written by the capped cross-account move only; and WR-04, because a move
    still leaves no audit trail and that is a carried-forward decision (37.1 D-01, 38 D-03), not an
    oversight.

Under RESTORE-02, add one short dated line: the two-refusal gate is unchanged, and a length 422 from
`RestoreRequest`'s new bounds now precedes both refusals.

Mark RESTORE-01 met only if Task 1 was green. Do not change the header's conflict counts: this plan
adds no new divergence from `10-restore-subscription.md` — every change here moves the code toward
what the brief and `SHARED-INVARIANTS.md` already require. Say that explicitly in the entry, so a
later reader does not think the count was forgotten. Do not edit
`10-restore-subscription.md` or `SHARED-INVARIANTS.md` (D-14).

Update the traceability row for RESTORE-01 … RESTORE-02 to name the gap-closure plans 45-06 … 45-09
alongside 45-01 … 45-05.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` carries a dated 2026-09-08 gap-closure entry under RESTORE-01 naming all three defects, all three fixes and all three covering test class names.
    - The entry records the four command names and the counts from Task 1's own run.
    - The entry names WR-01, WR-03, WR-04, WR-05, WR-06, WR-07, IN-01, IN-02, IN-03 and IN-04, each with a reason; `grep -c 'WR-0[1345677]\|IN-0[1234]' .planning/REQUIREMENTS.md` returns a non-zero count.
    - RESTORE-02 carries the one-line note about the length 422 preceding the two refusals.
    - The header's conflict-count paragraph is unchanged, and the entry states why.
    - `git diff --quiet -- ../specs` reports no change: neither `10-restore-subscription.md` nor `SHARED-INVARIANTS.md` is edited.
    - The traceability row names 45-06, 45-07, 45-08 and 45-09.
    - RESTORE-01 is marked met only if Task 1's four commands were green; if any was red, the requirement stays unchecked and the SUMMARY says so.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '45-06' .planning/REQUIREMENTS.md)" != "0"</automated>
    <fails_when>non-zero exit, which means the gap-closure plans are not named in the requirements record</fails_when>
    <automated>test "$(grep -c 'CR-01' .planning/REQUIREMENTS.md)" != "0" &amp;&amp; test "$(grep -c 'CR-02' .planning/REQUIREMENTS.md)" != "0" &amp;&amp; test "$(grep -c 'CR-03' .planning/REQUIREMENTS.md)" != "0"</automated>
    <fails_when>non-zero exit, which means one of the three closed defects is missing from the record</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED"</fails_when>
  </verify>
  <done>A reader who opens REQUIREMENTS.md finds why the phase was re-opened, what was changed, what proves it, and what was knowingly left alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan's claims -> a later reader | The record is the only durable artifact. A count copied from an earlier run, or a requirement marked met on absent evidence, misleads every phase after this one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-09-01 | Repudiation | `.planning/REQUIREMENTS.md` | medium | mitigate | Every count in the entry comes from Task 1's own run of the four named commands, and RESTORE-01 is marked met only if that run was green. The prohibition against copied evidence is recorded in this plan's `must_haves`. |
| T-45-09-02 | Tampering | the binding brief and `SHARED-INVARIANTS.md` | low | mitigate | Neither is edited (D-14); a `git diff --quiet -- ../specs` check is part of the acceptance criteria. |
| T-45-09-SC | Tampering | package-manager installs | low | accept | This plan adds no npm/pip/cargo dependency and touches no source file. No package legitimacy checkpoint is required. |
</threat_model>

<artifacts_produced>
## Artifacts this plan produces

**Documents**
- `.planning/REQUIREMENTS.md` — a dated 2026-09-08 gap-closure entry under RESTORE-01; a one-line dated note under RESTORE-02; an updated traceability row naming 45-06 … 45-09.
- `.planning/STATE.md` — § Current Position updated to "three verification gaps closed, awaiting re-verification"; `last_activity` and `last_activity_desc` updated; counters read off disk with the counting comment.

**No source symbols. No test symbols. No new files.**
</artifacts_produced>

<verification>
- `uv run pytest -q`, `uv run pytest -m e2e -q`, `uv run pytest -m schema -q` and `uv run ruff check src tests` are green in this plan's own run, with counts above the 45-05 figures.
- `.planning/REQUIREMENTS.md` names all three closed defects, their fixes, their covering cases and every deferred review finding.
- Neither `10-restore-subscription.md` nor `SHARED-INVARIANTS.md` is edited.
</verification>

<success_criteria>
`/gsd:verify-phase 45` can be re-run and answer VERIFIED on all seven truths, with the record in
REQUIREMENTS.md stating what was wrong, what changed, what proves it, and what was left open on
purpose.
</success_criteria>

<output>
Create `.planning/phases/45-post-auth-restore-subscription/45-09-SUMMARY.md` when done.
</output>
