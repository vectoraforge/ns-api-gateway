---
phase: 45-post-auth-restore-subscription
plan: 05
type: execute
wave: 5
depends_on: ["45-04"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [RESTORE-01, RESTORE-02]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every departure this phase made from 10-restore-subscription.md is recorded in REQUIREMENTS.md as a dated flagged conflict, by line, and the brief itself is unedited."
    - "D-09's change to the ingestion owner rule is recorded under APPLEHOOK-01 and in STATE.md Decisions, so a later phase does not read it as a regression."
    - "The Phase 40 forward flag on the restore_subscription operation label is answered: no label is needed."
    - "The unbounded Play read per restore attempt is recorded as accepted, not filed as a new defect."
    - "The ROADMAP success criteria for Phase 45 are marked with the decision that answers each."
  prohibitions:
    - statement: "A divergence from the binding brief must not be resolved by editing the brief; the record must show what this project chose and what the specification says."
      status: unverified
      verification: judgment
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  key_links:
    - "REQUIREMENTS.md RESTORE-01/02 -> 10-restore-subscription.md by line number"
    - "REQUIREMENTS.md APPLEHOOK-01 -> D-09 -> crud/subscriptions.py's owner rule"
---

<objective>
Record what this phase decided, what it departed from, and what it knowingly left open.

Purpose: D-13 makes this a phase deliverable, not bookkeeping. The brief and SHARED-INVARIANTS are
not edited (D-14), so REQUIREMENTS.md is the only place a later reader finds the departures.
Output: the amended requirement block, the STATE.md decision, and the marked ROADMAP criteria.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/45-post-auth-restore-subscription/45-CONTEXT.md
@.planning/phases/45-post-auth-restore-subscription/45-RESEARCH.md
@.planning/phases/45-post-auth-restore-subscription/45-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend RESTORE-01, RESTORE-02 and APPLEHOOK-01 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the RESTORE block, the APPLEHOOK-01 block, the PLAYHOOK block's answered-and-closed entry as the format model, and the header's amendment paragraphs with their conflict counts
    - /home/init/native-speaker/specs/auth-refactor-phases/10-restore-subscription.md — the brief, for the line numbers each conflict is against
    - .planning/phases/45-post-auth-restore-subscription/45-CONTEXT.md — D-13's list
    - .planning/phases/45-post-auth-restore-subscription/45-RESEARCH.md — the Open Questions and the Assumptions Log
    - .planning/phases/45-post-auth-restore-subscription/45-01-SUMMARY.md through 45-04-SUMMARY.md — what actually shipped
  </read_first>
  <action>
Write dated entries under RESTORE-01 and RESTORE-02 in the blockquote style the APPLEHOOK and
PLAYHOOK blocks already use. Do not edit `10-restore-subscription.md` or `SHARED-INVARIANTS.md`
(D-14): a divergence is recorded here, never resolved by editing the specification.

Record as **flagged conflicts against `10-restore-subscription.md`, each naming the brief's line**:
  - D-02 — the surface gate is the proof alone. Against § Request contract's web-call and
    other-platform clauses. State plainly that one store's artifact presented from the other
    platform is not detected, because the server has no signal for it.
  - D-03 — anonymous accounts may restore. Against "Destination must be an active, registered
    account" and its `restore_destination_anonymous` result.
  - D-04 — the Apple proof is verified locally and never live. Against steps 8 and 17 and the rule
    that the current row's status is necessary but not sufficient. Name the accepted blind spot: a
    subscription this server has never heard of, refunded after the proof was signed, gets a grant
    until the next webhook.
  - D-07 — the webhook's writer writes the grant. Against the `ends_at IS NULL` adoption grant,
    the UPDATE reactivation of the same row, and "never mints a fresh monthly counter for the same
    paid entitlement". Note that the repeat restore's `WriteOutcome.replayed` is what delivers the
    counter rule the brief asked for, by a different mechanism.
  - D-08 — a conditional UPDATE settles owner changes and `core.subscriptions` is never locked.
    Against step 9(a).
  - D-10 — one move per UTC calendar month. Against the lifetime binding, the
    `store_transaction_already_linked` result, and the DELETIONS line forbidding any read or write
    of `last_cross_account_transfer_month`.

Record as **answered**:
  - The Phase 40 forward flag on the `restore_subscription` operation label: **no label is
    needed**. Restore is not challenge-bearing and writes no audit row, so the surviving consumer
    `core.auth_challenges.operation` never wanted it. The single migration is not edited.
  - The obligations that were already dead before this phase began, each with the phase that
    removed it: the route registry, the foundation store-verification and vendor-proof interfaces,
    the audit row, the eleven rate-limit entries, the provider budgets, coalescing, the freshness
    bound, and the attempt-identity retry.
  - The eight result codes the brief names that have no trigger after D-03, D-04, D-07, D-08 and
    D-10, listed by name, with the decision that removed each one's trigger.

Record as **accepted, not as a new defect**:
  - The unbounded Play read per restore attempt. Follow the wording 41 D-20, 42 D-16 and 43-06
    used: state what bounds it (the caller must already hold a valid Firebase token for a linked
    account, and the Apple path costs no network call at all), state that it closes with the v2.1
    gateway contract, and say why it is narrower than the webhook case.

Record as **standing facts about the world, not gaps this phase left**:
  - A1 and A2. No iOS app exists, so no real StoreKit signed transaction and no real Play purchase
    token has ever reached this deployment. The first real refusal from either store is
    authoritative over anything in this repository. This is the same fact STATE.md records for
    Phases 41 and 43. Note the difference in this phase's favour, as 43-06 did: the Apple shapes
    come from Apple's own library and its chain walk runs for real in the unit suite.

Record as **open product questions raised and deliberately not answered here**:
  - Apple Family Sharing. `inAppOwnershipType` is `PURCHASED` or `FAMILY_SHARED`, this repository
    reads it nowhere, and under D-10 a family member's shared transaction can consume the paying
    account's one monthly move. No check was added, because CONTEXT names none. This is the
    sharpest edge D-10 has and the developer should decide it before an iOS app ships.
  - `linkedPurchaseToken`. The comment in `auth/google_play.py` says an upgrade's old token is
    this route's to read. It is not read: CONTEXT names no such branch and D-11 adds no code for
    it. The comment now over-promises.
  - The Google `stage` set. D-11 lists "package mismatch" as a proof-rejection stage; the package
    name travels in the Play URL path, so a token from another package answers 404 and reaches the
    gone-token stage. No separate stage exists.
  - The migration's own comment on `last_cross_account_transfer_month` — "Written by nothing" —
    became false when D-10 landed. The single migration is not edited, so this is a documentation
    item and not a code change.

Update the header's amendment paragraphs and their conflict counts to include this phase, in the
same voice the earlier paragraphs use.

Under APPLEHOOK-01, add the **D-09 dated amendment**: `SubscriptionsDB.upsert_subscription` no
longer lets a notification's attribution token overwrite an owner. The token attributes an unowned
row only; a row with an owner keeps it, and the owner is changed by restore alone. State why: a
renewal carrying the original buyer's own token passes the ingest attribution guard, `resolve_user`
maps it back to the original account, and the old rule would have silently reverted a restore move
and then collided with `ix_access_grants_one_per_subscription` on every retry. Name what this
amends — 43 D-19 and 43-03's "an owner is added, never cleared" — and name what did not change:
an unowned row is still attributed by the carried token.

Every sentence is ASD-STE100: short, one idea each, no metaphors.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a dated 2026 entry under RESTORE-01 naming D-02, D-03, D-04, D-07, D-08 and D-10 as flagged conflicts, each with a `10-restore-subscription.md` line reference.
    - RESTORE-02 carries its own entry stating that D-01's 403 and D-02's `proof_rejected` are the whole surface gate.
    - APPLEHOOK-01 carries a dated D-09 amendment naming 43 D-19 and 43-03.
    - The eight untriggered brief result codes are listed by name with the decision that removed each trigger.
    - The Phase 40 operation-label forward flag is answered with "no label is needed".
    - The accepted Play-read exposure entry names the v2.1 gateway contract as what closes it.
    - The four open questions — Family Sharing, `linkedPurchaseToken`, the Google stage set, and the stale migration comment — each appear once.
    - `/home/init/native-speaker/specs/auth-refactor-phases/10-restore-subscription.md` and `SHARED-INVARIANTS.md` are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'RESTORE-01' .planning/REQUIREMENTS.md)" -ge "2"</automated>
    <fails_when>non-zero exit, which means no amendment entry was written under the requirement</fails_when>
    <automated>test "$(grep -c '10-restore-subscription.md' .planning/REQUIREMENTS.md)" -ge "6"</automated>
    <fails_when>non-zero exit, which means fewer than six line-referenced conflicts against the brief were recorded</fails_when>
    <automated>test "$(grep -c 'D-09' .planning/REQUIREMENTS.md)" -ge "1"</automated>
    <fails_when>non-zero exit, which means the ingestion owner-rule amendment is missing from the requirement record</fails_when>
    <automated>echo "6506d26cc976484795263882b2f76e04236b3a7204b2898dd7770acdee1edd8f  /home/init/native-speaker/specs/auth-refactor-phases/10-restore-subscription.md
c04c58c2305a828dc1fceeae0e9aef03485d7efa49c72a76a10acd90482a919b  /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md" | sha256sum -c --status</automated>
    <fails_when>non-zero exit, which means the binding brief or the shared invariants were edited, against D-14</fails_when>
  </verify>
  <done>Every departure, every answered obligation, every accepted exposure and every open question of this phase is written into REQUIREMENTS.md, and the specification is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Record D-09 in STATE.md and mark the ROADMAP criteria</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md — the § Accumulated Context Decisions list, the v2.0 block, and the Phase 43 and 44 outcome paragraphs as the format model
    - .planning/ROADMAP.md — the Phase 45 section and its four success criteria
    - .planning/phases/45-post-auth-restore-subscription/45-01-SUMMARY.md through 45-04-SUMMARY.md
  </read_first>
  <action>
`.planning/STATE.md` — add one line to § Accumulated Context, the v2.0 decisions block: an owner,
once set on `core.subscriptions`, is kept by ingestion; the attribution token attributes an unowned
row only, and restore is the only thing that changes an owner. This is D-09 and it amends a rule
Phase 43 wrote, so a later phase must find it here rather than reading the change as a regression.

Write the Phase 45 outcome paragraph in the voice the Phase 43 and 44 paragraphs use: what shipped,
what was measured rather than assumed, and what is recorded as accepted rather than fixed. Name the
three outcomes, the two proof checks, the cap, and the four real-PostgreSQL cases. Record the three
suite counts — unit, e2e and schema — as measured after this phase, the way Phases 41 to 44 each
did, and `ruff check src tests` clean. Do not increment any counter: read the plan and summary file
counts from disk and write what you counted, with a comment saying so, as the existing counting
notes in this file do.

`.planning/ROADMAP.md` — mark the four Phase 45 success criteria against what answers each:
criterion 1 by the three shipped outcomes across both stores; criterion 2 by the ordering case that
asserts the store call precedes the first session statement; criterion 3 by D-01's unserved-store
403 and D-02's `proof_rejected`; criterion 4 by the refusal cases that assert unchanged row counts.
Use the marking convention the earlier completed phases in this file already use; do not invent a
new one.
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` § Accumulated Context carries a v2.0 decision line stating that an owner, once set, is kept by ingestion.
    - `.planning/STATE.md` carries a Phase 45 outcome paragraph naming the three outcomes and the four schema cases.
    - The three suite counts in that paragraph are numbers read from a run of the suite, not carried forward from Phase 44's paragraph.
    - `.planning/ROADMAP.md` marks all four Phase 45 success criteria, each naming what answers it.
    - `.planning/STATE.md` frontmatter counters are consistent with the plan and summary files on disk.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'Phase 45 outcome' .planning/STATE.md)" = "1"</automated>
    <fails_when>non-zero exit, which means the outcome paragraph is missing or was written twice</fails_when>
    <automated>uv run pytest -q &amp;&amp; uv run pytest -m e2e -q &amp;&amp; uv run pytest -m schema -q</automated>
    <fails_when>non-zero exit, or any line beginning "FAILED" — the counts recorded in STATE.md must come from a green run</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any output line beginning "src/" or "tests/"</fails_when>
  </verify>
  <done>STATE.md carries the D-09 decision and the Phase 45 outcome with measured suite counts, and the ROADMAP criteria are marked against what answers each.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's record -> a later phase's planner | A departure that is not written down is a departure a later phase silently re-decides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-12 | Repudiation | the phase record | medium | mitigate | Every departure is a dated, line-referenced entry in REQUIREMENTS.md; the brief is unedited, so the record shows both what the specification says and what this project chose. |
| T-45-06 | Denial of Service | the unbounded Play read | medium | accept | Recorded under RESTORE-01 with what bounds it and what closes it, on the 41 D-20, 42 D-16 and 43-06 precedent. |
| T-45-13 | Information Disclosure | the open Family Sharing question | medium | transfer | Raised to the developer in the requirement record with the reason it matters under D-10; no code decides it, because CONTEXT names no check. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and touches no source file. |
</threat_model>

<verification>
- `uv run pytest -q`, `uv run pytest -m e2e -q`, `uv run pytest -m schema -q` green
- `uv run ruff check src tests` clean
- The brief and SHARED-INVARIANTS are byte-identical to their pre-phase state.
</verification>

<success_criteria>
A reader who opens REQUIREMENTS.md after this phase can see every departure from the brief, why it
was made, what was accepted rather than fixed, and what remains an open product question.
</success_criteria>

## Artifacts this phase produces

Created by this plan:
- Dated amendment entries under RESTORE-01, RESTORE-02 and APPLEHOOK-01 in `.planning/REQUIREMENTS.md`
- One v2.0 decision line and one Phase 45 outcome paragraph in `.planning/STATE.md`
- Marked success criteria in the Phase 45 section of `.planning/ROADMAP.md`

No source symbol is created by this plan.

<output>
Create `.planning/phases/45-post-auth-restore-subscription/45-05-SUMMARY.md` when done
</output>
