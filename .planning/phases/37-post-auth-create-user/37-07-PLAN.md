---
phase: 37-post-auth-create-user
plan: 07
type: execute
wave: 3
depends_on: ["37-01", "37-03", "37-04", "37-05"]
files_modified:
  - src/nativespeaker/api/routers/auth.py
  - src/nativespeaker/api/routers/__init__.py
  - src/nativespeaker/api/auth/creation.py
  - src/nativespeaker/api/auth/registry.py
  - src/nativespeaker/api/app/main.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/app/lifespan.py
  - tests/unit/conftest.py
  - tests/unit/test_route_registry.py
  - tests/unit/test_create_user_modes.py
  - tests/e2e/conftest.py
  - tests/e2e/test_create_user.py
autonomous: true
requirements: [CREATE-01, CREATE-02, CREATE-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "POST /auth/create-user is the only route in REGISTRY declaring preauth_callable=True, and the startup enumeration assertion passes in both directions"
    - "An unlinked caller reaches POST /auth/create-user and is rejected with preauth_identity_not_allowed on every other authenticated route"
    - "POST /auth/create-user?challenge=true returns exactly {challenge_id, expires_at} with Cache-Control: no-store and creates no core.users row"
    - "A completion for an unlinked subject whose providerData is empty returns 200 with the classified identity_provider and creates exactly one core.users row, exactly one ACTIVE core.external_identities row with provider=anonymous and provider_uid NULL, and exactly two core.store_purchase_tokens rows (apple and google_play) with distinct identity_values"
    - "The successful completion writes exactly one audit.auth_events row with operation=create_user and result=succeeded, and the challenge row is consumed with preauth_subject_hash cleared"
    - "No access grant, no free credits, and no core.user_monthly_usage row are created by a successful completion"
    - "core.users.display_name is NULL after a successful completion"
    - "registered_at is NULL for an anonymous creation"
    - "The mode signal partitions exhaustively: challenge=true with no body handle is prepare; a body handle with no challenge=true is completion; both together, neither, a duplicated challenge parameter, a challenge value other than exactly true, and a null/empty/wrong-typed challenge_id are all invalid_request 400"
    - "An invalid_request rejection writes NO audit.auth_events row and consumes no challenge"
    - "No database transaction is open across the Firebase Admin call: the challenge claim commits before the lookup and the consuming transaction opens after it"
    - "A body whose challenge_id is a non-string (for example an integer) returns 400 invalid_request, never 422 validation_error"
  artifacts:
    - src/nativespeaker/api/routers/auth.py
    - src/nativespeaker/api/auth/creation.py
    - tests/e2e/test_create_user.py
    - tests/unit/test_create_user_modes.py
  key_links:
    - "auth/registry.py RouteMetadata <-> app/main.py include_router: assert_route_enumeration is set equality at startup, so both must land in one commit or the app does not boot"
    - "classify_mode_signal(raw_query, body_challenge_id) <- a Depends() accessor returning scope['query_string']: a first-value-wins query accessor cannot see the duplicated-parameter case"
    - "ChallengeStore.claim (its own committed transaction) -> the Firebase lookup -> ChallengeStore.consume (the consuming transaction): the claim must be committed before the provider call, or a crash mid-lookup leaves an unclaimed challenge a second attempt could win"
    - "RequestContext.evaluated_at and attempt_id -> expires_at, created_at, claim_attempt_id: never recomputed anywhere in the handler (35 D-02)"
  prohibitions:
    - "The purchase-attribution tokens must never be derivable from user identity, subject, email, or any other stable input. Each is a fresh random opaque server-generated value, distinct per store, and must never become a cross-store or cross-service correlation key."
    - "The audit.auth_events trail must not become a behavioral tracking archive: no raw subject, no email address, no client IP address, no device identifier, and no public challenge_id may reach any row, at any nesting depth of details. Correlate on the non-secret auth_challenges.id."
---

<objective>
**This is the phase's tracer.** One production-quality end-to-end slice — an unlinked caller preparing a challenge and completing an anonymous account creation — wired through every layer the phase touches, verified before any expansion task runs.

Purpose: prove the architecture end-to-end on this agent's best early-context tokens. If the route registration, the mode-signal dispatch, the claim-outside-the-transaction ordering, the threadpool-offloaded lookup, or the savepoint-wrapped insert is wrong, that is a one-commit correction here rather than a ten-layer unwind later. Plans 37-01 through 37-06 are enabling prerequisites — a dependency lock, a schema removal, a model layer, an error registry, an adapter — not layers of this slice.

Output: `POST /auth/create-user` registered, declared, and serving both modes for the one anonymous happy path, with the consuming transaction in `auth/creation.py` where 37-09 can drive it directly.

Stubs are permitted only where they can later be filled without an architectural change. Rejection *branches* are functionality gaps and belong to 37-08 and 37-09; the *ordering*, the transaction boundaries, and the audit-row placement are architectural and must be right here.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-post-auth-create-user/37-CONTEXT.md
@.planning/phases/37-post-auth-create-user/37-RESEARCH.md
@/home/init/native-speaker/specs/auth-refactor-phases/02-create-user.md
@/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "an unlinked anonymous caller gets an account" — one path only</name>
  <files>src/nativespeaker/api/auth/registry.py, src/nativespeaker/api/routers/auth.py, src/nativespeaker/api/routers/__init__.py, src/nativespeaker/api/app/main.py, src/nativespeaker/api/app/dependencies.py, src/nativespeaker/api/app/lifespan.py, src/nativespeaker/api/auth/creation.py, tests/e2e/conftest.py, tests/e2e/test_create_user.py</files>
  <read_first>
    - /home/init/native-speaker/specs/auth-refactor-phases/02-create-user.md lines 40-66 in full — the mode-signal partition, prepare mode, and the fourteen-step completion flow whose numbering IS the rejection precedence
    - src/nativespeaker/api/auth/registry.py lines 50-104 (the `_PREAUTH_CALLABLE_ROUTE` pin, `REGISTRY`, `lookup`) and 160-244 (conditions 1-2, 6, 7, 8, and the local-import discipline at 134-142)
    - src/nativespeaker/api/auth/challenges.py in full — `issue`, `locate`, `claim`, `consume`, `verify_binding`, and every docstring, which encode §6 rules the handler must not re-derive
    - src/nativespeaker/api/auth/modesignal.py in full
    - src/nativespeaker/api/auth/context.py lines 55-140 (`LinkedIdentity`, `PreAuthIdentity`, `RequestContext`)
    - src/nativespeaker/api/auth/audit.py lines 158-200 (`build_details`) and 208-312 (`AuditWriter.build_row`, `write_standalone`, `write_in_transaction`)
    - src/nativespeaker/api/app/dependencies.py in full — the `Depends()`-only convention and `get_preauth_identity` / `get_request_context`
    - src/nativespeaker/api/app/lifespan.py in full
    - src/nativespeaker/api/app/main.py in full
    - src/nativespeaker/api/routers/chats.py (the house router shape: `APIRouter`, `Depends()`-only parameters, no `Request`)
    - src/nativespeaker/api/models/identities.py, src/nativespeaker/api/models/users.py, src/nativespeaker/api/models/subscriptions.py (37-04's new module)
    - src/nativespeaker/api/auth/retry.py and src/nativespeaker/api/auth/firebase.py and src/nativespeaker/api/auth/classifier.py (37-02 / 37-05)
    - src/nativespeaker/api/auth/adapters.py `ProviderDataResult` **as amended by 37-05 Task 1**: it declares `outcome`, `entries`, `email: str | None = None`, and `email_verified: bool = False`, the last two populated on the `ok` arm from the same `UserRecord` the entries came from. `auth/classifier.py`'s `email_to_persist(result) -> str | None` is the single site that evaluates §02 step 10's copy rule over that pair. Both are settled contracts committed in wave 2 — consume them, do not redesign them, and do not re-derive the rule anywhere in this plan.
    - tests/e2e/conftest.py lines 36-141 (`_app_config`, `_app_lifespan`, `_db_transaction` with its `join_transaction_mode="create_savepoint"`, and `stub_verifier`)
    - .planning/phases/37-post-auth-create-user/37-RESEARCH.md § "Pattern 1", "Pattern 3", "Pitfall 6", "Pitfall 9", "Pitfall 10", "Code Example 4"
  </read_first>
  <precondition>Plans 37-01, 37-03, 37-04 and 37-05 are committed: the `operation_variant` column is gone from the live database, `IDENTITY_ALREADY_LINKED` and `OPERATION_NOT_ALLOWED` are registered, `StorePurchaseToken` is importable, `auth/firebase.py` / `auth/classifier.py` / `auth/retry.py` exist, and `ProviderDataResult` carries `email` and `email_verified` with `email_to_persist` importable from `nativespeaker.api.auth`.</precondition>
  <action>
Wire ONE entry point through every layer to the far end of the stack. No other call sites, no batching, real error handling on the single path.

**Registry and router, in the same commit.** Append `RouteMetadata(method="POST", path="/auth/create-user", category=Category.authenticated, operation=AuthOperation.create_user, preauth_callable=True, challenge_bearing=True)` to `auth/registry.py`'s `REGISTRY` tuple, and register the router in `app/main.py` in the same commit. `assert_route_enumeration` is set equality in both directions at real startup (`registry.py:162-165`), so a declaration without a route — or a route without a declaration — aborts boot and every test in the suite fails, not just the new ones. `category` must be `authenticated`: condition 8 rejects a non-`None` `operation` on any other category, and `preauth_callable` is the flag that admits the unlinked caller, not the category. If the new router imports the registry, follow the same local-import discipline `assert_route_enumeration` uses at lines 141-142 to avoid an import cycle.

**Dependencies.** Add three `Depends()` accessors to `app/dependencies.py`, keeping the route `Depends()`-only per the v1.3 convention (no handler takes `Request`): one returning `request.scope["query_string"]` as `bytes` (the seam phases 40/41/42 will reuse — `RequestContext` deliberately carries no query string, and a first-value-wins accessor cannot see the duplicated-`challenge` case §02 pins to `invalid_request`); one returning `request.app.state.challenge_store`; one returning `request.app.state.firebase_adapter`. Reuse the existing `get_db`, `get_request_context`, `get_preauth_identity`, and `request.app.state.audit_writer`.

**Lifespan.** Build the issuer-keyed Firebase apps with 37-05's `build_admin_apps(config)` and put the adapter on `app.state.firebase_adapter`, immediately after the challenge store block. Absent credential → `build_admin_apps` returns `{}`, the adapter is still constructed, and boot proceeds (37-03's recorded absent-vs-malformed split); a malformed credential already failed at configuration load. Add a comment reversing `lifespan.py:66-69`'s note that no Firebase client is read at boot — it now is, behind the §7.1 seam and never as an ambient client.

**The route.** One `APIRouter` in a new `routers/auth.py`, exported from `routers/__init__.py`. One registered path function that reads the body, calls `classify_mode_signal(raw_query, body_challenge_id)`, and dispatches — a `None` classification returns `error_response(INVALID_REQUEST)` with no side effects, no audit row, and no internal result, and the two modes go to two module-level functions the path function calls. (Whether prepare and completion are one function or two behind one route is Claude's discretion per 37-CONTEXT.md; this plan chooses one registered path function dispatching to two, so each mode's body reads on its own.)

The request body model declares `challenge_id` permissively — typed `object` or `Any`, defaulting to `None` — and the body parameter itself defaults to `None` so a bodyless `?challenge=true` prepare request is not a 422. §02 pins mode-signal violations to `invalid_request` (400) with no audit row; a Pydantic `str | None` annotation would surface a non-string handle as FastAPI's 422 `validation_error`, a class §02 never names for this route (Pitfall 6). Note `classify_mode_signal`'s deliberate asymmetry: a whitespace-padded handle is **not** `invalid_request` — it is `challenge_not_found`, because `locate` compares byte-for-byte.

**Prepare, happy path.** Take `get_preauth_identity()` and `get_request_context()`. Call `ChallengeStore.issue(session, operation=AuthOperation.create_user, identity=identity, now=ctx.evaluated_at)` — note the signature has no `operation_variant` after 37-01 — inside one committed transaction, and return exactly two fields, `challenge_id` and `expires_at`, with `Cache-Control: no-store`. Nothing else about the challenge is ever disclosed (§6.1). Prepare mutates no business state. The already-linked fail-fast pre-check is Task 2; the tracer's prepare path is the unlinked one only.

**Completion, happy path, in §02's literal order.** Locate the row by byte-for-byte `challenge_id`; verify the binding with `ChallengeStore.verify_binding` and check `row.operation is AuthOperation.create_user`; then claim.

The claim gets **its own committed transaction** (RESEARCH open question 4, and §02 step 8 plus SHARED-INVARIANTS § Locks). Commit it before the provider call. A crash mid-lookup then leaves a permanently-claimed dead row, which §6.2 says is the design — "a claimed challenge is dead" — whereas holding the claim uncommitted across the lookup would let a second attempt win it. Write a comment saying exactly that, because it looks like an accident otherwise.

With **no transaction open**, call `lookup_with_retry(adapter, identity.issuer, identity.subject)`. Then classify with 37-05's classifier, and in the same step resolve the email with 37-05's `email_to_persist(result)` — both read the one `ProviderDataResult` the lookup returned, and nothing downstream touches the adapter again. The tracer's path is the `ok` outcome with empty providerData → `(IdentityProvider.anonymous, None)`; every other outcome and every rejecting shape belongs to 37-08.

**The consuming transaction lives in a new module, `auth/creation.py`**, not inline in the router — 37-09 must drive it directly from `tests/schema/` with two real sessions, and a function reachable only through FastAPI cannot be driven that way. It takes a session and the already-resolved facts (the request context, the pre-auth identity, the challenge row, the classified provider, the `provider_uid`, and the email to copy — typed `email: str | None`) and performs, in one transaction: re-resolve `(issuer, subject)` against `core.external_identities` INSIDE the transaction (prepare-time pre-auth status never suffices); then `savepoint = await session.begin_nested()` around the business inserts — `core.users`, exactly one ACTIVE `core.external_identities` row, and one `core.store_purchase_tokens` row per store; then `await savepoint.commit()`; then `ChallengeStore.consume(session, challenge_id=..., claim_attempt_id=ctx.attempt_id, now=ctx.evaluated_at)` and `AuditWriter.write_in_transaction(...)` on the outer transaction; then commit.

**The savepoint is mandatory and its rationale is settled — do not re-litigate it.** 37-CONTEXT.md's original default was a consume-first conditional update; RESEARCH disproved it empirically against live PostgreSQL 17.11 under the exact e2e harness configuration (`join_transaction_mode="create_savepoint"`): an `IntegrityError` marks the session's transaction rolled back, so the post-conflict `consume` and audit write both raise `PendingRollbackError`, violating §02 step 12's "consumption + rejected audit row MUST survive the business rollback" and producing exactly the generic 500 step 12 forbids. Savepoint nesting under that harness was directly tested and works. The tracer builds the savepoint even though its single path never rolls back to it, because the transaction's *shape* is the architectural fact this slice exists to prove; 37-09 fills in the rollback arm.

Field rules the inserts must honor: `provider_uid` is NULL for anonymous — no sentinel, so the row falls outside the provider-account reservation; `registered_at` is NULL for anonymous and non-NULL for google/apple, with no third state; `email` is written straight through from the argument, with **no** condition re-evaluated inside `creation.py` — §02 step 10's rule (copy only when the same successful `getUser` response carried a non-empty address AND `emailVerified` was true, else NULL) is already applied by 37-05's `email_to_persist`, which the router calls on the `ProviderDataResult` it just got back from `lookup_with_retry` and passes down as the `email` argument. One rule, one site: a second `if verified` inside the transaction would be a second site that can disagree. `display_name` is **never** populated; both attribution tokens are minted eagerly on every branch, one per store, each a fresh `uuid4()` in `identity_value`, using `ctx.evaluated_at` for `created_at`. Create **no** access grant, **no** free credits, **no** `core.user_monthly_usage` row, and read or modify **no** per-device grant bits — §02 step 10 forbids all of it, and a brand-new account correctly answers `quota_exceeded` on its first chat until Phase 41/42 ships.

Response: 200 carrying the classified `identity_provider` and nothing else (D-10). No backend token, no session, no cookie, no generation counter — §02 step 14 and SHARED-INVARIANTS § Tokens. The attribution tokens are **not** in the response (D-11); Phase 39's rewritten `GET /users/me` surfaces them, in one place.

Never recompute `evaluated_at` or `attempt_id` — both are on `RequestContext` (35 D-02). Never write a `compare_digest` in the handler — `HmacKeyring.actor_subject_matches`, reached through `ChallengeStore.verify_binding`, is the only permitted comparison. Never re-check `expires_at` anywhere but `ChallengeStore.claim`. Never use `rowcount` for a conditional update. Never log or audit the public `challenge_id` — correlate on `AuthChallenge.id` via the audit row's `challenge_row_id`.

**The end-to-end verification.** Add to `tests/e2e/conftest.py` a fixture that swaps `app.state.firebase_adapter` for a fake returning a scripted `ProviderDataResult` (scriptable on all four of the amended dataclass's fields, since 37-10 drives the step 10 email cases through this same fixture) and restoring it afterwards — the same shape as the existing `stub_verifier` fixture, and the reason it works is the same: the handler reads the adapter off the application per request. Add `tests/e2e/test_create_user.py` with ONE case for now: using `stub_verifier` to mint a token for an unlinked subject and the fake adapter returning `ok` with empty entries, POST the prepare request, then POST the completion with the returned handle, and assert the 200 body, the row counts, the field values, the audit row, and the consumed challenge. This is a real end-to-end check of the one path, not a set of per-layer unit tests.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m e2e tests/e2e/test_create_user.py</automated>
    <automated>.venv/bin/pytest -q</automated>
    <automated>.venv/bin/pytest -q -m e2e</automated>
  </verify>
  <acceptance_criteria>
    - `src/nativespeaker/api/auth/registry.py` `REGISTRY` contains an entry with `path="/auth/create-user"`, `method="POST"`, `preauth_callable=True`, `challenge_bearing=True`, `operation=AuthOperation.create_user`, `category=Category.authenticated`
    - `src/nativespeaker/api/app/main.py` includes the new auth router
    - `.venv/bin/python -c "import nativespeaker.api.app.main"` exits 0 and the app boots under the e2e lifespan (`assert_route_enumeration` passes)
    - `POST /auth/create-user?challenge=true` for an unlinked caller returns 200, a body whose key set is exactly `{"challenge_id", "expires_at"}`, and the header `Cache-Control: no-store`
    - After that prepare request, `SELECT count(*) FROM core.users` is unchanged
    - `POST /auth/create-user` with the returned `challenge_id` and a fake adapter returning `ok` with no entries returns 200 with body key set exactly `{"identity_provider"}` and value `"anonymous"`
    - After that completion: exactly 1 new `core.users` row with `display_name IS NULL` and `registered_at IS NULL`; exactly 1 `core.external_identities` row with `identity_state='active'`, `provider='anonymous'`, `provider_uid IS NULL`; exactly 2 `core.store_purchase_tokens` rows for that user, one per provider, with distinct `identity_value`s
    - After that completion: 0 `core.access_grants` rows and 0 `core.user_monthly_usage` rows for that user
    - After that completion: exactly 1 `audit.auth_events` row with `operation='create_user'` and `result='succeeded'`, whose `details` JSON contains no `challenge_id` key at any depth
    - After that completion: the `core.auth_challenges` row has `consumed_at` set and `preauth_subject_hash IS NULL`
    - `src/nativespeaker/api/auth/creation.py` contains `begin_nested`
    - `auth/creation.py`'s signature accepts exactly one email-related parameter, `email: str | None`, and no verification-flag parameter of any name — checked by introspecting the function's parameter list, so the copy condition provably cannot be re-evaluated inside the transaction
    - `src/nativespeaker/api/routers/auth.py` calls `email_to_persist` exactly once and passes its result as `creation.py`'s `email` argument
    - The tracer's e2e completion, driven by a fake returning `ok` with no entries and `email=None`, leaves `core.users.email IS NULL`
    - `grep -c "compare_digest" src/nativespeaker/api/routers/auth.py src/nativespeaker/api/auth/creation.py` returns 0 for both
    - `grep -c "rowcount" src/nativespeaker/api/routers/auth.py src/nativespeaker/api/auth/creation.py` returns 0 for both
    - `.venv/bin/pytest -q` and `.venv/bin/pytest -q -m e2e` both exit 0
  </acceptance_criteria>
  <done>The single anonymous happy path works end-to-end over real HTTP against the real barrier, the real challenge store, and a real database, and it is committed together with its registry declaration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepare mode's already-linked fail-fast and its audit row</name>
  <files>src/nativespeaker/api/routers/auth.py, tests/e2e/test_create_user.py</files>
  <read_first>
    - src/nativespeaker/api/routers/auth.py (as written by Task 1)
    - /home/init/native-speaker/specs/auth-refactor-phases/02-create-user.md lines 45-50 (prepare mode, all five steps) and line 71 (`identity_already_linked`'s three triggers)
    - src/nativespeaker/api/auth/audit.py lines 256-312 (`write_standalone` and `write_in_transaction` signatures)
    - src/nativespeaker/api/auth/identity.py (how an `(issuer, subject)` is resolved to a row — the pre-check must use the same query the barrier uses, not a second one)
    - src/nativespeaker/api/errors.py (`IDENTITY_ALREADY_LINKED`, added by 37-03)
  </read_first>
  <behavior>
    - A prepare request from a caller whose `(issuer, subject)` already resolves to an ACTIVE identity returns 409 with body `{"code": "identity_already_linked"}` and issues no challenge
    - That rejection writes exactly one `audit.auth_events` row with `operation='create_user'` and `result='identity_already_linked'`
    - `SELECT count(*) FROM core.auth_challenges` is unchanged across that rejected prepare request
    - A prepare request from an unlinked caller still returns 200 and the two-field body
    - A second prepare request from the same unlinked caller issues a second, distinct `challenge_id` — prepare is not idempotent and does not reuse a row
  </behavior>
  <action>
Add §02 prepare step 1's fail-fast: before issuing anything, resolve `(issuer, subject)` against `core.external_identities` and reject an ACTIVE linked row with `identity_already_linked`, issuing no challenge.

Two properties §02 states about this check that the code must not overstate: it is **best-effort fail-fast only, racy, and never authoritative** — the authoritative re-resolution is inside the consuming transaction (step 10), and the race arbiters are the database's `UNIQUE (issuer, subject)` and `UNIQUE (user_id)` alone (step 12). Say so in a comment so nobody later "strengthens" it into a lock or a pre-SELECT-then-INSERT, which would add a window the constraint does not have.

Historical identity and blocked user are **already rejected by the barrier** with `account_unavailable` and must not be re-checked here — a pre-auth identity context reaching this handler means the barrier resolved no active identity row, so the only thing left to detect is the racy already-linked case. Note the barrier hands a `PreAuthIdentity` here, so the pre-check is a direct query, not a read of the context.

The rejection is on the audited path (the route's metadata carries a non-`None` `operation`, which is what `barrier.py:175` keys on), and no consuming transaction exists at prepare time, so the audit row is a **standalone durable** write through `AuditWriter.write_standalone`, written before the response returns. Build `details` with `build_details(...)`; the keyword-only builder makes a seventh top-level key a `TypeError` at the call site rather than a CHECK violation at insert. Put the route path, the method, the operation, the attempt id, the mode signal as a boolean, and the client-IP **bucket kind** in `context`; never the address, and never the public `challenge_id`.

Extend `tests/e2e/test_create_user.py` with the five behaviors above, seeding the already-linked case through the existing `seed_identity` helper in `tests/e2e/conftest.py`.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m e2e tests/e2e/test_create_user.py</automated>
    <automated>.venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - A prepare request from an already-linked caller returns HTTP 409 with body exactly `{"code": "identity_already_linked"}`
    - `core.auth_challenges` row count is unchanged across that request
    - Exactly one `audit.auth_events` row is written with `result='identity_already_linked'` and `operation='create_user'`
    - Two consecutive prepare requests from one unlinked caller return two different `challenge_id` values
    - `src/nativespeaker/api/routers/auth.py` contains a comment stating the pre-check is best-effort and non-authoritative
    - `.venv/bin/pytest -q -m e2e` exits 0
  </acceptance_criteria>
  <done>Prepare mode is complete per §02 steps 1-5: the racy pre-check rejects, the audit row is durable, and the happy path still issues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The exhaustive mode-signal partition, at unit level, with no audit row</name>
  <files>tests/unit/test_create_user_modes.py, tests/unit/conftest.py, tests/unit/test_route_registry.py</files>
  <read_first>
    - src/nativespeaker/api/auth/modesignal.py in full (the partition it already implements and unit-tests at `tests/unit/test_mode_signal.py`)
    - tests/unit/test_mode_signal.py (so the new file tests the *route's dispatch*, not the classifier again)
    - tests/unit/conftest.py in full — `make_test_verifier`, `TEST_IDENTITY`, and the `client` fixture that overrides `get_linked_identity`
    - tests/unit/test_route_registry.py lines 151-182 (conditions 6 and 7, which already have `create_user` placeholder cases)
    - src/nativespeaker/api/app/dependencies.py (`get_preauth_identity`, and Task 1's new accessors)
    - /home/init/native-speaker/specs/auth-refactor-phases/02-create-user.md lines 40-43
  </read_first>
  <behavior>
    - `?challenge=true` with no body handle dispatches to prepare
    - a body handle with no `challenge` parameter dispatches to completion
    - `?challenge=true` together with a body handle returns 400 `invalid_request`
    - neither signal returns 400 `invalid_request`
    - `?challenge=true&challenge=true` (duplicated parameter) returns 400 `invalid_request`
    - `?challenge=1`, `?challenge=TRUE`, `?challenge=` and `?challenge` each return 400 `invalid_request`
    - a body `challenge_id` that is `null`, `""`, `"   "`, an integer, a list, or an object returns 400 `invalid_request` — never 422 `validation_error`
    - a body `challenge_id` with surrounding whitespace dispatches to **completion** and is passed through untouched (it becomes `challenge_not_found`, not `invalid_request`)
    - every `invalid_request` rejection writes zero `audit.auth_events` rows, issues nothing, and consumes nothing
    - one prepare-mode request following an `invalid_request` rejection still succeeds — the rejection had no side effect
  </behavior>
  <action>
Write `tests/unit/test_create_user_modes.py` against a FastAPI test app that mounts the new auth router with the identity accessors overridden — mirroring `tests/unit/conftest.py`'s existing `client` fixture, which overrides `get_linked_identity` rather than installing the barrier, but overriding `get_preauth_identity` and the new adapter/challenge-store accessors instead. Add whatever shared fixture that needs to `tests/unit/conftest.py`, including the reusable fake `FirebaseAdminAdapter` that 37-08 and 37-09 will also need: a class returning a caller-supplied `ProviderDataResult` — all four of the amended dataclass's fields caller-supplied, the two email ones included — and recording its call count and arguments. Put it in `tests/unit/conftest.py`, not in a per-test file — `37-VALIDATION.md`'s Wave 0 list names it as shared by every substituted test.

Cover the ten behaviors above. Assert the status **and** the body code, since `invalid_request` (400) and `validation_error` (422) are different classes and Pitfall 6 is precisely the failure where the permissive body typing is quietly lost and every wrong-typed handle becomes a 422.

The zero-audit-row assertion is the one that needs care in a unit context: assert it by substituting a recording audit writer and checking it was never called, rather than by counting database rows. The e2e row-count version belongs in 37-08.

Also extend `tests/unit/test_route_registry.py`: the real registry now contains the `preauth_callable` route, so add a case asserting `POST /auth/create-user` is the **only** entry in the production `REGISTRY` with `preauth_callable=True` (CREATE-01's structural half), and confirm the existing condition-6 and condition-7 cases still describe the real table rather than a fixture-only one.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q tests/unit/test_create_user_modes.py tests/unit/test_route_registry.py tests/unit/test_mode_signal.py</automated>
    <automated>.venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_create_user_modes.py` exists and covers all ten behaviors listed above
    - Each `invalid_request` case asserts status 400 AND body `{"code": "invalid_request"}`
    - At least one case sends `challenge_id` as an integer and asserts 400, not 422
    - One case sends a whitespace-padded handle and asserts it reaches completion dispatch rather than being rejected
    - A shared fake `FirebaseAdminAdapter` fixture exists in `tests/unit/conftest.py` and records call count and arguments
    - `tests/unit/test_route_registry.py` contains a case asserting exactly one entry in the production `REGISTRY` has `preauth_callable=True` and that it is `("POST", "/auth/create-user")`
    - `.venv/bin/pytest -q` exits 0
  </acceptance_criteria>
  <done>CREATE-02's partition is proven exhaustively at unit speed, the no-side-effect property is asserted rather than assumed, and the shared fake adapter every later substituted test needs exists in one place.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created by this plan:
- `src/nativespeaker/api/routers/auth.py` (new file): the `APIRouter` instance, the registered path function for `POST /auth/create-user`, the permissive request body model (field `challenge_id`), the prepare-mode handler, the completion-mode handler, and the prepare/completion response models (prepare: `challenge_id`, `expires_at`; completion: `identity_provider`)
- `auth_router` added to `src/nativespeaker/api/routers/__init__.py` `__all__`
- `src/nativespeaker/api/auth/creation.py` (new file): the consuming-transaction function (name chosen at implementation time) taking a session plus the resolved facts
- `src/nativespeaker/api/app/dependencies.py`: three new `Depends()` accessors — the raw query-string accessor, the challenge-store accessor, and the Firebase-adapter accessor (names chosen at implementation time)
- `app.state.firebase_adapter` (new application-state attribute set in `app/lifespan.py`)
- A new `RouteMetadata` entry in `auth/registry.py` `REGISTRY` for `("POST", "/auth/create-user")`
- `tests/e2e/test_create_user.py`, `tests/unit/test_create_user_modes.py` (new files)
- A fake `FirebaseAdminAdapter` fixture in `tests/unit/conftest.py` and an adapter-swapping fixture in `tests/e2e/conftest.py` (names chosen at implementation time)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated internet → this route | The single pre-auth-callable surface in the system; anyone holding any valid Firebase ID token reaches it. |
| client → challenge handle | `challenge_id` is a secret capability handle: possession is the only thing that locates a row. |
| service → Firebase Admin API | The sole source of the account's classification and of `provider_uid`. |
| handler → database | The route mints accounts; the database's UNIQUE constraints are the only race arbiter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-24 | Elevation of Privilege | `preauth_callable=True` spreading to another route | high | mitigate | `registry.py:61` pins `_PREAUTH_CALLABLE_ROUTE` and condition 6 fails boot on any other route declaring it. A unit case asserts exactly one production entry carries the flag. |
| T-37-25 | Spoofing | Burning another user's in-flight challenge | high | mitigate | `verify_binding` and the operation check run **before** the claim and leave the row unconsumed (`challenges.py:33-37`), so a wrong presenter cannot consume the rightful user's challenge. The handler adds no comparison of its own. |
| T-37-26 | Information Disclosure | The public `challenge_id` reaching a log, audit row, URL, or error body | high | mitigate | Body-only transport (it is a body field, never a path or query parameter); audit correlation uses `AuthChallenge.id` via `challenge_row_id`; `auth/audit.py:94`'s redactor drops any key containing the fragment `challenge_id` at any depth; an acceptance criterion asserts the written `details` contains no such key. |
| T-37-27 | Denial of Service | Unbounded account creation on an unmetered route | high | accept | **Accepted under D-01, recorded as a flagged SHARED-INVARIANTS conflict, not silently resolved.** §02 names two backend IP-keyed entries and calls the gateway limits load-bearing; Phase 35 D-05 deleted backend rate limiting from the product and the developer reaffirmed it, explicitly rejecting a narrow per-route reinstatement. Mitigants on record: a valid Firebase ID token is still required, a new account carries **zero** entitlement (§02 step 10 creates no grant), and the v1.6 Helm chart's existing Envoy limits remain in place untouched. Closes when FOUND-09 lands in v2.1. |
| T-37-28 | Denial of Service | A database transaction held across the Firebase call | high | mitigate | §02's ordering is preserved literally: the claim commits in its own transaction (step 5), the lookup runs with nothing open (step 8), and the consuming transaction opens after it (step 10). SHARED-INVARIANTS § Locks forbids the alternative, which converts provider latency into a database-wide stall. |
| T-37-29 | Tampering | A partial account surviving a failed insert | high | mitigate | All three inserts are inside one `begin_nested()` savepoint inside one transaction; either insert failing rolls back every business mutation. 37-09 proves the rollback arm; the tracer proves the shape. |
| T-37-30 | Elevation of Privilege | A new account silently acquiring entitlement | high | mitigate | No grant, no free credits, and no `core.user_monthly_usage` row are written; acceptance criteria assert zero rows in both tables after a successful completion. A new account correctly answers `quota_exceeded` on its first chat until Phase 41/42 — that is correct behavior, not a regression. |
| T-37-31 | Information Disclosure | Prepare disclosing more than the two permitted fields | medium | mitigate | The response model declares exactly `challenge_id` and `expires_at`; an acceptance criterion asserts the response body's key set exactly. §6.1: nothing else about the challenge is ever disclosed — not the row id, not the binding, not the operation. |
| T-37-32 | Spoofing | Minting a backend token or session | high | mitigate | Nothing is minted. §02 step 14 and SHARED-INVARIANTS § Tokens: the same Firebase ID token simply resolves as linked on the next request. The success response carries one field. |
| T-37-33 | Information Disclosure | `display_name` or unverified email persisted | medium | mitigate | `display_name` is never populated (acceptance criterion asserts NULL); `email` is copied only when the same successful `getUser` response carried a non-empty address AND `emailVerified` was true. Breadcrumb: general PII-minimization is canon (GDPR); what is pinned here is §02 step 10's specific two-condition rule. |
| T-37-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install in this plan. |
</threat_model>

<verification>
- `.venv/bin/pytest -q` and `.venv/bin/pytest -q -m e2e` both green; the app boots with `assert_route_enumeration` passing.
- The anonymous happy path produces exactly the row set §02 step 10 specifies, and exactly one `succeeded` audit row.
- The mode-signal partition is exhaustive at unit level with zero side effects on the rejection arm.

**Wave note:** this plan runs no `-m schema` command and is wave 3's only plan.
</verification>

<success_criteria>
- One committed slice in which an unlinked caller goes from no account to an account over real HTTP.
- The registry declaration and the router registration in the same commit.
- The consuming transaction reachable as a plain function, so 37-09 can drive it with two real sessions.
</success_criteria>

<output>
Create `.planning/phases/37-post-auth-create-user/37-07-SUMMARY.md` when done. Record: the module and symbol names chosen (Claude's discretion per 37-CONTEXT.md); the decision that prepare and completion are two functions behind one registered path function; that the claim commits in its own transaction and why; and what the structured security log records on each fail-closed branch.
</output>
