---
phase: 37-post-auth-create-user
plan: 10
type: execute
wave: 5
depends_on: ["37-07", "37-08", "37-09"]
files_modified:
  - tests/e2e/conftest.py
  - tests/e2e/test_create_user.py
  - .env.example
autonomous: false
requirements: [CREATE-01, CREATE-03]

user_setup:
  - service: firebase
    why: "§02 step 8 requires exactly one mandatory fail-closed Firebase Admin getUser providerData read on EVERY completion — anonymous and registered alike, no branch skips it. No service-account credential exists anywhere in the repo today (verified: no service_account or private_key_id string under config/ or in .env), so every real completion, including the real-anonymous e2e D-09 mandates, is blocked without one."
    env_vars:
      - name: FIREBASE_SERVICE_ACCOUNT_JSON
        source: "Firebase Console -> Project settings -> Service accounts -> Generate new private key. Paste the entire downloaded JSON as a single line into the gitignored .env (D-08: inline JSON, no file mount, no GOOGLE_APPLICATION_CREDENTIALS, no ADC). Use the same project the existing JWT_PROJECT_ID / JWT_API_KEY point at, or the anonymous fixture will mint a token the Admin client cannot resolve."

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A registered-flow completion driven by a substituted adapter returning exactly one google.com entry creates an identity row with provider=google, provider_uid equal to that entry's uid, and a non-NULL registered_at"
    - "The same for exactly one apple.com entry and provider=apple"
    - "email is copied to core.users only when the getUser response carried a non-empty address AND emailVerified was true; it is NULL in every other case"
    - "display_name is NULL after every completion, on every branch"
    - "Both attribution tokens are minted on the registered branch as well as the anonymous branch"
    - "A genuinely anonymous Firebase user minted through Identity Toolkit accounts:signUp yields empty providerData from the real Admin SDK and completes as anonymous"
    - "The real-anonymous e2e proves the real SDK returns the shape the closed classifier expects — the assumption that otherwise fails silently in production"
    - "With FIREBASE_SERVICE_ACCOUNT_JSON absent, the service still boots, prepare mode still works, and completion fails closed as verification_temporarily_unavailable rather than crashing"
  artifacts:
    - tests/e2e/test_create_user.py
    - tests/e2e/conftest.py
  key_links:
    - "Identity Toolkit accounts:signUp with no email/password -> a genuinely anonymous Firebase user -> empty providerData -> the classifier's anonymous arm: the only credential shape that can drive a successful completion for real"
    - "app.state.firebase_adapter <- the e2e adapter-swapping fixture: the real adapter must be left in place for the real-anonymous case and only that case"
---

<objective>
Complete D-09's split test coverage: the registered flow and every remaining shape through a substituted adapter (which needs no credential), and the anonymous flow **for real** against a genuine Firebase anonymous user and a genuine Admin lookup.

Purpose: the existing e2e credential can only ever exercise the rejection arm. `tests/e2e/conftest.py:50-56` signs in with `accounts:signInWithPassword`, so `providerData == [{providerId: "password"}]` — an unrecognized single entry that §02 step 9's closed classifier rejects. A completion test written against `firebase_token` "passes" while testing `provider_not_linked`. D-09's answer is a real anonymous fixture for the one flow that can be minted reproducibly, and a substituted adapter for everything else: a real Google- or Apple-linked account cannot be scripted, and 37-CONTEXT.md § Deferred Ideas records that as unreproducible shared CI state to revisit only if the fake drifts.

Output: the registered-flow and field-rule coverage (Task 1, unblocked), the credential (Task 2, human), and the real-anonymous proof (Task 3).

**Sequencing note:** Task 1 needs no credential and runs first deliberately. Only Task 3 is blocked.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-post-auth-create-user/37-CONTEXT.md
@.planning/phases/37-post-auth-create-user/37-RESEARCH.md
@/home/init/native-speaker/specs/auth-refactor-phases/02-create-user.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The registered flow and the field rules, through the substituted adapter</name>
  <files>tests/e2e/test_create_user.py</files>
  <read_first>
    - tests/e2e/test_create_user.py (as left by 37-07 and 37-08)
    - tests/e2e/conftest.py (the `stub_verifier`, `_db_transaction`, `seed_identity` helpers and 37-07's adapter-swapping fixture)
    - /home/init/native-speaker/specs/auth-refactor-phases/02-create-user.md line 62 (step 10's field rules verbatim — `provider_uid`, `registered_at`, `email`, `display_name`, the attribution tokens)
    - src/nativespeaker/api/auth/creation.py and src/nativespeaker/api/routers/auth.py
    - src/nativespeaker/api/models/identities.py (the provider/provider_uid agreement rule the database enforces)
  </read_first>
  <behavior>
    - A completion whose substituted adapter returns `ok` with exactly one `google.com` entry (uid `"g-123"`) returns 200 with `identity_provider == "google"`, and creates an identity row with `provider='google'`, `provider_uid='g-123'`, `identity_state='active'`
    - That completion's `core.users` row has a non-NULL `registered_at`
    - The same for exactly one `apple.com` entry and `identity_provider == "apple"`
    - Both registered completions mint exactly two `core.store_purchase_tokens` rows, one per store, with distinct `identity_value`s
    - `core.users.display_name` is NULL after every completion in the file
    - With a `getUser` response carrying a non-empty email and `emailVerified` true, `core.users.email` holds that address
    - With a non-empty email and `emailVerified` false, `core.users.email` is NULL
    - With an empty or absent email and `emailVerified` true, `core.users.email` is NULL
    - A second registered completion for a *different* `(issuer, subject)` reusing the same `provider_uid` is refused with 403 `operation_not_allowed` and audits `provider_account_already_linked`
    - Every completion in the file leaves 0 `core.access_grants` and 0 `core.user_monthly_usage` rows
  </behavior>
  <action>
Extend `tests/e2e/test_create_user.py` with the registered flows and the step 10 field rules, all driven by 37-07's adapter-swapping fixture and `stub_verifier`, so nothing here needs a credential.

To carry `email` and `emailVerified` through to the assertions, the substituted adapter must return whatever shape the handler actually reads them from. Check `auth/firebase.py` and `auth/creation.py` first: if 37-05's `ProviderDataResult` does not carry the email fields (foundation's dataclass declares only `outcome` and `entries`), then the handler must be reading them from somewhere else, and this task's first job is to name that path and make the fake supply it. If the plumbing does not exist yet, add it here as the minimal extension the field rule requires — a per-result email/`email_verified` pair populated inside the same offloaded `getUser` call that materializes `provider_data`, since §02 pins the copy to "the same successful `getUser` response". Record the shape chosen in the SUMMARY; foundation's `ProviderDataResult` living in `auth/adapters.py` is Phase 35's, so if it must grow a field, say so explicitly as a foundation amendment rather than editing it quietly.

The `provider_account_already_linked` case is the one that proves 37-09's third constraint name fires end-to-end rather than only in a unit: seed a committed identity row for a different `(issuer, subject)` carrying `provider='google', provider_uid='g-123'`, then drive a completion for a fresh subject whose adapter returns the same uid. The partial index `ix_external_identities_provider_account` spans historical rows too, so add a second variant where the pre-existing row is `historical` and assert it still refuses — retirement never frees a provider account.

Keep every case inside the e2e rollback fixture; none of these needs real commits (37-09 owns the two that do).
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m e2e tests/e2e/test_create_user.py</automated>
    <automated>.venv/bin/pytest -q</automated>
    <automated>.venv/bin/pytest -q -m e2e</automated>
  </verify>
  <acceptance_criteria>
    - Cases exist for both `google` and `apple` asserting `identity_provider` in the response body, `provider` and `provider_uid` on the identity row, and a non-NULL `registered_at`
    - Three email cases exist (verified+non-empty, unverified+non-empty, empty+verified) asserting the copy rule
    - Every case asserts `core.users.display_name IS NULL`
    - Both registered cases assert exactly two `core.store_purchase_tokens` rows with distinct `identity_value`s
    - A case asserts 403 `operation_not_allowed` with audit `provider_account_already_linked` for a `provider_uid` already reserved by an ACTIVE identity row, and a second case asserts the same for a `historical` one
    - Every case asserts 0 `core.access_grants` and 0 `core.user_monthly_usage` rows
    - `.venv/bin/pytest -q -m e2e` exits 0
  </acceptance_criteria>
  <done>The registered flow, every step 10 field rule, and the provider-account reservation are covered without a credential.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Provision the Firebase service-account credential</name>
  <what-built>
Everything in Phase 37 except one thing: the real Firebase Admin lookup. `auth/firebase.py`'s `build_admin_apps` reads `FIREBASE_SERVICE_ACCOUNT_JSON` from the gitignored `.env`, parses it with `credentials.Certificate`, and registers one named app per configured issuer. With the variable absent it returns an empty mapping, the service still boots, prepare mode still works, and every completion fails closed as 503 `verification_temporarily_unavailable` — which is correct behavior, not a crash, and is why every other task in this phase ran without a credential.

RESEARCH verified the credential is genuinely absent: no `service_account` or `private_key_id` string exists anywhere under `config/` or in `.env`, and `app/lifespan.py:66-69` records that Phase 35 removed the last Firebase client at boot. The existing `.env` keys are `CONFIG_DIR`, `APPLE_CERTS_DIR`, `POSTGRES_*`, `DB_*`, `OPENAI_API_KEY`, `JWT_PROJECT_ID`, `JWT_API_KEY`, `FIREBASE_TEST_EMAIL`, `FIREBASE_TEST_PASSWORD`.
  </what-built>
  <how-to-verify>
1. Open the Firebase Console for the project that `JWT_PROJECT_ID` in `.env` names. **It must be that same project** — the anonymous test fixture mints its token with `JWT_API_KEY` against that project, and an Admin client for a different project would answer `selection_failure` on every lookup.
2. Go to Project settings → Service accounts → **Generate new private key**. Confirm the download.
3. Open the downloaded JSON and collapse it to a single line.
4. Add it to `/home/init/native-speaker/ns-api-gateway/.env` as `FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}` — the whole JSON as the value. `.env` is gitignored (`.gitignore:9`); **do not** put it in `config/config.yaml`, which is tracked in git and whose own comment at lines 26-31 explains that the YAML outranks the environment, so a block there would both leak the key and make the `.env` value unreachable.
5. Delete the downloaded file from your Downloads folder.
6. Confirm it loads without exposing it: run
   `.venv/bin/python -c "from nativespeaker.api.config import EnvironmentConfig; c = EnvironmentConfig().app_config; print('firebase configured:', c.firebase.service_account_json is not None)"`
   and expect `firebase configured: True`. If it prints `False`, the variable is not being read — check for a stray quote or a line break in the value.
7. Confirm the app builds a named Firebase app at boot: run `.venv/bin/pytest -q -m e2e tests/e2e/test_health.py` and confirm it passes (this exercises the lifespan).

Two things to know before approving. First, this is a real credential with real Admin privileges on the test project; D-08 accepts `.env` as its home, and `.planning/todos/pending/secret-manager-integration.md` already tracks moving both this and the HMAC keys to Secret Manager. Second, once Task 3 runs, each execution creates a **real, permanent anonymous user** in the shared Firebase project with no cleanup path (SHARED-INVARIANTS deletes purge jobs, and `auth.delete_user` teardown is itself an Admin call). Accumulation is the accepted cost; say so if you would rather Task 3 add a `delete_user` teardown instead.
  </how-to-verify>
  <resume-signal>Type "approved" once `firebase configured: True` prints, or describe what went wrong. Type "skip" to leave Task 3 unrun and the real-anonymous coverage recorded as an outstanding manual verification in `37-VALIDATION.md`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: The real-anonymous end-to-end proof (D-09)</name>
  <files>tests/e2e/conftest.py, tests/e2e/test_create_user.py, .env.example</files>
  <read_first>
    - tests/e2e/conftest.py lines 36-80 (`_app_config`, the `firebase_token` fixture at 44-60 and its `accounts:signInWithPassword` REST idiom, `_app_lifespan`, `async_client`)
    - tests/e2e/test_create_user.py (as left by Task 1)
    - src/nativespeaker/api/auth/firebase.py (37-05)
    - .planning/phases/37-post-auth-create-user/37-RESEARCH.md § "Pitfall 8" and "Code Example 6"
    - .planning/phases/37-post-auth-create-user/37-VALIDATION.md § "Manual-Only Verifications"
  </read_first>
  <precondition>`FIREBASE_SERVICE_ACCOUNT_JSON` is set in `.env` for the same Firebase project as `JWT_PROJECT_ID`, and Task 2 was approved.</precondition>
  <action>
Add a session-scoped fixture to `tests/e2e/conftest.py` minting a **genuine** anonymous Firebase user, mirroring the REST idiom the existing `firebase_token` fixture already uses at line 50: `POST https://identitytoolkit.googleapis.com/v1/accounts:signUp?key={_app_config.jwt.api_key}` with body `{"returnSecureToken": true}` and **no** `email` and **no** `password` — Google's Identity Platform reference states of the `email` field, "An anonymous user will be created if not provided". Return the `idToken` and the `localId`. Use the v1 `identitytoolkit.googleapis.com/v1/accounts:signUp` form, not the legacy v3 `relyingparty/signupNewUser` endpoint.

RESEARCH assumption A1 flags that the Identity Platform reference lists `email`, `password`, `displayName`, `photoUrl`, `phoneNumber`, `disabled`, `emailVerified`, `idToken`, `localId`, `mfaInfo[]`, `clientType`, `recaptchaVersion` but **not** `returnSecureToken` — the field is documented in the Firebase Auth REST reference and is already used successfully at `tests/e2e/conftest.py:55`. If the field is ignored the response will simply lack `idToken` and the fixture fails loudly on `data["idToken"]` at first run rather than degrading silently. Record A1 as closed either way in the SUMMARY.

Mark the fixture and the test that uses it so they skip cleanly — not fail — when `FIREBASE_SERVICE_ACCOUNT_JSON` is unset, so a contributor without the credential still gets a green `-m e2e` run and a visible skip reason naming the variable.

Then add the real-anonymous case to `tests/e2e/test_create_user.py`. It uses the **real** `app.state.firebase_adapter` — do not apply the adapter-swapping fixture here; that is the entire point. Drive prepare then completion with the real anonymous token, and assert: 200 with `identity_provider == "anonymous"`; one `core.users` row with `registered_at IS NULL` and `display_name IS NULL`; one ACTIVE `core.external_identities` row with `provider='anonymous'` and `provider_uid IS NULL`; two `core.store_purchase_tokens` rows; one `succeeded` audit row. Add one assertion that is the real point of this test and that no substituted adapter can make: that the real SDK returned **empty** providerData for a genuinely anonymous user — assert it directly by calling the real adapter's `get_user_provider_data` for that `localId` and checking `outcome is ok` and `entries == ()`. That is the assumption which otherwise fails silently in production.

Update `.env.example`'s `FIREBASE_SERVICE_ACCOUNT_JSON` line's comment to note that e2e's real-anonymous case skips without it.

Finally, update `.planning/phases/37-post-auth-create-user/37-VALIDATION.md`: tick the Wave 0 items this phase completed, fill the per-task verification map from the plan task ids, and set `wave_0_complete: true`. Leave `status: draft` and `nyquist_compliant: false` for `/gsd:validate-phase` to set.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m e2e tests/e2e/test_create_user.py</automated>
    <automated>.venv/bin/pytest -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/conftest.py` contains a fixture calling `accounts:signUp` with `returnSecureToken` and neither `email` nor `password`
    - The fixture and the real-anonymous test skip with a reason naming `FIREBASE_SERVICE_ACCOUNT_JSON` when it is unset
    - The real-anonymous case does not apply the adapter-swapping fixture
    - The real-anonymous case asserts the real adapter returned `outcome is ok` and `entries == ()` for the minted anonymous user
    - The real-anonymous case asserts 200 with `identity_provider == "anonymous"`, one user row with `registered_at IS NULL`, one ACTIVE identity row with `provider_uid IS NULL`, two attribution-token rows, and one `succeeded` audit row
    - `.venv/bin/pytest -q -m ""` exits 0
    - `37-VALIDATION.md` has `wave_0_complete: true` and a populated per-task verification map
  </acceptance_criteria>
  <done>A genuinely anonymous Firebase user completes account creation through the real Admin SDK, and the empty-providerData shape the classifier depends on is proven rather than assumed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created by this plan:
- An anonymous-Firebase-token fixture in `tests/e2e/conftest.py` (name chosen at implementation time) returning `(id_token, local_id)`
- Additional cases in `tests/e2e/test_create_user.py`
- `FIREBASE_SERVICE_ACCOUNT_JSON` documented in `.env.example` (the variable itself is introduced by 37-03)
- Possibly an email / `email_verified` carrier on the provider-lookup result (Task 1) — if `ProviderDataResult` in `auth/adapters.py` must grow a field, that is a Phase 35 foundation amendment and must be recorded as one
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → `.env` | A real service-account private key with Admin privileges on the test project enters the developer's filesystem. |
| test suite → shared Firebase project | Each real-anonymous run creates a permanent user in shared state. |
| substituted adapter → real SDK | A fake that drifts from the real SDK's shape makes green tests meaningless. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-47 | Information Disclosure | Service-account key committed or left on disk | high | mitigate | The checkpoint instructs `.env` only (gitignored at `.gitignore:9`), explicitly forbids `config/config.yaml` with the reason, and instructs deleting the download. `.env.example` carries a placeholder only. Breadcrumb: general secrets-in-VCS hygiene is canon (OWASP secrets management); what is pinned here is the tracked-YAML-outranks-env precedence specific to this repo. |
| T-37-48 | Elevation of Privilege | An Admin credential for the wrong project | medium | mitigate | The checkpoint requires the project to match `JWT_PROJECT_ID`; a mismatch surfaces as `selection_failure` → 503 on every completion rather than as a silent cross-project read, because the adapter's issuer lookup is exact-match and fails closed. |
| T-37-49 | Spoofing | A green completion test that never asserts a 200 | high | mitigate | Pitfall 8's exact failure. The real-anonymous case asserts the real adapter's `entries == ()`, and 37-08 separately asserts that the `password` shape the *existing* credential produces yields 403. The two together make it impossible for the success test to be silently testing the rejection arm. |
| T-37-50 | Denial of Service | Anonymous test users accumulating in the shared project | low | accept | No cleanup path exists by design (SHARED-INVARIANTS deletes purge jobs) and `auth.delete_user` teardown would itself be an Admin call. Accumulation of a handful of empty anonymous users in a test project is harmless; the checkpoint surfaces the choice to the developer. |
| T-37-51 | Tampering | The fake adapter drifting from the real SDK's providerData shape | medium | accept | 37-CONTEXT.md § Deferred Ideas records real Google/Apple test accounts as rejected here (unreproducible shared CI state) and worth revisiting exactly if this drift appears. The real-anonymous case bounds the risk for the one shape that can be minted reproducibly. |
| T-37-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install in this plan. |
</threat_model>

<verification>
- `.venv/bin/pytest -q -m ""` green (unit + e2e + schema together — the phase's final gate per `37-VALIDATION.md`'s per-wave sampling rate).
- The real-anonymous case runs green with the credential present and skips with a named reason without it.
- `37-VALIDATION.md` updated with `wave_0_complete: true`.

**Wave note:** this plan runs `-m ""`, which includes the schema suite; it is wave 5's only plan, so there is no concurrent schema-suite runner.
</verification>

<success_criteria>
- D-09's split is fully realized: registered flow and every rejection shape substituted, anonymous flow real.
- The credential lives only in `.env` and only Task 3 was blocked on it.
- RESEARCH assumption A1 (`returnSecureToken` on `accounts:signUp`) is closed by execution.
</success_criteria>

<output>
Create `.planning/phases/37-post-auth-create-user/37-10-SUMMARY.md` when done. Record: A1 as closed-confirmed or closed-refuted; whether the email / `email_verified` carrier required a Phase 35 foundation amendment to `ProviderDataResult` and what shape it took; and the accepted accumulation of real anonymous users in the shared test project (or the developer's decision to add `delete_user` teardown instead).
</output>
