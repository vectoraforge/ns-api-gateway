---
phase: 36-rebind-pre-existing-routes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nativespeaker/api/models/llm.py
  - tests/unit/test_models.py
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/restore-strict-structured-output.md
autonomous: true
requirements: [REBIND-03, REBIND-06]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A grammatically correct phrase — the case where the model returns neither issues nor suggestions — returns 200 with `issues == []` and `suggestions == []` instead of the current 500 (D-12, closing D-35-11-A)."
    - "`PROJECT.md` no longer asserts that constrained decoding is a shipped, validated capability; both the v1.2 capability claim and the decisions-table row say what the code actually does."
    - "`ROADMAP.md`'s Phase 36 Goal line says eight pre-existing routes, agreeing with `auth/registry.py` and with its own success criterion 2."
    - "The existing shared-error-taxonomy proof (`tests/unit/test_error_contract.py`) still passes after the D-12 response-shape change, so the narrow business-contract exception did not disturb the auth error contract (REBIND-03)."
  artifacts:
    - ".planning/todos/pending/restore-strict-structured-output.md"
  key_links:
    - "`services/chats.py:57` calls `AnalyzeResponse.model_validate(llm_response)` — that single call site is what converts the missing-field ValidationError into a 500 today, and defaulting the two fields is what makes it a 200."
  prohibitions:
    - statement: "Project documentation must never claim a validated capability the source has never contained. `git log -S\"with_structured_output\" -- src/` returns nothing: the claim was aspirational from the start, and leaving it standing is what made D-35-11-A reachable and unexplained."
      status: flagged
      verification: unverified
    - statement: "A narrow, knowingly-taken exception to a stated contract must never be widened silently. D-12 changes exactly two field defaults on one response model; it is not licence to relax validation anywhere else in `models/llm.py`."
      status: flagged
      verification: unverified
---

<objective>
Close D-35-11-A at the response-model level (D-12), correct the two `PROJECT.md` claims that assert
constrained decoding the source has never had (D-13), file the real fix as a backlog item, and
correct the ROADMAP's "nine pre-existing routes" wording to the eight the code declares.

Purpose: the phase's goal is a running application, and today the product's primary route returns
500 for exactly the input a user gets when their sentence is already correct. Under D-11 they are
about to be charged for it.
Output: two defaulted fields, one backlog item, three prose corrections, one unit test case.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-rebind-pre-existing-routes/36-CONTEXT.md
@.planning/phases/35-foundation/deferred-items.md
</context>

## Commit scope constraint (D-15)

`docker-compose.yml` and `uv.lock` are out of scope and must not be staged, committed, or reverted
by any task here. Scope every `git add` to explicitly named paths — never `git add -A`, never
`git add .`, never `git commit -a`.

## Flagged assumption — REBIND-03 (edge probe item 5, `unclassified`, left unresolved)

The probe could not classify REBIND-03 and it is **not** auto-resolved. Surfacing it rather than
inventing a criterion:

> REBIND-03 says auth rejections use the shared taxonomy "while their existing non-auth business
> error contracts are unchanged". The requirement defines no boundary for a rejection that is
> **neither** — the Phase 36 quota 429 and the two fail-closed 500s are new contracts on these
> routes, not existing ones and not auth rejections. This plan reads the requirement as: the *auth*
> rejection contract is unchanged and proven by the existing `test_error_contract.py`; D-12 is the
> one knowing, recorded exception on the business side; and the new quota statuses are governed by
> REBIND-05, not by REBIND-03's "unchanged" clause. **This reading is an assumption, not a
> derivation from the requirement text.** If a reviewer disagrees, REBIND-03's scope needs a
> decision before verification, not a re-plan of this task.

## Edge probe ledger (9 items, none dropped)

| # | Req | Category | Disposition |
|---|-----|----------|-------------|
| 1 | REBIND-01 | adjacency | resolved — explicit truth (36-03) |
| 2 | REBIND-01 | empty | resolved — backstop truth (36-03) |
| 3 | REBIND-01 | ordering | resolved — explicit truth (36-03) |
| 4 | REBIND-02 | unclassified | **unresolved — flagged assumption** (36-05) |
| 5 | REBIND-03 | unclassified | **unresolved — flagged assumption** (this plan, above) |
| 6 | REBIND-05 | unclassified | **unresolved — flagged assumption** (36-04) |
| 7 | REBIND-06 | adjacency | resolved — explicit truth (36-04) |
| 8 | REBIND-06 | empty | resolved — explicit truth (36-04) |
| 9 | REBIND-06 | ordering | resolved — explicit truth (36-04) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Default the two `AnalyzeResponse` list fields (D-12)</name>
  <files>src/nativespeaker/api/models/llm.py, tests/unit/test_models.py</files>
  <read_first>
    - `src/nativespeaker/api/models/llm.py` (whole file, ~40 lines) — `AnalyzeInput` at 11-14 already shows the defaulted-field form; `AnalyzeResponse` at 22-26 is the target
    - `src/nativespeaker/api/services/chats.py:40-70` — the `AnalyzeResponse.model_validate(llm_response)` call site at roughly line 57 that turns a missing field into a 500
    - `tests/unit/test_models.py:161-187` — the `TestAnalyzeResponse` class the new case joins, and the import block at 15-22 which is already sufficient
    - `.planning/phases/35-foundation/deferred-items.md` — D-35-11-A in full, including its four-way reproduction table
  </read_first>
  <behavior>
    - `AnalyzeResponse(resolved_mode="analyze", response="Looks good.")` constructs with both list fields omitted and yields `issues == []` and `suggestions == []`.
    - `AnalyzeResponse.model_validate({"resolved_mode": "analyze", "response": "ok"})` succeeds rather than raising `ValidationError`.
    - Two separately-constructed instances that both omit the fields do not share a list object — mutating one instance's `issues` must not affect the other's.
    - Explicitly-supplied values still win: passing `issues=[...]` yields exactly that list.
  </behavior>
  <action>
In `src/nativespeaker/api/models/llm.py`, give `AnalyzeResponse.issues` and
`AnalyzeResponse.suggestions` empty-list defaults so a grammatically correct phrase validates
instead of raising. Pydantic v2 deep-copies a mutable default per instance, so the plain
`= []` form is safe here and matches the defaulted-field style already used by `AnalyzeInput`; do
not reach for a factory unless `TestAnalyzeResponse` proves instance sharing.

Add a short comment beside the two fields recording *why* they are defaulted: the LLM chain is
unconstrained (`services/llm.py:30` pipes through a plain JSON output parser rather than a schema-
bound call), so the model legitimately omits both keys when a phrase needs no correction, and
`services/chats.py`'s `model_validate` turned that into a 500 — D-35-11-A. Name D-12 and state that
this is a knowing, narrow exception to §8.3's "existing non-auth error contracts unchanged",
justified because the product is pre-launch with no clients.

Then extend `TestAnalyzeResponse` in `tests/unit/test_models.py` with cases covering all four
behaviors listed above, following the class's existing case style.

Do **not** touch `FollowUpResponse`, `Issue`, or any other model in the file — D-12 is exactly two
field defaults.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_models.py -k Analyze -x -q && uv run pytest -q && uv run ruff check src tests && uv run ty check src</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_models.py -k analyze -x` passes.
    - `uv run python -c "from nativespeaker.api.models.llm import AnalyzeResponse as A; r=A(resolved_mode='analyze', response='ok'); assert r.issues==[] and r.suggestions==[]; s=A(resolved_mode='analyze', response='ok'); r.issues.append(1); assert s.issues==[], 'mutable default shared between instances'; print('ok')"` prints `ok`.
    - `uv run python -c "from nativespeaker.api.models.llm import AnalyzeResponse as A; A.model_validate({'resolved_mode':'analyze','response':'ok'}); print('validates')"` prints `validates`.
    - `uv run pytest tests/unit/test_error_contract.py -x` still passes — the D-12 exception did not disturb the shared auth error contract (REBIND-03).
    - `uv run pytest -q` passes; `uv run ruff check src tests` and `uv run ty check src` are clean.
    - `git show --stat HEAD` names only `src/nativespeaker/api/models/llm.py` and `tests/unit/test_models.py`.
  </acceptance_criteria>
  <reversibility rating="costly">CONTEXT.md D-12 rates this "one-way in principle (it changes a published response shape), cheap in practice — pre-launch, no clients". With no clients the practical rating is costly, not one-way, so no `checkpoint:decision` is inserted; the developer already locked the decision in discuss-phase.</reversibility>
  <done>A correct phrase validates to 200 with two empty arrays, the defaults are per-instance, the reason is recorded in the source, and the error-contract suite is still green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two constrained-decoding claims and file the real fix (D-13)</name>
  <files>.planning/PROJECT.md, .planning/todos/pending/restore-strict-structured-output.md</files>
  <read_first>
    - `.planning/PROJECT.md` line 51 (the v1.2 capability claim) and line 189 (the decisions-table row) — note CONTEXT.md cites these as 56 and 189; line 51 is the current position, so locate both by content rather than by number
    - `src/nativespeaker/api/services/llm.py:20-40` — the actual chain, `prompt_template | self.llm | JsonOutputParser()`, which is unconstrained JSON validated after the fact
    - `config/prompt.txt` around line 124 — the "if nearly perfect → provide 1 to 2 suggestions" instruction the model ignores
    - `.planning/todos/pending/secret-manager-integration.md` — the backlog item format to copy (frontmatter keys `title`, `area`, `created`, `source`, `status`, then a body with a "Why now (context)" and a "Scope when picked up" section)
  </read_first>
  <action>
`PROJECT.md` asserts a capability the source has never contained: `git log -S"with_structured_output"
-- src/` returns nothing. Correct both claims so they describe the code as it is, and file the real
fix as a backlog item. This is a documentation correction, not a code change — do not modify
`services/llm.py`.

**Claim 1** — the v1.2 capability bullet at line 51, currently a `✓`-marked validated capability
about schema-guaranteed LLM responses. Rewrite it to state that LLM responses are parsed as
unconstrained JSON and validated after the fact by the Pydantic response models, and that
schema-constrained decoding is **not** shipped. Move it out of the validated-capability form —
a `✓` there means "confirmed working in production".

**Claim 2** — the decisions-table row at line 189 whose rationale reads "Constrained decoding
eliminates fragile parsing" and whose verdict column is `✓ Good`. Change the verdict away from
`✓ Good` and rewrite the rationale to record that the decision was never implemented, citing
D-35-11-A as the evidence: an already-correct phrase produced a 500 because the model omitted two
keys nothing forced it to emit.

Both edits must name D-13 and Phase 36 so the correction is traceable to the decision that ordered
it.

**Backlog item** — create `.planning/todos/pending/restore-strict-structured-output.md` following
the `secret-manager-integration.md` format. Frontmatter: `title` naming the restoration of strict
structured output, `area: llm`, `created: 2026-08-21`, `source: Phase 36 D-13`, `status: open`.
Body must record: that the three response models in `models/llm.py` need binding as a strict schema
on the chat model call rather than parsed after the fact; that it needs real-provider e2e coverage,
which is why it is out of a rebinding phase's scope; that D-12 shipped the narrow instance fix while
this is the general one; and the note for whoever picks it up — `config/prompt.txt:124` already
instructs the model to supply 1-2 suggestions when a phrase is nearly perfect and the model ignores
it, so strengthening the prompt is demonstrably not the fix.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/restore-strict-structured-output.md && grep -q '^status: open$' .planning/todos/pending/restore-strict-structured-output.md && grep -q 'D-13' .planning/PROJECT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/pending/restore-strict-structured-output.md` exists and its frontmatter contains `status: open`, `area: llm`, and a `source` naming Phase 36 D-13.
    - `grep -n 'D-13' .planning/PROJECT.md` returns at least two lines — one at each corrected claim.
    - `grep -n 'D-35-11-A' .planning/PROJECT.md` returns at least one line — the decisions-table row cites the evidence.
    - `sed -n '51p' .planning/PROJECT.md | grep -qv '^- ✓'` succeeds: the v1.2 bullet is no longer marked as a validated capability.
    - `git diff HEAD~1 --stat -- src/` is empty for this commit — no source file changed.
    - `git show --stat HEAD` names only `.planning/PROJECT.md` and `.planning/todos/pending/restore-strict-structured-output.md`.
  </acceptance_criteria>
  <reversibility rating="reversible">Prose corrections and a backlog file; revert is a single `git revert`.</reversibility>
  <done>Neither PROJECT.md claim asserts constrained decoding as shipped, both cite D-13, and the real fix is filed as an open backlog item with its evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the ROADMAP Phase 36 goal from nine routes to eight</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — the `#### Phase 36: Rebind Pre-existing Routes` block: the `**Goal:**` line and success criterion 2
    - `src/nativespeaker/api/auth/registry.py:68-77` — the eight `RouteMetadata` declarations, the authority for the count
    - `.planning/phases/36-rebind-pre-existing-routes/36-RESEARCH.md` § "The Eight Pre-existing Routes" — the discrepancy write-up
  </read_first>
  <action>
The Phase 36 `**Goal:**` line says "the nine pre-existing routes". The code declares eight
(`GET /health/ready`, `GET /`, `GET /examples`, and five `/chats` routes) and the phase's own
success criterion 2 also totals eight (1 + 2 + 5). The missing ninth is `GET /users/me`, deleted by
Phase 35 D-16 and re-declared by Phase 39.

Edit only the Goal line's count to eight, and append a short parenthetical recording that the ninth
route was `GET /users/me`, removed by Phase 35 D-16 and returning in Phase 39 — so a future reader
finds the reconciliation rather than a silent renumbering. Leave success criterion 2 alone; it was
already correct.

Use a scoped `Edit` against that one line. Do not rewrite the Phase 36 block, do not touch any other
phase entry, and do not touch the plan checklist the planner wrote.
  </action>
  <verify>
    <automated>grep -c 'nine pre-existing routes' .planning/ROADMAP.md | grep -qx 0 && grep -q 'eight pre-existing routes' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/#### Phase 36/,/#### Phase 37/p' .planning/ROADMAP.md | grep -c 'eight pre-existing routes'` returns `1`.
    - `sed -n '/#### Phase 36/,/#### Phase 37/p' .planning/ROADMAP.md | grep -q 'users/me'` succeeds — the reconciliation note is present.
    - `sed -n '/#### Phase 36/,/#### Phase 37/p' .planning/ROADMAP.md | grep -c 'Success criteria'` returns `1` — the block was edited, not rewritten.
    - `git diff HEAD~1 -- .planning/ROADMAP.md | grep -c '^[-+][^-+]'` returns `2` — exactly one line replaced.
    - `git show --stat HEAD` names only `.planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>The Phase 36 goal says eight, carries a one-clause note explaining where the ninth went, and no other ROADMAP content moved.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols and files created by Phase 36 (exclude from drift verification). Full table in
`36-01-PLAN.md`. Created **by this plan**:

| Artifact | Kind |
|---|---|
| `.planning/todos/pending/restore-strict-structured-output.md` | backlog item |

No new code symbol is introduced here — Task 1 changes two existing field defaults on the
pre-existing `AnalyzeResponse`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider → `AnalyzeResponse.model_validate` | Untrusted, unconstrained JSON crosses here; the response model is the only validation |
| response model → API client | Field defaults change the published response shape |

## STRIDE Threat Register (ASVS L1, block_on = high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-llmshape | Tampering | `AnalyzeResponse.model_validate` (`services/chats.py:57`) | medium | mitigate | Default only the two list fields. `resolved_mode` and `response` stay required, so a truncated or wrong-mode provider payload still fails validation rather than reaching the client as an empty success. |
| T-36-drain | Denial of Service | correct-phrase 500 under D-11 | high | mitigate | The 500 on an already-correct phrase is the most likely single cause of a charged-but-failed request once quota lands. D-12 converts it to a 200 before plan 36-03 attaches `require_quota`, so the wave ordering itself is the mitigation. |
| T-36-oracle | Information Disclosure | `PROJECT.md` claims | low | accept | A documentation over-claim is not client-visible. Accepted as a correctness/trust issue, fixed here by D-13 rather than treated as a security control. |
| T-36-SC | Tampering | package installs | high | mitigate | Zero packages installed by this plan; `36-RESEARCH.md` § Package Legitimacy Audit records the phase installs none. Any proposed install is scope creep and requires the legitimacy gate plus a blocking human checkpoint. |
</threat_model>

<verification>
- `uv run pytest -q` — unit suite green (912+ with the new `TestAnalyzeResponse` cases).
- `uv run pytest tests/unit/test_error_contract.py -x` — REBIND-03's shared-taxonomy proof still green.
- `uv run ruff check src tests && uv run ty check src` — clean.
- `git log --stat -3` shows three scoped commits; none names `docker-compose.yml` or `uv.lock`.
- No e2e or schema run is required for this plan — every change is unit-testable or prose.
</verification>

<success_criteria>
1. A grammatically correct phrase validates to 200 with `issues == []` and `suggestions == []`, and
   the two defaults are per-instance rather than shared.
2. Both `PROJECT.md` constrained-decoding claims describe the code as it is and cite D-13; the real
   fix is an open backlog item citing D-35-11-A.
3. The Phase 36 ROADMAP goal says eight pre-existing routes and explains where the ninth went.
4. `docker-compose.yml` and `uv.lock` remain modified, unstaged, and uncommitted.
</success_criteria>

<output>
Create `.planning/phases/36-rebind-pre-existing-routes/36-02-SUMMARY.md` when done.
</output>
