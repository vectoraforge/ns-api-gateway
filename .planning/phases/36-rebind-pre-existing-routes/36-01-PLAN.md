---
phase: 36-rebind-pre-existing-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/20260818_01_initial-release.sql
  - tests/schema/conftest.py
  - tests/schema/test_apply_rollback.py
  - src/nativespeaker/api/models/grants.py
  - src/nativespeaker/api/models/__init__.py
  - tests/schema/helpers.py
  - tests/schema/test_constraints.py
autonomous: true
requirements: [REBIND-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`core.access_tiers` holds exactly three rows after a fresh `pogo apply`: `anonymous`=10, `registered`=50, `paid`=1000, and `registered` >= `anonymous` (D-01/D-02)."
    - "`AccessGrant`, `AccessTier`, and `UserMonthlyUsage` each round-trip a real row from the v2.0 migration through SQLModel without a column-name or type mismatch."
    - "`AccessGrant` maps none of the four `GENERATED ALWAYS AS (...) STORED` columns, so no insert or update this phase or a later one writes can name them."
  artifacts:
    - "src/nativespeaker/api/models/grants.py"
    - "tests/schema/helpers.py::insert_usage"
  key_links:
    - "`models/__init__.py` barrel re-exports the three new classes, which is how `database/grants.py` and `quota.py` import them (the house convention: no deep module imports)."
    - "The seeded `registered` tier id is the FK target the Phase-36 e2e grant fixture inserts against; if the seed is missing the fixture's grant insert fails on the FK."
  prohibitions:
    - statement: "The quota path must never become an entitlement-granting path — no code added by this phase may INSERT a `core.access_grants` row. D-09 forbids minting the usage row; nothing else forbids minting the grant, and a silently minted grant hands out the product for free while hiding a broken write path. Grant creation belongs to Phases 41/42/45."
      status: flagged
      verification: unverified
    - statement: "The test-only grant seeder must never be promoted into `src/` or made reachable by any route. A write path that exists only to make tests pass is how free entitlements ship to production."
      status: flagged
      verification: unverified
    - statement: "A paying user must never be charged a credit for a request the application itself rejected before the provider was contacted — malformed body, out-of-scope, or circuit-open. D-11 accepts the burn only for a genuine provider failure."
      status: flagged
      verification: unverified
---

<objective>
Carry the already-applied D-01 tier seeding into this phase's commits, then land the SQLModel layer
for `core.access_tiers`, `core.access_grants`, and `core.user_monthly_usage` that every later plan
in Phase 36 queries.

Purpose: nothing in the quota flow can be written until the three tables have Python models. This
plan is pure structure — it changes no runtime behavior and breaks no existing test.
Output: `models/grants.py`, its barrel exports, an `insert_usage` schema helper, and three
verified-then-committed D-01 files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-rebind-pre-existing-routes/36-CONTEXT.md
@.planning/phases/36-rebind-pre-existing-routes/36-PATTERNS.md
</context>

## Commit scope constraint (D-15) — applies to every task in this plan

`docker-compose.yml` and `uv.lock` are **out of scope**. Both are modified in the working tree and
neither is named by D-01. The `uv.lock` change is the deferred, still-unowned D-35-05-A (including
the local-uv `revision 2 -> 3` bump that `35-foundation/deferred-items.md` warns against
committing).

**Every commit in this phase must scope its `git add` to explicitly named paths.** Never
`git add -A`, never `git add .`, never `git commit -a`. Do not stage, commit, or revert
`docker-compose.yml` or `uv.lock`.

## Phase goal wording discrepancy — noted once, corrected in plan 36-02

`ROADMAP.md`'s Phase 36 **Goal** line says "the nine pre-existing routes". The code declares
**eight** (`auth/registry.py:68-77`) and ROADMAP success criterion 2 also totals eight
(1 public + 2 authenticated + 5 `/chats`). The missing ninth is `GET /users/me`, deleted by Phase 35
D-16 and re-added by Phase 39. **Eight is correct.** Plan 36-02 owns the ROADMAP wording fix; no
plan silently reconciles it.

## Edge probe ledger (9 items, none dropped)

| # | Req | Category | Disposition |
|---|-----|----------|-------------|
| 1 | REBIND-01 | adjacency | resolved — explicit truth (plan 36-03) |
| 2 | REBIND-01 | empty | resolved — backstop truth (plan 36-03) |
| 3 | REBIND-01 | ordering | resolved — explicit truth (plan 36-03) |
| 4 | REBIND-02 | unclassified | **unresolved — flagged assumption** (plan 36-05) |
| 5 | REBIND-03 | unclassified | **unresolved — flagged assumption** (plan 36-02) |
| 6 | REBIND-05 | unclassified | **unresolved — flagged assumption** (plan 36-04) |
| 7 | REBIND-06 | adjacency | resolved — explicit truth (plan 36-04) |
| 8 | REBIND-06 | empty | resolved — explicit truth (plan 36-04) |
| 9 | REBIND-06 | ordering | resolved — explicit truth (plan 36-04) |

<tasks>

<task type="auto">
  <name>Task 1: Verify and commit the three already-applied D-01 files</name>
  <files>migrations/20260818_01_initial-release.sql, tests/schema/conftest.py, tests/schema/test_apply_rollback.py</files>
  <precondition>`migrations/20260818_01_initial-release.sql`, `tests/schema/conftest.py`, and `tests/schema/test_apply_rollback.py` are modified-but-uncommitted in the working tree, and the developer's PostgreSQL 17 database has already had `pogo rollback -c 1 && pogo apply` run against it. Assert with `git status --porcelain` before starting; if any of the three is clean, halt and report — the change was lost or already committed, and re-applying it blind would produce a duplicate seed.</precondition>
  <read_first>
    - `migrations/20260818_01_initial-release.sql` (the `core.access_tiers` DDL and its `INSERT` at roughly lines 260-285, plus the override comment block)
    - `tests/schema/test_apply_rollback.py` (`SEEDED_TIERS` at line 20, `test_previous_test_rows_were_rolled_back`, `test_only_the_seeded_tiers_survive`, and the `TestSeededTiers` class at roughly 71-91)
    - `tests/schema/conftest.py` (the `tier` fixture at roughly 141-148 and its corrected docstring)
    - `.planning/phases/36-rebind-pre-existing-routes/36-CONTEXT.md` § Tier seeding (D-01, D-02) — the full record of what was applied and why
    - `.planning/phases/36-rebind-pre-existing-routes/36-PATTERNS.md` § Shared Patterns -> "Seeded tier reference data" — the exact SQL shape to expect
  </read_first>
  <action>
Do **not** redo D-01. Verify the working-tree change matches what CONTEXT.md D-01 records, then commit it.

Verify, by reading the diff (`git diff -- migrations/20260818_01_initial-release.sql tests/schema/conftest.py tests/schema/test_apply_rollback.py`):
- The migration contains an `INSERT INTO core.access_tiers (id, monthly_credits)` with exactly the three rows `anonymous`/10, `registered`/50, `paid`/1000, and a comment block recording that this overrides `00-schema.md:249` and is the required SHARED-INVARIANTS conflict flag rather than a silent resolution.
- The old "this migration seeds NO tier rows" comment is gone.
- `tests/schema/test_apply_rollback.py` defines `SEEDED_TIERS = {"anonymous", "registered", "paid"}`, no longer asserts `core.access_tiers` is empty, has `test_only_the_seeded_tiers_survive` asserting surviving ids equal that set (set equality, not a count — a leak plus a missing row would cancel out in a count), and has a `TestSeededTiers` class pinning the three `monthly_credits` values and the `registered >= anonymous` sizing invariant from `07-claim-registered-grant.md:59`.
- `tests/schema/conftest.py`'s `tier` fixture inserts randomised `tier_<hex>` ids that cannot collide with the seeded three.

If any of the above is absent, stop and report the discrepancy rather than writing the missing piece — CONTEXT.md states it is already applied, so an absence means something was lost and the developer needs to know.

Then commit exactly those three paths and nothing else. Use `git add` with the three literal paths listed one by one. Commit message: `feat(36-01): seed access tiers as migration reference data (D-01)`.
  </action>
  <verify>
    <automated>uv run pytest tests/schema/test_apply_rollback.py -x -m schema -q && git status --porcelain -- docker-compose.yml uv.lock | grep -q '^ M' && echo "OUT-OF-SCOPE FILES CORRECTLY LEFT UNSTAGED"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/schema/test_apply_rollback.py -x -m schema` passes, including `TestSeededTiers`.
    - `git show --stat HEAD` lists exactly three files: `migrations/20260818_01_initial-release.sql`, `tests/schema/conftest.py`, `tests/schema/test_apply_rollback.py`.
    - `git show --stat HEAD -- docker-compose.yml uv.lock` produces no file rows (D-15: neither file is in the commit).
    - `git status --porcelain` still shows ` M docker-compose.yml` and ` M uv.lock` after the commit — both remain modified and unowned.
    - `grep -c "INSERT INTO core.access_tiers" migrations/20260818_01_initial-release.sql` returns `1`.
  </acceptance_criteria>
  <reversibility rating="costly">D-01 is costly per CONTEXT.md: changing `monthly_credits` later means editing an applied migration and clearing pogo's history row (or renaming the file to a new id), not a config edit. Already decided by the developer; no checkpoint.</reversibility>
  <done>The three D-01 files are committed as one scoped commit, the schema suite is green, and `docker-compose.yml` and `uv.lock` are untouched and still modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLModel classes for the three grant tables</name>
  <files>src/nativespeaker/api/models/grants.py, src/nativespeaker/api/models/__init__.py</files>
  <read_first>
    - `src/nativespeaker/api/models/identities.py` (whole file) — the analog: module docstring style, the `StrEnum` + `cast(Any, Enum(..., name=..., schema='core'))` native-enum idiom, `DateTimeType`, and the table-class shape
    - `src/nativespeaker/api/models/users.py:1-15` — the "columns deliberately absent, and why" docstring block to imitate
    - `src/nativespeaker/api/models/chats.py:38` — the non-`default_factory` primary-key precedent for a `str` PK
    - `src/nativespeaker/api/models/__init__.py` (whole file) — the alphabetised `__all__` and one-grouped-import-per-module barrel convention
    - `migrations/20260818_01_initial-release.sql` lines 71-82 (the two enums), 260-285 (`core.access_tiers` + seed), 392-460 (`core.access_grants` incl. the four generated columns at 405-416 and `ix_access_grants_one_active_per_user` at 455-460), 578-585 (`core.user_monthly_usage`)
    - `.planning/phases/36-rebind-pre-existing-routes/36-PATTERNS.md` § `models/grants.py` — the excerpts with line numbers
  </read_first>
  <behavior>
    - `AccessGrant(user_id=..., tier_id="registered", source=AccessGrantSource.registered_account_grant)` constructs with `status` defaulting to `AccessGrantStatus.active`.
    - `AccessTier(id="registered", monthly_credits=50)` constructs; `id` has no default and is the primary key.
    - `UserMonthlyUsage(grant_id=..., monthly_period="2026-08")` constructs with `monthly_used` defaulting to `0` and both `created_at`/`updated_at` populated by their default factories (the DB supplies no DEFAULT for these two columns).
    - `AccessGrant.model_fields` contains none of `anti_abuse_required_grant_id`, `active_registered_account_grant_id`, `active_subscription_grant_subscription_id`, `active_subscription_grant_user_id`.
    - Every `__tablename__`/`__table_args__` pair resolves to the `core` schema.
  </behavior>
  <action>
Create `src/nativespeaker/api/models/grants.py` mapping the three tables, following `models/identities.py` exactly.

Two native PostgreSQL enums, mirrored as `StrEnum` with members copied verbatim from the migration:
- `AccessGrantSource` — `subscription`, `anonymous_device_grant`, `registered_account_grant`, `manual`; aliased as `AccessGrantSourceType = cast(Any, Enum(AccessGrantSource, name='access_grant_source', schema='core'))`.
- `AccessGrantStatus` — `active`, `revoked`, `expired`; aliased as `AccessGrantStatusType` naming `access_grant_status` in schema `core`.

Three table classes, all with `__table_args__ = {"schema": "core"}`:
- `AccessTier` — `__tablename__ = "access_tiers"`; `id: str = Field(primary_key=True)` with **no** default (tier ids are seeded reference data, not generated); `monthly_credits: int`; `created_at`/`updated_at` as `DateTimeType` with `default_factory`.
- `AccessGrant` — `__tablename__ = "access_grants"`; `id: UUID = Field(default_factory=uuid7, primary_key=True)`; `user_id: UUID = Field(foreign_key="core.users.id")`; `tier_id: str = Field(foreign_key="core.access_tiers.id")`; `source: AccessGrantSource = Field(sa_type=AccessGrantSourceType)`; `status: AccessGrantStatus = Field(sa_type=AccessGrantStatusType, default=AccessGrantStatus.active)`; `subscription_id: UUID | None = Field(default=None)`; `starts_at: datetime = Field(sa_type=DateTimeType, default_factory=...)`; `ends_at: datetime | None = Field(sa_type=DateTimeType, default=None)`; `created_at`/`updated_at`.
- `UserMonthlyUsage` — `__tablename__ = "user_monthly_usage"`; `grant_id: UUID = Field(foreign_key="core.access_grants.id", primary_key=True)` (grant id *is* the PK); `monthly_period: str`; `monthly_used: int = Field(default=0)`; `created_at`/`updated_at` with `default_factory` — these two are `NOT NULL` with no DB DEFAULT, unlike every other table, so the factories are load-bearing rather than decorative.

**Omit all four `GENERATED ALWAYS AS (...) STORED` columns** from `AccessGrant`. PostgreSQL rejects an explicit value for a generated column, no SQLModel class in this repo maps one, and this phase never writes a grant row. Record the omission and its reason in the module docstring in the style of `models/users.py:1-15`.

The module docstring must also carry the standing rule copied from `models/identities.py:1-24`: the database owns every constraint — the six CHECKs, the three partial unique indexes including `ix_access_grants_one_active_per_user`, and the FK cascade are declared in the migration and are deliberately not re-encoded here, because a Python copy of a CHECK is a second source of truth that can drift from the one that actually enforces. Name `ix_access_grants_one_active_per_user` explicitly, since D-10's tripwire in a later plan reasons from it.

Then extend `src/nativespeaker/api/models/__init__.py`: add `AccessGrant`, `AccessGrantSource`, `AccessGrantStatus`, `AccessTier`, `UserMonthlyUsage` to the alphabetised `__all__`, and add one grouped `from nativespeaker.api.models.grants import (...)` block in alphabetical module position (between the `chats` and `identities` blocks).
  </action>
  <verify>
    <automated>uv run pytest -q && uv run ruff check src tests && uv run ty check src</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from nativespeaker.api.models import AccessGrant, AccessGrantSource, AccessGrantStatus, AccessTier, UserMonthlyUsage; print('ok')"` prints `ok`.
    - `uv run python -c "from nativespeaker.api.models import AccessGrant; g={'anti_abuse_required_grant_id','active_registered_account_grant_id','active_subscription_grant_subscription_id','active_subscription_grant_user_id'}; assert not (g & set(AccessGrant.model_fields)), g & set(AccessGrant.model_fields); print('no generated columns mapped')"` prints the success line.
    - `uv run python -c "from nativespeaker.api.models import AccessGrant, AccessTier, UserMonthlyUsage as U; assert AccessGrant.__table__.schema=='core' and AccessTier.__table__.schema=='core' and U.__table__.schema=='core'; assert AccessGrant.__tablename__=='access_grants' and AccessTier.__tablename__=='access_tiers' and U.__tablename__=='user_monthly_usage'; print('ok')"` prints `ok`.
    - `uv run python -c "from nativespeaker.api.models import UserMonthlyUsage as U; from uuid import uuid4; r=U(grant_id=uuid4(), monthly_period='2026-08'); assert r.monthly_used==0 and r.created_at is not None and r.updated_at is not None; print('ok')"` prints `ok`.
    - `uv run pytest -q` passes with no fewer tests than the 912 currently passing.
    - `uv run ruff check src tests` and `uv run ty check src` are both clean.
    - The commit stages only `src/nativespeaker/api/models/grants.py` and `src/nativespeaker/api/models/__init__.py` (D-15 scoped `git add`).
  </acceptance_criteria>
  <reversibility rating="reversible">Adding read-only model classes touches no existing behavior and can be deleted without a migration.</reversibility>
  <done>The three classes import from the `models` barrel, construct with the defaults above, map no generated column, and the unit suite plus ruff and ty are clean.</done>
</task>

<task type="auto">
  <name>Task 3: `insert_usage` helper for the schema test package</name>
  <files>tests/schema/helpers.py, tests/schema/test_constraints.py</files>
  <read_first>
    - `tests/schema/helpers.py` (whole file, 70 lines) — `insert_user`, `insert_tier`, `insert_grant`: keyword-only signature shape, `$N` parameter binding, and the deliberate absence of any commit
    - `tests/schema/test_constraints.py:60-90` — the inlined `INSERT INTO core.user_monthly_usage` string this helper replaces, and the column set it uses
    - `migrations/20260818_01_initial-release.sql:578-585` — the four columns and the `monthly_used >= 0` CHECK
  </read_first>
  <action>
Add a fourth helper `insert_usage` to `tests/schema/helpers.py`, matching the signature shape of the
existing `insert_grant`: `async def insert_usage(conn: asyncpg.Connection, *, grant_id: uuid.UUID,
monthly_period: str = "2026-08", monthly_used: int = 0) -> None`.

It issues one `INSERT INTO core.user_monthly_usage (grant_id, monthly_period, monthly_used,
created_at, updated_at)` with `$1..$3` bound parameters and `CURRENT_TIMESTAMP` for the two
timestamp columns — those two are `NOT NULL` with no DB DEFAULT, so they must be named explicitly.
Bind every caller-supplied value as a `$N` parameter; do not build the statement by string
formatting. Like the other three helpers it must **not** commit — the `conn` fixture's transaction
owns the rollback.

Then replace the inlined INSERT at `tests/schema/test_constraints.py` with a call to the new helper,
so there is one definition of this insert rather than two.
  </action>
  <verify>
    <automated>uv run pytest tests/schema -x -m schema -q && uv run ruff check tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/schema -x -m schema` passes with the same test count as before the change.
    - `grep -c "INSERT INTO core.user_monthly_usage" tests/schema/helpers.py` returns `1`.
    - `grep -c "INSERT INTO core.user_monthly_usage" tests/schema/test_constraints.py` returns `0` — the inline copy is gone.
    - `uv run python -c "import inspect,sys; sys.path.insert(0,'tests'); from schema.helpers import insert_usage; p=inspect.signature(insert_usage).parameters; assert list(p)[0]=='conn' and all(p[k].kind is inspect.Parameter.KEYWORD_ONLY for k in list(p)[1:]); print('ok')"` prints `ok`.
    - `uv run ruff check tests` is clean.
    - The commit stages only `tests/schema/helpers.py` and `tests/schema/test_constraints.py`.
  </acceptance_criteria>
  <done>`insert_usage` exists beside the other three helpers, `test_constraints.py` uses it instead of an inline string, and the schema suite is green.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files **created by Phase 36** (new — not pre-existing; exclude from drift verification):

| Artifact | Kind | Created in |
|---|---|---|
| `src/nativespeaker/api/models/grants.py` | module | 36-01 |
| `AccessGrantSource`, `AccessGrantStatus` | `StrEnum` classes | 36-01 |
| `AccessGrantSourceType`, `AccessGrantStatusType` | SQLAlchemy `Enum` aliases | 36-01 |
| `AccessTier`, `AccessGrant`, `UserMonthlyUsage` | SQLModel table classes | 36-01 |
| `tests/schema/helpers.py::insert_usage` | test helper function | 36-01 |
| `src/nativespeaker/api/quota.py` | module (resolver seam, D-03) | 36-03 |
| `consume_quota` | async function | 36-03 |
| `src/nativespeaker/api/database/grants.py`, `GrantsDB` | module + class | 36-03 |
| `GrantsDB.lock_effective_grants`, `GrantsDB.lock_usage`, `GrantsDB.monthly_credits` | methods | 36-03 / 36-04 |
| `require_quota`, `require_quota_create_chat`, `require_quota_send_message` | FastAPI dependencies | 36-03 / 36-05 |
| `MissingUsageRowError`, `MultipleEffectiveGrantsError` | `ServiceError` subclasses | 36-04 |
| registry condition 10 (`quota_checked` cross-check) | assertion branch | 36-03 |
| `tests/e2e/conftest.py::seed_grant`, `quota_grant` fixture, `REGISTERED_TIER_ID` | test fixtures | 36-03 |
| `tests/e2e/test_quota.py`, `tests/unit/test_quota_resolver.py`, `tests/schema/test_grant_locks.py` | test modules | 36-03 / 36-04 / 36-05 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → PostgreSQL | Reference data written once at apply time; an edited already-applied migration is silently skipped by pogo's `if not migration.applied` filename-stem gate |
| SQLModel class → `core.*` tables | A model that maps a column the DB generates, or omits one the DB requires, fails at write time rather than import time |

## STRIDE Threat Register (ASVS L1, block_on = high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-mint | Elevation of Privilege | `models/grants.py`, later `GrantsDB` | high | mitigate | Models are read-shaped for this phase; no plan in Phase 36 issues an INSERT against `core.access_grants` or `core.user_monthly_usage` outside the test packages. D-09 forbids minting the usage row; the prohibition block extends that to the grant row. |
| T-36-sqli | Tampering | `tests/schema/helpers.py::insert_usage` | medium | mitigate | Every caller-supplied value is bound as a `$N` asyncpg parameter; the statement is never assembled by string formatting. Matches the three existing helpers. |
| T-36-drift | Tampering | migration seed vs. `AccessTier` | medium | mitigate | The model re-encodes no CHECK and no index; the migration remains the single enforcing source of truth. `TestSeededTiers` pins the three credit values and the `registered >= anonymous` invariant against the real database. |
| T-36-SC | Tampering | package installs | high | mitigate | This phase installs **zero** packages — `36-RESEARCH.md` § Package Legitimacy Audit records that every symbol is an existing `pyproject.toml` pin or in-repo. Any plan proposing an install is scope creep and must run the legitimacy gate with a blocking human checkpoint before the install task. |
| T-36-envdrift | Denial of Service | other databases | low | accept | Assumption A4: only the developer's database has been re-applied. pogo skips an edited already-applied migration, so a CI or teammate database silently lacks the tier rows and every grant FK fails there. Accepted for a pre-launch, single-developer product; the fix is a documented `pogo rollback -c 1 && pogo apply`. |
</threat_model>

<verification>
- `uv run pytest -q` — unit suite green, no regression against the current 912.
- `uv run pytest -q -m ""` — full suite green (requires a live PostgreSQL 17; see manual note below).
- `uv run ruff check src tests && uv run ty check src` — clean.
- `git log --stat -3` shows three scoped commits, none naming `docker-compose.yml` or `uv.lock`.
- **Manual (A1):** the sandbox cannot reach PostgreSQL, so the schema-marked assertions above must be
  run in the developer's environment: `docker compose up -d db && uv run pogo apply`, then
  `uv run pytest tests/schema -m schema -q`.
</verification>

<success_criteria>
1. D-01's three files are committed as one scoped commit and the schema suite proves the three
   seeded tiers survive with the correct credit values and sizing order.
2. `AccessTier`, `AccessGrant`, and `UserMonthlyUsage` import from the `models` barrel and map the
   migration's columns exactly, with all four generated columns omitted.
3. `insert_usage` exists in `tests/schema/helpers.py` and is the only definition of that INSERT.
4. `docker-compose.yml` and `uv.lock` remain modified, unstaged, and uncommitted.
</success_criteria>

<output>
Create `.planning/phases/36-rebind-pre-existing-routes/36-01-SUMMARY.md` when done.
</output>
