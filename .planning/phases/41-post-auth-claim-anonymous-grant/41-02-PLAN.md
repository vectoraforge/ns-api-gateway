---
phase: 41-post-auth-claim-anonymous-grant
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nativespeaker/api/resilience.py
  - config/config.yaml
  - tests/unit/test_resilience_retry.py
  - tests/unit/test_quota_seam.py
  - tests/unit/test_config.py
  - AGENTS.md
  - .planning/todos/pending/breaker-check-moved-to-admission.md
  - .planning/todos/pending/admission-holds-a-db-connection.md
autonomous: true
requirements: [D-14, D-15, D-16]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A chat request already in flight when the circuit breaker opens fails on its next attempt with the same 503 and Retry-After a fresh request gets, instead of spending its remaining attempts against a provider already declared dead (D-14)"
    - "The pass-through arm for the gate and breaker errors is reachable again and is ordered first, so a CircuitOpenError raised mid-flight is never recorded as a provider failure and never rewrapped into a generic 503 (D-14)"
    - "The quota charge commits and releases its database connection before the request waits for a provider permit; a slow database no longer occupies one (D-15)"
    - "An open breaker or a full queue still answers 503 having spent nothing: the breaker check and the in-flight slot stay in admission, and both are instantaneous (D-15)"
    - "Admitted is still minted only by ResiliencePolicy.admission, and services/chats.py is unchanged (D-15)"
    - "The twenty billing cases in tests/unit/test_quota_seam.py stay green in substance: what changes is the wording of the cases that described admission as holding a permit (D-15)"
    - "db.pool_size resolves to 12 against the tracked configuration file, while every DB_* credential still arrives from .env (D-16)"
  prohibitions:
    - "An exhausted retry budget, a timeout, or a rejection the provider never saw must never be reported to a caller as success."
    - "A request that never reached the provider must never be billed, and a request that was billed must never be silently double-billed by a retry."
    - "A breaker or queue rejection must never be recorded as a provider failure, because that would let the system talk itself into a longer outage than the provider caused."
    - "Neither the circuit breaker nor the execution gate may be replaced by a library or deleted as redundant; they are deliberate, and nothing installed replaces them."
  artifacts:
    - path: "src/nativespeaker/api/resilience.py"
      provides: "The per-attempt breaker check, and the semaphore moved off the admission path and around the retry loop"
      contains: "before_call"
    - path: "config/config.yaml"
      provides: "The database pool size, on the screen above the resilience pool size it is derived from"
      contains: "pool_size"
    - path: "tests/unit/test_resilience_retry.py"
      provides: "The mid-flight breaker case: 503 with Retry-After instead of the remaining attempts"
      contains: "Retry-After"
  key_links:
    - from: "src/nativespeaker/api/resilience.py"
      to: "src/nativespeaker/api/services/quota.py"
      via: "the charge runs inside admission and outside both the semaphore and the retry loop, so it is neither permit-holding nor repeated"
      pattern: "admission"
    - from: "config/config.yaml"
      to: "src/nativespeaker/api/app/lifespan.py"
      via: "pool_size already flows from config into create_async_engine; only the value moves"
      pattern: "pool_size"
---

<objective>
The two folded todos and the pool finding, all three independent of the endpoint and sharing no file
with it: the circuit breaker is consulted before every attempt rather than only at admission (D-14),
the provider permit is taken around the retry loop rather than across the quota charge's database
round trip (D-15), and `db.pool_size` rises from 5 to 12 (D-16).

Purpose: these close `breaker-check-moved-to-admission`, `admission-holds-a-db-connection` and
STATE.md blocker A-15, all three of which are live defects on the chat path today — a pool that
exhausts at three concurrent chat posts, and an admitted request that burns about ninety seconds
against a provider the breaker already opened.

Output: `resilience.py` corrected in place with its cases, the pool value raised where the relation to
`resilience.pool_size` is legible, `AGENTS.md` § Resilience realigned, and both todo files closed the
way this repository records a finished todo.

Note on this plan's `requirements` field: the folded todos carry no ANONGRANT requirement — they are
in scope by decision, not by requirement — so the field names the three decisions instead of borrowing
a requirement id that does not describe them.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-post-auth-claim-anonymous-grant/41-CONTEXT.md
@.planning/phases/41-post-auth-claim-anonymous-grant/41-RESEARCH.md
@.planning/phases/41-post-auth-claim-anonymous-grant/41-PATTERNS.md
@AGENTS.md
</context>

## Artifacts this phase produces

`resilience.py` gains `LLMExecutionGate.inflight_slot` and `LLMExecutionGate.concurrency` (the two
halves of today's `hold`); `config/config.yaml` gains a `db` block declaring `pool_size`. The full
phase inventory of new symbols is in `41-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The breaker is consulted before every attempt (D-14)</name>
  <precondition>None — the unit suite runs with no infrastructure.</precondition>
  <files>src/nativespeaker/api/resilience.py, tests/unit/test_resilience_retry.py</files>
  <behavior>
    - A request admitted while the breaker is closed, whose first attempt fails and whose breaker then opens, fails on its second attempt with `CircuitOpenError` rather than spending the third.
    - That failure reaches the caller as 503 carrying `Retry-After`, the same answer a fresh request gets, not the generic wrapped 503.
    - The mid-flight `CircuitOpenError` is not recorded as a provider failure: the breaker's failure count after the case equals the count from the genuine provider failures alone.
    - A queue-full rejection still propagates unwrapped, as the existing case already asserts.
    - Control: with the breaker held closed throughout, the same operation still spends all three attempts, so the case above cannot pass by the retry budget being broken generally.
  </behavior>
  <read_first>
    - `41-PATTERNS.md` § "`src/nativespeaker/api/resilience.py`" (:663-717) — where `before_call()` goes and why the pass-through arm must stay first
    - `41-RESEARCH.md` § Common Pitfall 13
    - `41-CONTEXT.md` § D-14
    - `src/nativespeaker/api/resilience.py` in full — `CircuitBreaker`, `LLMExecutionGate` (:83-102), `Admitted`, `ResiliencePolicy.admission` (:133-138) and `ainvoke`/`attempt` (:140-169), including the existing pass-through arm and the generic arm below it
    - `tests/unit/test_resilience_retry.py` in full, especially `TestGateAndBreakerErrorsAreNeverWrapped` (:157-196) and `test_circuit_open_propagates_unwrapped_and_is_not_retried`
    - `.planning/todos/pending/breaker-check-moved-to-admission.md` in full, including its "scope when picked up" section
    - `src/nativespeaker/api/errors.py` — what `CircuitOpenError` answers and where its `Retry-After` comes from
  </read_first>
  <action>
Put `await self._circuit_breaker.before_call()` at the top of `attempt()` in
`ResiliencePolicy.ainvoke`, so the breaker is consulted on every attempt and not only once at
admission. Leave the call in `admission()` as well: an open breaker must still refuse a new request
before it is charged anything.

Placement is the whole of this change and it is easy to get wrong. Put the call **above** the `try`,
or inside it strictly above the timeout call with the existing
pass-through arm ordered first. If the raise lands under the generic arm, the breaker records its own
rejection as a provider failure and rewraps it, and the caller loses the `Retry-After` that tells them
when to come back — which is the opposite of what this change is for.

The pass-through arm at the top of the `except` chain becomes reachable again and **stays**. Do not
delete it and do not reorder the chain: the todo file offers removing it as the alternative, and D-14
chose the other branch.

A request already charged is not refunded when it dies on the reopened breaker. That is already true
when all three attempts fail, so it is a property being kept rather than one being introduced. Say so
in one line in the summary; do not build a refund path — the quota code deliberately has none.

Then add the case the change exists for, in `TestGateAndBreakerErrorsAreNeverWrapped` beside the
existing one: an operation whose first call fails enough to open the breaker, driven through
`ainvoke`, answering on its second attempt with the breaker's own error and its `Retry-After` rather
than spending the third. Assert the attempt count explicitly and assert the breaker's failure tally,
so a change that merely re-raises without the count staying honest fails here. Add the control case
described above in the same class.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_resilience_retry.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed or "no tests ran"</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or fewer passed than the pre-task baseline, or any error during collection</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_resilience_retry.py -q` passes with at least two more collected cases than before the task and zero skipped.
    - `uv run python -c "import inspect, nativespeaker.api.resilience as r; print(inspect.getsource(r.ResiliencePolicy).count('before_call'))"` prints `2` — one in admission, one per attempt.
    - `uv run python -c "import inspect, nativespeaker.api.resilience as r; s=inspect.getsource(r.ResiliencePolicy.ainvoke); print(s.index('CircuitOpenError') < s.index('except Exception'))"` prints `True` — the pass-through arm is still ordered above the generic one.
    - One case asserts the number of attempts spent is 2, not 3, when the breaker opens after the first failure.
    - One case asserts the caller's answer carries `Retry-After`.
    - The control case asserts 3 attempts are still spent with the breaker held closed.
    - `uv run pytest tests/unit/test_docstring_bar.py -q` passes and `uv run ruff check src tests` exits 0.
  </acceptance_criteria>
  <done>A request in flight when the breaker opens stops on its next attempt with the answer a new request would get, and the arm that carries it is reachable, first, and covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The quota charge no longer holds a provider permit (D-15)</name>
  <precondition>None — the unit suite runs with no infrastructure.</precondition>
  <files>src/nativespeaker/api/resilience.py, tests/unit/test_quota_seam.py</files>
  <behavior>
    - Entering `admission()` acquires an in-flight slot and does not acquire the concurrency semaphore.
    - `ainvoke` acquires the semaphore around its whole retry loop and releases it when the loop ends, on success or on failure.
    - With the semaphore exhausted by other holders, a caller can still enter admission, be charged, and leave the charge committed while it waits for a permit.
    - A full in-flight queue still raises the queue-full rejection from admission, before anything is charged.
    - An open breaker still refuses from admission, before anything is charged.
    - The twenty existing billing cases still assert what they assert; only the wording that described admission as holding a permit changes.
  </behavior>
  <read_first>
    - `41-PATTERNS.md` § "`src/nativespeaker/api/resilience.py`" (:663-711) — what splits, and that `_inflight_slot` is already a standalone context manager
    - `41-RESEARCH.md` § Common Pitfall 12
    - `41-CONTEXT.md` § D-15 and § D-16, including the two alternatives D-15 rejected
    - `src/nativespeaker/api/resilience.py` :83-102 (`_inflight_slot` and `hold`), :133-169 (`admission` and `ainvoke`)
    - `src/nativespeaker/api/services/llm.py` :32 — the admission passthrough, which must not change
    - `src/nativespeaker/api/services/chats.py` :92-94 and :116-118 — the admission blocks, which must not change
    - `tests/unit/test_quota_seam.py` in full (:200-448, six classes and twenty cases) — the billing property to preserve and the docstrings that describe admission as holding a permit
    - `.planning/todos/pending/admission-holds-a-db-connection.md` in full
  </read_first>
  <action>
Split `LLMExecutionGate.hold` into the two context managers it is already made of, so the caller
chooses which resource it takes. Promote `_inflight_slot` to a public `inflight_slot` — it is already
a standalone context manager, so this is exposure and a rename, not new machinery — and add a second
public context manager, `concurrency`, holding the semaphore alone. Either delete `hold` or leave it
as the composition of the two; nothing outside this module calls it, so deleting it is the smaller
surface and the better answer unless a caller turns up.

`ResiliencePolicy.admission()` then keeps the breaker check and takes the in-flight slot only. Both
are instantaneous, so an open breaker or a full queue still answers 503 having spent nothing — that is
the property that makes charging inside admission safe, and it is why D-15 rejected the alternative of
charging before admission outright.

`ainvoke()` wraps its whole `AsyncRetrying` — the construction and the call — in the semaphore. The
permit is therefore taken after the caller's body has already charged and released its database
connection, which is the defect being closed: a slow database no longer occupies a provider permit.
The permit still covers all three attempts as one unit, so concurrency against the provider is bounded
exactly as before.

`Admitted` is still minted only by `admission()`. `services/llm.py` and `services/chats.py` do not
change: check by reading them, and if either would have to change, stop and say so rather than
widening the plan.

Both docstrings change, because both are now wrong: the gate's says it holds a slot and the semaphore
together, and admission's says a slot is held for the caller's whole body. Three lines maximum, and
they state what the thing does rather than what moved.

In `tests/unit/test_quota_seam.py`, reword the cases and docstrings that describe admission as holding
a provider permit, and add the two cases the split now makes assertable: that entering admission
leaves the semaphore untouched, and that a caller whose charge succeeds while the semaphore is
exhausted still has its charge committed before the permit is granted. The twenty billing cases stay
green in substance — if any of them has to change its assertion rather than its wording, that is a
behaviour change this plan did not intend, and the summary must name it.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_quota_seam.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports fewer passed than the twenty-two this task leaves behind, or "no tests ran"</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or fewer passed than after Task 1, or any error during collection</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_quota_seam.py -q` passes with at least 22 collected cases and zero skipped.
    - `uv run python -c "import inspect, nativespeaker.api.resilience as r; print('_semaphore' in inspect.getsource(r.ResiliencePolicy.ainvoke), '_semaphore' in inspect.getsource(r.ResiliencePolicy.admission))"` prints `True False`.
    - `uv run python -c "import nativespeaker.api.resilience as r; g=r.LLMExecutionGate; print(hasattr(g,'inflight_slot'), hasattr(g,'concurrency'))"` prints `True True`.
    - `git diff --stat src/nativespeaker/api/services/llm.py src/nativespeaker/api/services/chats.py` shows no change.
    - One case asserts the semaphore is untouched after entering admission; one asserts a charge commits while the semaphore is exhausted.
    - `uv run pytest tests/unit/test_docstring_bar.py -q` passes and `uv run ruff check src tests` exits 0.
    - If any of the twenty billing cases changed an assertion rather than its wording, the summary names it and says why.
  </acceptance_criteria>
  <done>The database round trip that charges the quota happens on an in-flight slot alone, and the provider permit is taken only when the request is ready to talk to the provider.</done>
</task>

<task type="auto">
  <name>Task 3: The pool size, the rule it contradicted, and the two closed todos</name>
  <precondition>None for the unit assertions; the boot check runs against whatever `.env` supplies and does not require a reachable database.</precondition>
  <files>config/config.yaml, tests/unit/test_config.py, AGENTS.md, .planning/todos/pending/breaker-check-moved-to-admission.md, .planning/todos/pending/admission-holds-a-db-connection.md</files>
  <read_first>
    - `41-RESEARCH.md` § Common Pitfall 14 and § Assumptions Log A8
    - `41-CONTEXT.md` § D-16 and § D-21
    - `config/config.yaml` in full — the `resilience` block, the `jwt` partial block that is the working precedent, and the header comment stating that this file is authoritative for anything it declares
    - `src/nativespeaker/api/config.py` :12-16, :19-30, :64-76 — the nested delimiter, `DatabaseConfig` and where `db` sits on `AppConfig`
    - `src/nativespeaker/api/app/lifespan.py` :34 — `pool_size` already flows from config, so no code change is needed there
    - `tests/unit/test_config.py` :1-45 and :100-134 — `TRACKED_CONFIG`, `_ENV_SECRETS`, `test_resilence_config_defaults`, and the surface-is-gone class as the shape for a value assertion against the tracked file
    - `.env.example` — that `DB_POOL_SIZE` is not among the documented variables today
    - `AGENTS.md` § Resilience in full
    - `.planning/todos/done/restore-strict-structured-output.md` — the frontmatter this repository writes when a todo is finished
    - `.planning/todos/pending/admission-holds-a-db-connection.md` and `.planning/todos/pending/breaker-check-moved-to-admission.md`
  </read_first>
  <action>
Add a `db:` block to `config/config.yaml` declaring exactly one key, `pool_size: 12`, placed adjacent
to the `resilience:` block so the two numbers sit on one screen. Above it put the one-line comment D-16
asks for, naming the relation the number comes from: two connections per possible in-flight chat plus
two spare, at the configured `resilience.pool_size` of 5. The relation is a comment and not code —
deriving one value from the other was offered and declined, and the two stay independent numbers.

A partial block deep-merges rather than replacing: the `jwt:` block already declares one key while
`JWT_PROJECT_ID` and `JWT_API_KEY` still arrive from `.env` and are required with no defaults. That
precedent is what makes this safe, and it is also the thing to prove rather than assume — the merge
semantics were reasoned about in research, not executed. Prove it in `tests/unit/test_config.py` with
a case built from `TRACKED_CONFIG` plus `_ENV_SECRETS`, asserting `db.pool_size == 12` **and** that
the five credential fields still carry their environment values. If the merge does not behave as the
`jwt` precedent predicts, the fallback is `config.py`'s field default; take it, and record in the
summary that the precedent did not generalise.

The trade-off is real and must be stated in the summary rather than discovered later: this file is
authoritative for anything it declares, so a `db:` block forecloses `DB_POOL_SIZE` from `.env`. That
variable is not documented in `.env.example` and nothing sets it today, which is why the trade is
worth making; if a deployment later needs a per-environment pool size, the value moves to
`config.py`'s default and the YAML key goes.

Realign `AGENTS.md` § Resilience where D-14 and D-15 have made its wording no longer true, and nowhere
else. The section's argument for why the breaker exists is unchanged and gets stronger; what changes
is any sentence that describes when the breaker is consulted or what admission holds. Do not restate
the decisions — one or two sentences, in the register the file already uses. The rule that these two
pieces of machinery are deliberate and not awaiting replacement stays exactly as it is.

Close both todo files the way this repository records a finished todo: move each from
`.planning/todos/pending/` to `.planning/todos/done/`, and in the frontmatter set `status: done` and
add `completed:` with today's date and `completed_in: 41`. Leave the body prose as it is — it is the
record of what was wrong, and a finished todo that erases its own diagnosis is worth less than one
that keeps it. Do not touch the two todos this phase reviewed and did not fold.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_config.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed or "no tests ran"</fails_when>
    <automated>uv run python -c "from nativespeaker.api.config import EnvironmentConfig; c=EnvironmentConfig().app_config; print(c.db.pool_size, bool(c.db.host), c.resilience.pool_size)"</automated>
    <fails_when>non-zero exit, a traceback, or output whose first field is not `12` or whose second is not `True`</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or fewer passed than after Task 2, or any error during collection</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_config.py -q` passes with at least one more collected case than before the task.
    - The new case asserts `db.pool_size == 12` and asserts all five `DB_*` credential fields still hold their environment values.
    - `uv run python -c "from nativespeaker.api.config import EnvironmentConfig; print(EnvironmentConfig().app_config.db.pool_size)"` prints `12`.
    - `ls .planning/todos/pending/` lists neither of the two folded todo files, and `ls .planning/todos/done/` lists both.
    - Each moved file's frontmatter carries `status: done`, a `completed:` date and `completed_in: 41`.
    - `git diff AGENTS.md` touches only the § Resilience section and adds no new section.
    - `uv run ruff check src tests` exits 0.
  </acceptance_criteria>
  <done>The pool holds 12 connections with the reason for the number legible beside its operand, the written rule matches the code again, and both todos are recorded as finished rather than left open behind a phase that fixed them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → LLM provider | the outbound call the breaker, the queue and the semaphore bound |
| application → PostgreSQL | the quota charge's round trip, and the pool it draws a connection from |

No client input crosses any boundary this plan touches: `resilience.py` sees an operation callable and
a token, never a request body.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-11 | Denial of Service | a request in flight against an open breaker | high | mitigate | D-14: the breaker is consulted before every attempt, so a dead provider costs one attempt rather than about ninety seconds, and the caller gets a `Retry-After` instead of a bare 503 |
| T-41-12 | Denial of Service | provider permits held across a database round trip | high | mitigate | D-15: the semaphore moves off the admission path and around the retry loop, so a slow database can no longer starve the provider concurrency budget. The in-flight slot and the breaker check stay in admission, both instantaneous |
| T-41-13 | Denial of Service | pool exhaustion at three concurrent chat posts | high | mitigate | D-16: `db.pool_size` rises to 12, two per possible in-flight chat plus two spare, with `max_overflow` unchanged. This is a live defect today, recorded as STATE.md A-15 |
| T-41-14 | Repudiation / integrity | a rejection the provider never saw recorded as a provider failure | medium | mitigate | The gate-and-breaker pass-through arm is ordered first and asserted to be, so the system cannot talk itself into a longer outage than the provider caused |
| T-41-15 | Tampering | double or phantom billing | medium | mitigate | The charge stays inside admission and outside the retry loop, so it runs exactly once per request and never for a request the breaker or the queue refused. The twenty billing cases are the standing proof and must stay green in substance |
| T-41-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not reachable: this plan installs nothing and `pyproject.toml` is not in `files_modified`. `41-RESEARCH.md` § Package Legitimacy Audit records no newly introduced package anywhere in this phase |
</threat_model>

<verification>
- `uv run pytest -q` green with a non-zero collected count
- `uv run ruff check src tests` exits 0
- the breaker is consulted in exactly two places and the pass-through arm is ordered first
- admission touches no semaphore and `ainvoke` does
- the loaded configuration reports a pool size of 12 with the database credentials still arriving from the environment
</verification>

<success_criteria>
Three live defects on the chat path are gone: a request no longer burns its retry budget against a
provider the breaker already opened, a provider permit is no longer held across the quota charge's
database round trip, and the pool no longer exhausts at three concurrent chat posts. The written rule
in `AGENTS.md` matches the code, and both todos are closed rather than left open behind their fix.
</success_criteria>

<output>
Create `.planning/phases/41-post-auth-claim-anonymous-grant/41-02-SUMMARY.md` when done
</output>
