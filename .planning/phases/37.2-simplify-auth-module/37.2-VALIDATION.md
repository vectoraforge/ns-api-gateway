---
phase: 37.2
slug: simplify-auth-module
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: true
# Wave 0 gaps are satisfied inline by TDD tasks inside plans 01/03/05/06 — there is no separate
# Wave 0 plan. This flips true once those five test files exist on disk during execution.
wave_0_complete: false
created: 2026-08-25
---

# Phase 37.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `37.2-RESEARCH.md` § Validation Architecture (Directive → Test Map, Sampling Rate,
> Wave 0 Gaps).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.0.2 + pytest-asyncio 1.3.0 (`asyncio_mode = "auto"`) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` — `addopts = "-v --tb=short -m 'not e2e and not schema'"` |
| **Quick run command** | `.venv/bin/pytest` (addopts already deselect e2e and schema) |
| **Full suite command** | `.venv/bin/pytest -m 'e2e or schema' && .venv/bin/pytest` |
| **Estimated runtime** | quick ~28s measured (831 passed, 309 deselected, 2026-08-25); full ~3–5 min (e2e needs PostgreSQL 17 on localhost:5432) |

Environment: Python 3.14.7 in `.venv`, PostgreSQL 17.11 available, ruff 0.15.7.
Firebase ADC and the OpenAI key may be absent locally — the affected e2e cases `pytest.skip` rather
than fail, which is why plan 03 carries a blocking human checkpoint (see Manual-Only Verifications).

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/pytest` (unit only — fast)
- **After every plan wave:** Run `.venv/bin/pytest -m 'e2e or schema'` plus the unit suite
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds (the unit gate)

No watch-mode flags anywhere: every command above is a single-shot run that exits.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 37.2-01-01 | 01 | 1 | CREATE-01, CREATE-02 (D-01, D-02) | T-37.2-02 / T-37.2-03 | Challenge id travels in the body only under `Cache-Control: no-store`; the new route is a declared, reviewed member of the pre-auth allowlist | e2e + unit (tracer, tdd) | `.venv/bin/pytest tests/unit/test_app_wiring.py -x -q && .venv/bin/pytest tests/e2e/test_create_user.py -m e2e -k "TestTheChallengeEndpoint or TestTheAnonymousHappyPath" -x -q` | ✅ retarget existing | ⬜ pending |
| 37.2-01-02 | 01 | 1 | CREATE-01 (D-01) | T-37.2-01 | Every non-issuable string operation collapses to one 400 `invalid_request`, disclosing nothing about the vocabulary; the 400 arm issues nothing and reads nothing | unit (tdd) | `.venv/bin/pytest tests/unit/test_challenge_endpoint.py -x -q` | ❌ W0 (new, created by this task) | ⬜ pending |
| 37.2-02-01 | 02 | 1 | — (D-06, D-07) | T-37.2-07 / T-37.2-08 | No service-account key path survives in `src/`; a stale key in a developer `.env` is ignored; `projectId` stays explicit so no app binds to an inferred project | unit / static | `.venv/bin/python -c "import nativespeaker.api.config, nativespeaker.api.auth.firebase" && grep -rn --include='*.py' -c 'credentials.Certificate' src \| grep -v ':0$' ; test $? -eq 1` | ✅ existing | ⬜ pending |
| 37.2-02-02 | 02 | 1 | — (D-06, D-07) | T-37.2-05 / T-37.2-06 | Absent ADC → `{}`, no Admin app, boot proceeds and completion fails closed as 503; the e2e skip guard still gates the real-Firebase fixtures | unit | `.venv/bin/pytest tests/unit/test_firebase_adapter.py tests/unit/test_config.py -x -q && .venv/bin/pytest -q` | ✅ shrink existing | ⬜ pending |
| 37.2-03-01 | 03 | 1 | — (folded todo) | T-37.2-09 / T-37.2-12 | The schema actually sent to the provider has every declared key required and `additionalProperties: false`; flat root, so strict conversion cannot silently no-op | unit (tdd) | `.venv/bin/pytest tests/unit/test_llm_chain_schema.py tests/unit/test_services.py -x -q` | ❌ W0 (new, created by this task) | ⬜ pending |
| 37.2-03-02 | 03 | 1 | — (folded todo) | T-37.2-10 / T-37.2-12 | A real provider returns all four declared keys — a mocked chain cannot satisfy this gate; a missing key skips rather than passes | e2e, **real provider** | `.venv/bin/pytest tests/e2e/test_llm_schema.py -m e2e -q` | ❌ W0 (new, created by this task) | ⬜ pending |
| 37.2-03-03 | 03 | 1 | — (folded todo) | T-37.2-12 | Distinguishes PASSED from SKIPPED — the automated command exits 0 in both cases, so only a human can confirm the constraint was honored | **manual** (checkpoint:human-verify, blocking) | none — see Manual-Only Verifications | n/a | ⬜ pending |
| 37.2-03-04 | 03 | 1 | — (folded todo) | — | Documentation only | static | `test ! -f .planning/todos/pending/restore-strict-structured-output.md && grep -rl 'restore-strict-structured-output' .planning/todos \| head -1` | ✅ existing | ⬜ pending |
| 37.2-04-01 | 04 | 2 | FOUND-08 (D-09, D-11) | T-37.2-13 / T-37.2-15 | Every deleted name was grepped across `src/` and `tests/` first; nothing deleted carried a runtime check — import + full collection is the proof | unit (collection) | `.venv/bin/python -c "import nativespeaker.api.app.main" && .venv/bin/pytest -q --collect-only >/dev/null` | ✅ implicit (D-11) | ⬜ pending |
| 37.2-04-02 | 04 | 2 | FOUND-08 (D-09) | T-37.2-14 | The surviving seam module still imports only stdlib and this project — no provider SDK, no credential source drifts in | unit | `.venv/bin/pytest tests/unit/test_adapter_interfaces.py tests/unit/test_firebase_adapter.py -x -q && .venv/bin/pytest -q` | ✅ shrink existing (363 → ~120 lines) | ⬜ pending |
| 37.2-05-01 | 05 | 3 | CREATE-01, CREATE-02 (D-03, D-05) | T-37.2-16 / T-37.2-18 | Every challenge rejection still collapses to one 409 `challenge_required`; the duplicate-account race is answered by the DB UNIQUE constraint plus savepoint rollback, not by the deleted pre-check | unit | `.venv/bin/pytest tests/unit/test_create_user_precedence.py tests/unit/test_create_user_rollback.py tests/unit/test_challenge_endpoint.py -x -q` | ✅ existing | ⬜ pending |
| 37.2-05-02 | 05 | 3 | CREATE-01, CREATE-02 (D-04, D-05) | T-37.2-17 / T-37.2-19 | The 400→422 flip leaks no field detail (shared handler returns `{"code": "validation_error"}` only); whitespace is not trimmed, so no handle family widens | unit | `.venv/bin/pytest tests/unit/test_create_user_body.py tests/unit/test_create_user_precedence.py tests/unit/test_create_user_rollback.py -x -q && .venv/bin/pytest -q` | ❌ W0 (`test_create_user_body.py` new); `test_mode_signal.py` deleted, `test_create_user_modes.py` rewritten | ⬜ pending |
| 37.2-05-03 | 05 | 3 | CREATE-01, CREATE-02 (D-03) | T-37.2-16 / T-37.2-20 | An already-linked caller hears 409 `identity_already_linked` at completion from the constraint, not from a pre-check; the extra challenge row + lookup cost is accepted per D-03 | e2e | `.venv/bin/pytest tests/e2e/test_create_user.py tests/e2e/test_admission.py tests/e2e/test_challenge_store.py -m e2e -q` | ✅ rewrite in place | ⬜ pending |
| 37.2-06-01 | 06 | 4 | — (D-10) | T-37.2-21 / T-37.2-24 | The off-loop threadpool hop and the attempt budget survive the merge; the closed provider classification moves verbatim with its cases | unit | `.venv/bin/pytest tests/unit/test_provider_classifier.py tests/unit/test_firebase_retry.py tests/unit/test_firebase_adapter.py tests/unit/test_adapter_interfaces.py tests/unit/test_create_user_precedence.py -x -q` | ✅ existing | ⬜ pending |
| 37.2-06-02 | 06 | 4 | — (D-10) | T-37.2-22 | The `auth_rejected` event name and its three keys stay byte-identical after `record_rejection` is inlined into `app/dependencies.py::_reject` — the rejection record is not lost | unit | `.venv/bin/pytest tests/unit/test_identity_resolution.py tests/unit/test_auth_security.py tests/unit/test_wire_contract.py -x -q` | ✅ retarget existing (3 direct calls) | ⬜ pending |
| 37.2-06-03 | 06 | 4 | ROADMAP criterion 5 (baseline shrink) | T-37.2-23 / T-37.2-25 | The path-keyed adapter permission map still names modules that exist; the HMAC keyring is out of scope for every merge — both pinned by the AST assertion | unit (tdd, AST assertion) | `.venv/bin/pytest tests/unit/test_auth_package_shape.py -x -q` | ❌ W0 (new, created by this task) | ⬜ pending |
| 37.2-07-01 | 07 | 5 | CREATE-01, CREATE-02, FOUND-08 | T-37.2-26 / T-37.2-27 / T-37.2-29 | The credential divergence is written as the third flagged conflict rather than silently resolved; the widened pre-auth set is stated where a later phase will read it; no note points at a deleted module | static (grep) | `grep -c 'Phase 37.2' .planning/REQUIREMENTS.md` plus the acceptance greps, incl. `grep -rn 'auth/telemetry' .planning/REQUIREMENTS.md` returning nothing | ✅ existing | ⬜ pending |
| 37.2-07-02 | 07 | 5 | CREATE-01, CREATE-02, FOUND-08 | T-37.2-28 | Phases 43–46 inherit the deleted adapter seam with an explicit forward flag, not by surprise | static (grep) | `grep -c '37.2-0' .planning/ROADMAP.md` | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Sampling continuity:** the only task without an `<automated>` verify is `37.2-03-03`, a blocking
human checkpoint sitting between two automated tasks. No three consecutive tasks lack automated
verification.

---

## Wave 0 Requirements

**Satisfied inline, not by a separate Wave 0 plan.** Every ❌ file above is created by the TDD task
that first needs it, inside its own plan, before the production edit it gates. Recorded here so the
gate reads as deliberate rather than missed:

- [ ] `tests/unit/test_challenge_endpoint.py` — D-01's six-row status matrix; created by plan 01 Task 2 (`tdd="true"`); plan 01 Task 1 is the `tracer` that starts red
- [ ] `tests/unit/test_llm_chain_schema.py` — the provider-free proof that the emitted schema is strict; created by plan 03 Task 1 (`tdd="true"`)
- [ ] `tests/e2e/test_llm_schema.py` — the folded todo's real-provider gate (blocked on assumption A2); created by plan 03 Task 2
- [ ] `tests/unit/test_create_user_body.py` — D-04's usable/unusable handle partition after the 400→422 flip; created by plan 05 Task 2
- [ ] `tests/unit/test_auth_package_shape.py` — the AST assertion pinning the auth-package shrink below the 14 files / 28 classes / 57 functions baseline, so ROADMAP success criterion 5 is machine-checked rather than asserted in prose; created by plan 06 Task 3 (`tdd="true"`)
- [x] Framework install — **none needed.** pytest 9.0.2 + pytest-asyncio 1.3.0 are installed and configured.

Everything else in the phase retargets, shrinks, rewrites or deletes existing suites — see
RESEARCH.md § "Test files whose fate is decided, not discovered".

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The real provider actually honored `strict=True` on the bound JSON schema — every declared key present in its raw output | — (folded todo `restore-strict-structured-output`; threat T-37.2-12) | Strict-schema support is provider- **and model**-specific (assumption A2, unverified). Every provider wrapper *accepts* the argument; only some *honor* it. `tests/e2e/test_llm_schema.py` skips cleanly when no key is configured, so `pytest … -q` exits 0 for both "honored" and "never ran" — no automated command can tell those apart. RESEARCH.md is explicit that a mocked chain proves nothing here. | Plan 03 Task 3 (`checkpoint:human-verify`, `gate="blocking"`): (1) put a real provider key in the environment; (2) run `.venv/bin/pytest tests/e2e/test_llm_schema.py -m e2e -v`; (3) every case must report **PASSED** — any **SKIPPED** means the gate did not run and the checkpoint is unsatisfied; (4) confirm the configured model in `config/config.yaml` is the one being shipped; (5) optionally run `.venv/bin/pytest tests/e2e/test_chats.py -m e2e -v` and confirm both chat routes still answer 200 with non-empty `content`. |

All other phase behaviors have automated verification.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — 17 of 18 tasks carry an automated command; the 18th is the blocking human checkpoint listed above
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — the single manual task is bracketed by automated ones
- [x] Wave 0 covers all MISSING references — all five new test files are created inline by the TDD task that needs them, ahead of the production edit
- [x] No watch-mode flags — every command is single-shot
- [x] Feedback latency < 30s — the unit gate measured 27.99s (831 passed) on 2026-08-25
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-25 (planning-time sign-off; the Per-Task Status column and
`wave_0_complete` flip during execution)
