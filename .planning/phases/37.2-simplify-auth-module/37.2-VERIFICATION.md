---
phase: 37.2-simplify-auth-module
verified: 2026-08-26T03:25:54Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:

  - test: "CR-01: decide whether to bound POST /auth/challenge issuance (per-identity cap and/or reaper and/or gateway rate limit on /auth) before shipping this phase, or formally accept the risk"
    expected: "Either a fix lands (cheapest: an already-linked pre-check restored at issuance, or a per-identity outstanding-count cap, or a reaper migration/CronJob, or an HTTPRoute+BackendTrafficPolicy entry for /auth), or the risk is explicitly accepted in writing given AGENTS.md's threat model, with the STRIDE register's T-37.2-04 disposition corrected (its 'accept' reasoning — 'per-minute request limiting is Envoy's job' — is factually false for this route per CR-02)"
    why_human: "This is a product-risk tradeoff (cost of a fix vs. cost of leaving an unauthenticated-adjacent, unbounded, unreaped state-insertion endpoint live), not a fact a test can settle. The gap is real and codebase-confirmed (see Anti-Patterns Found), and it is attributable to this phase's own deletions and additions, not inherited debt."
---

# Phase 37.2: Simplify auth module Verification Report

**Phase Goal:** Continue Phase 37.1's de-complication into the auth feature itself: collapse the `auth/` package (was 14 files, 28 classes, 57 functions for one create-user feature) to the minimum that carries the behavior, drop the `credentials.Certificate()` path so Firebase initializes via ADC only, move the request/response models out of `routers/auth.py` into `models/auth.py`, and replace the `?challenge=true` mode signal with an explicit `POST /auth/challenge` endpoint taking `{"operation": "create_user"}` — deleting the mode-signal classifier that existed only to disambiguate the old shape.
**Verified:** 2026-08-26T03:25:54Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria, verified directly against code)

| # | Truth (ROADMAP success criterion) | Status | Evidence |
|---|---|---|---|
| 1 | `credentials.Certificate()` and the credential-dict path are gone from `auth/firebase.py`; Firebase initializes via ADC only | ✓ VERIFIED | `grep -n "Certificate\|credential_dict" src/nativespeaker/api/auth/firebase.py` → no match. `firebase.py:55-63` builds the only credential via `credentials.ApplicationDefault()`, probed first by `google.auth.default()`. `config.py` has no `FirebaseConfig`/`AppConfig.firebase` field (`grep -rn FirebaseConfig src` → none). `.env.example` documents ADC only. |
| 2 | `CreateUserRequest`, `PrepareResponse`, `CompletionResponse` live in `models/auth.py`; `routers/auth.py` defines no Pydantic models | ✓ VERIFIED | `grep -n "^class " src/nativespeaker/api/models/auth.py` shows all three plus `ChallengeRequest`, `AuthOperation`, `AuthEventResult`, `AuthChallenge`. `grep -n "^class " src/nativespeaker/api/routers/auth.py` → zero matches; the router imports all three models from `models.auth`. |
| 3 | Challenge issuance is its own route (`POST /auth/challenge`, operation in body); single rejection class 400 `invalid_request` for both unknown and valid-but-unbuilt operations; accepted set is exactly `{create_user}`; `?challenge=true`, `auth/modesignal.py`, `classify_mode_signal`, `get_raw_query_string` all deleted; `POST /auth/create-user` handles completion only | ✓ VERIFIED | `routers/auth.py:49-70` — `issue_challenge` compares only against `AuthOperation.create_user.value`; every other value (including valid-but-unbuilt enum members like `sync`, `sign_out_all`) hits the identical `error_response(INVALID_REQUEST)` branch. `auth/modesignal.py` absent (`ls` fails). `grep -rn 'classify_mode_signal\|ModeSignal\|get_raw_query_string\|challenge=true' src` → no matches. `create_user` handler (`routers/auth.py:77-87`) takes only a required `CreateUserRequest` body, dispatches on nothing, parses no query string. `tests/unit/test_challenge_endpoint.py` (24 cases) and `tests/unit/test_create_user_body.py` pin the 400/422 matrix; both pass live (`pytest tests/unit/test_challenge_endpoint.py -q` → 24 passed). |
| 4 | `_classification_cause` and similar single-caller name-a-value indirections are inlined or deleted | ✓ VERIFIED | `grep -rn '_classification_cause' src` → no matches; the conditional is inlined at `routers/auth.py:143` (`cause="empty" if not provider_data.entries else "invalid-shape"`). Plan 06 additionally folded `_already_linked`/`_prepare` deletions (plan 05) and inlined the one-caller `record_rejection`/`auth/telemetry.py` into `app/dependencies.py::_reject` — confirmed: `auth/telemetry.py` absent, `_reject` in `app/dependencies.py` emits `auth_rejected` with the same three keys. |
| 5 | `auth/` package measurably smaller (14/28/57 baseline) with every deletion justified as carrying no behavior; e2e admission matrix and unit suites pass with edits only where the challenge-endpoint split moved a contract | ✓ VERIFIED | `ls src/nativespeaker/api/auth/*.py \| wc -l` → 10 (matches `tests/unit/test_auth_package_shape.py`'s `CURRENT = (10, 19, 44)`, both below `BASELINE = (14, 28, 57)`, machine-asserted by two parametrized cases plus two non-vacuity controls, all passing). `tests/e2e/test_admission.py` shows **zero diff** against the pre-phase commit (`git diff --stat fb95569 HEAD -- tests/e2e/test_admission.py` → empty) and its 23-case matrix collects and passes. `tests/unit/test_create_user_precedence.py` shows exactly a one-line import retarget (`auth.retry` → `auth.firebase`, forced by plan 06's cohesion merge), zero status-code/body-literal changes. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/nativespeaker/api/auth/firebase.py` | ADC-only credential build; classifier + retry merged in (plan 06) | ✓ VERIFIED | No `Certificate`; `run_in_threadpool` and `retry_if_result` present exactly once each (call sites); `classify_provider_data`, `email_to_persist`, `lookup_with_retry`, `_is_retryable` all present |
| `src/nativespeaker/api/models/auth.py` | Owns all four request/response models | ✓ VERIFIED | `ChallengeRequest`, `PrepareResponse`, `CreateUserRequest`, `CompletionResponse` all present; `challenge_id` has `min_length=1` (PydanticField alias) |
| `src/nativespeaker/api/routers/auth.py` | Two routes, no local models, completion-only create-user | ✓ VERIFIED | `/auth/challenge` and `/auth/create-user`, zero `class` statements |
| `src/nativespeaker/api/auth/adapters.py` | Shrunk to the providerData seam only | ✓ VERIFIED | `ProviderDataOutcome`, `ProviderDataEntry`, `ProviderDataResult`, one-method `FirebaseAdminAdapter` (plan 04); `StoreAdapter`/`VendorProofAdapter`/etc. absent from `src` |
| `src/nativespeaker/api/auth/__init__.py` | Near-empty, no re-export machinery | ✓ VERIFIED | 1 line; no `__all__`/`__getattr__`/`__dir__` |
| `src/nativespeaker/api/auth/modesignal.py` | Deleted | ✓ VERIFIED | Absent |
| `src/nativespeaker/api/auth/classifier.py`, `retry.py`, `telemetry.py` | Merged away (plan 06) | ✓ VERIFIED | All absent; contents verified present verbatim in `firebase.py` / `app/dependencies.py` |
| `tests/unit/test_auth_package_shape.py` | Machine-checked size assertion | ✓ VERIFIED | Exists, passes, literal baseline/current constants |
| `.planning/REQUIREMENTS.md` | CREATE-01/02, FOUND-08 amended; third flagged conflict recorded | ✓ VERIFIED | All three amendment blockquotes present and match shipped code exactly (verified by direct read); `FLAGGED CONFLICT against the binding specification` appears 3 times |
| `.planning/ROADMAP.md` | Phase 37.2 entry closed with 7/7 plans, measured outcome | ✓ VERIFIED | All 7 plans listed by wave; measured-outcome line matches `test_auth_package_shape.py`'s literals |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `routers/auth.py::issue_challenge` | `ChallengeStore.issue` | direct call, no adapter/raw-query dep declared | ✓ WIRED | Confirmed in source; handler signature has no `get_firebase_adapter`/`get_raw_query_string` |
| `create_user body` | `CreateUserRequest.challenge_id` → `ChallengeStore.locate` | required `str`, `min_length=1`, forwarded untouched | ✓ WIRED | `routers/auth.py:84-87`, `_complete` at line 98 |
| already-linked caller → completion | `create_account` → UNIQUE violation → `classify_insert_conflict` → 409 | in-transaction constraint, not a pre-check | ✓ WIRED | `_already_linked`/`_prepare` deleted; e2e `TestCompletionRejectsAnAlreadyLinkedCaller` exercises this path and passes |
| `test_app_wiring.py::PREAUTH_CALLABLE_PATHS` | admission dependency | literal allowlist, hand-maintained | ✓ WIRED | `{"/auth/create-user", "/auth/challenge"}`; `TestEveryRouteIsAuthenticated` passes |

### Requirements Coverage

No requirement IDs are mapped to Phase 37.2 in ROADMAP.md ("none mapped — scope is the five directives"). Plan frontmatter (01, 04, 05, 07) declares `CREATE-01`, `CREATE-02`, `FOUND-08` as *touched-and-amended*, not newly satisfied. Cross-referenced against `.planning/REQUIREMENTS.md`:

| Requirement | Source Plan | Treatment | Status | Evidence |
|---|---|---|---|---|
| CREATE-01 | 01, 05, 07 | Amended (count 1→2 routes, substance intact) | ✓ SATISFIED | REQUIREMENTS.md blockquote matches `PREAUTH_CALLABLE_PATHS` exactly |
| CREATE-02 | 01, 05, 07 | Amended (mode signal → route partition) | ✓ SATISFIED | REQUIREMENTS.md blockquote matches shipped `create_user`/`issue_challenge` split |
| FOUND-08 | 04, 07 | Amended, flagged forward to 43-46 | ✓ SATISFIED | REQUIREMENTS.md blockquote matches shrunk `adapters.py`; forward flags present on APPLEHOOK-01, PLAYHOOK-02, RESTORE-01, SIGNOUT-01 |

No orphaned requirements found for this phase in REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/nativespeaker/api/auth/challenges.py` | 46-72 (`issue`) | No per-identity cap on outstanding challenges; no reaper for `core.auth_challenges` despite `ix_auth_challenges_expires_at` existing for exactly that purpose | 🛑 High (code-review CR-01, independently confirmed) | `POST /auth/challenge` is a new public, state-inserting endpoint. This phase deleted the only pre-existing gate (`_prepare`'s already-linked pre-check) and added no replacement. Confirmed live: `grep` shows no `DELETE FROM ... auth_challenges` anywhere in `src/`, `migrations/`, or k8s manifests. |
| `k8s/templates/backend-traffic-policy.yaml` | 9-12 | `BackendTrafficPolicy.spec.targetRefs` names only `{fullname}-llm-routes`; no HTTPRoute matches `/auth` at all (`k8s/templates/httproute-app.yaml:11-24` matches only `/chats`, `/users`, `/examples`) | ℹ️ Info (CR-02) — **pre-existing, confirmed not a regression of this phase.** `git show 9ecebed:k8s/templates/httproute-app.yaml` shows the same three-path match before this phase started; `/auth` was already unrouted. This phase made the gap worse in effect (added a second, mandatory-first-step endpoint under the same unrouted prefix) but did not create the routing gap itself. |

Both findings were independently reproduced against the live codebase during this verification (not merely taken from `37.2-REVIEW.md`).

**Verdict on these findings, per the instructed weighting:** CR-02 (routing) is correctly attributed as inherited/pre-existing and is not counted against this phase's success criteria — none of the five ROADMAP criteria mention gateway routing. CR-01 (uncapped, unreaped challenge issuance), however, **is this phase's own creation**: it deleted the one pre-existing precondition check (`_prepare`'s already-linked gate) per an explicit, deliberate decision (D-03, recorded and defensible on its own terms — the deleted check was racy and the DB constraint is the real authority for *duplicate accounts*), but that same commit also removed the only friction that made issuance non-trivial to loop on, while adding zero replacement bound. The phase's own STRIDE register (plan 01, threat T-37.2-04) explicitly rated this "low / accept" reasoning that "per-minute request limiting is Envoy's job" — and that premise is now known to be false for this route (CR-02: no HTTPRoute matches `/auth` at all, so Envoy applies no rate limit here). The risk-acceptance argument the phase relied on does not hold. This is not one of the five literal ROADMAP success criteria, so it does not FAIL a must-have truth and does not block the phase's stated goal from being scored achieved — but it is a real, live, codebase-confirmed gap that this phase introduced and left unresolved, and it should not be silently passed through to ship.

### Human Verification Required

### 1. CR-01 disposition: bound or formally accept the uncapped `/auth/challenge` issuance path

**Test:** Decide and record one of: (a) restore a cheap already-linked pre-check at issuance (no query needed — `LinkedIdentity` is already in the request context), (b) add a per-identity outstanding-challenge cap in `ChallengeStore.issue`, (c) add a reaper (migration/CronJob) for `core.auth_challenges`, (d) add `/auth` to an HTTPRoute covered by a `BackendTrafficPolicy`, or (e) formally accept the risk in writing, correcting T-37.2-04's now-false "Envoy handles this" rationale.
**Expected:** One of the five is chosen and either implemented or explicitly accepted with corrected reasoning, before this phase ships to a state where `/auth/challenge` is reachable.
**Why human:** This is a cost/risk tradeoff appropriate to AGENTS.md's stated threat model (pre-launch, sub-$5/month, no users yet, "don't skip normal security measures just because there are no users yet") — not a fact a test can settle.

### Gaps Summary

No gaps against the phase's five stated ROADMAP success criteria — all five verified directly against the live codebase (imports, greps, and live test runs), not taken on SUMMARY.md's word. Full suite: 762 passed / 313 deselected (unit), 313 passed / 762 deselected (`-m 'e2e or schema'`), `ruff check src tests` clean, `python -c "import nativespeaker.api.app.main"` exits 0 — all reproduced live during this verification, matching every SUMMARY's claimed numbers exactly.

The one open item is CR-01, a real and phase-attributable security/reliability gap (unbounded, unreaped, unrate-limited state-insertion endpoint) that falls outside the five literal success criteria but was flagged as critical by this phase's own code review and independently reproduced here. It is surfaced as a human-verification item rather than a FAILED must-have, because no must-have truth in ROADMAP.md or any PLAN's frontmatter asserts a rate-limit or cap on challenge issuance — the gap is real but is not a broken promise this phase made about itself in its own success criteria.

Two additional lower-severity items surfaced by `37.2-REVIEW.md` and independently spot-checked (not full-blocking, noted for completeness): WR-03 (the `FirebaseAdminAdapter` Protocol declares a sync method its only implementation returns as a coroutine, leaving the entire adapter call chain untyped) and WR-11 (a model refusal now trips the shared circuit breaker as a provider-health failure rather than mapping to `out_of_scope` — this is in plan 03's "folded todo" workstream, which is outside Phase 37.2's five ROADMAP directives but shipped in the same phase). Neither blocks the five stated success criteria; both are legitimate follow-up candidates.

---

_Verified: 2026-08-26T03:25:54Z_
_Verifier: Claude (gsd-verifier)_
