---
status: testing
phase: 37.2-simplify-auth-module
source: [37.2-VERIFICATION.md]
started: 2026-08-26T03:30:00Z
updated: 2026-08-26T03:30:00Z
---

## Current Test

number: 1
name: CR-01 — decide whether to bound POST /auth/challenge issuance before shipping, or formally accept the risk
expected: |
  Either a fix lands (cheapest options: an already-linked pre-check restored at
  issuance, a per-identity outstanding-count cap, a reaper migration/CronJob, or an
  HTTPRoute + BackendTrafficPolicy entry for /auth), or the risk is explicitly
  accepted in writing given AGENTS.md's threat model — with the STRIDE register's
  T-37.2-04 disposition corrected, since its "accept" reasoning ("per-minute request
  limiting is Envoy's job") is factually false for this route per CR-02.
awaiting: user response

## Tests

### 1. CR-01: bound or formally accept unbounded POST /auth/challenge issuance
expected: A fix lands, or the risk is accepted in writing with the T-37.2-04 STRIDE disposition corrected.
result: [pending]

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

Codebase-confirmed, attributable to this phase:

- `challenges.py::issue` performs no outstanding-count check per identity.
- No `DELETE FROM auth_challenges` exists anywhere, despite the migration shipping
  an `ix_auth_challenges_expires_at` index — expiry appears to have been intended
  and never implemented.
- Plan 37.2-05 deleted `_prepare`'s already-linked gate, which was the only prior
  check on this path, and did not replace it.
- `BackendTrafficPolicy.spec.targetRefs` names only `-llm-routes`, so no gateway
  rate limit reaches `/auth`.

Related but NOT attributable to this phase (pre-existing): CR-02 — no HTTPRoute
matches `/auth` at all. Confirmed via `git show 9ecebed:k8s/templates/httproute-app.yaml`,
which matches only `/chats`, `/users`, `/examples`. The whole `/auth` prefix was
already unreachable in-cluster before Phase 37.2 began.
