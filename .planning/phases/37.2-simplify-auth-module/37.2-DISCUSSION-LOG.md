# Phase 37.2: Simplify auth module - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-25
**Phase:** 37.2-simplify-auth-module
**Areas discussed:** Challenge endpoint contract, Create-user completion contract, Firebase ADC-only fallout, Deletion reach

---

## Todo cross-reference

| Option | Description | Selected |
|--------|-------------|----------|
| Neither — record as reviewed (Recommended) | Both matched on generic keywords; 37.1 folded neither | |
| Fold secret-manager-integration | Tangentially related via the Firebase credential deletion | |
| Fold restore-strict-structured-output | Restore with_structured_output(strict=True) on the LLM chain | ✓ |

**User's choice:** Fold restore-strict-structured-output
**Notes:** Deliberate scope addition against the recommendation — the LLM-chain fix ships alongside this cleanup phase as its own workstream. secret-manager-integration recorded as reviewed, not folded.

---

## Challenge endpoint contract

| Option | Description | Selected |
|--------|-------------|----------|
| One bucket: 400 invalid_request (Recommended) | Unknown values and known-but-unbuilt operations reject identically; no vocabulary disclosure | ✓ |
| Distinguish: 400 vs operation_not_allowed | More informative, discloses the operation vocabulary | |
| Accept any enum member | Mints live challenges nothing can consume | |

**User's choice:** One bucket: 400 invalid_request

| Option | Description | Selected |
|--------|-------------|----------|
| 200, same body, verbatim (Recommended) | {challenge_id, expires_at} + Cache-Control: no-store, mechanical test moves | ✓ |
| 201 Created | More correct REST, extra contract change | |
| 200 with operation echoed | Self-describing, wider body | |

**User's choice:** 200, same body, verbatim

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, move it verbatim (Recommended) | Keep the 409 already-linked rejection at issuance | |
| No, drop it — let completion catch it | Constraints are the sole authority; 409 surfaces at completion | ✓ |

**User's choice:** No, drop it — let completion catch it
**Notes:** Deliberate contract change against the recommendation: 409-at-prepare e2e cases are rewritten to assert at completion; an already-linked caller burns a challenge row and one Firebase lookup.

---

## Create-user completion contract

| Option | Description | Selected |
|--------|-------------|----------|
| Keep 400 invalid_request (Recommended) | Any-typed field + handler check preserves the e2e status codes exactly | |
| Let FastAPI validate: 422 | challenge_id: str required; idiomatic; flips several e2e cases 400 → 422 | ✓ |

**User's choice:** Let FastAPI validate: 422

| Option | Description | Selected |
|--------|-------------|----------|
| Ignore them entirely (Recommended) | Delete all query parsing; params ignored like every other route | ✓ |
| Reject ?challenge=true with 400 | Migration hint with no clients to hint at | |

**User's choice:** Ignore them entirely

---

## Firebase ADC-only fallout

| Option | Description | Selected |
|--------|-------------|----------|
| Delete the whole surface (Recommended) | FirebaseConfig, AppConfig.firebase, .env.example line, credential_dict() branch | ✓ |
| Keep an empty FirebaseConfig shell | Placeholder for future settings — speculative structure | |

**User's choice:** Delete the whole surface

| Option | Description | Selected |
|--------|-------------|----------|
| Keep warn-and-boot (Recommended) | No ADC → warn, no Admin app, lookups fail closed as 503 | ✓ |
| Fail at boot | Louder in production, breaks credential-less dev/test runs | |

**User's choice:** Keep warn-and-boot
**Notes:** The ADC-only directive reverses Phase 37 D-08 and conflicts with SHARED-INVARIANTS' no-ambient-credentials rule — recorded as a flagged conflict per the standing rule (not asked; mandatory bookkeeping).

---

## Deletion reach

| Option | Description | Selected |
|--------|-------------|----------|
| Delete all zero-consumer interfaces (Recommended) | Store/vendor/device adapters + unconsumed FirebaseAdminAdapter methods; FOUND-08 amended, flagged forward to 43–46 | ✓ |
| Keep them — they're the recorded contract | ~80 lines documenting future obligations | |
| Middle: delete store/vendor, keep revocation | Phase 46's seam is tiny and nearer | |

**User's choice:** Delete all zero-consumer interfaces

| Option | Description | Selected |
|--------|-------------|----------|
| Merge by cohesion, planner decides target (Recommended) | Each merge justified by cohesion, each deletion by carrying no behavior | ✓ |
| Aggressive: collapse to ~5-6 files | Maximal shrink, riskier review | |
| Keep layout, delete dead code only | Smallest diff, leaves the sprawl | |

**User's choice:** Merge by cohesion, planner decides target

| Option | Description | Selected |
|--------|-------------|----------|
| Delete it — import from submodules (Recommended) | __init__.py near-empty; mechanical import-site edits | ✓ |
| Keep a plain re-export list | Drop the lazy machinery, keep import sites | |
| Keep as is | Weakest fit with the phase's intent | |

**User's choice:** Delete it — import from submodules

---

## Claude's Discretion

- Directive 4's single-caller-indirection sweep, judged per-case during the rewrite
- Module names and final layout under the cohesion-merge rule; where the Firebase constants land
- Challenge request-model name and the accepted-set check mechanism (Literal vs handler check)
- For the folded LLM todo: fate of ask_llm's post-hoc validation and the Phase 36 D-12 empty-list defaults

## Deferred Ideas

- 37-REVIEW.md open findings (CR-02, WR-01) — CR-02 sits in code D-03 rewrites; fix in passing
- Provider-callback category and now adapter interfaces for phases 43/44 — flagged forward
- .planning/codebase/*.md refresh after this phase lands
- Envoy gateway contract (§9 / FOUND-09) — v2.1
- secret-manager-integration — reviewed, not folded; scope shrinks via the credential deletion
