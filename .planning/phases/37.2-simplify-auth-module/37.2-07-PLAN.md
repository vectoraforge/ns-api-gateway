---
phase: 37.2-simplify-auth-module
plan: 07
type: execute
wave: 5
depends_on: ["37.2-06"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CREATE-01, CREATE-02, FOUND-08]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CREATE-01 records that the pre-auth-callable set is now two enumerated routes, not one — substance intact, count changed"
    - "CREATE-02 records that the two phases of the operation are partitioned by route rather than by a mode signal — amended, not withdrawn, because both phases still exist"
    - "FOUND-08 is amended and flagged forward to phases 43–46: each defines its adapter interface beside its first implementation"
    - "A third flagged conflict against SHARED-INVARIANTS.md is recorded — the ADC decision against the no-ambient-credentials rule — flagged, never silently resolved"
    - "Every note naming a module or function this phase deleted or moved names something that exists"
    - "The Traceability rows, the treatment-summary table and the last-updated line all reflect this phase"
    - "The ROADMAP Phase 37.2 entry records its plans and its outcome"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "CREATE-01's enforcement point is tests/unit/test_app_wiring.py::PREAUTH_CALLABLE_PATHS — the amendment and the allowlist must agree"
    - "REBIND-02 and PROF-02 both name the rejection-log function by module path; plan 06 moved it, so both notes need the new location"
---

<objective>
Make the written record match the code this phase shipped: amend the three requirements the sanctioned
changes touched, record the third conflict against the binding specification, and repoint every note
that names a module this phase deleted or moved.

Purpose: this project's standing rule is that a divergence from the binding specification is flagged,
never silently resolved, and that a requirement whose mechanism changed is amended rather than quietly
left false. A note pointing at a deleted module is how a later reader loses an afternoon.

Output: a requirements file a reader can trust, and a roadmap entry that records what the phase did.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37.2-simplify-auth-module/37.2-CONTEXT.md
@.planning/phases/37.2-simplify-auth-module/37.2-RESEARCH.md
@.planning/phases/37.2-simplify-auth-module/37.2-02-SUMMARY.md
@.planning/phases/37.2-simplify-auth-module/37.2-04-SUMMARY.md
@.planning/phases/37.2-simplify-auth-module/37.2-05-SUMMARY.md
@.planning/phases/37.2-simplify-auth-module/37.2-06-SUMMARY.md
</context>

## Artifacts this phase produces

See `37.2-01-PLAN.md`. This plan creates no code symbol; it amends two planning documents.

<tasks>

<task type="auto">
  <name>Task 1: amend the three touched requirements and record the third flagged conflict</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the whole file, but especially: the header amendment blockquote at :10-12, FOUND-01's amendment and its flagged-conflict paragraph at :43-47, FOUND-05's withdrawal and flagged conflict at :56-60, FOUND-08 at :63, REBIND-02 at :78-79, CREATE-01 and CREATE-02 at :86-87, PROF-02 at :103-106, and the tail tables at :190-228)
    - .planning/phases/37.2-simplify-auth-module/37.2-CONTEXT.md (D-08's flagged-conflict wording and D-09's flag-forward instruction)
    - .planning/phases/37.2-simplify-auth-module/37.2-RESEARCH.md (the Phase Requirements table naming the required treatment for each of the three)
    - /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md (the no-ambient/default/fallback-credential rule, quoted verbatim in the new conflict note)
  </read_first>
  <action>
    Follow the amendment register Phase 37.1 established exactly: a bolded treatment word, the phase,
    the date, then what changed and what did not. Use blockquote notes indented under the requirement,
    matching the existing ones character for character in shape.

    **CREATE-01 — amended.** Its substance holds: pre-auth admission is still a closed, enumerated set
    and every other route still rejects an unlinked caller. What changed is the count — the challenge
    route must be pre-auth callable, because an unlinked caller needs a challenge in order to create an
    account at all. Say the set is now two routes, name both, and name the enforcement point:
    `tests/unit/test_app_wiring.py`'s literal pre-auth allowlist, which is deliberately hand-maintained
    so widening it is a visible edit.

    **CREATE-02 — amended, not withdrawn.** Both phases of the operation still exist and both are still
    challenge-bearing; they are partitioned by route rather than by a mode signal. Say what was deleted
    (the mode-signal classifier and the query-parameter signal) and what replaced it (one route per
    phase). Do not withdraw it: the substance — prepare plus completion — survives.

    **FOUND-08 — amended and flagged forward.** Eight zero-consumer adapter types and two unconsumed
    Protocol methods were deleted; what survives is the providerData surface the live code actually
    consumes. Flag forward to phases 43, 44, 45 and 46: each defines its adapter interface beside its
    first implementation, the same treatment Phase 37.1 gave the provider-callback category. Add the
    forward flag to those phases' own entries where they reference an adapter seam, in the same style
    the existing forward flags use.

    **The third flagged conflict.** Add it under CREATE-01 or as its own note beside the Firebase
    material, following the shape of the two existing flagged-conflict paragraphs verbatim: bolded
    "FLAGGED CONFLICT against the binding specification — recorded, NOT resolved", the quoted rule from
    `SHARED-INVARIANTS.md` forbidding any ambient, default or fallback credential, the statement that
    the document binds every phase and has not been amended, and then this project's knowing
    divergence — production runs on GKE where Application Default Credentials means Workload Identity,
    which is strictly better than a mounted key; the sub-five-dollar-a-month threat model does not
    justify maintaining a parallel credential path; and the earlier decision that rejected this is
    superseded. Note that the named-app-per-issuer discipline is unchanged and still satisfies the
    no-ambient-*client* rule at the app-selection layer.

    **Repoint the two notes that name a moved function.** REBIND-02's note and PROF-02's note both name
    the rejection-log function by its old module path. The event name and its three keys are unchanged,
    but the function was folded into the dependency that raises rejections. Update both notes to name
    where it lives now, in one clause each — do not rewrite the surrounding argument, which still holds.

    **Update the three tail structures.** Add CREATE-01, CREATE-02 and FOUND-08 to the amended row of
    the treatment-summary table; add the new flagged conflict to that table's conflicts row as the
    third entry; add the forward flags for 43–46 to the flagged-forward row. Update the Traceability
    rows for the FOUND and CREATE prefixes to say which requirements this phase amended, in the same
    parenthetical style the existing rows use. Retitle the "Standing after Phase 37.1's two deletions"
    heading so it covers both phases. Update the last-updated line at the foot with today's date and
    this phase's plan.

    Amend by scoped edit only — replace the specific blocks named above. Never rewrite the whole file.
  </action>
  <verify>
    <automated>grep -c 'Phase 37.2' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 37.2' .planning/REQUIREMENTS.md` is ≥ 6 — every amendment carries its dated attribution
    - `sed -n '/CREATE-01/,/CREATE-03/p' .planning/REQUIREMENTS.md | grep -c 'test_app_wiring'` is ≥ 1 — the amendment names its enforcement point
    - `sed -n '/CREATE-02/,/CREATE-03/p' .planning/REQUIREMENTS.md | grep -ci 'withdrawn'` is 0 — it was amended, not withdrawn
    - `grep -c 'FLAGGED CONFLICT against the binding specification' .planning/REQUIREMENTS.md` is 3
    - `grep -c 'FOUND-08' .planning/REQUIREMENTS.md` is ≥ 3 — the requirement, its amendment note, and the tail tables
    - `grep -rn 'auth/telemetry' .planning/REQUIREMENTS.md` returns nothing (exit 1) — no note names a deleted module
    - The treatment-summary "Amended, substance intact" row lists CREATE-01, CREATE-02 and FOUND-08
    - The last-updated line names 37.2 and today's date
    - `git diff --stat .planning/REQUIREMENTS.md` shows a scoped diff, not a whole-file rewrite (unchanged sections stay unchanged)
  </acceptance_criteria>
  <done>Every requirement the sanctioned changes touched carries a dated note saying which treatment it got, the third divergence from the binding specification is written down, and no note points at a module that no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: close out the roadmap entry for the phase</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 37.2 entry — its goal, its five success criteria, its plan list and count; and the Phase 37.1 entry just above it as the shape a completed entry takes)
    - .planning/phases/37.2-simplify-auth-module/37.2-06-SUMMARY.md (the measured final package shape)
    - .planning/phases/37.2-simplify-auth-module/37.2-05-SUMMARY.md (which status codes moved)
  </read_first>
  <action>
    Bring the Phase 37.2 roadmap entry into line with what shipped, by scoped edit only — never rewrite
    the file, and never touch another phase's entry.

    Replace the placeholder plan list with the seven plans and their wave numbers, in the format the
    Phase 37.1 entry uses, and set the plan count to match.

    Refine success criterion 3's wording so it describes what was actually built: challenge issuance is
    its own route taking an operation in the body; a non-issuable operation is one rejection class; the
    query-parameter mode and its classifier module are deleted; and create-user handles completion only.

    Add a short cross-cutting constraints block below the criteria, in the shape Phase 37.1's entry
    uses, recording the three things that must stay true and were checked: the admission matrix suite
    passed with zero edits; the completion precedence suite passed with zero edits; and the only status
    codes that moved are the sanctioned ones — malformed bodies from a hand-chosen rejection to the
    framework's validation rejection, and the already-linked conflict from issuance to completion.

    Record the measured package shape as a fact, not a claim: the file, class and function counts after
    the merges, against the recorded baseline, and note that a test now asserts it.
  </action>
  <verify>
    <automated>grep -c '37.2-0' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '37.2-0' .planning/ROADMAP.md` is ≥ 7 — every plan is listed
    - `sed -n '/Phase 37.2/,/Phase 38/p' .planning/ROADMAP.md | grep -c 'Plans:'` is ≥ 1 and the count matches the number of listed plans
    - `sed -n '/Phase 37.2/,/Phase 38/p' .planning/ROADMAP.md | grep -c 'TBD'` is 0
    - `sed -n '/Phase 37.2/,/Phase 38/p' .planning/ROADMAP.md | grep -c 'test_admission'` is ≥ 1 — the zero-edit constraint is recorded
    - `git diff --stat .planning/ROADMAP.md` shows only the Phase 37.2 section changed
    - Every other phase entry is byte-identical: `git diff .planning/ROADMAP.md | grep -c 'Phase 3[456789]:'` is 0
  </acceptance_criteria>
  <done>The roadmap entry records the seven plans, the refined criterion, the zero-edit constraints and the measured shape — and no other phase entry moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| written record → a later phase's author | A requirement note is the only channel through which this phase's security-relevant decisions reach phases 38–46 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.2-26 | Repudiation | An unrecorded divergence from the binding specification | high | mitigate | The credential decision is written as the third flagged conflict, quoting the rule it diverges from and stating the reasoning, in the same shape as the two existing ones. The standing rule is flag, never silently resolve — and the acceptance criteria count the three conflict notes so a missing one fails. |
| T-37.2-27 | Elevation of privilege | A later phase widening pre-auth admission without noticing the set grew | high | mitigate | CREATE-01's amendment states the set is now two enumerated routes and names the hand-maintained allowlist as its enforcement point, so a phase adding a third has both a written statement and a failing test to walk into. |
| T-37.2-28 | Repudiation | Phases 43–46 inheriting a deleted adapter seam with no notice | medium | mitigate | FOUND-08 is flagged forward to each of those phases, in the same style the existing forward flags use, so each finds the obligation to define its own interface beside its first implementation. |
| T-37.2-29 | Repudiation | A note pointing at a deleted module | medium | mitigate | Both notes naming the rejection-log function by module path are repointed, and the criteria grep for the old path returning nothing. |
| T-37.2-SC | Tampering | package installs | low | accept | This plan touches only markdown; no dependency is involved. |
</threat_model>

<verification>
- `grep -c 'FLAGGED CONFLICT against the binding specification' .planning/REQUIREMENTS.md` is 3
- `grep -rn -E 'auth/telemetry|modesignal|classify_mode_signal|credential_dict|FirebaseConfig' .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns nothing outside the Phase 37.2 entry's own account of what it deleted
- `git diff --stat .planning/` shows only the two files this plan owns
</verification>

<success_criteria>
- The three touched requirements each carry a dated note in the established register saying which
  treatment they got and why
- The divergence from the binding specification is recorded as the third flagged conflict, not resolved
- No planning note names a module or function this phase deleted or moved
- The roadmap entry records the phase's plans, its refined criterion and its measured outcome
</success_criteria>

<output>
Create `.planning/phases/37.2-simplify-auth-module/37.2-07-SUMMARY.md` when done
</output>
