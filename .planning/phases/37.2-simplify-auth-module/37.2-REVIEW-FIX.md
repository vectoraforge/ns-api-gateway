---
phase: 37.2-simplify-auth-module
fixed_at: 2026-09-09T13:22:53Z
review_path: .planning/phases/37.2-simplify-auth-module/37.2-REVIEW.md
iteration: 1
findings_in_scope: 42
fixed: 34
skipped: 8
status: partial
---

# Phase 37.2: Code Review Fix Report

**Fixed at:** 2026-09-09T13:22:53Z
**Source review:** `.planning/phases/37.2-simplify-auth-module/37.2-REVIEW.md` (commit `557ee3e`)
**Iteration:** 1

**Summary:**
- Findings in scope: 42 (4 Critical + 38 Warning; the 33 Info findings were out of scope — no `--all`)
- Fixed: 34
- Skipped: 8

The review was produced by five parallel reviewers over one 149-file scope. The fixes were applied by
five `gsd-code-fixer` agents run **sequentially** over the single shared working tree — one per review
group, in source-before-tests order so later test fixes saw the corrected behaviour. Parallel fixers
were rejected deliberately: concurrent commits into one index would have corrupted each other.

All eight skips are recorded, ratified project decisions or stale premises, not fix failures. Six of
them trace to the same cause: the reviewers read `specs/auth-refactor-phases/` as binding without
consulting `.planning/REQUIREMENTS.md`, where later phases had already amended or struck the clause
being cited.

## Verification (run by the orchestrator, after all five fixers)

| Gate | Baseline at `31da38a` | After fixes |
|---|---|---|
| `.venv/bin/ruff check src tests` | passes clean | **passes clean** |
| `.venv/bin/pytest tests/unit -q -p no:cacheprovider` | 1386 passed | **1449 passed** |
| `.venv/bin/ty check src` | Found 5 diagnostics | **Found 5 diagnostics** |

63 unit cases were added across the pass; none were deleted or weakened. `tests/e2e` (350 cases) and
`tests/schema` (232 cases) need a live database and are outside this gate; both were confirmed to
still import and collect cleanly after the edits.

## Fixer 1 — app core + infrastructure

Gate after every commit: `ruff check src tests` clean; `pytest tests/unit -q` 1386 -> 1398 passed
(12 tests added, 0 deleted); `ty check src` still 5 diagnostics. Every gate ran in the main checkout
(`/home/init/native-speaker/ns-api-gateway`) — no worktree was created, per the submodule rule.

### Fixed

#### CR-01: Duplicate `Authorization` headers are first-wins, not rejected
**Files modified:** `src/nativespeaker/api/app/dependencies.py`, `src/nativespeaker/api/auth/jwt_verifier.py`, `tests/unit/test_auth_security.py`, `tests/unit/test_identity_accessors.py`, `tests/unit/test_google_play_notifications.py`
**Commit:** 0e3b2b5
**Applied fix:** The finding holds. I read the clause myself: `SHARED-INVARIANTS.md:19` says "Envoy's
JWT filter is defense-in-depth only; no backend correctness depends on it", and `01-foundation.md:45`
names two or more `Authorization` field instances as `duplicate_authorization` — so Envoy's folding is
not a defence the backend may rely on. Root cause: `HTTPBearer` reads the merged header view, which
returns the first value and hides the rest, so the multiplicity check the deleted `extract_bearer`
performed disappeared with it. `get_identity` now counts the raw `authorization` fields in
`request.scope["headers"]` before any value is read, and raises
`InvalidExternalJwt(bounded_reason=BoundedReason.duplicate_authorization)`; the member was restored to
`BoundedReason` (it is one of the eight the specs close over, so this does not widen the vocabulary).
Added the missing two-header-line case, parametrized over both orders — the valid-credential-first
order is the one that answered 200 before. Two tests pinned the old behaviour and were corrected, not
weakened: `test_a_duplicate_authorization_field_is_resolved_by_taking_the_first` now asserts 401, and
`test_bounded_reason_still_has_exactly_five_members` now asserts the enum stays inside the specs' eight
closed values instead of counting to five.

#### WR-01: A startup failure after `build_admin_apps` poisons every later boot, and leaks both httpx clients
**Files modified:** `src/nativespeaker/api/app/lifespan.py`
**Commit:** 0a05d48
**Applied fix:** Root cause: the teardown sat after `yield` with no `try`/`finally`, so any raise
between `build_admin_apps` and `yield` skipped it, leaving the process-global named Firebase apps
registered (a repeat `initialize_app` then raises) and both httpx clients open. The whole startup body
plus the `yield` is now inside `try`, and every disposal moved to `finally`, each guarded by a local
pre-initialised to `None`/`{}` so a boot that failed part-way still reaches every object it did build.
A lifespan cancellation at `yield` now also runs the teardown.

#### WR-02: `config.yaml` states an invariant that is provably false
**Files modified:** `config/config.yaml`
**Commit:** a355dd4
**Applied fix:** I reproduced the claim before deciding, and the review is half right: with
`APP_STORE_PRODUCTS='{"evil.product":"paid"}'` set, `app_store.products` gained the extra entitled
product — but a variable naming an entry the YAML already declares does NOT win
(`APP_STORE_PRODUCTS='{"com.nativespeaker.subscription.monthly":"free"}'` left it `paid`), and neither
does a nested scalar (`RESILIENCE_POOL_SIZE=99` left it 5). The real rule is per key, not per table:
the deep merge lets an environment variable ADD a key the file does not declare. I corrected the comment
to say exactly that and to name the consequence for the two product maps, and did not add a
`model_validator` guard: an operator who can set an environment variable on the pod can equally edit
the mounted config or the image, so a boot-failure mode buys nothing against this project's threat
model (AGENTS.md: no over-engineering).

#### WR-03: The only backend 429 the service raises carries no `Retry-After`
**Files modified:** `src/nativespeaker/api/errors.py`, `src/nativespeaker/api/services/quota.py`, `tests/unit/test_quota_resolver.py`
**Commit:** 9df0eac
**Applied fix:** `SHARED-INVARIANTS` § Errors requires the header on a 429 where computable, and
`QuotaExceededError` inherited `extra_headers()` returning an always-`None` `_headers`.
`QuotaExceededError` now takes `retry_after_seconds` and emits the header; `seconds_until_rollover`
derives the wait from the request's captured `evaluated_at` (never a clock) to the UTC month boundary
the allowance rolls over on, rounded up and floored at one second. Both refusal branches
(`no_effective_grant` and `allowance_exhausted`) pass the same value, because the anti-oracle clause
forbids a client distinguishing which branch fired — and the rollover is a valid floor for both, since
neither an absent grant nor a spent one changes before the period does.

#### WR-04: A client-attributable provider rejection trips the circuit breaker for every user
**Files modified:** `src/nativespeaker/api/resilience.py`, `tests/unit/test_resilience_retry.py`
**Commit:** 4ff7313
**Applied fix:** `record_failure()` ran before the transient/permanent split, so a content-policy 400
caused by one user's phrase incremented the same counter an outage does — five in a row with no success
between them opened the breaker and answered 503 to everyone for a minute. The call moved inside the
transient arm; a permanent rejection now neither trips nor clears the breaker. Three tests asserted the
old accounting and were updated to the corrected expectation (the permanent case now records 0, the
"once per failed provider attempt" invariant became "every attempt still consults the breaker"), and the
`CircuitOpenError` case now trips the threshold with a transient failure, which is the only thing that
can trip it. Added `test_a_permanent_rejection_never_trips_the_breaker` with the measurement-fires
control: five permanent rejections leave the breaker closed, two transient ones open it.

#### WR-05: Every 4xx writes an ERROR access line
**Files modified:** `src/nativespeaker/api/logs.py`, `src/nativespeaker/api/app/error_handlers.py`, `tests/unit/test_logging.py`, `tests/unit/test_exception_handlers.py`
**Commit:** b3b854b
**Applied fix:** The access line split at 400, which put a 401 probe, a 404 typo and a 429 at the level
reserved for a whole-product outage — undoing the `log_level = None` every client-visible rejection
class carries. `_log_request` now splits at the 5xx boundary with 4xx at WARNING, and
`validation_error_handler`'s own line dropped from ERROR to WARNING for the same reason. Added a 404
case asserting WARNING and a 422 case asserting the line lands on `logger.warning` and never on
`logger.error`; `TestARejectedBodyValueNeverReachesTheLog` now spies the warning binding, so the
secret-redaction assertions still observe the real call.

#### WR-06: The chart renders `image: ":"` when the deployer forgets `--set`
**Files modified:** `k8s/templates/deployment.yaml`, `k8s/values.yaml`
**Commit:** c8dc94d
**Applied fix:** Applied the chart's own precedent from `security-policy.yaml:29`: both halves of the
image reference are now `required`, so an install missing `--set image.repository=...` / `--set
image.tag=...` fails at render with a message naming the value instead of shipping `image: ":"`. Helm
is not installed here, so I verified by substituting the two actions and parsing the result: the line
yields `image: "ghcr.io/ns/api:1.2.3"`. The `values.yaml` entries carry the same note the `jwt.issuer`
entry does.

### Skipped

None.



## Fixer 2 — auth adapters + routers + schemas

Gate at the end of the run: `ruff check src tests` clean, `pytest tests/unit -q` **1414 passed**
(baseline 1398, +16 from added cases, none deleted or weakened), `ty check src` **5 diagnostics**
(unchanged). Every gate was run in the main checkout — no worktree was created, per the repository
shape note, so the numbers are reproducible from the tree as it stands.

### Fixed

#### WR-20: an Apple notification with no `data.status` becomes a permanent 500 retry loop
**Files modified:** `src/nativespeaker/api/auth/app_store.py`, `tests/unit/test_app_store_notifications.py`
**Commit:** bed5175
**Applied fix:** `verify` now branches on `data.rawStatus is None` before consulting
`_APPLE_STATUSES`, and returns the "verified and unwritable" value line 98 already used —
`_crossed(payload, None, None, status=expired, tier_id=None)` — so `SubscriptionsService.ingest`
takes its existing `external_id is None` early return, writes nothing, and the route answers 200.
A status value that is *present but outside Apple's enum* still raises `UnknownStoreSubscriptionStatus`.

Root cause: `_APPLE_STATUSES.get(data.status)` conflated *absent* with *unrecognized*. The record at
`.planning/REQUIREMENTS.md:402` (OQ-2) kept the 500 leaf only because the claim "`data.status` is
always present" could be neither confirmed nor refuted — Apple's documentation page returned 404
during Phase 44 research — and stated that "the first real 500 on it is authoritative over this
paragraph". I supplied the observation the record lacked, from the vendored library rather than from
the missing page:

- `appstoreserverlibrary.models.Data.status` docstring: *"The status of an auto-renewable
  subscription as of the signedDate"* — subscription-scoped, and `Optional[Status]`.
- `NotificationTypeV2` contains `CONSUMPTION_REQUEST`, `ONE_TIME_CHARGE`, `EXTERNAL_PURCHASE_TOKEN`
  and `RESCIND_CONSENT`, which carry a signed transaction but name no auto-renewable subscription.
- `Data(rawStatus=99).status is None` and `Data().rawStatus is None`, so `rawStatus` separates the
  two cases exactly.

I deliberately did **not** take the review's first snippet, which passes the `transaction` through
with `status=expired`: `core.subscriptions.status` is NOT NULL, and inventing `expired` for a
`CONSUMPTION_REQUEST` would end a live subscriber's grant. The review's own caveat says as much; the
transaction is dropped with the status.

Test: `test_a_subscription_payload_with_no_status_raises_rather_than_deriving_one` asserted the
defect, so it was rewritten rather than deleted. The replacement asserts the acknowledged shape for
all three statusless types, and the loud arm is re-pinned at `status=99` so the 500 leaf keeps a
named executed case.

#### WR-22: `BoundedReason` implements five of the eight rejection reasons the spec closes over
**Files modified:** `src/nativespeaker/api/auth/jwt_verifier.py`, `src/nativespeaker/api/app/dependencies.py`, `src/nativespeaker/api/errors.py`, `tests/unit/test_jwt_security.py`, `tests/unit/test_identity_accessors.py`, `tests/unit/test_exception_handlers.py`
**Commit:** c41fb3a
**Applied fix:** Added `missing_token` and `malformed`, completing the closed set
`11-sign-out-all.md:80` names (the enum held six after fixer 1's CR-01 restored
`duplicate_authorization`; I re-read it before acting, as instructed). Three parts:

1. `dependencies.get_identity` raises `InvalidExternalJwt(bounded_reason=BoundedReason.missing_token)`
   where it raised `bounded_reason=None`.
2. `bounded_reason_for` returns `malformed` for a `DecodeError` that is not an
   `InvalidSignatureError`. `InvalidSignatureError` subclasses `DecodeError` and is a real forgery;
   `PyJWKClientError` is not a `DecodeError` at all, so an unknown key id stays `bad_signature`.
   Verified by MRO rather than assumed.
3. `InvalidExternalJwt.bounded_reason` is narrowed from `BoundedReason | None` to `BoundedReason`.
   The null label was the defect the finding names — a population the spike alert cannot name — and
   leaving the type optional would let it come back.

Root cause of a near-regression I had to handle: `extra_headers()` used `bounded_reason is None` to
decide between the bare RFC 6750 §3.1 challenge and `error="invalid_token"`. It now keys on
`is BoundedReason.missing_token`, so a request that presented no credential still gets the bare
`Bearer` and a duplicated or garbage one still gets `invalid_token`.

Two assertions moved because the classification they pinned was the defect:
`test_rejects_empty_string` and the `not.a.jwt` case in `test_identity_accessors` now expect
`malformed`. `test_rejects_missing_exp` correctly still expects `bad_signature`
(`MissingRequiredClaimError` is not a `DecodeError`).

#### WR-23: a JWKS-endpoint outage is recorded as `bad_signature`
**Files modified:** `src/nativespeaker/api/auth/jwt_verifier.py`, `tests/unit/test_jwks_offload.py`
**Commit:** 9c7d29d
**Applied fix:** The `except PyJWKClientError` arm now tests `isinstance(exc, PyJWKClientConnectionError)`
first and emits one operator line, `jwks_endpoint_unreachable`, before returning. The module gained a
`structlog` logger, consistent with `firebase.py` and `google_play.py`; the event name alone reaches
the log, never the exception text, which embeds the JWKS URL.

I did **not** add a `jwks_unavailable` member. The review is right that it would contradict the
eight-value closed set quoted under WR-22, so the client answer and the metric label are unchanged
and only the operator log gains the cause.

The `elif` preserves the existing rule that a connection error records no `kid` — caching an outage
would prolong it fleet-wide. Two new cases: the outage logs exactly once while the reason stays
`bad_signature`, plus the control that a bogus key id over a healthy endpoint logs nothing and still
reaches for the keys.

#### WR-25: an empty or oversized Pub/Sub `data` field is a permanent 422 redelivery loop
**Files modified:** `src/nativespeaker/api/schemas/webhooks.py`, `src/nativespeaker/api/auth/google_play.py`, `tests/unit/test_models.py`
**Commit:** ffe3092
**Applied fix:** `PubSubPushMessage.data` drops its `min_length`/`max_length`; the same bound moved
into `developer_notification_from`, which logs `google_play_message_out_of_range` and returns `None`
so the route answers 200. The value of `PUBSUB_DATA_LIMIT` is unchanged.

Root cause: the bound sat on a pydantic field, and FastAPI validates the body model before any
dependency runs, so an out-of-range body was a 422. Cloud Pub/Sub acknowledges only 2xx and
redelivers everything else, so that message backs the subscription up forever — while the
*undecodable* body one layer down was already handled correctly at 200
(`google_play.py`: *"A body that does not parse never will, so answering 200 is what stops the
redelivery."*). Apple's `signedPayload` bound is untouched: this finding is scoped to Pub/Sub.

`_BOUNDED_WEBHOOK_FIELDS` in `test_models.py` lost its Pub/Sub row, and three cases were added in its
place: the model now accepts an out-of-range body, the decoder drops it, and a body at the bound
still decodes (the control against an off-by-one that would drop every genuine RTDN at the ceiling).

#### WR-26: `read()` skips the path-segment guard `read_for_restore()` applies
**Files modified:** `src/nativespeaker/api/auth/google_play.py`, `tests/unit/test_google_play_notifications.py`
**Commit:** 7ac0f05
**Applied fix:** `read` now applies `_names_one_path_segment(purchase_token)` before `_get`, logging
`google_play_unusable_purchase_token` and returning `None` — the acknowledged answer this route
needs, not a raise.

Root cause: both entry points reach the same `_get`, which interpolates the token into the URL path,
and only one guarded it. `_get`'s own docstring admits the asymmetry. The token is the untrusted
value here (it comes straight off the RTDN body); I deliberately did **not** add the review's
`package_name` half, because `dependencies.verify_google_play_notification` already refuses a
delivery whose `packageName` differs from the configured one, so on this path it is server-controlled
and returning `None` for it would silently acknowledge an operator misconfiguration.

Cases added for `""`, `"."`, `".."`, `"..."` asserting no request is sent, plus the control that a
real token still reaches Play and logs nothing.

#### WR-27: the Play adapter reads the clock per call instead of the request's captured instant
**Files modified:** `src/nativespeaker/api/auth/google_play.py`, `src/nativespeaker/api/app/lifespan.py`, `src/nativespeaker/api/app/dependencies.py`, `src/nativespeaker/api/services/restore.py`, `tests/unit/test_google_play_notifications.py`, `tests/unit/test_restore_proof.py`, `tests/e2e/conftest.py`
**Commits:** 991a414, d3b2a68 (e2e seam)
**Applied fix:** `evaluated_at_source` is gone from `PlayDeveloperSubscriptions.__init__` and from
the `PlaySubscriptionSource` protocol. `read` and `read_for_restore` take `evaluated_at: datetime`.
`verify_google_play_notification` forwards its own `Depends(get_evaluated_at)`, and `RestoreService`
forwards `self.evaluated_at`.

Root cause, confirmed against `dependencies.py`'s own note on `get_linked_identity`: FastAPI's
per-request cache only covers solver-resolved dependencies, so calling `get_evaluated_at` directly
from inside the class produced a *second* instant. `_status_for`'s entitlement decision was therefore
made at a different moment from the one the grant writer computes `ends_at` against, and a
`SUBSCRIPTION_STATE_CANCELED` term falling between the two commits an active grant outside its own
term — the state `ActiveGrantOutsideItsTerm` exists for. Against SHARED-INVARIANTS § Grants and
evaluation time: *"Derive every time-dependent value from ONE captured evaluation time or one
consistent snapshot per request."*

Two cases pin it: a canceled term read on both sides of its own expiry follows the instant passed in,
and the constructor signature is read off `inspect.signature` so a reintroduced clock source fails.

#### WR-29: `sign_out_all` can answer `401 auth_required` for a post-barrier condition
**Files modified:** `src/nativespeaker/api/auth/firebase.py`, `tests/unit/test_firebase_adapter.py`, `tests/unit/test_firebase_retry.py`, `tests/e2e/test_sign_out_all.py`
**Commits:** 1ad377d, d3b2a68 (e2e case)
**Applied fix:** `FirebaseAdminLookup._revoke` raises `RevocationUnconfirmed(stage="subject_absent")`
where it raised `UserNotFound(stage="token_revocation")`, and logs at ERROR rather than INFO — an
`active` identity row naming a uid Firebase does not have is an integrity break, not routine.

I fixed the adapter rather than the route, which is where the review put it. `revoke_with_retry` has
exactly one caller, and `_revoke` is revocation-only, so this is the line where the wrong class is
minted; a catch in the router would leave the adapter free to hand a 401 to the next caller. The
sibling `_read` keeps `UserNotFound(stage="provider_lookup")`, which spec 11 *does* map to
`auth_required` for the provider-lookup routes — a control case now pins that.

The answer stays definitive and spends exactly one attempt: `RevocationUnconfirmed` is not a
`RetryableLookupError`. Against spec 11: *"`auth_required` — **barrier token-acceptance failure**"*,
and *"on any non-success other than `account_unavailable`, keep the credential and report sign-out
unconfirmed/retryable"*. SIGNOUT-02's fail-closed half still binds and is unaffected — the answer is
503, never 204.

`tests/e2e/test_sign_out_all.py::test_an_account_the_provider_does_not_have_answers_401` asserted the
defect; it was rewritten to the correct expectation (503, no `WWW-Authenticate`, still one attempt)
rather than deleted. The e2e suite needs a live database and is outside this run's gate, so it was
checked by compile only.

### Skipped

#### CR-10: `/auth/restore-subscription` admits an anonymous account as the restore destination
**Reason:** A **recorded, ratified product decision**, not an oversight — and the review's harm
argument is additionally stale against the shipped code. Review group 3 is right and review group 2
is wrong. `.planning/REQUIREMENTS.md:489`:

> **FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 45 D-03,
> 2026-09-08). Anonymous accounts may restore.** `10-restore-subscription.md:50` requires that
> *"Destination must be an active, **registered** account"*, and gives the refusal its own result
> value, `restore_destination_anonymous`. `:68` restates the same check inside the locked
> transaction. **What shipped:** the route sits behind `get_linked_identity` and reads no provider
> column at all. `restore_destination_anonymous` is not built. **The ground, which is a product
> judgment and not a technical limit:** under D-10 one receipt serves at most two accounts in a UTC
> calendar month. That is an accepted loss on a subscription under $5 a month. A stolen proof is a
> risk of restore in any form, and admitting anonymous accounts adds none. **Reversible:** the check
> is one read of the stored provider and one refusal, ahead of the store call.

`REQUIREMENTS.md:499` (Phase 45 D-11) lists `restore_destination_anonymous` by name among eight
brief-named result codes that have no trigger, with the decision that removed each — and states that
none is added to the shared registry *"because § 'Errors' says to add a class only where the
specification names a live one."* Adding `RestoreDestinationAnonymous`, as CR-10 asks, is what that
clause forbids.

I read `10-restore-subscription.md` myself: the four clauses the review quotes are real and verbatim
(`:36`, `:50`, `:68`, `:94`). The spec does say this. But the project's recorded process for a
divergence is a dated flagged conflict under the requirement, and this one has it, with its ground
and its reversibility rated. It is a user-level product call made at a Phase 45 checkpoint, not a
reviewer-discoverable defect, and reversing it in a code-review pass would overturn that decision
silently.

The review's stated *consequence* is also no longer true of this code. It rests on
`core.subscriptions.restore_bound_user_id` being set for life ("moving the binding is manual operator
repair only"). Phase 45 D-10 ships cross-account transfer under a once-per-UTC-month cap and
`restore_bound_user_id` is not built (`grep -rn restore_bound_user_id src/` returns nothing; the
route's refusal for the cap is `restore_transfer_rejected`). So the permanent, operator-only-
repairable binding the finding calls "entitlement loss on a paid path" does not exist. To reverse
D-03, amend `10-restore-subscription.md` or re-open the decision with the user — not a fixer's call.

#### WR-21: no auth route writes the mandated `audit.auth_events` row
**Reason:** The obligation was **removed at its source**, not diverged from, and the finding's own
Fix section asks for a "named deferral" that already exists. `SHARED-INVARIANTS.md` today has no
§ "Audit" and zero occurrences of `auth_events` (verified: its ten section headings are Identity and
ownership, The barrier, Wire contract, Tokens and sessions, Fail-closed defaults, Locks and
transactions, Grants and evaluation time, Errors, Rate limits, Global deletions).
`.planning/REQUIREMENTS.md:32`:

> **Amended again by Phase 38 (2026-09-01) for the milestone's audit removal.** […] **option (b)** —
> no durable `audit.auth_events` row is rebuilt for `POST /auth/sync`, and no success event is added,
> because `RequestLoggingMiddleware` already writes one `request` line per attempt and the shared
> error handler already writes one WARNING per rejection (D-01/D-02). The obligation was removed at
> its source rather than diverged from: plan 38-04 struck § "Audit" in full from
> `SHARED-INVARIANTS.md` along with the § "Errors" clause naming `core.auth_event_result` […] The
> same stroke settles the **audit half** of the three siblings Phase 37.1 flagged forward on the same
> mechanism: **SIGNOUT-02** (46), **APPLEHOOK-02** (43) and **PLAYHOOK-03** (44) […]

Phase 45 D-11 closes the restore half on the same terms: *"restore is not challenge-bearing and
writes no audit row"*. Building an audit table, its writer and an actor-subject HMAC would reinstate
a subsystem the project deliberately deleted, across all eight routes — the largest possible
over-engineering in a pre-launch app whose brief says not to. Review group 1 reached the same
conclusion and declined to re-report it.

The finding's secondary observation is also answered by the same record: the Phase 40 forward flag on
the `restore_subscription` operation label is *"ANSWERED AND CLOSED: no label is needed"*, so
`AuthOperation`'s four values are correct and `issue_challenge`'s gate stays as it is. Not a fix; no
change made.

#### WR-24: the Google replay key contradicts spec 09 and persists the raw purchase token
**Reason:** The finding's own Fix is *"Amend `specs/auth-refactor-phases/09-webhook-google-play-rtdn.md`"*
— a specification amendment, which is out of scope for a code-review fixer and explicitly not
something this pass may do (the specs are the authority I am measured against). Both halves are
already recorded as flagged conflicts. `.planning/REQUIREMENTS.md:44`:

> **(4) `core.subscriptions.external_id` holds the raw Google purchase token (D-10)**, against the
> DELETIONS line at `:53` — recorded with its ground, that the token is the only handle
> `purchases.subscriptionsv2.get` accepts, so the same line forbids the mechanism it mandates two
> clauses earlier, and rated **one-way** because rows are keyed on it; and **(5) the replay key is
> the payload-derived composite `google_play:{purchaseToken}:{eventTimeMillis}:{notificationType}`
> rather than the Pub/Sub message ID (OQ-4)**, against `:24` and `:41` — **decided by the user at
> plan 44-01's blocking checkpoint**, rated one-way, and chosen because it dedupes a redelivery *and*
> a Play republish without resting on any Google delivery-id stability guarantee, which Google's
> reference does not state.

The review states it is not asking for a revert. The remaining suggestion — hashing the token
component — is, in its own words, *"a schema-visible change and belongs in the same amendment"*, and
the composite key is already rated one-way because live rows are keyed on it. No code change is
correct here.

#### WR-28: the two callback routes answer the shared client-visible error classes specs 08/09 forbid
**Reason:** Both halves are recorded, ratified divergences, and the finding's own Fix demands an
either/or where one branch is a spec amendment and the other reverses a decision made twice.
`.planning/REQUIREMENTS.md:44` counts them against spec 09's own unamended text:

> **Three are Phase 43's three, counted again against this brief's own lines, because the brief
> states them again and no passage is amended:** **(1) the always-registered router answering 503
> while unconfigured (D-18, inheriting 43 D-02)**, against `:30` and `:49`; **(2) the shared
> one-field refusal body (D-20, inheriting 43 D-04)**, against `:7`, `:23`, `:37` and `:45` […]

D-20 is the error-envelope half; D-18 is the route-registration half — exactly the two things WR-28
reports, each already flagged against the clause it violates and each inherited from Phase 43 rather
than newly introduced. `lifespan.py:124,137`'s comment (*"Set unconditionally, so the route set is
the same in every environment"*) is that decision in the code. The review rates the impact as low
itself: Apple and Google read the status code and ignore the body, and the 503 is retryable, which is
the right shape for an unconfigured deployment. Changing it would replace a ratified decision with
the other half of an either/or the reviewer did not pick.



## Fixer 3 — services + crud + tables

Gate after every commit: `.venv/bin/ruff check src tests` clean, `.venv/bin/pytest tests/unit -q`
green (1386 at the recorded baseline → 1444 now; +58 from fixers 1/2 and my 24 new cases, none
deleted or weakened), `.venv/bin/ty check src` still 5 diagnostics. Verification ran in the main
checkout at `/home/init/native-speaker/ns-api-gateway`, not in a worktree (submodule; worktrees
disabled per the run brief), so the numbers are reproducible from the tree as it stands.

### Fixed

#### CR-20: A store notification with no term end mints a permanent paid grant
**Files modified:** `src/nativespeaker/api/auth/store_notifications.py`,
`src/nativespeaker/api/services/subscriptions.py`, `src/nativespeaker/api/services/restore.py`,
`tests/unit/test_subscription_attribution.py`, `tests/unit/test_auth_package_shape.py`
**Commit:** 2ed86ca
**Applied fix:** The grace-or-paid term choice was spelled twice — once in `RestoreService.restore`,
once inline in `SubscriptionsService.ingest`'s grant write — and only the restore copy was guarded.
Rather than write a second parallel rule, the choice is now one function, `term_end_for(status, term)`
in `auth/store_notifications.py`, where both payload dataclasses live, and both services read it.
`ingest` then refuses before any write: an entitled status (`active`/`grace_period`) whose term end is
`None`, or is no later than the term's start, logs `store_notification_without_term` and raises
`InternalError`, mirroring the existing `store_notification_without_tier` arm two branches up. The
root cause is that an absent term reached `write_subscription_grant` as `ends_at=None`, which
`_effective_grants_statement` (`crud/grants.py:33-34`) reads as unbounded — a `paid` grant no store
event can ever end. The `<= starts_at` half also stops the row's own
`CHECK (ends_at IS NULL OR ends_at > starts_at)` firing as a non-unique `IntegrityError` that the
writer re-raises as a bare 500 on every redelivery.
The guard sits **after** the replay and out-of-order branches, deliberately: a stale or already-recorded
payload must not earn the 500, exactly as the existing comment on the superseded branch argues.
Six unit cases added, including two controls (the same shape carrying a term still writes it; a
non-entitled status still writes its termless end, because only an entitled term is an entitlement).
`tests/unit/test_auth_package_shape.py`'s recorded shape moved 64 → 65 functions, which is the
workflow that file's own docstring prescribes.

#### WR-40: `store_signed_at` is never advanced on the `replayed` branch
**Files modified:** `src/nativespeaker/api/crud/subscriptions.py`,
`tests/unit/test_subscription_store_clock.py`
**Commit:** 20df3d5
**Applied fix:** `upsert_subscription` now advances `store_signed_at` (and `updated_at`) whenever the
delivery carries a strictly newer store clock, before the `(tier_id, status, user_id)` comparison
decides `applied` vs `replayed`; the outcome value is unchanged, only the clock moves. The root cause
is that the out-of-order guard in `ingest` compares `notification.signed_at` against
`stored.store_signed_at`, so a no-state-change delivery carrying a newer clock left the stored clock
behind and a later, genuinely stale delivery was not detected as superseded. Six unit cases added over
a stub session, including the two controls (the replay is still reported as a replay; an absent clock
still clears nothing) and the case that an older clock never moves the stored one back.

#### WR-41: `flip_provider` reports a broken invariant as `provider_account_already_linked`
**Files modified:** `src/nativespeaker/api/crud/identities.py`, `tests/unit/test_identity_flip.py`
**Commit:** 11dc01e
**Applied fix:** The `except IntegrityError` arm now gates on `is_unique_violation` and re-raises
anything else, with the same comment the other eight writers in the package carry. The root cause is
that a `CHECK` or foreign-key failure — divergent stored state — was answered to the client as a 409
"provider account already linked". New test file drives the real crud method over a stub session whose
flush raises: `23505` is the reservation, `23502`/`23503`/`23514` propagate, an `IntegrityError`
carrying no DBAPI exception propagates (fail closed), and one control confirms the ordinary path.

#### WR-42: the flip overwrites a non-NULL `registered_at`
**Files modified:** `src/nativespeaker/api/crud/identities.py`, `tests/unit/test_identity_flip.py`
**Commit:** 689e92e
**Applied fix:** `user.registered_at = evaluated_at` is now guarded by `if user.registered_at is None`,
matching the `email` copy three lines below. `req~users-upgrade-step-07~1`
(`specs/auth-refactor/02-user-creation-and-anonymous-continuity.md:538`) reads: *"set `registered_at`
if it is `NULL`"*, and `oft-conventions.md:122` names the pair as one fold point. Only the `email`
half had been implemented, and the recorded waiver D-13 covers `email` alone. Because the flip doubles
as the idempotent repair path for a crash-stranded upgrade, a repair restamped a registration instant
that already existed. Four cases added, including the control that `updated_at` still moves on the
repair, so the "nothing was written" case is not vacuous.

### Skipped

#### WR-43: a subscription write silently strands another entitled subscription with no grant
**Reason:** The behaviour the finding calls a defect is a recorded, ratified and separately-verified
decision, and the finding rests on that divergence. `45-08-PLAN.md:245` registers it as the mitigation
for a named threat: *"T-45-08-02 | Tampering | `ix_access_grants_one_active_per_user` | The
`grant.user_id == user_id` clause keeps the destination's whole set, so the index invariant is still
satisfied by construction rather than by a caught 23505."* `.planning/STATE.md:773` restates it: *"The
two clauses are the two things a subscription write is entitled to end: everything the destination
holds, because ix_access_grants_one_active_per_user allows one; and the old owner row for this
subscription, because D-10 expires it in the same transaction (45-08, closes VERIFICATION truth 6 /
CR-03)."* Two schema classes pin it on real PostgreSQL, one of them as its whole subject:
`tests/schema/test_restore_race.py::TestTheDestinationStillLosesEverythingItHeld`
(`test_the_destinations_grant_for_another_subscription_is_ended_by_the_move`, *"this is what stops the
narrowing going too far"*) and `TestAMoveTakesOnlyTheGrantForTheSubscriptionItMoves`, whose fixture
restores two subscriptions onto one account.
Honest disclosure: I implemented the reviewer's fix first (commit **d5dcd37**), then found T-45-08-02
and removed the guard in a forward commit (**9f3e3c5**) rather than rewriting history. The net source
effect of that pair is zero; `crud/subscriptions.py` differs from the reviewed state only by WR-40.
What survives from the attempt is `tests/unit/test_subscription_grant_write.py`, ten cases that pin
the ratified supersession set at the unit level, where only schema cases held it before.
The reviewer's underlying observation is still true and is *not* closed by this skip: after a write for
subscription B, subscription A can be left `status='active'` with no grant row, which
`req~restore-same-account-mutation-02-settle-grant-status~1` calls data corruption, and spec 10 line 77c
mandates `restore_destination_already_entitled` — *"Restore never expires a different active grant to
make room, with no grant source exempt and none outranking another"*. That class does not exist in the
closed error registry, and the webhook path has no spec text for a second store subscription at all.
Resolving it means either amending the phase-45 record or building the class, both larger than a
code-review fix and both needing the tests/schema cases moved with them.

#### WR-44: restore skips the attribution check entirely when the proof carries no store token
**Reason:** The proposed refusal would delete the capability D-10 exists for, which `PROJECT.md:227`
ratifies as a flagged conflict: *"Restore ships a cross-account **move** capped at one per UTC calendar
month, where the brief forbids any transfer (Phase 45 D-10) | A customer who reinstalls under a new
account must be able to recover a paid subscription; a monthly cap bounds the abuse the brief was
guarding against."* Traced through the code, the move branch is reachable in exactly two shapes, and the
refusal closes the one D-10 names. `PurchasesDB.resolve_user` maps the attribution token to the user
whose `core.store_purchase_tokens` row carries it, and every account is minted one token per store at
creation (`insert_account`), so a token is either absent or resolves. A proof carrying the *original*
buyer's token therefore already fails `attributed != destination` and raises `RestoreAttributionMismatch`
before the move — which is precisely the reinstall-under-a-new-account case. What is left after refusing
token-less moves is only "move back to the account whose own token the proof carries", i.e. D-10's
stated purpose becomes unreachable. Spec 10 forbids all cross-account transfer anyway (*"different linked
account → reject `store_transaction_already_linked`. Cross-account entitlement transfer is **never**
performed"*), so the correct resolution is a decision about D-10, not a narrowing inside it.
The residual half of the finding is real and unaddressed: nothing records that attribution was
unverifiable when a move runs. A one-line `logger.warning("restore_move_without_attribution",
provider=str(proof.provider))` beside the existing `store_notification_owner_moved` line would close it
with no behaviour change. I left it out rather than smuggle a partial fix in under a skipped finding —
it is a small, safe follow-up for whoever rules on D-10.

### Handoff to fixer 5

One schema case is invalidated by my CR-20 source fix, and it is the CR-40 case you already own:

- `tests/schema/test_subscription_ingestion.py:691-700`,
  `TestAGoogleGracePeriodGrantIsEffective::test_an_absent_grace_end_writes_a_grant_carrying_no_end_at_all_control`.
  `SubscriptionsService.ingest` now raises `InternalError` for `(grace_period, grace_period_expires_at=None)`
  **before any write**, so `await buyer.deliver(...)` itself raises. The reviewer's suggested replacement
  body (`assert await buyer.grants() == []`) will error rather than fail, because the raise happens inside
  `deliver`. The case that matches the shipped behaviour is
  `with pytest.raises(InternalError): await buyer.deliver(...)` followed by
  `assert await buyer.counts() == (0, 0, 0, 0)` — no subscription row, no purchase row, no event row and no
  grant, because the guard sits ahead of `upsert_subscription`. The route answer is 500, not 200, so the
  store resends; that is the fail-closed reading CR-40 argues for, one step stricter than its draft.

Checked and **not** affected, so leave them alone:
- `test_a_grace_window_already_closed_leaves_no_effective_grant_control` (same file, ~line 701): the
  helper sets `purchased_at = evaluated_at - _A_MONTH`, so its already-past term end is still later than
  `starts_at` and the new `term_ends_at <= starts_at` half does not fire.
- `tests/e2e/test_restore_subscription.py:323-341`: the restore guard is byte-equivalent to before, only
  re-expressed through the shared `term_end_for`.
- Everything under `tests/schema/test_restore_race.py`: WR-43 was skipped and `crud/subscriptions.py`'s
  supersession set is unchanged.
- No schema or e2e case reads `store_signed_at` on a replayed delivery (WR-40), and none exercises the
  flip's `registered_at`/`IntegrityError` arms (WR-41, WR-42).



## Fixer 4 — unit tests

Gates after every commit: `ruff check src tests` clean, `pytest tests/unit` **1449 passed** (up from
1444 — five cases added, none deleted or weakened), `ty check src` **5 diagnostics** (unchanged).

### Fixed

#### WR-60: the conftest verifier restates production's decode rules under a docstring claiming it cannot
**Files modified:** `src/nativespeaker/api/auth/jwt_verifier.py`, `tests/unit/conftest.py`, `tests/unit/test_jwt_security.py`, `tests/unit/test_google_play_notifications.py`
**Commit:** f63cdda (plus 7cf8926, typing follow-up)
**Applied fix:** Root cause was the copy, so the copy is gone. Production now declares
`DECODE_ALGORITHMS`, `DEFAULT_LEEWAY` and `DECODE_OPTIONS` at module level and passes those objects
to `jwt.decode`; `_FixedKeyVerifier` imports all three instead of restating them, and gained the bare
`except Exception -> bad_signature` clause that makes production's "never raises" structural. Four
AUTH-07-adjacent claims now also run against the real class: `TestProductionVerifier` gained
alg:none, accept-inside-leeway, reject-past-leeway and require-`exp` cases, and its `jwks_client` /
`real_verifier` fixtures were hoisted to module scope to carry them. Drift was verified empirically,
not assumed — widening `DEFAULT_LEEWAY` to 300 fails two cases, dropping `exp` from `require` fails
three. Two drifts PyJWT itself blocks (`alg:none` and HS256-over-a-PEM both raise inside PyJWT even
when the algorithm is listed), so a `DECODE_ALGORITHMS == ["RS256"]` case pins the list itself.
`_require_list()` in `test_google_play_notifications.py` had read the `require` literal out of the
verifier's source with `ast`; it now reads the value the decode call is handed and asserts by AST
that the call is the one carrying it — a stronger measurement, and it no longer breaks when the
constant moves.

#### WR-63: `test_the_owner_is_resolved_before_the_first_write` asserts no ordering at all
**Files modified:** `tests/unit/test_subscription_attribution.py`
**Commit:** 889edc2
**Applied fix:** `_RecordingSubscriptions` owns a `timeline` list that `_RecordingPurchases` is
handed, so the two stands-in record onto one sequence; `resolve_user`, `lock_grants`,
`upsert_subscription`, `insert_purchase`, `append_event` and `write_subscription_grant` each append.
The case now asserts `resolve_user` is first and precedes `lock_grants`, which is the D-16 claim its
name makes. Confirmed the instrument records the real order:
`['resolve_user', 'lock_grants', 'upsert_subscription', 'insert_purchase', 'append_event', 'write_subscription_grant']`.

#### WR-64: two assertions can only fail by editing the test file that contains them
**Files modified:** `tests/unit/test_adapter_interfaces.py`, `tests/unit/test_app_wiring.py`
**Commit:** 0d40648
**Applied fix:** `test_the_seam_declares_exactly_one_value_type` now measures the module —
`{node.name for node in _classes()} - {p.__name__ for p in PROTOCOLS} == {"VerifiedProviderIdentity"}`
— so a second value type in `auth/adapters.py` fails it. `test_the_public_allowlist_is_exactly_the_readiness_probe`
now reads the live router: the routes declaring neither accessor and not a provider callback must be
exactly `{"/health/ready"}`. Controlled empirically by removing the auth dependency from
`routers/root.py`: the rewritten case fails, the old literal comparison could not.

#### WR-65: `_fresh_interpreter` runs a subprocess with no timeout, in two files
**Files modified:** `tests/unit/error_tree.py`, `tests/unit/test_error_registry.py`, `tests/unit/test_exception_handlers.py`
**Commit:** 6b81467
**Applied fix:** Both copies of the helper and both copies of the `_SUBPROCESS_PATH` computation are
deleted; one `fresh_interpreter` lives beside the totality check in `unit/error_tree.py`, carrying
`timeout=SUBPROCESS_TIMEOUT_SECONDS` (120). All ten call sites use it. Verified the timeout fires by
running a sleeping child under a one-second bound: `subprocess.TimeoutExpired`.

#### WR-66: the committed-credential scan passes vacuously if it finds no files
**Files modified:** `tests/unit/test_config.py`
**Commit:** d6d9488
**Applied fix:** The `rglob` result is collected first and asserted non-empty before the loop, so a
renamed `config/` directory fails the scan instead of reporting green having read nothing — the same
control every other walking guard in this suite carries.

#### WR-67: the JWKS offload proof gates on wall-clock heartbeat counts
**Files modified:** `tests/unit/test_jwks_offload.py`, `pyproject.toml`
**Commit:** a7f5dc6
**Applied fix:** The absolute `ticks >= 10` is replaced by `Heartbeat.unstarved_floor(started,
finished)` — a tenth of the ideal tick count for the window the case actually observed. The starved
control keeps `ticks <= 2` (load pushes a starved count down, never up, so that bound cannot go
flaky) and now additionally asserts it sits below the same derived floor, so the two cases provably
partition the outcome rather than each pinning an unrelated integer. Both are marked
`@pytest.mark.timing`, registered in `pyproject.toml`, so CI can report them separately:
`pytest -m timing` selects exactly those two.

#### WR-68: the secret-redaction case loads the live repository config directory
**Files modified:** `tests/unit/test_config.py`
**Commit:** 3749805
**Applied fix:** `test_the_key_never_renders_in_a_dump_or_a_repr` now calls `load_tracked_config({})`,
which already copies the tracked file into a temp directory and already carries the
`assert loaded.app_config is not None` guard. What the case asserts no longer depends on a
developer's `config/`, and a load producing no `app_config` fails readably instead of as an
`AttributeError`.

#### WR-69: `_ProbeSession.instances` is class-level state shared across the module
**Files modified:** `tests/unit/test_identity_accessors.py`
**Commit:** 8ed0010
**Applied fix:** The class attribute is gone. `_ProbeSession.__init__` takes the list to append to,
`_client` creates one per client and publishes it as `app.state.opened_sessions`, and the two
counting cases read it off the client they drove. There is no cross-test list left to survive
teardown and no clear-as-a-side-effect-of-a-factory invariant to break.

### Skipped

#### WR-61: the tests pin `/auth/challenge` as a second pre-auth-callable route, which the invariants forbid
**Reason:** Ratified override — the test is right and the literal is the recorded enforcement point.
`.planning/REQUIREMENTS.md` CREATE-01: *"**Amended by Phase 37.2 (D-01/D-03), 2026-08-25 — the count
changed from one route to two; the substance did not.** … Challenge issuance moved out of
`POST /auth/create-user`'s prepare mode onto its own `POST /auth/challenge` route, and that route
**must** be pre-auth-callable: an unlinked caller needs a challenge in order to create an account at
all, so demanding a link first would make account creation unreachable."* The same record names this
exact literal as the guard: *"**The enforcement point is `tests/unit/test_app_wiring.py`'s
`PREAUTH_CALLABLE_PATHS` literal** — today exactly `{"/auth/create-user", "/auth/challenge"}`. It is
deliberately hand-maintained rather than derived from the routers, so widening the set is a visible
edit to a two-element literal that a reviewer meets in the diff."* Changing the test would delete the
recorded guard; amending `SHARED-INVARIANTS.md:14` and `01-foundation.md:138,156` is a spec edit
outside a code-review fix, and the divergence is already written down where a reader meets it.

#### WR-62: the tests pin the raw Firebase subject as plaintext in the challenge row
**Reason:** Ratified override, recorded as a flagged conflict. `.planning/REQUIREMENTS.md` CREATE-02:
*"**FLAGGED CONFLICT against the binding specification — recorded, NOT resolved (Phase 37.4
D-11/A-08, 2026-08-30). The pre-auth subject is stored in plaintext.** `02-create-user.md:49`
requires … and states in terms that *"raw subject never stored"* … This project has knowingly
diverged: `preauth_subject_hash BYTEA` is now `preauth_subject TEXT` holding the raw subject, the
comparison is a plain `!=`, and `auth/hmac_keyring.py` (118 lines), `tests/unit/test_hmac_keys.py`
(314 lines) … are all deleted."* The record also states the finding that drove it —
`core.external_identities.subject` already stores the same raw value permanently — and
`.planning/REQUIREMENTS.md:50` counts it among the thirty-one conflicts recorded as FLAGGED. The
deleted `test_hmac_keys.py` was deleted by the same ratified decision, not silently. So
`test_the_preauth_subject_is_stored_in_plaintext` pins the decision the project made; restoring the
HMAC would revert it, and amending `01-foundation.md:344` / `02-create-user.md:49` is a spec edit
outside this pass.



## Fixer 5 — e2e + schema tests

### Fixed

#### CR-40: the ingestion case locks in an unbounded paid grant, and contradicts the restore case
**Files modified:** `tests/schema/test_subscription_ingestion.py`
**Commit:** 66c3320
**Applied fix:** Verified fixer 3's handoff against the source first. `services/subscriptions.py:110-115`
now computes `term_ends_at = term_end_for(status, notification)` and raises `InternalError` when an
entitled status carries no open term — before `upsert_subscription`, so nothing is written.
`test_an_absent_grace_end_writes_a_grant_carrying_no_end_at_all_control` (which asserted the
unbounded grant and froze the defect) is replaced by
`test_a_grace_delivery_with_no_grace_end_writes_no_grant`: `pytest.raises(InternalError)` around
`buyer.deliver(...)`, then `counts() == (0, 0, 0, 0)`, `effective() == []`, and the
`store_notification_without_term` error line through the file's own `service_logs` spy. This makes
the webhook path agree with `tests/e2e/test_restore_subscription.py:323-341` for the same shape.
Checked the two neighbours fixer 3 named: `test_a_grace_window_already_closed...` places its grace
end one minute before the captured instant but 30 days after `starts_at`, so it passes the new
`term_ends_at <= starts_at` guard and is unaffected.
**Verified by:** py_compile; `--collect-only` (imports and collects, 27 deselected); reasoning
against `ENTITLED_STATUSES = {active, grace_period}` and the guard's position ahead of the first
write. The case itself needs a live database and cannot run here.

#### WR-80: the lock-order file proves PostgreSQL's semantics against a hand-copied statement
**Files modified:** `tests/schema/test_grant_locks.py`
**Commit:** 8fa2e43
**Applied fix:** Added `TestTheMirrorsStillMatchProduction`, which compiles
`_effective_grants_statement(...).with_for_update()` and `_usage_statement(...).with_for_update()`
and asserts the mirrors' load-bearing terms are still what production emits: `FOR UPDATE`, `ORDER BY
core.access_grants.id ASC`, no `LIMIT`, the four predicates, and the usage row's primary-key
equality. A dropped lock, a reversed order or an added `LIMIT 1` now fails here instead of leaving
all four contention cases green against a statement nothing runs. I did not rewrite the contention
cases to issue the compiled statement — that means replacing raw asyncpg connections with sessions
in cases I cannot execute. The one remaining mirror difference (production binds the request instant,
the mirror asks for `CURRENT_TIMESTAMP`, since a raw connection has no captured instant to bind) is
now stated in the comment above `_LOCK_GRANTS` rather than left for a reader to discover.
**Verified by:** ruff; py_compile; **executed** — these two cases need no database and pass
(`2 passed, 26 deselected`).

#### WR-81: the DeviceCheck bit carry-forward is never exercised
**Files modified:** `tests/e2e/test_claim_anonymous_grant.py`, `tests/e2e/test_claim_registered_grant.py`
**Commit:** d1c7cb5
**Applied fix:** Root cause is coverage, not production: `services/auth.py:218` and `:274` do carry
the other bit forward, but every `write_calls ==` assertion ran against a never-set device, so
`False` in the third position was equally consistent with a hard-coded bit. Added
`test_a_set_registered_bit_survives_the_anonymous_claim` (scripts `BitState(bit0=False, bit1=True)`,
expects `(DEVICE_TOKEN, True, True)`) and its mirror
`test_a_set_anonymous_bit_survives_the_registered_claim` (`bit0=True, bit1=False` → `(DEVICE_TOKEN,
True, True)`). Each also asserts the claim still succeeds and writes its grant and usage row, since
neither path gates on the other's bit. Corrected the two happy-path comments that claimed to prove
the carry-forward; they now say what they actually pin and point at the new cases.
**Verified by:** ruff; py_compile; `--collect-only` (both new cases collect); reasoning against the
two gates (`if state.bit0` on the anonymous path, `if state.bit1` on the registered one), so neither
new state is refused before the write.

#### WR-82: both registration-pairing scans run over empty tables
**Files modified:** `tests/schema/test_registration_pairing.py`
**Commits:** 8a01c0f, c501ec1
**Applied fix:** Added `TestTheProductionWriterLeavesNeitherHalf`, which drives the real
`AuthService.complete` through `run_creation` (reused from `test_create_atomicity.py`, the same
cross-file helper import `test_grant_locks.py` already makes) for a `google` provider, then scans.
`IdentitiesDB.create_user` sets `registered_at` for a non-anonymous provider in the same transaction
as the identity row, so this is the one writer that could reach the third state. The scans are keyed
to the harness's own issuer, and a preliminary count asserts the completion actually committed an
identity, so the new case cannot pass vacuously the way the empty-table ones do. The two original
scans are kept and their class docstring now says they are whole-database conformance probes rather
than coverage. The imported fixture is aliased (`harness as creation_harness`) because pytest
registers an imported fixture under the name it is bound to. c501ec1 shortens that class docstring
to three lines: `tests/unit/test_docstring_bar.py` records a baseline of 0 over-long docstrings for
`tests/schema`, and my four-line version broke it.
**Verified by:** ruff; py_compile; `--collect-only` (5 cases); the aliased-fixture resolution was
proved empirically with a throwaway two-module pytest run before applying it; unit suite back to
1449 after c501ec1.

#### WR-83: the token-rollback assertion is a whole-table count
**Files modified:** `tests/schema/test_create_atomicity.py`
**Commit:** 993c3c4
**Applied fix:** The fixture now returns the `minted` uuid it pinned, and
`test_no_attribution_token_survives_for_the_would_be_user` counts
`provider = 'google_play' AND identity_value = :value` instead of the whole table. Added the control
the case was missing: the rival `apple` row the fixture committed carries the same value, so a
second count keyed on the value alone asserts `1` — which is what makes the zero above mean "the
rolled-back row is gone" rather than "the predicate matched nothing".
**Verified by:** ruff; py_compile; `--collect-only`; reasoning against the fixture, which commits
one `apple` row on `other_user` with `identity_value = str(minted)` and monkeypatches
`identities_crud.uuid4` so both of the creation's token inserts carry that same value.

#### WR-84: `anonymous_firebase_credential` leaks a permanent Firebase user on every session
**Files modified:** `tests/e2e/conftest.py`
**Commit:** 8087756
**Applied fix:** Fixed the fixture's lifecycle, not the symptom: it is now a generator fixture that
deletes the minted user in `finally`, on the exact shape `google_linked_firebase_credential` already
uses. Its scope moved from `session` to `module` because the delete needs the Admin app the lifespan
built (`firebase_admin.get_app(name=f"issuer:{...}")`), and `lifespan.py:179` deletes that app at
shutdown — a session-scoped fixture outlives it and would find nothing to delete through. Module
scope is what the sibling Google fixture uses, and both consumers live in one module
(`test_create_user.py`), so no extra user is minted in practice.
**Verified by:** ruff; py_compile; `--collect-only`; reasoning about finalizer ordering — the
fixture requests `_app_lifespan`, so it is torn down before the lifespan that owns the Admin app.

#### WR-85: the contention fixture commits into the real application database
**Files modified:** `tests/e2e/test_challenge_store.py`
**Commit:** 7752a28
**Applied fix:** I did not repoint the second engine at a scratch database — real contention needs
eight committed transactions against the app's own schema, and the scratch-database machinery lives
in `tests/schema/conftest.py`, outside this suite's wiring. I fixed the two things the committed
rows actually break. (1) `row_count` is no longer a whole-table count: it counts the rows this
module owns, the pre-auth ones by `preauth_issuer == ISSUER` and the bound ones through an outer
join to the identity they name, so another module's leftover can no longer fail
`test_no_row_this_module_wrote_survives_its_test` (renamed from
`test_the_table_is_empty_at_the_start_of_a_test`) or the `== 1` case beside it. (2) The fixture's
`finally` now deletes by `preauth_issuer == ISSUER` rather than by the single handle, so a run
interrupted before the cleanup is swept by the next run instead of leaking a row for good.
**Verified by:** ruff; py_compile; `--collect-only` (31 cases); the scoped count statement was
compiled standalone to confirm the outer join and `or_` build the intended SQL.

#### WR-86: the log-hygiene walk only inspects `str` fields
**Files modified:** `tests/e2e/test_sign_out_all.py`
**Commit:** 9351f04
**Applied fix:** Replaced the `isinstance(value, str)` comprehension with the render-then-search
pattern the two webhook twins already use (`repr(entries)`, then `assert secret not in rendered` per
secret), so a UUID, a dict or a row carrying the subject is searched exactly as the final log line
would render it. The comment's promise and the assertion now agree.
**Verified by:** ruff; py_compile; `--collect-only`; reasoning about the captured entries, which are
`(event, fields)` pairs of small scalar dicts, and about `identity.provider_uid`, which `seed_identity`
sets to a non-None string for a `google` provider.

### Skipped
None — all eight in-scope findings were applied.

### Gate
`.venv/bin/ruff check src tests` — clean.
`.venv/bin/pytest tests/unit -q -p no:cacheprovider` — 1449 passed (baseline held).
`.venv/bin/ty check src` — Found 5 diagnostics (baseline held).
Working tree clean; every fix is its own commit. The edited e2e and schema cases need a live
database and were not executed here, apart from the two WR-80 cases, which need none and pass.



---

_Fixed: 2026-09-09T13:22:53Z_
_Fixer: Claude (gsd-code-fixer x5, sequential by review group)_
_Iteration: 1_
