---
phase: 47-stop-threading-an-evaluation-instant-through-the-layers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md
autonomous: true
requirements: []

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "SHARED-INVARIANTS.md no longer binds any phase to one captured evaluation time per request; the current_period clause of that bullet survives unchanged (D-02)."
    - "REQUIREMENTS.md records under SYNC-01, in a dated note of the same shape as the Phase 40 UPGRADE-02 amendment, that the one-evaluation-time property is given up and why; the requirement stays checked and is amended, never withdrawn."
    - "Nothing outside /home/init/native-speaker/ns-api-gateway is staged, committed or pushed."
  artifacts:
    - .planning/REQUIREMENTS.md
    - /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md
  key_links:
    - "SHARED-INVARIANTS.md:44 -> REQUIREMENTS.md SYNC-01: the invariant is the source of the obligation and the requirement is where this project records that it was met; both must move, or a later reader finds one contradicting the other."
    - "ROADMAP.md:98 ('flag conflicts, never resolve them silently') -> D-02: the user chose to strike rather than flag, on the precedent of Phase 38 plan 38-04; the strike is the recorded resolution."
---

<objective>
Strike the invariant this phase deletes, and record the amendment under the requirement that claims
it was met.

**D-02 (locked by the user, 2026-09-11).** `SHARED-INVARIANTS.md:44` binds every phase to "ONE
captured evaluation time or one consistent snapshot per request", and `REQUIREMENTS.md:226` records
SYNC-01 as met on exactly that property. Phase 47 deletes the property. The user's decision is to
**strike the invariant, not merely flag it** — edit the bullet so it no longer claims one evaluation
time per request, keeping its `current_period` clause — and to amend SYNC-01 with a dated note in the
shape of the Phase 40 UPGRADE-02 amendment already in this file.

RESEARCH Finding 7 recommended flagging rather than striking (Assumption A4, rated medium risk) and
PATTERNS Cluster 12 says "Do not edit `SHARED-INVARIANTS.md`". **Both are overridden by D-02.** The
precedent the user is following is Phase 38 plan 38-04, which struck § "Audit" from the same file
under a decision the developer had taken. Recording this override here so a reviewer reading the two
research artifacts does not read the plan as ignoring them.

**Reversibility: `one-way`.** The invariant binds every phase of this milestone; once struck, the
phases that land after it are written against its absence, so restoring it would mean re-auditing
every later phase against a rule they were not built to. `REVERSIBILITY_GATES` is on, and the rule
calls for a `checkpoint:decision` before a one-way task.

**No checkpoint is emitted, and that is deliberate.** The human decision this gate exists to collect
has already been taken — it is D-02 itself, dated 2026-09-11, quoted above. A checkpoint here would
stop an autonomous run to re-ask a question the user has answered. The gate is therefore **discharged
by D-02**, the `one-way` rating is carried on the task so the signal survives in the plan, and
`autonomous` stays `true`.

Purpose: the record moves before the code contradicts it, so no commit in this phase lands against a
live invariant it violates.
Output: one edited bullet in the shared specification, and one dated amendment plus an updated
traceability row in `REQUIREMENTS.md`.

**Executor notes.** `IS_WORKTREE` is **false** in this repository: GSD's detector reports true because
this repo is a submodule whose `.git` is a file, and believing it makes tracking writes vanish. The
specification file is **outside this repository's git tree**; edit it in place and never `git add`,
`git commit` or `git push` anything outside `/home/init/native-speaker/ns-api-gateway`. Phase 46's
plans pin this file's sha256 in `<automated>` verify commands; **those pins are historical and go
stale by design** — this plan changes the file, so do not re-run them and do not treat a changed
digest as a defect.
</objective>

<!-- Literals below appear verbatim in this plan's actions because the plan's own gates grep
     for them; the greps run over source trees, never over this file. -->
<!-- planner-discipline-allow: ONE captured evaluation time -->
<!-- planner-discipline-allow: consistent snapshot per request -->

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strike the one-evaluation-time clause from the shared invariants, keeping the current_period rule</name>
  <reversibility rating="one-way">The invariant binds every phase of the v2.0 milestone; the phases landing after this strike are written against its absence, so restoring it would require re-auditing each of them against a rule they were not built to. The gate this rating calls for is discharged by D-02, dated 2026-09-11.</reversibility>
  <precondition>/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md exists and is writable; it lives outside this repository's git tree, so no git command in this plan may reference it.</precondition>
  <read_first>
    - /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md (§ "Grants and evaluation time", lines 39-44 — line 41 states the shared predicate and stays; line 44 is the bullet being struck)
    - .planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-RESEARCH.md § "Finding 7" (the four texts that describe the deleted property, quoted verbatim)
    - .planning/ROADMAP.md line 98 (the milestone rule this strike resolves under D-02)
  </read_first>
  <files>/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md</files>
  <action>
Rewrite the single bullet at line 44 of § "Grants and evaluation time". Today it opens with a
derivation rule binding every time-dependent value to one captured evaluation time or one consistent
snapshot per request, and closes with the `current_period` rule (`YYYY-MM`, UTC calendar month).

Keep the closing rule **verbatim**: `current_period` is `YYYY-MM`, UTC calendar month. Remove the
opening derivation rule entirely. Do not replace it with a weaker version of itself — no "where
practical", no "per transaction", no "prefer". The property is deleted, and a hedged restatement is
the flagged conflict this strike exists to avoid.

Leave line 41 — the shared effective-grant predicate, `status='active' AND starts_at <= now AND
(ends_at IS NULL OR ends_at > now)` — **exactly as written**. Phase 47 keeps that predicate's shape
and only changes what supplies the compared value; the invariant's wording already says `now`.

Change no other line, no other section and no heading in the file. Write the edit in Simplified
Technical English: short sentences, one idea each.

Do not run `git add`, `git commit`, `git push` or any other git command against this path or its
parent directory. The file is outside `/home/init/native-speaker/ns-api-gateway`, and this project's
standing rule scopes every push to that submodule.
  </action>
  <verify>
    <automated>test "$(grep -c 'ONE captured evaluation time' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md)" = "0"</automated>
    <fails_when>non-zero exit — the struck phrase is still present in the file</fails_when>
    <automated>test "$(grep -c 'current_period. is .YYYY-MM., UTC calendar month' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md)" = "1"</automated>
    <fails_when>non-zero exit — the surviving clause was dropped with the struck one, or duplicated</fails_when>
    <automated>test "$(grep -c "starts_at <= now AND (ends_at IS NULL OR ends_at > now)" /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md)" = "1"</automated>
    <fails_when>non-zero exit — the shared-predicate invariant at line 41 was altered, which this plan must not touch</fails_when>
    <automated>git -C /home/init/native-speaker/ns-api-gateway status --porcelain -- /home/init/native-speaker/specs | wc -l | grep -qx 0</automated>
    <fails_when>non-zero exit — the specification path appears in this repository's status, meaning something outside the submodule was staged</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'consistent snapshot per request' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` prints `0`.
    - `grep -c 'YYYY-MM' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` prints `1`.
    - `grep -c '^## Grants and evaluation time' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` prints `1` — the heading is untouched.
    - `git -C /home/init/native-speaker/ns-api-gateway status --porcelain` lists no path beginning with `/home/init/native-speaker/specs` and no path outside the submodule.
    - No `git` invocation in the task's shell history targets the specification path.
  </acceptance_criteria>
  <done>The bullet no longer binds any phase to one captured evaluation time per request; its `current_period` rule survives word for word; line 41's shared predicate is unchanged; nothing outside the submodule is staged.</done>
</task>

<task type="auto">
  <name>Task 2: Amend SYNC-01 with the dated note, and carry it into the traceability row</name>
  <read_first>
    - .planning/REQUIREMENTS.md:226 (SYNC-01 as written) and :229-231 (the SYNC-03 amendment — the shape to copy: a dated blockquote indented under the bullet)
    - .planning/REQUIREMENTS.md:754-760 (the traceability table, including the Phase 40 UPGRADE-02 row D-02 names as the model)
    - .planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-RESEARCH.md § "Finding 7"
    - .planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-PATTERNS.md § "Cluster 12"
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Add one dated blockquote amendment indented under the SYNC-01 bullet, in the same shape as the SYNC-03
amendment three lines below it: a bold lead naming the phase, the decision id and the date
(`Amended by Phase 47 (D-02), 2026-09-11`), then what the requirement used to rest on, then what it
rests on now, then an explicit statement of treatment.

The note must state, in Simplified Technical English:
1. As written, SYNC-01 says the four reported values all derive from **one captured evaluation time**.
   That property is deleted by this phase.
2. What replaces it: `/auth/sync` reads the clock once inside `SyncService.read_entitlement`, and the
   effective-grant predicate asks PostgreSQL for the time with `clock_timestamp()` (D-01). The four
   values the endpoint reports are still mutually consistent for the read that produces them; what is
   gone is the guarantee that one instant is shared across the whole request and across services.
3. Why a database-side `now()` would not have restored it either: a request in this product spans
   several transactions, because `services/restore.py` and `services/auth.py` each commit mid-request,
   so `now()` is one snapshot per transaction and not per request.
4. The treatment is **amended, not withdrawn**: the endpoint still returns the effective grant,
   `current_period`, `monthly_used` and the stored `identity_provider`, and the checkbox stays `[x]`.
5. The source obligation was removed rather than diverged from: `SHARED-INVARIANTS.md`'s
   one-evaluation-time clause was struck by Task 1 under D-02, on the precedent of Phase 38 plan
   38-04. No permanent flagged conflict is carried, because after that edit there is no surviving
   invariant text to diverge from.
6. Phase 38's own success criterion 1 rested on the same property and is superseded by this note.

Do not uncheck SYNC-01, do not edit its bullet text, and do not touch SYNC-02 or SYNC-03 or their
notes.

Then update the traceability table row `SYNC-01 … SYNC-03 | Phase 38 | …`: append to its parenthetical
that SYNC-01 is **amended by Phase 47 (D-02), 2026-09-11** — the one-evaluation-time derivation is
given up and the shared invariant that stated it was struck rather than flagged. Keep the row a range,
as every row in that table is; do not reshape it into per-id rows.

If the "Coverage" block under the table carries a flagged-conflict count, leave the count alone and
say why in the SUMMARY: this phase resolves its conflict by removal and files no new flagged conflict,
which is the Phase 38 D-03 precedent.
  </action>
  <verify>
    <automated>test "$(grep -c 'Amended by Phase 47 (D-02), 2026-09-11' .planning/REQUIREMENTS.md)" -ge "1"</automated>
    <fails_when>non-zero exit — the dated lead is absent, so the amendment was not written in the recorded shape</fails_when>
    <automated>test "$(grep -c '\[x\] \*\*SYNC-01\*\*' .planning/REQUIREMENTS.md)" = "1"</automated>
    <fails_when>non-zero exit — SYNC-01 was unchecked, withdrawn or duplicated instead of amended</fails_when>
    <automated>grep -q 'clock_timestamp' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit — the note does not name what replaces the deleted property, so a later reader cannot tell what the endpoint now rests on</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'SYNC-01' .planning/REQUIREMENTS.md` shows the bullet still checked `[x]` with its original text, followed by a blockquote line beginning with `> **Amended by Phase 47 (D-02), 2026-09-11`.
    - The traceability row matching `SYNC-01 … SYNC-03` contains the string `Phase 47`.
    - `grep -c 'transaction_timestamp\|several transactions' .planning/REQUIREMENTS.md` is at least `1` — the note explains why a database-side snapshot would not have restored the property.
    - `git -C /home/init/native-speaker/ns-api-gateway status --porcelain .planning/REQUIREMENTS.md` shows the file modified, and no path outside the submodule appears anywhere in `git status --porcelain`.
  </acceptance_criteria>
  <done>SYNC-01 carries a dated Phase 47 amendment in the file's established shape, stays checked, and the traceability row names Phase 47; no new flagged conflict is filed and no requirement is withdrawn.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces no source symbol. It changes two records. The phase-wide symbol ledger is in
`47-01-PLAN.md` § "Artifacts this phase produces"; the rows this plan owns are:

| Symbol / record | Change | Plan |
|---|---|---|
| `SHARED-INVARIANTS.md:44` one-evaluation-time clause | **struck**; the `current_period` clause survives | **[02]** |
| `REQUIREMENTS.md` SYNC-01 | **amended**, dated, still checked | **[02]** |
| `REQUIREMENTS.md` traceability row `SYNC-01 … SYNC-03` | amended to name Phase 47 | **[02]** |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the shared specification tree | An edit crosses out of the submodule into a path this project must never stage, commit or push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-06 | Tampering | `/home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` | medium | mitigate | The edit is scoped to one bullet; three greps pin the struck phrase at zero, the surviving clause at one and the untouched shared predicate at one, so a wider rewrite of the specification fails the task. |
| T-47-07 | Repudiation | the milestone record | medium | mitigate | Striking an invariant with no record would leave a later phase unable to tell whether the property was dropped or forgotten. Task 2's dated amendment under SYNC-01 is the record, and it names the decision, the date and what replaces the property. |
| T-47-08 | Tampering | git scope | high | mitigate | A commit or push outside the submodule is refused by construction: no git command in this plan references the specification path, and a `git status --porcelain` gate asserts no out-of-submodule path is staged. |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing and touches no manifest. |
</threat_model>

<verification>
1. `grep -c 'ONE captured evaluation time' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` prints `0`.
2. `grep -c 'YYYY-MM' /home/init/native-speaker/specs/auth-refactor-phases/SHARED-INVARIANTS.md` prints `1`.
3. `grep -c 'Amended by Phase 47 (D-02), 2026-09-11' .planning/REQUIREMENTS.md` is at least `1`.
4. `git -C /home/init/native-speaker/ns-api-gateway status --porcelain` lists only paths inside the submodule.
</verification>

<success_criteria>
- The invariant no longer claims one evaluation time per request; its `current_period` rule survives.
- SYNC-01 is amended with a dated note, stays checked, and the traceability row names Phase 47.
- Nothing outside the submodule is staged, committed or pushed.
</success_criteria>

<output>
Create `.planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-02-SUMMARY.md` when done.
</output>
