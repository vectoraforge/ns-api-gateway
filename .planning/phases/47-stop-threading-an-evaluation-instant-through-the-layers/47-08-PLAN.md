---
phase: 47-stop-threading-an-evaluation-instant-through-the-layers
plan: 08
type: execute
wave: 6
depends_on: ["47-02", "47-07"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`.venv/bin/pytest -q -m ''`, `-m e2e` and `-m schema` all exit 0 (roadmap criterion 5), measured on this plan's own run rather than copied from an earlier summary."
    - "Each of the five roadmap success criteria is re-derived here with the command that proves it and the answer that command gave, criterion 3 including the D-01 deviation stated plainly."
    - "The ROADMAP entry for Phase 47 carries the measured result per criterion and the plan list, and STATE.md records D-01 and D-02 in the accumulated context."
    - "ruff is clean over src and tests."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "This plan measures rather than reports: this project's own convention (the Phase 45 and Phase 46 records in STATE.md) is that the closing plan runs all four commands itself, because a copied count proves nothing about the tree as it now stands."
    - "Criterion 3's wording says now(); D-01 resolved it to clock_timestamp(). The criterion is closed as met in spirit with the deviation named, not quietly marked met."
---

<objective>
The phase gate. Run the four commands, re-derive the five criteria against the tree as it now stands,
and close the record.

This plan writes no source. Its whole value is that the counts and the answers are measured **here**,
on this run, against the finished tree — the convention this project set in Phase 45 plan 45-09 and
Phase 46 plan 46-05, where a copied count from an earlier summary was refused as proof.

**Runner note.** `uv run pytest` and `.venv/bin/pytest` are the same interpreter and the same
environment; RESEARCH § "Environment Availability" measured both this session. Earlier plans used
`uv run pytest`, the form Phase 46 proved. Criterion 5 is worded with `.venv/bin/pytest`, so this plan
uses that form verbatim, and a green run under it satisfies the criterion as written.

**Baselines measured before this phase began** (RESEARCH § "Environment Availability"): 2570 collected
for `-m ''`, 1917 for the bare unit run, 362 for `-m e2e`, 291 for `-m schema`. This phase deletes one
test module and adds two, so the totals move; the point of quoting the baseline is that a large drop is
a defect, not a success.

Purpose: criterion 5, and an honest close for the other four.
Output: three measured suite runs, a criterion-by-criterion record, and the two planning files updated.

**Reversibility:** `reversible` — records only.

**Executor note.** `IS_WORKTREE` is **false** in this repository: GSD's detector reports true because
this repo is a submodule whose `.git` is a file, and believing it makes tracking writes vanish.
Nothing outside `/home/init/native-speaker/ns-api-gateway` is staged, committed or pushed — the
specification file plan 47-02 edited lives outside this tree by design and stays unstaged.
</objective>

<!-- Literals below appear verbatim in this plan's actions because the plan's own gates grep
     for them; the greps run over source trees, never over this file. -->
<!-- planner-discipline-allow: get_evaluated_at -->
<!-- planner-discipline-allow: evaluated_at -->
<!-- planner-discipline-allow: TBD (run /gsd-plan-phase 47 -->

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-RESEARCH.md
@.planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-02-SUMMARY.md
@.planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the three suites and the linter, and record the counts measured here</name>
  <read_first>
    - .planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-RESEARCH.md § "Environment Availability" (the four exact invocations and the pre-phase baselines)
    - pyproject.toml lines 65-76 (the addopts and the three declared markers)
    - .planning/STATE.md (the Phase 45 and Phase 46 records, for the shape of a measured close)
  </read_first>
  <files>.planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-08-SUMMARY.md</files>
  <action>
Run all four commands in this plan, in this order, and record each command's collected count and its
pass/fail line in the SUMMARY. Do not copy a count from any earlier plan's summary.

Run `.venv/bin/pytest -q -m ''` first — it is everything, roughly 2570 cases before this phase, and it
needs the database. Then `.venv/bin/pytest -q -m e2e`, then `.venv/bin/pytest -q -m schema`, then
`uv run ruff check src tests`.

Note in the SUMMARY that a bare `.venv/bin/pytest -q` is the unit suite only, because the addopts
already carry the two negative markers, and that `-m ''` is not that — RESEARCH Pitfall 6. State the
delta from the pre-phase baselines and account for it: this phase deleted
`tests/unit/test_sync_clock_capture.py`, added `tests/unit/test_open_term.py` and
`tests/unit/test_instant_is_not_threaded.py`, deleted one case in the Google Play notifications unit
file and replaced one case in the challenge-store e2e file. A drop larger than the deleted module's own
case count is a defect to investigate, not a result to record.

If the database is unreachable, stop and say so plainly. Do not substitute a bare unit run for `-m ''`
and do not report criterion 5 as met on a deselected suite — that is the exact failure RESEARCH
Pitfall 6 names.

If any suite is red, stop and report which case and why. Do not repair source in this plan: a defect
found here belongs in a gap-closure plan, and this plan's job is to measure honestly.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q -m ''</automated>
    <fails_when>non-zero exit, or fewer than 2400 tests collected (the suite was deselected or a marker was lost), or "failed" in the summary line</fails_when>
    <automated>.venv/bin/pytest -q -m e2e</automated>
    <fails_when>non-zero exit, or fewer than 340 passed</fails_when>
    <automated>.venv/bin/pytest -q -m schema</automated>
    <fails_when>non-zero exit, or fewer than 280 passed</fails_when>
    <automated>uv run ruff check src tests</automated>
    <fails_when>non-zero exit, or any line of output naming a rule code</fails_when>
  </verify>
  <acceptance_criteria>
    - All four commands exit 0, and the SUMMARY records each command's own collected count and summary line from this run.
    - The `-m ''` run collects at least 2400 cases — the pre-phase baseline was 2570 and this phase's net case change is small.
    - The SUMMARY names the delta from the 2570 / 362 / 291 baselines and attributes it to the modules this phase added and deleted.
    - The SUMMARY states that `-m ''` and the bare unit run are different suites.
  </acceptance_criteria>
  <done>The three suites and the linter were run in this plan and are green, with counts measured here and the delta from the baseline accounted for.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the five roadmap criteria, criterion 3 with its deviation stated</name>
  <read_first>
    - .planning/ROADMAP.md, the Phase 47 entry (the five success criteria as written)
    - .planning/ROADMAP.md, the Phase 46 entry (the shape of a criterion annotated "met as written" with its proof named)
    - the seven plan SUMMARYs of this phase
  </read_first>
  <files>.planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-08-SUMMARY.md</files>
  <action>
Re-derive each criterion by running its command now, and record the command and its answer.

**Criterion 1** — `get_evaluated_at` does not exist, no `Depends(...)` supplies a datetime, and the e2e
override is gone. Run `grep -rl 'get_evaluated_at' src/ tests/` and `grep -rn 'Depends(get_' src/nativespeaker/api/app/dependencies.py src/nativespeaker/api/routers/`;
the first must list nothing and the second must list no factory returning a datetime.

**Criterion 2** — no service constructor or method and no crud method takes one. Run
`grep -rl 'evaluated_at' src/nativespeaker/api/services src/nativespeaker/api/crud src/nativespeaker/api/routers src/nativespeaker/api/app`;
it must list nothing. Record that `auth/` and `tables/` are deliberately out of this scope, because
`monthly_period_for`, `seconds_until_rollover`, `_transaction_status` and `_status_for` are pure
helpers that take the datetime they compute from, which criterion 3 explicitly allows.

**Criterion 3** — the SQL comparisons and the Python reads. Record the two SQL sites by name,
`_effective_grants_statement` in `crud/grants.py` and `ChallengesDB.claim` in `crud/challenges.py`, and
**state the deviation plainly**: the criterion is worded `now()`, and D-01 resolved it to
`func.clock_timestamp()` because PostgreSQL's `now()` is `transaction_timestamp()` and the e2e harness
holds one savepoint-joined transaction open per test, which RESEARCH Finding 1 measured as 11 of 41
quota tests turning red. Criterion 3 is closed as **met in spirit, deviation named** — the comparison is
database-side — not as met as written. Then list every Python read this phase introduced, one per
method, and confirm the count with
`grep -rc 'datetime.now(UTC)' src/nativespeaker/api/services src/nativespeaker/api/crud src/nativespeaker/api/auth`.

**Criterion 4** — no comment or docstring names the removed dependency or the shared instant, and the
clock-capture module is gone. Run the guard, `uv run pytest -q tests/unit/test_instant_is_not_threaded.py`,
and `ls tests/unit/test_sync_clock_capture.py` expecting a non-zero exit.

**Criterion 5** — the three suites, from Task 1's run.

Also record, in the SUMMARY and under its own heading, **what this phase gave up**, so a later reader
finds it rather than rediscovers it: the one-evaluation-time property is gone and the invariant that
stated it was struck under D-02 rather than carried as a permanent flagged conflict; the exact-equality
expiry boundary in the challenge store is now asserted over compiled SQL because a live database clock
never lands on a stored value; a superseded grant's `ends_at` can fall microseconds before its
replacement's `starts_at`; and `func.clock_timestamp()` is VOLATILE, so the effective-grant predicate
cannot drive an index scan on `starts_at` or `ends_at` — accepted on a table holding a handful of rows
per user and already filtered by `user_id` and `status`.

Write the whole record in Simplified Technical English: short sentences, one idea each.
  </action>
  <verify>
    <automated>test "$(grep -rl 'get_evaluated_at' src/ tests/ | wc -l)" = "0"</automated>
    <fails_when>non-zero exit — criterion 1 is not met; some file still names the dependency</fails_when>
    <automated>test "$(grep -rl 'evaluated_at' src/nativespeaker/api/services src/nativespeaker/api/crud src/nativespeaker/api/routers src/nativespeaker/api/app | wc -l)" = "0"</automated>
    <fails_when>non-zero exit — criterion 2 is not met; a service, crud, router or app file still carries the parameter</fails_when>
    <automated>uv run pytest -q tests/unit/test_instant_is_not_threaded.py</automated>
    <fails_when>non-zero exit, or fewer than 6 passed — criterion 4's guard is absent or incomplete</fails_when>
    <automated>test "$(grep -rc 'clock_timestamp' src/nativespeaker/api/crud/grants.py src/nativespeaker/api/crud/challenges.py | grep -c ':0')" = "0"</automated>
    <fails_when>non-zero exit — one of the two SQL comparison sites does not use the database clock, so criterion 3 is unmet</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY carries one section per criterion, each naming the command run and the answer it gave.
    - Criterion 3's section states the `now()` versus `clock_timestamp()` deviation, cites D-01 and the measured 11-of-41 result, and closes the criterion as met in spirit rather than as written.
    - The SUMMARY carries a "what this phase gave up" heading listing at least the four items named in the action.
    - `grep -rl 'get_evaluated_at' src/ tests/` lists nothing.
  </acceptance_criteria>
  <done>All five criteria are re-derived with their commands and answers recorded here, criterion 3 closed with its deviation stated, and the four accepted losses written down.</done>
</task>

<task type="auto">
  <name>Task 3: Close the record in ROADMAP.md and STATE.md</name>
  <read_first>
    - .planning/ROADMAP.md, the Phase 46 entry (the shape of a closed phase: criteria annotated with a date and the proof named, and the plan list with waves)
    - .planning/ROADMAP.md, the Phase 47 entry as plan-phase left it
    - .planning/STATE.md § "Accumulated Context" and the Phase 45/46 records
    - .planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-02-SUMMARY.md (the D-02 record)
  </read_first>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Update the Phase 47 entry in `.planning/ROADMAP.md` with scoped edits only — replace the criteria block
and the plan list, and do not rewrite the file or touch any other phase entry. Annotate each of the five
criteria with its measured result and the date, following the Phase 46 entry's shape: state what was
built, name the proof, and for criterion 3 state the D-01 deviation in the criterion's own annotation
rather than in a footnote. Set the `**Plans:**` line to `8/8 plans complete` and list the eight plans
under their waves, each with a one-line objective.

Update `.planning/STATE.md`:
- Move the current position past Phase 47 and record the phase as complete with this plan's date.
- Add to the accumulated context: **D-01**, the two SQL comparisons use `func.clock_timestamp()` and not
  `func.now()`, with the one-sentence reason and the measured 11-of-41 result; and **D-02**, the
  one-evaluation-time invariant was struck rather than flagged, with SYNC-01 amended and dated.
- Record the two forward notes the next phases need: Phase 49 still owns the `PlaySubscriptionSource`
  Protocol deletion and the `ChallengesDB` construction move, and Phase 50 still owns the
  `dependencies.py` rewrite onto a runtime container — this phase left that file otherwise intact by
  design, which is what Phase 50's dependency line assumes.

Stage and commit only paths inside `/home/init/native-speaker/ns-api-gateway`. The specification file
plan 47-02 edited is outside this tree and must not appear in `git status`, `git add` or any commit of
this phase. Phase 46's plans pin that file's sha256 in their verify commands; those pins are historical
and are stale by design after 47-02. Do not re-run them and do not treat the changed digest as a defect.
  </action>
  <verify>
    <automated>test "$(grep -c '8/8 plans complete' .planning/ROADMAP.md)" -ge "1"</automated>
    <fails_when>non-zero exit — the Phase 47 plan count was not updated</fails_when>
    <automated>test "$(grep -c 'TBD (run /gsd-plan-phase 47' .planning/ROADMAP.md)" = "0"</automated>
    <fails_when>non-zero exit — the placeholder plan line is still there</fails_when>
    <automated>test "$(grep -c 'clock_timestamp' .planning/STATE.md)" -ge "1"</automated>
    <fails_when>non-zero exit — D-01 is not in the accumulated context, so the next phase cannot see it</fails_when>
    <automated>git status --porcelain | grep -c '^' | xargs -I{} true; git status --porcelain | grep -v '^.. \.planning/' | grep -c 'specs/' | grep -qx 0</automated>
    <fails_when>non-zero exit — a path under the external specification tree appears in this repository's status</fails_when>
    <automated>test "$(grep -c '#### Phase 4[89]:\|#### Phase 50:' .planning/ROADMAP.md)" = "3"</automated>
    <fails_when>non-zero exit — a later phase entry was lost, which is what a whole-file rewrite of ROADMAP.md does</fails_when>
  </verify>
  <acceptance_criteria>
    - The Phase 47 ROADMAP entry lists eight plans under six waves, each checked, with no `TBD` line.
    - Each of the five criteria in that entry carries a measured annotation and a date; criterion 3's annotation names `clock_timestamp()` and D-01.
    - Phase 48, 49 and 50 entries are present and byte-identical to before this edit apart from nothing — `git diff .planning/ROADMAP.md` shows changes only inside the Phase 47 entry and the progress table row for Phase 47.
    - `.planning/STATE.md` names D-01 and D-02 in its accumulated context and records Phase 47 complete.
    - `git status --porcelain` lists no path outside the submodule.
  </acceptance_criteria>
  <done>The ROADMAP entry is closed with measured criteria and the eight plans, STATE.md carries D-01, D-02 and the two forward notes, and the diff touches only the Phase 47 entry and its progress row.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces no source symbol. The phase-wide symbol ledger is in `47-01-PLAN.md` § "Artifacts
this phase produces"; this plan is where that ledger is checked against the finished tree. Rows owned
here:

| Record | Change | Plan |
|---|---|---|
| `.planning/ROADMAP.md` Phase 47 entry | criteria annotated with measured results; eight plans listed | **[08]** |
| `.planning/STATE.md` | Phase 47 closed; D-01 and D-02 recorded; two forward notes for Phases 49 and 50 | **[08]** |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the finished tree → the project record | A claim of completeness crosses here and later phases build on it |
| this repository → the external specification tree | Nothing may be staged or committed across it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-23 | Repudiation | criterion 5 reported on a deselected suite | high | mitigate | `-m ''` is 2570 cases and needs the database; a bare run is 1917 and needs none, and reporting the second as the first would claim the e2e and schema suites passed when they never ran. The gate asserts a minimum collected count, and the action refuses a substitution outright if the database is unreachable. |
| T-47-24 | Repudiation | a copied count | medium | mitigate | Counts copied from an earlier plan's summary prove nothing about the finished tree. All four commands run in this plan, and the acceptance criteria require this run's own numbers in the SUMMARY. |
| T-47-25 | Tampering | a whole-file rewrite of ROADMAP.md | high | mitigate | Rewriting that file destroys every phase entry outside the diff window. The action requires scoped edits and a gate asserts the three later phase entries are still present. |
| T-47-08 | Tampering | git scope | high | mitigate | A gate asserts no path under the external specification tree appears in this repository's status, and the action names the stale sha256 pins so a changed digest is not mistaken for a defect. |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installed nothing. No manifest changed in any of its eight plans. |
</threat_model>

<verification>
1. `.venv/bin/pytest -q -m ''` exits 0 with at least 2400 collected.
2. `.venv/bin/pytest -q -m e2e` and `.venv/bin/pytest -q -m schema` both exit 0.
3. `uv run ruff check src tests` exits 0.
4. `grep -rl 'get_evaluated_at' src/ tests/` lists nothing.
5. `git status --porcelain` lists no path outside `/home/init/native-speaker/ns-api-gateway`.
</verification>

<success_criteria>
- The three suites and the linter are green, measured in this plan.
- Every roadmap criterion is re-derived with its command and answer, criterion 3 with its deviation.
- ROADMAP.md and STATE.md carry the close, and the ROADMAP diff touches only the Phase 47 entry.
</success_criteria>

<output>
Create `.planning/phases/47-stop-threading-an-evaluation-instant-through-the-layers/47-08-SUMMARY.md` when done.
</output>
