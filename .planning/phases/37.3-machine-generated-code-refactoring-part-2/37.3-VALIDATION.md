---
phase: 37.3
slug: machine-generated-code-refactoring-part-2
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-26
---

# Phase 37.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest ≥9.0 + pytest-asyncio ≥1.3 (`asyncio_mode = "auto"`) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` |
| **Quick run command** | `.venv/bin/python -m pytest -q` (762 tests, ~30s) |
| **Full suite command** | `.venv/bin/python -m pytest -q && .venv/bin/python -m pytest -q -m e2e && .venv/bin/python -m pytest -q -m schema` (1075 tests, ~97s) |
| **Estimated runtime** | ~30s quick / ~97s full |

**Baseline established during research — all green:** 762 unit / 198 e2e / 115 schema.

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/python -m pytest -q` (default selection, ~30s)
- **After every plan wave:** Run all three selections (unit + e2e + schema) — **mandatory**; the default `addopts` deselects e2e and schema, and two schema-marked tests (`test_create_atomicity.py`, `test_create_race.py`) break in this phase but are invisible to the quick run
- **Before `/gsd:verify-work`:** Full 1075 green
- **Max feedback latency:** ~97 seconds

---

## Per-Task Verification Map

> Task IDs are assigned at planning time; this map is keyed by decision (this phase carries no new REQ-IDs — D-19 amends FOUND-08 rather than adding requirements). The planner lifts these into per-task `<automated>` verifies.

| Decision | Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|--------|
| D-01/D-02 | Handler answers every subclass; event name = snake_cased class | unit | `pytest tests/unit/test_error_handlers.py -x` | ❌ W0 | ⬜ pending |
| D-02 | Event-name set matches a hand-written literal | unit | `pytest tests/unit/test_rejection_vocabulary.py -x` | ❌ W0 | ⬜ pending |
| D-03 | Unmapped constraint still re-raises (500 tripwire) | unit | `pytest tests/unit/test_conflict_classification.py -x` | ✅ (rewire) | ⬜ pending |
| D-04 | Consuming rejections consume **and commit** | unit + schema | `pytest tests/unit/test_create_user_precedence.py::TestEveryProviderStageRejectionConsumes -x` | ✅ | ⬜ pending |
| D-05 | Savepoint rollback leaves consume committable | schema | `pytest -m schema tests/schema/test_create_atomicity.py -x` | ✅ (rewire) | ⬜ pending |
| D-06/D-07 | Admission matrix unchanged | e2e | `pytest -m e2e tests/e2e/test_admission.py -x` | ✅ zero-edit gate | ⬜ pending |
| D-07 | Wire contract unchanged | unit + e2e | `pytest tests/unit/test_wire_contract.py -x` | ✅ | ⬜ pending |
| D-09/D-10 | 3 attempts; terminals spend none; exhaustion → 503 | unit | `pytest tests/unit/test_firebase_retry.py -x` | ✅ (rewire) | ⬜ pending |
| D-10 | Classification + email rule preserved | unit | `pytest tests/unit/test_provider_classifier.py -x` | ✅ (move to adapter level) | ⬜ pending |
| D-11/D-16 | Rejection **precedence** unchanged | unit | `pytest tests/unit/test_create_user_precedence.py -x` | ✅ zero-edit gate (status/body literals) | ⬜ pending |
| D-13 | `copy` gone; registry still total | unit | `pytest tests/unit/test_error_registry.py -x` | ✅ (drop 2 cases) | ⬜ pending |
| D-14 | Five rejections → one 409 body | unit | `pytest tests/unit/test_create_user_precedence.py::TestTheFiveChallengeRejections -x` | ✅ | ⬜ pending |
| D-15 | Pre-claim rejections roll back, consume nothing | unit | `pytest tests/unit/test_create_user_body.py tests/unit/test_challenge_ids.py -x` | ✅ (fix `get_db` double — RESEARCH Pitfall 4) | ⬜ pending |
| D-12/D-18 | Package shape recorded | unit | `pytest tests/unit/test_auth_package_shape.py -x` | ✅ **needs a decision** (class ceiling — RESEARCH Open Question 1) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/test_error_handlers.py` — the `AuthRejected` handler: every subclass answers its `error_class`; exactly one log line; `route` is the path template; negative control proving an unregistered exception still reaches the 500 handler
- [ ] `tests/unit/test_rejection_vocabulary.py` — enumerate `AuthRejected.__subclasses__()` transitively; derived event-name set equals a hand-written literal; every subclass overrides `error_class`
- [ ] Regression test for RESEARCH Pitfall 1: raise a challenge rejection through a real `get_db` generator and assert 409, not 500 (the guard that would have caught CR-02)

No framework install needed.

---

## Manual-Only Verifications

All phase behaviors have automated verification. (One human *decision* is required — the `test_auth_package_shape.py` class-count ceiling — but it is a planning decision, not a manual test.)

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 100s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
