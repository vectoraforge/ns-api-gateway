---
phase: 37.3-machine-generated-code-refactoring-part-2
verified: 2026-08-27T09:06:09Z
status: passed
score: 19/19 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 37.3: Machine-generated code refactoring, part 2 — Verification Report

**Phase Goal:** Kill the last machine-generated pattern in `auth/` — functions that *return* a
rejection vocabulary — replacing it with a DRF-style exception family raised where the rejection
is discovered and answered by one FastAPI handler, with zero client-visible change.

**Verified:** 2026-08-27T09:06:09Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (derived from D-01…D-19 and the four plans' must_haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A raised `AuthRejected` subclass reaches one handler by MRO and answers with that class's `error_class`, status/body byte-identical (D-01) | ✓ VERIFIED | `app/errors.py:85` registers `add_exception_handler(AuthRejected, auth_rejected_handler)`; `tests/unit/test_exception_handlers.py::REJECTION_CASES` drives it through `TestClient` for 3 rejection classes, asserting status/body |
| 2 | The handler emits exactly one WARNING named by `camel_to_snake(type(exc).__name__)`, carrying `route` and never a challenge handle (D-02) | ✓ VERIFIED | `app/errors.py:47-58` `auth_rejected_handler`; `log_fields()` on every `AuthRejected` subclass returns only plain scalars (`exceptions.py`); verified `camel_to_snake` output directly: `PreAuthIdentityNotAllowed`→`pre_auth_identity_not_allowed`, `IdentityAlreadyLinked`→`identity_already_linked` |
| 3 | `create_account` raises instead of returning an outcome enum; a rejection after the claim still consumes the challenge and COMMITs before the client is answered (D-04) | ✓ VERIFIED | `creation.py` raises `IdentityAlreadyLinked`/`AccountUnavailable`/`ProviderAccountAlreadyLinked`; `routers/auth.py::_complete`'s `try/except AuthRejected` around lookup+create_account calls `_consume_and_commit` then bare `raise` |
| 4 | An IntegrityError with an unmapped constraint name still re-raises the original error → 500, never a benign 409 (D-03) | ✓ VERIFIED | `creation.py:113-115` re-raises unchanged `conflict` when `name` matches neither mapped constraint; `tests/unit/test_conflict_classification.py::test_an_unknown_constraint_name_is_re_raised` and `::test_the_unmapped_arm_is_not_a_member_of_the_rejection_family` pass |
| 5 | The single savepoint, rollback-before-classify ordering, and `resolve_existing_identity` pre-check are unchanged (D-05) | ✓ VERIFIED | `creation.py:94-115` — `begin_nested()`, `savepoint.rollback()` before classification; `resolve_existing_identity` called before insert at line 39 |
| 6 | `resolve_identity` raises directly; `Admit`/`Reject`/`AdmissionDecision` no longer exist (D-06) | ✓ VERIFIED | `identity.py` raises `PreAuthIdentityNotAllowed`/`IdentityUnresolvable`/`AccountUnavailable`; `grep -rn "Admit\|AdmissionDecision" src/ tests/` → 0 hits |
| 7 | The handler is the only rejection logging site — no thin re-logging catch in `dependencies.py` (D-06) | ✓ VERIFIED | `app/dependencies.py` has no `except`/logger call around `resolve_identity`/`get_request_context`; `_reject` deleted (`grep -rn "_reject\b" src/` → 0 hits in code, 1 comment cross-reference) |
| 8 | Absent-token/failed-verify arms raise `InvalidExternalJwt` inline from `get_request_context`; `wire.py`/`verification.py` untouched (D-07) | ✓ VERIFIED | `dependencies.py:43-50`; `git log` shows no commit touching `wire.py`/`verification.py` since phase 37.1 — zero diff across all of 37.3's commits |
| 9 | `get_quota_gate`'s unreachable pre-auth guard and `AuthenticationError` survive, fail closed (D-08) | ✓ VERIFIED | `dependencies.py:102-112` raises `AuthenticationError` when identity isn't `LinkedIdentity` |
| 10 | The provider read returns `VerifiedProviderIdentity` or raises; no result type crosses the seam (D-09) | ✓ VERIFIED | `adapters.py` defines only `VerifiedProviderIdentity` + Protocol; `grep -rn "ProviderDataOutcome\|ProviderDataEntry\|ProviderDataResult"` → 0 hits in src/ (1 historical mention in a test docstring) |
| 11 | `_read` resolves the identity inline and applies the unchanged email rule (D-10) | ✓ VERIFIED | `firebase.py:82-131`, `_resolve_provider` and `_verified_email` inlined into `_read`'s call chain |
| 12 | 3 attempts is the whole retry budget; `UserNotFound`/`NotLinked` spend none; exhaustion → 503 (D-10) | ✓ VERIFIED | `firebase.py:158-165` `retry_if_exception_type(RetryableLookupError)` — only that marker retries; `UserNotFound`/`NotLinked` raised directly, bypassing retry; `_exhausted` raises `Unavailable` |
| 13 | Issuer-selection failure fails closed to 503, never an ambient app (D-10) | ✓ VERIFIED | `firebase.py:74-77` raises `Unavailable(stage="issuer_selection")` with no call made when `self._apps.get(issuer)` is `None` |
| 14 | The lookup exception never escapes `_complete` uncaught — consumes+commits before re-raise (D-04/D-11) | ✓ VERIFIED | `routers/auth.py:121-144` — one `try` wraps both `lookup_with_retry` and `create_account`, `except AuthRejected` (covers `ProviderLookupError`, a subclass) |
| 15 | `AuthEventResult` no longer exists anywhere in src/ or tests/; `AuthOperation` survives (D-12) | ✓ VERIFIED | `grep -rn "AuthEventResult" src/ tests/` → 0 hits; `models/auth.py:16` `class AuthOperation(StrEnum)` intact |
| 16 | D-13 sweep: `get_preauth_identity`, `ErrorClass.copy`, `ChallengeRejection` enum, `AuthRejectionError` are gone | ✓ VERIFIED | All four `grep -rn` → 0 hits in src/ and tests/; `ErrorClass` dataclass (`errors.py:29-33`) has only `name`/`status`/`code` |
| 17 | All five challenge rejections answer exactly one 409 body — structurally, via the shared base declaring the class (D-14) | ✓ VERIFIED | `exceptions.py:180-215` — `ChallengeRejected` declares `error_class = CHALLENGE_REQUIRED`; none of the 5 leaves overrides it or defines `__init__`; `tests/unit/test_rejection_vocabulary.py::TestTheChallengeArmsAnswerOneThingAndDeclareNothing` (incl. a mutation-testing control) passes |
| 18 | Pre-claim rejections escape to the handler with no local catch; `get_db` rolls back, nothing consumed (D-15) | ✓ VERIFIED | `routers/auth.py:95-116` — `ChallengeNotFound`/`verify_binding`'s raises/`ChallengeOperationMismatch`/`ChallengeExpired`/`ChallengeConsumed` all raise with no surrounding `try`; `get_db` (`dependencies.py:24-31`) rolls back on any exception |
| 19 | The rejection precedence order in `_complete` is unchanged; zero edits to any status/body literal in the pinning suites (D-16/D-18) | ✓ VERIFIED | `git diff` across every 37.3 commit touching `tests/unit/test_create_user_precedence.py` and `tests/e2e/test_admission.py` shows no added status-code/`"code"` literal; `test_admission.py` was **never touched** by any 37.3 commit (`git log` shows last edit in phase 37.1) |

**Score:** 19/19 truths verified (0 present-but-behavior-unverified)

### Independently Reproduced Test Evidence

Re-ran (not trusted from SUMMARY) from the current worktree:

| Suite | Command | Result | Matches claimed count |
|---|---|---|---|
| Unit | `pytest tests/unit -q` | **788 passed** | Yes |
| E2E | `pytest tests/e2e -q -m e2e` | **198 passed** | Yes |
| Schema | `pytest tests/schema -q -m schema` | **116 passed** | Yes |
| Targeted | `pytest tests/unit/test_create_user_precedence.py tests/unit/test_rejection_vocabulary.py tests/unit/test_auth_package_shape.py tests/unit/test_conflict_classification.py -q` | 92 passed | — |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/nativespeaker/api/auth/exceptions.py` | `AuthRejected` base + full family (admission/creation/lookup/challenge arms) | ✓ VERIFIED | 17 classes present, grouped by arm per the file's own convention (D-17) |
| `src/nativespeaker/api/auth/creation.py` | `create_account` raises | ✓ VERIFIED | No return of an outcome type; raises 3 exception classes |
| `src/nativespeaker/api/auth/identity.py` | `resolve_identity` raises | ✓ VERIFIED | No `Admit`/`Reject` union; raises 3 exception classes |
| `src/nativespeaker/api/auth/adapters.py` | `VerifiedProviderIdentity` + Protocol | ✓ VERIFIED | Frozen/slotted dataclass; one-method Protocol |
| `src/nativespeaker/api/auth/firebase.py` | `_read` raises; retry by exception type | ✓ VERIFIED | `retry_if_exception_type(RetryableLookupError)`; classifier/email rule inlined |
| `src/nativespeaker/api/auth/challenges.py` | `verify_binding` raises | ✓ VERIFIED | Returns `None`, raises `ChallengeIdentityMismatch`/`ChallengeConsumed`; `issue`/`locate`/`claim`/`consume` untouched in signature/return type |
| `src/nativespeaker/api/routers/auth.py` | try/except choreography replaces mapping tables | ✓ VERIFIED | `_LOOKUP_REJECTIONS`, `_consuming_rejection`, `_completion_response`, `_challenge_rejected` all absent |
| `src/nativespeaker/api/app/errors.py` | `AuthRejected` handler + `camel_to_snake` | ✓ VERIFIED | Both present and registered |
| `src/nativespeaker/api/app/dependencies.py` | `get_request_context` raises inline; `_reject`/`get_preauth_identity` gone | ✓ VERIFIED | Confirmed by grep and read |
| `src/nativespeaker/api/errors.py` | `ErrorClass.copy` and `AuthRejectionError` gone | ✓ VERIFIED | `ErrorClass` has 3 fields only |
| `src/nativespeaker/api/models/auth.py` | `AuthEventResult` gone, `AuthOperation` intact | ✓ VERIFIED | Confirmed by grep and read |
| `.planning/REQUIREMENTS.md` | FOUND-08 amended, dated, accurate (D-19) | ✓ VERIFIED | See dedicated section below |
| `.planning/ROADMAP.md` | 37.3 stub reconciled with real goal | ✓ VERIFIED | Entry at line 371 states the real goal, not `TBD` |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `app/errors.py::register_exception_handlers` | `auth_rejected_handler` | `add_exception_handler(AuthRejected, ...)` | ✓ WIRED | Line 85, ordered before the generic `Exception` handler |
| `auth/creation.py::create_account` raise | `routers/auth.py::_complete` except arm | `except AuthRejected` | ✓ WIRED | `_complete`'s single try wraps both `lookup_with_retry` and `create_account` |
| `firebase.py::_read` raise | `AsyncRetrying(retry_if_exception_type(RetryableLookupError))` | tenacity retry predicate | ✓ WIRED | Only `RetryableLookupError` retries; terminal rejections propagate on attempt 1 |
| `challenges.py::verify_binding` raise | `get_db` rollback → handler | no local catch | ✓ WIRED | Confirmed no `try` wraps the pre-claim block in `_complete` |
| `camel_to_snake(type(exc).__name__)` | structured log event | `logger.warning(...)` | ✓ WIRED | Verified output for 3 representative class names, incl. the documented `PreAuthIdentityNotAllowed` divergence |

### FOUND-08 Requirement Accuracy (REQUIREMENTS.md, D-19)

`.planning/REQUIREMENTS.md:76-80` amends FOUND-08 a second time, dated 2026-08-26, and its claims were checked against the code:

- "`ProviderDataOutcome`, `ProviderDataEntry` and `ProviderDataResult` are deleted" — confirmed, 0 hits in src/.
- "The seam carries one immutable value type, `VerifiedProviderIdentity(provider, provider_uid, email)`, frozen and slotted" — confirmed verbatim in `adapters.py`.
- "a `ProviderLookupError` hierarchy — `UserNotFound` (→ `AUTH_REQUIRED`), `Unavailable` (→ `VERIFICATION_TEMPORARILY_UNAVAILABLE`) and `NotLinked` (→ `OPERATION_NOT_ALLOWED`)" — confirmed, `error_class` assignments match exactly.
- "`FirebaseAdminAdapter` is still the one-method Protocol; only its return type changed" — confirmed.
- "`tests/unit/test_adapter_interfaces.py::TestNoProviderDependency` still holds" — file exists, part of the green 788-test unit run.
- "gated by an unedited `tests/e2e/test_admission.py`" — confirmed by `git log`: zero commits touch that file since phase 37.1.

FOUND-08's amendment is accurate.

### Anti-Patterns Found

None. Scanned all eleven files this phase modified in `src/` for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/empty-implementation patterns — zero hits (two incidental `return {}` matches are the deliberate no-extra-fields default on `AuthRejected.log_fields()` and the deliberate "no ADC available" fail-closed branch in `build_admin_apps`, neither a stub).

### Code Review Cross-Check

`37.3-REVIEW.md` (already run, not duplicated): 0 critical, 13 warnings, 7 info. Spot-checked WR-02 (`verify_binding`'s bare-statement call site is fail-open to future breakage) and WR-06 (post-commit ORM read depends on `expire_on_commit=False`, set in an untouched file) — both are forward-looking robustness concerns about *future* regressions, not evidence that today's client-visible behavior differs. Neither contradicts any must-have truth above. Judged independently per the execution notes; not re-litigated here.

### Deferred Items

None — everything D-01…D-19 required for this phase was found in the codebase; nothing was pushed to a later phase beyond what CONTEXT.md itself already flagged forward (phases 38–46's adapter-seam and audit-mechanism decisions, unaffected by this phase's scope).

### Requirements Coverage

No new requirement IDs are declared by this phase (`requirements: []` in all four PLAN.md frontmatters, matching the phase's own statement that it only amends FOUND-08). FOUND-08's amendment is verified accurate above. No orphaned requirement IDs map to phase 37.3 in REQUIREMENTS.md's phase map.

### Human Verification Required

None. Every must-have truth was verifiable by source inspection, targeted grep, and independently reproduced automated test runs (unit/e2e/schema, all matching the executor's claimed pass counts exactly).

### Gaps Summary

No blocking gaps. One documentation-accuracy observation, below the bar of a phase-goal gap:

**Informational — stale `_reject` references survive outside FOUND-08's entry.** `37.3-04-SUMMARY.md` claims "Two stale references in REQUIREMENTS.md outside FOUND-08 were corrected in place" and indeed exactly two were (SCHEMA-06's note and FOUND-05's note, both carrying dated "Corrected by Phase 37.3" annotations). However, three further entries still describe `app/dependencies.py::_reject` and the single `auth_rejected` event as the *current* mechanism, uncorrected:
- **REBIND-02** (`.planning/REQUIREMENTS.md` ~line 92): "Rejections keep their stable internal result in the structured security log because D-03 deliberately kept the rejection log — `app/dependencies.py::_reject` since Phase 37.2 (D-10)"
- **PROF-02** (~line 105): "the `auth_rejected` event, emitted by `app/dependencies.py::_reject` since Phase 37.2 (D-10)"
- **SYNC-03** (~line 127): "the rejection log in `app/dependencies.py::_reject` already covers the rejection arm"

`_reject` and the single `auth_rejected` event are both deleted by this phase (confirmed by grep — 0 hits in `src/`); the mechanism these three entries point to no longer exists. This is **not** a failure of any must-have or D-01…D-19 decision — D-19 only mandated amending the FOUND-08 entry, which is accurate, and none of these three requirements' *substance* is affected (the compensating log still exists, just under `auth_rejected_handler` now). It is a documentation-consistency loose end that phases 38 and 46 (which inherit SYNC-03 and a decision analogous to it) will read and find pointing at deleted code. Recommend a small follow-up dated note the next time REQUIREMENTS.md is touched; does not block this phase.
