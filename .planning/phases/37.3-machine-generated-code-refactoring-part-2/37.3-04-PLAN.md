---
phase: 37.3-machine-generated-code-refactoring-part-2
plan: 04
type: execute
wave: 4
depends_on:
  - 37.3-03
files_modified:
  - src/nativespeaker/api/auth/exceptions.py
  - src/nativespeaker/api/auth/challenges.py
  - src/nativespeaker/api/routers/auth.py
  - src/nativespeaker/api/models/auth.py
  - tests/unit/test_create_user_body.py
  - tests/unit/test_create_user_precedence.py
  - tests/unit/test_challenge_ids.py
  - tests/unit/test_auth_package_shape.py
  - tests/unit/test_rejection_vocabulary.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "verify_binding raises instead of returning a rejection; issue, locate, claim and consume are unchanged and still return bool or a row (D-14/D-15)."
    - "All five challenge rejections answer exactly one 409 body, and that property is structural — the shared base declares the error class and no subclass overrides it (D-14)."
    - "Each of the five logs as its own event, so the internal distinction survives in the log while the client sees one answer (D-02/D-14)."
    - "Pre-claim rejections escape to the handler with no local catch; get_db rolls the session back and nothing is consumed (D-15)."
    - "The rejection precedence order in _complete is unchanged, proven by the precedence suite with zero edits to any status code or body literal (D-16/D-18)."
    - "The deliberate commit after the claim is unchanged, and post-claim rejections still consume and commit (D-04)."
    - "AuthEventResult no longer exists anywhere in src/ or tests/; AuthOperation still exists and still backs core.auth_challenges.operation (D-12)."
  artifacts:
    - src/nativespeaker/api/auth/challenges.py
    - .planning/REQUIREMENTS.md
  key_links:
    - "ChallengeRejected base declares CHALLENGE_REQUIRED; the five subclasses declare nothing — the anti-oracle property is structural"
    - "pre-claim raise -> no local catch -> get_db rollback -> auth_rejected_handler -> one 409 body"
---

<objective>
Convert the challenge binding — D-17's directive 3.2, deliberately last and never bundled with the
Firebase refactor (D-16) — then delete the outcome enum and reconcile the project documents.

Purpose: the rejection precedence order in `_complete` **is** the spec of that block, it is pinned by a
526-line suite, and the transactional choreography must survive exactly. It changes for one reason at a
time, and this is that reason.

Output: `verify_binding` raising; the five subclasses under one 409 base; `_challenge_rejected` gone;
`AuthEventResult`'s 44 members gone; FOUND-08 amended and the ROADMAP stub reconciled.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-CONTEXT.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-RESEARCH.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-PATTERNS.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-03-SUMMARY.md
</context>

## Artifacts this phase produces

See `37.3-01-PLAN.md` § "Artifacts this phase produces" for the full list of symbols phase 37.3 creates.
This plan adds these members of that list: `ChallengeRejected`, `ChallengeNotFound`, `ChallengeExpired`,
`ChallengeConsumed`, `ChallengeIdentityMismatch`, `ChallengeOperationMismatch` (all in
`src/nativespeaker/api/auth/exceptions.py`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify_binding raises; the anti-oracle property moves into a shared base</name>
  <files>
    src/nativespeaker/api/auth/exceptions.py,
    src/nativespeaker/api/auth/challenges.py
  </files>
  <read_first>
    - src/nativespeaker/api/auth/challenges.py (the whole file — the module docstring `:1` stating that this module holds no logger, the `ChallengeRejection` enum `:27-33`, `issue`/`locate`/`claim`/`consume`, and `verify_binding` `:108-125`)
    - src/nativespeaker/api/auth/exceptions.py (as it stands after plan 03 — the base, `log_fields()`, the module ordering convention)
    - src/nativespeaker/api/errors.py (`AnalysisError` / `TransientLLMError` at `:249-264` — the two-level hierarchy precedent, and note that the existing pair re-declares `error_class` on the child, which is exactly what this family must NOT do)
    - src/nativespeaker/api/models/auth.py (`AuthChallenge` `:104-113` — the handle is a secret capability, body-only transport)
  </read_first>
  <behavior>
    - A challenge bound to an external identity that does not match the caller raises `ChallengeIdentityMismatch`.
    - A pre-auth challenge whose `preauth_subject_hash` is cleared raises `ChallengeConsumed`.
    - A pre-auth challenge whose `preauth_issuer` differs, or whose keyed subject comparison fails, raises `ChallengeIdentityMismatch`.
    - A matching binding returns `None` and raises nothing.
    - All five subclasses resolve `error_class` to `CHALLENGE_REQUIRED` (409) and none declares one of its own.
    - Each of the five produces a distinct log event name: `challenge_not_found`, `challenge_expired`, `challenge_consumed`, `challenge_identity_mismatch`, `challenge_operation_mismatch`.
    - The client body for all five is byte-identical.
  </behavior>
  <action>
    **`auth/exceptions.py`.** Append a *challenge arms* group in the position the module ordering
    convention declares. `ChallengeRejected(AuthRejected)` declares `error_class = CHALLENGE_REQUIRED`
    and nothing else. Then five subclasses with **no body but a one-line docstring** —
    `ChallengeNotFound`, `ChallengeExpired`, `ChallengeConsumed`, `ChallengeIdentityMismatch`,
    `ChallengeOperationMismatch` — and, critically, **none of them declares an `error_class`**.

    That omission is the design, not laziness, and it is a deliberate divergence from the
    `AnalysisError`/`TransientLLMError` precedent in `errors.py`, which does re-declare on the child.
    Carry a comment on `ChallengeRejected` saying why: completion must not become a
    challenge-enumeration oracle, so the one 409 answer for all five lives in the shared base where no
    future edit can make one of them answer differently without deliberately overriding it. Because the
    five carry no `__init__` and no fields, none can leak the handle into a log line either.

    Note for the vocabulary test's leaf rule: each of the five inherits `error_class` from
    `ChallengeRejected`, which is a strict ancestor other than `AuthRejected` — so the rule is satisfied
    without any of them declaring one.

    **`auth/challenges.py`.** Delete the `ChallengeRejection` enum (D-13 — its last consumer is being
    converted here) and drop the now-unused `StrEnum` import if nothing else in the module uses it.

    Change `verify_binding`'s return annotation to `None` and its docstring to say it returns when the
    binding matches and raises the rejection it earned otherwise. Substitute one for one: each
    `return ChallengeRejection.x` becomes `raise ChallengeX()`, and each `return None` stays. The
    keyed comparison stays inside the keyring exactly as it is — `self._keyring.actor_subject_matches(...)`
    is unchanged and D-14 converts only the *return* of this method, never the comparison. Keep the
    pre-auth arm's comment about a cleared hash never being compared.

    **The module still holds no logger and must not gain one.** Its docstring says a handle is a secret
    capability and that no logger lives here so none is logged; that statement stays true because the
    raise carries no fields and the handler is the only logging site. Do not import `structlog` here.

    `issue`, `locate`, `claim` and `consume` are untouched and keep returning `bool` /
    `AuthChallenge | None` — D-15 is explicit that only `verify_binding` converts.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_rejection_vocabulary.py tests/unit/test_challenge_ids.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^class ChallengeRejection' src/nativespeaker/api/auth/challenges.py` returns `0`. <!-- planner-discipline-allow: class ChallengeRejection -->
    - `src/nativespeaker/api/auth/challenges.py` still holds no logger: `grep -cE 'structlog|logger' src/nativespeaker/api/auth/challenges.py` returns `0`.
    - The four unconverted store methods survive: `grep -cE '(async )?def (issue|locate|claim|consume)\(' src/nativespeaker/api/auth/challenges.py` returns `4`.
    - The anti-oracle property is structural: `.venv/bin/python -c "import nativespeaker.api.auth.exceptions as e; subs=[e.ChallengeNotFound,e.ChallengeExpired,e.ChallengeConsumed,e.ChallengeIdentityMismatch,e.ChallengeOperationMismatch]; assert all('error_class' not in c.__dict__ for c in subs), 'a subclass declares its own error_class'; assert all(c.error_class is e.ChallengeRejected.error_class for c in subs); assert e.ChallengeRejected.error_class.status==409; print('structural')"` prints `structural`.
    - Behaviour: all five classes produce identical response bodies and status; their `camel_to_snake` event names are five distinct strings.
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>`verify_binding` raises, the five rejections share one 409 declared once in their base, and the store's other four methods and the module's no-logger rule are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The precedence block becomes raises; pre-claim rejections escape to the handler</name>
  <files>
    src/nativespeaker/api/routers/auth.py,
    tests/unit/test_create_user_body.py,
    tests/unit/test_create_user_precedence.py
  </files>
  <read_first>
    - src/nativespeaker/api/routers/auth.py (the whole file as it stands after plan 03 — `_complete`'s pre-claim block, the deliberate commit after the claim, `_challenge_rejected`, and the `except AuthRejected` arm)
    - src/nativespeaker/api/app/dependencies.py (`get_db` `:38-45` — the rollback contract D-15 delegates to)
    - tests/unit/test_create_user_body.py (the `get_db` override at `:97` and the two `assert session.rollbacks == 1` assertions at `:119-121` and `:181`)
    - tests/unit/test_create_user_precedence.py (the `client` fixture `:188-199`, its `get_db` override at `:194`, `_assert_challenge_required` `:233-236`, and the precedence-order assertions)
    - src/nativespeaker/api/auth/challenges.py and src/nativespeaker/api/auth/exceptions.py (as written in Task 1)
    - .planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-RESEARCH.md (Pitfall 4)
  </read_first>
  <reversibility rating="costly">The precedence order in this block is the spec of the completion flow, and the transactional asymmetry — pre-claim rollback versus post-claim consume-and-commit — is the difference between a handle that was spent and one that was not. A reordering or a mis-scoped catch is a live bug that every current test may still pass.</reversibility>
  <behavior>
    - No such challenge row raises `ChallengeNotFound`; the request answers 409 and consumes nothing.
    - A binding mismatch raises out of `verify_binding`; a challenge issued for another operation raises `ChallengeOperationMismatch`; both answer 409 and consume nothing.
    - A lost claim raises `ChallengeExpired` when `claimed_at` is still `None` after refresh, `ChallengeConsumed` otherwise; both answer 409 and consume nothing.
    - The five arms fire in exactly today's order: locate, binding, operation, then claim.
    - No pre-claim arm is caught locally; the session is rolled back by `get_db`, and the rollback is observable in the test doubles.
    - The commit immediately after a successful claim is unchanged and still precedes the provider call.
    - Post-claim rejections still consume and commit before being re-raised.
  </behavior>
  <action>
    **`routers/auth.py`.** Delete `_challenge_rejected` and convert its four call sites to bare raises.
    The explicit `await session.rollback()` it performed moves nowhere: `get_db` already rolls the
    session back on any exception, and per-arm rollbacks are how this helper accumulated its coupling in
    the first place. Its `logger.warning("create_user_challenge_rejected", stage=str(result))` line goes
    entirely — the handler is the logging site now. Its docstring's reasoning ("answer every challenge
    rejection with one class, so completion is not a challenge-enumeration oracle") no longer describes
    code here; that property now lives in `ChallengeRejected` and the comment belongs there, not
    duplicated.

    Convert the arms in place, preserving the order exactly — the order below **is** the rejection
    precedence and is the spec of this block:
    the `locate` no-row arm raises `ChallengeNotFound`, keeping its comment that a lookup outage raises
    out of `locate` rather than answering "no such challenge";
    the `verify_binding` call becomes a bare statement, since it raises rather than returning — delete
    the `rejection is not None` branch and the stale comment claiming the rejection values are outcome
    enum values, which becomes factually wrong here;
    the operation check raises `ChallengeOperationMismatch`, keeping the comment that it is a pre-claim
    rejection like the binding mismatch above;
    the lost-claim arm keeps the `await session.refresh(challenge)` probe imperative and ends by raising
    `ChallengeExpired` when `claimed_at` is `None` and `ChallengeConsumed` otherwise — write it as an
    explicit `if`/`else`, and keep the comment that `claimed_at` distinguishes the two losses and that
    the claim's `WHERE` is the only expiry evaluation anywhere.

    **Carry no fields on these raises.** The refresh has just loaded the row, but the exception travels
    through `get_db`'s rollback, which expires every instance in the session; an attribute read in the
    handler would then attempt I/O outside a greenlet, the handler itself would raise, and the client
    would get 500 where 409 was owed. Today's helper logs no row id either, so parity and safety agree.

    The deliberate `await session.commit()` after a successful claim is unchanged, along with its
    comment — it is the boundary between the two halves of the choreography, and an uncommitted claim
    across the provider call would let a second attempt win the challenge. Everything after it stays as
    plan 03 left it.

    **`tests/unit/test_create_user_body.py` and the precedence suite's `client` fixture.** Both override
    `get_db` with a plain callable (`lambda: session`). A plain callable has no `try`/`except`, so
    nothing rolls back — and `test_create_user_body.py` asserts `session.rollbacks == 1` twice, on the
    arm that releases `locate`'s read transaction. Once the explicit rollback moves into `get_db`, those
    assertions fail against a double that never rolls back. Replace both overrides with an async
    generator mirroring `dependencies.py:38-45` — `try: yield session; await session.commit()` /
    `except Exception: await session.rollback(); raise`. Do **not** weaken the assertions to `== 0`:
    the property under test is that the read transaction *is* released, and a faithful double keeps
    testing it. The same double also makes the post-claim path faithful, since the handler-commit then
    rollback-on-exception ordering is exactly what D-04 depends on.

    **`tests/unit/test_create_user_precedence.py`.** Log edits only; zero edits to any status code or
    body literal, and `_assert_challenge_required`'s byte-equality assertion is untouchable. The five
    challenge arms' asserted strings need no change — the snake_cased class names already equal today's
    asserted values — but they now arrive on the handler's logger rather than the router's, which the
    recorder set up in plan 01 already covers. Verify the precedence-order assertions still read in the
    same order, and that `TestTheFiveChallengeRejections` still proves one body for all five.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_create_user_precedence.py tests/unit/test_create_user_body.py tests/unit/test_challenge_ids.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'async def _challenge_rejected' src/nativespeaker/api/routers/auth.py` returns `0`. <!-- planner-discipline-allow: async def _challenge_rejected -->
    - No pre-claim arm rolls back locally: in `_complete`, `session.rollback` does not appear — `awk '/^async def _complete/,/^async def _consume_and_commit/' src/nativespeaker/api/routers/auth.py | grep -c 'session.rollback'` returns `0`.
    - The deliberate commit survives: `grep -c 'await session.commit()' src/nativespeaker/api/routers/auth.py` returns at least `1`, and `_complete` still contains it between the claim and the provider call.
    - The pre-claim raises carry no state: `awk '/^async def _complete/,/^async def _consume_and_commit/' src/nativespeaker/api/routers/auth.py | grep -cE 'raise Challenge[A-Za-z]+\([^)]'` returns `0` — every challenge raise is argument-free.
    - Both test doubles are faithful generators: `grep -c 'lambda: session' tests/unit/test_create_user_body.py tests/unit/test_create_user_precedence.py` returns `0` for both files.
    - The rollback assertions were not weakened: `grep -c 'session.rollbacks == 1' tests/unit/test_create_user_body.py` returns `2`.
    - `tests/unit/test_create_user_precedence.py` shows no status-code or body-literal edit in this plan: `git diff -U0 HEAD~1 -- tests/unit/test_create_user_precedence.py | grep -E '^[+-]' | grep -Ev '^(\+\+\+|---)' | grep -cE '(409|403|401|503|500|"code")'` returns `0`.
    - Behaviour: each of the five challenge rejections answers 409 with a body whose keys are exactly `["code"]` and value `challenge_required`, and consumes nothing — the same handle presented again is still rejected for the same reason.
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>The precedence block raises in unchanged order, pre-claim rejections escape and are rolled back by the dependency, the deliberate commit and the post-claim choreography are intact, and the test doubles now mirror the real generator.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the outcome enum and close the sweep</name>
  <files>
    src/nativespeaker/api/models/auth.py,
    tests/unit/test_auth_package_shape.py,
    tests/unit/test_rejection_vocabulary.py
  </files>
  <read_first>
    - src/nativespeaker/api/models/auth.py (the whole file — `AuthEventResult` `:27-72`, `AuthOperation`, the request/response models, `AuthChallenge` `:104-113`)
    - tests/unit/test_auth_package_shape.py (the literals and the two assertions)
    - tests/unit/test_rejection_vocabulary.py (the hand-written event-name set)
    - .planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-01-SUMMARY.md (the recorded ceiling decision from plan 01 Task 1)
  </read_first>
  <reversibility rating="costly">Reversible in code from git history, but the briefs for phases 38–46 reference this enum's vocabulary; their planners inherit the exception idiom instead. The developer chose this knowingly (D-12).</reversibility>
  <action>
    **`models/auth.py`.** Delete `AuthEventResult` entirely. Note for the SUMMARY: D-12 says "all 43
    members" but the enum has **44** — an off-by-one in the decision text, not a substantive
    disagreement; write 44. Its PostgreSQL backing type was already dropped in phase 37.1, so nothing
    persisted, no column stores any of these values, and no migration is needed.

    **Keep `AuthOperation`** — `core.auth_challenges.operation` uses it (D-12 is explicit) — along with
    the request/response models and `AuthChallenge`, whose secret-handle comments stay byte-identical.

    **Close the sweep.** Search `src/` and `tests/` for every remaining reference to the deleted enum
    and to any helper the four conversions stranded, and delete what has zero consumers (D-13, the
    standing rule — do not bring instances back for discussion). Known survivors that must NOT be swept:
    `_conflicting_constraint_name` in `creation.py` (the constraint-name decision it serves survives
    D-03), `AuthenticationError` and `get_quota_gate`'s guard (D-08), and `AuthOperation`. A stale
    *comment* referencing the deleted vocabulary counts as a reference: rewrite it rather than leaving
    a comment that describes code which no longer exists — a stale comment in a phase whose whole point
    is de-complication is the failure mode to avoid.

    **`tests/unit/test_rejection_vocabulary.py`.** Extend the hand-written event-name literal with the
    six classes Task 1 added, so the file now enumerates the complete family.

    **`tests/unit/test_auth_package_shape.py`.** Update `CURRENT` to the final measured triple. This is
    the plan where the class count breaches `BASELINE`; apply plan 01 Task 1's recorded decision and add
    the note the chosen option calls for. If option-a was chosen, the recorded rationale goes in the file
    beside the widened literal so a reviewer reads the reason in the diff, not just the number — the
    file counts classes as a complexity proxy and that proxy inverts for this refactor, since a set of
    declarative one-line exception classes replaced a 44-member enum, three mapping tables and five
    classifier functions. Do not change what `_measure` counts.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_auth_package_shape.py tests/unit/test_rejection_vocabulary.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/python -m pytest -q -m e2e && .venv/bin/python -m pytest -q -m schema</automated>
  </verify>
  <acceptance_criteria>
    - The enum is gone everywhere: `grep -rn 'AuthEventResult' src/ tests/ | grep -vc '^$'` returns `0`. <!-- planner-discipline-allow: AuthEventResult -->
    - `AuthOperation` survives: `grep -c 'class AuthOperation' src/nativespeaker/api/models/auth.py` returns `1`, and `.venv/bin/python -c "from nativespeaker.api.models.auth import AuthOperation; print(AuthOperation.create_user.value)"` prints `create_user`.
    - The named survivors were not swept: `grep -c 'def _conflicting_constraint_name' src/nativespeaker/api/auth/creation.py` returns `1`; `grep -c 'class AuthenticationError' src/nativespeaker/api/errors.py` returns `1`.
    - The vocabulary test enumerates the complete family: `.venv/bin/python -c "import nativespeaker.api.auth.exceptions as e; s=set(); st=[e.AuthRejected]; \nwhile st:\n c=st.pop(); s.add(c.__name__); st.extend(c.__subclasses__())\nprint(len(s))"` prints `18` (the base plus 17 subclasses), and `tests/unit/test_rejection_vocabulary.py` passes.
    - The app still boots and the registry is still total: `.venv/bin/python -c "from nativespeaker.api.app.lifespan import *; from nativespeaker.api.errors import assert_registry_total; assert_registry_total(); print('boots')"` prints `boots`.
    - The full suite is green across all three selections, and the SUMMARY records the three counts.
    - `tests/unit/test_auth_package_shape.py` records the decision in prose, not only in numbers: the diff to `BASELINE` (if any) is accompanied by an added comment.
  </acceptance_criteria>
  <done>The outcome enum and every stranded reference are gone, the named survivors are intact, the app boots with a total registry, and the package-shape file records this phase's shape change with its reason.</done>
</task>

<task type="auto">
  <name>Task 4: Amend the adapter-seam requirement and reconcile the roadmap stub</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (FOUND-08 and its Phase 37.2 amendment — the surface this phase deleted; and FOUND-05's amendment block, which is the established dated-amendment style to copy)
    - .planning/ROADMAP.md (the Phase 37.3 entry)
    - .planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-CONTEXT.md (D-19, and the Phase Boundary paragraph — the source of the real goal)
    - The three preceding SUMMARYs for this phase
  </read_first>
  <action>
    **`.planning/REQUIREMENTS.md` — FOUND-08 (D-19).** Its Phase 37.2 amendment states that what remains
    in `auth/adapters.py` is the providerData surface the create-user flow actually calls, naming
    `ProviderDataOutcome`, `ProviderDataEntry`, `ProviderDataResult` and the one-method
    `FirebaseAdminAdapter`. This phase replaced that surface. Append a new dated amendment in the
    established style — the same `> **Amended by Phase 37.3 (D-09/D-19), 2026-08-26 — ...**` blockquote
    shape FOUND-05 and the existing FOUND-08 amendment use, not a rewrite of the existing text, which
    stays as the record of what was true until this date. <!-- planner-discipline-allow: ProviderDataOutcome --> <!-- planner-discipline-allow: ProviderDataEntry --> <!-- planner-discipline-allow: ProviderDataResult -->

    Say what replaced it: the seam now carries one immutable value type, `VerifiedProviderIdentity`,
    and the read raises a `ProviderLookupError` hierarchy rather than returning a result. State that
    `tests/unit/test_adapter_interfaces.py::TestNoProviderDependency` still holds and that the seam
    still imports no provider SDK — that was the requirement's actual content and it is still met.
    Carry forward the existing flag to phases 43–46 unchanged, adding D-12's note that those phases name
    their outcomes as exception classes rather than re-growing an outcome enum. Do not mark FOUND-08
    unchecked; it remains satisfied.

    **`.planning/ROADMAP.md` — the Phase 37.3 entry (D-19).** The `**Requirements**: TBD` line is the
    remaining stub: replace it with a statement that this phase carries no new requirement IDs and
    amends FOUND-08 instead, so a later reader does not go looking for IDs that were never minted. The
    Goal, plan count and plan list were already filled in at planning time; verify they match what
    shipped and correct them if any plan was added or renamed during execution. Mark the four plan
    checkboxes complete.

    Use scoped edits — replace only the lines named above. Do not rewrite either file wholesale; both
    hold many entries this phase does not touch.

    **Also record in the phase SUMMARY**, so an operator learns it from the doc rather than from a
    silent dashboard: the three retired log event names (`auth_rejected`,
    `create_user_challenge_rejected`, `create_user_lookup_rejected`) and the per-class events that
    replace them, alongside plan 01 Task 1's recorded answer about log-pipeline alerts. Also record that
    37-REVIEW's WR-01 remains deferred — it is still live in `app/lifespan.py` but this phase touched no
    line of it — and that CR-02 is not live, its site having been deleted in an earlier phase, with its
    failure mode answered structurally by the scalars-only rule.

    **Re-check `SHARED-INVARIANTS.md` before closing.** No new conflict is expected: the client contract
    is unchanged and the structured-log-only rejection record was already the flagged 37.1 position. If
    a conflict is found, record it in `REQUIREMENTS.md` in the established flagged-conflict style rather
    than resolving it silently.
  </action>
  <verify>
    <automated>grep -c 'Amended by Phase 37.3' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c 'TBD' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a new dated amendment: `grep -c 'Amended by Phase 37.3' .planning/REQUIREMENTS.md` returns at least `1`, and it contains `VerifiedProviderIdentity`.
    - The prior amendment was appended to, not overwritten: `grep -c 'Amended by Phase 37.2' .planning/REQUIREMENTS.md` still returns `1`.
    - FOUND-08 is still marked satisfied: `grep -c '\[x\] \*\*FOUND-08\*\*' .planning/REQUIREMENTS.md` returns `1`.
    - The ROADMAP 37.3 entry has no stub left: `awk '/#### Phase 37.3:/,/^#### Phase 38/' .planning/ROADMAP.md | grep -cE 'TBD|\[Urgent work' ` returns `0`.
    - The ROADMAP 37.3 entry lists four plans and all four checkboxes are complete: `awk '/#### Phase 37.3:/,/^#### Phase 38/' .planning/ROADMAP.md | grep -c '\- \[x\] 37.3-0'` returns `4`.
    - Neither file lost unrelated content: `git diff --stat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows only additions and small scoped changes, with no deletion of any other phase or requirement entry.
  </acceptance_criteria>
  <done>FOUND-08 carries a dated amendment naming the replacement surface, the roadmap stub is gone, and the retired log vocabulary, the WR-01 deferral and the invariants re-check are recorded in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `POST /auth/create-user` | The challenge handle crosses here; the completion flow must not become an oracle for challenge state |
| API process → structured log pipeline | The internal distinction between the five rejections lives only here |
| API process → PostgreSQL | The pre-claim/post-claim transactional asymmetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.3-21 | Information Disclosure | The five challenge rejections becoming distinguishable to a client — a challenge-enumeration oracle | high | mitigate | The 409 is declared once on `ChallengeRejected` and no subclass overrides it; Task 1 acceptance asserts `'error_class' not in c.__dict__` for all five. The precedence suite's byte-equality body assertion is a zero-edit gate. |
| T-37.3-22 | Information Disclosure | The secret challenge handle reaching the new handler's log line | high | mitigate | The five classes take no `__init__` and carry no fields, so nothing can be passed; `challenges.py` still imports no logger, gated by a grep; the pre-claim raises are asserted argument-free. |
| T-37.3-23 | Spoofing | A pre-claim rejection that fails to roll back, or a post-claim one that fails to consume | high | mitigate | Pre-claim arms have no local catch and rely on `get_db`; the test doubles are replaced with faithful async generators so `session.rollbacks == 1` keeps testing the property instead of passing vacuously. The post-claim arm is unchanged from plan 03. |
| T-37.3-24 | Elevation of Privilege | The rejection precedence order changing during the conversion | high | mitigate | The arms are converted in place with no reordering; the 526-line precedence suite passes with zero edits to any status code or body literal, and its order assertions are re-read after the edit. D-16 isolated this conversion to its own plan for exactly this reason. |
| T-37.3-25 | Tampering | The keyed subject comparison moving out of the keyring during the raise conversion | high | mitigate | D-14 converts only the *return* of `verify_binding`; `actor_subject_matches` is untouched and the comparison stays inside `HmacKeyring`. |
| T-37.3-26 | Repudiation | Losing the internal distinction between the five rejections | medium | mitigate | Each subclass logs as its own event name (D-02); Task 1 acceptance asserts five distinct event strings. |
| T-37.3-27 | Repudiation | Deleting a persisted enum by mistake while removing the outcome vocabulary | high | mitigate | `AuthOperation` backs `core.auth_challenges.operation` and is explicitly kept; Task 3 acceptance imports it and reads a member. The deleted enum was never persisted and its PostgreSQL backing type was already dropped in phase 37.1. |
| T-37.3-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing; no package-manager step exists in any task. |
</threat_model>

<verification>
Phase gate — the full suite must be green across all three selections before `/gsd:verify-work`:

```
.venv/bin/python -m pytest -q            # expect ~762 passed
.venv/bin/python -m pytest -q -m e2e     # expect ~198 passed
.venv/bin/python -m pytest -q -m schema  # expect ~115 passed
```

Zero-edit gates across the whole phase: `tests/e2e/test_admission.py` unedited;
`tests/unit/test_create_user_precedence.py` with no status-code or body-literal change;
`tests/unit/test_adapter_interfaces.py` unedited; `src/nativespeaker/api/auth/wire.py` and
`src/nativespeaker/api/auth/verification.py` unedited.
</verification>

<success_criteria>
- `verify_binding` raises; the five rejections share one 409 declared once in their base.
- `_challenge_rejected` is gone and no pre-claim arm rolls back locally. <!-- planner-discipline-allow: async def _challenge_rejected -->
- The rejection precedence order and the deliberate post-claim commit are unchanged.
- The test doubles mirror `get_db`'s real generator contract; the rollback assertions were not weakened.
- The outcome enum's 44 members are gone; `AuthOperation` and the named survivors are intact.
- FOUND-08 carries a dated Phase 37.3 amendment; the ROADMAP 37.3 stub is reconciled.
- All three test selections green; every zero-edit gate holds.
</success_criteria>

<output>
Create `.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-04-SUMMARY.md` when done.
Record: the 44-member count (correcting D-12's "43"), the retired log event names and their replacements,
the final package-shape triple and the ceiling decision as applied, the WR-01 deferral and CR-02
disposition, and the result of the `SHARED-INVARIANTS.md` re-check.
</output>
