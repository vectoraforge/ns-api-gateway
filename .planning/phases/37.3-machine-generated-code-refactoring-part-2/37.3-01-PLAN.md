---
phase: 37.3-machine-generated-code-refactoring-part-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nativespeaker/api/auth/exceptions.py
  - src/nativespeaker/api/app/errors.py
  - src/nativespeaker/api/auth/creation.py
  - src/nativespeaker/api/routers/auth.py
  - tests/unit/test_exception_handlers.py
  - tests/unit/test_rejection_vocabulary.py
  - tests/unit/test_auth_package_shape.py
  - tests/unit/test_conflict_classification.py
  - tests/unit/test_create_user_precedence.py
  - tests/schema/test_create_race.py
  - tests/schema/test_create_atomicity.py
autonomous: false
requirements: []
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A raised AuthRejected subclass reaches one handler by MRO and answers with that subclass's error_class — status and body byte-identical to today (D-01)."
    - "The handler emits exactly one WARNING whose event name is the snake_cased exception class name, carrying route and no challenge handle (D-02)."
    - "create_account raises instead of returning an outcome enum; a rejection after the claim still consumes the challenge and COMMITs before the client is answered (D-04)."
    - "An IntegrityError whose constraint name is unmapped still re-raises the original error and becomes a 500 — it is never reported as a benign 409 (D-03)."
    - "The single savepoint, rollback-before-classify ordering, and the resolve_existing_identity pre-check are unchanged (D-05)."
    - "A challenge rejection raised through a real get_db generator answers 409, never 500 (RESEARCH Pitfall 1 — exceptions carry only plain scalars)."
  artifacts:
    - src/nativespeaker/api/auth/exceptions.py
    - tests/unit/test_rejection_vocabulary.py
  key_links:
    - "app/errors.py::register_exception_handlers -> add_exception_handler(AuthRejected, auth_rejected_handler)"
    - "auth/creation.py::create_account raise -> routers/auth.py::_complete except arm -> _consume_and_commit -> bare raise -> handler"
    - "camel_to_snake(type(exc).__name__) -> the structured log event name (the outcome vocabulary)"
---

<objective>
Establish the exception family, its one handler, and convert the consuming transaction (`auth/creation.py`)
to raise — D-17's directive 1, and the phase's tracer slice.

Purpose: prove the whole rejection path end-to-end on one arm before three more plans build on it. A raise
in a domain module must travel through the route, the dependency stack, Starlette's MRO dispatch, and one
handler, and come out as the byte-identical status and body the client sees today.

Output: `auth/exceptions.py` with the base and the creation arms; `auth_rejected_handler` +
`camel_to_snake` in `app/errors.py`; `create_account` raising; `routers/auth.py`'s
`_completion_response` mapping collapsed into try/except choreography; the two Wave 0 test files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-CONTEXT.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-RESEARCH.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-PATTERNS.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-VALIDATION.md
</context>

## Planning provenance

**Spec-less probe fallback: SKIPPED (visible skip).** No SPEC file exists for this phase and the phase
carries no requirement IDs to probe — the ROADMAP lists `TBD` and D-19 makes this phase an *amendment*
to FOUND-08 rather than a new requirement. `must_haves.truths` are therefore derived goal-backward from
the phase goal and the locked decisions D-01…D-19, not from probe predicates. Recorded here rather than
dropped silently.

**Detectors run at planning time, both negative:**
- API coverage (`api-coverage.cjs --json`) → `{"detected": false}`. No external API integration is
  introduced; the Firebase seam this phase reshapes was integrated in a prior phase. No `COVERAGE.md`.
- Assumption-delta (`assumption-delta scan 37.3 --json`) → `{"detected": false}`. No
  singular→plural / required→optional / derived→chosen transition. No `<assumption_delta_decision>` block.
- Schema push gate: no ORM schema files in scope (this project uses raw SQL migrations under
  `migrations/`); this phase touches no migration and no model column. Skipped silently.

**Discretion exercised (CONTEXT.md "Claude's Discretion"), recorded so later plans do not re-litigate:**
1. `camel_to_snake` lives in `app/errors.py`, module-private, beside its only caller — keeping it out
   of `auth/` also keeps it off the package-shape function budget.
2. The `ProviderLookupError` hierarchy **consolidates into `auth/exceptions.py`** as `AuthRejected`
   subclasses rather than living in `adapters.py`. The discretion bullet names this choice explicitly.
   Consequence: one base, one `add_exception_handler` registration, one vocabulary test.
   `VerifiedProviderIdentity` still lives in `adapters.py` — it is the seam's value type. (Plan 03.)
3. `InvalidExternalJwt` is **one** class for both wire arms, with `bounded_reason` distinguishing them
   — matching today's single `AuthEventResult.invalid_external_jwt` for both. (Plan 02.)
4. Exceptions carry **no** `issuer`/`subject`. Today's `Reject` carries `actor_issuer`/`actor_subject`
   and no consumer ever reads them; D-13 deletes zero-consumer payload, and not carrying them removes
   any chance of an identity reaching a log line that never held one.
5. The two `AccountUnavailable` arms stay one class (D-01/D-05 indistinguishability) and stay
   distinguishable **in the log only**, via `cause="historical_identity"` / `cause="blocked_user"` —
   the same bounded-string mechanism D-09 uses for `NotLinked`. (RESEARCH Open Question 3.)
6. `assert_registry_total` needs no change: no arm reads the deleted `copy` field
   (RESEARCH Open Question 6, verified).
7. The constraint-name helper and the two name constants **are** deleted. D-03 names them in its
   delete list and the Canonical References section repeats it; CONTEXT.md is the contract, and a
   locked decision is not re-opened by a planning input. RESEARCH.md and PATTERNS.md each recommended
   sparing the helper — both are inputs, not contracts, and both are superseded here. What survives is
   the **behaviour**, not the symbol: the `__cause__` walk moves inline into the `except IntegrityError`
   arm, which keeps its three-way branch and its 500 tripwire (Task 3).

**37-REVIEW CR-02 / WR-01 disposition (the 37.1/37.2 standing note), verified during planning:**
CR-02's site (`_prepare` + the audit writer) no longer exists — **not live**, nothing to fix in place.
Its *failure mode* recurs structurally and is answered by the scalars-only rule and Task 2's regression
test. WR-01 is still live in `app/lifespan.py` (bare `yield`, no `try/finally`) but this phase touches
no line of it — it stays deferred, and the phase summary must say so.

## Multi-source coverage audit

Every item from all four sources, and the plan that covers it. No item is deferred, simplified, or
reduced to a "v1".

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | ROADMAP 37.3 goal is a stub (`[Urgent work - to be planned]`) | Plan 04 T4 replaces it with the real goal (D-19) |
| REQ | No phase REQ-IDs; FOUND-08's 37.2 amendment names the surface this phase deletes | Plan 04 T4 amends FOUND-08 in the dated style (D-19) |
| CONTEXT | D-01 exception family, base + subclass per outcome | Plan 01 T2 (base + creation arms), 02, 03, 04 (append) |
| CONTEXT | D-02 handler derives the log event from the class name | Plan 01 T2 |
| CONTEXT | D-03 return-value plumbing deleted; 500 tripwire preserved | Plan 01 T3 (creation), 03 T3 (`_LOOKUP_REJECTIONS`), 04 T1 |
| CONTEXT | D-04 consuming rejections consume + COMMIT, then re-raise | Plan 01 T3 (choreography established), 03 T3 (widened) |
| CONTEXT | D-05 savepoint / rollback-first / pre-check / indistinguishability | Plan 01 T3 |
| CONTEXT | D-06 `resolve_identity` raises; `_reject` + `AuthRejectionError` die | Plan 02 T1, T2, T3 |
| CONTEXT | D-07 JWT/wire arm raises inline from `get_request_context` | Plan 02 T2 |
| CONTEXT | D-08 fail-closed guards stay | Plan 02 T2 (explicit non-deletion gate) |
| CONTEXT | D-09 lookup value type + error hierarchy; classifier types die | Plan 03 T1 |
| CONTEXT | D-10 `_read` classifies inline; retry on exception type | Plan 03 T2 |
| CONTEXT | D-11 `_complete` wraps the lookup, never lets it escape | Plan 03 T2 |
| CONTEXT | D-12 `AuthEventResult` dies entirely | Plan 04 T3 |
| CONTEXT | D-13 zero-consumer sweep | Plan 01 T3, 02 T2/T3, 03 T1/T2, 04 T1/T3 |
| CONTEXT | D-14 `verify_binding` raises; five subclasses, one 409 base | Plan 04 T1 |
| CONTEXT | D-15 pre-claim rejections escape; `_challenge_rejected` dies | Plan 04 T2 |
| CONTEXT | D-16 binding conversion is its own plan, last | Plan 04 (this is that plan) |
| CONTEXT | D-17 plan order 1 → 3.1 → 2 → 3.2, one commit each | The four plans, four sequential waves |
| CONTEXT | D-18 test migration preserves statuses/classes/consumption/log presence | Every plan's test tasks |
| CONTEXT | D-19 amend REQUIREMENTS.md FOUND-08 + reconcile ROADMAP 37.3 | Plan 04 T4 |
| RESEARCH | Pitfall 1 — ORM instance on an exception → 500 instead of 409 | Plan 01 T2 (scalars-only rule + regression test) |
| RESEARCH | Pitfall 2 — `RetryError` escaping as 500 | Plan 03 T2 |
| RESEARCH | Pitfall 3 — collapsing the unmapped-constraint tripwire | Plan 01 T3 |
| RESEARCH | Pitfall 4 — plain-callable `get_db` doubles hide the rollback | Plan 04 T3 |
| RESEARCH | Pitfall 5 — default `addopts` deselects e2e + schema | Every plan's plan-level `<verification>` |
| RESEARCH | Pitfall 6 — the precedence suite's log spy points at the router | Plan 01 T4 (once, spanning both loggers) |
| RESEARCH | OQ 1 — package-shape class ceiling cannot be met | Plan 01 T1 (`checkpoint:decision`) |
| RESEARCH | OQ 2 / A3 — who owns the consume on a raising `create_account` | Plan 01 T3 (resolved to D-04's letter, pinned by a test) |
| RESEARCH | OQ 3 — the two `AccountUnavailable` arms in the log | Plan 01 T3 (`cause` field) |
| RESEARCH | OQ 4 / A2 — log-pipeline alerts on the retiring event names | Plan 01 T1 (asked alongside the checkpoint) |
| RESEARCH | OQ 5 — CR-02 / WR-01 disposition | Recorded above; WR-01 stays deferred, noted in the summary |
| RESEARCH | OQ 6 — `assert_registry_total` under the `copy` deletion | Recorded above; no change needed, gated in Plan 02 T3 |
| RESEARCH | `AuthEventResult` has 44 members, not D-12's "43" | Plan 04 T3 (write 44 in the summary) |
| RESEARCH | Naming collision: `test_error_handlers.py` vs existing `test_exception_handlers.py` | Plan 01 T2 (extend the existing file) |

## Artifacts this phase produces

New symbols created by phase 37.3. Nothing in this list exists in the codebase today.

**New files:** `src/nativespeaker/api/auth/exceptions.py`, `tests/unit/test_rejection_vocabulary.py`.

**New classes in `src/nativespeaker/api/auth/exceptions.py`:**
`AuthRejected` (base), `IdentityAlreadyLinked`, `ProviderAccountAlreadyLinked`, `AccountUnavailable`,
`InvalidExternalJwt`, `PreAuthIdentityNotAllowed`, `IdentityUnresolvable`, `ProviderLookupError` (base),
`UserNotFound`, `Unavailable`, `NotLinked`, `ChallengeRejected` (base), `ChallengeNotFound`,
`ChallengeExpired`, `ChallengeConsumed`, `ChallengeIdentityMismatch`, `ChallengeOperationMismatch`.

**New classes elsewhere:** `VerifiedProviderIdentity` (`auth/adapters.py`),
`RetryableLookupError` (`auth/firebase.py`).

**New functions/methods:** `AuthRejected.log_fields` (base) and its subclass overrides;
`camel_to_snake` and `auth_rejected_handler` (`app/errors.py`);
`_consume_and_commit` (`routers/auth.py`); `_resolve_provider`, `_verified_email`, `_exhausted`
(`auth/firebase.py`).

**New CLI flags / dataclass fields / env vars / migrations:** none.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: The auth package-shape class ceiling (one-way ratchet release)</name>
  <read_first>
    - tests/unit/test_auth_package_shape.py (the whole file — 2 literals, 2 assertions)
    - .planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-RESEARCH.md (Open Question 1)
  </read_first>
  <reversibility rating="one-way">Widening `BASELINE` releases a ratchet whose entire stated purpose is that it only tightens; once released, the recorded historical shape is gone and no later phase can re-derive it.</reversibility>
  <decision>
    `tests/unit/test_auth_package_shape.py` asserts the auth package measures strictly below
    `BASELINE = (14, 28, 57)` on every axis, and exactly `CURRENT = (10, 19, 44)`. This phase provably
    cannot meet the class ceiling. How should that be resolved?
  </decision>
  <context>
    Measured today: 10 modules, 19 classes, 44 functions — matching `CURRENT` exactly.

    The arithmetic for this phase: 6 classes leave `auth/` (`ProviderDataOutcome`, `ProviderDataEntry`,
    `ProviderDataResult`, `ChallengeRejection`, `Admit`, `Reject`) leaving 13; 19 arrive (17 exception
    classes, `VerifiedProviderIdentity`, `RetryableLookupError`), landing at **32**. The ceiling is
    strictly-below 28, i.e. 27. Modules go 10 → 11 (< 14, fine) and functions stay below 57 (fine).
    **Classes is the only failing axis, and it fails by 5.** The breach lands in plan 04; this decision
    is taken now because option (b) changes plan 01's file path.

    D-18 authorizes updating "its shape literals" — that covers `CURRENT`. It does not authorize
    relaxing the separate `BASELINE` assertion, whose stated purpose is that widening it is
    "an edit someone makes on purpose and a reviewer sees in the diff". D-01 locks the new module's
    path to `src/nativespeaker/api/auth/exceptions.py`, i.e. inside the measured package.

    **Also confirm, one line (RESEARCH assumption A2, unverifiable from this repo):** does any deployed
    log-pipeline alert or dashboard key on the literal event names `auth_rejected`,
    `create_user_challenge_rejected`, or `create_user_lookup_rejected`? D-02 retires all three. If yes,
    the dashboards go silent at deploy with no error; the phase summary needs to name the replacement
    events. If no, nothing further is needed.
  </context>
  <options>
    <option id="option-a">
      <name>Update both literals, with a recorded note in the file</name>
      <pros>Honest; respects locked D-01; is exactly the visible edit the file was built to force. The defensible rationale: the file counts classes as a complexity proxy, and that proxy inverts here — 17 declarative one-line exception classes replace a 44-member enum plus three mapping tables plus five classifier functions.</pros>
      <cons>Releases a one-way ratchet. A later phase can no longer see the pre-trim shape.</cons>
    </option>
    <option id="option-b">
      <name>Relocate the exception family outside `auth/` (e.g. into `api/errors.py`, beside `ErrorClass` and `ServiceError`)</name>
      <pros>Keeps the ratchet intact; arguably better cohesion, since `ServiceError` and the registry already live there.</pros>
      <cons>Contradicts locked D-01, which names `src/nativespeaker/api/auth/exceptions.py`. Requires the developer to amend D-01 before plan 01 writes a file.</cons>
    </option>
    <option id="option-c">
      <name>Change what the file measures (exclude exception `ClassDef`s from the class count)</name>
      <pros>No literal changes; the ratchet nominally survives.</pros>
      <cons>Special-cases the metric so it agrees with whatever is convenient — the failure mode the file's own docstring warns against. Not recommended.</cons>
    </option>
  </options>
  <verify>
    <human-check>Developer states the chosen option and answers the log-pipeline question.</human-check>
  </verify>
  <acceptance_criteria>
    - The chosen option id is recorded in the plan SUMMARY under a "Decisions" heading, with the developer's rationale.
    - The log-pipeline answer (yes/no) is recorded in the plan SUMMARY.
    - If option-b is chosen, execution HALTS and the orchestrator is told D-01 needs amending before any file is written.
  </acceptance_criteria>
  <resume-signal>Select: option-a, option-b, or option-c — and answer the log-pipeline question (yes/no).</resume-signal>
  <done>The developer has chosen an option and answered the log-pipeline question; both are recorded.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end "a raised rejection becomes its client response" — one path, wired through every layer</name>
  <files>
    src/nativespeaker/api/auth/exceptions.py,
    src/nativespeaker/api/app/errors.py,
    tests/unit/test_exception_handlers.py,
    tests/unit/test_rejection_vocabulary.py,
    tests/unit/test_auth_package_shape.py
  </files>
  <read_first>
    - src/nativespeaker/api/errors.py (the `ServiceError` hierarchy at `:229-236`, the subclass shapes at `:249-264`, `:312-313`, `:394-397`, `error_response` at `:57-61`, and the eight `ErrorClass` constants this family imports)
    - src/nativespeaker/api/app/errors.py (the whole file — `service_error_handler` at `:22-29` is the shape to copy; `http_exception_handler` at `:37-46` is the model for a handler that does work before responding; `register_exception_handlers` at `:54-58`)
    - src/nativespeaker/api/app/dependencies.py (`get_db` at `:38-45` — the real generator the regression test must drive through; `_reject` at `:81-88` — the log field vocabulary being relocated)
    - tests/unit/test_exception_handlers.py (the whole file — the `CASES` table, `_make_raise_route`, and the `handler_client` fixture to extend)
    - tests/unit/test_auth_package_shape.py (the literals at `:17-21` and the two assertions)
    - src/nativespeaker/api/models/auth.py (`AuthChallenge` at `:104-113` — why the public handle is never a log field)
    - .planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-PATTERNS.md (Pattern Assignments for `exceptions.py` and `app/errors.py`)
  </read_first>
  <behavior>
    - A route raising `IdentityAlreadyLinked` answers HTTP 409 with body `{"code": "identity_already_linked"}` and exactly one key.
    - A route raising `ProviderAccountAlreadyLinked` answers 403 `{"code": "operation_not_allowed"}`.
    - A route raising `AccountUnavailable(cause="blocked_user")` answers 403 `{"code": "account_unavailable"}`.
    - Each of the three emits exactly one WARNING log record, whose event name is the snake_cased class
      name (`identity_already_linked`, `provider_account_already_linked`, `account_unavailable`) and
      whose fields include `route` set to the path template.
    - An exception raised from a **router-level dependency** (not the route body) reaches the same
      handler and records the same `route` value.
    - Regression control (RESEARCH Pitfall 1): a subclass raised from a route whose session comes from a
      real `get_db`-shaped async generator — so `session.rollback()` runs on the way out — still answers
      its own status, never 500.
    - Negative control: an exception that is not an `AuthRejected` subclass still reaches the generic
      500 handler and answers `{"code": "internal_error"}`.
    - Vocabulary control: a synthetic `AuthRejected` leaf that declares no `error_class` and inherits
      none from an intermediate base is reported as a defect by the vocabulary test.
  </behavior>
  <action>
    Honour the Task 1 outcome. If option-b was chosen, HALT — the module path is no longer this one.

    **`src/nativespeaker/api/auth/exceptions.py` (new).** Copy the class-attribute idiom from
    `errors.py:229-236` verbatim in shape: base `AuthRejected(Exception)` with
    `error_class: ErrorClass = INTERNAL_ERROR` as a class annotation-with-default. `INTERNAL_ERROR` is
    the fail-closed control, not a convenience default — a subclass that forgets to declare one answers
    500, never 200 and never a weaker class; say that in a one-line comment above the class in the
    37.1 D-16 prose register. Do NOT subclass `ServiceError`: this family gets its own handler because
    `service_error_handler` takes `_: Request` and cannot supply `route`.

    Add `def log_fields(self) -> dict[str, str | None]: return {}` on the base — the `extra_headers()`
    of this family, in the same position in the class body. It exists so the handler stays generic and
    so the base can guarantee scalars: **every value an exception contributes to a log line is a plain
    scalar** (`str`, `UUID`, `BoundedReason`, `None`). Never a SQLModel/ORM instance. This is not a new
    rule — all eleven `__init__`s in `errors.py` already keep it; state it as conformance. The concrete
    failure it prevents: a rollback expires ORM attributes, the handler's attribute read then attempts
    I/O outside a greenlet, the handler itself raises, and the client gets 500 where 409 was owed.

    Establish the module ordering convention plans 02/03/04 will append to, and write it as a comment
    block at the top of the class section: base first, then subclasses grouped by arm in D-17 order —
    admission arms, creation arms, lookup arms, challenge arms. Plans 02–04 append within their group;
    no plan reorganises.

    This task writes the base plus the **creation arms** only:
    `IdentityAlreadyLinked` → `IDENTITY_ALREADY_LINKED`;
    `ProviderAccountAlreadyLinked` → `OPERATION_NOT_ALLOWED`;
    `AccountUnavailable` → `ACCOUNT_UNAVAILABLE`, taking a keyword-only `cause: str` in `__init__`,
    storing it, and returning `{"cause": self.cause}` from `log_fields()`. Its docstring states that it
    covers both the blocked-user and historical-identity arms and that the two are indistinguishable to
    clients on purpose — `cause` is the log-only distinction. Carry no `issuer` and no `subject` on any
    class: today's `Reject` carries them and nothing reads them, and an identity has never reached a
    rejection log line.

    **`src/nativespeaker/api/app/errors.py`.** Add module-private `_FIRST = re.compile(r"(.)([A-Z][a-z]+)")`
    and `_REST = re.compile(r"([a-z0-9])([A-Z])")` beside `_LOGGABLE`, and
    `def camel_to_snake(name: str) -> str` applying `_REST.sub` over `_FIRST.sub` then `.lower()` — the
    two-substitution form, because a single `\B([A-Z])` rule mishandles acronym boundaries. Docstring
    gives the two worked cases the family actually needs.

    Add `async def auth_rejected_handler(request: Request, exc: Exception) -> JSONResponse` modelled on
    `service_error_handler`: `assert isinstance(exc, AuthRejected)` to narrow, then one unconditional
    `logger.warning(camel_to_snake(type(exc).__name__), route=<path template>, **exc.log_fields())`,
    then `return error_response(exc.error_class)`. `error_response` is the only body builder in the
    codebase and nothing here changes it. Read the route as
    `getattr(request.scope.get("route"), "path", None)` — FastAPI's `APIRoute.matches` sets
    `scope["route"]` and Starlette builds the handler's `Request` from that same scope, but a request
    that matched no route has no key and the handler must not raise on it. `route` is the path
    template, never the caller's raw path. The challenge handle is never a field here and
    `log_fields()` is the only channel by which anything reaches this line.

    Register it in `register_exception_handlers` as the first line of the function. One registration
    covers every subclass because Starlette resolves handlers by walking `type(exc).__mro__`.
    Registration order does not matter and no comment should claim it does: dispatch is by MRO
    specificity, and Starlette routes the `Exception` key to `ServerErrorMiddleware` rather than into
    the same lookup table.

    **`tests/unit/test_exception_handlers.py`.** Extend the existing file — do NOT create
    `tests/unit/test_error_handlers.py`. Two files differing by one word and testing the same function
    is the regrowth this phase deletes. Add an `AuthRejected` `CASES` block reusing `_make_raise_route`
    and the `handler_client` fixture, keeping the `list(body.keys()) == ["code"]` assertion verbatim —
    that assertion is the anti-oracle check. Add a case that raises from a **router-level dependency**
    rather than a route body (copy the dependency-override client fixture shape from
    `tests/unit/test_create_user_precedence.py:188-199`). Add the log assertions using the suite's
    existing structlog capture idiom. Add the negative control and the Pitfall-1 regression described
    in `<behavior>`; for the regression, the route's session dependency must be a real async generator
    with `try: yield / await commit / except Exception: await rollback; raise`, mirroring
    `dependencies.py:38-45` — a plain callable has no `except` and would not exercise the property.

    **`tests/unit/test_rejection_vocabulary.py` (new).** Follow `test_auth_package_shape.py`'s
    literal-vs-measured idiom, and carry an equivalent module docstring saying why both sides are
    literals. Walk `AuthRejected.__subclasses__()` transitively; assert the derived event-name set
    (`camel_to_snake` of each class name) equals a hand-written literal `frozenset`, so a class rename
    is a visible edit rather than a silent log-vocabulary rename. Second assertion: for every **leaf**
    class (no subclasses of its own), `error_class` must be found in the class's own `__dict__` or in a
    strict ancestor other than `AuthRejected` — i.e. no leaf silently resolves to the fail-closed base
    default. `IdentityUnresolvable` (plan 02) is the one class that legitimately declares
    `INTERNAL_ERROR`; the rule above admits it because it declares rather than inherits. Report every
    defect at once via an accumulated `problems: list[str]` raised as one message, the way
    `errors.py:196-226` reports registry defects. Add the `TestTheMeasurementFires`-style control
    proving a synthetic non-declaring leaf is caught. Plans 02–04 each extend the literal set.

    **`tests/unit/test_auth_package_shape.py`.** Update `CURRENT` to the measured triple after this
    task, per the Task 1 outcome. Leave `BASELINE` alone unless option-a was chosen AND the class count
    already breaches it (it does not after this task — it will in plan 04).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_exception_handlers.py tests/unit/test_rejection_vocabulary.py tests/unit/test_auth_package_shape.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/nativespeaker/api/auth/exceptions.py` exists and contains `class AuthRejected(Exception):`, `error_class: ErrorClass = INTERNAL_ERROR`, and `def log_fields(`.
    - `grep -c 'ServiceError' src/nativespeaker/api/auth/exceptions.py` returns `0` — the family is not a `ServiceError` subtree.
    - `src/nativespeaker/api/app/errors.py` contains `def camel_to_snake(`, `async def auth_rejected_handler(`, and `app.add_exception_handler(AuthRejected, auth_rejected_handler)`.
    - `.venv/bin/python -c "from nativespeaker.api.app.errors import camel_to_snake as c; assert c('IdentityAlreadyLinked')=='identity_already_linked'; assert c('InvalidExternalJwt')=='invalid_external_jwt'; assert c('ChallengeNotFound')=='challenge_not_found'; print('ok')"` prints `ok`.
    - `tests/unit/test_error_handlers.py` does **not** exist (the near-homonym was not created): `test ! -e tests/unit/test_error_handlers.py`.
    - `tests/unit/test_rejection_vocabulary.py` exists and both suites above exit 0.
    - Behaviour: a route raising `IdentityAlreadyLinked` returns 409 with body whose keys are exactly `["code"]` and value `identity_already_linked`; a route raising a non-`AuthRejected` exception returns 500 `internal_error`.
    - Behaviour: a subclass raised through a real `get_db`-shaped async generator returns its own status, not 500.
    - No exception class stores a SQLModel instance: `grep -nE 'self\.(challenge|row|user|identity) *= *[a-z_]+$' src/nativespeaker/api/auth/exceptions.py` returns no match.
    - The default suite is green: `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>A rejection raised anywhere in the request path — route body or router-level dependency — travels through Starlette's MRO dispatch to one handler and comes out as the same status and body a client sees today, with one WARNING carrying the class-derived event name and `route`. The slice is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert the consuming transaction to raise, and collapse the router's result mapping</name>
  <files>
    src/nativespeaker/api/auth/creation.py,
    src/nativespeaker/api/routers/auth.py
  </files>
  <read_first>
    - src/nativespeaker/api/auth/creation.py (the whole file — the constraint-name constants `:26-32`, `CLIENT_CLASS_FOR_RESULT` `:34-40`, `create_account` `:43-75`, `classify_insert_conflict` `:86-93`, `_conflicting_constraint_name` `:96-101` — read the `__cause__` walk to *inline* it, not to keep it — `_result_for_existing` `:104-113`, `_insert_account` `:116-131`)
    - src/nativespeaker/api/routers/auth.py (the whole file — `_complete` `:90-158`, `_consuming_rejection` `:186-206`, `_completion_response` `:209-215`)
    - src/nativespeaker/api/auth/exceptions.py (written in Task 2)
    - .planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-RESEARCH.md (Pattern 3, Pitfall 3, Open Question 2)
  </read_first>
  <reversibility rating="costly">Placing the post-claim consume on the wrong side of the `create_account` boundary double-consumes: the second call returns `False` and logs at ERROR without changing the client response — a silent-ish defect. Reversible in code, expensive to notice.</reversibility>
  <behavior>
    - `create_account` returns the new user's `UUID` on success and raises on every other outcome; it
      has no non-raising rejection path left.
    - An already-linked active identity raises `IdentityAlreadyLinked`; a non-active identity row raises
      `AccountUnavailable(cause="historical_identity")`; a missing or inactive user raises
      `AccountUnavailable(cause="blocked_user")`.
    - An `IntegrityError` naming a constraint in the race set raises `IdentityAlreadyLinked`; one naming
      the provider-account index raises `ProviderAccountAlreadyLinked`; **any other constraint name
      re-raises the original `IntegrityError`** and becomes a 500.
    - In every conflict arm the savepoint is rolled back **before** anything else runs.
    - On success the challenge is consumed and the session committed inside `create_account`, exactly as today.
    - On a raising arm the challenge is consumed and the session committed by `_complete`'s except arm
      **before** the exception is re-raised — consumed exactly once, never twice.
    - `POST /auth/create-user` returns the same status and body for every one of these outcomes as it does today.
  </behavior>
  <action>
    **`auth/creation.py`.** Change `create_account`'s return annotation to `UUID` and its docstring to
    say it returns the new user's id or raises the rejection the transaction earned. This is total, not
    a narrowing: the existing-identity branch has no success arm (all three of its outcomes are
    rejections) and the insert branch either succeeds or conflicts.

    Delete the whole classification apparatus D-03 names: the result-to-class mapping dict, the
    conflict-classifier function, the constraint-name helper at `:96-101`, and both module-level
    constraint-name constants at `:26-32`. No named helper and no module constant replaces them — that
    is the letter of D-03, which lists them in its delete sentence and again in CONTEXT.md's Canonical
    References. (RESEARCH.md and PATTERNS.md both recommended sparing the helper; they are inputs, this
    is the contract.)

    **The behaviour those symbols carried survives, inline in the `except IntegrityError` arm.** After
    the rollback, walk the driver's cause chain for `constraint_name` right there — `cause = conflict.orig`,
    then `while cause is not None and not hasattr(cause, "constraint_name"): cause = cause.__cause__`,
    then read it with `getattr(..., None)`. Read the driver's attribute, never the rendered message —
    that was the point of the helper's docstring and the point does not move with the code. Then branch
    three ways on the name, with the two constraint names written as string literals in the arm:
    `"external_identities_issuer_subject_key"` or `"external_identities_user_id_key"` → raise
    `IdentityAlreadyLinked`; `"ix_external_identities_provider_account"` → raise
    `ProviderAccountAlreadyLinked`; **anything else → bare `raise` of the original `IntegrityError`,
    unchanged**. That third branch is a deliberate 500 tripwire, not an oversight — a new constraint
    violation is a bug and must surface as one, never as a benign 409. Carry a one-line comment saying
    so, in the 37.1 D-16 prose register.

    In `_insert_account`, keep `await savepoint.rollback()` as the **first** statement of the `except
    IntegrityError` arm, before any classification — until the rollback runs the session refuses every
    further statement. Then classify and raise. Its return annotation narrows to `UUID`, and
    `_flush_account`'s to `UUID` (drop the tuple and the enum member it carried).

    Rewrite `_result_for_existing` as a raising helper (name it for what it now does, e.g.
    `_reject_existing_identity`, annotated `-> NoReturn`): non-active identity state raises
    `AccountUnavailable(cause="historical_identity")`; a missing user or `user.active is not True`
    raises `AccountUnavailable(cause="blocked_user")`; otherwise raises `IdentityAlreadyLinked`. Keep
    the positive-test polarity (`!= IdentityState.active`, `is not True`) and its fail-closed comment
    verbatim — that polarity is a V4 access-control control, not a style choice.

    Preserve exactly, per D-05: the single `begin_nested()` savepoint around all three inserts, the
    pre-check via `resolve_existing_identity` (still "not the race arbiter, and never to be one"), and
    the blocked/historical indistinguishability to clients.

    The consume block at `:65-74` stays where it is but now serves **the success path only** — its
    comment currently reads "on success and rejection alike" and becomes factually wrong the moment the
    rejection arms raise past it. Rewrite it, do not carry it: say that the raising arms leave the
    consume to the route's except arm so the two paths spend the handle exactly once between them.
    Keep the `logger.error("challenge_consume_did_not_match", challenge_row_id=str(challenge.id))`
    guard byte-identical — the row id stringified, never the public handle.

    **`routers/auth.py`.** Delete `_completion_response`. Extract the consume-then-commit sequence out
    of `_consuming_rejection` into `async def _consume_and_commit(session, *, context, challenge, challenge_store) -> None`
    containing exactly today's `consume` → check-`False` → `logger.error(...)` → `await session.commit()`,
    and have `_consuming_rejection` call it so the lookup arms keep working unchanged until plan 03
    deletes them. Do not move or duplicate the `logger.error` guard.

    Wrap the `create_account` call in `try/except AuthRejected:` whose body is
    `await _consume_and_commit(...)` followed by a bare `raise`. Resolution of RESEARCH Open Question 2,
    recorded here so plan 03 does not re-decide: **`_complete` owns the post-claim consume for raising
    arms**, per D-04's literal wording, and `create_account` keeps it only for success. The bare `raise`
    is safe because after `session.commit()` the session holds no transaction, so `get_db`'s
    rollback-on-exception cannot un-consume the handle. Add a one-line comment saying exactly that —
    the client spent this handle and must not get it back. On success, return the `JSONResponse`
    carrying `CompletionResponse(identity_provider=provider)` directly.

    Drop the now-unused imports (`CLIENT_CLASS_FOR_RESULT`, and any `AuthEventResult`/`ErrorClass`
    import this task strands in either file). Leave the lookup arms, `_LOOKUP_REJECTIONS`,
    `_challenge_rejected`, and the pre-claim block untouched — plans 03 and 04 own them.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_conflict_classification.py tests/unit/test_create_user_precedence.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/nativespeaker/api/auth/creation.py` contains `-> UUID:` on `create_account` and no mapping dict: `grep -c 'CLIENT_CLASS_FOR_RESULT' src/nativespeaker/api/auth/creation.py src/nativespeaker/api/routers/auth.py` returns `0` for both files. <!-- planner-discipline-allow: CLIENT_CLASS_FOR_RESULT -->
    - The classification apparatus D-03 names is gone, helper and constants included: `grep -vE '^ *#' src/nativespeaker/api/auth/creation.py | grep -cE 'def (_conflicting_constraint_name|classify_insert_conflict)|^(RACE_CONSTRAINT_NAMES|PROVIDER_ACCOUNT_INDEX_NAME) ='` returns `0`. <!-- planner-discipline-allow: _conflicting_constraint_name --> <!-- planner-discipline-allow: classify_insert_conflict --> <!-- planner-discipline-allow: RACE_CONSTRAINT_NAMES --> <!-- planner-discipline-allow: PROVIDER_ACCOUNT_INDEX_NAME -->
    - The cause-chain walk moved inline rather than being dropped: `grep -c '__cause__' src/nativespeaker/api/auth/creation.py` returns `1`, and that line sits inside `_insert_account`'s `except IntegrityError` arm.
    - The 500 tripwire survives: within that arm, an unmapped constraint name re-raises the original error. `grep -nE 'raise (conflict|exc)$' src/nativespeaker/api/auth/creation.py` returns at least one match, and the behaviour itself is proven by `.venv/bin/python -m pytest tests/unit/test_conflict_classification.py -x -q` (Task 4 keeps the case asserting `pytest.raises(IntegrityError)` for an unmapped name).
    - Rollback-before-classify ordering holds: in `_insert_account`, `await savepoint.rollback()` is the first statement of the `except IntegrityError` arm — `awk '/except IntegrityError/,/^async def /' src/nativespeaker/api/auth/creation.py | grep -v '^ *#' | grep -m1 'await' | grep -q 'savepoint.rollback'`.
    - `src/nativespeaker/api/routers/auth.py` contains `async def _consume_and_commit(` and `except AuthRejected:`; `grep -c '_completion_response' src/nativespeaker/api/routers/auth.py` returns `0`. <!-- planner-discipline-allow: _completion_response -->
    - The consume guard is unduplicated: `grep -c 'challenge_consume_did_not_match' src/nativespeaker/api/routers/auth.py` returns `1`.
    - Behaviour: `POST /auth/create-user` for an already-linked active identity returns 409 `{"code": "identity_already_linked"}`; for a non-active identity row and for a blocked user it returns 403 `{"code": "account_unavailable"}` with byte-identical bodies.
    - Behaviour: a rejection after the claim leaves the challenge consumed and committed — a second presentation of the same handle is rejected, and `challenge_consume_did_not_match` is not logged (consumed exactly once).
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>The consuming transaction raises, the router's result-to-class mapping is gone, the 500 tripwire and the savepoint ordering are intact, and every create-user status and body is unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Rewire the tests that read the deleted outcome vocabulary</name>
  <files>
    tests/unit/test_conflict_classification.py,
    tests/unit/test_create_user_precedence.py,
    tests/schema/test_create_race.py,
    tests/schema/test_create_atomicity.py
  </files>
  <read_first>
    - tests/unit/test_conflict_classification.py (the whole file — the import block `:12-24`, `_DriverViolation` + `_integrity_error` `:33-46`, `_NoMutationSession` `:59-79`)
    - tests/unit/test_create_user_precedence.py (`_RejectionLog` `:95-110`, the `rejections` fixture `:157-162`, the `client` fixture `:188-199`, `_assert_challenge_required` `:233-236`)
    - tests/schema/test_create_race.py (`:203-204` — the `by_result` assertion)
    - tests/schema/test_create_atomicity.py (`:270`, `:336`, `:365`)
    - src/nativespeaker/api/auth/creation.py and src/nativespeaker/api/routers/auth.py (as rewritten in Task 3)
  </read_first>
  <action>
    **`tests/unit/test_conflict_classification.py`.** Its import block breaks — it imports the mapping
    dict, the classifier, and both constraint-name constants, all four of which Task 3 deleted. Declare
    the two constraint names as module-level string literals in the test file
    (`"external_identities_issuer_subject_key"`, `"external_identities_user_id_key"`,
    `"ix_external_identities_provider_account"`), matching the literals now inline in the production
    arm. The test is the only remaining place that names them alongside production, and that is the
    point: a rename on one side without the other fails this suite loudly rather than drifting.

    Reuse `_DriverViolation`
    and `_integrity_error` unchanged — they already build a realistic `IntegrityError`. Convert each
    case from "the classifier returns outcome X" to "the arm raises exception X", via
    `pytest.raises`. Keep the case that asserts an **unmapped** constraint name re-raises the original
    `IntegrityError` — that case is the 500 tripwire's only guard and must assert the original
    exception type, not a subclass of the rejection family. Reuse `_NoMutationSession` for any case
    asserting a rejection arm mutated nothing.

    **`tests/unit/test_create_user_precedence.py` — the spy migration, done once here for all four plans.**
    Zero edits to any status code or body literal; `_assert_challenge_required`'s byte-equality
    assertion is untouchable. What changes is where rejections are observed and what string identifies
    them. `_RejectionLog` currently spies only on the router's logger and reads
    `kwargs.get("result", kwargs.get("stage"))`; after Task 3 the transaction arm logs from the handler
    instead, and plans 03 and 04 move the remaining arms the same way. Monkeypatch **both**
    `nativespeaker.api.routers.auth.logger.warning` and `nativespeaker.api.app.errors.logger.warning`
    into the same recorder, and change `results` to yield the **event name** for handler-sourced entries
    while keeping the existing `result`/`stage` kwarg for router-sourced ones. Entries then migrate from
    one logger to the other as plans land, with the asserted string lists staying stable — the
    snake_cased challenge class names already equal today's asserted enum values.

    Two asserted strings genuinely change in this task, because two former results collapse into one
    class by design: `historical_identity` and `blocked_user` both become the event
    `account_unavailable`. Keep the distinction observable by asserting the `(event, cause)` pair for
    those two cases rather than the event alone — `cause` carries the former result name verbatim.
    Update the `_EVENTS` frozenset for the retired router event.

    **`tests/schema/test_create_race.py` and `tests/schema/test_create_atomicity.py`.** These are
    deselected by the default `addopts` and drive `create_account` directly, asserting its return value
    — neither appears in D-18's migration list, so treat this as discovered work. Replace the
    `by_result` bucketing with bucketing on the outcome the call produced: the returned `UUID` for the
    winner, and the raised exception's class name for each loser. The race assertion keeps its meaning
    — exactly one winner, the rest rejected as already-linked. In `test_create_atomicity.py`, the
    docstring at `:336` reasons from the enum's existence ("no member describes an attribution
    collision, so inventing one would tell a lie"); rewrite it to make the same point about the
    exception family rather than deleting the reasoning.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_conflict_classification.py tests/unit/test_create_user_precedence.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q -m schema tests/schema/test_create_race.py tests/schema/test_create_atomicity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest -q` exits 0 (default selection, ~762 tests).
    - `.venv/bin/python -m pytest -q -m schema` exits 0 (~115 tests).
    - `.venv/bin/python -m pytest -q -m e2e` exits 0 (~198 tests) with **zero edits** to `tests/e2e/test_admission.py`: `git diff --name-only -- tests/e2e/test_admission.py` is empty.
    - `tests/unit/test_create_user_precedence.py` has no status-code or body-literal edit: `git diff -U0 -- tests/unit/test_create_user_precedence.py | grep -E '^[+-]' | grep -Ev '^(\+\+\+|---)' | grep -cE '(409|403|401|503|500|"code")'` returns `0`.
    - `tests/unit/test_create_user_precedence.py` spies on the handler's logger: it contains `nativespeaker.api.app.errors.logger.warning`.
    - `tests/unit/test_conflict_classification.py` asserts the tripwire: it contains `pytest.raises(IntegrityError`.
  </acceptance_criteria>
  <done>All three test selections are green, the two zero-edit gates hold, and the precedence suite observes rejections at the handler with the distinction between the two account-unavailable arms preserved in `cause`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `POST /auth/create-user` | Untrusted body carrying a secret challenge handle and an external JWT |
| API process → structured log pipeline | Every rejection's only record; a secret reaching it is unrecoverable |
| API process → PostgreSQL | The consuming transaction; a mis-sequenced commit spends or refunds a handle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.3-01 | Information Disclosure | `app/errors.py::auth_rejected_handler` — a new logging site on the challenge path | high | mitigate | `log_fields()` is the only channel into the log line; the base returns `{}` and no subclass carries an `AuthChallenge` or a `challenge_id`. Task 2 acceptance greps for ORM assignment in `exceptions.py`; the existing `challenge_row_id=str(challenge.id)` guard is left byte-identical. |
| T-37.3-02 | Elevation of Privilege | `auth/exceptions.py::AuthRejected` — a subclass that declares no `error_class` | high | mitigate | The base default is `INTERNAL_ERROR`, so a forgotten declaration fails **closed** (500), never open. `tests/unit/test_rejection_vocabulary.py` asserts every leaf declares or inherits from a declaring base, with a control proving the assertion fires. |
| T-37.3-03 | Spoofing | `routers/auth.py::_complete` — a rejected attempt whose handle is not spent | high | mitigate | The except arm consumes and COMMITs before the bare `raise`; after commit the session holds no transaction so `get_db`'s rollback cannot un-consume. Pinned by the precedence suite's consuming-rejection cases and by Task 3's consumed-exactly-once acceptance criterion. |
| T-37.3-04 | Tampering | `auth/creation.py` — an unmapped constraint violation reported as a benign 409 | high | mitigate | The `except IntegrityError` arm keeps its three-way branch; the third branch re-raises the original error as a 500. Guarded by a dedicated case in `test_conflict_classification.py` asserting `pytest.raises(IntegrityError)`. |
| T-37.3-05 | Information Disclosure | The handler's `route` field — reflecting a caller-supplied path | medium | mitigate | `route` is read as the matched `APIRoute`'s `.path` template, never `request.url.path`; an unmatched request yields `None` rather than an echo. |
| T-37.3-06 | Information Disclosure | Two `AccountUnavailable` arms becoming distinguishable to clients | medium | mitigate | One class, one `error_class`, one body. The distinction lives only in the log's `cause` field. The e2e admission suite is a zero-edit gate on the statuses and bodies. |
| T-37.3-07 | Repudiation | Retiring three log event names breaks a deployed dashboard | low | accept | D-02 accepts the loss of a single greppable rejection event knowingly. Task 1 asks the developer to confirm no alert keys on them; the phase summary names the replacement vocabulary either way. |
| T-37.3-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs nothing. Every module it touches imports only the standard library and packages already pinned in `pyproject.toml` and present in `.venv`. RESEARCH's Package Legitimacy Audit records "not applicable — no external packages"; no install task exists in any plan. |
</threat_model>

<verification>
Run all three selections — the default `addopts` deselects e2e and schema, and two schema-marked files
are affected by this plan:

```
.venv/bin/python -m pytest -q            # expect ~762 passed
.venv/bin/python -m pytest -q -m e2e     # expect ~198 passed
.venv/bin/python -m pytest -q -m schema  # expect ~115 passed
```

Baseline before this plan: 762 / 198 / 115, all green. Total counts may shift as cases convert; the
gate is that all three selections exit 0 and no case is silently dropped.

Zero-edit gates: `git diff --name-only` must not list `tests/e2e/test_admission.py`, and
`tests/unit/test_create_user_precedence.py` must show no status-code or body-literal change.
</verification>

<success_criteria>
- `auth/exceptions.py` exists with `AuthRejected`, `log_fields()`, and the three creation-arm classes.
- One `add_exception_handler(AuthRejected, ...)` registration answers every subclass.
- The log event name is the snake_cased class name; `route` is the path template; no handle is logged.
- `create_account` raises; `CLIENT_CLASS_FOR_RESULT` and `_completion_response` are gone. <!-- planner-discipline-allow: CLIENT_CLASS_FOR_RESULT --> <!-- planner-discipline-allow: _completion_response -->
- The classifier, the constraint-name helper and both name constants are gone (D-03); the cause-chain walk lives inline in the `except IntegrityError` arm.
- The unmapped-constraint 500 tripwire and the rollback-before-classify ordering survive.
- A post-claim rejection consumes and commits exactly once before the client is answered.
- All three test selections green; both zero-edit gates hold.
- Task 1's chosen option and the log-pipeline answer are recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-01-SUMMARY.md` when done.
Record: the Task 1 decision and rationale, the log-pipeline answer, the resolution of RESEARCH Open
Question 2 (`_complete` owns the post-claim consume), and that WR-01 remains deferred because this
phase touches no line of `app/lifespan.py`.
</output>
