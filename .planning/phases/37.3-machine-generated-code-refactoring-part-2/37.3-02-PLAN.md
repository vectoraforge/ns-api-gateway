---
phase: 37.3-machine-generated-code-refactoring-part-2
plan: 02
type: execute
wave: 2
depends_on:
  - 37.3-01
files_modified:
  - src/nativespeaker/api/auth/exceptions.py
  - src/nativespeaker/api/auth/identity.py
  - src/nativespeaker/api/app/dependencies.py
  - src/nativespeaker/api/errors.py
  - tests/unit/test_identity_resolution.py
  - tests/unit/test_identity_accessors.py
  - tests/unit/test_jwt_security.py
  - tests/unit/test_error_registry.py
  - tests/unit/test_rejection_vocabulary.py
  - tests/unit/test_auth_package_shape.py
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "resolve_identity returns the resolved identity or raises; the Admit/Reject union no longer exists (D-06)."
    - "The admission matrix is unchanged: every (issuer, subject, state, active) combination produces the same status and body it does today, proven by an unedited e2e admission suite (D-06/D-18)."
    - "The absent-token and failed-verify arms raise InvalidExternalJwt carrying the BoundedReason, raised inline from get_request_context; wire.py and verification.py are untouched (D-07)."
    - "The handler is the only rejection logging site — no thin re-logging catch survives in dependencies.py (D-06)."
    - "get_quota_gate's unreachable pre-auth guard and the AuthenticationError class still exist and still fail closed (D-08)."
    - "ErrorClass carries no unread copy field and the registry is still total (D-13)."
  artifacts:
    - src/nativespeaker/api/auth/identity.py
    - src/nativespeaker/api/app/dependencies.py
  key_links:
    - "get_request_context raises inline -> Starlette MRO dispatch -> auth_rejected_handler (a dependency-raised exception, probed as reaching the handler with the route template)"
    - "resolve_identity raise -> get_request_context (no catch) -> handler"
---

<objective>
Convert identity resolution and the admission dependency to the exception family — D-17's directive 3.1.

Purpose: this is the small conversion that proves plan 01's handler under the admission tests before the
larger lookup work builds on it. It also carries the D-13 sweep of the two now-stranded symbols in
`errors.py`.

Output: `identity.py` raising instead of returning a union; `get_request_context` raising inline;
`_reject`, `get_preauth_identity`, `AuthRejectionError` and `ErrorClass.copy` gone.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-CONTEXT.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-RESEARCH.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-PATTERNS.md
@.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-01-SUMMARY.md
</context>

## Artifacts this phase produces

See `37.3-01-PLAN.md` § "Artifacts this phase produces" for the full list of symbols phase 37.3
creates. This plan adds these members of that list: `InvalidExternalJwt`,
`PreAuthIdentityNotAllowed`, `IdentityUnresolvable` (all in `src/nativespeaker/api/auth/exceptions.py`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve_identity raises the rejection it earned</name>
  <files>
    src/nativespeaker/api/auth/exceptions.py,
    src/nativespeaker/api/auth/identity.py,
    tests/unit/test_identity_resolution.py
  </files>
  <read_first>
    - src/nativespeaker/api/auth/identity.py (the whole file — the `Admit`/`Reject` dataclasses `:19-34`, the union `:34`, and the four outcomes `:47-63`)
    - src/nativespeaker/api/auth/exceptions.py (as written by plan 01 — the base, `log_fields()`, `AccountUnavailable`, and the module ordering convention comment)
    - src/nativespeaker/api/auth/context.py (`LinkedIdentity` / `PreAuthIdentity` at `:18-34` — the return types, and the register for value-type docstrings)
    - tests/unit/test_identity_resolution.py (the whole file)
    - .planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-RESEARCH.md (Code Example 3 and the gap note that follows it)
  </read_first>
  <behavior>
    - No matching row + `allow_preauth=True` returns `PreAuthIdentity(issuer, subject)`.
    - No matching row + `allow_preauth=False` raises `PreAuthIdentityNotAllowed` → 403 `preauth_identity_not_allowed`.
    - A row whose `user_id` resolves to nothing raises `IdentityUnresolvable` → 500 `internal_error`.
    - `identity_state != IdentityState.active` raises `AccountUnavailable(cause="historical_identity")` → 403 `account_unavailable`.
    - `user.active is not True` raises `AccountUnavailable(cause="blocked_user")` → 403 `account_unavailable`.
    - A live linked pair returns `LinkedIdentity(user, identity, issuer, subject)`.
    - The two account-unavailable arms are byte-identical to the client and differ only in the log's `cause`.
  </behavior>
  <action>
    **`auth/exceptions.py`.** Append to the *admission arms* group established by plan 01, in the order
    the convention comment declares:
    `PreAuthIdentityNotAllowed` → `PREAUTH_IDENTITY_NOT_ALLOWED`, no `__init__`;
    `IdentityUnresolvable` → `INTERNAL_ERROR`, declared explicitly, with a docstring saying a broken
    identity→user link is unresolvable state and this class is the fail-closed reading of it. Note the
    subtlety for the vocabulary test: `IdentityUnresolvable` is the one class that legitimately
    *declares* `INTERNAL_ERROR`, and it declares it rather than inheriting it. `AccountUnavailable`
    already exists from plan 01 and is reused unchanged.

    **`auth/identity.py`.** Delete `Admit`, `Reject`, and the `AdmissionDecision` union alias. Change
    `resolve_identity`'s return annotation to `LinkedIdentity | PreAuthIdentity` and its docstring to
    say it resolves a verified `(issuer, subject)` or raises the rejection it earned, still using a
    single query. The query itself, the outer-join comment at `:40`, and the module docstring's framing
    are untouched — only the four `return Admit(...)` / `return Reject(...)` statements change.

    Substitute one for one: the `row is None` + `allow_preauth` arm returns the `PreAuthIdentity`
    directly (drop the `Admit` wrapper, keep the `# only POST /auth/create-user passes True` comment);
    the `row is None` + not-allowed arm raises `PreAuthIdentityNotAllowed`; the broken-link arm raises
    `IdentityUnresolvable`; the two account arms raise `AccountUnavailable` with
    `cause="historical_identity"` and `cause="blocked_user"` respectively; the final arm returns the
    `LinkedIdentity` directly.

    **Preserve the positive-test polarity exactly** — `identity.identity_state != IdentityState.active`
    and `user.active is not True`, with the comment at `:58` saying why. That polarity is a V4
    access-control control: it fails closed on a NULL or on any future enum member. Rewriting either as
    a negative test is a security regression even though every current test still passes.

    Drop `ErrorClass` and `AuthEventResult` from the imports; add the three exception names.

    **`tests/unit/test_identity_resolution.py`.** Convert each case from asserting a returned `Reject`'s
    `error_class`/`result` to asserting the raised class via `pytest.raises`, and for the two
    account-unavailable cases additionally assert `exc.value.cause`. The admission *semantics* are
    unchanged — every case keeps testing the same (row, state, active, allow_preauth) input and the same
    outcome; only the shape of the outcome changes. Keep every case; do not consolidate the two
    account-unavailable cases now that they share a class.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_identity_resolution.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/nativespeaker/api/auth/identity.py` declares no admission-result dataclasses: `grep -cE '^(class Admit|class Reject)\b' src/nativespeaker/api/auth/identity.py` returns `0`. <!-- planner-discipline-allow: class Admit --> <!-- planner-discipline-allow: class Reject -->
    - `grep -c 'AdmissionDecision' src/nativespeaker/api/auth/identity.py` returns `0`. <!-- planner-discipline-allow: AdmissionDecision -->
    - `src/nativespeaker/api/auth/identity.py` contains `-> LinkedIdentity | PreAuthIdentity:`.
    - The fail-closed polarity survives: the file contains `!= IdentityState.active` and `is not True`.
    - `src/nativespeaker/api/auth/exceptions.py` contains `class PreAuthIdentityNotAllowed(AuthRejected):` and `class IdentityUnresolvable(AuthRejected):`.
    - Behaviour: a non-active identity row and a blocked user both produce `AccountUnavailable`, and their `cause` attributes are `historical_identity` and `blocked_user` respectively.
    - `.venv/bin/python -m pytest tests/unit/test_identity_resolution.py -x -q` exits 0.
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>`resolve_identity` returns an identity or raises; the union and its two dataclasses are gone; the fail-closed polarity and the four outcomes are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_request_context raises inline; the re-logging helper and the unused accessor die</name>
  <files>
    src/nativespeaker/api/auth/exceptions.py,
    src/nativespeaker/api/app/dependencies.py,
    tests/unit/test_identity_accessors.py,
    tests/unit/test_jwt_security.py
  </files>
  <read_first>
    - src/nativespeaker/api/app/dependencies.py (the whole file — `get_db` `:38-45`, `get_request_context` `:49-78`, `_reject` `:81-88`, `get_linked_identity` `:92-99`, `get_preauth_identity` `:102-109`, `get_quota_gate` `:139-149`)
    - src/nativespeaker/api/auth/wire.py (`BoundedReason` at `:8-18` — a `StrEnum`, so a plain scalar, and `extract_bearer`'s return shape)
    - src/nativespeaker/api/auth/exceptions.py (as extended by Task 1)
    - src/nativespeaker/api/app/errors.py (`auth_rejected_handler` from plan 01 — the one logging site, and what `log_fields()` feeds it)
    - tests/unit/test_identity_accessors.py (the whole file)
    - tests/unit/test_jwt_security.py (the rejection and log assertions; the admission matrix cases)
  </read_first>
  <behavior>
    - No bearer token raises `InvalidExternalJwt` carrying the `BoundedReason` `extract_bearer` returned → 401 `auth_required`.
    - A token that fails verification raises `InvalidExternalJwt` carrying the verifier's `BoundedReason` → 401 `auth_required`.
    - Both arms log one WARNING with event `invalid_external_jwt`, field `bounded_reason` set to the reason string, and `route` set to the path template.
    - A rejection raised inside `resolve_identity` propagates out of `get_request_context` untouched — no catch, no second log line.
    - `get_linked_identity` raises `PreAuthIdentityNotAllowed` for a pre-auth caller → 403, unchanged status and body.
    - `get_quota_gate` still raises `AuthenticationError` for a pre-auth identity (unreachable, fails closed anyway).
    - Exactly one log record per rejected request — the handler's. No request produces two.
  </behavior>
  <action>
    **`auth/exceptions.py`.** Append `InvalidExternalJwt` → `AUTH_REQUIRED` to the admission-arms group.
    **One class for both wire arms**, per the discretion recorded in plan 01: today both produce the same
    single internal result and the same 401, and `bounded_reason` is what distinguishes them. Its
    `__init__` takes a keyword-only `bounded_reason: BoundedReason | None`, stores it, and `log_fields()`
    returns `{"bounded_reason": None if self.bounded_reason is None else str(self.bounded_reason)}` —
    stringified exactly the way `_reject` stringifies it today, so the field's type in the log pipeline
    does not change. `BoundedReason` is a `StrEnum` and therefore a plain scalar; carrying it is
    consistent with the scalars-only rule.

    **`app/dependencies.py`.** Delete `_reject` entirely and inline a bare `raise` at each of its call
    sites. The two properties it carried must survive at the new sites: `route` is
    `request.scope["route"].path`, a path template and never the caller's raw path — that value is now
    read by the handler off its own `Request`, so `get_request_context` keeps computing `route` only for
    `RequestContext`, not for the raise. And the rejection's structured log line is still emitted — but
    **once, by the handler**. D-06 is explicit: no thin re-logging catch survives here. Remove the
    module's rejection logging and the `AuthRejectionError` import; `logger` stays only if another site
    in the file still uses it, otherwise remove it too.

    `_reject(AUTH_REQUIRED, ..., reason, route)` at the absent-token site becomes
    `raise InvalidExternalJwt(bounded_reason=reason)`; the same at the failed-verify site. Because
    `_reject` was annotated `NoReturn` and read as a bare statement, each substitution is one line for
    one line and the surrounding control flow does not change.

    The `resolve_identity` call loses its `isinstance(decision, Reject)` branch: the function now
    returns the identity or raises through. Keep the short-session `async with` block and its comment —
    `Depends(get_db)` would hold the session across the provider call, which is why this session is its
    own. Keep the `allow_preauth=True` comment.

    `get_linked_identity`'s `_reject(...)` becomes `raise PreAuthIdentityNotAllowed()`. Keep the
    "Declared, never called directly" comment above it — FastAPI's cache only sees solver-resolved
    dependencies, so the reason it exists is unchanged.

    Delete `get_preauth_identity` (D-13, zero declarers — Phase 40 can resurrect it from git).

    **D-08 — do not touch:** `get_quota_gate`'s unreachable pre-auth guard and the `AuthenticationError`
    class both survive as-is. They are fail-closed tripwires, not the returned-enum pattern this phase
    kills. `AuthenticationError` keeps at least one live raise site after `get_preauth_identity` goes.

    **`tests/unit/test_identity_accessors.py` and `tests/unit/test_jwt_security.py`.** Update the
    exception and log assertions only; admission semantics are unchanged. Cases asserting the old
    rejection type now assert the family member; cases asserting the `auth_rejected` event with a
    `result` field now assert the class-derived event name with the `bounded_reason` field. Delete only
    the cases whose subject is `get_preauth_identity` itself. Add a case asserting a rejected request
    produces **exactly one** rejection log record, so a re-logging catch cannot silently return.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_identity_accessors.py tests/unit/test_jwt_security.py tests/unit/test_wire_contract.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q -m e2e tests/e2e/test_admission.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^def _reject\(' src/nativespeaker/api/app/dependencies.py` returns `0`.
    - `grep -c 'get_preauth_identity' src/nativespeaker/api/app/dependencies.py` returns `0`. <!-- planner-discipline-allow: get_preauth_identity -->
    - The module no longer logs rejections: `grep -c 'auth_rejected' src/nativespeaker/api/app/dependencies.py` returns `0`. <!-- planner-discipline-allow: auth_rejected -->
    - D-08 survives: `grep -c 'AuthenticationError' src/nativespeaker/api/app/dependencies.py` returns at least `2` (import plus the `get_quota_gate` raise), and `grep -c 'class AuthenticationError' src/nativespeaker/api/errors.py` returns `1`.
    - D-07 untouched: `git diff --name-only -- src/nativespeaker/api/auth/wire.py src/nativespeaker/api/auth/verification.py` is empty.
    - `src/nativespeaker/api/auth/exceptions.py` contains `class InvalidExternalJwt(AuthRejected):` and its `log_fields` returns a `bounded_reason` key.
    - Behaviour: a request with no `Authorization` header returns 401 `{"code": "auth_required"}` and produces exactly one WARNING with event `invalid_external_jwt` and a `bounded_reason` field.
    - Behaviour: a request whose token fails verification returns the same 401 body, with its own `bounded_reason`.
    - `.venv/bin/python -m pytest -q -m e2e` exits 0 with **zero edits** to `tests/e2e/test_admission.py`: `git diff --name-only -- tests/e2e/test_admission.py` is empty.
    - `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>The admission dependency raises family members inline, logs nothing itself, and the admission matrix is proven unchanged by an unedited e2e suite.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the two symbols the conversion stranded in the error registry</name>
  <files>
    src/nativespeaker/api/errors.py,
    tests/unit/test_error_registry.py,
    tests/unit/test_rejection_vocabulary.py,
    tests/unit/test_auth_package_shape.py
  </files>
  <read_first>
    - src/nativespeaker/api/errors.py (the `ErrorClass` dataclass `:29-35`, the sixteen `register_class(ErrorClass(...))` calls `:65-178`, `assert_registry_total` `:196-226`, `ServiceError` `:229-236`, `AuthRejectionError` `:340-347`)
    - tests/unit/test_error_registry.py (`:83-86`, the synthetic `ErrorClass(...)` constructions at `:100`, `:107`, `:114`, and `:310-318`)
    - src/nativespeaker/api/app/lifespan.py (where `assert_registry_total` runs at boot)
    - tests/unit/test_rejection_vocabulary.py and tests/unit/test_auth_package_shape.py (the literals to extend/update)
  </read_first>
  <action>
    **D-13, resolved instances.** `ErrorClass.copy` is dead: `ErrorResponse` carries exactly one field
    (`code`) and says so in its docstring, `error_response` builds `ErrorResponse(code=cls.code)` and
    nothing else, and no read of the field exists anywhere in `src/`. Remove the field from the
    `ErrorClass` dataclass and the sixteen keyword arguments that populate it across the registration
    calls. The dataclass is `frozen=True, slots=True` — nothing else about it changes.

    `AuthRejectionError` lost its last raise site in Task 2. Delete the class. It is a `ServiceError`
    subclass, so `service_error_handler` loses one caller and nothing else; `ServiceError` itself, the
    registry, `error_response`, `register_class` and `STATUS_TO_CLASS` are untouched.

    **`assert_registry_total` needs no change** — verified during planning: its four checks cover
    `name`, `code`, `STATUS_TO_CLASS` and the `ErrorCode` `Literal`, and no arm reads the removed
    field. `STATUS_TO_CLASS` loses no entry. Do not edit it; the acceptance criteria gate that it still
    runs clean at boot.

    **`tests/unit/test_error_registry.py`.** Two cases read the removed field, at `:86` and `:312` —
    delete those cases. Three more construct synthetic `ErrorClass(...)` instances at `:100`, `:107`
    and `:114` to exercise duplicate-name, duplicate-code and never-registered paths — those cases stay;
    only their now-invalid keyword argument goes. Do not weaken any assertion while removing the
    argument.

    **`tests/unit/test_rejection_vocabulary.py`.** Extend the hand-written event-name literal set with
    the three classes Tasks 1 and 2 added, so the file keeps measuring the whole family.

    **`tests/unit/test_auth_package_shape.py`.** Update `CURRENT` to the triple measured after this
    plan, honouring plan 01 Task 1's chosen option. `BASELINE` is not breached yet.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_error_registry.py tests/unit/test_rejection_vocabulary.py tests/unit/test_auth_package_shape.py -x -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - The registry dataclass carries no unread copy field: `grep -c 'copy' src/nativespeaker/api/errors.py` returns `0`. <!-- planner-discipline-allow: copy -->
    - `grep -c 'AuthRejectionError' src/nativespeaker/api/errors.py src/nativespeaker/api/app/dependencies.py` returns `0` for both files. <!-- planner-discipline-allow: AuthRejectionError -->
    - The registry is still total and still runs at boot: `.venv/bin/python -c "from nativespeaker.api.errors import assert_registry_total; assert_registry_total(); print('total')"` prints `total`.
    - `STATUS_TO_CLASS` lost no entry: `.venv/bin/python -c "from nativespeaker.api.errors import STATUS_TO_CLASS, REGISTRY; print(len(STATUS_TO_CLASS), len(REGISTRY))"` prints the same two numbers as before this task (record them in the SUMMARY).
    - `tests/unit/test_error_registry.py` still exercises duplicate-name, duplicate-code and never-registered registration: it contains `is already registered`.
    - `.venv/bin/python -m pytest -q` exits 0, `.venv/bin/python -m pytest -q -m e2e` exits 0, `.venv/bin/python -m pytest -q -m schema` exits 0.
  </acceptance_criteria>
  <done>The two stranded symbols are gone, the registry is still total at boot, and all three test selections are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → any authenticated route | An untrusted `Authorization` header crosses here; the barrier is `get_request_context` |
| API process → structured log pipeline | A rejection's only record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37.3-08 | Elevation of Privilege | `auth/identity.py::resolve_identity` — rewriting a positive test as a negative one during the raise conversion | high | mitigate | The `!= IdentityState.active` / `is not True` polarity is carried verbatim with its comment; Task 1 acceptance greps for both literals; the e2e admission suite is a zero-edit gate on the resulting matrix. |
| T-37.3-09 | Elevation of Privilege | `app/dependencies.py::get_request_context` — an inlined raise that no longer fires on some arm | high | mitigate | `_reject` was `NoReturn` and read as a bare statement, so each substitution is one-line-for-one-line with no control-flow change. The e2e admission suite and `test_jwt_security.py` cover every arm. |
| T-37.3-10 | Repudiation | Double-logging or zero-logging a rejection after the logging site moves | medium | mitigate | The handler is the single logging site (D-06); the module's rejection logging is removed and gated by a grep; a new case asserts exactly one rejection record per rejected request. |
| T-37.3-11 | Information Disclosure | `InvalidExternalJwt` carrying token-derived material into a log line | medium | mitigate | It carries only `BoundedReason`, a `StrEnum` from a closed set, stringified exactly as today. No token, claim, issuer or subject is stored on any class in the family. |
| T-37.3-12 | Elevation of Privilege | Deleting a fail-closed guard while sweeping zero-consumer code | high | mitigate | D-08 names the two survivors explicitly; Task 2 acceptance asserts `get_quota_gate`'s guard and the `AuthenticationError` class both still exist. Only the accessor with zero declarers is removed. |
| T-37.3-13 | Tampering | Removing a registry field weakens `assert_registry_total` | low | mitigate | Verified during planning that no arm reads the removed field. Task 3 acceptance re-runs `assert_registry_total()` and pins the registry and status-map sizes. |
| T-37.3-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing; no package-manager step exists. |
</threat_model>

<verification>
```
.venv/bin/python -m pytest -q            # expect ~762 passed
.venv/bin/python -m pytest -q -m e2e     # expect ~198 passed
.venv/bin/python -m pytest -q -m schema  # expect ~115 passed
```

Zero-edit gate: `git diff --name-only -- tests/e2e/test_admission.py` must be empty for this plan's
whole diff. D-07 gate: `wire.py` and `verification.py` must not appear in this plan's diff either.
</verification>

<success_criteria>
- `resolve_identity` returns an identity or raises; `Admit`, `Reject` and the union are gone. <!-- planner-discipline-allow: class Admit --> <!-- planner-discipline-allow: class Reject --> <!-- planner-discipline-allow: AdmissionDecision -->
- The fail-closed polarity in both account arms is unchanged.
- `get_request_context` raises inline; the re-logging helper and the zero-declarer accessor are gone.
- `get_quota_gate`'s guard and `AuthenticationError` survive (D-08).
- `wire.py` and `verification.py` are untouched (D-07).
- The unread registry field and the stranded auth exception are gone; the registry is still total.
- All three test selections green; `tests/e2e/test_admission.py` unedited.
</success_criteria>

<output>
Create `.planning/phases/37.3-machine-generated-code-refactoring-part-2/37.3-02-SUMMARY.md` when done.
Record the registry and status-map sizes measured in Task 3, and the package-shape triple after this plan.
</output>
