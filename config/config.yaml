# The model and the resilience caps are NOT declared here: both vary per deployment -- a model
# switched during a provider incident, and caps sized for one staging replica or for production --
# and a value in this file outranks the environment. `config.py` holds their defaults, and
# MODEL_NAME, RESILIENCE_POOL_SIZE and their siblings are the levers.
# One connection per request at a time, and none held across the provider call, so this bounds
# short database work only. The relation to `resilience.pool_size` is this comment, not code:
# `resilience.pool_size * 2 + 2` at that field's default (Phase 41 D-16, which accepts that
# declaring it here forecloses DB_POOL_SIZE).
db:
  pool_size: 12
jwt:
  jwks_cache_ttl_seconds: 3600
# NO SECRET MATERIAL: this file is tracked in git, so it carries public reference data only. Every
# credential is an environment variable or a path to a file outside this repository -- see .env.example.
# A value declared here outranks the environment, but a variable naming a nested table can still ADD
# an entry to a map below. Add a key here only when it must NOT vary per deployment.
# An operator edits this map; a verified product id absent from it is a 500 and Apple's next retry succeeds.
app_store:
  products:
    com.nativespeaker.subscription.monthly: paid
# The same map for Google. An unmapped verified product id is a 500 and Pub/Sub redelivers.
google_play:
  products:
    nativespeaker.subscription.monthly: paid
chats_limit: 50
messages_limit: 50
